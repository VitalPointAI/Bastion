---
phase: 64-scenario-agnostic-refactor-and-baltic-shield-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/robot/calibration-service.ts
  - backend/src/robot/robot-mission-service.ts
  - backend/src/robot/swarm-cop-bridge.ts
  - backend/src/api/robot-routes.ts
  - backend/src/coordinates/mgrs-coordinator.ts
  - backend/src/robot/vehicle-database.ts
  - backend/src/robot/skills/symbology-skill.ts
  - backend/src/robot/vision-cop-pipeline.ts
  - backend/src/robot/skills/tactical-skills.ts
  - frontend/src/components/exercise/OrderEditor.tsx
  - frontend/src/components/exercise/ExerciseDashboard.tsx
  - frontend/src/components/exercise/IPBPanel.tsx
autonomous: true
requirements:
  - SA-64-01
  - SA-64-02
  - SA-64-03

must_haves:
  truths:
    - "All coordinate conversion code uses CalibrationService singleton, not duplicated local loaders"
    - "Team labels in UI show generic fallbacks or scenario-driven names, not hardcoded CJTF WestPAC / PRC/TCC"
    - "Vehicle identification uses single VehicleDatabase, not 3 separate hardcoded tables"
  artifacts:
    - path: "backend/src/robot/calibration-service.ts"
      provides: "CalibrationService singleton with roomToGeo(), getProfile()"
      exports: ["calibrationService", "CalibrationProfile"]
    - path: "backend/src/robot/vehicle-database.ts"
      provides: "Consolidated VehicleDatabase replacing KNOWN_VEHICLES, THREAT_CLASS_MAP, and tactical threatClasses"
      exports: ["vehicleDatabase", "VehicleEntry"]
  key_links:
    - from: "backend/src/robot/robot-mission-service.ts"
      to: "backend/src/robot/calibration-service.ts"
      via: "import calibrationService"
      pattern: "calibrationService\\.roomToGeo"
    - from: "backend/src/robot/swarm-cop-bridge.ts"
      to: "backend/src/robot/calibration-service.ts"
      via: "import calibrationService"
      pattern: "calibrationService\\.roomToGeo"
    - from: "backend/src/robot/skills/symbology-skill.ts"
      to: "backend/src/robot/vehicle-database.ts"
      via: "import vehicleDatabase"
      pattern: "vehicleDatabase\\."
---

<objective>
Create the three foundation services that all downstream refactoring depends on: CalibrationService (centralized coordinate conversion), VehicleDatabase (consolidated vehicle identification), and scenario-driven team labels (replacing hardcoded coalition names).

Purpose: Every subsequent plan needs these services in place. Without centralized calibration, mission config and theater context cannot be parameterized. Without VehicleDatabase, threat classification stays hardcoded. Without team label extraction, UI stays coupled to Pacific Strategy.

Output: Three new service files + all consumers updated to use them instead of local hardcoded values.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-scenario-agnostic-refactor-and-baltic-shield-demo/64-RESEARCH.md

@backend/data/calibration-profiles.json
@backend/src/robot/robot-mission-service.ts
@backend/src/robot/swarm-cop-bridge.ts
@backend/src/api/robot-routes.ts
@backend/src/coordinates/mgrs-coordinator.ts
@backend/src/robot/skills/symbology-skill.ts
@backend/src/robot/vision-cop-pipeline.ts
@backend/src/robot/skills/tactical-skills.ts
@frontend/src/components/exercise/OrderEditor.tsx
@frontend/src/components/exercise/ExerciseDashboard.tsx
@frontend/src/components/exercise/IPBPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CalibrationService singleton and migrate all coordinate consumers</name>
  <files>
    backend/src/robot/calibration-service.ts,
    backend/src/robot/robot-mission-service.ts,
    backend/src/robot/swarm-cop-bridge.ts,
    backend/src/api/robot-routes.ts,
    backend/src/coordinates/mgrs-coordinator.ts
  </files>
  <action>
Create `backend/src/robot/calibration-service.ts` as a singleton service that:
- Reads `backend/data/calibration-profiles.json` synchronously at first access (lazy init, not async)
- Exports `CalibrationProfile` interface: `{ room_width, room_height, map_bounds: { north, south, east, west }, label? }`
- Exports `calibrationService` singleton instance with methods: `loadProfiles()`, `getProfile(name='default')`, `roomToGeo(x, y, profileName='default')`
- Fallback if file missing: Latvia EFDL coordinates (north: 56.858, south: 56.840, east: 27.708, west: 27.688, room_width: 5, room_height: 15)
- Use `import.meta.url` + `fileURLToPath` for path resolution (matches existing ESM pattern in the codebase)
- The `roomToGeo` formula: `lat = south + (y / room_height) * (north - south)`, `lng = west + (x / room_width) * (east - west)`

Then update all 4 consumer files:
1. `robot-mission-service.ts`: Remove local `loadDefaultCalibration()` and local `roomToGeo()`. Import `calibrationService` from `./calibration-service`. Replace all calls.
2. `swarm-cop-bridge.ts`: Remove local `loadDefaultCalibration()` and local `roomToGeo()`. Import from `./calibration-service`. Replace all calls.
3. `robot-routes.ts`: Remove inline `loadProfiles()` call. Import `calibrationService`. Use `calibrationService.getProfile()` where profiles are served.
4. `coordinates/mgrs-coordinator.ts`: Remove hardcoded `CAL_SOUTH`, `CAL_NORTH`, `CAL_WEST`, `CAL_EAST` constants. Import `calibrationService` from `../robot/calibration-service`. Derive bounds from `calibrationService.getProfile()`.

Do NOT make CalibrationService async. Synchronous `readFileSync` at startup is the correct pattern here.
  </action>
  <verify>
    <automated>bash -lc 'cd /home/vitalpointai/projects/ssr/backend && npx tsc --noEmit && echo "BUILD OK"'</automated>
  </verify>
  <done>
    - `calibration-service.ts` exists with exported singleton
    - Zero `loadDefaultCalibration()` functions remain in robot-mission-service.ts or swarm-cop-bridge.ts
    - Zero `CAL_SOUTH/NORTH/WEST/EAST` constants remain in coordinates/mgrs-coordinator.ts
    - Zero inline `loadProfiles()` in robot-routes.ts
    - `tsc --noEmit` passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Create VehicleDatabase service and replace hardcoded team labels</name>
  <files>
    backend/src/robot/vehicle-database.ts,
    backend/src/robot/skills/symbology-skill.ts,
    backend/src/robot/vision-cop-pipeline.ts,
    backend/src/robot/skills/tactical-skills.ts,
    frontend/src/components/exercise/OrderEditor.tsx,
    frontend/src/components/exercise/ExerciseDashboard.tsx,
    frontend/src/components/exercise/IPBPanel.tsx
  </files>
  <action>
**Part A: VehicleDatabase (SA-64-03)**

Create `backend/src/robot/vehicle-database.ts`:
- Export `VehicleEntry` interface: `{ id: string, name: string, type: string, threat_class: string, mil2525_sidc?: string, description?: string }`
- Export `vehicleDatabase` singleton that consolidates the 3 separate tables:
  - `KNOWN_VEHICLES` from `symbology-skill.ts`
  - `THREAT_CLASS_MAP` from `vision-cop-pipeline.ts`
  - Inline `threatClasses` from `tactical-skills.ts`
- Methods: `getByClassification(classification: string): VehicleEntry | undefined`, `getAllVehicles(): VehicleEntry[]`, `getThreatClasses(): string[]`
- Default vehicles loaded from a static array (the current combined data). Later plans will wire this to problem set ORBAT data.

Update consumers:
1. `symbology-skill.ts`: Remove `KNOWN_VEHICLES` constant. Import `vehicleDatabase`. Use `vehicleDatabase.getByClassification()` where vehicle lookups happen.
2. `vision-cop-pipeline.ts`: Remove `THREAT_CLASS_MAP` constant. Import `vehicleDatabase`. Use `vehicleDatabase.getByClassification()`.
3. `tactical-skills.ts`: Remove inline `threatClasses` array. Import `vehicleDatabase`. Use `vehicleDatabase.getThreatClasses()`.

**Part B: Team Labels (SA-64-02)**

In the 3 frontend files (OrderEditor.tsx, ExerciseDashboard.tsx, IPBPanel.tsx), find all hardcoded strings "CJTF WestPAC" and "PRC/TCC" (and variants). Replace with generic defaults:
- `"Blue Force"` instead of `"Blue (CJTF WestPAC)"`
- `"Red Force"` instead of `"Red (PRC/TCC)"`

Use a pattern where the label is derived from a prop or context variable with a fallback: `const blueLabel = scenarioConfig?.blueTeamLabel ?? 'Blue Force'`. For now, just replace the hardcoded strings with the generic fallbacks. The scenarioConfig wiring will come when problem set metadata is integrated (later phase work or manual config).

Do NOT create a React context provider for team labels yet -- just replace hardcoded strings with parameterized fallbacks.
  </action>
  <verify>
    <automated>bash -lc 'cd /home/vitalpointai/projects/ssr && cd backend && npx tsc --noEmit && cd ../frontend && npx tsc --noEmit && echo "BOTH BUILD OK"'</automated>
  </verify>
  <done>
    - `vehicle-database.ts` exists with consolidated vehicle data
    - Zero `KNOWN_VEHICLES` in symbology-skill.ts
    - Zero `THREAT_CLASS_MAP` in vision-cop-pipeline.ts
    - Zero inline `threatClasses` arrays in tactical-skills.ts
    - Zero "CJTF WestPAC" or "PRC/TCC" strings in OrderEditor, ExerciseDashboard, IPBPanel
    - Both backend and frontend `tsc --noEmit` pass
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `bash -lc 'cd /home/vitalpointai/projects/ssr/backend && npx tsc --noEmit'` passes
2. `bash -lc 'cd /home/vitalpointai/projects/ssr/frontend && npx tsc --noEmit'` passes
3. `grep -rn "loadDefaultCalibration\|CAL_SOUTH\|CAL_NORTH\|KNOWN_VEHICLES\|THREAT_CLASS_MAP\|CJTF WestPAC\|PRC/TCC" backend/src/ frontend/src/ --include="*.ts" --include="*.tsx"` returns zero matches (excluding test files and the new service files)
</verification>

<success_criteria>
- CalibrationService singleton is the single source of truth for coordinate conversion
- VehicleDatabase is the single source of truth for vehicle identification
- No hardcoded Pacific Strategy team labels remain in UI components
- All TypeScript compilation passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/64-scenario-agnostic-refactor-and-baltic-shield-demo/64-01-SUMMARY.md`
</output>
