---
phase: 64-scenario-agnostic-refactor-and-baltic-shield-demo
plan: 04
type: execute
wave: 4
depends_on: ["64-03"]
files_modified:
  - backend/src/robot/mission-sequence-orchestrator.ts
  - backend/src/robot/skills/navigation-skill.ts
  - backend/src/robot/mission-simulator.ts
  - backend/src/robot/autonomous-mission-orchestrator.ts
  - backend/src/exercise/ipb-service.ts
  - backend/src/exercise/order-generator.ts
  - backend/src/api/robot-routes.ts
  - backend/src/design-interview/design-interview-service.ts
  - backend/src/doc-intelligence/interview/interview-service.ts
  - backend/src/doc-intelligence/interview/interview-prompts.ts
  - backend/src/graph/agents/jpp-planning-init-agent.ts
  - backend/src/graph/agents/conflict-detection-agent.ts
  - backend/src/graph/agents/entity-resolution-agent.ts
  - backend/src/graph/agents/osint-monitor-agent.ts
  - backend/src/graph/agents/raft-reasoning-agent.ts
  - backend/src/graph/agents/strategic-fusion-agent.ts
  - backend/src/graph/resolution/canonical-aliases.ts
  - backend/src/strategic/agents/strategy-reviewer.ts
  - backend/src/validation/fixture-generator.ts
  - backend/src/lib/geocoding-service.ts
  - frontend/src/components/cop/COPMapView.tsx
  - frontend/src/components/cop/MissionSequencePanel.tsx
  - frontend/src/components/cop/COPTab.tsx
  - frontend/src/components/plan/steps/StrategicAlignment.tsx
  - frontend/src/components/direct/CoalitionCaveatDashboard.tsx
  - frontend/src/components/admin/TeamDesignerPanel.tsx
  - frontend/src/components/agent-config/tabs/RoutinesTab.tsx
  - frontend/src/components/agent-config/tabs/IdentityTab.tsx
  - frontend/src/components/problem-set/CreateProblemSetWizard.tsx
  - frontend/src/components/tabs/CreateScenarioPanel.tsx
  - frontend/src/lib/mgrs-coordinator.ts
  - frontend/src/lib/governance-service.ts
autonomous: true
requirements:
  - SA-64-12

must_haves:
  truths:
    - "Zero grep hits for scenario-specific variable names (ZHONGZHENG_MAP, IRON_BASTION_DEFAULTS, startIronBastion, AO_CENTER with Taipei coords)"
    - "Zero grep hits for hardcoded scenario strings (Taipei, Zhongzheng, Iron Bastion, PRC/TCC, Pacific Strategy, INDOPACOM) in production code"
    - "All renamed variables compile cleanly"
    - "Comments reference generic concepts, not specific scenarios"
  artifacts:
    - path: "all files in files_modified"
      provides: "Clean scenario-agnostic codebase"
  key_links:
    - from: "grep sweep"
      to: "zero matches"
      via: "comprehensive search for all scenario-specific terms"
      pattern: "Taipei|Zhongzheng|Iron.Bastion|IRON_BASTION|PRC/TCC|INDOPACOM|Pacific Strategy"
---

<objective>
Final cleanup sweep: rename all remaining scenario-specific variable names, constant names, and comments across the entire codebase. Verify zero hardcoded scenario references remain in production code.

Purpose: Plans 01-03 handled the functional refactoring (services, orchestrators, display). This plan catches every remaining string, comment, variable name, and constant that still references the Pacific Strategy/Taiwan/Iron Bastion scenario. This is the "polish and verify" pass.

Output: A fully scenario-agnostic codebase where grep for any scenario-specific term returns zero matches in production code.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/64-scenario-agnostic-refactor-and-baltic-shield-demo/64-RESEARCH.md
@.planning/phases/64-scenario-agnostic-refactor-and-baltic-shield-demo/64-01-SUMMARY.md
@.planning/phases/64-scenario-agnostic-refactor-and-baltic-shield-demo/64-02-SUMMARY.md
@.planning/phases/64-scenario-agnostic-refactor-and-baltic-shield-demo/64-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename all scenario-specific variables, constants, and comments in backend</name>
  <files>
    backend/src/robot/mission-sequence-orchestrator.ts,
    backend/src/robot/skills/navigation-skill.ts,
    backend/src/robot/mission-simulator.ts,
    backend/src/robot/autonomous-mission-orchestrator.ts,
    backend/src/exercise/ipb-service.ts,
    backend/src/exercise/order-generator.ts,
    backend/src/api/robot-routes.ts,
    backend/src/design-interview/design-interview-service.ts,
    backend/src/doc-intelligence/interview/interview-service.ts,
    backend/src/doc-intelligence/interview/interview-prompts.ts,
    backend/src/graph/agents/jpp-planning-init-agent.ts,
    backend/src/graph/agents/conflict-detection-agent.ts,
    backend/src/graph/agents/entity-resolution-agent.ts,
    backend/src/graph/agents/osint-monitor-agent.ts,
    backend/src/graph/agents/raft-reasoning-agent.ts,
    backend/src/graph/agents/strategic-fusion-agent.ts,
    backend/src/graph/resolution/canonical-aliases.ts,
    backend/src/strategic/agents/strategy-reviewer.ts,
    backend/src/validation/fixture-generator.ts,
    backend/src/lib/geocoding-service.ts
  </files>
  <action>
Run this grep to find every remaining occurrence:
```
grep -rn "Taipei\|Zhongzheng\|Iron.Bastion\|IRON_BASTION\|ZHONGZHENG\|PRC/TCC\|INDOPACOM\|Pacific Strategy\|startIronBastion\|ironBastion\|Taiwan Strait\|Western Pacific\|South China Sea\|PLA \|PLA_\|Indo-Pacific\|CJTF WestPAC\|25\.042\|25\.054\|121\.512\|121\.518" backend/src/ --include="*.ts" -l
```

For EACH file with matches, apply these rename rules:

**Variable/constant renames:**
- `ZHONGZHENG_MAP` -> `DEFAULT_AREA_MAP` (should be done in Plan 02, verify)
- `IRON_BASTION_DEFAULTS` -> `DEFAULT_MISSION_CONFIG` (should be done in Plan 02, verify)
- `startIronBastion` -> `startMissionSequence` (should be done in Plan 02, verify)
- `ironBastion` in any camelCase variable -> `missionSequence` or `activeMission`
- `AO_CENTER` with Taipei coords -> derive from calibration or use generic name
- `DEFAULT_CENTER` with Taipei coords -> derive from calibration

**String/comment replacements (in LLM prompts, comments, descriptions):**
- "Taipei" -> "the operational area" or remove if in a comment
- "Zhongzheng" -> "the engagement zone" or remove
- "Taiwan Strait" -> "the strait" or "the maritime approaches" or remove
- "Western Pacific" -> "the theater" or remove
- "Indo-Pacific" -> "the theater" or remove
- "INDOPACOM" -> "the combatant command" or "theater HQ" or remove
- "Pacific Strategy AY26" -> "the active exercise" or remove
- "PLA" (as a force reference) -> "adversary forces" or "OPFOR"
- "PRC/TCC" -> "Red Force" or "OPFOR"
- "CJTF WestPAC" -> "Blue Force" or "coalition headquarters"
- "Iron Bastion" -> "the mission sequence" or "the operation"
- Taipei coordinate constants (25.042, 25.054, 121.512, 121.518) -> remove or replace with calibration calls

**For agent prompt strings** (in graph/agents/*.ts, strategic/agents/*.ts, doc-intelligence/interview/*.ts):
- These files contain LLM system prompts with scenario context. Replace specific scenario references with generic framing. The prompts should work for ANY scenario, not just Pacific Strategy.
- Keep the doctrinal/military analytical framing -- just remove the specific geographic/political references.

**For canonical-aliases.ts:**
- This may contain entity name mappings specific to Taiwan/PRC actors. Make these data-driven or move to a config file, OR keep them if they are part of the graph resolution logic that handles historical data.

**For fixture-generator.ts:**
- Test fixture data referencing specific scenarios. Update to generic equivalents.

**IMPORTANT:** Do NOT modify test files (*.test.ts, *.spec.ts) in this task -- they may legitimately reference scenario data for historical test cases. Do NOT modify files in `scenario/` directory, `backend/data/demo-baltic-seed/`, or `scripts/demo-data/` -- those ARE scenario data, not production code.
  </action>
  <verify>
    <automated>bash -lc 'cd /home/vitalpointai/projects/ssr && npx --prefix backend tsc --noEmit && grep -rn "Taipei\|Zhongzheng\|Iron.Bastion\|IRON_BASTION\|ZHONGZHENG\|PRC/TCC\|INDOPACOM\|Pacific Strategy" backend/src/ --include="*.ts" | grep -v "test\.\|spec\.\|\.test\." | head -20; echo "SWEEP DONE"'</automated>
  </verify>
  <done>
    - `grep -rn "Taipei|Zhongzheng|Iron.Bastion|IRON_BASTION|ZHONGZHENG|PRC/TCC|INDOPACOM|Pacific Strategy" backend/src/ --include="*.ts"` returns zero matches in non-test files
    - All variable renames compile: `tsc --noEmit` passes
    - Agent LLM prompts use generic theater references
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename all scenario-specific references in frontend and run final E2E verification sweep</name>
  <files>
    frontend/src/components/cop/COPMapView.tsx,
    frontend/src/components/cop/MissionSequencePanel.tsx,
    frontend/src/components/cop/COPTab.tsx,
    frontend/src/components/plan/steps/StrategicAlignment.tsx,
    frontend/src/components/direct/CoalitionCaveatDashboard.tsx,
    frontend/src/components/admin/TeamDesignerPanel.tsx,
    frontend/src/components/agent-config/tabs/RoutinesTab.tsx,
    frontend/src/components/agent-config/tabs/IdentityTab.tsx,
    frontend/src/components/problem-set/CreateProblemSetWizard.tsx,
    frontend/src/components/tabs/CreateScenarioPanel.tsx,
    frontend/src/lib/mgrs-coordinator.ts,
    frontend/src/lib/governance-service.ts
  </files>
  <action>
Run this grep to find every remaining frontend occurrence:
```
grep -rn "Taipei\|Zhongzheng\|Iron.Bastion\|IRON_BASTION\|ZHONGZHENG\|PRC/TCC\|INDOPACOM\|Pacific Strategy\|ironBastion\|Taiwan\|Western Pacific\|Indo-Pacific\|CJTF WestPAC\|25\.042\|121\.51" frontend/src/ --include="*.ts" --include="*.tsx" -l
```

For EACH file:

**COPMapView.tsx:** Replace any hardcoded map center coordinates (Taipei lat/lng) with values from the frontend calibration state (mgrs-coordinator's current AO calibration). Replace "Taipei" or scenario-specific labels in UI strings.

**MissionSequencePanel.tsx:** Replace "Iron Bastion" display labels with "Mission Sequence" or the active operation name. Replace any `startIronBastion` API calls with the new `/mission/start` or `/scenarios/mission-sequence` endpoint.

**COPTab.tsx:** Replace hardcoded center coordinates and any Taiwan/Pacific references.

**StrategicAlignment.tsx:** Replace any Pacific Strategy / Indo-Pacific references in display strings or strategic context descriptions.

**CoalitionCaveatDashboard.tsx:** Replace nation-specific labels if hardcoded to TW/AU/US. Use generic coalition member references.

**TeamDesignerPanel.tsx:** Replace any scenario-specific team templates.

**RoutinesTab.tsx, IdentityTab.tsx:** Replace scenario references in agent configuration display strings.

**CreateProblemSetWizard.tsx, CreateScenarioPanel.tsx:** Replace Pacific Strategy as example/default text with generic examples.

**mgrs-coordinator.ts (frontend):** Remove any hardcoded Taipei coordinates. Ensure all defaults come from `setAOCalibration()` state.

**governance-service.ts:** Replace any scenario-specific governance references.

**After all renames, run the comprehensive sweep:**
```
grep -rn "Taipei\|Zhongzheng\|Iron.Bastion\|IRON_BASTION\|ZHONGZHENG\|PRC/TCC\|INDOPACOM\|Pacific Strategy\|25\.042\|25\.054\|121\.512\|121\.518" backend/src/ frontend/src/ --include="*.ts" --include="*.tsx" | grep -v "test\.\|spec\.\|\.test\.\|calibration-profiles"
```
This MUST return zero lines. If any remain, fix them.
  </action>
  <verify>
    <automated>bash -lc 'cd /home/vitalpointai/projects/ssr && npx --prefix frontend tsc --noEmit && npx --prefix backend tsc --noEmit && HITS=$(grep -rn "Taipei\|Zhongzheng\|Iron.Bastion\|IRON_BASTION\|ZHONGZHENG\|PRC/TCC\|INDOPACOM\|Pacific Strategy" backend/src/ frontend/src/ --include="*.ts" --include="*.tsx" | grep -v "test\.\|spec\.\|\.test\.\|calibration-profiles" | wc -l) && echo "Remaining scenario refs: $HITS" && test "$HITS" -eq 0 && echo "CLEAN SWEEP"'</automated>
  </verify>
  <done>
    - Full grep sweep returns zero scenario-specific references in production TS/TSX files
    - Both `tsc --noEmit` pass (backend and frontend)
    - All UI labels use generic or scenario-driven names
    - Variable names are all scenario-agnostic
    - The codebase is ready for any scenario, not just Pacific Strategy or Baltic Shield
  </done>
</task>

</tasks>

<verification>
Final comprehensive verification:
1. `bash -lc 'cd /home/vitalpointai/projects/ssr/backend && npx tsc --noEmit && cd ../frontend && npx tsc --noEmit'` -- both compile
2. `grep -rn "Taipei\|Zhongzheng\|Iron.Bastion\|IRON_BASTION\|ZHONGZHENG\|PRC/TCC\|INDOPACOM\|Pacific Strategy\|25\.042\|25\.054\|121\.512\|121\.518" backend/src/ frontend/src/ --include="*.ts" --include="*.tsx" | grep -v "test\.\|spec\.\|\.test\.\|calibration-profiles"` -- zero matches
3. `grep -rn "startMissionSequence\|calibrationService\|vehicleDatabase\|DEFAULT_AREA_MAP\|DEFAULT_MISSION_CONFIG" backend/src/ --include="*.ts" | wc -l` -- confirms new generic names are in use (expect 10+ hits)
</verification>

<success_criteria>
- Zero hardcoded scenario references in production TypeScript/TSX code
- All variable names are scenario-agnostic
- All comments reference generic concepts
- Both backend and frontend compile cleanly
- The application is functionally identical but fully parameterized by calibration profile and problem set data
</success_criteria>

<output>
After completion, create `.planning/phases/64-scenario-agnostic-refactor-and-baltic-shield-demo/64-04-SUMMARY.md`
</output>
