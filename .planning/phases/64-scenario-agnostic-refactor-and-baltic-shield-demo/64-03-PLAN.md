---
phase: 64-scenario-agnostic-refactor-and-baltic-shield-demo
plan: 03
type: execute
wave: 3
depends_on: ["64-02"]
files_modified:
  - backend/src/exercise/ipb-service.ts
  - frontend/src/components/exercise/ExerciseDashboard.tsx
  - backend/src/api/robot-routes.ts
  - frontend/src/components/cop/COPGateNotifications.tsx
  - frontend/src/components/cop/COPTab.tsx
  - scripts/seed-strategic-cop.ts
autonomous: true
requirements:
  - SA-64-06
  - SA-64-07
  - SA-64-09
  - SA-64-10

must_haves:
  truths:
    - "IPB service theater context reads from parameterized config, not hardcoded Western Pacific values"
    - "Exercise phases display from scenario data, not hardcoded Pacific Strategy 6-phase array"
    - "Strategic force disposition loads from seed data, not hardcoded PLA/Taiwan positions"
    - "Engagement zoom targets derive from live COP threat data, not hardcoded room coordinates"
  artifacts:
    - path: "backend/src/exercise/ipb-service.ts"
      provides: "Parameterized THEATER_DEFAULTS and LLM prompt context"
    - path: "frontend/src/components/exercise/ExerciseDashboard.tsx"
      provides: "Dynamic exercise phase loading"
    - path: "frontend/src/components/cop/COPGateNotifications.tsx"
      provides: "Live-data-driven engagement zoom"
  key_links:
    - from: "backend/src/exercise/ipb-service.ts"
      to: "backend/src/robot/calibration-service.ts"
      via: "calibrationService.getProfile() for theater center coordinates"
      pattern: "calibrationService"
    - from: "frontend/src/components/cop/COPGateNotifications.tsx"
      to: "COP threat position data"
      via: "props or context for active threat coordinates"
      pattern: "threatPosition|activeTarget"
---

<objective>
Parameterize theater context, exercise phases, strategic force disposition, and engagement zoom. These are the display/context layer items that reference Pacific Strategy scenario data in IPB prompts, exercise dashboards, COP seed data, and notification zoom targets.

Purpose: After Plans 01-02 parameterized the mission execution core, this plan handles the intelligence/exercise/display layer that still has hardcoded Indo-Pacific references.

Output: IPB service, exercise dashboard, strategic COP seed, and gate notifications all scenario-agnostic.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/64-scenario-agnostic-refactor-and-baltic-shield-demo/64-RESEARCH.md
@.planning/phases/64-scenario-agnostic-refactor-and-baltic-shield-demo/64-01-SUMMARY.md
@.planning/phases/64-scenario-agnostic-refactor-and-baltic-shield-demo/64-02-SUMMARY.md

@backend/src/exercise/ipb-service.ts
@frontend/src/components/exercise/ExerciseDashboard.tsx
@backend/src/api/robot-routes.ts
@frontend/src/components/cop/COPGateNotifications.tsx
@frontend/src/components/cop/COPTab.tsx
@scripts/seed-strategic-cop.ts
@backend/src/robot/calibration-service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize IPB theater context and exercise phases (SA-64-06, SA-64-07)</name>
  <files>
    backend/src/exercise/ipb-service.ts,
    frontend/src/components/exercise/ExerciseDashboard.tsx
  </files>
  <action>
**ipb-service.ts (SA-64-06):**
1. Find `THEATER_DEFAULTS` object with hardcoded Taiwan/Indo-Pacific coordinates (center lat/lng, operational area, adversary staging). Replace with values derived from `calibrationService.getProfile()`:
   - `center`: Use calibration profile map_bounds center point `{ lat: (north+south)/2, lng: (east+west)/2 }`
   - `operationalArea`: Same as center (it's the AO)
   - `adversaryStaging`: Offset from map bounds (e.g., north+0.25, east+0.5 for "eastern approach" generic)
   - Import `calibrationService` from `../robot/calibration-service`
2. Find all LLM prompt strings that reference "Western Pacific", "Taiwan Strait", "Indo-Pacific", "South China Sea", "INDOPACOM", "PLA", "PRC" in prompt templates. Replace with generic placeholders that can be filled from scenario context:
   - Replace literal theater geography with `the operational theater` or `the area of operations`
   - Replace specific adversary references in prompts with `adversary forces` or `opposing force`
   - If there is a `buildFallbackExtraction()` or similar function that returns hardcoded Taiwan-specific IPB data, replace the fallback data with generic/Baltic-appropriate equivalents (Latvian geography, Russian threat axis)
3. Find `THEATER_DEFAULTS` usage sites within ipb-service.ts. All should now read from the parameterized version.
4. Do NOT change the IPB analysis logic itself -- only the input data and prompt strings.

**ExerciseDashboard.tsx (SA-64-07):**
1. Find `DEFAULT_EXERCISE_PHASES` array (hardcoded 6 phases: Competition, Crisis, Conflict Day 4/10/22, Negotiation or similar Pacific Strategy phases).
2. Replace with a generic default array that is scenario-agnostic. Use doctrinal phase names: `['Phase 0: Shape', 'Phase 1: Deter', 'Phase 2: Seize Initiative', 'Phase 3: Dominate', 'Phase 4: Stabilize', 'Phase 5: Enable Civil Authority']` -- OR keep the existing phases but remove Pacific Strategy-specific naming (e.g., remove "AY26" or date references).
3. Add a comment indicating these defaults should eventually load from the active problem set's phase definitions.
4. Replace any "Pacific Strategy" display strings with generic equivalents ("Active Exercise" or similar).
  </action>
  <verify>
    <automated>bash -lc 'cd /home/vitalpointai/projects/ssr && cd backend && npx tsc --noEmit && cd ../frontend && npx tsc --noEmit && echo "BOTH BUILD OK"'</automated>
  </verify>
  <done>
    - Zero "Taiwan Strait", "Western Pacific", "Indo-Pacific", "INDOPACOM" strings in ipb-service.ts prompt templates
    - THEATER_DEFAULTS derives coordinates from calibrationService
    - DEFAULT_EXERCISE_PHASES uses generic phase names, no Pacific Strategy references
    - Both tsc --noEmit pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Parameterize force disposition seed and engagement zoom (SA-64-09, SA-64-10)</name>
  <files>
    backend/src/api/robot-routes.ts,
    scripts/seed-strategic-cop.ts,
    frontend/src/components/cop/COPGateNotifications.tsx,
    frontend/src/components/cop/COPTab.tsx
  </files>
  <action>
**Strategic force disposition (SA-64-09):**
1. In `robot-routes.ts`, find the `/scenarios/seed-strategic-cop` handler (or wherever the 18 hardcoded force symbols for PLA/Taiwan are defined). These are likely mil-2525 symbol objects with hardcoded lat/lng positions and unit names like "PLA Marine Brigade Taipei".
2. Replace the hardcoded force array with a data-driven approach:
   - Create a generic `DEFAULT_FORCE_DISPOSITION` array with Baltic-appropriate forces using `calibrationService.roomToGeo()` for positions. Use generic NATO force names: "1st Mechanized Company (LVA)", "Reconnaissance Platoon (CAN)", "Artillery Battery (US)" etc.
   - OR load from a JSON file (e.g., `backend/data/demo-baltic-seed/coalition-forces.json` already exists). If the JSON structure is compatible, read from there. If not, create the default inline but using calibration-derived coordinates.
3. In `scripts/seed-strategic-cop.ts`, apply the same treatment: replace hardcoded Pacific force data with Baltic equivalents or calibration-derived positions.
4. Remove all "PLA", "Taiwan", "Taipei" references from force unit names and descriptions.

**Engagement zoom (SA-64-10):**
1. In `COPGateNotifications.tsx`, find the hardcoded `roomToLatLng(2.5, 3.5)` or similar call that determines where the map zooms when an engagement gate notification fires.
2. Replace with a dynamic approach: the zoom target should come from the notification's associated threat position data (if available in the notification payload) or fall back to the calibration profile center point.
3. If the notification payload already carries lat/lng for the threat, use that directly. If it only carries room coordinates, use a `roomToGeo` conversion from the frontend's mgrs-coordinator (which Plan 01 connected to CalibrationService).
4. In `COPTab.tsx`, check for any hardcoded map center coordinates. Replace with calibration-derived center: `{ lat: (north+south)/2, lng: (east+west)/2 }` from the frontend calibration state.
  </action>
  <verify>
    <automated>bash -lc 'cd /home/vitalpointai/projects/ssr && cd backend && npx tsc --noEmit && cd ../frontend && npx tsc --noEmit && ! grep -q "PLA Marine" backend/src/api/robot-routes.ts && ! grep -q "roomToLatLng(2.5" frontend/src/components/cop/COPGateNotifications.tsx && echo "DONE"'</automated>
  </verify>
  <done>
    - Zero hardcoded PLA/Taiwan force symbols in robot-routes.ts seed handler
    - Zero hardcoded room coordinates in COPGateNotifications zoom target
    - Force disposition uses calibration-derived or Baltic seed data positions
    - Engagement zoom derives from notification threat position or calibration center
    - Both tsc --noEmit pass
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `bash -lc 'cd /home/vitalpointai/projects/ssr/backend && npx tsc --noEmit && cd ../frontend && npx tsc --noEmit'` passes
2. `grep -rn "Taiwan Strait\|Western Pacific\|Indo-Pacific\|PLA Marine\|INDOPACOM" backend/src/exercise/ frontend/src/ --include="*.ts" --include="*.tsx"` returns zero matches in modified files
3. `grep -rn "roomToLatLng(2.5, 3.5)" frontend/src/` returns zero matches
</verification>

<success_criteria>
- IPB theater context is parameterized via CalibrationService
- Exercise phases are generic, not Pacific Strategy-specific
- Force disposition seed uses Baltic/calibration data
- Engagement zoom is data-driven, not hardcoded coordinates
</success_criteria>

<output>
After completion, create `.planning/phases/64-scenario-agnostic-refactor-and-baltic-shield-demo/64-03-SUMMARY.md`
</output>
