---
phase: 64-scenario-agnostic-refactor-and-baltic-shield-demo
plan: 02
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - backend/src/robot/mission-sequence-orchestrator.ts
  - backend/src/robot/skills/navigation-skill.ts
  - backend/src/robot/mission-simulator.ts
  - backend/src/robot/autonomous-mission-orchestrator.ts
  - backend/src/robot/tactical-ai-service.ts
  - backend/src/robot/skills/tactical-skills.ts
autonomous: true
requirements:
  - SA-64-04
  - SA-64-05
  - SA-64-08
  - SA-64-11

must_haves:
  truths:
    - "Mission sequence starts via generic startMissionSequence(), not startIronBastion()"
    - "Navigation skill uses scenario-loaded area map, not hardcoded ZHONGZHENG_MAP"
    - "Recon sweep waypoints and enemy spawn positions derive from calibration profile, not Taipei coordinates"
    - "Threat vectors and engagement axes are parameterized, not hardcoded to specific streets"
  artifacts:
    - path: "backend/src/robot/mission-sequence-orchestrator.ts"
      provides: "startMissionSequence() with DEFAULT_MISSION_CONFIG derived from calibration"
      contains: "startMissionSequence"
    - path: "backend/src/robot/skills/navigation-skill.ts"
      provides: "Scenario area map loading via setActiveMap()"
      contains: "setActiveMap"
    - path: "backend/src/robot/mission-simulator.ts"
      provides: "Parameterized recon sweep and enemy spawn positions"
  key_links:
    - from: "backend/src/robot/mission-sequence-orchestrator.ts"
      to: "backend/src/robot/calibration-service.ts"
      via: "calibrationService.roomToGeo for position derivation"
      pattern: "calibrationService"
    - from: "backend/src/robot/mission-simulator.ts"
      to: "backend/src/robot/calibration-service.ts"
      via: "calibrationService for waypoint generation"
      pattern: "calibrationService"
---

<objective>
Generalize the mission orchestration layer: rename Iron Bastion references to generic mission config, replace ZHONGZHENG_MAP with scenario-loaded area map, parameterize threat vectors, and make simulation events derive from calibration profile instead of hardcoded Taipei coordinates.

Purpose: This is the core of preserving the autonomous mission sequence (HOLD through ENGAGE) while making it work with any scenario's coordinates and terrain. The 8-phase sequence logic stays identical -- only the data inputs change.

Output: All mission-related files parameterized, using CalibrationService from Plan 01.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/64-scenario-agnostic-refactor-and-baltic-shield-demo/64-RESEARCH.md
@.planning/phases/64-scenario-agnostic-refactor-and-baltic-shield-demo/64-01-SUMMARY.md

@backend/src/robot/calibration-service.ts
@backend/src/robot/mission-sequence-orchestrator.ts
@backend/src/robot/skills/navigation-skill.ts
@backend/src/robot/mission-simulator.ts
@backend/src/robot/autonomous-mission-orchestrator.ts
@backend/src/robot/tactical-ai-service.ts
@backend/src/robot/skills/tactical-skills.ts
@backend/src/api/robot-routes.ts
@backend/data/demo-baltic-seed/adversary-orbat.json
</context>

<interfaces>
<!-- From Plan 01 outputs (CalibrationService) -->
From backend/src/robot/calibration-service.ts:
```typescript
export interface CalibrationProfile {
  room_width: number;
  room_height: number;
  map_bounds: { north: number; south: number; east: number; west: number };
  label?: string;
}
export const calibrationService: {
  getProfile(name?: string): CalibrationProfile;
  roomToGeo(x: number, y: number, profileName?: string): { lat: number; lng: number };
};
```

From backend/src/robot/vehicle-database.ts:
```typescript
export const vehicleDatabase: {
  getByClassification(classification: string): VehicleEntry | undefined;
  getThreatClasses(): string[];
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Generalize mission config and orchestrator (SA-64-04, SA-64-08)</name>
  <files>
    backend/src/robot/mission-sequence-orchestrator.ts,
    backend/src/robot/autonomous-mission-orchestrator.ts,
    backend/src/robot/tactical-ai-service.ts,
    backend/src/robot/skills/tactical-skills.ts,
    backend/src/api/robot-routes.ts
  </files>
  <action>
**mission-sequence-orchestrator.ts:**
1. Rename `IRON_BASTION_DEFAULTS` to `DEFAULT_MISSION_CONFIG`. Keep the same SequenceConfig structure. Derive default positions from `calibrationService.getProfile()` using room-coordinate proportions:
   - homeBase: `{ x: 0.5, y: 1.0 }` (near origin, staging area)
   - reconArea: `{ x_min: 0.5, y_min: 5.0, x_max: 4.5, y_max: 13.0 }` (middle-to-north of room)
   - overwatchPosition: `{ x: 2.5, y: 12.0 }` (northern ridge area)
   - firingPositions: `[{ x: 1.0, y: 11.0 }, { x: 4.0, y: 11.0 }]` (flanking)
2. Rename `startIronBastion()` to `startMissionSequence()`. Keep the exact same logic and phase sequence. Export both the new name and the old as a deprecated alias if any other file references `startIronBastion`.
3. Replace any hardcoded geo coordinates in the function body with `calibrationService.roomToGeo(x, y)` calls using the room-coord positions above.
4. Import `calibrationService` from `./calibration-service`.

**autonomous-mission-orchestrator.ts:**
1. Find `classDesc: 't-99'` or any hardcoded threat class references. Replace with `vehicleDatabase.getThreatClasses()[0]` or parameterize from mission config.
2. Replace any `ironBastion` references in variable names or comments with generic mission terms.

**tactical-ai-service.ts and tactical-skills.ts:**
1. Find `enemy_advance_axis` or any hardcoded axis descriptions ("Zhongxiao West", specific street names). Replace with a generic parameter: `advanceAxis: string` that defaults to `'primary approach corridor'` and can be overridden from mission config.
2. Import `vehicleDatabase` where threat class lookups happen (if not already done in Plan 01).

**robot-routes.ts:**
1. Find the `/scenarios/iron-bastion` route (or similar). Rename to `/scenarios/mission-sequence` or `/mission/start`. Keep old route as alias if needed for backward compat.
2. Replace any `startIronBastion()` call with `startMissionSequence()`.
3. Find `/scenarios/seed-strategic-cop` route -- do NOT modify force disposition data yet (that is SA-64-09, Plan 03). Just note for Plan 03.
  </action>
  <verify>
    <automated>bash -lc 'cd /home/vitalpointai/projects/ssr/backend && npx tsc --noEmit && echo "BUILD OK"'</automated>
  </verify>
  <done>
    - Zero `IRON_BASTION_DEFAULTS` references remain
    - Zero `startIronBastion` references remain (except optional deprecated alias)
    - Zero hardcoded street-name axis descriptions remain
    - `startMissionSequence()` exported and called from routes
    - `tsc --noEmit` passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace ZHONGZHENG_MAP and parameterize simulation events (SA-64-05, SA-64-11)</name>
  <files>
    backend/src/robot/skills/navigation-skill.ts,
    backend/src/robot/mission-simulator.ts
  </files>
  <action>
**navigation-skill.ts (SA-64-05):**
1. Find `ZHONGZHENG_MAP` constant (12 Taipei streets + 4 landmarks). Replace with a generic `DEFAULT_AREA_MAP` constant containing Baltic-style open terrain features:
   - 3 N-S farm tracks (at x=1.0, x=2.5, x=4.2)
   - 3 E-W movement lines (at y=1.0, y=9.5, y=12.5)
   - 4 landmarks: Northern Ridge (2.5, 12.0), Tree Line Boundary (2.5, 9.5), Staging Area (2.5, 1.0), UEA Center (2.5, 7.5)
   - Use the same `MapData` type structure as existing ZHONGZHENG_MAP (roads array with name/axis/position/range/roadClass/lanes, landmarks array with name/position/type/notes)
   - The A* pathfinding should work on this track network the same way it worked on the street grid
2. Verify that `setActiveMap()` and `getActiveMap()` functions exist (research says they do). If they exist, ensure `DEFAULT_AREA_MAP` is set as the initial active map. If they don't exist, create them as simple getter/setter on a module-level variable.
3. Rename any `ZHONGZHENG` references in variable names to generic equivalents (e.g., `scenarioAreaMap`).
4. Update geo bounds in the map data to use `calibrationService.getProfile().map_bounds` instead of hardcoded lat/lng. If the map data structure stores geoBounds statically, derive them from the calibration profile at load time.

**mission-simulator.ts (SA-64-11):**
1. Find hardcoded recon sweep waypoints (12-point Taipei intersections). Replace with waypoints generated from the active area map: distribute N points along the farm tracks within the recon area bounds. Use a simple approach: create waypoints at regular intervals along each N-S track within y_min to y_max of the recon area.
2. Find hardcoded enemy spawn positions (Taipei intersection coordinates). Replace with positions derived from calibration profile: enemy approaches from the north/east edge of the room. Use `calibrationService.roomToGeo()` with room coordinates like `(3.0, 14.0)` for northern approach.
3. Find hardcoded `threatClasses: ['CHN-99G', 'T-90']` or similar arrays. Replace with `vehicleDatabase.getThreatClasses()`.
4. Replace any remaining Taipei-specific comments with generic equivalents.
  </action>
  <verify>
    <automated>bash -lc 'cd /home/vitalpointai/projects/ssr/backend && npx tsc --noEmit && ! grep -q "ZHONGZHENG" src/robot/skills/navigation-skill.ts src/robot/mission-simulator.ts && echo "BUILD OK"'</automated>
  </verify>
  <done>
    - Zero `ZHONGZHENG_MAP` references remain in navigation-skill.ts
    - DEFAULT_AREA_MAP with open terrain features is the active map
    - Recon sweep waypoints derive from area map, not hardcoded Taipei intersections
    - Enemy spawn positions use calibrationService.roomToGeo(), not hardcoded geo coords
    - Inline threatClasses arrays replaced with vehicleDatabase.getThreatClasses()
    - `tsc --noEmit` passes
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `bash -lc 'cd /home/vitalpointai/projects/ssr/backend && npx tsc --noEmit'` passes
2. `grep -rn "IRON_BASTION\|ZHONGZHENG\|startIronBastion\|CHN-99G" backend/src/ --include="*.ts"` returns zero matches (excluding deprecated alias comments)
3. `grep -rn "startMissionSequence\|DEFAULT_MISSION_CONFIG\|DEFAULT_AREA_MAP" backend/src/ --include="*.ts"` confirms new names in use
</verification>

<success_criteria>
- Mission sequence is fully generic: starts via startMissionSequence() with config derived from calibration profile
- Navigation uses open terrain map structure, not Taipei street grid
- Simulation events (recon sweeps, enemy spawns, threat classes) all parameterized
- The 8-phase mission logic (HOLD through ENGAGE) is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/64-scenario-agnostic-refactor-and-baltic-shield-demo/64-02-SUMMARY.md`
</output>
