---
phase: 60-rearchitect-ironclaw-integration
plan: 04
type: execute
wave: 3
depends_on: [60-03]
files_modified:
  - frontend/src/components/agent-config/AgentConfigPanel.tsx
  - frontend/src/components/agent-config/tabs/IdentityTab.tsx
  - frontend/src/components/agent-config/tabs/PersonalityTab.tsx
  - frontend/src/components/agent-config/components/AgentPreviewChat.tsx
  - frontend/src/components/agent-config/hooks/useAgentConfig.ts
  - frontend/src/components/agent-config/hooks/useAgentPreview.ts
autonomous: true
requirements: [IC-03-UI-PANEL, IC-03-IDENTITY-TAB, IC-03-PERSONALITY-TAB, IC-03-PREVIEW]

must_haves:
  truths:
    - "User can view and edit their identity fields (name, rank, position, unit)"
    - "User can configure personality preferences (tone, verbosity, BLUF, output format)"
    - "User can preview how Ironclaw will respond with current config via AgentPreviewChat"
    - "Changes auto-save and trigger identity sync to Ironclaw"
  artifacts:
    - path: "frontend/src/components/agent-config/AgentConfigPanel.tsx"
      provides: "Tab container for agent configuration"
      min_lines: 50
    - path: "frontend/src/components/agent-config/tabs/IdentityTab.tsx"
      provides: "Identity fields form"
      min_lines: 80
    - path: "frontend/src/components/agent-config/tabs/PersonalityTab.tsx"
      provides: "Personality preference controls"
      min_lines: 60
    - path: "frontend/src/components/agent-config/components/AgentPreviewChat.tsx"
      provides: "Live preview chat with configured persona"
      min_lines: 40
  key_links:
    - from: "frontend/src/components/agent-config/hooks/useAgentConfig.ts"
      to: "/api/agent-config/:userId"
      via: "fetch GET/PUT"
      pattern: "api/agent-config"
    - from: "frontend/src/components/agent-config/AgentConfigPanel.tsx"
      to: "frontend/src/components/agent-config/tabs/*.tsx"
      via: "tab rendering"
      pattern: "IdentityTab|PersonalityTab"
---

<objective>
Build the Agent Config UI with Identity tab, Personality tab, and Preview chat.

Purpose: Blueprint Phase 3 — Users need a UI to configure their Chief of Staff persona. The Identity tab captures military identity (rank, position, unit). The Personality tab controls how Ironclaw communicates (tone, verbosity, BLUF enforcement). The Preview chat lets users see how their configured agent will respond before committing.

Output: AgentConfigPanel with 2 tabs + preview chat component, hooks for data fetching/saving.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/60-rearchitect-ironclaw-integration-use-ironclaw-bastion-blueprint-v1-3-pdf-to-rearchitect-how-ironclaw-and-bastion-integrate-and-work-together/60-RESEARCH.md
@.planning/phases/60-rearchitect-ironclaw-integration-use-ironclaw-bastion-blueprint-v1-3-pdf-to-rearchitect-how-ironclaw-and-bastion-integrate-and-work-together/60-03-SUMMARY.md

<interfaces>
<!-- From 60-03 (will exist) -->
```typescript
// AgentConfig type from ironclaw-types.ts
interface AgentConfig {
  did: string;
  nearAccount: string;
  displayName: string;
  rank: string;
  staffSection: StaffSection;
  position: string;
  unit: string;
  higherHQ: string;
  // ... full field list in ironclaw-types.ts
}

// REST API
// GET /api/agent-config/:userId -> AgentConfig
// PUT /api/agent-config/:userId -> AgentConfig (triggers identity sync)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useAgentConfig hook and AgentConfigPanel shell</name>
  <files>frontend/src/components/agent-config/hooks/useAgentConfig.ts, frontend/src/components/agent-config/hooks/useAgentPreview.ts, frontend/src/components/agent-config/AgentConfigPanel.tsx</files>
  <action>
**useAgentConfig.ts:**
- Export `useAgentConfig(userId: string)` hook that:
  - Fetches config via `GET /api/agent-config/${userId}` on mount
  - Returns `{ config, loading, error, updateConfig, saving }`
  - `updateConfig(partial: Partial<AgentConfig>)` calls `PUT /api/agent-config/${userId}` with merged config
  - Debounce saves by 500ms to avoid excessive API calls during form editing
  - Use the project's existing fetch/API utility (check `frontend/src/utils/` or `frontend/src/api/` for the standard API call pattern)

**useAgentPreview.ts:**
- Export `useAgentPreview(config: AgentConfig)` hook that:
  - Provides a `sendPreviewMessage(text: string): Promise<string>` function
  - Calls the existing Ironclaw chat endpoint with a preview flag or temporary session
  - Returns `{ sendPreviewMessage, response, streaming, error }`
  - Use the existing Ironclaw WebSocket or REST pattern from `frontend/src/components/ironclaw/` for message sending

**AgentConfigPanel.tsx:**
- Tab-based panel with tabs: "Identity", "Personality" (more tabs added in later plans: Skills, Channels, Routines, Advanced)
- Use project's existing tab component pattern (check `frontend/src/components/` for how tabs are implemented — likely Material UI or custom)
- Renders selected tab component and passes `config`/`updateConfig` as props
- Shows a save status indicator (saving/saved/error)
- Place AgentPreviewChat as a collapsible sidebar or bottom panel within the config panel
- Accept `userId` as prop, get from auth context if not provided
  </action>
  <verify>
    <automated>bash -lc 'test -f frontend/src/components/agent-config/hooks/useAgentConfig.ts && test -f frontend/src/components/agent-config/hooks/useAgentPreview.ts && test -f frontend/src/components/agent-config/AgentConfigPanel.tsx && echo "OK"'</automated>
  </verify>
  <done>useAgentConfig hook fetches/saves config with debounce. useAgentPreview hook sends preview messages. AgentConfigPanel renders tab layout with Identity and Personality tabs.</done>
</task>

<task type="auto">
  <name>Task 2: Build IdentityTab, PersonalityTab, and AgentPreviewChat components</name>
  <files>frontend/src/components/agent-config/tabs/IdentityTab.tsx, frontend/src/components/agent-config/tabs/PersonalityTab.tsx, frontend/src/components/agent-config/components/AgentPreviewChat.tsx</files>
  <action>
**IdentityTab.tsx:**
- Form fields for: displayName (text), rank (text or dropdown with common military ranks), staffSection (dropdown: Commander/S1/S2/S3/S4/S6/S9/XO/CSM/Other), position (text), unit (text), higherHQ (text), reportingToDid (text, optional), areasOfResponsibility (tag input or comma-separated text)
- Each field change calls `updateConfig({ fieldName: value })` from parent
- Group fields logically: "Personal" (name, rank), "Assignment" (staffSection, position, unit, higherHQ), "Relationships" (reportingTo, AOR)
- Use the project's existing form field patterns (check other form components in the codebase for styling conventions — Material UI, Tailwind, or custom)

**PersonalityTab.tsx:**
- Controls for: blufEnforced (toggle), outputFormat (dropdown: MDMP/StaffSummary/FreeForm/Auto), verbosityLevel (slider 1-5 with labels: "Terse" to "Comprehensive"), tone (dropdown: FormalMilitary/Professional/Direct/Collaborative), expandAcronyms (toggle), classificationMarkings (toggle), customPersonaInstructions (textarea, with hint "Additional instructions for how your Chief of Staff should communicate")
- Group fields: "Communication Style" (tone, verbosity, BLUF), "Output Preferences" (format, acronyms, markings), "Custom Instructions" (persona text)

**AgentPreviewChat.tsx:**
- Compact chat interface showing a single exchange: user types a message, sees how Ironclaw would respond with current config
- Uses `useAgentPreview(config)` hook
- Shows the current SOUL.md template excerpt above the chat as a "Your Chief of Staff will..." summary
- Has a "Test Message" button with a default prompt like "Brief me on current operations" that users can edit
- Displays streaming response with a typing indicator
- Keep it simple: single message/response, not a full conversation. Purpose is preview, not interaction.
  </action>
  <verify>
    <automated>bash -lc 'test -f frontend/src/components/agent-config/tabs/IdentityTab.tsx && test -f frontend/src/components/agent-config/tabs/PersonalityTab.tsx && test -f frontend/src/components/agent-config/components/AgentPreviewChat.tsx && echo "OK"'</automated>
  </verify>
  <done>IdentityTab has all military identity form fields. PersonalityTab has communication style controls. AgentPreviewChat shows single-exchange preview with streaming response.</done>
</task>

</tasks>

<verification>
- All component files exist and export React components
- useAgentConfig fetches from /api/agent-config endpoint
- Identity fields match AgentConfig type
- Personality controls cover all blueprint-specified preferences
- Preview chat uses existing Ironclaw communication pattern
- Frontend compiles: `bash -lc 'cd frontend && npx tsc --noEmit 2>&1 | head -20'`
</verification>

<success_criteria>
- AgentConfigPanel renders with Identity and Personality tabs
- Identity form captures rank, position, unit, staff section, etc.
- Personality form captures tone, verbosity, BLUF, output format
- Preview chat shows how configured agent responds
- Config changes auto-save with debounce and trigger identity sync
</success_criteria>

<output>
After completion, create `.planning/phases/60-rearchitect-ironclaw-integration-use-ironclaw-bastion-blueprint-v1-3-pdf-to-rearchitect-how-ironclaw-and-bastion-integrate-and-work-together/60-04-SUMMARY.md`
</output>
