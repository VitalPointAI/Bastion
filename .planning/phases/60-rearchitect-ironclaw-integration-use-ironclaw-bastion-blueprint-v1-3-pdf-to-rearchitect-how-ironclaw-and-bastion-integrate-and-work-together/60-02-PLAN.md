---
phase: 60-rearchitect-ironclaw-integration
plan: 02
type: execute
wave: 2
depends_on: [60-01]
files_modified:
  - backend/src/mcp/mcp-server.ts
  - backend/src/mcp/tools/knowledge.ts
  - backend/src/mcp/tools/operations.ts
  - backend/src/mcp/tools/calendar.ts
  - backend/src/mcp/tools/resources.ts
  - backend/src/mcp/tools/personnel.ts
  - backend/src/mcp/middleware/did-auth.ts
autonomous: true
requirements: [IC-01-MCP-TOOLS, IC-01-DID-AUTH]

must_haves:
  truths:
    - "MCP server exposes full tool catalog per blueprint Section 4.1"
    - "DID VC claim middleware gates clearance-sensitive tools"
    - "Tool groups are organized by domain (knowledge, operations, calendar, resources, personnel)"
  artifacts:
    - path: "backend/src/mcp/tools/knowledge.ts"
      provides: "Knowledge graph tool group"
      exports: ["knowledgeTools"]
    - path: "backend/src/mcp/tools/operations.ts"
      provides: "Operations tool group"
      exports: ["operationsTools"]
    - path: "backend/src/mcp/tools/calendar.ts"
      provides: "Calendar tool group"
      exports: ["calendarTools"]
    - path: "backend/src/mcp/tools/resources.ts"
      provides: "Resources tool group"
      exports: ["resourcesTools"]
    - path: "backend/src/mcp/tools/personnel.ts"
      provides: "Personnel tool group (clearance-gated)"
      exports: ["personnelTools"]
    - path: "backend/src/mcp/middleware/did-auth.ts"
      provides: "DID VC claim resolution and tool gating"
      exports: ["resolveDIDClaims", "requireClearance"]
  key_links:
    - from: "backend/src/mcp/mcp-server.ts"
      to: "backend/src/mcp/tools/*.ts"
      via: "import and register tool groups"
      pattern: "import.*Tools.*from.*tools/"
    - from: "backend/src/mcp/middleware/did-auth.ts"
      to: "backend/src/mcp/tools/personnel.ts"
      via: "clearance gate on personnel tools"
      pattern: "requireClearance"
---

<objective>
Expand Bastion MCP server with full tool catalog and DID-authenticated access control.

Purpose: Blueprint Phase 1 — Ironclaw needs a rich set of Bastion tools organized by domain. The personnel tool group is clearance-gated via DID VC claims. This gives Ironclaw the ability to take actions inside Bastion on behalf of authenticated users.

Output: 5 tool group modules, DID auth middleware, updated mcp-server.ts registration.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/60-rearchitect-ironclaw-integration-use-ironclaw-bastion-blueprint-v1-3-pdf-to-rearchitect-how-ironclaw-and-bastion-integrate-and-work-together/60-RESEARCH.md

<interfaces>
<!-- Existing MCP server interface -->

From backend/src/mcp/mcp-server.ts:
```typescript
export function createMcpServer(): Server;
export function getMcpServer(): Server;
export async function connectMcpServer(transport: Transport): Promise<Server>;
```

From backend/src/ironclaw/tool-bridge.ts:
```typescript
export interface MCPToolDefinition {
  name: string;
  description: string;
  inputSchema: object;
  // ...
}
export const BASTION_TOOLS: MCPToolDefinition[];
```

From 60-01 (new):
```typescript
export function didToSlug(did: string): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DID auth middleware and 5 tool group modules</name>
  <files>backend/src/mcp/middleware/did-auth.ts, backend/src/mcp/tools/knowledge.ts, backend/src/mcp/tools/operations.ts, backend/src/mcp/tools/calendar.ts, backend/src/mcp/tools/resources.ts, backend/src/mcp/tools/personnel.ts</files>
  <action>
**DID auth middleware (backend/src/mcp/middleware/did-auth.ts):**
- Export `interface VCClaim { type: string; value: string; issuer: string; issuedAt: Date; }`
- Export `type ClearanceLevel = 'UNCLASSIFIED' | 'CUI' | 'SECRET' | 'TOP_SECRET';`
- Export `type StaffSection = 'Commander' | 'S1' | 'S2' | 'S3' | 'S4' | 'S6' | 'S9' | 'XO' | 'CSM' | 'Other';`
- Export `async function resolveDIDClaims(did: string): Promise<VCClaim[]>` — For now, implement a stub that returns claims from the AgentConfig table (to be created in 60-03). Add a 1-hour TTL cache using a simple Map with timestamp. Include a TODO comment: "Replace with NEAR blockchain DID document resolution when VC infrastructure is live."
- Export `function requireClearance(claims: VCClaim[], minLevel: ClearanceLevel): boolean` — checks ClearanceLevel claim against hierarchy.
- Export `function getStaffSection(claims: VCClaim[]): StaffSection` — extracts StaffRole claim.

**Tool groups (each file exports an array of MCPToolDefinition):**

**knowledge.ts** — `export const knowledgeTools: MCPToolDefinition[]`:
- `bastion.knowledge.search` — search knowledge graph entities
- `bastion.knowledge.get_entity` — get entity details by ID
- `bastion.knowledge.get_relationships` — get entity relationships
- `bastion.knowledge.search_documents` — full-text document search

**operations.ts** — `export const operationsTools: MCPToolDefinition[]`:
- `bastion.ops.get_problem_set` — read problem set details
- `bastion.ops.list_problem_sets` — list available problem sets
- `bastion.ops.get_operational_design` — read operational design artifacts
- `bastion.ops.get_campaign_plan` — read campaign plan data
- `bastion.ops.get_coa` — read course of action details

**calendar.ts** — `export const calendarTools: MCPToolDefinition[]`:
- `bastion.calendar.get_schedule` — get exercise/training schedule
- `bastion.calendar.get_events` — list events for date range

**resources.ts** — `export const resourcesTools: MCPToolDefinition[]`:
- `bastion.resources.list` — list resources by type/capability
- `bastion.resources.get_status` — get resource status/readiness
- `bastion.resources.search_capabilities` — search by capability

**personnel.ts** — `export const personnelTools: MCPToolDefinition[]` (clearance-gated):
- `bastion.personnel.list_staff` — list staff members (requires CUI+)
- `bastion.personnel.get_member` — get member details (requires CUI+)
- `bastion.personnel.get_clearances` — get clearance info (requires SECRET+)

Each tool definition follows the existing `MCPToolDefinition` interface from `tool-bridge.ts`. Tool implementations should call existing backend services (stores, services) where they exist, and return stub responses with TODO comments where backend services don't yet exist.
  </action>
  <verify>
    <automated>bash -lc 'ls backend/src/mcp/tools/knowledge.ts backend/src/mcp/tools/operations.ts backend/src/mcp/tools/calendar.ts backend/src/mcp/tools/resources.ts backend/src/mcp/tools/personnel.ts backend/src/mcp/middleware/did-auth.ts && grep -c "export" backend/src/mcp/middleware/did-auth.ts'</automated>
  </verify>
  <done>All 5 tool group files and DID auth middleware exist with correct exports. Personnel tools are annotated as clearance-gated.</done>
</task>

<task type="auto">
  <name>Task 2: Wire tool groups into MCP server</name>
  <files>backend/src/mcp/mcp-server.ts</files>
  <action>
Modify `backend/src/mcp/mcp-server.ts` to:

1. Import all 5 tool group arrays: `knowledgeTools`, `operationsTools`, `calendarTools`, `resourcesTools`, `personnelTools`.
2. Import `resolveDIDClaims`, `requireClearance` from `middleware/did-auth.ts`.
3. In the `createMcpServer()` function, register all tool groups. The existing tool registration pattern uses `server.setRequestHandler(ListToolsRequestSchema, ...)` and `server.setRequestHandler(CallToolRequestSchema, ...)`.
4. For the `CallToolRequestSchema` handler, add DID extraction from the request context (the `agentDID` field that's already being read — see the existing `isDIDAuthorized` function). Before executing personnel tools, call `resolveDIDClaims(agentDID)` and `requireClearance(claims, 'CUI')`. Return an error response if clearance check fails.
5. Combine all tool definitions into a merged array: `[...BASTION_TOOLS, ...knowledgeTools, ...operationsTools, ...calendarTools, ...resourcesTools, ...personnelTools]` for the `ListToolsRequestSchema` response.
6. Replace the existing `isDIDAuthorized` allowlist check with the new DID auth middleware for a more granular approach. Keep the existing `MCP_ALLOWED_DIDS` as a fallback allowlist for backwards compatibility, but add a comment: "Per blueprint, migrating to per-tool VC claim gating."
  </action>
  <verify>
    <automated>bash -lc 'grep -c "knowledgeTools\|operationsTools\|calendarTools\|resourcesTools\|personnelTools\|resolveDIDClaims" backend/src/mcp/mcp-server.ts'</automated>
  </verify>
  <done>MCP server registers all 5 tool groups, personnel tools are clearance-gated via DID auth middleware, existing tools preserved.</done>
</task>

</tasks>

<verification>
- All 5 tool group files exist with exported tool arrays
- DID auth middleware exports resolveDIDClaims and requireClearance
- MCP server imports and registers all tool groups
- Personnel tools are gated by clearance check
- TypeScript compiles: `bash -lc 'cd backend && npx tsc --noEmit 2>&1 | head -20'`
</verification>

<success_criteria>
- MCP server exposes 17+ tools organized by domain
- DID VC claim middleware resolves claims with 1-hour cache
- Personnel tools require CUI+ clearance
- Existing BASTION_TOOLS from tool-bridge.ts still registered
</success_criteria>

<output>
After completion, create `.planning/phases/60-rearchitect-ironclaw-integration-use-ironclaw-bastion-blueprint-v1-3-pdf-to-rearchitect-how-ironclaw-and-bastion-integrate-and-work-together/60-02-SUMMARY.md`
</output>
