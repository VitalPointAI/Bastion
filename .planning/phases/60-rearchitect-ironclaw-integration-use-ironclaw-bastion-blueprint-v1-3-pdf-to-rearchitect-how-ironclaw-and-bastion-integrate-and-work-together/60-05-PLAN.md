---
phase: 60-rearchitect-ironclaw-integration
plan: 05
type: execute
wave: 4
depends_on: [60-04]
files_modified:
  - frontend/src/components/agent-config/tabs/SkillsTab.tsx
  - frontend/src/components/agent-config/tabs/ChannelsTab.tsx
  - frontend/src/components/agent-config/components/TelegramPairWizard.tsx
  - backend/src/ironclaw/skill-packs.ts
autonomous: true
requirements: [IC-04-SKILLS, IC-04-SKILLPACKS, IC-04-TELEGRAM, IC-04-CHANNELS]

must_haves:
  truths:
    - "User can enable/disable role-specific skill packs"
    - "User can initiate Telegram pairing from the Channels tab"
    - "7 role-specific skill packs exist with appropriate triggers and tools"
    - "Telegram notification level is configurable"
  artifacts:
    - path: "frontend/src/components/agent-config/tabs/SkillsTab.tsx"
      provides: "Skill pack enable/disable UI"
      min_lines: 60
    - path: "frontend/src/components/agent-config/tabs/ChannelsTab.tsx"
      provides: "Channel configuration (Telegram, notifications)"
      min_lines: 50
    - path: "frontend/src/components/agent-config/components/TelegramPairWizard.tsx"
      provides: "3-step Telegram bot pairing flow"
      min_lines: 60
    - path: "backend/src/ironclaw/skill-packs.ts"
      provides: "7 role-specific SKILL.md templates"
      exports: ["SKILL_PACKS", "renderSkillPackMd"]
  key_links:
    - from: "frontend/src/components/agent-config/tabs/SkillsTab.tsx"
      to: "backend/src/ironclaw/skill-packs.ts"
      via: "skill pack definitions rendered in UI"
      pattern: "SKILL_PACKS"
    - from: "frontend/src/components/agent-config/components/TelegramPairWizard.tsx"
      to: "backend/src/ironclaw/ironclaw-client.ts"
      via: "sendMessage('/telegram pair')"
      pattern: "telegram.*pair"
---

<objective>
Add Skills tab with role-specific skill packs and Channels tab with Telegram pairing.

Purpose: Blueprint Phase 4 — Skill packs give Ironclaw domain expertise for each staff role (S2 gets intelligence analysis skills, S3 gets operations planning skills, etc.). Telegram pairing enables multi-channel notification routing per the blueprint's priority system.

Output: SkillsTab, ChannelsTab, TelegramPairWizard, 7 skill pack templates.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/60-rearchitect-ironclaw-integration-use-ironclaw-bastion-blueprint-v1-3-pdf-to-rearchitect-how-ironclaw-and-bastion-integrate-and-work-together/60-RESEARCH.md
@.planning/phases/60-rearchitect-ironclaw-integration-use-ironclaw-bastion-blueprint-v1-3-pdf-to-rearchitect-how-ironclaw-and-bastion-integrate-and-work-together/60-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skill pack definitions and SkillsTab UI</name>
  <files>backend/src/ironclaw/skill-packs.ts, frontend/src/components/agent-config/tabs/SkillsTab.tsx</files>
  <action>
**skill-packs.ts:**
- Export `interface SkillPack { id: string; name: string; description: string; staffSections: StaffSection[]; triggers: string[]; requiredTools: string[]; trustLevel: 'high' | 'medium' | 'low'; }`
- Export `const SKILL_PACKS: SkillPack[]` with 7 role-specific packs:
  1. `intel-analysis` — S2: intelligence preparation, threat assessment, collection management. Triggers: "analyze threat", "IPB", "collection plan". Required tools: bastion.knowledge.search, bastion.ops.get_problem_set.
  2. `ops-planning` — S3: MDMP workflow, OPORD drafting, synchronization matrix. Triggers: "draft OPORD", "synchronize", "COA analysis". Required tools: bastion.ops.get_campaign_plan, bastion.ops.get_coa.
  3. `logistics-support` — S4: supply management, LOGSTAT formatting, maintenance tracking. Triggers: "supply status", "LOGSTAT", "maintenance report". Required tools: bastion.resources.list, bastion.resources.get_status.
  4. `personnel-admin` — S1: personnel readiness, morale reports, casualty tracking. Triggers: "personnel status", "strength report". Required tools: bastion.personnel.list_staff.
  5. `comms-cyber` — S6: C4ISR architecture, network status, spectrum management. Triggers: "network status", "comms plan", "spectrum". Required tools: bastion.resources.search_capabilities.
  6. `civil-affairs` — S9: PMESII analysis, civil-military coordination. Triggers: "civil assessment", "PMESII". Required tools: bastion.knowledge.search.
  7. `command-decision` — Commander/XO: COA comparison, decision brief, risk assessment. Triggers: "decision brief", "compare COAs", "risk matrix". Required tools: bastion.ops.get_coa, bastion.knowledge.get_relationships.
- Export `function renderSkillPackMd(pack: SkillPack): string` — generates SKILL.md with YAML frontmatter (name, triggers, required_tools, trust) and markdown body per Ironclaw's expected format.

**SkillsTab.tsx:**
- Displays all 7 skill packs as toggleable cards
- Each card shows: name, description, applicable staff sections, trigger examples
- Cards relevant to the user's staffSection are highlighted/recommended
- Toggle adds/removes pack ID from `config.enabledSkillPacks` array
- Custom skills section at bottom: list of `config.customSkills` with add/edit/remove. Each custom skill has name, description, triggers (comma-separated).
- Register SkillsTab in AgentConfigPanel's tab list.
  </action>
  <verify>
    <automated>bash -lc 'grep -c "SKILL_PACKS\|renderSkillPackMd" backend/src/ironclaw/skill-packs.ts && test -f frontend/src/components/agent-config/tabs/SkillsTab.tsx && echo "OK"'</automated>
  </verify>
  <done>7 skill packs defined with triggers and required tools. SkillsTab shows toggleable cards with staff section recommendations. Custom skills editable.</done>
</task>

<task type="auto">
  <name>Task 2: Create ChannelsTab and TelegramPairWizard</name>
  <files>frontend/src/components/agent-config/tabs/ChannelsTab.tsx, frontend/src/components/agent-config/components/TelegramPairWizard.tsx</files>
  <action>
**ChannelsTab.tsx:**
- Sections: "Bastion In-App" (always enabled, shows notification preference), "Telegram" (enable/disable toggle, pairing status, notification level dropdown)
- Telegram section shows:
  - If `config.telegramEnabled && config.telegramChatId`: "Paired" status with chat ID and "Unpair" button
  - If `config.telegramEnabled && !config.telegramChatId`: "Not paired" with "Pair Now" button that opens TelegramPairWizard
  - If `!config.telegramEnabled`: Toggle to enable, then pair
- Notification level dropdown (Critical/Urgent/Routine/Informational) per channel
- Display the blueprint's notification priority routing table as a reference card:
  - CRITICAL: Telegram DM + Bastion in-app
  - URGENT: Telegram during duty hours; Bastion-only outside
  - ROUTINE: Bastion in-app only; daily digest
  - INFORMATIONAL: Workspace memory write only
- Register ChannelsTab in AgentConfigPanel's tab list.

**TelegramPairWizard.tsx:**
- 3-step wizard:
  1. "Initiate" — Explain the process, show "Start Pairing" button. On click, call backend to send `/telegram pair` to Ironclaw via webhook for this user's DID.
  2. "Enter Code" — Show 6-digit code input field. Instruct user to check their Telegram for a message from the bot. On submit, call backend to send `/telegram confirm ${code}` via webhook.
  3. "Complete" — Show success/failure. On success, update `config.telegramChatId` and show confirmation.
- Each step has back/cancel navigation
- Error handling for: timeout (code not entered within 5 minutes), invalid code, network errors
- The backend calls go through existing Ironclaw webhook (add a thin endpoint `/api/agent-config/:userId/telegram-pair` and `/api/agent-config/:userId/telegram-confirm` that call `ironclawClient.sendMessage(did, '/telegram pair')` and `ironclawClient.sendMessage(did, '/telegram confirm ${code}')` respectively). Add these two endpoints to `backend/src/api/routes/agent-config.ts`.
  </action>
  <verify>
    <automated>bash -lc 'test -f frontend/src/components/agent-config/tabs/ChannelsTab.tsx && test -f frontend/src/components/agent-config/components/TelegramPairWizard.tsx && grep -c "telegram" backend/src/api/routes/agent-config.ts && echo "OK"'</automated>
  </verify>
  <done>ChannelsTab shows Telegram pairing status and notification levels. TelegramPairWizard implements 3-step pairing flow. Backend telegram-pair and telegram-confirm endpoints exist.</done>
</task>

</tasks>

<verification>
- 7 skill packs defined with correct staff section mappings
- SkillsTab and ChannelsTab registered in AgentConfigPanel
- TelegramPairWizard sends /telegram pair and /telegram confirm via webhook
- Telegram pairing endpoints added to agent-config routes
- Frontend compiles: `bash -lc 'cd frontend && npx tsc --noEmit 2>&1 | head -20'`
</verification>

<success_criteria>
- Users can enable/disable skill packs with role-specific recommendations
- Custom skills can be added/edited/removed
- Telegram pairing works via 3-step wizard
- Notification levels configurable per channel
- Skill pack Markdown generation via renderSkillPackMd
</success_criteria>

<output>
After completion, create `.planning/phases/60-rearchitect-ironclaw-integration-use-ironclaw-bastion-blueprint-v1-3-pdf-to-rearchitect-how-ironclaw-and-bastion-integrate-and-work-together/60-05-SUMMARY.md`
</output>
