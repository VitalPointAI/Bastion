---
phase: 4.3-strategic-intelligence-fusion
plan: 14
subsystem: api
tags: [validity, trend, graph, typescript]

# Dependency graph
requires:
  - phase: 4.3-plan-6
    provides: validityService.calculateTrend() implementation and validity_scores table

provides:
  - Bulk /validity/objectives endpoint returns real trend direction per objective
  - mapTrend() helper mapping improving/declining/stable to up/down/stable

affects:
  - validity-dashboard
  - frontend validity components consuming /api/graph/validity/objectives

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - backend/src/api/graph.ts

key-decisions:
  - "mapTrend() added as local helper function (inside graph.ts module scope) rather than exported utility — keeps concern close to the endpoint that uses it"
  - "calculateTrend() defaults 30-day window; objectives with no score history safely return stable — no special-casing needed"

patterns-established:
  - "Service method reuse pattern: bulk endpoint calls same service method as individual trend endpoint, avoiding duplicate logic"

requirements-completed: []

# Metrics
duration: 1min
completed: 2026-02-22
---

# Phase 4.3 Plan 14: Validity Trend Gap Closure Summary

**Bulk /validity/objectives endpoint wired to real calculateTrend() per objective, replacing hard-coded 'stable' with live improving/declining/stable trend mapped to up/down/stable for the validity dashboard**

## Performance

- **Duration:** 1 min
- **Started:** 2026-02-22T18:59:03Z
- **Completed:** 2026-02-22T19:00:07Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Replaced `trend: 'stable' as const, // Trend analysis not yet implemented` with real `validityService.calculateTrend()` call
- Added `mapTrend()` helper converting `improving/declining/stable` to `up/down/stable` for frontend compatibility
- Error fallback still returns `'stable'` for objectives that fail (no regression in error handling)
- No new dependencies added - reuses existing ValidityService already used by individual trend endpoint

## Task Commits

Each task was committed atomically:

1. **Task 1: Replace hard-coded trend with calculateTrend() calls** - `574cc0b` (feat)

**Plan metadata:** (see final commit)

## Files Created/Modified
- `backend/src/api/graph.ts` - Added `mapTrend()` helper, wired `calculateTrend()` into bulk validity objectives handler

## Decisions Made
- `mapTrend()` placed as module-level function before the router handler — keeps it close to its only consumer without polluting module exports
- No changes to error fallback branch — `'stable'` remains correct default when an objective fails to calculate (insufficient data)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Pre-existing TypeScript error in `backend/src/orchestration/agent-wrapper.ts` unrelated to this plan (type incompatibility in tool input casting). Confirmed not introduced by this change — `graph.ts` itself has zero TypeScript errors. The pre-existing error is in scope of other in-progress uncommitted work.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Validity dashboard now shows real trend arrows (up/down/stable) based on score history
- `calculateTrend()` defaults to 30-day window with stable fallback for no-history objects — safe for production
- Phase 4.3 gap closure complete

---
*Phase: 4.3-strategic-intelligence-fusion*
*Completed: 2026-02-22*
