---
phase: 25.1-training-package-upload-from-scenario-integration
plan: 01
subsystem: ui, api
tags: [react, express, wizard, scenario, problem-set, training]

# Dependency graph
requires:
  - phase: 23-problem-set-model-workspace-rename
    provides: problem set CRUD, CreateProblemSetWizard, exercise_scenarios table with problem_set_id column
  - phase: 14-exercise-scenario
    provides: ScenarioStore, exerciseService, ExerciseScenario type
provides:
  - Step 0 source choice in CreateProblemSetWizard (fresh vs from-scenario)
  - Scenario picker with usage count badges
  - Expanded from-scenario endpoint accepting all wizard fields
  - Scenario-to-problem-set linkage via updateProblemSetLink
  - GET /api/problem-sets/scenario-usage-counts endpoint
  - GET /api/problem-sets/:id/linked-scenario endpoint
  - Auto-navigation to Understand tab after from-scenario creation
affects: [25.1-02, training-packages, operational-design]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Source choice step (Step 0) pattern for wizard flows with multiple creation paths"
    - "navigateTo option pattern for post-creation navigation"

key-files:
  created: []
  modified:
    - backend/src/exercise/scenario-store.ts
    - backend/src/api/problem-sets.ts
    - backend/src/problem-set/types.ts
    - backend/src/problem-set/problem-set-store.ts
    - frontend/src/lib/problem-set-service.ts
    - frontend/src/components/problem-set/CreateProblemSetWizard.tsx
    - frontend/src/components/problem-set/CreateProblemSetWizard.css
    - frontend/src/components/problem-set/ProblemSetSwitcher.tsx
    - frontend/src/components/problem-set/ProblemSetSelector.tsx
    - frontend/src/components/problem-set/CommanderPanel.tsx

key-decisions:
  - "Added problemStatement to CreateProblemSetInput and store INSERT for full field passthrough"
  - "Used navigateTo option pattern on onCreated callback instead of separate callback prop"

patterns-established:
  - "Source choice pattern: Step 0 with card-based selection before standard wizard flow"
  - "navigateTo callback option: extending onCreated(id, options?) for post-creation routing"

requirements-completed: []

# Metrics
duration: 6min
completed: 2026-03-06
---

# Phase 25.1 Plan 01: Scenario-to-Problem-Set Wizard Integration Summary

**4-step wizard with source choice, scenario picker, field pre-fill, training mode lock, and auto-navigation to Understand tab**

## Performance

- **Duration:** 6 min
- **Started:** 2026-03-06T07:45:16Z
- **Completed:** 2026-03-06T07:51:25Z
- **Tasks:** 3
- **Files modified:** 10

## Accomplishments
- CreateProblemSetWizard now starts at Step 0 with "Start Fresh" and "From Scenario" cards
- Selecting a scenario fetches the list with usage badges and pre-fills wizard fields (name, description, echelon=strategic)
- Backend from-scenario endpoint accepts all wizard fields with sensible fallbacks, defaults echelon to strategic
- Scenario-to-problem-set linkage established via updateProblemSetLink after creation
- From-scenario creation auto-navigates to /problem-set/{id}/understand

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix backend from-scenario endpoint and add scenario-problemset linkage** - `c64d89c` (feat)
2. **Task 2: Update frontend services and add Step 0 with scenario picker** - `5b2b51e` (feat)
3. **Task 3: Update onCreated callback signature in parent components** - `62ddc4c` (feat)

## Files Created/Modified
- `backend/src/exercise/scenario-store.ts` - Added updateProblemSetLink, findByProblemSetId, getUsageCounts methods
- `backend/src/api/problem-sets.ts` - Expanded CreateFromScenarioSchema, added scenario-usage-counts and linked-scenario endpoints
- `backend/src/problem-set/types.ts` - Added problemStatement to CreateProblemSetInput
- `backend/src/problem-set/problem-set-store.ts` - Added problem_statement to INSERT and return
- `frontend/src/lib/problem-set-service.ts` - Expanded createFromScenario, added getScenarioUsageCounts and getLinkedScenario
- `frontend/src/components/problem-set/CreateProblemSetWizard.tsx` - Step 0 source choice, scenario picker, pre-fill, mode lock, review row
- `frontend/src/components/problem-set/CreateProblemSetWizard.css` - Source cards, scenario picker, mode locked indicator styles
- `frontend/src/components/problem-set/ProblemSetSwitcher.tsx` - navigateTo option in handleCreated
- `frontend/src/components/problem-set/ProblemSetSelector.tsx` - Updated onCreated signature
- `frontend/src/components/problem-set/CommanderPanel.tsx` - Updated onCreated signature

## Decisions Made
- Added problemStatement field to CreateProblemSetInput and the store INSERT query (was previously always null at creation time) -- enables from-scenario to pass through problem statement
- Used callback options pattern `onCreated(id, { navigateTo })` rather than adding a separate callback prop for navigation

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Added problemStatement to CreateProblemSetInput and store INSERT**
- **Found during:** Task 1 (backend from-scenario endpoint)
- **Issue:** problemStatement was accepted by the schema but never passed to createProblemSet -- the store always set it to null
- **Fix:** Added problemStatement field to CreateProblemSetInput interface and included problem_statement in the INSERT query
- **Files modified:** backend/src/problem-set/types.ts, backend/src/problem-set/problem-set-store.ts
- **Verification:** Backend TypeScript compiles cleanly
- **Committed in:** c64d89c (Task 1 commit)

**2. [Rule 1 - Bug] Fixed Express params type cast**
- **Found during:** Task 1 (linked-scenario endpoint)
- **Issue:** req.params.id typed as string | string[] by Express types, causing TS error
- **Fix:** Cast to string: `req.params.id as string`
- **Files modified:** backend/src/api/problem-sets.ts
- **Verification:** Backend TypeScript compiles cleanly
- **Committed in:** c64d89c (Task 1 commit)

---

**Total deviations:** 2 auto-fixed (1 missing critical, 1 bug)
**Impact on plan:** Both fixes necessary for correctness. No scope creep.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Plan 02 can use getLinkedScenario to resolve scenario for Training Packages view
- Scenario usage counts available for all UI components
- navigateTo pattern established for post-creation routing

---
*Phase: 25.1-training-package-upload-from-scenario-integration*
*Completed: 2026-03-06*
