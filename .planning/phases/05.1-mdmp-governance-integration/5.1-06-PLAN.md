---
phase: 5.1-mdmp-governance-integration
plan: 06
type: execute
wave: 2
depends_on: [5.1-01]
files_modified:
  - backend/src/agents/orders-validator.ts
  - backend/src/agents/types.ts
autonomous: true

must_haves:
  truths:
    - "Orders Validator agent validates OPORD/FRAGO format, consistency, and traceability to commander's intent"
    - "Agent outputs include confidence intervals per INVARIANT 5 (Uncertainty Transparency)"
    - "Validation checks cover format compliance, internal consistency, intent traceability, and degraded execution simulation"
    - "AgentCapability enum extended with OrdersValidation capability"
    - "Agent manifest has maxAutonomy SemiAutonomous"
  artifacts:
    - path: "backend/src/agents/orders-validator.ts"
      provides: "Orders Validator agent implementation"
      contains: "OrdersValidatorAgent"
    - path: "backend/src/agents/types.ts"
      provides: "Extended AgentCapability with OrdersValidation"
      contains: "OrdersValidation"
  key_links:
    - from: "backend/src/agents/orders-validator.ts"
      to: "backend/src/agents/types.ts"
      via: "AgentManifest and AgentCapability imports"
      pattern: "AgentCapability"
    - from: "backend/src/agents/orders-validator.ts"
      to: "backend/src/mdmp/types.ts"
      via: "PlanningProduct and MDMPPhase imports"
      pattern: "PlanningProduct"
---

<objective>
Create the Orders Validator AI agent that validates OPORD/FRAGO format, internal consistency, traceability to commander's intent, and simulates degraded execution scenarios.

Purpose: Ensures orders produced in MDMP Phase 7 (Orders Production) meet doctrinal format standards, are internally consistent, and traceable to commander's intent. Catches format and logic errors before human review.
Output: `backend/src/agents/orders-validator.ts` with agent implementation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05.1-mdmp-governance-integration/5.1-CONTEXT.md
@.planning/mdmp-governance/mdmp-types.ts
@backend/src/agents/types.ts
@backend/src/agents/copilot.ts
@backend/src/mdmp/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Orders Validator agent with output interfaces and manifest</name>
  <files>backend/src/agents/orders-validator.ts</files>
  <action>
Create `backend/src/agents/orders-validator.ts` following the pattern in `copilot.ts`:

1. **Import types** from mdmp/types.ts (PlanningProduct, MDMPPhase) and agents/types.ts (AgentManifest, AgentCapability).

2. **Output interfaces**:

   ```typescript
   export interface FormatIssue {
     section: string;
     issue: string;
     severity: 'critical' | 'major' | 'minor';
     suggestion: string;
     doctrinalReference: string;
   }

   export interface ConsistencyIssue {
     elementA: string;
     elementB: string;
     contradiction: string;
     severity: 'critical' | 'major' | 'minor';
     detectionConfidence: number;
   }

   export interface IntentTraceability {
     intentElement: string;
     tracedToSection: string | null;
     traceabilityScore: number;
     gaps: string[];
   }

   export interface DegradedScenario {
     scenario: string;
     affectedParagraphs: string[];
     mitigationPresent: boolean;
     recommendation: string;
   }

   export interface OrdersValidationOutput {
     /** Product validated (OPORD, FRAGO, etc.) */
     productType: PlanningProduct;
     /** Format compliance issues */
     formatIssues: FormatIssue[];
     /** Internal consistency issues */
     consistencyIssues: ConsistencyIssue[];
     /** Commander's intent traceability analysis */
     intentTraceability: IntentTraceability[];
     /** Degraded execution scenario analysis */
     degradedScenarios: DegradedScenario[];
     /** Overall validation score (0-100) */
     overallScore: number;
     /** Confidence in validation completeness (0-1) per INVARIANT 5 */
     validationConfidence: number;
     /** Confidence interval bounds */
     confidenceBounds: { lower: number; upper: number };
     /** Summary of findings */
     summary: string;
     /** Whether the order passes minimum validation threshold */
     passesMinimumThreshold: boolean;
   }
   ```

3. **Agent manifest** with:
   - agentId: 'orders-validator'
   - capabilities: [AgentCapability.OrdersValidation]
   - maxAutonomy: AutonomyLevel.SemiAutonomous
   - System prompt covering: OPORD 5-paragraph format (Situation, Mission, Execution, Sustainment, C2), FRAGO format, ATP 5-0.1 references, degraded execution scenarios (DDIL, loss of comms, loss of leader, reduced force)

4. **Core function stub**:
   ```typescript
   export async function validateOrder(
     orderText: string,
     commanderIntent: string,
     missionId: string,
     productType: PlanningProduct,
   ): Promise<OrdersValidationOutput>
   ```
  </action>
  <verify>
Run `npx tsc --noEmit` in backend/ to verify TypeScript compilation.
  </verify>
  <done>
Orders Validator agent created with comprehensive validation output interfaces. Manifest defines agent with SemiAutonomous maxAutonomy. INVARIANT 5 enforced via confidence fields.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add OrdersValidation to AgentCapability enum</name>
  <files>backend/src/agents/types.ts</files>
  <action>
Add to the MDMP Agent Capabilities section of the AgentCapability enum:
```typescript
/** Validate orders format, consistency, and intent traceability */
OrdersValidation = 'OrdersValidation',
```
  </action>
  <verify>
Run `npx tsc --noEmit` in backend/ to verify TypeScript compilation.
  </verify>
  <done>
AgentCapability.OrdersValidation added. Compilation passes.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes in backend/
2. OrdersValidationOutput includes validationConfidence and confidenceBounds (INVARIANT 5)
3. Validation covers format, consistency, intent traceability, degraded scenarios
4. Agent manifest has maxAutonomy: SemiAutonomous
5. AgentCapability.OrdersValidation exists in types.ts
</verification>

<success_criteria>
- Orders Validator agent validates OPORD 5-paragraph format
- Internal consistency checking between order elements
- Commander's intent traceability scoring
- Degraded execution scenario simulation
- All outputs include confidence intervals (INVARIANT 5)
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-mdmp-governance-integration/5.1-06-SUMMARY.md`
</output>
