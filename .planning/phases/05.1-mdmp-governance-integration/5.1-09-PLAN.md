---
phase: 5.1-mdmp-governance-integration
plan: 09
type: execute
wave: 2
depends_on: [5.1-01]
files_modified:
  - backend/src/agents/problem-framing.ts
  - backend/src/agents/types.ts
autonomous: true

must_haves:
  truths:
    - "Problem Framing agent generates alternative problem framings from multiple theoretical perspectives"
    - "Agent outputs include confidence intervals per INVARIANT 5"
    - "Framings cover military, diplomatic, economic, informational, and social perspectives"
    - "Each framing identifies different root causes, stakeholders, and intervention points"
    - "AgentCapability enum extended with ProblemFraming capability"
    - "Agent manifest has maxAutonomy NotAutonomous (human-led activity per safety matrix)"
  artifacts:
    - path: "backend/src/agents/problem-framing.ts"
      provides: "Problem Framing agent implementation"
      contains: "ProblemFramingAgent"
    - path: "backend/src/agents/types.ts"
      provides: "Extended AgentCapability with ProblemFraming"
      contains: "ProblemFraming"
  key_links:
    - from: "backend/src/agents/problem-framing.ts"
      to: "backend/src/agents/types.ts"
      via: "AgentManifest imports"
      pattern: "AgentManifest"
---

<objective>
Create the Problem Framing AI agent that generates alternative problem framings from multiple perspectives to challenge planners' default mental models.

Purpose: MDMP Phase 2 (Mission Analysis) requires problem framing before COA development. Per the safety matrix, PROBLEM_FRAMING is HYBRID_HUMAN_LED -- human selects and owns the framing, AI offers alternatives. This agent prevents tunnel vision by presenting multiple perspectives.
Output: `backend/src/agents/problem-framing.ts` with agent implementation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05.1-mdmp-governance-integration/5.1-CONTEXT.md
@.planning/mdmp-governance/mdmp-types.ts
@backend/src/agents/types.ts
@backend/src/agents/copilot.ts
@backend/src/mdmp/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Problem Framing agent with multi-perspective output interfaces</name>
  <files>backend/src/agents/problem-framing.ts</files>
  <action>
Create `backend/src/agents/problem-framing.ts` following the pattern in `copilot.ts`:

1. **Import types** from mdmp/types.ts and agents/types.ts.

2. **Output interfaces**:

   ```typescript
   export type PerspectiveType =
     | 'military'
     | 'diplomatic'
     | 'economic'
     | 'informational'
     | 'social'
     | 'legal'
     | 'adversary'
     | 'coalition_partner'
     | 'local_population'
     | 'historical_analogy';

   export interface AlternativeFraming {
     perspectiveType: PerspectiveType;
     framingStatement: string;
     rootCauses: string[];
     keyStakeholders: Array<{ name: string; interest: string; influence: 'high' | 'medium' | 'low' }>;
     interventionPoints: string[];
     assumptions: string[];
     blindSpots: string[];
     framingConfidence: number;
     confidenceBounds: { lower: number; upper: number };
   }

   export interface FramingComparison {
     framingA: string;
     framingB: string;
     sharedElements: string[];
     contradictions: string[];
     complementaryInsights: string[];
   }

   export interface ProblemFramingOutput {
     /** The default/dominant framing identified */
     defaultFraming: AlternativeFraming;
     /** Alternative framings from different perspectives */
     alternativeFramings: AlternativeFraming[];
     /** Comparison between framings */
     framingComparisons: FramingComparison[];
     /** Hidden assumptions in the default framing */
     hiddenAssumptions: string[];
     /** Recommended framing (human must choose, this is AI suggestion) */
     recommendedFramingPerspective: PerspectiveType | null;
     /** Reasoning for recommendation */
     recommendationReasoning: string;
     /** Confidence in framing completeness (0-1) per INVARIANT 5 */
     completenessConfidence: number;
     /** Known blind spots the agent cannot address */
     knownBlindSpots: string[];
   }
   ```

3. **Agent manifest** with:
   - agentId: 'problem-framing'
   - capabilities: [AgentCapability.ProblemFraming]
   - maxAutonomy: AutonomyLevel.NotAutonomous (PROBLEM_FRAMING is HYBRID_HUMAN_LED per safety matrix)
   - System prompt covering: multi-perspective problem framing, DIME framework (Diplomatic, Informational, Military, Economic), adversary perspective taking, historical analogy identification, assumption surfacing in problem statements

4. **Core function stub**:
   ```typescript
   export async function generateFramings(
     situationDescription: string,
     missionStatement: string,
     commanderIntent: string,
     existingAssumptions: string[],
   ): Promise<ProblemFramingOutput>
   ```
  </action>
  <verify>
Run `npx tsc --noEmit` in backend/ to verify TypeScript compilation.
  </verify>
  <done>
Problem Framing agent created with multi-perspective framing output. maxAutonomy correctly set to NotAutonomous per safety matrix. INVARIANT 5 enforced via confidence fields. Compilation passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add ProblemFraming to AgentCapability enum</name>
  <files>backend/src/agents/types.ts</files>
  <action>
Add to the MDMP Agent Capabilities section of the AgentCapability enum:
```typescript
/** Generate alternative problem framings from multiple perspectives */
ProblemFraming = 'ProblemFraming',
```
  </action>
  <verify>
Run `npx tsc --noEmit` in backend/ to verify TypeScript compilation.
  </verify>
  <done>
AgentCapability.ProblemFraming added. Compilation passes.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes in backend/
2. 10 perspective types covering DIME + adversary + coalition + historical
3. AlternativeFraming includes root causes, stakeholders, intervention points, assumptions, blind spots
4. Agent manifest maxAutonomy is NotAutonomous (matching PROBLEM_FRAMING safety matrix entry)
5. All outputs include confidence intervals (INVARIANT 5)
</verification>

<success_criteria>
- Multi-perspective problem framing with 10 perspective types
- Default framing identification + alternative generation
- Framing comparison showing shared elements and contradictions
- maxAutonomy: NotAutonomous (human-led per safety matrix)
- All outputs include confidence intervals (INVARIANT 5)
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-mdmp-governance-integration/5.1-09-SUMMARY.md`
</output>
