---
phase: 5.1-mdmp-governance-integration
plan: 03
type: execute
wave: 2
depends_on: [5.1-01]
files_modified:
  - near-contracts/src/mdmp/workflow.rs
  - near-contracts/src/mdmp/safety.rs
  - near-contracts/src/mdmp/mod.rs
autonomous: true

must_haves:
  truths:
    - "MDMPWorkflow tracks current phase per mission and enforces gate satisfaction before transition"
    - "GateStatus records who satisfied each gate and when with proposal linkage"
    - "validate_phase_complete returns unsatisfied gate IDs when gates are incomplete"
    - "execute_transition records full audit trail with satisfied gates, assumptions, and challenges"
    - "validate_autonomy_assignment enforces INVARIANT 8 (FullyDelegated scope) and INVARIANT 9 (safety matrix)"
    - "PhaseTransitionRecord captures from_phase, to_phase, authorized_by, proposal_id, and all gate/assumption/challenge metadata"
  artifacts:
    - path: "near-contracts/src/mdmp/workflow.rs"
      provides: "MDMP Workflow Engine with phase progression enforcement"
      contains: "MDMPWorkflow"
    - path: "near-contracts/src/mdmp/safety.rs"
      provides: "Safety matrix validation function"
      contains: "validate_autonomy_assignment"
    - path: "near-contracts/src/mdmp/mod.rs"
      provides: "Updated module declaration with workflow and safety"
      contains: "pub mod workflow"
  key_links:
    - from: "near-contracts/src/mdmp/workflow.rs"
      to: "near-contracts/src/mdmp/types.rs"
      via: "Uses MDMPPhase for phase tracking"
      pattern: "MDMPPhase"
    - from: "near-contracts/src/mdmp/safety.rs"
      to: "near-contracts/src/dao/types.rs"
      via: "Uses AutonomyLevel and ProposalKind for validation"
      pattern: "AutonomyLevel"
    - from: "near-contracts/src/mdmp/safety.rs"
      to: "near-contracts/src/mdmp/types.rs"
      via: "Uses ActivityCategory for safety matrix checks"
      pattern: "ActivityCategory"
---

<objective>
Create the MDMP Workflow Engine with phase progression enforcement and safety matrix validation.

Purpose: Enforce INVARIANT 2 (Phase Progression gates must be satisfied before advancing), INVARIANT 4 (Red Team Completeness), INVARIANT 8 (FullyDelegated scope restriction), and INVARIANT 9 (Safety Matrix Enforcement).
Output: `workflow.rs` with MDMPWorkflow contract + `safety.rs` with validate_autonomy_assignment function.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.1-mdmp-governance-integration/5.1-CONTEXT.md
@.planning/mdmp-governance/mdmp-contracts.rs
@near-contracts/src/mdmp/mod.rs
@near-contracts/src/mdmp/types.rs
@near-contracts/src/dao/types.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MDMP Workflow Engine with gate enforcement and phase transitions</name>
  <files>near-contracts/src/mdmp/workflow.rs</files>
  <action>
Create `near-contracts/src/mdmp/workflow.rs` with:

1. **GateStatus struct** with `#[near(serializers = [json, borsh])]`, Clone, Debug:
   - gate_id: String
   - gate_type: String (maps to GateType enum in TypeScript)
   - satisfied: bool
   - satisfied_by: Option<AccountId>
   - satisfied_at: Option<u64>
   - proposal_id: Option<u64>

2. **PhaseTransitionRecord struct** with same derives:
   - from_phase: String
   - to_phase: String
   - authorized_by: AccountId
   - transitioned_at: u64
   - proposal_id: u64
   - satisfied_gates: Vec<String>
   - accepted_assumptions: Vec<String>
   - addressed_challenges: Vec<String>

3. **MDMPWorkflow struct** with `#[near(serializers = [borsh])]`:
   - current_phases: LookupMap<String, String> (mission_id -> phase string)
   - gate_statuses: LookupMap<String, GateStatus> (key: "mission_id:phase:gate_id")
   - phase_gates: LookupMap<String, Vec<String>> (key: "mission_id:phase" -> gate_ids)
   - transitions: LookupMap<String, Vec<PhaseTransitionRecord>> (mission_id -> records)
   - mission_daos: LookupMap<String, String> (mission_id -> dao_id)

4. **MDMPWorkflow::new()** with unique storage key prefixes (b"mdmp_phases", b"mdmp_gates", b"mdmp_phase_gates", b"mdmp_transitions", b"mdmp_daos").

5. **create_workflow(&mut self, mission_id, dao_id, initiator)**: Initialize workflow at phase_1_receipt_of_mission. Log MDMP_WORKFLOW_CREATED event.

6. **register_gate(&mut self, mission_id, phase, gate: GateStatus)**: Store gate status and track gate ID for the phase.

7. **satisfy_gate(&mut self, mission_id, phase, gate_id, satisfied_by, proposal_id)** -> Result<(), String>:
   - Validate gate exists and is not already satisfied
   - Set satisfied=true, record who/when/proposal
   - Log MDMP_GATE_SATISFIED event

8. **validate_phase_complete(&self, mission_id, phase)** -> Result<(), Vec<String>>:
   - INVARIANT 2: Check all registered gates for the phase are satisfied
   - Return Ok(()) if all satisfied, Err(unsatisfied_gate_ids) if not

9. **request_phase_transition(&self, mission_id, to_phase)** -> Result<(), String>:
   - Get current phase, validate all gates satisfied via validate_phase_complete
   - Return error with unsatisfied gates if incomplete

10. **execute_transition(&mut self, mission_id, to_phase, authorized_by, proposal_id, satisfied_gates, accepted_assumptions, addressed_challenges)**:
    - Record PhaseTransitionRecord in transitions list
    - Update current_phases to new phase
    - Log MDMP_PHASE_TRANSITION event with full metadata

11. **get_current_phase(&self, mission_id)** -> Option<String>

12. **get_transitions(&self, mission_id)** -> Vec<PhaseTransitionRecord>

13. **Tests**:
    - test_workflow_creation_starts_at_phase_1
    - test_gate_satisfaction_flow
    - test_phase_complete_validation (with unsatisfied gates)
    - test_phase_transition_requires_all_gates (INVARIANT 2)
    - test_execute_transition_records_audit_trail
    - test_satisfy_already_satisfied_gate_fails

Reference: `.planning/mdmp-governance/mdmp-contracts.rs` lines 450-681 for exact specification.
  </action>
  <verify>
Run `cargo check` in near-contracts/ to verify compilation.
Run `cargo test` in near-contracts/ to verify all tests pass.
  </verify>
  <done>
MDMPWorkflow contract exists with phase progression enforcement, gate tracking, and transition audit trail. INVARIANT 2 enforced via validate_phase_complete. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create safety matrix validation and update module declaration</name>
  <files>
near-contracts/src/mdmp/safety.rs
near-contracts/src/mdmp/mod.rs
  </files>
  <action>
1. **Create `near-contracts/src/mdmp/safety.rs`** with:

   **validate_autonomy_assignment(autonomy: &AutonomyLevel, category: &ActivityCategory, proposal_kind: &ProposalKind)** -> Result<(), String>:

   - INVARIANT 8: If autonomy is FullyDelegated, check category.permits_fully_delegated() returns true. Also check proposal_kind.prohibits_fully_delegated() returns false. Return descriptive error if either fails.
   - INVARIANT 9: If category.requires_human_in_loop(), autonomy MUST be NotAutonomous. Return error otherwise.
   - Existing INVARIANT 1: If proposal_kind.requires_human_in_loop(), autonomy MUST be NotAutonomous. Return error otherwise.
   - Return Ok(()) if all checks pass.

   Import AutonomyLevel and ProposalKind from `crate::dao::types` and ActivityCategory from `super::types`.

   **Tests**:
   - test_fully_delegated_scope_restriction (DataAggregation OK, MissionAnalysis fails)
   - test_fully_delegated_with_authority_proposal_fails (FullyDelegated + PhaseTransition)
   - test_human_only_categories (RiskJudgment, EthicalLegal, AuthorityDecision all require NotAutonomous)
   - test_strike_authorization_invariant_preserved (always requires NotAutonomous regardless of category)
   - test_valid_hybrid_assignments (SemiAutonomous with COAGeneration passes)

   Reference: `.planning/mdmp-governance/mdmp-contracts.rs` lines 684-740 for exact specification.

2. **Update `near-contracts/src/mdmp/mod.rs`** to include all submodules:

   ```rust
   pub mod types;
   pub mod assumptions;
   pub mod workflow;
   pub mod safety;
   ```
  </action>
  <verify>
Run `cargo check` in near-contracts/ to verify compilation (imports from dao::types must resolve).
Run `cargo test` in near-contracts/ to verify all tests pass.
  </verify>
  <done>
Safety matrix validation function enforces INVARIANT 8 (FullyDelegated scope) and INVARIANT 9 (safety matrix). Module declaration exports all 4 MDMP submodules. All tests pass.
  </done>
</task>

</tasks>

<verification>
1. `cargo check` in near-contracts/ passes
2. `cargo test` in near-contracts/ passes (existing + new tests)
3. MDMPWorkflow creates workflows starting at phase_1_receipt_of_mission
4. Gate registration, satisfaction, and validation work correctly
5. Phase transitions blocked when gates are unsatisfied (INVARIANT 2)
6. Phase transitions record full audit trail
7. validate_autonomy_assignment enforces INVARIANT 8 (FullyDelegated scope)
8. validate_autonomy_assignment enforces INVARIANT 9 (human-only categories)
9. Existing INVARIANT 1 (StrikeAuthorization) preserved
</verification>

<success_criteria>
- MDMPWorkflow manages phase progression per mission
- Gates must be satisfied before phase transition (INVARIANT 2)
- PhaseTransitionRecord captures complete audit trail
- validate_autonomy_assignment blocks FullyDelegated for non-permitted categories (INVARIANT 8)
- validate_autonomy_assignment blocks AI autonomy for HUMAN_ONLY categories (INVARIANT 9)
- StrikeAuthorization invariant preserved through safety checks
- All tests pass, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-mdmp-governance-integration/5.1-03-SUMMARY.md`
</output>
