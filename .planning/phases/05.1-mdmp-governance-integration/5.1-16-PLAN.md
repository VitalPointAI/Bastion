---
phase: 5.1-mdmp-governance-integration
plan: 16
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/mdmp/activity-registry.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All 65 MDMP activities are registered in the Activity Registry"
    - "getPhaseStatistics() returns correct counts for all 9 phases (0-8)"
    - "getActivitiesByPhase() returns activities for every phase"
  artifacts:
    - path: "backend/src/mdmp/activity-registry.ts"
      provides: "Complete 65-activity MDMP registry"
      contains: "PHASE_1_ACTIVITIES"
  key_links:
    - from: "backend/src/mdmp/activity-registry.ts"
      to: "backend/src/mdmp/types.ts"
      via: "MDMPPhase, ActivityCategory, etc. imports"
      pattern: "import.*from.*types"
---

<objective>
Complete the MDMP Activity Registry by adding the 55 missing Phase 1-8 activity definitions.

Purpose: The activity registry currently only has 10 Phase 0 activities. The phase goal requires all 65 MDMP activities registered. Reference files with complete definitions exist in `.planning/mdmp-governance/`.

Output: `backend/src/mdmp/activity-registry.ts` with all 65 activities and ALL_ACTIVITIES.length === 65.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05.1-mdmp-governance-integration/5.1-VERIFICATION.md
@backend/src/mdmp/activity-registry.ts
@backend/src/mdmp/types.ts
@.planning/mdmp-governance/mdmp-activity-registry.ts
@.planning/mdmp-governance/mdmp-activity-registry-p3-8.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 1-8 activity definitions to activity-registry.ts</name>
  <files>backend/src/mdmp/activity-registry.ts</files>
  <action>
    Copy activity definitions from the reference files into backend/src/mdmp/activity-registry.ts:

    1. Read `.planning/mdmp-governance/mdmp-activity-registry.ts` lines 532-1711 (PHASE_1_ACTIVITIES and PHASE_2_ACTIVITIES arrays)
    2. Read `.planning/mdmp-governance/mdmp-activity-registry-p3-8.ts` lines 21-1497 (PHASE_3_ACTIVITIES through PHASE_8_ACTIVITIES arrays)
    3. Insert all 8 phase constant arrays (PHASE_1_ACTIVITIES through PHASE_8_ACTIVITIES) into `backend/src/mdmp/activity-registry.ts` BEFORE the ALL_ACTIVITIES export (currently at line 545)
    4. Place each array after the PHASE_0_ACTIVITIES closing bracket and the TODO comment block (lines 536-549)

    IMPORTANT adaptation notes:
    - The reference files import from `'./mdmp-types.js'` but the backend file imports from `'./types.js'`. All type references (MDMPPhase, ActivityCategory, AuthorityDesignation, ControlPosture, DAOTier, AgentRole, AgentOutputType, GateType, VerificationMechanism, EvidenceType, RetentionPeriod, AuditRequirement, ChallengeType, ResponseDepth, CoalitionImpact, SharingLevel, ImplementationStatus, EscalationPath, PlanningProduct) are already imported in the existing file. Do NOT add new import statements.
    - Remove the import statements from the reference file content when copying (only copy the const arrays)
    - Check the existing imports at the top of activity-registry.ts. If any types used in the reference arrays are missing from the import (e.g., PlanningProduct), add them to the existing import block.
    - The reference files may use `PlanningProduct` in some activity definitions. Verify this type exists in the imports. If not, add it.

    5. Update the ALL_ACTIVITIES export to include all phase arrays:
    ```typescript
    export const ALL_ACTIVITIES: MDMPActivity[] = [
      ...PHASE_0_ACTIVITIES,
      ...PHASE_1_ACTIVITIES,
      ...PHASE_2_ACTIVITIES,
      ...PHASE_3_ACTIVITIES,
      ...PHASE_4_ACTIVITIES,
      ...PHASE_5_ACTIVITIES,
      ...PHASE_6_ACTIVITIES,
      ...PHASE_7_ACTIVITIES,
      ...PHASE_8_ACTIVITIES,
    ];
    ```

    6. Remove the TODO comment block (lines 538-548) since it will be resolved.

    Expected activity counts per phase (from reference file comments):
    - Phase 0: 11 activities (wait - current file has 10; check reference for Phase 0 count)
    - Phase 1: 6 activities
    - Phase 2: 15 activities
    - Phase 3: 8 activities
    - Phase 4: 6 activities
    - Phase 5: 6 activities
    - Phase 6: 3 activities
    - Phase 7: 5 activities
    - Phase 8: 5 activities
    Total: 65

    Note: The reference file lists Phase 0 as 11 activities but the current implementation has 10. Read the reference PHASE_0_ACTIVITIES (lines 35-531 of mdmp-activity-registry.ts) and compare with the existing backend implementation. If Phase 0 is missing an activity, add it. The exact total MUST be 65.
  </action>
  <verify>
    After editing, verify:
    1. TypeScript compiles: `cd backend && npx tsc --noEmit`
    2. Count verification: Add a temporary console.log or use grep to count activity IDs:
       `grep -c "id: 'MDMP-" backend/src/mdmp/activity-registry.ts` should return 65
    3. All phase arrays exist: `grep "PHASE_._ACTIVITIES" backend/src/mdmp/activity-registry.ts` should show phases 0-8
    4. ALL_ACTIVITIES spreads all 9 arrays: `grep "\.\.\.PHASE_" backend/src/mdmp/activity-registry.ts` should show 9 spread operators
  </verify>
  <done>
    ALL_ACTIVITIES contains exactly 65 activities covering all 9 MDMP phases (0-8). TypeScript compiles without errors. getPhaseStatistics() and getActivitiesByPhase() work for all phases.
  </done>
</task>

</tasks>

<verification>
1. `cd backend && npx tsc --noEmit` passes
2. `grep -c "id: 'MDMP-" backend/src/mdmp/activity-registry.ts` returns 65
3. ALL_ACTIVITIES array spreads all 9 phase arrays (PHASE_0 through PHASE_8)
4. No TODO comments remain about missing activities
</verification>

<success_criteria>
- Activity registry contains all 65 MDMP activities
- TypeScript compiles without errors
- All 9 phase arrays (PHASE_0 through PHASE_8) are defined and exported
- ALL_ACTIVITIES.length === 65
- getPhaseStatistics() returns non-empty results for all phases
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-mdmp-governance-integration/5.1-16-SUMMARY.md`
</output>
