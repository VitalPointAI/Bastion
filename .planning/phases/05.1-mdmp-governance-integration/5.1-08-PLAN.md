---
phase: 5.1-mdmp-governance-integration
plan: 08
type: execute
wave: 2
depends_on: [5.1-01]
files_modified:
  - backend/src/agents/data-bias-detector.ts
  - backend/src/agents/types.ts
autonomous: true

must_haves:
  truths:
    - "Data Bias Detector agent identifies statistical bias, coverage gaps, and data staleness in intelligence feeds"
    - "Agent outputs include confidence intervals per INVARIANT 5"
    - "Bias detection covers selection bias, confirmation bias, anchoring bias, survivorship bias, and cultural bias"
    - "Staleness detection flags data beyond configurable freshness thresholds"
    - "AgentCapability enum extended with DataBiasDetection capability"
  artifacts:
    - path: "backend/src/agents/data-bias-detector.ts"
      provides: "Data Bias Detector agent implementation"
      contains: "DataBiasDetectorAgent"
    - path: "backend/src/agents/types.ts"
      provides: "Extended AgentCapability with DataBiasDetection"
      contains: "DataBiasDetection"
  key_links:
    - from: "backend/src/agents/data-bias-detector.ts"
      to: "backend/src/agents/types.ts"
      via: "AgentManifest imports"
      pattern: "AgentManifest"
---

<objective>
Create the Data Bias Detector AI agent that identifies statistical bias, data staleness, coverage gaps, and systematic errors in intelligence data feeds.

Purpose: Intelligence analysis quality depends on data quality. This agent flags when the data underlying AI agent outputs may be biased, stale, or incomplete, supporting better human decision-making.
Output: `backend/src/agents/data-bias-detector.ts` with agent implementation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05.1-mdmp-governance-integration/5.1-CONTEXT.md
@.planning/mdmp-governance/mdmp-types.ts
@backend/src/agents/types.ts
@backend/src/agents/copilot.ts
@backend/src/mdmp/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Data Bias Detector agent with bias analysis interfaces</name>
  <files>backend/src/agents/data-bias-detector.ts</files>
  <action>
Create `backend/src/agents/data-bias-detector.ts` following the pattern in `copilot.ts`:

1. **Import types** from mdmp/types.ts and agents/types.ts.

2. **Output interfaces**:

   ```typescript
   export type BiasType =
     | 'selection_bias'
     | 'confirmation_bias'
     | 'anchoring_bias'
     | 'survivorship_bias'
     | 'cultural_bias'
     | 'temporal_bias'
     | 'geographic_bias'
     | 'source_bias'
     | 'availability_bias';

   export interface DetectedBias {
     biasType: BiasType;
     description: string;
     affectedData: string[];
     severity: 'critical' | 'major' | 'minor';
     detectionConfidence: number;
     confidenceBounds: { lower: number; upper: number };
     mitigationSuggestion: string;
   }

   export interface StalenessReport {
     dataSource: string;
     lastUpdated: number;
     ageHours: number;
     freshnessThresholdHours: number;
     isStale: boolean;
     impactIfStale: string;
   }

   export interface CoverageGap {
     domain: string;
     expectedCoverage: string;
     actualCoverage: string;
     gapDescription: string;
     impactOnAnalysis: string;
     severity: 'critical' | 'major' | 'minor';
   }

   export interface BiasAnalysisOutput {
     /** Detected biases */
     detectedBiases: DetectedBias[];
     /** Data staleness findings */
     stalenessReports: StalenessReport[];
     /** Coverage gaps identified */
     coverageGaps: CoverageGap[];
     /** Overall data quality score (0-100) */
     dataQualityScore: number;
     /** Confidence in bias analysis completeness (0-1) per INVARIANT 5 */
     analysisConfidence: number;
     /** Summary of findings */
     summary: string;
     /** Recommendations for improving data quality */
     recommendations: string[];
     /** Whether any critical biases were found */
     hasCriticalBiases: boolean;
   }
   ```

3. **Agent manifest** with:
   - agentId: 'data-bias-detector'
   - capabilities: [AgentCapability.DataBiasDetection]
   - maxAutonomy: AutonomyLevel.SemiAutonomous
   - System prompt covering: statistical bias identification, data freshness assessment, coverage gap analysis, cultural bias in intelligence, source diversity assessment

4. **Core function stub**:
   ```typescript
   export async function analyzeBias(
     dataSources: Array<{ sourceId: string; sourceType: string; lastUpdated: number; content: string }>,
     analysisContext: string,
   ): Promise<BiasAnalysisOutput>
   ```
  </action>
  <verify>
Run `npx tsc --noEmit` in backend/ to verify TypeScript compilation.
  </verify>
  <done>
Data Bias Detector agent created with comprehensive bias, staleness, and coverage gap analysis. INVARIANT 5 enforced via confidence fields. Compilation passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add DataBiasDetection to AgentCapability enum</name>
  <files>backend/src/agents/types.ts</files>
  <action>
Add to the MDMP Agent Capabilities section of the AgentCapability enum:
```typescript
/** Detect statistical bias, data staleness, and coverage gaps */
DataBiasDetection = 'DataBiasDetection',
```
  </action>
  <verify>
Run `npx tsc --noEmit` in backend/ to verify TypeScript compilation.
  </verify>
  <done>
AgentCapability.DataBiasDetection added. Compilation passes.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes in backend/
2. BiasAnalysisOutput includes analysisConfidence (INVARIANT 5)
3. 9 bias types covered including cultural and geographic bias
4. Staleness detection with configurable freshness thresholds
5. Coverage gap analysis with severity classification
6. Agent manifest has maxAutonomy: SemiAutonomous
</verification>

<success_criteria>
- Bias detection covers 9 bias types relevant to intelligence analysis
- Staleness detection with age calculation and threshold comparison
- Coverage gap identification with impact assessment
- All outputs include confidence intervals (INVARIANT 5)
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-mdmp-governance-integration/5.1-08-SUMMARY.md`
</output>
