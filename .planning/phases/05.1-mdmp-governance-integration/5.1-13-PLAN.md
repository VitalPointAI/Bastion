---
phase: 5.1-mdmp-governance-integration
plan: 13
type: execute
wave: 2
depends_on: [5.1-01]
files_modified:
  - backend/src/raft/templates/mdmp-task-extraction.ts
  - backend/src/raft/templates/mdmp-ccir-generation.ts
  - backend/src/raft/templates/mdmp-opord-generation.ts
  - backend/src/raft/templates/mdmp-ipb-analysis.ts
  - backend/src/raft/templates/mdmp-wargame-extraction.ts
  - backend/src/raft/templates/index.ts
autonomous: true

must_haves:
  truths:
    - "Task extraction template extracts tasks, constraints, and specified/implied tasks from orders/directives"
    - "CCIR generation template produces Commander's Critical Information Requirements from mission analysis"
    - "OPORD generation template produces 5-paragraph OPORD from COA and planning products"
    - "IPB analysis template covers weather, terrain, civil considerations per ATP 2-01.3"
    - "Wargame extraction template captures decision points, HPTs, and IRs from wargame outputs"
    - "All 5 templates are exported via index.ts barrel file and importable by agents"
  artifacts:
    - path: "backend/src/raft/templates/mdmp-task-extraction.ts"
      provides: "RAFT template for extracting tasks from orders"
      contains: "TaskExtractionTemplate"
    - path: "backend/src/raft/templates/mdmp-ccir-generation.ts"
      provides: "RAFT template for CCIR generation"
      contains: "CCIRGenerationTemplate"
    - path: "backend/src/raft/templates/mdmp-opord-generation.ts"
      provides: "RAFT template for OPORD generation"
      contains: "OPORDGenerationTemplate"
    - path: "backend/src/raft/templates/mdmp-ipb-analysis.ts"
      provides: "RAFT template for IPB analysis"
      contains: "IPBAnalysisTemplate"
    - path: "backend/src/raft/templates/mdmp-wargame-extraction.ts"
      provides: "RAFT template for wargame output extraction"
      contains: "WargameExtractionTemplate"
    - path: "backend/src/raft/templates/index.ts"
      provides: "Barrel export for all MDMP RAFT templates"
      contains: "export"
  key_links:
    - from: "backend/src/raft/templates/mdmp-task-extraction.ts"
      to: "backend/src/mdmp/types.ts"
      via: "MDMPPhase and PlanningProduct references"
      pattern: "MDMPPhase"
    - from: "backend/src/raft/templates/index.ts"
      to: "backend/src/raft/templates/mdmp-*.ts"
      via: "Barrel re-export of all template constants"
      pattern: "export.*TEMPLATE"
---

<objective>
Create MDMP-specific RAFT (Retrieval Augmented Fine-Tuning) pipeline templates for extracting structured data from military planning documents.

Purpose: The RAFT pipeline transforms unstructured military documents into structured data used by MDMP agents and governance. These 5 templates cover the highest-value extraction tasks: task extraction, CCIR generation, OPORD production, IPB analysis, and wargame output capture.
Output: 5 RAFT template files in `backend/src/raft/templates/`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05.1-mdmp-governance-integration/5.1-CONTEXT.md
@.planning/mdmp-governance/mdmp-types.ts
@.planning/mdmp-governance/mdmp-roadmap-integration.md
@backend/src/mdmp/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Task Extraction and CCIR Generation RAFT templates</name>
  <files>
backend/src/raft/templates/mdmp-task-extraction.ts
backend/src/raft/templates/mdmp-ccir-generation.ts
  </files>
  <action>
0. **Create the RAFT templates directory** if it does not exist:
   ```bash
   mkdir -p backend/src/raft/templates
   ```

Check for existing RAFT template patterns:
```bash
ls backend/src/raft/ 2>/dev/null
```

Follow any existing RAFT template patterns. If no existing patterns, define a template structure.

1. **Create `backend/src/raft/templates/mdmp-task-extraction.ts`**:

   Define a RAFT extraction template for MDMP-1-02 (Extract tasks, constraints, specified/implied tasks from higher orders):

   ```typescript
   export interface TaskExtractionInput {
     documentText: string;
     documentType: 'OPORD' | 'FRAGO' | 'WARNO' | 'directive';
     missionId: string;
     currentPhase: string;
   }

   export interface ExtractedTask {
     taskId: string;
     taskType: 'specified' | 'implied' | 'essential';
     description: string;
     sourceSection: string;
     sourceExcerpt: string;
     constraints: string[];
     associatedUnit: string | null;
     extractionConfidence: number;
   }

   export interface TaskExtractionOutput {
     specifiedTasks: ExtractedTask[];
     impliedTasks: ExtractedTask[];
     essentialTasks: ExtractedTask[];
     constraints: Array<{ constraint: string; source: string; severity: 'binding' | 'limiting' }>;
     coordRequirements: string[];
     extractionCompleteness: number;
   }

   export const TASK_EXTRACTION_TEMPLATE = {
     name: 'mdmp-task-extraction',
     description: 'Extract tasks, constraints, and coordination requirements from military orders and directives',
     mdmpPhase: 'phase_1_receipt_of_mission',
     mdmpActivity: 'MDMP-1-02',
     inputSchema: { ... }, // JSON Schema for TaskExtractionInput
     outputSchema: { ... }, // JSON Schema for TaskExtractionOutput
     systemPrompt: `Extract all specified tasks, implied tasks, essential tasks, and constraints from the military document...`,
     examples: [
       // 1-2 few-shot examples of input -> output
     ],
   };
   ```

2. **Create `backend/src/raft/templates/mdmp-ccir-generation.ts`**:

   Define RAFT template for MDMP-2-07 (Generate CCIR from mission analysis):

   ```typescript
   export interface CCIRInput {
     missionStatement: string;
     commanderIntent: string;
     identifiedGaps: string[];
     currentAssumptions: string[];
     adversaryCapabilities: string;
   }

   export interface CCIR {
     ccirId: string;
     type: 'PIR' | 'FFIR' | 'EEFI';
     requirement: string;
     justification: string;
     collectionPlan: string;
     latestTimeOfValue: string;
     linkedAssumptions: string[];
     priority: 'immediate' | 'priority' | 'routine';
     generationConfidence: number;
   }

   export interface CCIRGenerationOutput {
     priorityIntelligenceRequirements: CCIR[];
     friendlyForceInfoRequirements: CCIR[];
     essentialElementsFriendlyInfo: CCIR[];
     summary: string;
     completenessConfidence: number;
   }

   export const CCIR_GENERATION_TEMPLATE = {
     name: 'mdmp-ccir-generation',
     description: 'Generate Commander Critical Information Requirements from mission analysis products',
     mdmpPhase: 'phase_2_mission_analysis',
     mdmpActivity: 'MDMP-2-07',
     // ... schema, prompt, examples
   };
   ```
  </action>
  <verify>
Run `npx tsc --noEmit` in backend/ to verify TypeScript compilation.
  </verify>
  <done>
Task Extraction template handles orders/directives. CCIR Generation template produces PIR/FFIR/EEFI. Both templates include confidence scores and extraction metadata.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create OPORD, IPB, and Wargame Extraction RAFT templates</name>
  <files>
backend/src/raft/templates/mdmp-opord-generation.ts
backend/src/raft/templates/mdmp-ipb-analysis.ts
backend/src/raft/templates/mdmp-wargame-extraction.ts
  </files>
  <action>
1. **Create `backend/src/raft/templates/mdmp-opord-generation.ts`**:

   RAFT template for MDMP-7-01 (Generate OPORD from approved COA and planning products):
   - Input: approved COA, commander's intent, task organization, coordinating instructions, service/support plan, C2 plan
   - Output: Structured 5-paragraph OPORD (Situation, Mission, Execution, Sustainment, Command and Signal)
   - Include annexes list and cross-reference validation
   - mdmpPhase: 'phase_7_orders_production', mdmpActivity: 'MDMP-7-01'

2. **Create `backend/src/raft/templates/mdmp-ipb-analysis.ts`**:

   RAFT template for MDMP-2-03 (IPB - Intelligence Preparation of the Battlefield per ATP 2-01.3):
   - Input: area of operations definition, current intelligence, terrain data, weather data, civil considerations
   - Output: terrain analysis (OAKOC), weather effects, civil considerations (ASCOPE), threat assessment, named areas of interest (NAIs)
   - mdmpPhase: 'phase_2_mission_analysis', mdmpActivity: 'MDMP-2-03'

3. **Create `backend/src/raft/templates/mdmp-wargame-extraction.ts`**:

   RAFT template for MDMP-4-04 (Extract decision points, HPTs, and IRs from wargame outputs):
   - Input: wargame output text, COA being wargamed, adversary COA, terrain/weather conditions
   - Output: decision points (with triggers and options), high-payoff targets (HPTs), intelligence requirements (IRs), branches identified, sequels identified
   - mdmpPhase: 'phase_4_coa_analysis', mdmpActivity: 'MDMP-4-04'

All templates should follow the same structure as Task 1 templates: input/output interfaces, template constant with name, description, mdmpPhase, mdmpActivity, systemPrompt, and examples array.
  </action>
  <verify>
Run `npx tsc --noEmit` in backend/ to verify TypeScript compilation.
  </verify>
  <done>
OPORD generation template produces structured 5-paragraph orders. IPB template covers OAKOC, ASCOPE, threat assessment. Wargame extraction captures decision points, HPTs, and IRs. All templates have schema definitions and system prompts.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create barrel export and verify templates are importable</name>
  <files>backend/src/raft/templates/index.ts</files>
  <action>
1. **Create `backend/src/raft/templates/index.ts`** that re-exports all 5 template constants and their types:

   ```typescript
   export { TASK_EXTRACTION_TEMPLATE, type TaskExtractionInput, type TaskExtractionOutput } from './mdmp-task-extraction.js';
   export { CCIR_GENERATION_TEMPLATE, type CCIRInput, type CCIRGenerationOutput } from './mdmp-ccir-generation.js';
   export { OPORD_GENERATION_TEMPLATE, type OPORDGenerationInput, type OPORDGenerationOutput } from './mdmp-opord-generation.js';
   export { IPB_ANALYSIS_TEMPLATE, type IPBAnalysisInput, type IPBAnalysisOutput } from './mdmp-ipb-analysis.js';
   export { WARGAME_EXTRACTION_TEMPLATE, type WargameExtractionInput, type WargameExtractionOutput } from './mdmp-wargame-extraction.js';
   ```

   Adjust the exact exported names to match the actual exports created in Tasks 1 and 2.

2. **Verify imports resolve** by running `npx tsc --noEmit` in backend/. This confirms the barrel file correctly re-exports all templates and that agents can import from `'../raft/templates/index.js'`.
  </action>
  <verify>
Run `npx tsc --noEmit` in backend/ to verify TypeScript compilation including the barrel export.
  </verify>
  <done>
index.ts barrel file exports all 5 RAFT templates. Templates are importable via `import { TASK_EXTRACTION_TEMPLATE } from '../raft/templates/index.js'`. TypeScript compilation passes.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes in backend/
2. 5 RAFT template files created in backend/src/raft/templates/
3. index.ts barrel file exports all 5 templates and their types
4. Task extraction handles specified, implied, and essential tasks
5. CCIR generation produces PIR, FFIR, EEFI
6. OPORD generation covers all 5 paragraphs
7. IPB analysis covers OAKOC, ASCOPE, threat assessment
8. Wargame extraction captures decision points, HPTs, IRs
9. All templates reference correct MDMP activities (MDMP-1-02, MDMP-2-07, MDMP-7-01, MDMP-2-03, MDMP-4-04)
</verification>

<success_criteria>
- 5 RAFT templates covering highest-value MDMP extraction tasks
- Each template has typed input/output interfaces with confidence scores
- Templates reference correct MDMP phase and activity IDs
- System prompts provide doctrinal context for extraction
- Example input/output pairs for few-shot learning
- Barrel export (index.ts) re-exports all 5 templates for agent consumption
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-mdmp-governance-integration/5.1-13-SUMMARY.md`
</output>
