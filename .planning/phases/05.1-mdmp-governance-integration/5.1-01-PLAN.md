---
phase: 5.1-mdmp-governance-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - near-contracts/src/dao/types.rs
  - near-contracts/src/mdmp/mod.rs
  - near-contracts/src/mdmp/types.rs
  - near-contracts/src/lib.rs
  - backend/src/dao/types.ts
  - backend/src/mdmp/types.ts
autonomous: true

must_haves:
  truths:
    - "AutonomyLevel enum has 4 variants including FullyDelegated with correct ordering"
    - "ProposalKind enum has 13 variants including 5 new MDMP types"
    - "MDMPPhase enum has 9 variants with next() and can_revisit() methods"
    - "ActivityCategory enum has 22 variants with safety classification methods"
    - "TypeScript types mirror Rust enums exactly for backend use"
    - "Cross-module imports compile: dao::types can reference mdmp::types (ActivityCategory) and vice versa"
  artifacts:
    - path: "near-contracts/src/dao/types.rs"
      provides: "Extended AutonomyLevel and ProposalKind enums"
      contains: "FullyDelegated"
    - path: "near-contracts/src/mdmp/types.rs"
      provides: "MDMPPhase and ActivityCategory enums"
      contains: "MDMPPhase"
    - path: "near-contracts/src/mdmp/mod.rs"
      provides: "MDMP module declaration"
      contains: "pub mod types"
    - path: "backend/src/mdmp/types.ts"
      provides: "TypeScript MDMP type definitions"
      contains: "MDMPPhase"
  key_links:
    - from: "near-contracts/src/lib.rs"
      to: "near-contracts/src/mdmp/mod.rs"
      via: "pub mod mdmp"
      pattern: "pub mod mdmp"
    - from: "near-contracts/src/dao/types.rs"
      to: "near-contracts/src/mdmp/types.rs"
      via: "ActivityCategory used in safety checks"
      pattern: "FullyDelegated"
---

<objective>
Add the FullyDelegated autonomy level and 5 MDMP-specific ProposalKind variants to the existing DAO types, and create the new MDMP module with MDMPPhase and ActivityCategory enums.

Purpose: Establish the type foundation that all other Phase 5.1 plans depend on. Every contract, agent, and UI component references these types.
Output: Extended Rust contract types + new MDMP module + mirrored TypeScript types for backend.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.1-mdmp-governance-integration/5.1-CONTEXT.md
@.planning/mdmp-governance/mdmp-contracts.rs
@.planning/mdmp-governance/mdmp-types.ts
@near-contracts/src/dao/types.rs
@near-contracts/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend DAO types.rs with FullyDelegated and MDMP ProposalKind variants</name>
  <files>near-contracts/src/dao/types.rs</files>
  <action>
Modify the existing `near-contracts/src/dao/types.rs` to:

1. **Extend AutonomyLevel enum** - Add `FullyDelegated` as the 4th variant AFTER Autonomous. Add `PartialOrd, Ord` derives. Ordering: NotAutonomous < SemiAutonomous < Autonomous < FullyDelegated. Keep `#[default] NotAutonomous` unchanged. Add doc comment explaining FullyDelegated is restricted to DATA_AGGREGATION, VALIDATION_CONSISTENCY, MONITORING, META_COGNITIVE categories only.

2. **Add 5 new ProposalKind variants** after the existing `Custom(String)`:
   - `PhaseTransition { from_phase: String, to_phase: String, red_team_responses: Vec<String>, accepted_assumptions: Vec<String> }`
   - `AssumptionAcceptance { assumptions: Vec<String>, risk_owner: AccountId }`
   - `ProductApproval { product_type: String, mdmp_phase: String, ai_confidence: f64, product_hash: String }`
   - `RedTeamGate { phase: String, challenges_addressed: Vec<String>, unresolved_risks: Vec<String> }`
   - `CommanderGuidance { guidance_text: String, modifies_assumptions: Vec<String> }`

3. **Extend `requires_human_in_loop()`** to also return true for PhaseTransition, AssumptionAcceptance, and CommanderGuidance (not ProductApproval or RedTeamGate - those use standard voting).

4. **Add `prohibits_fully_delegated()`** method returning true for all kinds that involve authority exercise (all new kinds + StrikeAuthorization, ConfigChange, MissionOrder).

5. **Extend `to_policy_label()`** with labels for new variants: "phase_transition", "assumption_acceptance", "product_approval", "red_team_gate", "commander_guidance".

6. **Update `get_effective_autonomy()`** in Proposal impl to also force NotAutonomous for the new kinds that require human-in-loop.

7. **Add tests** for: FullyDelegated ordering, new ProposalKind human-in-loop checks, policy labels, prohibits_fully_delegated.

IMPORTANT: Do NOT use AssumptionRecord struct in AssumptionAcceptance variant - use Vec<String> for assumption IDs instead (the full struct lives in the assumptions module). This keeps types.rs self-contained.

Reference: `.planning/mdmp-governance/mdmp-contracts.rs` lines 21-167 for the exact specification.
  </action>
  <verify>
Run `cargo check` in near-contracts/ to verify compilation.
Run `cargo test` in near-contracts/ to verify all existing + new tests pass.
  </verify>
  <done>
AutonomyLevel has 4 ordered variants. ProposalKind has 13 variants with correct human-in-loop and policy label behavior. All existing tests still pass. New tests verify MDMP-specific behavior.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MDMP module with types and mirror TypeScript types</name>
  <files>
near-contracts/src/mdmp/mod.rs
near-contracts/src/mdmp/types.rs
near-contracts/src/lib.rs
backend/src/mdmp/types.ts
backend/src/dao/types.ts
  </files>
  <action>
1. **Create `near-contracts/src/mdmp/mod.rs`**:
   ```rust
   pub mod types;
   ```

2. **Create `near-contracts/src/mdmp/types.rs`** with:
   - `MDMPPhase` enum (9 variants: Phase0Continuous through Phase8Assessment) with `#[near(serializers = [json, borsh])]`, Clone, Debug, PartialEq, Eq, Hash derives
   - `MDMPPhase::next()` method returning Option<MDMPPhase> (Phase0 and Phase8 return None)
   - `MDMPPhase::can_revisit()` method (all backward transitions allowed per JP 5-0)
   - `MDMPPhase::to_string_label()` returning the string form (e.g., "phase_1_receipt_of_mission")
   - `ActivityCategory` enum (22 variants: DataAggregation through EthicalLegal) with `#[near(serializers = [json, borsh])]`
   - `ActivityCategory::permits_fully_delegated()` returning true for DataAggregation, ValidationConsistency, Monitoring, MetaCognitive only
   - `ActivityCategory::requires_human_in_loop()` returning true for RiskJudgment, AuthorityDecision, EthicalLegal
   - Tests for MDMPPhase progression and ActivityCategory safety methods

   Reference: `.planning/mdmp-governance/mdmp-contracts.rs` lines 172-262 for the exact spec.

3. **Update `near-contracts/src/lib.rs`** to add `pub mod mdmp;` alongside existing `pub mod dao;`.

4. **Create `backend/src/mdmp/types.ts`** mirroring the Rust types:
   - `AuthorityDesignation` enum (5 values)
   - `ControlPosture` enum (5 values)
   - `MDMPPhase` enum (9 values matching Rust string labels)
   - `ActivityCategory` enum (22 values)
   - `GateType` enum (6 values)
   - `PlanningProduct` enum (11 values)
   - `SensitivityLevel` enum (4 values)
   - `AssumptionStatus` enum (5 values)
   - `AssumptionSource` enum (5 values)
   - `SafetyMatrixEntry` interface and `SAFETY_MATRIX` constant array (22 entries)
   - `GOVERNANCE_INVARIANTS` constant object (9 invariants)

   Reference: `.planning/mdmp-governance/mdmp-types.ts` Sections 1, 4, 7, 12, 13.

5. **Update `backend/src/dao/types.ts`** to add the new AutonomyLevel.FullyDelegated value and the 5 new ProposalKind values, keeping backward compatibility.
  </action>
  <verify>
Run `cargo check` and `cargo test` in near-contracts/.
Run `npx tsc --noEmit` in backend/ to verify TypeScript compilation.
  </verify>
  <done>
MDMP module exists with MDMPPhase (9 variants) and ActivityCategory (22 variants) with safety methods. TypeScript types mirror Rust types. lib.rs exports mdmp module. All compilation passes.
  </done>
</task>

</tasks>

<verification>
1. `cargo check` in near-contracts/ passes
2. `cargo test` in near-contracts/ passes (existing + new tests)
3. TypeScript compilation passes in backend/
4. AutonomyLevel::FullyDelegated exists with correct ordering
5. All 5 new ProposalKind variants exist with correct human-in-loop behavior
6. MDMPPhase has 9 variants with working next() progression
7. ActivityCategory safety methods correctly classify categories
8. Cross-module imports compile: `use crate::mdmp::types::ActivityCategory` in dao context and `use crate::dao::types::AutonomyLevel` in mdmp context both resolve
</verification>

<success_criteria>
- AutonomyLevel has 4 ordered variants (NotAutonomous < SemiAutonomous < Autonomous < FullyDelegated)
- ProposalKind has 13 variants with correct policy labels and human-in-loop enforcement
- MDMPPhase enum with phase progression logic
- ActivityCategory enum with safety classification (4 fully-delegated, 3 human-only, 15 hybrid)
- TypeScript types in backend/src/mdmp/types.ts mirror all Rust types
- SAFETY_MATRIX constant defines permitted authority ranges per category
- GOVERNANCE_INVARIANTS constant documents all 9 invariants
- All tests pass, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-mdmp-governance-integration/5.1-01-SUMMARY.md`
</output>
