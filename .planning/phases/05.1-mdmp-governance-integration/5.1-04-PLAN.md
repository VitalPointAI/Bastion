---
phase: 5.1-mdmp-governance-integration
plan: 04
type: execute
wave: 2
depends_on: [5.1-01]
files_modified:
  - near-contracts/src/dao/voting.rs
  - near-contracts/src/dao/execution.rs
autonomous: true

must_haves:
  truths:
    - "VotePolicy::for_proposal_kind has default policies for all 5 new MDMP ProposalKind variants"
    - "PhaseTransition requires 2/3 threshold with 2/3 quorum"
    - "AssumptionAcceptance requires 1/2 threshold with 1/2 quorum"
    - "CommanderGuidance requires Absolute{count:1} threshold (single commander approval)"
    - "ProposalExecutor::process_voting_complete handles FullyDelegated as immediate execution with audit log"
    - "execute_by_kind handles all 5 new ProposalKind variants with appropriate audit events"
  artifacts:
    - path: "near-contracts/src/dao/voting.rs"
      provides: "Extended vote policies for MDMP proposal kinds"
      contains: "PhaseTransition"
    - path: "near-contracts/src/dao/execution.rs"
      provides: "Extended execution flow for FullyDelegated and MDMP types"
      contains: "FullyDelegated"
  key_links:
    - from: "near-contracts/src/dao/voting.rs"
      to: "near-contracts/src/dao/types.rs"
      via: "ProposalKind variants used in match arms"
      pattern: "PhaseTransition"
    - from: "near-contracts/src/dao/execution.rs"
      to: "near-contracts/src/dao/types.rs"
      via: "AutonomyLevel::FullyDelegated in execution flow"
      pattern: "FullyDelegated"
---

<objective>
Extend the existing DAO voting and execution modules to handle the 5 new MDMP ProposalKind variants and the FullyDelegated autonomy level.

Purpose: Wire up the governance backbone so MDMP proposal types have appropriate voting thresholds and execution flows. FullyDelegated proposals execute immediately with enhanced audit logging.
Output: Extended voting.rs (5 new vote policies) + extended execution.rs (FullyDelegated flow + 5 new execution handlers).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.1-mdmp-governance-integration/5.1-CONTEXT.md
@.planning/mdmp-governance/mdmp-contracts.rs
@near-contracts/src/dao/voting.rs
@near-contracts/src/dao/execution.rs
@near-contracts/src/dao/types.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add vote policies for 5 new MDMP ProposalKind variants</name>
  <files>near-contracts/src/dao/voting.rs</files>
  <action>
Modify `near-contracts/src/dao/voting.rs` to extend `VotePolicy::for_proposal_kind()` with 5 new match arms BEFORE the existing `_ => Self::default()` fallback:

1. **PhaseTransition { .. }**: RoleWeight, 2/3 threshold, 2/3 quorum, 1/3 veto threshold.
   Comment: "High bar because it gates all subsequent planning."

2. **AssumptionAcceptance { .. }**: RoleWeight, 1/2 threshold, 1/2 quorum, no veto.
   Comment: "Lower bar to avoid bottlenecking the planning process."

3. **ProductApproval { .. }**: RoleWeight, 2/3 threshold, 1/2 quorum, 1/3 veto threshold.
   Comment: "Significant planning products require strong consensus."

4. **RedTeamGate { .. }**: RoleWeight, 1/2 threshold, 1/2 quorum, no veto.
   Comment: "Red team gates acknowledge challenges were considered, not block."

5. **CommanderGuidance { .. }**: RoleWeight, Absolute{count:1} threshold, Absolute{count:1} quorum, no veto.
   Comment: "Commander guidance is recorded, not voted on; requires only commander's approval."

Add tests:
- test_phase_transition_vote_policy (verify 2/3 threshold, 2/3 quorum)
- test_assumption_acceptance_vote_policy (verify 1/2 threshold)
- test_commander_guidance_vote_policy (verify Absolute{count:1})
- test_product_approval_has_veto (verify veto_threshold is Some)
- test_red_team_gate_no_veto (verify veto_threshold is None)

Reference: `.planning/mdmp-governance/mdmp-contracts.rs` lines 745-828 for exact specification.
  </action>
  <verify>
Run `cargo check` in near-contracts/ to verify compilation.
Run `cargo test` in near-contracts/ to verify all existing + new tests pass.
  </verify>
  <done>
VotePolicy::for_proposal_kind returns appropriate policies for all 5 new MDMP proposal types. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend execution engine for FullyDelegated and MDMP proposal execution</name>
  <files>near-contracts/src/dao/execution.rs</files>
  <action>
Modify `near-contracts/src/dao/execution.rs`:

1. **Extend process_voting_complete()** to handle `AutonomyLevel::FullyDelegated`:
   - Add new match arm BEFORE the existing Autonomous arm
   - FullyDelegated transitions to ReadyForExecution (same as Autonomous)
   - Add FULLY_DELEGATED_EXECUTION log event with dao_id, proposal_id, and note "No human monitoring required"
   - This ensures FullyDelegated proposals skip both veto window and human approval

2. **Extend execute_by_kind()** with handlers for the 5 new ProposalKind variants:
   - **PhaseTransition**: Log MDMP_PHASE_TRANSITION_EXECUTED with from_phase, to_phase, red_team_responses count, accepted_assumptions count
   - **AssumptionAcceptance**: Log ASSUMPTION_ACCEPTANCE_EXECUTED with assumptions count, risk_owner
   - **ProductApproval**: Log PRODUCT_APPROVED with product_type, mdmp_phase, ai_confidence, product_hash
   - **RedTeamGate**: Log RED_TEAM_GATE_PASSED with phase, challenges_addressed count, unresolved_risks count
   - **CommanderGuidance**: Log COMMANDER_GUIDANCE_RECORDED with guidance_text length, modifies_assumptions count. Add INVARIANT 7 comment.

   Each handler returns Ok("description of what was executed") on success.

3. **Add tests**:
   - test_fully_delegated_executes_immediately (FullyDelegated -> ReadyForExecution, not InVetoWindow)
   - test_phase_transition_execution_logs_metadata
   - test_commander_guidance_execution

Reference: `.planning/mdmp-governance/mdmp-contracts.rs` lines 835-879 for exact specification.
  </action>
  <verify>
Run `cargo check` in near-contracts/ to verify compilation.
Run `cargo test` in near-contracts/ to verify all tests pass.
  </verify>
  <done>
Execution engine handles FullyDelegated as immediate execution with audit logging. All 5 new ProposalKind variants have execution handlers with JSON-formatted audit events. All tests pass.
  </done>
</task>

</tasks>

<verification>
1. `cargo check` in near-contracts/ passes
2. `cargo test` in near-contracts/ passes (existing + new tests)
3. VotePolicy returns correct thresholds for all 5 new MDMP proposal kinds
4. PhaseTransition has highest governance bar (2/3 threshold + 2/3 quorum)
5. CommanderGuidance has lowest bar (single approval, no voting)
6. FullyDelegated autonomy executes immediately with audit log
7. All new ProposalKind variants have execution handlers producing audit events
8. No regressions in existing voting or execution behavior
</verification>

<success_criteria>
- 5 new match arms in VotePolicy::for_proposal_kind with military-appropriate thresholds
- FullyDelegated execution flow: approved -> ReadyForExecution (no veto window, no human approval)
- 5 new execution handlers in execute_by_kind with structured JSON audit events
- INVARIANT 7 documented in CommanderGuidance handler
- All tests pass, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-mdmp-governance-integration/5.1-04-SUMMARY.md`
</output>
