---
phase: 5.1-mdmp-governance-integration
plan: 11
type: execute
wave: 3
depends_on: [5.1-01, 5.1-04]
files_modified:
  - frontend/src/components/governance/GovernanceGateDashboard.tsx
  - frontend/src/components/governance/PhaseProgressionBar.tsx
  - frontend/src/components/governance/AssumptionTracker.tsx
  - frontend/src/components/governance/CommanderGuidanceForm.tsx
autonomous: true

must_haves:
  truths:
    - "GovernanceGateDashboard shows all gates per MDMP phase with satisfaction status"
    - "PhaseProgressionBar visualizes current MDMP phase (0-8) with gate completion indicators"
    - "AssumptionTracker displays assumptions with sensitivity color coding and lifecycle status"
    - "CommanderGuidanceForm creates CommanderGuidance proposals through the DAO system"
    - "All components integrate with existing DAODashboard layout pattern"
  artifacts:
    - path: "frontend/src/components/governance/GovernanceGateDashboard.tsx"
      provides: "Gate status dashboard for MDMP phases"
      contains: "GovernanceGateDashboard"
    - path: "frontend/src/components/governance/PhaseProgressionBar.tsx"
      provides: "Visual MDMP phase progression indicator"
      contains: "PhaseProgressionBar"
    - path: "frontend/src/components/governance/AssumptionTracker.tsx"
      provides: "Assumption lifecycle management panel"
      contains: "AssumptionTracker"
    - path: "frontend/src/components/governance/CommanderGuidanceForm.tsx"
      provides: "Commander guidance proposal creation form"
      contains: "CommanderGuidanceForm"
  key_links:
    - from: "frontend/src/components/governance/GovernanceGateDashboard.tsx"
      to: "frontend/src/components/dao/DAODashboard.tsx"
      via: "Integrated into governance section"
      pattern: "GovernanceGateDashboard"
    - from: "frontend/src/components/governance/CommanderGuidanceForm.tsx"
      to: "backend/src/dao/types.ts"
      via: "ProposalKind.CommanderGuidance type"
      pattern: "CommanderGuidance"
---

<objective>
Create the frontend governance UI components for MDMP workflow management: gate dashboard, phase progression visualization, assumption tracking, and commander guidance form.

Purpose: Provide the human interface for MDMP governance. Commanders and staff need to see phase progression, review gates, manage assumptions, and issue guidance. Without these components, governance operations require direct API calls.
Output: 4 new React components in `frontend/src/components/governance/`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05.1-mdmp-governance-integration/5.1-CONTEXT.md
@.planning/mdmp-governance/mdmp-types.ts
@frontend/src/components/dao/DAODashboard.tsx
@frontend/src/components/dao/VotingInterface.tsx
@frontend/src/components/dao/ProposalCard.tsx
@backend/src/dao/types.ts
@backend/src/mdmp/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GovernanceGateDashboard and PhaseProgressionBar components</name>
  <files>
frontend/src/components/governance/GovernanceGateDashboard.tsx
frontend/src/components/governance/PhaseProgressionBar.tsx
  </files>
  <action>
0. **Create governance directory** if it does not exist:
   ```bash
   mkdir -p frontend/src/components/governance
   ```

1. **Create `frontend/src/components/governance/PhaseProgressionBar.tsx`**:
   - Props: `currentPhase: string`, `phases: Array<{ phase: string; label: string; gatesTotal: number; gatesSatisfied: number }>`, `onPhaseClick: (phase: string) => void`
   - Visual: Horizontal bar showing 9 MDMP phases (0-8) as connected steps
   - Current phase highlighted with accent color
   - Each step shows gate completion ratio (e.g., "3/5 gates")
   - Past phases shown as completed (green), future phases shown as locked (gray)
   - Use Tailwind CSS classes consistent with existing DAODashboard styling
   - Responsive: collapses to vertical on mobile

2. **Create `frontend/src/components/governance/GovernanceGateDashboard.tsx`**:
   - Props: `missionId: string`, `currentPhase: string`, `gates: Array<GateDisplayData>`, `onSatisfyGate: (gateId: string) => void`
   - Define `GateDisplayData` interface: gateId, gateType, phase, satisfied, satisfiedBy, satisfiedAt, proposalId, description
   - Layout: Card grid showing all gates for the selected phase
   - Each gate card shows: type badge, satisfaction status (green check / red X), who satisfied it, linked proposal ID
   - Action button to initiate gate satisfaction (opens proposal creation)
   - Filter by: gate type, satisfaction status
   - Include PhaseProgressionBar at top
   - Follow the card layout pattern from existing ProposalCard.tsx
  </action>
  <verify>
Run `npx tsc --noEmit` in frontend/ to verify TypeScript compilation.
  </verify>
  <done>
GovernanceGateDashboard shows gates per phase with satisfaction status. PhaseProgressionBar visualizes all 9 MDMP phases with gate completion indicators.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create AssumptionTracker and CommanderGuidanceForm components</name>
  <files>
frontend/src/components/governance/AssumptionTracker.tsx
frontend/src/components/governance/CommanderGuidanceForm.tsx
  </files>
  <action>
1. **Create `frontend/src/components/governance/AssumptionTracker.tsx`**:
   - Props: `missionId: string`, `assumptions: Array<AssumptionDisplayData>`, `onAccept: (assumptionId: string) => void`, `onReject: (assumptionId: string) => void`, `onInvalidate: (assumptionId: string, evidence: string) => void`
   - Define `AssumptionDisplayData` interface mirroring AssumptionRecord from Rust: id, description, sensitivity, validationMethod, acceptedBy, status, sourcePhase
   - Sensitivity color coding: Critical=red, High=orange, Medium=yellow, Low=green
   - Status badges: Pending (gray), Accepted (green), Rejected (red), Invalidated (purple), Expired (dark gray)
   - Sortable by sensitivity, status, source phase
   - Filterable by status and sensitivity level
   - Action buttons: Accept (creates AssumptionAcceptance proposal), Reject, Invalidate (with evidence input)
   - Summary stats at top: total count, count by status, count by sensitivity

2. **Create `frontend/src/components/governance/CommanderGuidanceForm.tsx`**:
   - Props: `daoId: string`, `missionId: string`, `existingAssumptions: Array<{ id: string; description: string }>`, `onSubmit: (guidance: CommanderGuidanceData) => void`
   - Define `CommanderGuidanceData` interface: guidanceText: string, modifiesAssumptions: string[]
   - Form fields:
     - Guidance text (multi-line textarea with minimum length validation)
     - Assumption modifier selection (multi-select from existing assumptions that this guidance modifies)
   - Preview section showing the CommanderGuidance ProposalKind that will be created
   - Submit button creates the proposal through the DAO system
   - INVARIANT 7: Display notice that guidance will be recorded on-chain for traceability
   - Follow form patterns from existing VotingInterface.tsx
  </action>
  <verify>
Run `npx tsc --noEmit` in frontend/ to verify TypeScript compilation.
  </verify>
  <done>
AssumptionTracker displays assumptions with sensitivity color coding and lifecycle management. CommanderGuidanceForm creates CommanderGuidance proposals with assumption linkage. INVARIANT 7 notice displayed.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes in frontend/
2. PhaseProgressionBar renders 9 MDMP phases with visual gate completion
3. GovernanceGateDashboard shows gates per phase with satisfaction status
4. AssumptionTracker has sensitivity color coding (Critical=red through Low=green)
5. CommanderGuidanceForm creates CommanderGuidance ProposalKind
6. INVARIANT 7 traceability notice displayed on guidance form
7. Components follow existing DAO dashboard styling patterns
</verification>

<success_criteria>
- 4 new governance components created
- Phase progression visualization with 9 phases and gate completion
- Gate dashboard with satisfaction tracking and proposal linkage
- Assumption tracker with sensitivity color coding and lifecycle management
- Commander guidance form with assumption linkage and on-chain recording notice
- Components integrate with existing frontend patterns
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-mdmp-governance-integration/5.1-11-SUMMARY.md`
</output>
