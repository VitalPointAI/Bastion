---
phase: 38-inheritance-deepening
plan: 05
type: execute
wave: 3
depends_on: [38-02]
files_modified:
  - frontend/src/components/inheritance/AcknowledgmentBanner.tsx
  - frontend/src/components/inheritance/ChangelogView.tsx
  - frontend/src/components/inheritance/InheritedContextSection.tsx
  - frontend/src/components/inheritance/RFIThread.tsx
  - frontend/src/components/inheritance/InterpretationAckPanel.tsx
  - frontend/src/components/problem-set/ProblemSetTabContainer.tsx
  - frontend/src/components/problem-set/ProblemSetSelector.tsx
autonomous: true
requirements: [INH-01, INH-02, INH-03, INH-04, INH-05, INH-06, INH-07, INH-08]

must_haves:
  truths:
    - "Significant change notifications show amber banner requiring commander acknowledgment"
    - "Minor change notifications show info-blue banner that can be dismissed"
    - "Understand tab header shows badge count of pending notifications"
    - "Problem set selector shows dot indicator for PSes with pending updates"
    - "Inherited content sections show read-only state with modification request button"
    - "Parent can acknowledge, clarify, or correct child interpretations in UI"
  artifacts:
    - path: "frontend/src/components/inheritance/AcknowledgmentBanner.tsx"
      provides: "Severity-tiered persistent banner with commander ack requirement"
    - path: "frontend/src/components/inheritance/InterpretationAckPanel.tsx"
      provides: "Parent view of child interpretations with 3-action response"
    - path: "frontend/src/components/problem-set/ProblemSetTabContainer.tsx"
      provides: "Understand tab badge count"
    - path: "frontend/src/components/problem-set/ProblemSetSelector.tsx"
      provides: "Dot indicator for pending inheritance updates"
  key_links:
    - from: "frontend/src/components/inheritance/AcknowledgmentBanner.tsx"
      to: "/api/problem-sets/:id/notification-counts"
      via: "fetch for badge counts"
      pattern: "notification-counts"
    - from: "frontend/src/components/inheritance/InterpretationAckPanel.tsx"
      to: "/api/problem-sets/:id/annotations/:annotationId/acknowledge"
      via: "POST for ack action"
      pattern: "acknowledge"
---

<objective>
Build the frontend change notification UX, override tracking UI, and interpretation acknowledgment panel. Enhance existing inheritance components with severity-tiered banners, read-only enforcement, and tab/PS-selector badge indicators.

Purpose: Commanders must acknowledge inherited context changes (not silently dismiss). Children interact with inherited content through annotations and modification requests (not direct edits).
Output: Enhanced AcknowledgmentBanner, ChangelogView, InheritedContextSection, RFIThread. New InterpretationAckPanel. Badge integration in ProblemSetTabContainer and ProblemSetSelector.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/38-inheritance-deepening/38-CONTEXT.md
@.planning/phases/38-inheritance-deepening/38-02-SUMMARY.md

<interfaces>
<!-- From frontend/src/components/inheritance/AcknowledgmentBanner.tsx -->
```typescript
interface AcknowledgmentBannerProps {
  // existing props — will be extended
}
export function AcknowledgmentBanner({ ... })
```

<!-- Notification badge already exists -->
```typescript
// frontend/src/components/problem-set/NotificationBadge.tsx
// Reusable badge with count, pulse animation, "99+"
```

<!-- API endpoints from Plan 02 -->
```
GET  /:id/notification-counts -> { pendingAcks, unreadChangelog, openRFIs, pendingFRAGOs, total }
POST /:id/annotations/:annotationId/acknowledge -> { action, comment? }
POST /:id/modification-requests -> { targetProblemSetId, targetItemId, targetItemType, subject, description }
POST /:id/guidance-requests -> { targetProblemSetId, subject, situationDescription }
GET  /:id/rfis/by-subtype/:subtype -> InheritanceRFI[]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance AcknowledgmentBanner with severity tiers, enhance ChangelogView, and create InterpretationAckPanel</name>
  <files>frontend/src/components/inheritance/AcknowledgmentBanner.tsx, frontend/src/components/inheritance/ChangelogView.tsx, frontend/src/components/inheritance/InterpretationAckPanel.tsx</files>
  <action>
**File 1: AcknowledgmentBanner.tsx — Severity-tiered persistent banner**

Refactor the existing AcknowledgmentBanner to support two severity tiers per user decision:

1. **Amber/warning tier** (for significant changes — new/removed docs): Background color #FFF3CD with amber border. Shows: "Significant updates from {sourcePsName}: {count} changes require acknowledgment". Buttons: "View Changes" (opens changelog) and "Acknowledge" (calls POST /inherited-context/acknowledge). NO dismiss/close button — commander must acknowledge to dismiss (per locked decision). Show source PS name and update count.

2. **Info-blue tier** (for minor changes — edits, graph updates): Background color #D1ECF1 with blue border. Shows: "{count} minor updates from {sourcePsName}". Buttons: "View Changes" and "Dismiss" (dismiss allowed for minor). Dismissing adds to localStorage to hide until next change.

Props interface: `{ pendingAcks: PendingAck[]; changelogEntries: InheritanceChangelog[]; onAcknowledge: (sourceProblemSetId: string) => void; onViewChanges: () => void; }`. Separate significant and minor entries using changelogEntries' changeSeverity field. Render amber banner first if significant exist, then blue banner if minor exist. Use inline styles per project convention.

**File 2: ChangelogView.tsx — Enhanced with severity badges and click-to-navigate**

Enhance the existing ChangelogView:
- Add severity badge next to each entry: amber pill for 'significant', blue pill for 'minor'.
- Add timestamp formatting (relative time, e.g., "2 hours ago").
- Add source PS name display per entry.
- Make each entry clickable — calls `onNavigateToItem(itemId: string, sourceProblemSetId: string)` prop to jump to the inherited document in the Understand tab.
- Sort by createdAt descending (most recent first).

**File 3: InterpretationAckPanel.tsx — New component for parent acknowledgment of child interpretations**

Create new component that shows child interpretations requiring parent response:

Props: `{ parentProblemSetId: string; childAnnotations: InheritanceAnnotation[]; onActionComplete: () => void; }`

For each interpretation annotation:
- Show: child PS name, target item title, interpretation content, creation date.
- Three action buttons in a button group:
  - "Acknowledge" (green) — interpretation is correct, calls POST /annotations/:id/acknowledge with action='acknowledge'
  - "Clarify" (amber) — opens inline text input for clarification message, calls POST with action='clarify' and comment
  - "Correct" (red) — opens inline text input for correction guidance, calls POST with action='correct' and comment
- After action: show the selected action with timestamp, disable buttons.
- If already acknowledged: show the previous action and comment.

Use inline styles. Follow existing inheritance component patterns.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && cd frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>AcknowledgmentBanner shows severity-tiered banners (amber non-dismissable, blue dismissable). ChangelogView has severity badges and click-to-navigate. InterpretationAckPanel supports acknowledge/clarify/correct actions. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add read-only enforcement to InheritedContextSection, extend RFIThread, wire badges into tab container and PS selector</name>
  <files>frontend/src/components/inheritance/InheritedContextSection.tsx, frontend/src/components/inheritance/RFIThread.tsx, frontend/src/components/problem-set/ProblemSetTabContainer.tsx, frontend/src/components/problem-set/ProblemSetSelector.tsx</files>
  <action>
**File 1: InheritedContextSection.tsx — Read-only enforcement + modification request button**

Enhance the existing InheritedContextSection:
- Add visual read-only indicator: subtle lock icon and "Inherited — Read Only" label on each inherited item card.
- Add "Request Modification" button on each inherited item — opens a modal/inline form with subject and description fields. On submit, calls POST `/:id/modification-requests` with the target item details.
- Add "Request Guidance" button (more prominent, for upward questions) — opens inline form with subject and situation description. Calls POST `/:id/guidance-requests`.
- Disable any edit buttons/actions on inherited items (if any exist from Phase 26).

**File 2: RFIThread.tsx — Support new RFI subtypes**

Enhance existing RFIThread to render differently based on rfiSubtype:
- `'clarification'` (existing) — no change, standard Q&A thread.
- `'modification_request'` — show amber header "Modification Request", show resolution status badge (pending/approved/denied), if parent view show approve/deny buttons that call PUT `/:id/modification-requests/:rfiId/resolve`.
- `'guidance_request'` — show blue header "Guidance Request", standard thread for discussion.
- Add subtype badge in thread header for visual distinction.

**File 3: ProblemSetTabContainer.tsx — Understand tab badge count**

Find where tab headers are rendered. Add notification badge to the Understand tab:
- Fetch notification counts from GET `/:id/notification-counts` on mount and on a 30-second polling interval.
- Use existing NotificationBadge component to show total count on the Understand tab header.
- The `tabNotifications` pattern from ProblemSetContext may already support this — check if it exists and use it, otherwise add the fetch and badge directly.

**File 4: ProblemSetSelector.tsx — Dot indicator for pending updates**

Add a small dot indicator (8px amber circle) next to each PS name in the selector list that has pending inheritance updates:
- When loading the PS list, also fetch notification counts for each PS.
- If `total > 0`, show amber dot.
- Use a lightweight approach: batch-fetch counts or add a `has_pending_inheritance` flag to the existing PS list API response.
- If batch-fetching is too expensive, add a simple endpoint or extend the existing PS list query to include a pending inheritance flag (LEFT JOIN on unacknowledged changelog entries).

Use inline styles. Minimize API calls — prefer extending existing queries over adding N+1 fetches.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && cd frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>InheritedContextSection shows read-only state with modification/guidance request buttons. RFIThread renders subtypes with appropriate headers and actions. Understand tab shows NotificationBadge. PS selector shows dot indicator. TypeScript compiles.</done>
</task>

</tasks>

<verification>
- Frontend TypeScript compiles without errors
- AcknowledgmentBanner: amber tier has no dismiss button, requires acknowledge
- AcknowledgmentBanner: blue tier allows dismiss
- Inherited items show lock icon and "Request Modification" button
- Understand tab header shows notification count badge
- PS selector shows dot indicator for PSes with pending updates
</verification>

<success_criteria>
- Two severity tiers visually distinct (amber vs blue)
- Commander acknowledgment required for significant changes (no dismiss)
- Read-only inherited content with modification request workflow
- Interpretation ack panel with 3 response options
- Tab badge and PS selector dot indicator functional
</success_criteria>

<output>
After completion, create `.planning/phases/38-inheritance-deepening/38-05-SUMMARY.md`
</output>
