---
phase: 4.4-mission-context-force-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/mission/types.ts
  - backend/src/mission/schemas.ts
  - backend/src/mission/mission-store.ts
  - backend/src/mission/participant-store.ts
  - backend/src/mission/invite-store.ts
  - backend/src/command/types.ts
  - backend/src/command/schemas.ts
  - backend/src/command/unit-store.ts
  - backend/src/command/relationship-store.ts
  - backend/src/resources/types.ts
  - backend/src/resources/schemas.ts
  - backend/src/resources/resource-store.ts
  - backend/src/resources/personnel-store.ts
  - backend/src/resources/consumable-store.ts
  - backend/src/sensors/types.ts
  - backend/src/sensors/schemas.ts
  - backend/src/sensors/sensor-store.ts
  - frontend/package.json
autonomous: true

must_haves:
  truths:
    - "Database tables exist for missions, participants, invites, units, command relationships, resources, personnel, consumables, and sensors"
    - "TypeScript types and Zod schemas validate all mission-related data"
    - "Store classes provide CRUD operations for all domain entities"
  artifacts:
    - path: "backend/src/mission/mission-store.ts"
      provides: "Mission CRUD with lifecycle states"
      exports: ["MissionStore", "missionStore"]
    - path: "backend/src/command/unit-store.ts"
      provides: "Unit CRUD operations"
      exports: ["UnitStore", "unitStore"]
    - path: "backend/src/resources/resource-store.ts"
      provides: "Resource catalog operations"
      exports: ["ResourceStore", "resourceStore"]
    - path: "backend/src/sensors/sensor-store.ts"
      provides: "Sensor registration operations"
      exports: ["SensorStore", "sensorStore"]
  key_links:
    - from: "backend/src/mission/mission-store.ts"
      to: "backend/src/lib/database.js"
      via: "getPool() for PostgreSQL operations"
      pattern: "getPool\\(\\)"
---

<objective>
Create database schemas, TypeScript types, Zod validation schemas, and store classes for the mission domain.

Purpose: Establish the data foundation for mission workspaces, participants, command hierarchies, resource catalogs, and sensor registration. This is the foundation layer that all subsequent plans depend on.

Output: Complete backend data layer with 9 database tables and corresponding stores.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.4-mission-context-force-onboarding/4.4-RESEARCH.md

Reference existing patterns:
@backend/src/graph/workspace/types.ts
@backend/src/graph/workspace/store.ts
@backend/src/strategic/schemas.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Dependencies and Create Type Definitions</name>
  <files>
    frontend/package.json
    backend/src/mission/types.ts
    backend/src/mission/schemas.ts
    backend/src/command/types.ts
    backend/src/command/schemas.ts
    backend/src/resources/types.ts
    backend/src/resources/schemas.ts
    backend/src/sensors/types.ts
    backend/src/sensors/schemas.ts
  </files>
  <action>
    1. Install frontend dependencies (from research):
       ```bash
       cd frontend && npm install milsymbol react-organizational-chart react-complex-tree leaflet-realtime react-csv-importer gantt-task-react react-leaflet-draw
       ```
       Note: react-leaflet-draw is required for AO polygon drawing in Plan 4.4-03.
       Note: @types/leaflet-realtime may not exist, skip if unavailable.

    2. Create backend/src/mission/types.ts with:
       - MissionState: 'planning' | 'active' | 'complete' | 'archived'
       - Mission interface: id (MSN-uuid), name, description, classification, areaOfOps (GeoJSON Polygon), workspaceId (links to existing workspace), state, createdBy (DID), createdAt, updatedAt, activatedAt?, completedAt?
       - MissionParticipant: id, missionId, userDid, role ('commander' | 'staff' | 'observer'), joinedAt, invitedBy
       - MissionInvite: id (INV-uuid), missionId, token (hashed), inviteeEmail?, inviteeDid?, role, expiresAt, acceptedAt?, createdBy

    3. Create backend/src/mission/schemas.ts with Zod schemas:
       - MissionInputSchema (name 1-100 chars, description, classification enum, areaOfOps as GeoJSON)
       - MissionParticipantSchema
       - MissionInviteSchema

    4. Create backend/src/command/types.ts with:
       - RelationshipType: 'OPCON' | 'TACON' | 'ADCON' | 'COCOM' | 'DS' | 'GS' | 'GSR' | 'R'
       - Unit interface: id (UNIT-uuid), missionId, name, sidc (MIL-STD-2525D code), parentDid? (for linking to org entity), location? (lat/lng), createdAt
       - CommandRelationship: id, missionId, superiorUnitId, subordinateUnitId, relationshipType, effectiveFrom?, effectiveTo?

    5. Create backend/src/command/schemas.ts with Zod schemas

    6. Create backend/src/resources/types.ts with:
       - ResourceCategory: 'weapon_system' | 'vehicle' | 'equipment' | 'communication'
       - ResourceStatus: 'FMC' | 'PMC' | 'NMC' (Full/Partial/Non Mission Capable)
       - Resource interface: id (RES-uuid), missionId, name, category, serialNumber?, sidc?, status, specifications (JSONB), location?, createdAt
       - Personnel interface: id (PER-uuid), missionId, unitId?, name, rank, specialty, readinessStatus, clearanceLevel, createdAt
       - Consumable interface: id (CON-uuid), missionId, category ('ammunition' | 'fuel' | 'medical' | 'rations' | 'other'), name, quantity, unit, minimumLevel, currentLevel, location?, createdAt

    7. Create backend/src/resources/schemas.ts with Zod schemas

    8. Create backend/src/sensors/types.ts with:
       - SensorCategory: 'airborne' | 'ground' | 'maritime' | 'space' | 'autonomous'
       - SensorStatus: 'operational' | 'degraded' | 'offline' | 'maintenance'
       - Sensor interface: id (SEN-uuid), missionId, name, category, sidc?, capabilities (range, resolution, coverageArea), status, location? (lat/lng), dataFeedUrl?, createdAt
       - SensorCoverage interface (for visualization): sensorId, coveragePolygon (GeoJSON), confidenceLevel

    9. Create backend/src/sensors/schemas.ts with Zod schemas
  </action>
  <verify>
    - npm install completes without errors
    - TypeScript compiles: `cd backend && npx tsc --noEmit`
    - All type files export their interfaces
  </verify>
  <done>
    All type definitions and Zod schemas in place for mission, command, resources, and sensors domains.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Mission and Participant Stores</name>
  <files>
    backend/src/mission/mission-store.ts
    backend/src/mission/participant-store.ts
    backend/src/mission/invite-store.ts
  </files>
  <action>
    1. Create backend/src/mission/mission-store.ts:
       - Follow pattern from graph/workspace/store.ts
       - initMissionTable() creates missions table with all fields
       - MissionStore class with ensureInitialized() pattern
       - createMission(input, createdBy): generates MSN-{uuid} ID
       - getMission(id), listMissions(filters), updateMission(id, updates)
       - transitionState(id, newState): validates state machine transitions (planning->active->complete->archived)
       - Export singleton: missionStore

    2. Create backend/src/mission/participant-store.ts:
       - initParticipantTable() with foreign key to missions
       - ParticipantStore with addParticipant, removeParticipant, listParticipants(missionId), getParticipant
       - Export singleton: participantStore

    3. Create backend/src/mission/invite-store.ts:
       - initInviteTable() with token, expiry, foreign key to missions
       - generateInviteToken(expirationHours=72): returns { token, hashedToken } using crypto.randomBytes
       - createInvite(missionId, role, createdBy, inviteeEmail?, inviteeDid?)
       - getInviteByToken(token): hash incoming token and lookup
       - acceptInvite(token, acceptorDid): mark as accepted, create participant
       - deleteExpiredInvites(): cleanup job
       - Export singleton: inviteStore
  </action>
  <verify>
    - TypeScript compiles: `cd backend && npx tsc --noEmit`
    - Stores export expected classes and singletons
  </verify>
  <done>
    Mission, participant, and invite stores with full CRUD operations and secure token generation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Command, Resource, and Sensor Stores</name>
  <files>
    backend/src/command/unit-store.ts
    backend/src/command/relationship-store.ts
    backend/src/resources/resource-store.ts
    backend/src/resources/personnel-store.ts
    backend/src/resources/consumable-store.ts
    backend/src/sensors/sensor-store.ts
  </files>
  <action>
    1. Create backend/src/command/unit-store.ts:
       - initUnitTable() with sidc field for military symbology
       - UnitStore with CRUD operations
       - Export singleton: unitStore

    2. Create backend/src/command/relationship-store.ts:
       - initRelationshipTable() with composite unique constraint (mission, superior, subordinate, type)
       - createRelationship(input)
       - getHierarchy(missionId): use recursive CTE (from RESEARCH.md Pattern 3)
       - validateNoCycle(missionId, superiorId, subordinateId): DFS check before insert
       - Export singleton: relationshipStore

    3. Create backend/src/resources/resource-store.ts:
       - initResourceTable()
       - CRUD operations with category filtering
       - updateStatus(id, newStatus)
       - Export singleton: resourceStore

    4. Create backend/src/resources/personnel-store.ts:
       - initPersonnelTable() with unitId foreign key
       - CRUD with unit assignment
       - listByUnit(unitId), listByMission(missionId)
       - Export singleton: personnelStore

    5. Create backend/src/resources/consumable-store.ts:
       - initConsumableTable()
       - CRUD with quantity tracking
       - updateLevel(id, newLevel)
       - getLowStock(missionId): returns items below minimumLevel
       - Export singleton: consumableStore

    6. Create backend/src/sensors/sensor-store.ts:
       - initSensorTable()
       - CRUD with category filtering
       - updateStatus(id, newStatus)
       - getSensorsByLocation(missionId, bbox): spatial query for map view
       - Export singleton: sensorStore
       - Note: API endpoints will be created in Plan 4.4-08
  </action>
  <verify>
    - TypeScript compiles: `cd backend && npx tsc --noEmit`
    - All stores export expected singletons
    - Relationship store includes cycle detection logic
  </verify>
  <done>
    All 6 stores created with CRUD operations, relationship hierarchy queries, and resource status tracking.
  </done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors: `cd backend && npx tsc --noEmit`
- All store files exist and export singletons
- Frontend dependencies installed: check frontend/package.json includes milsymbol, react-organizational-chart, react-leaflet-draw, etc.
- Types are coherent: interfaces match Zod schemas
</verification>

<success_criteria>
- 9 database tables designed via init functions
- 9 store singletons exported
- All types have corresponding Zod validation schemas
- New frontend dependencies installed for Phase 4.4 UI work (including react-leaflet-draw)
</success_criteria>

<output>
After completion, create `.planning/phases/04.4-mission-context-force-onboarding/4.4-01-SUMMARY.md`
</output>
