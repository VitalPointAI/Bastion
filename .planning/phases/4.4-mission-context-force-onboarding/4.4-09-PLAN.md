---
phase: 4.4-mission-context-force-onboarding
plan: 09
type: execute
wave: 4
depends_on: ["4.4-03", "4.4-05", "4.4-07", "4.4-08"]
files_modified:
  - frontend/src/components/mission/MissionDetail.tsx
  - frontend/src/components/mission/MissionDetail.css
  - frontend/src/components/mission/MissionList.tsx
  - frontend/src/components/mission/index.ts
  - frontend/src/App.tsx
autonomous: false

must_haves:
  truths:
    - "User can navigate to mission list from main app"
    - "User can view mission detail with all sections"
    - "User can switch between mission sections (map, command, resources, participants)"
    - "User can create new mission via wizard"
    - "End-to-end flow from list to detail works"
  artifacts:
    - path: "frontend/src/components/mission/MissionDetail.tsx"
      provides: "Mission detail view with all tabs"
      min_lines: 150
    - path: "frontend/src/App.tsx"
      provides: "App navigation including missions"
      contains: "MissionList|MissionDetail"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/mission/MissionList.tsx"
      via: "Navigation state"
      pattern: "MissionList"
---

<objective>
Create the mission detail view, mission list, and integrate into main app navigation.

Purpose: Connect all mission components into a cohesive user experience accessible from the main application.

Output: Complete mission UI with list, detail, navigation, and all integrated subsections.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.4-mission-context-force-onboarding/4.4-03-PLAN.md
@.planning/phases/04.4-mission-context-force-onboarding/4.4-05-PLAN.md
@.planning/phases/04.4-mission-context-force-onboarding/4.4-07-PLAN.md
@.planning/phases/04.4-mission-context-force-onboarding/4.4-08-PLAN.md

Reference existing navigation patterns:
@frontend/src/App.tsx
@frontend/src/components/strategic/StrategicDashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Mission List and Detail Views</name>
  <files>
    frontend/src/components/mission/MissionList.tsx
    frontend/src/components/mission/MissionDetail.tsx
    frontend/src/components/mission/MissionDetail.css
    frontend/src/components/mission/index.ts
  </files>
  <action>
    1. Create MissionList.tsx:
       - Fetch missions on mount via missionService.listMissions()
       - Display as cards or table
       - Card shows: name, classification badge, state badge, participant count, created date
       - State colors: planning=blue, active=green, complete=gray, archived=dim
       - "New Mission" button opens wizard
       - Click mission navigates to MissionDetail
       - Filter by state dropdown

    2. Create MissionDetail.tsx:
       - Props: missionId, onBack
       - Fetch mission data on mount
       - Tabbed interface using react-tabs:
         - Overview: mission info, AO on mini-map, stats
         - Map: MissionMap component (fullscreen toggle)
         - Command: CommandTreeView with toggle to CommandMatrixView
         - Resources: ResourceCatalog component
         - Participants: ParticipantList with InviteModal trigger
         - Sensors: SensorList with registration form
       - Header with mission name, classification badge, state controls
       - State transition buttons (Activate, Complete, Archive)

    3. Create MissionDetail.css:
       - Tab styling matching command-center theme
       - Responsive layout
       - Fullscreen map toggle

    4. Create index.ts:
       - Export all mission components
  </action>
  <verify>
    - TypeScript compiles: `cd frontend && npx tsc --noEmit`
    - MissionList and MissionDetail render without errors
  </verify>
  <done>
    Mission list and detail views with all tabs complete.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate into App Navigation</name>
  <files>
    frontend/src/App.tsx
  </files>
  <action>
    1. Add mission views to App.tsx navigation state:
       - Add 'missions' and 'mission-detail' to view type
       - Import MissionList, MissionDetail, MissionWizard

    2. Add "Missions" button to navigation/header:
       - Position alongside existing nav (Dashboard, Strategic Planning, Validity, Admin)
       - Icon: map or target icon

    3. Add view rendering for mission views:
       ```tsx
       {view === 'missions' && (
         <MissionList
           onSelectMission={(id) => { setSelectedMissionId(id); setView('mission-detail'); }}
           onCreateMission={() => setView('mission-wizard')}
         />
       )}
       {view === 'mission-detail' && selectedMissionId && (
         <MissionDetail
           missionId={selectedMissionId}
           onBack={() => setView('missions')}
         />
       )}
       {view === 'mission-wizard' && (
         <MissionWizard
           onComplete={(id) => { setSelectedMissionId(id); setView('mission-detail'); }}
           onCancel={() => setView('missions')}
         />
       )}
       ```

    4. Add selectedMissionId to state
  </action>
  <verify>
    - TypeScript compiles: `cd frontend && npx tsc --noEmit`
    - Navigation to Missions works
    - Mission list displays
    - Clicking mission navigates to detail
  </verify>
  <done>
    Mission views integrated into main app navigation.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete mission context and force onboarding system:
    - Mission creation wizard with AO drawing
    - Participant invitation system
    - Command hierarchy tree and matrix views
    - Resource catalog with bulk import
    - Sensor registration
    - Mission map with MIL-STD-2525D symbology
    - Layer toggle controls
  </what-built>
  <how-to-verify>
    1. Start backend: `cd backend && npm run dev`
    2. Start frontend: `cd frontend && npm run dev`
    3. Navigate to Missions
    4. Click "New Mission" and complete wizard:
       - Enter name and classification
       - Draw area of operations on map
       - Add participants (optional)
       - Review and create
    5. View mission detail
    6. Test each tab:
       - Map: toggle layers, view symbology
       - Command: add units, create relationships
       - Resources: add equipment, import CSV
       - Sensors: register sensors
    7. Verify military symbols render correctly
    8. Verify layer toggles work
  </how-to-verify>
  <resume-signal>Type "approved" if end-to-end flow works, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compiles for frontend
- Navigation to missions works from any view
- Mission wizard creates missions
- Mission detail shows all sections
- Map displays with correct symbology
- All sub-components integrated properly
</verification>

<success_criteria>
- Mission list with filtering
- Mission detail with 6 tabs
- Navigation integrated into main app
- End-to-end flow: list -> wizard -> detail -> all tabs
- Human verification of complete system
</success_criteria>

<output>
After completion, create `.planning/phases/04.4-mission-context-force-onboarding/4.4-09-SUMMARY.md`
</output>
