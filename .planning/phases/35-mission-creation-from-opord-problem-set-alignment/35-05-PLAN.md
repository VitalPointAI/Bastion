---
phase: 35-mission-creation-from-opord-problem-set-alignment
plan: 05
type: execute
wave: 3
depends_on: ["35-02"]
files_modified:
  - backend/src/mission/mission-store.ts
  - backend/src/mission/participant-store.ts
  - backend/src/mission/invite-store.ts
  - backend/src/mission/schemas.ts
  - backend/src/mission/types.ts
autonomous: true
requirements:
  - MC-14
  - MC-15

must_haves:
  truths:
    - "Legacy backend/src/mission/ module is fully deleted"
    - "No other file in the codebase imports from backend/src/mission/"
    - "Application still compiles and starts without the legacy module"
  artifacts:
    - path: "backend/src/mission/"
      provides: "Directory should not exist after this plan"
  key_links: []
---

<objective>
Delete the legacy mission module and clean up all references.

Purpose: Complete the deprecation of `backend/src/mission/` -- missions are now tactical problem sets with JSONB metadata. The legacy module used a separate `missions` table with `MissionState`, `GeoJSONPolygon`, and `ParticipantRole` (`commander | staff | observer`) which are all superseded by the problem set framework.

Output: Legacy module deleted, all import references cleaned up, application compiles.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-mission-creation-from-opord-problem-set-alignment/35-CONTEXT.md
@.planning/phases/35-mission-creation-from-opord-problem-set-alignment/35-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy mission module and clean up imports</name>
  <files>backend/src/mission/mission-store.ts, backend/src/mission/participant-store.ts, backend/src/mission/invite-store.ts, backend/src/mission/schemas.ts, backend/src/mission/types.ts</files>
  <action>
**Step 1: Find all imports from the legacy module.**

Run grep to find any file importing from `backend/src/mission/`:
```bash
grep -rn "from.*['\"].*mission/" backend/src/ --include="*.ts" | grep -v "mission-creation" | grep -v node_modules
```

Also check for:
- `import.*from.*'../mission/` or `'./mission/`
- `require.*mission/`
- Any route registration for `/api/missions`

**Step 2: Remove imports and references.**

For each file that imports from the legacy module:
- If the import is used (e.g., `MissionState`, `GeoJSONPolygon`, `missionStore`): determine if the usage still exists in the consuming code. If so, either:
  - Replace with equivalent from the new `mission-creation` module or `problem-set` module
  - Remove the usage if it's dead code
- If the import is unused: just delete the import line

Check `backend/src/index.ts` (or equivalent server entry) for route registration of legacy mission routes and remove them.

Check `backend/src/mdmp/workflow-service.ts` and `backend/src/mdmp/integration.ts` for any references to the `missions` table or legacy mission types. The MDMP service uses `missionId` as a string key -- confirm it does NOT query the `missions` table. If it does, update to query `problem_sets` instead.

**Step 3: Delete all files in backend/src/mission/.**

Delete these files:
- `backend/src/mission/mission-store.ts`
- `backend/src/mission/participant-store.ts`
- `backend/src/mission/invite-store.ts`
- `backend/src/mission/schemas.ts`
- `backend/src/mission/types.ts`

Then delete the directory itself.

**Step 4: Verify clean compile.**

Run `npx tsc --noEmit` to confirm no broken imports. Fix any remaining references.

Note: Per user decision, no production data migration is needed -- the `missions` table is test/dev only. The table can remain in the database (it just won't be used). No DROP TABLE is needed in this plan.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Legacy backend/src/mission/ module fully deleted. No remaining imports. TypeScript compiles cleanly. Application builds without the legacy module.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `grep -rn "from.*mission/" backend/src/ --include="*.ts" | grep -v mission-creation | grep -v node_modules` returns no results
- `ls backend/src/mission/` fails (directory deleted)
</verification>

<success_criteria>
- All 5 legacy mission module files deleted
- All import references cleaned up or replaced
- MDMP service confirmed to not query missions table
- Full TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/35-mission-creation-from-opord-problem-set-alignment/35-05-SUMMARY.md`
</output>
