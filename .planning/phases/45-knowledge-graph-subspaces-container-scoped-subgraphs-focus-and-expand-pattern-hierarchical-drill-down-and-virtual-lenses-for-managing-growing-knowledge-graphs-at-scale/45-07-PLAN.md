---
phase: 45-knowledge-graph-subspaces
plan: 07
type: execute
wave: 4
depends_on: ["45-03", "45-04", "45-05", "45-06"]
files_modified:
  - frontend/src/components/brain/BrainController.tsx
  - frontend/src/components/brain/BrainToolbar.tsx
  - frontend/src/components/brain/BrainVisualization.tsx
  - frontend/src/components/brain/BrainLayout.tsx
autonomous: false
requirements:
  - WIRE-CONTROLLER
  - WIRE-TOOLBAR
  - WIRE-VISUALIZATION
  - WIRE-GHOST-RENDER

must_haves:
  truths:
    - "BrainController orchestrates all new hooks (lens, subspace, drill-down, N-hop)"
    - "BrainToolbar shows LensSelector instead of cluster toggle buttons"
    - "BrainBreadcrumb visible between toolbar and graph"
    - "SubspaceSidebar appears in left panel below ingestion feed"
    - "Double-click a node drills into subspace or node detail"
    - "Ghost stub nodes render with low opacity and small scale"
    - "Animated camera transitions on drill-down level changes"
    - "Expand button on Level 3 triggers N-hop loading"
  artifacts:
    - path: "frontend/src/components/brain/BrainController.tsx"
      provides: "Wired controller with all Phase 45 hooks"
      contains: "useBrainLens"
    - path: "frontend/src/components/brain/BrainToolbar.tsx"
      provides: "Toolbar with LensSelector replacing cluster toggle"
      contains: "LensSelector"
    - path: "frontend/src/components/brain/BrainVisualization.tsx"
      provides: "Ghost node rendering + double-click drill-down"
      contains: "isGhostStub"
  key_links:
    - from: "BrainController.tsx"
      to: "useBrainLens.ts"
      via: "import { useBrainLens }"
      pattern: "useBrainLens"
    - from: "BrainController.tsx"
      to: "useBrainSubspaces.ts"
      via: "import { useBrainSubspaces }"
      pattern: "useBrainSubspaces"
    - from: "BrainController.tsx"
      to: "useBrainDrillDown.ts"
      via: "import { useBrainDrillDown }"
      pattern: "useBrainDrillDown"
    - from: "BrainController.tsx"
      to: "useBrainNHop.ts"
      via: "import { useBrainNHop }"
      pattern: "useBrainNHop"
    - from: "BrainToolbar.tsx"
      to: "LensSelector.tsx"
      via: "replaces cluster toggle buttons"
      pattern: "LensSelector"
---

<objective>
Wire all Phase 45 hooks and components into the existing brain visualization. BrainController becomes the orchestrator for lens, subspace, drill-down, and N-hop state. BrainToolbar replaces cluster toggle with LensSelector. BrainVisualization adds ghost node rendering and double-click drill-down. BrainLayout accommodates breadcrumb and subspace sidebar.

Purpose: This is the integration plan that makes all previous plans functional as a unified user experience. Without this wiring, the individual hooks and components are isolated.
Output: Updated BrainController, BrainToolbar, BrainVisualization, BrainLayout
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-knowledge-graph-subspaces-container-scoped-subgraphs-focus-and-expand-pattern-hierarchical-drill-down-and-virtual-lenses-for-managing-growing-knowledge-graphs-at-scale/45-CONTEXT.md
@.planning/phases/45-knowledge-graph-subspaces-container-scoped-subgraphs-focus-and-expand-pattern-hierarchical-drill-down-and-virtual-lenses-for-managing-growing-knowledge-graphs-at-scale/45-RESEARCH.md
@.planning/phases/45-knowledge-graph-subspaces-container-scoped-subgraphs-focus-and-expand-pattern-hierarchical-drill-down-and-virtual-lenses-for-managing-growing-knowledge-graphs-at-scale/45-03-SUMMARY.md
@.planning/phases/45-knowledge-graph-subspaces-container-scoped-subgraphs-focus-and-expand-pattern-hierarchical-drill-down-and-virtual-lenses-for-managing-growing-knowledge-graphs-at-scale/45-04-SUMMARY.md
@.planning/phases/45-knowledge-graph-subspaces-container-scoped-subgraphs-focus-and-expand-pattern-hierarchical-drill-down-and-virtual-lenses-for-managing-growing-knowledge-graphs-at-scale/45-05-SUMMARY.md
@.planning/phases/45-knowledge-graph-subspaces-container-scoped-subgraphs-focus-and-expand-pattern-hierarchical-drill-down-and-virtual-lenses-for-managing-growing-knowledge-graphs-at-scale/45-06-SUMMARY.md

<interfaces>
From BrainController.tsx (current):
```typescript
export function BrainController({ problemSetId }: BrainControllerProps) {
  const fgRef = useRef<ForceGraphMethods | undefined>(undefined);
  const { data, loading, refetch } = useBrainData(problemSetId);
  const { clusterMode, setClusterMode, clusterLabels } = useBrainClustering(fgRef, data.nodes);
  // ... state for selectedNodeId, searchMatchIds, etc.
  // processedData pipeline: effectiveData -> markGapNodes -> searchDimming
  // Render: BrainLayout with BrainToolbar, IngestionSidebar, BrainVisualization, BrainDetailPanel, BrainTimeline
}
```

From useBrainLens (Plan 03):
```typescript
function useBrainLens(problemSetId): {
  activeLens, allLenses, setActiveLensId, saveLens, deleteLens, cloneLens, applyLensFilters
}
```

From useBrainSubspaces (Plan 04):
```typescript
function useBrainSubspaces(problemSetId, data): {
  subspaces, activeSubspaceId, setActiveSubspaceId, subspaceData,
  createManualSubspace, createSmartSubspace, deleteSubspace, updateSubspace
}
```

From useBrainDrillDown (Plan 05):
```typescript
function useBrainDrillDown(data, activeSubspaceId, subspaceData): {
  level, breadcrumbs, drillIntoSubspace, drillIntoNode, drillIntoDocuments, drillUp, drillData, focusNodeId, getCameraTarget
}
```

From useBrainNHop (Plan 06):
```typescript
function useBrainNHop(workspaceId): {
  expandedHops, expandedData, loading, showWarning, expand, reset, setFocusNode
}
```

From LensSelector (Plan 03):
```typescript
interface LensSelectorProps {
  activeLens, allLenses, onLensChange, onSaveLens?, onDeleteLens?, onCloneLens?
}
```

From SubspaceSidebar (Plan 04):
```typescript
interface SubspaceSidebarProps {
  subspaces, activeSubspaceId, onSubspaceSelect, onCreateManual?, onCreateSmart?, onDelete?, selectedNodeIds?
}
```

From BrainBreadcrumb (Plan 05):
```typescript
interface BrainBreadcrumbProps {
  breadcrumbs: BreadcrumbEntry[]; onNavigate: (index: number) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Phase 45 hooks into BrainController and update data pipeline</name>
  <files>frontend/src/components/brain/BrainController.tsx, frontend/src/components/brain/BrainLayout.tsx</files>
  <action>
**BrainController.tsx changes:**

1. **Add imports:** Import `useBrainLens`, `useBrainSubspaces`, `useBrainDrillDown`, `useBrainNHop` from their hook files. Import `LensSelector`, `SubspaceSidebar`, `BrainBreadcrumb` components.

2. **Add hooks (after existing hooks):**
```typescript
// Lens system (replaces direct useBrainClustering for mode management)
const {
  activeLens, allLenses, setActiveLensId,
  saveLens, deleteLens: deleteLensAction, cloneLens, applyLensFilters,
} = useBrainLens(problemSetId);

// Subspace system
const {
  subspaces, activeSubspaceId, setActiveSubspaceId, subspaceData,
  createManualSubspace, createSmartSubspace, deleteSubspace,
} = useBrainSubspaces(problemSetId, data);

// Drill-down system
const {
  level: drillLevel, breadcrumbs, drillIntoSubspace, drillIntoNode,
  drillIntoDocuments, drillUp, drillData, focusNodeId, getCameraTarget,
} = useBrainDrillDown(data, activeSubspaceId, subspaceData);

// N-hop expansion
const {
  expandedHops, expandedData, loading: nhopLoading, showWarning: nhopWarning,
  expand: expandNHop, reset: resetNHop, setFocusNode: setNHopFocusNode,
} = useBrainNHop(problemSetId);
```

3. **Update useBrainClustering call:** Change `clusterMode` source from direct state to `activeLens.clusterMode`:
```typescript
const { clusterMode: _clusterMode, setClusterMode, clusterLabels: _clusterLabels } = useBrainClustering(fgRef, data.nodes);
```
Add a `useEffect` that calls `setClusterMode(activeLens.clusterMode)` when `activeLens.clusterMode` changes. Per RESEARCH.md Pitfall 5, only call this when clusterMode actually changes (use a ref to track previous value).

4. **Update processedData pipeline:** Insert lens filtering and drill-down filtering:
```typescript
const processedData: BrainGraphData = (() => {
  // Start from drill-level data (already subspace-filtered if at Level 2+)
  let baseNodes = drillLevel === 'full' ? effectiveData.nodes : drillData.nodes;
  let baseEdges = drillLevel === 'full' ? effectiveData.edges : drillData.edges;

  // If at Level 3 with expanded N-hop data, merge it in
  if (drillLevel === 'node' && expandedData) {
    baseNodes = expandedData.nodes;
    baseEdges = expandedData.edges;
  }

  // Apply lens filters
  const lensFiltered = applyLensFilters(baseNodes);

  // Apply gap marking
  const withGaps = markGapNodes(lensFiltered);

  // Apply search dimming
  const withSearch = searchMatchIds !== null
    ? withGaps.map(n => ({ ...n, isSearchDimmed: !searchMatchIds.includes(n.id) }))
    : withGaps;

  // Filter edges to only include those between visible nodes
  const visibleIds = new Set(withSearch.map(n => n.id));
  const filteredEdges = baseEdges.filter(e => {
    const srcId = typeof e.source === 'string' ? e.source : (e.source as any).id;
    const tgtId = typeof e.target === 'string' ? e.target : (e.target as any).id;
    return visibleIds.has(srcId) && visibleIds.has(tgtId);
  });

  return { nodes: withSearch, edges: filteredEdges };
})();
```

5. **Add camera transition effect:** useEffect watching `drillLevel` + `breadcrumbs.length`:
```typescript
useEffect(() => {
  const target = getCameraTarget();
  if (target && fgRef.current) {
    fgRef.current.cameraPosition(target.position, target.lookAt, target.duration);
  }
}, [drillLevel, breadcrumbs.length]);
```

6. **Add double-click handler:**
```typescript
const handleNodeDoubleClick = useCallback((node: BrainNode) => {
  if (drillLevel === 'full' && node.containerId) {
    // At full graph: double-click drills into the node's container subspace
    setActiveSubspaceId(node.containerId);
    drillIntoSubspace(node.containerId, node.containerLabel || node.containerId);
  } else if (drillLevel === 'subspace') {
    // Inside subspace: double-click drills into node detail
    drillIntoNode(node.id);
    setNHopFocusNode(node.id);
  } else if (drillLevel === 'node' && node.type === 'document') {
    // At node detail: double-click on document drills into document layer
    drillIntoDocuments(node.id);
  }
}, [drillLevel, drillIntoSubspace, drillIntoNode, drillIntoDocuments, setActiveSubspaceId, setNHopFocusNode]);
```

7. **Wire subspace sidebar select:** When sidebar calls `onSubspaceSelect(id)`:
```typescript
const handleSubspaceSelect = useCallback((id: string | null) => {
  setActiveSubspaceId(id);
  if (id) {
    const sub = subspaces.find(s => s.id === id);
    drillIntoSubspace(id, sub?.name || id);
  } else {
    drillUp(0); // back to full graph
  }
}, [subspaces, setActiveSubspaceId, drillIntoSubspace, drillUp]);
```

8. **Update render tree:**
- Replace cluster toggle in BrainToolbar with LensSelector props
- Add BrainBreadcrumb between toolbar and visualization
- Add SubspaceSidebar below IngestionSidebar in leftSidebar
- Pass `onNodeDoubleClick={handleNodeDoubleClick}` to BrainVisualization
- At Level 3, show an "Expand" button (can be in the detail panel or as a floating button) that calls `expandNHop(focusNodeId!)`. Show the nhopWarning if `showWarning`.

**BrainLayout.tsx changes:**
Add a `breadcrumb` slot between `topBar` and `center`. This is a new prop rendered as a thin bar. Add the prop to BrainLayoutProps interface:
```typescript
interface BrainLayoutProps {
  topBar: React.ReactNode;
  breadcrumb?: React.ReactNode;  // NEW
  leftSidebar: React.ReactNode;
  center: React.ReactNode;
  rightPanel?: React.ReactNode;
  rightPanelOpen: boolean;
  timeline: React.ReactNode;
}
```
Render `breadcrumb` between `topBar` and the main content area in the layout JSX.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr/frontend && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <done>BrainController wires all 4 new hooks. Data pipeline: drill-level data > lens filter > gap marking > search dimming. Camera transitions fire on level change. Double-click drills down. Subspace sidebar and breadcrumb rendered in layout.</done>
</task>

<task type="auto">
  <name>Task 2: Update BrainToolbar and BrainVisualization for lens and ghost rendering</name>
  <files>frontend/src/components/brain/BrainToolbar.tsx, frontend/src/components/brain/BrainVisualization.tsx</files>
  <action>
**BrainToolbar.tsx changes:**

1. Remove the cluster toggle buttons entirely (the `CLUSTER_OPTIONS` array, the `cluster-toggle` div, the cluster toggle buttons)
2. Replace with LensSelector component in the same left position:
```typescript
import { LensSelector } from './LensSelector.js';
import type { BrainLens } from './types.js';
```

3. Update `BrainToolbarProps`:
   - Remove: `clusterMode: ClusterMode`, `onClusterModeChange: (mode: ClusterMode) => void`
   - Add: `activeLens: BrainLens`, `allLenses: BrainLens[]`, `onLensChange: (lensId: string) => void`, `onSaveLens?: () => void`, `onDeleteLens?: (id: string) => void`, `onCloneLens?: (id: string) => void`

4. In the JSX, replace the cluster toggle div with:
```tsx
<LensSelector
  activeLens={activeLens}
  allLenses={allLenses}
  onLensChange={onLensChange}
  onSaveLens={onSaveLens}
  onDeleteLens={onDeleteLens}
  onCloneLens={onCloneLens}
/>
```

**BrainVisualization.tsx changes:**

1. Add `onNodeDoubleClick` prop:
```typescript
interface BrainVisualizationProps {
  // ... existing props
  onNodeDoubleClick?: (node: BrainNode) => void;
}
```
Wire it to `react-force-graph-3d`'s `onNodeDoubleClick` callback prop (the library natively supports this).

2. Add ghost node rendering. In the node rendering callback (the function that creates Three.js meshes for each node), check for `(node as any).isGhostStub`:
   - If ghost: use opacity 0.15, scale 0.4x of normal node size, no label rendering
   - Keep the same shape as the node's type, just dimmed
   - For ghost nodes in the `nodeColor` callback: return `'rgba(100, 160, 255, 0.15)'`

3. Add ghost edge rendering. In the link rendering callbacks:
   - `linkColor`: if `(link as any).isGhostLink`, return `'rgba(100, 160, 255, 0.1)'`
   - `linkWidth`: if ghost link, return 0.3 (vs normal link width)
   - `linkOpacity`: if ghost link, return 0.1

4. Add expand button for Level 3. Add props for expansion:
```typescript
interface BrainVisualizationProps {
  // ... existing + onNodeDoubleClick
  drillLevel?: DrillLevel;
  expandedHops?: number;
  nhopWarning?: boolean;
  onExpand?: () => void;
}
```
When `drillLevel === 'node'` and `onExpand` is provided, render a floating "Expand" button (positioned bottom-center of the canvas) with a ripple/ring icon. If `nhopWarning` is true, show a small warning text: "Loading may be slow". Show `expandedHops` count as "Hop N".

Do NOT modify BrainVisualization.css for ghost rendering — use inline Three.js material properties. For the expand button, add minimal CSS or use inline styles.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr/frontend && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <done>BrainToolbar renders LensSelector instead of cluster toggle. BrainVisualization handles double-click for drill-down. Ghost stubs render at 0.15 opacity. Ghost edges render thin and faded. Expand button visible at Level 3. TypeScript compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 45 integration</name>
  <files>frontend/src/components/brain/BrainController.tsx</files>
  <action>
Human verification of the complete Phase 45 integration. All automated tasks are complete — this checkpoint confirms the visual and interactive behavior works correctly in the browser.
  </action>
  <verify>Manual browser verification per steps below</verify>
  <done>User confirms all 12 verification steps pass or provides issues to fix</done>
  <what-built>Complete Phase 45 integration: lens selector, subspace sidebar, breadcrumb trail, 4-level drill-down with animated transitions, ghost stub nodes, and N-hop expansion.</what-built>
  <how-to-verify>
    1. Open a problem set with brain data in the browser
    2. Verify the toolbar shows a lens dropdown instead of Container/DIME/Organic buttons
    3. Switch between J2 Intel, J3 Ops, J5 Plans, and Overview lenses — graph should filter nodes accordingly
    4. Check the left sidebar shows a "Subspaces" section with container-based entries
    5. Click a container subspace in the sidebar — graph should zoom into that subspace's nodes
    6. Verify breadcrumb trail appears: "Full Brain (N) > [container name] (M)"
    7. At Level 2 (inside subspace): verify ghost stub nodes visible at subspace boundary (faded, small)
    8. Double-click a node at Level 2 — should drill into Level 3 (node detail)
    9. Verify breadcrumb updates with node name
    10. At Level 3: click "Expand" button — should load 1-hop neighbors
    11. Click breadcrumb entries to navigate back up the hierarchy
    12. Verify the graph renders without errors across all transitions
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run typecheck` passes in frontend
- `npm run build` succeeds
- All new hooks wired in BrainController
- Cluster toggle removed from toolbar, replaced by LensSelector
- Ghost rendering uses low opacity (not hidden/removed)
- Camera transitions animate on drill-level changes
- Double-click triggers drill-down at each level
</verification>

<success_criteria>
- Complete user flow: Full graph > pick lens > enter subspace > drill into node > expand neighbors > view documents
- Breadcrumb trail always visible and clickable
- Ghost stubs at subspace boundaries
- N-hop expansion loads from backend
- No TypeScript errors, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/45-knowledge-graph-subspaces-container-scoped-subgraphs-focus-and-expand-pattern-hierarchical-drill-down-and-virtual-lenses-for-managing-growing-knowledge-graphs-at-scale/45-07-SUMMARY.md`
</output>
