---
phase: 39-operational-demonstration-data-package
plan: 05
type: execute
wave: 3
depends_on: ["39-01"]
files_modified:
  - scripts/seed-governance.sh
  - scripts/demo-data/governance/decision-gates.json
  - scripts/seed-assessment.sh
  - scripts/demo-data/assessment/aars.json
  - scripts/demo-data/assessment/metl-tasks.json
autonomous: true
requirements: [DEMO-06, DEMO-07]

must_haves:
  truths:
    - "Decision gates with recorded votes and commander approvals exist across Plan/Direct tabs"
    - "AAR records exist for tactical training events"
    - "METL proficiency tracking shows T/P/U ratings per task"
    - "Governance artifacts demonstrate the blockchain value proposition (audit trail, transparency)"
  artifacts:
    - path: "scripts/seed-governance.sh"
      provides: "Decision gate creation with approval history via gate API"
    - path: "scripts/seed-assessment.sh"
      provides: "AAR and METL task seeding via assessment API"
    - path: "scripts/demo-data/governance/"
      provides: "Decision gate fixture data with votes and approvals"
    - path: "scripts/demo-data/assessment/"
      provides: "AAR and METL task fixture data"
  key_links:
    - from: "scripts/seed-governance.sh"
      to: "http://localhost:3001/api/gates"
      via: "curl POST for gate creation"
      pattern: "curl.*gates"
    - from: "scripts/seed-assessment.sh"
      to: "http://localhost:3001/api/assessment"
      via: "curl POST for AARs and METL tasks"
      pattern: "curl.*assessment"
---

<objective>
Create DAO governance artifacts (decision gates with votes, approvals, audit trails) and assessment data (AARs, METL tasks, MOE/MOP measures). Governance demonstrates the blockchain value proposition — the key differentiator for BASTION. Assessment shows the training evaluation loop.

Purpose: Governance gates are essential to showing BASTION's unique value — blockchain-verified decision authority with transparent audit trails. Assessment data closes the training loop by showing how tactical events feed upward evaluation. Both are critical for military stakeholder credibility.

Output: Working seed-governance.sh creating 8-12 decision gates with approval history, and seed-assessment.sh creating AARs and METL proficiency records.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-operational-demonstration-data-package/39-CONTEXT.md
@.planning/phases/39-operational-demonstration-data-package/39-RESEARCH.md
@.planning/phases/39-operational-demonstration-data-package/39-01-SUMMARY.md
@backend/src/gates/gate-types.ts (GateType, GateTab, GateStatus const objects)
@backend/src/gates/gate-routes.ts (gate API endpoints)
@backend/src/api/assessment-routes.ts (AAR, METL endpoints)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create decision gates with votes and commander approvals</name>
  <files>
    scripts/seed-governance.sh
    scripts/demo-data/governance/decision-gates.json
  </files>
  <action>
Create decision gate fixtures spanning multiple tabs and problem sets, showing the governance workflow at different stages. Check backend/src/gates/gate-types.ts for valid GateType, GateTab, GateStatus values and gate-routes.ts for the API endpoint structure.

**Decision Gates (8-12 across tabs and levels):**

Plan tab gates:
1. DEMO-GATE-mission-analysis — "Mission Analysis Approval" (PS: CJTF, status: approved, tab: plan)
   - Commander approved mission analysis products
   - decisionContext: { approvedBy, rationale, timestamp }

2. DEMO-GATE-coa-selection — "COA 2 Selection: Maritime Blockade" (PS: CJTF, status: approved, tab: plan)
   - Commander selected COA 2 over alternatives
   - Includes voting record: 3 votes (Commander approve, XO approve, J3 approve)

3. DEMO-GATE-opord-release — "OPORD Release Authorization" (PS: CJTF, status: pending, tab: plan)
   - Awaiting final commander signature — demonstrates gate-blocked workflow

Direct tab gates:
4. DEMO-GATE-frago-release — "FRAGO 001 Release" (PS: CJTF, status: approved, tab: direct)
   - FRAGO authorizing shift to maritime interdiction strategy

5. DEMO-GATE-fire-authority — "Fire Authority Delegation" (PS: Strike Alpha, status: approved, tab: direct)
   - Lethal authority delegation with ROE constraints in decisionContext

Design tab gates:
6. DEMO-GATE-design-approval — "Operational Design Approval" (PS: Theater, status: approved, tab: design)
   - Commander approved operational approach and LOEs

Assess tab gates:
7. DEMO-GATE-aar-publish — "AAR Publication" (PS: Strike Alpha, status: approved, tab: assess)
   - Training AAR approved for distribution

8. DEMO-GATE-readiness-cert — "Unit Readiness Certification" (PS: Theater, status: pending, tab: assess)
   - Theater-level readiness assessment pending final review

Each gate: id, problemSetId, gateType, tab, title, description, enforcement (hard_block/soft_advisory), status (pending/approved/rejected), decisionContext (JSON with votes, rationale, commander DID, timestamps).

Script approach:
1. Source _helpers.sh, expect PS_* env vars
2. Read fixture JSON, create gates via POST /api/gates (or the actual endpoint from gate-routes.ts)
3. For approved gates, also set the approval via PATCH if gates are created in pending status first
4. DEMO-GATE-* prefix IDs
5. Print summary per tab
  </action>
  <verify>
    <automated>bash -n scripts/seed-governance.sh && python3 -c "import json; g=json.load(open('scripts/demo-data/governance/decision-gates.json')); assert len(g) >= 8, f'Only {len(g)} gates'; print(f'{len(g)} decision gates defined')"</automated>
  </verify>
  <done>8-12 decision gates across Plan/Direct/Design/Assess tabs with mix of approved/pending states, commander approval records, voting history, and audit trail metadata demonstrating blockchain governance value.</done>
</task>

<task type="auto">
  <name>Task 2: Create AARs and METL proficiency tracking data</name>
  <files>
    scripts/seed-assessment.sh
    scripts/demo-data/assessment/aars.json
    scripts/demo-data/assessment/metl-tasks.json
  </files>
  <action>
Create assessment fixtures for the Assess tab showing training evaluation loop: AARs from tactical events feeding METL proficiency tracking.

**AARs (3-5):**
1. DEMO-AAR-strike-alpha — "Strike Package Alpha After Action Review"
   - problemSetId: PS_STRIKE_ALPHA (tactical)
   - exercisePhase: "conflict-day-4"
   - observations: sustains (3-4), improves (3-4)
   - Sustains: "Effective coordination between CSG elements", "Rapid target acquisition under jamming"
   - Improves: "ROE confirmation delays at division level", "ISR feed latency exceeding 5min threshold"
   - recommendations: 2-3 action items

2. DEMO-AAR-logistics-bravo — "Logistics Convoy Bravo AAR"
   - exercisePhase: "conflict-day-10"
   - Focus on sustainment challenges and force protection

3. DEMO-AAR-cap-delta — "Combat Air Patrol Delta AAR"
   - exercisePhase: "conflict-day-22"
   - Focus on air superiority and electronic warfare challenges

**METL Tasks (10-15):**
METL tasks with T/P/U (Trained/Practiced/Untrained) proficiency ratings:
- Maritime Strike Operations (T) — demonstrated in Day 4
- Anti-Submarine Warfare (P) — partially practiced
- Amphibious Operations (U) — not exercised due to scenario constraints
- Integrated Air Defense (T) — demonstrated throughout
- Electronic Warfare/Counter-EW (P) — improving
- Maritime Interdiction Operations (T) — demonstrated in Day 22
- Joint Fire Support (P) — coordination challenges noted
- Force Protection (T) — sustained performance
- Intelligence Collection & Dissemination (P) — ISR latency issues
- Logistics/Sustainment Operations (P) — supply chain stress
- Coalition Interoperability (P) — communications challenges with partners
- Strategic Communications (U) — not exercised

Each METL task: id (DEMO-METL-NNN), name, proficiency (T/P/U), lastEvaluated (date), evaluationNotes, linkedAarIds, problemSetId.

Check backend/src/api/assessment-routes.ts for exact endpoint paths and request schemas. Script approach:
1. Source _helpers.sh, expect PS_* env vars
2. Create AARs via POST /api/assessment/aars (or actual endpoint)
3. Create METL tasks via POST /api/assessment/metl (or actual endpoint)
4. Link AARs to METL tasks where applicable
5. DEMO-AAR-* and DEMO-METL-* prefix IDs
  </action>
  <verify>
    <automated>bash -n scripts/seed-assessment.sh && python3 -c "import json; a=json.load(open('scripts/demo-data/assessment/aars.json')); m=json.load(open('scripts/demo-data/assessment/metl-tasks.json')); assert len(a) >= 3; assert len(m) >= 10; print(f'{len(a)} AARs, {len(m)} METL tasks')"</automated>
  </verify>
  <done>3-5 AARs linked to exercise phases with sustains/improves/recommendations. 10-15 METL tasks with T/P/U proficiency ratings showing realistic training evaluation patterns. Assessment data demonstrates upward aggregation from tactical events to theater readiness.</done>
</task>

</tasks>

<verification>
- Both scripts pass bash -n syntax check
- All fixture JSON files are valid
- Gate types and statuses match backend/src/gates/gate-types.ts constants
- AARs reference exercise phases and problem sets from prior seeds
- METL proficiency distribution is realistic (mix of T/P/U)
</verification>

<success_criteria>
- 8-12 decision gates with mix of approved/pending states and audit metadata
- 3-5 AARs with realistic observations and action items
- 10-15 METL tasks with proficiency ratings
- Governance data demonstrates blockchain audit trail value proposition
- Assessment data shows training-to-readiness evaluation loop
</success_criteria>

<output>
After completion, create `.planning/phases/39-operational-demonstration-data-package/39-05-SUMMARY.md`
</output>
