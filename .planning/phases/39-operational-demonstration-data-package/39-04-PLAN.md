---
phase: 39-operational-demonstration-data-package
plan: 04
type: execute
wave: 3
depends_on: ["39-01", "39-02"]
files_modified:
  - scripts/seed-jpp.sh
  - scripts/demo-data/jpp/cjtf-jpp-instance.json
  - scripts/demo-data/jpp/jpp-step-products.json
  - scripts/seed-agents.sh
  - scripts/demo-data/agents/strategic-fusion-outputs.json
  - scripts/demo-data/agents/adversary-modeling-outputs.json
  - scripts/demo-data/agents/escalation-modeling-outputs.json
  - scripts/demo-data/agents/assumption-audit-outputs.json
autonomous: true
requirements: [DEMO-04, DEMO-05]

must_haves:
  truths:
    - "JPP instance exists for CJTF-WestPac with step products across 7 JPP steps"
    - "Pre-seeded AI agent outputs exist for strategic fusion, adversary modeling, escalation modeling, and assumption auditing"
    - "AI outputs are polished and demonstrate the walkthrough path quality"
    - "JPP products reference real entities (units, actors, problem sets) from prior seeds"
  artifacts:
    - path: "scripts/seed-jpp.sh"
      provides: "JPP instance and step product creation via JPP API"
    - path: "scripts/seed-agents.sh"
      provides: "Pre-computed AI agent analysis outputs seeded as staff products"
    - path: "scripts/demo-data/jpp/"
      provides: "JPP instance and step product fixture data"
    - path: "scripts/demo-data/agents/"
      provides: "Pre-computed AI output fixtures for demo walkthrough"
  key_links:
    - from: "scripts/seed-jpp.sh"
      to: "http://localhost:3001/api/jpp"
      via: "curl POST for instance and step products"
      pattern: "curl.*jpp"
    - from: "scripts/seed-agents.sh"
      to: "http://localhost:3001/api/exercise"
      via: "curl POST for staff products/AI outputs"
      pattern: "curl.*(exercise|agents)"
---

<objective>
Create JPP (Joint Planning Process) instance with step products for the Plan tab, and pre-seeded AI agent analysis outputs for the walkthrough demo path. JPP shows the doctrinal planning workflow. AI outputs demonstrate the platform's autonomous analysis capability without requiring live LLM calls during the demo.

Purpose: JPP and AI outputs are the core value proposition — showing that BASTION automates the joint planning process with AI staff assistance. Pre-seeded outputs ensure predictable, polished demo content for the critical walkthrough path.

Output: Working seed-jpp.sh creating a JPP instance with 7-step products, and seed-agents.sh inserting pre-computed analysis from strategic fusion, adversary modeling, escalation modeling, and assumption auditing agents.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-operational-demonstration-data-package/39-CONTEXT.md
@.planning/phases/39-operational-demonstration-data-package/39-RESEARCH.md
@.planning/phases/39-operational-demonstration-data-package/39-01-SUMMARY.md
@.planning/phases/39-operational-demonstration-data-package/39-02-SUMMARY.md
@backend/src/api/jpp.ts (JPP instance and step product endpoints)
@backend/src/api/exercise.ts (staff product and AI output endpoints)
@backend/src/agents/langgraph/agent-seeder.ts (registered AI agents)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create JPP instance with 7-step products for CJTF-WestPac</name>
  <files>
    scripts/seed-jpp.sh
    scripts/demo-data/jpp/cjtf-jpp-instance.json
    scripts/demo-data/jpp/jpp-step-products.json
  </files>
  <action>
Create JPP fixtures and seed script for the CJTF-WestPac (operational level) problem set. The JPP follows JP 5-0's 7 steps.

**JPP Instance:** DEMO-JPP-cjtf-westpac
- problemSetId: PS_CJTF (from env)
- name: "CJTF-WestPac Campaign Plan — JPP"
- status: "step_4" (in progress — COA analysis)

**JPP Step Products (7 steps, each with key outputs):**

Step 1 — Planning Initiation:
- Initiating directive summary (from INDOPACOM theater directive)
- Mission statement: "CJTF-WestPac deters PRC aggression in the Western Pacific; on order, conducts maritime operations to defend Taiwan and maintain freedom of navigation in the Taiwan Strait and South China Sea"
- Commander's intent and planning guidance
- Status: complete

Step 2 — Mission Analysis:
- Specified, implied, essential tasks
- Facts and assumptions list (references assumption audit agent output)
- Risk assessment matrix
- Status: complete

Step 3 — COA Development:
- COA 1: Maritime Blockade — isolate Taiwan Strait, deny PLA naval access
- COA 2: Distributed Maritime Operations — dispersed lethality across island chain
- COA 3: Integrated Air-Maritime Defense — air superiority with naval screening
- Each COA: scheme of maneuver, main effort, supporting efforts, task organization
- Status: complete

Step 4 — COA Analysis and Wargaming:
- Wargaming results for each COA (strengths, weaknesses, key findings)
- Decision support matrix
- Status: in_progress (current step)

Step 5 — COA Comparison: status: pending
Step 6 — COA Approval: status: pending
Step 7 — Plan/Order Development: status: pending

Check backend/src/api/jpp.ts for exact endpoint paths, request body schemas, and step product format. The script should use POST /api/jpp/:problemSetId for instance creation and appropriate endpoints for step products.

Script approach:
1. Source _helpers.sh, expect PS_CJTF env var
2. Create JPP instance via API
3. Create step products for steps 1-4 (5-7 left pending for demo progression)
4. Use DEMO-JPP-* prefix IDs
5. Print summary of created steps
  </action>
  <verify>
    <automated>bash -n scripts/seed-jpp.sh && python3 -c "import json; j=json.load(open('scripts/demo-data/jpp/jpp-step-products.json')); assert len(j) >= 4, f'Only {len(j)} steps'; print(f'{len(j)} JPP step products defined')"</automated>
  </verify>
  <done>JPP instance created for CJTF-WestPac with 7 steps defined, steps 1-4 populated with realistic doctrinal content, steps 5-7 pending for demo progression narrative.</done>
</task>

<task type="auto">
  <name>Task 2: Create pre-seeded AI agent analysis output fixtures and seed script</name>
  <files>
    scripts/seed-agents.sh
    scripts/demo-data/agents/strategic-fusion-outputs.json
    scripts/demo-data/agents/adversary-modeling-outputs.json
    scripts/demo-data/agents/escalation-modeling-outputs.json
    scripts/demo-data/agents/assumption-audit-outputs.json
  </files>
  <action>
Create pre-computed AI agent output fixtures that represent polished analysis products. These are seeded as staff products or AI run logs so they appear in the UI without requiring live LLM invocations during the demo walkthrough.

**Strategic Fusion Agent outputs (2-3):**
- Cross-document objective synthesis across theater strategy documents
- Key finding: 4-5 fused strategic objectives with source document cross-references
- DIME/MIDLIFE theme classification
- Validity scoring with confidence levels

**Adversary Modeling Agent outputs (2-3):**
- PRC military capability assessment (PLA ETC force structure, readiness, doctrine)
- PRC strategic calculus analysis (decision factors, red lines, escalation thresholds)
- Adversary COA development (most likely, most dangerous, most unusual)

**Escalation Modeling Agent outputs (1-2):**
- Escalation ladder analysis for Taiwan Strait scenario (current level, trigger events, de-escalation paths)
- Effect cascade analysis from key US/PRC actions

**Assumption Audit Agent outputs (1-2):**
- Audit of JPP Step 2 assumptions against current intelligence
- Flagged assumptions: "Japan basing access" (HIGH confidence), "PRC no first-use" (MEDIUM — degrading), "Australia naval commitment" (HIGH)

Each output fixture includes: id (DEMO-AGENT-OUTPUT-NNN), agentRole, agentName, problemSetId, content (the actual analysis text — 200-500 words, polished), metadata (model used, run timestamp, confidence), status: "complete".

Check backend/src/api/exercise.ts for staff product or AI output storage endpoints. Look for routes like POST /api/exercise/:scenarioId/staff-products or similar. If no direct API exists, seed via direct PostgreSQL INSERT into the appropriate table (check StaffProductStore or similar).

Script approach:
1. Source _helpers.sh, expect PS_THEATER, PS_CJTF env vars
2. Read fixture JSON files
3. Insert via API or direct SQL depending on available endpoints
4. Use DEMO-AGENT-OUTPUT-* prefix IDs
5. Print summary of seeded agent outputs by role
  </action>
  <verify>
    <automated>bash -n scripts/seed-agents.sh && python3 -c "
import json, glob
total = 0
for f in glob.glob('scripts/demo-data/agents/*.json'):
    outputs = json.load(open(f))
    total += len(outputs) if isinstance(outputs, list) else 1
    print(f'{f}: {len(outputs) if isinstance(outputs, list) else 1} outputs')
assert total >= 6, f'Only {total} agent outputs, need 6+'
print(f'Total: {total} agent outputs')
"</automated>
  </verify>
  <done>8-10 pre-computed AI agent analysis outputs spanning strategic fusion, adversary modeling, escalation modeling, and assumption auditing. Content is polished walkthrough-quality with realistic military analysis language.</done>
</task>

</tasks>

<verification>
- Both scripts pass bash -n syntax check
- All fixture JSON files are valid JSON
- JPP step products follow the 7-step JP 5-0 structure
- AI agent outputs are polished and realistic
- All IDs use DEMO- prefix convention
</verification>

<success_criteria>
- JPP instance with 7 steps for CJTF-WestPac, steps 1-4 populated with doctrinal content
- 8-10 pre-seeded AI agent outputs covering 4 agent roles
- Content quality suitable for military/academic walkthrough presentation
- All data idempotent and tagged for cleanup
</success_criteria>

<output>
After completion, create `.planning/phases/39-operational-demonstration-data-package/39-04-SUMMARY.md`
</output>
