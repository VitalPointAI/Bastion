---
phase: 39-operational-demonstration-data-package
plan: 06
type: execute
wave: 4
depends_on: ["39-01", "39-02", "39-04", "39-05"]
files_modified:
  - scripts/seed-inheritance.sh
  - scripts/demo-data/inheritance/fragos.json
  - scripts/demo-data/inheritance/status-reports.json
  - scripts/demo-data/inheritance/overrides.json
  - scripts/seed-demo.sh
autonomous: true
requirements: [DEMO-08, DEMO-09]

must_haves:
  truths:
    - "Inheritance artifacts (FRAGOs, status reports, overrides) demonstrate cross-level propagation"
    - "FRAGO from theater propagates change notifications to component and tactical levels"
    - "Tactical status reports roll up to component and theater levels"
    - "Override tracking shows where child problem sets diverged from parent directives"
    - "Master orchestrator seed-demo.sh runs complete pipeline end-to-end"
  artifacts:
    - path: "scripts/seed-inheritance.sh"
      provides: "Cross-level inheritance artifacts (FRAGOs, status reports, overrides)"
    - path: "scripts/seed-demo.sh"
      provides: "Finalized master orchestrator with all scripts wired"
    - path: "scripts/demo-data/inheritance/"
      provides: "FRAGO, status report, and override fixture data"
  key_links:
    - from: "scripts/seed-inheritance.sh"
      to: "http://localhost:3001/api/problem-sets/:id/inheritance"
      via: "curl POST for FRAGOs, status reports, overrides"
      pattern: "curl.*inheritance"
    - from: "scripts/seed-demo.sh"
      to: "scripts/seed-*.sh"
      via: "source command in dependency order"
      pattern: "source scripts/seed-"
---

<objective>
Create inheritance artifacts demonstrating Phase 38's cross-level propagation system (FRAGOs, status reports, override tracking) and finalize the master orchestrator so the entire demo data package can be created with a single command.

Purpose: Inheritance is the architectural crown jewel of BASTION — showing how strategic directives propagate to tactical execution and how tactical status rolls back up. The finalized orchestrator enables repeatable one-command demo setup. This plan completes the data package.

Output: Working seed-inheritance.sh creating FRAGOs, status reports, and overrides across the hierarchy, and a complete seed-demo.sh that runs the full pipeline.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-operational-demonstration-data-package/39-CONTEXT.md
@.planning/phases/39-operational-demonstration-data-package/39-RESEARCH.md
@.planning/phases/39-operational-demonstration-data-package/39-01-SUMMARY.md
@.planning/phases/39-operational-demonstration-data-package/39-02-SUMMARY.md
@.planning/phases/39-operational-demonstration-data-package/39-04-SUMMARY.md
@.planning/phases/39-operational-demonstration-data-package/39-05-SUMMARY.md
@backend/src/api/inheritance.ts (FRAGO, status snapshots, annotations, RFIs — requires auth)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create inheritance artifacts (FRAGOs, status reports, overrides)</name>
  <files>
    scripts/seed-inheritance.sh
    scripts/demo-data/inheritance/fragos.json
    scripts/demo-data/inheritance/status-reports.json
    scripts/demo-data/inheritance/overrides.json
  </files>
  <action>
Create inheritance artifact fixtures and seed script demonstrating Phase 38's cross-level propagation. Note: inheritance routes DO require auth (requireAuth middleware confirmed). Use direct PostgreSQL INSERT matching the inheritance store's table schema, OR check if there's an internal-only endpoint.

**FRAGOs (3-4):**
1. DEMO-FRAGO-001 — "FRAGO 001: Shift to Maritime Interdiction"
   - Origin: PS_THEATER (INDOPACOM)
   - Propagates to: PS_CJTF, PS_7FLEET, PS_STRIKE_ALPHA
   - Content: Redirects main effort from deterrence to active maritime interdiction in Taiwan Strait
   - Status: propagated, acknowledged by CJTF, pending acknowledgment at tactical level

2. DEMO-FRAGO-002 — "FRAGO 002: ROE Update — Weapons Hold in Luzon Strait"
   - Origin: PS_THEATER
   - Propagates to: all children
   - Content: Updated ROE restricting engagement in Luzon Strait pending diplomatic resolution

3. DEMO-FRAGO-003 — "FRAGO 003: Logistics Priority Shift"
   - Origin: PS_CJTF
   - Propagates to: PS_STRIKE_ALPHA, PS_LOGISTICS_BRAVO
   - Content: Reprioritize logistics to support maritime interdiction ops

**Status Reports (3-4 upward):**
1. DEMO-STATUS-strike-alpha — Tactical status from Strike Package Alpha
   - Reports to: PS_7FLEET → PS_CJTF → PS_THEATER
   - Content: personnel readiness 92%, equipment 87%, mission completion 3/5 tasked sorties
   - Key events, resource status, objective progress (JSONB fields per Phase 38 convention)

2. DEMO-STATUS-logistics-bravo — Tactical status from Logistics Convoy Bravo
   - Reports to: PS_CJTF
   - Content: convoy 65% complete, fuel reserves at 40%, 2 ships diverted

3. DEMO-STATUS-7fleet — Component status from 7th Fleet
   - Reports to: PS_THEATER
   - Content: aggregate of subordinate unit statuses

**Overrides (2-3):**
1. DEMO-OVERRIDE-001 — CJTF overrides theater ROE for time-critical target authority
   - parentDirective: ROE from theater
   - override: Delegated authority to component for targets of opportunity
   - justification: Time-critical nature of maritime targets
   - parentNotified: true

2. DEMO-OVERRIDE-002 — 7th Fleet adjusts theater logistics priorities for emergency resupply
   - parentDirective: Logistics allocation from CJTF
   - override: Emergency fuel resupply to USS Ronald Reagan
   - justification: Operational necessity

Check backend/src/api/inheritance.ts for exact table names, column schemas, and store patterns. Since inheritance routes require auth, use direct PostgreSQL INSERT via docker exec. Look at the inheritance store files in backend/src/ for table schema.

Script approach:
1. Source _helpers.sh, expect PS_* env vars
2. Use PSQL direct inserts matching the inheritance tables (check store files for schema)
3. Create FRAGOs with propagation metadata
4. Create status reports with JSONB content
5. Create override records with justification
6. DEMO-FRAGO-*, DEMO-STATUS-*, DEMO-OVERRIDE-* prefix IDs
  </action>
  <verify>
    <automated>bash -n scripts/seed-inheritance.sh && python3 -c "import json; f=json.load(open('scripts/demo-data/inheritance/fragos.json')); s=json.load(open('scripts/demo-data/inheritance/status-reports.json')); o=json.load(open('scripts/demo-data/inheritance/overrides.json')); print(f'{len(f)} FRAGOs, {len(s)} status reports, {len(o)} overrides')"</automated>
  </verify>
  <done>3-4 FRAGOs propagating downward through hierarchy, 3-4 status reports aggregating upward, 2-3 overrides with justification and parent notification. Demonstrates full cross-level inheritance system.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize master orchestrator and end-to-end verification</name>
  <files>
    scripts/seed-demo.sh
  </files>
  <action>
Update seed-demo.sh (created as skeleton in Plan 01) to replace all placeholder echo statements with actual `source` calls to the now-complete seed scripts. Add:

1. **Remove all placeholder lines** — replace "Skipping {name} — not yet implemented" with actual source calls
2. **Timing per script:** Record start/end time for each script and print duration
3. **Summary report:** At end, print table showing:
   - Script name, records created, duration
   - Total counts: problem sets, actors, relationships, OSINT events, documents, gates, AARs, METL tasks, FRAGOs
4. **Error handling:** If any script fails, report which script failed and which succeeded, then exit with error
5. **--only flag:** Allow running individual scripts: `seed-demo.sh --only=graph` runs just seed-graph.sh (after ensuring dependencies ran or PS_* env vars are set)
6. **--verbose flag:** When set, show curl output; when not set, redirect to /dev/null
7. **Health check enhancement:** Add retry logic for backend health check (up to 30 seconds wait with 5-second intervals)
8. **Mode demonstration:** After all seeding, switch user mode between training/operational to show both views exist (via the mode toggle API if available)

Final execution order (all source calls):
```
source scripts/seed-problem-sets.sh    # L1: hierarchy + env vars
source scripts/seed-command-units.sh   # L1: military units
source scripts/seed-graph.sh           # L2: RAFT actors/relationships
source scripts/seed-osint.sh           # L2: OSINT events
source scripts/seed-documents.sh       # L2: document uploads
source scripts/seed-design.sh          # L3: operational design
source scripts/seed-jpp.sh            # L3: joint planning process
source scripts/seed-agents.sh         # L3: AI agent outputs
source scripts/seed-governance.sh     # L3: decision gates
source scripts/seed-assessment.sh     # L3: AARs + METL
source scripts/seed-inheritance.sh    # L4: cross-level propagation
```

Also update seed-cleanup.sh if any new tables or data stores were discovered during plans 02-05 that need cleanup.
  </action>
  <verify>
    <automated>bash -n scripts/seed-demo.sh && grep -c "source scripts/seed-" scripts/seed-demo.sh | xargs -I {} test {} -ge 10 && echo "All 11 seed scripts wired in orchestrator"</automated>
  </verify>
  <done>Master orchestrator runs all 11 seed scripts in dependency order with timing, summary report, --reset/--clean/--verbose/--only flags. One command creates the complete demo data package.</done>
</task>

</tasks>

<verification>
- seed-demo.sh passes syntax check and has all 11 source calls
- seed-inheritance.sh passes syntax check
- All fixture JSON files are valid
- seed-cleanup.sh handles all data stores (PostgreSQL tables, Neo4j nodes)
- --reset flag runs cleanup before seeding
- --clean flag runs cleanup only and exits
</verification>

<success_criteria>
- Inheritance artifacts demonstrate downward propagation (FRAGOs) and upward reporting (status)
- Override tracking shows divergence from parent directives with justification
- Master orchestrator runs complete pipeline end-to-end with one command
- Summary report shows counts for all seeded data types
- --reset flag enables clean slate demo setup
- Complete demo data package is repeatable and idempotent
</success_criteria>

<output>
After completion, create `.planning/phases/39-operational-demonstration-data-package/39-06-SUMMARY.md`
</output>
