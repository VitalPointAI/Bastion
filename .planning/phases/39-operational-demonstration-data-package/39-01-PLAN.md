---
phase: 39-operational-demonstration-data-package
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seed-demo.sh
  - scripts/seed-cleanup.sh
  - scripts/seed-problem-sets.sh
  - scripts/demo-data/problem-sets/theater-campaign.json
  - scripts/demo-data/problem-sets/component-plans.json
  - scripts/demo-data/problem-sets/tactical-missions.json
  - scripts/seed-command-units.sh
  - scripts/demo-data/command/units.json
autonomous: true
requirements: [DEMO-01, DEMO-09, DEMO-10]

must_haves:
  truths:
    - "Running seed-problem-sets.sh creates a 3-level problem set hierarchy (theater > component > tactical)"
    - "Running seed-cleanup.sh removes all DEMO- prefixed data without touching user-created data"
    - "seed-demo.sh skeleton exists with --reset, --clean, --verbose flags"
    - "Problem sets include real unit designations (INDOPACOM, CJTF-WestPac, strike packages)"
    - "Command units with MIL-STD-2525D SIDC codes are created for each problem set level"
  artifacts:
    - path: "scripts/seed-problem-sets.sh"
      provides: "3-level problem set hierarchy creation with auth handling"
    - path: "scripts/seed-cleanup.sh"
      provides: "Tagged data cleanup (Neo4j DEMO- prefix + PostgreSQL cascade delete)"
    - path: "scripts/seed-demo.sh"
      provides: "Master orchestrator skeleton with flags and dependency order"
    - path: "scripts/seed-command-units.sh"
      provides: "Military unit creation with SIDC codes"
    - path: "scripts/demo-data/problem-sets/"
      provides: "JSON fixture files for all 3 hierarchy levels"
  key_links:
    - from: "scripts/seed-demo.sh"
      to: "scripts/seed-problem-sets.sh"
      via: "source command in dependency order"
      pattern: "source scripts/seed-problem-sets.sh"
    - from: "scripts/seed-problem-sets.sh"
      to: "http://localhost:3001/api/problem-sets"
      via: "curl POST with auth token"
      pattern: "curl.*POST.*problem-sets"
---

<objective>
Create the foundation seed scripts: problem set hierarchy (3 levels deep), command units, cleanup script, and master orchestrator skeleton. This is the foundation all other seed scripts depend on because they need problem set IDs to associate their data with.

Purpose: Problem sets are the top-level organizational structure in BASTION. Every other data type (graph, documents, design, governance, etc.) belongs to a problem set. This plan creates the hierarchy and the shared infrastructure (cleanup, orchestrator) that all subsequent plans build on.

Output: Working seed-problem-sets.sh that creates INDOPACOM Theater Campaign > CJTF-WestPac + 7th Fleet > tactical strike/logistics missions. Cleanup script. Orchestrator skeleton. Command units with real military designations.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-operational-demonstration-data-package/39-CONTEXT.md
@.planning/phases/39-operational-demonstration-data-package/39-RESEARCH.md
@scripts/seed-graph-data.sh (existing pattern — curl + cypher-shell + python3 JSON parsing)
@backend/src/api/problem-sets.ts (POST / requires requireAuth — need auth token approach)
@backend/src/api/command.ts (unit creation endpoints)
@backend/src/auth/auth-instance.ts (auth middleware — near-phantom-auth)

<interfaces>
<!-- Problem set creation requires auth. The seed script must either:
     1. Create a demo user via direct SQL INSERT into anon_users/user_profiles tables
     2. Use the backend's internal store directly
     Auth approach: Insert a demo user row directly via psql, then use x-did header
     with that user's DID for all authenticated requests.

     The requireAuth middleware from @vitalpoint/near-phantom-auth checks for a valid
     session cookie or token. For seed scripts, the simplest approach is to create
     a helper function that registers a seed user and obtains a session token via
     the auth API, OR bypass auth by inserting directly through the PostgreSQL stores.

     Recommended: Use direct psql INSERT for problem sets since the API has complex
     auth requirements. Alternatively, check if there's a way to create a session
     programmatically. The existing seed-graph-data.sh doesn't need auth (graph routes
     are unprotected), so this is a new challenge.

     Routes WITHOUT requireAuth (safe for curl):
     - All graph routes (actors, relationships, tensions, OSINT events)
     - Exercise routes (scenarios, documents, IPB, COAs)
     - Design routes (operational design sections)
     - JPP routes
     - Assessment routes
     - Gate routes
     - Command routes
     - Document routes

     Routes WITH requireAuth:
     - Problem set CRUD (POST /, GET /:id, PATCH /:id, DELETE /:id)
     - Problem set members, invites, compartments
     - Inheritance routes (context, changelog, notifications)

     Strategy: For problem sets, use the ProblemSetStore directly via a small
     TypeScript seed helper, OR seed via direct SQL INSERT matching the store's
     schema. For all other data, use curl against unprotected API endpoints.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create problem set hierarchy seed script with fixtures</name>
  <files>
    scripts/seed-problem-sets.sh
    scripts/demo-data/problem-sets/theater-campaign.json
    scripts/demo-data/problem-sets/component-plans.json
    scripts/demo-data/problem-sets/tactical-missions.json
  </files>
  <action>
Create `scripts/demo-data/problem-sets/` directory and JSON fixture files for a 3-level problem set hierarchy following the Pacific Strategy AY26 arc:

**Theater level (strategic) — 1 problem set:**
- INDOPACOM Theater Campaign — "Pacific Strategy AY26: Indo-Pacific theater-level campaign plan addressing PRC aggression toward Taiwan"
- echelon: "strategic", mode: "training", classification: "SECRET"

**Component level (operational) — 3 problem sets, children of theater:**
- CJTF-WestPac Campaign Plan — Combined Joint Task Force Western Pacific
- 7th Fleet Maritime Operations — Naval component campaign
- 5th Air Force Air Operations — Air component campaign
- All echelon: "operational", parentProblemSetId references theater

**Tactical level — 4-5 missions, children of various component plans:**
- Strike Package Alpha — Taiwan Strait (carrier strike, child of 7th Fleet)
- Logistics Convoy Bravo — Guam-Philippines resupply (child of CJTF-WestPac)
- 3rd MEF Amphibious Ready Group — Marine expeditionary (child of CJTF-WestPac)
- CAP Station Delta — Combat air patrol (child of 5th Air Force)
- ISR Collection Echo — Intelligence mission (child of 5th Air Force)
- All echelon: "tactical"

Create `scripts/seed-problem-sets.sh` that:
1. Sources a shared helper from `scripts/demo-data/_helpers.sh` (create this too) with common vars (API base URL, DID header, error handling, JSON parsing via python3)
2. Since POST /api/problem-sets requires auth, use direct PostgreSQL INSERT via `docker exec bastion-postgres psql` matching the problem_sets table schema. Look at the ProblemSetStore in `backend/src/problem-set/problem-set-store.ts` for exact column names and defaults.
3. Reads fixture JSON files and creates problem sets in dependency order (theater first, then component, then tactical)
4. Uses deterministic IDs with DEMO- prefix (e.g., DEMO-PS-indopacom-theater, DEMO-PS-cjtf-westpac, DEMO-PS-strike-alpha)
5. Uses INSERT ... ON CONFLICT (id) DO UPDATE for idempotent re-runs
6. Exports environment variables: PS_THEATER, PS_CJTF, PS_7FLEET, PS_5AF, and tactical PS IDs for downstream scripts
7. Tags all records with source='demo-seed' column (add to INSERT if column exists, or use description field)
8. Prints created IDs and hierarchy summary

Check `backend/src/problem-set/problem-set-store.ts` for exact table schema (columns, types, defaults). The key columns are likely: id, name, description, echelon, classification, mode, parent_problem_set_id, created_by_did, created_at, updated_at.

Also create the duplicate problem sets in operational mode (1 theater + 1 component) to demonstrate the Phase 22 training/operational mode transition — per user decision "Demo shows both training mode AND operational mode."
  </action>
  <verify>
    <automated>bash -n scripts/seed-problem-sets.sh && echo "Syntax OK" && test -f scripts/demo-data/problem-sets/theater-campaign.json && test -f scripts/demo-data/problem-sets/component-plans.json && test -f scripts/demo-data/problem-sets/tactical-missions.json && echo "All fixture files exist"</automated>
  </verify>
  <done>seed-problem-sets.sh creates 9-12 problem sets across 3 echelons in dependency order with deterministic DEMO- IDs, exports PS IDs as env vars, handles both training and operational modes, uses idempotent upserts via direct PostgreSQL</done>
</task>

<task type="auto">
  <name>Task 2: Create cleanup script, command units seed, and master orchestrator skeleton</name>
  <files>
    scripts/seed-cleanup.sh
    scripts/seed-command-units.sh
    scripts/demo-data/command/units.json
    scripts/seed-demo.sh
    scripts/demo-data/_helpers.sh
  </files>
  <action>
**Create `scripts/demo-data/_helpers.sh`** — shared helper sourced by all seed scripts:
- `API="http://localhost:3001/api"` base URL
- `DID="demo-seed"` ownership tag
- `CYPHER="docker exec bastion-neo4j cypher-shell -u neo4j -p password"` Neo4j access
- `PSQL="docker exec bastion-postgres psql -U bastion -d bastion -t -A"` PostgreSQL access
- Helper functions: `check_backend()` (curl health check), `check_neo4j()` (cypher-shell ping), `parse_json_id()` (python3 JSON field extractor), `log_success()`, `log_error()`, `log_info()`
- `set -e` for fail-fast

**Create `scripts/seed-cleanup.sh`:**
- Sources _helpers.sh
- Deletes all Neo4j nodes with ID starting with 'DEMO-': `MATCH (n) WHERE n.id STARTS WITH 'DEMO-' DETACH DELETE n`
- Deletes all PostgreSQL problem sets with DEMO- prefix IDs (cascade handles members, activities, etc.): `DELETE FROM problem_sets WHERE id LIKE 'DEMO-%'`
- Deletes demo decision gates: `DELETE FROM decision_gates WHERE id LIKE 'DEMO-%'` (check actual table name in gate store)
- Deletes demo OSINT events if stored in PostgreSQL
- Prints count of deleted records per table
- Never touches records without DEMO- prefix

**Create `scripts/seed-command-units.sh`:**
- Sources _helpers.sh and expects PS_* env vars from seed-problem-sets.sh
- Creates 10-15 military units with real designations via POST /api/command/units (no auth required):
  - Theater: INDOPACOM HQ, PACFLT, PACAF
  - Component: CJTF-WestPac staff, 7th Fleet (USS Ronald Reagan CSG, USS America ESG), 5th Air Force (18th Wing, 35th FW)
  - Tactical: 3rd MEF, VFA-102 Diamondbacks, HSC-25 Island Knights
- Each unit has MIL-STD-2525D SIDC code, parent unit reference, problem set association
- Uses deterministic DEMO-UNIT-* IDs
- JSON fixtures in scripts/demo-data/command/units.json

**Create `scripts/seed-demo.sh`** — master orchestrator:
- Sources _helpers.sh
- Parses flags: --reset, --clean, --verbose, --only=SCRIPT_NAME (for selective seeding)
- Health checks: verify backend API and Neo4j are responsive before proceeding
- If --reset: runs seed-cleanup.sh first
- If --clean: runs seed-cleanup.sh and exits
- Dependency-ordered execution (use `source` to share env vars):
  1. seed-problem-sets.sh (Level 1 — exports PS_* IDs)
  2. seed-command-units.sh (Level 1 — uses PS_* IDs)
  3. seed-graph.sh (Level 2 — placeholder echo)
  4. seed-osint.sh (Level 2 — placeholder echo)
  5. seed-documents.sh (Level 2 — placeholder echo)
  6. seed-design.sh (Level 3 — placeholder echo)
  7. seed-jpp.sh (Level 3 — placeholder echo)
  8. seed-agents.sh (Level 3 — placeholder echo)
  9. seed-governance.sh (Level 3 — placeholder echo)
  10. seed-assessment.sh (Level 3 — placeholder echo)
  11. seed-inheritance.sh (Level 4 — placeholder echo)
- Prints final summary with counts and elapsed time
- Placeholder scripts just echo "Skipping {name} — not yet implemented" for now
  </action>
  <verify>
    <automated>bash -n scripts/seed-cleanup.sh && bash -n scripts/seed-demo.sh && bash -n scripts/seed-command-units.sh && bash -n scripts/demo-data/_helpers.sh && echo "All scripts pass syntax check"</automated>
  </verify>
  <done>Cleanup script deletes all DEMO- prefixed data from PostgreSQL and Neo4j. Command units script creates 10-15 real military units. Master orchestrator runs all scripts in order with --reset/--clean/--verbose flags. Helper file provides shared utilities.</done>
</task>

</tasks>

<verification>
- All shell scripts pass `bash -n` syntax check
- Fixture JSON files are valid JSON (`python3 -m json.tool` on each)
- seed-demo.sh --help or dry run shows correct execution order
- _helpers.sh defines all shared functions and variables
</verification>

<success_criteria>
- 3-level problem set hierarchy defined with 9-12 problem sets across strategic/operational/tactical echelons
- Deterministic DEMO- prefixed IDs on all entities
- Cleanup script targets only DEMO- records
- Master orchestrator skeleton with all flags and placeholder slots
- Command units with real military designations and SIDC codes
- Both training and operational mode problem sets for Phase 22 demo
</success_criteria>

<output>
After completion, create `.planning/phases/39-operational-demonstration-data-package/39-01-SUMMARY.md`
</output>
