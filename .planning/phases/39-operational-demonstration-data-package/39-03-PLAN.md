---
phase: 39-operational-demonstration-data-package
plan: 03
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - scripts/seed-documents.sh
  - scripts/demo-data/documents/document-manifest.json
  - scripts/seed-design.sh
  - scripts/demo-data/design/theater-design.json
  - scripts/demo-data/design/component-design.json
autonomous: true
requirements: [DEMO-03, DEMO-04]

must_haves:
  truths:
    - "Sample documents from scenario/ directory are uploaded to problem sets via the exercise API"
    - "Operational design sections (problem framing, COG analysis, LOE/LOO, operational approach) are populated for theater and component problem sets"
    - "Documents cover multiple types (strategy docs, OPORDs, intelligence reports)"
    - "Design data shows a coherent planning progression from strategic to operational level"
  artifacts:
    - path: "scripts/seed-documents.sh"
      provides: "Document upload via exercise API with scenario PDFs"
    - path: "scripts/seed-design.sh"
      provides: "Operational design section population via design API"
    - path: "scripts/demo-data/documents/document-manifest.json"
      provides: "Manifest of documents to upload with metadata"
    - path: "scripts/demo-data/design/"
      provides: "Design section content for theater and component levels"
  key_links:
    - from: "scripts/seed-documents.sh"
      to: "http://localhost:3001/api/exercise/scenarios"
      via: "curl POST multipart upload"
      pattern: "curl.*exercise.*documents.*upload"
    - from: "scripts/seed-design.sh"
      to: "http://localhost:3001/api/design"
      via: "curl PATCH per section"
      pattern: "curl.*PATCH.*design.*problem-framing"
---

<objective>
Create document upload seed script (leveraging existing scenario/ PDFs) and operational design content for the Design tab. Documents feed the strategy document reviewer agent and populate the Understand tab. Design content populates the Design tab showing JP 5-0 operational design methodology.

Purpose: Documents demonstrate the full document ingestion pipeline (upload > extraction > objective fusion). Design content demonstrates the operational design workflow central to JP 5-0 planning methodology — a key differentiator for military audiences.

Output: Working seed-documents.sh uploading 8-12 scenario documents, and seed-design.sh populating problem framing, COG analysis, LOE/LOO, and operational approach sections.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-operational-demonstration-data-package/39-CONTEXT.md
@.planning/phases/39-operational-demonstration-data-package/39-RESEARCH.md
@.planning/phases/39-operational-demonstration-data-package/39-01-SUMMARY.md
@backend/src/api/exercise.ts (document upload pipeline)
@backend/src/api/design.ts (operational design CRUD — PATCH per section)
@scenario/ (existing Pacific Strategy AY26 PDFs)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create document upload seed script with scenario PDF manifest</name>
  <files>
    scripts/seed-documents.sh
    scripts/demo-data/documents/document-manifest.json
  </files>
  <action>
Create a manifest JSON file listing 8-12 documents to upload from the existing scenario/ directory, and a seed script that uploads them via the exercise API.

**Document manifest** — select from existing scenario/ files:
1. "01. Pacific Strategy Experiential Learning Event_Directive_AY26.pdf" — strategic directive (theater level)
2. Blue Team planning documents (2-3 key ones from scenario/blue team/)
3. Red Team situation updates (2-3 from scenario/red team/)
4. Scenario phase documents (2-3 from scenario/scenario phases/)

Each manifest entry: { id: "DEMO-DOC-NNN", filename, sourcePath (relative to repo root), problemSetId (which PS to associate), team (blue/red/scenario), phase (competition/crisis/conflict/negotiation), docType (directive/sitrep/plan/intelligence) }

**Seed script approach:**
1. Source _helpers.sh, expect PS_THEATER and PS_CJTF env vars
2. Read manifest JSON
3. For each document, check if the exercise API has a scenario linked to the problem set. If not, create a scenario entry first via POST /api/exercise/scenarios (check endpoint structure in exercise.ts)
4. Upload documents via multipart/form-data curl to the exercise document upload endpoint
5. Use --max-time 120 on curl for document uploads (NLP extraction takes time per research pitfall 4)
6. Track upload results and print summary
7. For documents where extraction would be too slow, note that pre-computed AI outputs will be seeded separately in plan 05

Important: Check the actual upload endpoint in backend/src/api/exercise.ts — it may be POST /api/exercise/scenarios/:scenarioId/documents/upload or similar. The exact path and required fields must match the actual route definition.
  </action>
  <verify>
    <automated>bash -n scripts/seed-documents.sh && python3 -c "import json; m=json.load(open('scripts/demo-data/documents/document-manifest.json')); assert len(m) >= 8, f'Only {len(m)} docs'; print(f'{len(m)} documents in manifest')"</automated>
  </verify>
  <done>Document manifest defines 8-12 scenario documents mapped to problem sets. Seed script uploads via exercise API with multipart/form-data and handles extraction timeouts gracefully.</done>
</task>

<task type="auto">
  <name>Task 2: Create operational design seed script with Design tab content</name>
  <files>
    scripts/seed-design.sh
    scripts/demo-data/design/theater-design.json
    scripts/demo-data/design/component-design.json
  </files>
  <action>
Create JSON fixtures with realistic operational design content and seed script that populates the Design tab via PATCH /api/design/:psId/:section (no auth required).

**Theater-level design (INDOPACOM):**
- **Problem Framing:**
  - problemStatement: "How does USINDOPACOM deter PRC military coercion against Taiwan while maintaining regional stability, alliance cohesion, and freedom of navigation in the Indo-Pacific?"
  - desiredEndState: "PRC deterred from forceful reunification; Taiwan maintains de facto independence; alliance network strengthened; rules-based international order preserved in Western Pacific"
  - limitations: ROE constraints, coalition partner national caveats, logistics sustainment constraints, escalation management requirements
  - assumptions: Japan provides basing access under treaty, Australia commits to SCS operations, PRC rational actor calculus, no first-use nuclear policy holds
  - status: "complete"

- **Center of Gravity Analysis:**
  - friendly COG: Alliance cohesion and forward basing network
  - adversary COG: PRC integrated joint force projection capability
  - critical capabilities, requirements, vulnerabilities for each
  - status: "complete"

- **Lines of Effort/Operation:**
  - LOE 1: Deter PRC Military Aggression (maritime presence, air superiority, ISR)
  - LOE 2: Strengthen Alliance Network (bilateral exercises, interoperability, burden sharing)
  - LOE 3: Maintain Regional Stability (ASEAN engagement, economic alternatives to BRI)
  - LOE 4: Information Operations (counter PRC narrative, support Taiwan resilience)

- **Operational Approach:**
  - approach text summarizing the operational logic connecting LOEs to end state
  - phasing concept: Shape > Deter > Seize Initiative > Dominate > Stabilize

**Component-level design (CJTF-WestPac):**
- Similar structure but focused on the combined joint task force operational problem
- More tactical detail on force employment, objectives, decisive points

Script approach:
1. Source _helpers.sh, expect PS_THEATER and PS_CJTF env vars
2. For each problem set, PATCH each design section: problem-framing, cog-analysis, lines-of-effort, operational-approach
3. Check exact section names by reviewing backend/src/api/design.ts route parameters
4. Print summary of sections populated
  </action>
  <verify>
    <automated>bash -n scripts/seed-design.sh && python3 -c "import json; t=json.load(open('scripts/demo-data/design/theater-design.json')); assert 'problemFraming' in t or 'problem_framing' in t or 'sections' in t, 'Missing design sections'; print('Theater design fixture valid')"</automated>
  </verify>
  <done>Design tab content populated for theater and component problem sets covering all 4 operational design sections with realistic JP 5-0 methodology content. PATCH calls use correct API endpoints.</done>
</task>

</tasks>

<verification>
- Both scripts pass bash -n syntax check
- All fixture JSON files are valid
- Document manifest references files that actually exist in scenario/
- Design fixtures contain all 4 operational design sections
</verification>

<success_criteria>
- 8-12 scenario documents mapped in manifest with upload instructions
- Operational design content for theater and component levels
- All 4 design sections (problem framing, COG, LOE/LOO, operational approach) populated
- Content is doctrinally accurate and tells a coherent strategic narrative
</success_criteria>

<output>
After completion, create `.planning/phases/39-operational-demonstration-data-package/39-03-SUMMARY.md`
</output>
