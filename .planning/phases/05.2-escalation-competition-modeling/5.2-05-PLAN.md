---
phase: 5.2-escalation-competition-modeling
plan: 05
type: execute
wave: 3
depends_on: [5.2-02, 5.2-03]
files_modified:
  - frontend/src/components/escalation/EscalationLadder.tsx
  - frontend/src/components/escalation/EscalationLadder.css
  - frontend/src/components/escalation/EffectChainDiagram.tsx
  - frontend/src/components/escalation/EffectChainDiagram.css
  - frontend/src/components/escalation/index.ts
autonomous: true

must_haves:
  truths:
    - "Escalation ladder displays as a vertical visualization with rungs from peace to conflict"
    - "Current position on the ladder is highlighted with threshold markers visible"
    - "Clicking a rung shows triggers, de-escalation conditions, and characteristic actions"
    - "Effect chain diagram shows directed flow with DIME domain swim lanes"
    - "Effect nodes are clickable to see details and explore chains"
    - "Both visualizations are fully interactive"
  artifacts:
    - path: "frontend/src/components/escalation/EscalationLadder.tsx"
      provides: "Escalation ladder vertical visualization"
      contains: "EscalationLadder"
    - path: "frontend/src/components/escalation/EffectChainDiagram.tsx"
      provides: "Effect chain directed flow diagram with DIME swim lanes"
      contains: "EffectChainDiagram"
    - path: "frontend/src/components/escalation/index.ts"
      provides: "Barrel exports"
      contains: "export"
  key_links:
    - from: "frontend/src/components/escalation/EscalationLadder.tsx"
      to: "backend/src/agents/escalation-modeler.ts"
      via: "Shared type definitions for EscalationLadder, EscalationRung"
      pattern: "EscalationRung"
    - from: "frontend/src/components/escalation/EffectChainDiagram.tsx"
      to: "backend/src/agents/effect-cascader.ts"
      via: "Shared type definitions for EffectChain, EffectNode, DIMEDomain"
      pattern: "DIMEDomain"
---

<objective>
Build the Escalation Ladder and Effect Chain Diagram interactive visualizations that render data from the Escalation Modeler and Effect Cascader agents.

Purpose: CONTEXT decision specifies a classic vertical ladder visualization for escalation (vertical axis, current position highlighted, thresholds marked) and a directed flow diagram with DIME domain swim lanes for effect chains. Both must be fully interactive (click rungs/nodes for details).
Output: Two React components with CSS: `EscalationLadder.tsx` and `EffectChainDiagram.tsx`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.2-escalation-competition-modeling/5.2-CONTEXT.md
@backend/src/agents/escalation-modeler.ts
@backend/src/agents/effect-cascader.ts
@frontend/src/components/planning/COACard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Escalation Ladder vertical visualization component</name>
  <files>
    frontend/src/components/escalation/EscalationLadder.tsx
    frontend/src/components/escalation/EscalationLadder.css
  </files>
  <action>
Create the EscalationLadder component in `frontend/src/components/escalation/`:

1. **Define frontend-side types** mirroring the backend escalation-modeler.ts types (EscalationRung, EscalationLadder, EscalationRiskAssessment). Use inline type definitions to avoid cross-project imports.

2. **EscalationLadder.tsx component**:
   - Props: `{ ladder: EscalationLadderData; riskAssessments?: EscalationRiskAssessmentData[]; onRungClick?: (rung: EscalationRungData) => void }`
   - Render a vertical ladder from bottom (peace/level 1) to top (highest escalation)
   - Each rung is a horizontal bar spanning the width, colored by severity:
     - Levels 1-3: green (peaceful/competitive)
     - Levels 4-6: amber (crisis/confrontation)
     - Levels 7+: red (conflict/war)
   - Current position highlighted with a pulsing border and "CURRENT" label
   - Threshold markers shown as dashed horizontal lines across the ladder
   - Kinetic rungs marked with a distinct icon/indicator
   - Nuclear-relevant rungs marked with a warning indicator
   - Click any rung to expand an inline detail panel showing:
     - Triggers (what would escalate TO this level)
     - Characteristic actions at this level
     - De-escalation conditions
     - Risk assessments that project to this rung (if provided)
   - If `riskAssessments` provided, show projected rung arrows for each COA

3. **EscalationLadder.css**:
   - Vertical layout with rungs stacked bottom-to-top
   - Color coding: green/amber/red gradient
   - Pulsing animation on current position
   - Smooth expand/collapse animation for detail panels
   - Responsive: stacks cleanly at narrow widths
   - Use existing project CSS patterns (no external CSS framework)
  </action>
  <verify>
Run `npx tsc --noEmit` in frontend/ (or from project root) to verify TypeScript compilation. Visually confirm component structure renders correct HTML by reviewing the JSX output.
  </verify>
  <done>
EscalationLadder component renders a vertical ladder with color-coded rungs, highlighted current position, threshold markers, and interactive rung detail expansion.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Effect Chain Diagram with DIME swim lanes</name>
  <files>
    frontend/src/components/escalation/EffectChainDiagram.tsx
    frontend/src/components/escalation/EffectChainDiagram.css
    frontend/src/components/escalation/index.ts
  </files>
  <action>
Create the EffectChainDiagram component and barrel export:

1. **Define frontend-side types** mirroring backend effect-cascader.ts types (DIMEDomain, EffectNode, EffectEdge, EffectChain). Use inline type definitions.

2. **EffectChainDiagram.tsx component**:
   - Props: `{ chains: EffectChainData[]; selectedChainId?: string; onNodeClick?: (node: EffectNodeData) => void }`
   - Render a directed flow diagram with 4 horizontal swim lanes (DIME: Diplomatic, Information, Military, Economic)
   - Swim lanes are horizontal bands, labeled on the left
   - Effect nodes placed in their domain's swim lane, positioned left-to-right by order (root action -> first-order -> second-order -> third-order)
   - Nodes are colored by sentiment: green (positive), red (negative), gray (neutral)
   - Unintended effects shown with dashed border
   - Directed arrows between nodes show causal flow, with labels showing the mechanism
   - Arrow thickness proportional to causal strength
   - Click any node to show detail popover:
     - Full description
     - Probability with confidence bounds
     - Duration (temporary/persistent/permanent)
     - Affected population
     - Whether reversible
   - Support multiple chains overlaid (toggle chains on/off)
   - Use SVG for arrows/lines, HTML divs for nodes (hybrid approach for interactive elements)

3. **EffectChainDiagram.css**:
   - Swim lane layout with alternating background colors (very subtle)
   - Domain labels styled as sidebar
   - Node styling: rounded rectangles with sentiment color fill
   - Arrow styling: SVG paths with arrowheads
   - Popover styling for node details
   - Scrollable if diagram exceeds viewport width

4. **index.ts barrel export**:
   ```typescript
   export { EscalationLadder } from './EscalationLadder';
   export { EffectChainDiagram } from './EffectChainDiagram';
   ```
  </action>
  <verify>
Run `npx tsc --noEmit` in frontend/ to verify TypeScript compilation. Confirm barrel exports resolve correctly.
  </verify>
  <done>
EffectChainDiagram component renders directed flow diagram with DIME swim lanes, color-coded effect nodes by sentiment, SVG arrows showing causal flow, and interactive node detail popovers. Barrel export created for both escalation components.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes in frontend/
2. EscalationLadder renders vertically with bottom=peace, top=conflict
3. Current position is visually highlighted
4. Rung click expands inline detail panel
5. EffectChainDiagram has 4 swim lanes (Diplomatic, Information, Military, Economic)
6. Effect nodes positioned by order (root -> first -> second -> third)
7. Nodes colored by sentiment (green/red/gray)
8. Directed arrows between nodes with mechanism labels
9. Node click shows detail popover with probability and confidence bounds
10. Barrel exports from index.ts
</verification>

<success_criteria>
- Escalation ladder follows classic vertical ladder pattern per CONTEXT decision
- Current position highlighted, thresholds marked as per CONTEXT
- Effect chains render as directed flow diagrams with DIME domain swim lanes per CONTEXT
- Both visualizations are fully interactive (click for details) per CONTEXT
- Components follow existing project patterns (React + CSS, no external viz library required)
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-escalation-competition-modeling/5.2-05-SUMMARY.md`
</output>
