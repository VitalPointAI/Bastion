---
phase: 5.2-escalation-competition-modeling
plan: 04
type: execute
wave: 2
depends_on: [5.2-01]
files_modified:
  - backend/src/agents/deception-detector.ts
autonomous: true

must_haves:
  truths:
    - "Deception Detector agent identifies inconsistencies between adversary stated intent and observed behavior"
    - "Agent detects patterns of denial, deception, and misdirection in intelligence"
    - "Agent produces both inline deception flags and a dedicated deception analysis section"
    - "Agent outputs include confidence intervals per INVARIANT 5 (Uncertainty Transparency)"
    - "Agent manifest follows existing AgentManifest pattern with maxAutonomy SemiAutonomous"
  artifacts:
    - path: "backend/src/agents/deception-detector.ts"
      provides: "Deception Detector agent implementation"
      contains: "DeceptionDetectorAgent"
  key_links:
    - from: "backend/src/agents/deception-detector.ts"
      to: "backend/src/agents/types.ts"
      via: "AgentManifest and AgentCapability imports"
      pattern: "AgentCapability\\.DeceptionDetection"
    - from: "backend/src/agents/deception-detector.ts"
      to: "backend/src/mdmp/types.ts"
      via: "MDMPPhase, AgentRole imports"
      pattern: "AgentRole\\.DECEPTION_DETECTOR"
---

<objective>
Create the Deception Detector AI agent that identifies inconsistencies between adversary stated intent and observed behavior, detects denial and deception patterns, and produces deception indicators for the integrated brief.

Purpose: Red team challenge MDMP-0-08 requires adversary deception detection. Adversaries use denial and deception (D&D) to mask true intentions. The Deception Detector compares stated intent against behavioral signals, historical patterns, and doctrinal norms to flag potential deception, supporting both inline flags in adversary narrative and a dedicated deception analysis section.
Output: `backend/src/agents/deception-detector.ts` with agent implementation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.2-escalation-competition-modeling/5.2-CONTEXT.md
@backend/src/agents/types.ts
@backend/src/agents/assumption-auditor.ts
@backend/src/mdmp/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Deception Detector agent with deception indicator interfaces</name>
  <files>backend/src/agents/deception-detector.ts</files>
  <action>
Create `backend/src/agents/deception-detector.ts` following the pattern in `assumption-auditor.ts`:

1. **File header comment** describing the agent's purpose: detects inconsistencies between adversary stated intent and observed behavior, identifies denial and deception patterns, produces inline flags and dedicated analysis. CRITICAL: All outputs include confidence intervals per INVARIANT 5.

2. **Import types** from:
   - `'../mdmp/types.js'` (MDMPPhase, AgentRole, AgentOutputType)
   - `'../agents/types.js'` (AgentManifest, AgentCapability)
   - `'../dao/types.js'` (AutonomyLevel)

3. **Deception detection interfaces**:

   ```typescript
   /** Deception technique categories */
   export type DeceptionTechnique =
     | 'camouflage'        // Hide true position/capability
     | 'concealment'       // Prevent observation
     | 'ruse'              // Misleading actions
     | 'display'           // Show false capability/position
     | 'demonstration'     // Threat of action in wrong area
     | 'feint'             // Limited offensive to mislead
     | 'disinformation'    // False information spread
     | 'maskirovka';       // Comprehensive deception (Russian doctrine)

   /** A specific deception indicator */
   export interface DeceptionIndicator {
     /** Indicator identifier */
     id: string;
     /** What was observed that suggests deception */
     observation: string;
     /** What was expected based on stated intent */
     expectedBehavior: string;
     /** The actual observed behavior */
     actualBehavior: string;
     /** The inconsistency between expected and actual */
     inconsistency: string;
     /** Deception technique this most resembles */
     technique: DeceptionTechnique;
     /** Source of the observation */
     source: string;
     /** Severity: low, moderate, high, critical */
     severity: 'low' | 'moderate' | 'high' | 'critical';
     /** Confidence that this IS deception (0-1) per INVARIANT 5 */
     deceptionConfidence: number;
     /** Confidence bounds */
     confidenceBounds: { lower: number; upper: number };
     /** Alternative explanations (not every inconsistency is deception) */
     alternativeExplanations: string[];
     /** Recommended collection to confirm/deny */
     recommendedCollection: string[];
   }

   /** Inline deception flag for embedding in adversary narrative */
   export interface InlineDeceptionFlag {
     /** Reference to the narrative section */
     narrativeReference: string;
     /** Brief flag text (e.g., "DECEPTION POSSIBLE: stated withdrawal contradicts observed reinforcement") */
     flagText: string;
     /** Traffic light: green (low risk), amber (moderate), red (high/critical) */
     trafficLight: 'green' | 'amber' | 'red';
     /** Link to full indicator */
     indicatorId: string;
   }

   /** Dedicated deception analysis section */
   export interface DeceptionAnalysis {
     /** All identified deception indicators */
     indicators: DeceptionIndicator[];
     /** Pattern summary — what deception story is the adversary telling? */
     deceptionNarrative: string;
     /** Assessed adversary deception objective */
     assessedObjective: string;
     /** Indicators grouped by technique */
     techniqueBreakdown: Array<{
       technique: DeceptionTechnique;
       count: number;
       indicators: string[];
     }>;
     /** Historical deception patterns for this adversary */
     historicalPatterns: string[];
     /** How confident we are in the overall deception assessment (0-1) per INVARIANT 5 */
     overallConfidence: number;
     /** Confidence bounds */
     overallConfidenceBounds: { lower: number; upper: number };
     /** What collection would confirm or deny deception */
     confirmationPlan: string[];
   }

   /** Complete deception detection output */
   export interface DeceptionDetectionOutput {
     /** Inline flags for embedding in adversary narrative (quick reference per CONTEXT) */
     inlineFlags: InlineDeceptionFlag[];
     /** Full deception analysis (dedicated section per CONTEXT) */
     analysis: DeceptionAnalysis;
     /** Summary statement */
     summary: string;
     /** Whether adversary deception is assessed as active */
     deceptionAssessed: boolean;
     /** Overall analysis confidence (0-1) per INVARIANT 5 */
     analysisConfidence: number;
     /** Timestamp */
     analyzedAt: number;
   }
   ```

4. **Agent manifest constant** following the `ASSUMPTION_AUDITOR_MANIFEST` pattern. Use:
   - `agentId: 'deception-detector'`
   - `name: 'Deception Detector'`
   - `description: 'Identifies inconsistencies between adversary stated intent and observed behavior to detect denial and deception'`
   - `phase: 'Support' as any`
   - `capabilities: [AgentCapability.DeceptionDetection]`
   - `maxAutonomy: AutonomyLevel.SemiAutonomous`
   - `modelConfig: { provider: 'anthropic', model: 'claude-sonnet', temperature: 0.3, maxTokens: 12288 }`
   - Character with bio, lore, knowledge covering denial and deception methodology, maskirovka doctrine, deception indicators and warnings, confirmation bias awareness
   - System prompt covering: intent-behavior comparison, deception technique classification, alternative explanation generation (avoid confirmation bias), collection recommendations for confirmation

5. **Core function stubs** with TODO comments:

   ```typescript
   export async function detectDeception(
     statedIntent: string,
     observedBehaviors: Array<{ source: string; observation: string; timestamp: number }>,
     adversaryProfile: string,
     historicalContext: string,
   ): Promise<DeceptionDetectionOutput>
   ```

   ```typescript
   export async function generateInlineFlags(
     adversaryNarrative: string,
     indicators: DeceptionIndicator[],
   ): Promise<InlineDeceptionFlag[]>
   ```

   Both return stub/default values with TODO comments for agent executor integration.
  </action>
  <verify>
Run `npx tsc --noEmit` in backend/ to verify TypeScript compilation.
  </verify>
  <done>
Deception Detector agent created with DeceptionIndicator, InlineDeceptionFlag, DeceptionAnalysis, and DeceptionDetectionOutput interfaces. Both inline flags (quick reference) and dedicated analysis section (detailed breakdown) supported per CONTEXT decision. INVARIANT 5 enforced via confidence fields. Alternative explanations tracked to avoid confirmation bias.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes in backend/
2. DeceptionDetectionOutput includes analysisConfidence field (INVARIANT 5)
3. DeceptionIndicator includes deceptionConfidence and confidenceBounds (INVARIANT 5)
4. InlineDeceptionFlag has trafficLight for quick visual reference
5. DeceptionAnalysis has deceptionNarrative for pattern summary
6. alternativeExplanations field prevents confirmation bias
7. Agent manifest has maxAutonomy: SemiAutonomous
8. Both inline flags and dedicated analysis section exist (dual display per CONTEXT)
</verification>

<success_criteria>
- Agent implementation file follows assumption-auditor.ts pattern
- All output interfaces include confidence intervals (INVARIANT 5)
- Dual presentation: inline flags + dedicated analysis section
- 8 deception techniques covered (camouflage, concealment, ruse, display, demonstration, feint, disinformation, maskirovka)
- Alternative explanations tracked to prevent false positives
- Traffic light indicators on inline flags (green/amber/red) per CONTEXT
- detectDeception and generateInlineFlags function stubs defined
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-escalation-competition-modeling/5.2-04-SUMMARY.md`
</output>
