---
phase: 37-training-assessment-loop
plan: 04
type: execute
wave: 3
depends_on: [37-02, 37-03]
files_modified:
  - frontend/src/components/assess/TrainingTacticalAssess.tsx
  - frontend/src/components/assess/AARForm.tsx
  - frontend/src/components/assess/AARObservationCard.tsx
  - frontend/src/components/assess/METLTaskAssessment.tsx
  - frontend/src/lib/assessment-service.ts
autonomous: true
requirements: [TAL-01, TAL-03, TAL-09]

must_haves:
  truths:
    - "O/C or Commander can create and edit a structured AAR with 4 doctrinal sections"
    - "AAR observations can be added as sustain or improve, each linked to a METL task"
    - "AAR can be finalized (locked, read-only) by the initiator"
    - "O/C can assign T/P/U ratings per METL task from the Task Assessment sidebar"
    - "Commander can override O/C ratings"
  artifacts:
    - path: "frontend/src/components/assess/TrainingTacticalAssess.tsx"
      provides: "Tactical-level training assessment with AAR and Task Assessment sidebar"
      exports: ["TrainingTacticalAssess"]
    - path: "frontend/src/components/assess/AARForm.tsx"
      provides: "Structured 4-section AAR form with lifecycle management"
      exports: ["AARForm"]
    - path: "frontend/src/components/assess/METLTaskAssessment.tsx"
      provides: "T/P/U rating assignment per METL task"
      exports: ["METLTaskAssessment"]
  key_links:
    - from: "frontend/src/components/assess/TrainingTacticalAssess.tsx"
      to: "frontend/src/lib/assessment-service.ts"
      via: "AAR and METL API calls"
      pattern: "assessmentService\\."
    - from: "frontend/src/components/assess/AARForm.tsx"
      to: "backend/src/api/assessment-routes.ts"
      via: "POST/PATCH /api/assessment/aars"
      pattern: "fetch.*api/assessment/aars"
    - from: "frontend/src/components/assess/AssessEchelonRouter.tsx"
      to: "frontend/src/components/assess/TrainingTacticalAssess.tsx"
      via: "echelon=tactical in training mode"
      pattern: "TrainingTacticalAssess"
---

<objective>
Build the training tactical assessment view: structured AAR form with 4 doctrinal sections, observation management, and METL task assessment (T/P/U rating assignment).

Purpose: This is the core training assessment workflow -- where O/Cs and Commanders capture AARs at tactical training events and assess METL proficiency.
Output: Tactical assess view with AAR form, observation cards, and task assessment panel.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-training-assessment-loop/37-CONTEXT.md
@.planning/phases/37-training-assessment-loop/37-RESEARCH.md
@.planning/phases/37-training-assessment-loop/37-01-SUMMARY.md
@.planning/phases/37-training-assessment-loop/37-03-SUMMARY.md

<interfaces>
From frontend/src/lib/assessment-service.ts (Plan 03 -- will be extended):
- assessmentService singleton with MOE/MOP methods

From frontend/src/components/assess/AssessEchelonRouter.tsx (Plan 03):
- TrainingTacticalAssess placeholder -- REPLACE with actual component import

From frontend/src/components/tabs/TabLayout.tsx:
```typescript
export interface SidebarItem { id: string; label: string; tooltip?: string; status?: 'not-started' | 'in-progress' | 'complete'; }
export function TabLayout(props: TabLayoutProps): JSX.Element;
```

From backend assessment types (Plan 01):
- StructuredAAR: { id, problemSetId, trainingEventName, initiatedBy, status, whatWasPlanned, whatHappened, why, finalizedAt?, ... }
- AARObservation: { id, aarId, observationType, content, metlTaskId?, suggestedByAi, aiAccepted?, createdBy, ... }
- METLTask: { id, taskName, competencyArea?, isSupplemental, ... }
- METLAssessment: { id, metlTaskId, rating, assessedBy, commanderOverride, ... }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AAR form and observation card components</name>
  <files>frontend/src/components/assess/AARForm.tsx, frontend/src/components/assess/AARObservationCard.tsx, frontend/src/lib/assessment-service.ts</files>
  <action>
**First, extend assessment-service.ts** with AAR methods:
- listAARs(problemSetId: string): Promise<StructuredAAR[]>
- createAAR(input: { problemSetId, trainingEventName, initiatedBy }): Promise<StructuredAAR>
- getAAR(id: string): Promise<StructuredAAR>
- updateAAR(id: string, input: { whatWasPlanned?, whatHappened?, why?, status? }): Promise<StructuredAAR>
- finalizeAAR(id: string, finalizedBy: string): Promise<StructuredAAR>
- listAARObservations(aarId: string): Promise<AARObservation[]>
- addAARObservation(aarId: string, input: { observationType, content, metlTaskId?, suggestedByAi?, createdBy }): Promise<AARObservation>
- updateAARObservation(id: string, input: { aiAccepted?, content? }): Promise<AARObservation>

Also add METL methods:
- listMETLTasks(problemSetId: string): Promise<METLTask[]>
- getInheritedMETLTasks(problemSetId: string, sourceProblemSetId: string): Promise<METLTask[]>
- createMETLAssessment(input: CreateMETLAssessmentInput): Promise<METLAssessment>
- getAssessmentsByAAR(aarId: string): Promise<METLAssessment[]>

**AARForm.tsx:**
Props: { aar: StructuredAAR; observations: AARObservation[]; metlTasks: METLTask[]; onUpdate: (updated) => void; onFinalize: () => void; onAddObservation: (obs) => void; onUpdateObservation: (id, updates) => void }

Layout (4 doctrinal sections per FM 7-0):
1. Section header: AAR title with training event name + status badge (draft/in_review/finalized)
2. "What Was Planned?" -- textarea, editable if not finalized
3. "What Happened?" -- textarea, editable if not finalized
4. "Why?" (analysis) -- textarea, editable if not finalized
5. "Observations" section:
   - Two columns or tabs: "Sustain" and "Improve"
   - Each observation rendered as AARObservationCard
   - "Add Observation" button at bottom of each column (opens inline form with: type select, content textarea, METL task dropdown)
6. Action bar: "Save Draft" button, "Submit for Review" button (sets status to in_review), "Finalize" button (only if status=in_review, confirms with dialog)

When finalized: all textareas become read-only, "Finalize" button hidden, status shows "FINALIZED" with timestamp.

**AARObservationCard.tsx:**
Props: { observation: AARObservation; metlTasks: METLTask[]; readOnly: boolean; onUpdate?: (id, updates) => void }

Layout:
- Badge: "SUSTAIN" (green) or "IMPROVE" (amber)
- If suggestedByAi: small "AI Suggested" tag
- If suggestedByAi and aiAccepted is null: Accept/Reject buttons
- If suggestedByAi and aiAccepted === true: "Accepted" chip
- If suggestedByAi and aiAccepted === false: "Rejected" chip (dimmed card)
- Content text
- Linked METL task name (if metlTaskId) -- resolve from metlTasks prop
- Created by and timestamp
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit frontend/src/components/assess/AARForm.tsx frontend/src/components/assess/AARObservationCard.tsx 2>&1 | head -20</automated>
  </verify>
  <done>AARForm renders 4 doctrinal sections with lifecycle controls, AARObservationCard shows sustain/improve with AI suggestion handling</done>
</task>

<task type="auto">
  <name>Task 2: METL Task Assessment and TrainingTacticalAssess view</name>
  <files>frontend/src/components/assess/METLTaskAssessment.tsx, frontend/src/components/assess/TrainingTacticalAssess.tsx, frontend/src/components/assess/AssessEchelonRouter.tsx</files>
  <action>
**METLTaskAssessment.tsx:**
Props: { problemSetId: string; aarId?: string; metlTasks: METLTask[]; existingAssessments: METLAssessment[]; onAssess: (assessment) => void }

Layout:
- Header: "Task Assessment" with AAR reference if linked
- Table/list of METL tasks with columns: Task Name, Competency Area, Current Rating, New Rating, Notes
- Each row shows:
  - Task name and competency area
  - Current rating from existingAssessments (or "Not assessed" if none)
  - Rating selector: 3 radio buttons or button group for T / P / U, color-coded (green/yellow/red)
  - Notes text input (optional)
  - "Override" checkbox (for commander -- only shown if role allows, derive from useProblemSet().userRoleInActive being 'commander' or 'xo')
- "Submit Assessments" button that creates assessments for all tasks with selected ratings
- Supplemental tasks shown in a separate section with a lighter background and "(Supplemental)" label

**TrainingTacticalAssess.tsx:**
Full tactical-level training assessment view using TabLayout with sidebar:

Sidebar items:
```typescript
const TACTICAL_TRAINING_ITEMS: SidebarItem[] = [
  { id: 'aar', label: 'After-Action Review' },
  { id: 'task-assessment', label: 'Task Assessment' },
];
```

State management:
- selectedView (default 'aar')
- aars: list of AARs for this problem set (fetched on mount)
- selectedAAR: currently viewed/editing AAR
- observations: observations for selected AAR
- metlTasks: METL tasks (inherited from parent + local supplemental)
- assessments: assessments linked to selected AAR

AAR sidebar view:
- If no AARs exist: empty state with "Create AAR" button (form: training event name input)
- If AARs exist: list of AAR cards (event name, status badge, date) -- click to select
- When AAR selected: render AARForm with full edit capability
- Create new AAR: inline form at top with event name input

Task Assessment sidebar view:
- If no AAR selected: prompt to select or create an AAR first
- If AAR selected: render METLTaskAssessment with tasks and existing assessments for that AAR
- After submission, assessments are linked to the AAR and the AAR's status is shown

METL task loading:
- Fetch tasks for this problem set AND inherited tasks from parent (use the problem set's parentProblemSetId as sourceProblemSetId for getInheritedMETLTasks)
- Combine: inherited tasks first, then local supplemental tasks

**AssessEchelonRouter.tsx:**
Update the file to import TrainingTacticalAssess and replace the inline placeholder:
- Remove the inline TrainingTacticalAssess placeholder function
- Add: import { TrainingTacticalAssess } from './TrainingTacticalAssess.tsx';
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit frontend/src/components/assess/TrainingTacticalAssess.tsx frontend/src/components/assess/METLTaskAssessment.tsx frontend/src/components/assess/AssessEchelonRouter.tsx 2>&1 | head -20</automated>
  </verify>
  <done>TrainingTacticalAssess renders AAR + Task Assessment sidebar views, METLTaskAssessment allows T/P/U rating per task with commander override, AssessEchelonRouter imports real TrainingTacticalAssess</done>
</task>

</tasks>

<verification>
- Tactical training view has AAR and Task Assessment sidebar items
- AAR form shows 4 doctrinal sections and lifecycle controls
- Observations display sustain/improve with METL task linking
- T/P/U rating selector works with commander override option
- Finalized AARs are read-only
</verification>

<success_criteria>
- User can create a new AAR with training event name
- User can fill in all 4 doctrinal sections
- User can add sustain/improve observations linked to METL tasks
- User can finalize an AAR (becomes read-only)
- O/C can assign T/P/U ratings per METL task
- Commander can override ratings
</success_criteria>

<output>
After completion, create `.planning/phases/37-training-assessment-loop/37-04-SUMMARY.md`
</output>
