---
phase: 28-embedded-dao-governance-at-decision-gates-inserted
plan: 09
type: execute
wave: 4
depends_on: ["28-03", "28-04"]
files_modified:
  - frontend/src/components/governance/MDMPGovernancePanel.tsx
  - frontend/src/lib/mdmp-service.ts
  - backend/src/gates/gate-service.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "MDMP governance gates and new decision gates use the same registry system"
    - "Training mode gates are tagged as TRAINING in the governance timeline"
    - "Gate service handles training mode configuration per exercise"
    - "MDMPGovernancePanel renders both old MDMP and new gate data in unified view"
  artifacts:
    - path: "frontend/src/components/governance/MDMPGovernancePanel.tsx"
      provides: "MDMP as one gate workflow type within the system"
      contains: "DecisionGate"
    - path: "frontend/src/lib/mdmp-service.ts"
      provides: "Dual-read from old MDMP and new gate sources"
      contains: "gateService"
    - path: "backend/src/gates/gate-service.ts"
      provides: "Training mode auto-approval and instructor-approval logic"
      contains: "training"
  key_links:
    - from: "frontend/src/lib/mdmp-service.ts"
      to: "frontend/src/lib/gate-service.ts"
      via: "dual-read from both old and new gate sources"
      pattern: "gateService"
    - from: "frontend/src/components/governance/MDMPGovernancePanel.tsx"
      to: "frontend/src/context/DecisionGateContext.tsx"
      via: "reads gates from context for unified rendering"
      pattern: "useDecisionGates"
---

<objective>
Unify MDMP gates into the decision gate registry via dual-read integration, implement training mode gate tagging on the backend, and update MDMPGovernancePanel for unified rendering.

Purpose: Makes MDMP one gate workflow type within the unified system, not a separate system. Implements training mode auto-approval and tagging.
Output: MDMP dual-read integration, training mode backend logic, unified MDMPGovernancePanel.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-embedded-dao-governance-at-decision-gates-inserted/28-CONTEXT.md
@.planning/phases/28-embedded-dao-governance-at-decision-gates-inserted/28-RESEARCH.md
@.planning/phases/28-embedded-dao-governance-at-decision-gates-inserted/28-01-SUMMARY.md
@.planning/phases/28-embedded-dao-governance-at-decision-gates-inserted/28-02-SUMMARY.md

<interfaces>
From frontend/src/components/governance/MDMPGovernancePanel.tsx:
```typescript
// Container with tab-based governance UI — refactor to be one workflow type
```

From frontend/src/lib/mdmp-service.ts:
```typescript
export async function createWorkflow(params: CreateWorkflowParams): Promise<WorkflowState>;
export async function getWorkflow(missionId: string): Promise<{...}>;
export async function registerPhaseGates(missionId: string, phase: string): Promise<void>;
export async function satisfyGate(missionId: string, gateId: string, ...): Promise<void>;
// Uses separate data model from decision_gates table
```

From frontend/src/context/ModeContext.tsx:
```typescript
const { mode } = useMode();  // 'training' | 'operational'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MDMP dual-read integration and MDMPGovernancePanel update</name>
  <files>frontend/src/lib/mdmp-service.ts, frontend/src/components/governance/MDMPGovernancePanel.tsx</files>
  <action>
Read mdmp-service.ts and MDMPGovernancePanel.tsx fully before modifying.

**mdmp-service.ts (frontend):**
- Add import for gateService from gate-service.ts
- In functions that read MDMP gates (getWorkflow, or wherever phaseGates are returned):
  - After fetching MDMP data, also fetch gates from gateService for the same problem set
  - Merge: return MDMP gates as-is (for backward compatibility) AND append decision gates that have gate_type matching MDMP gate patterns
  - This is the "dual-read" approach from the research: new system is authoritative for new gates, old MDMP data still visible
- In registerPhaseGates: keep existing behavior (registers MDMP gates in old system). New gates are created via gate-service directly.
- Add a helper function `convertMdmpGateToDecisionGate(mdmpGate)` that maps old MDMP gate format to DecisionGate interface for unified rendering

**MDMPGovernancePanel.tsx:**
- Read the component to understand its current structure
- Import useDecisionGates hook
- In addition to existing MDMP data, also render DecisionGate data from context
- Show both old MDMP gates (from mdmp-service) and new gates (from gate context) in a unified view
- Use the generalized GovernanceGateDashboard for rendering when gate context data is available
- This makes MDMPGovernancePanel one view within the unified system rather than a standalone
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>mdmp-service reads from both old MDMP and new gate sources; MDMPGovernancePanel renders unified gate view; dual-read ensures no data loss from MDMP migration</done>
</task>

<task type="auto">
  <name>Task 2: Backend training mode gate logic</name>
  <files>backend/src/gates/gate-service.ts</files>
  <action>
Read backend/src/gates/gate-service.ts fully before modifying.

Add training mode awareness to gate-service.ts:
- In createGate: if mode is 'training', set training_config with the exercise's training gate behavior (instructor-approved, auto-approved, full-governance)
- In submitForApproval: if gate.mode === 'training' and training_config.behavior === 'auto-approved', automatically approve the gate (transition to 'approved' with decision_context noting "Auto-approved in training mode")
- In submitForApproval: if gate.mode === 'training' and training_config.behavior === 'instructor-approved', proceed normally (instructor has commander-level authority via DAO role mapping)
- In processTimeouts: training mode gates follow the same timeout logic
- Tag all training mode gate status transitions with mode: 'training' metadata in decision_context
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit --project backend/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>Backend gate-service handles training mode auto-approval and instructor-approval; training gates tagged as TRAINING in decision_context</done>
</task>

</tasks>

<verification>
- MDMP panel shows both old and new gates in unified view
- Training mode auto-approval works on backend
- Training gates display TRAINING tag in decision_context
- TypeScript compiles cleanly for both frontend and backend
- Existing MDMP workflows still function (backward compatible)
</verification>

<success_criteria>
- One gate system, not two (locked decision: unify MDMP gates into same registry)
- MDMP becomes one gate workflow type (locked decision)
- Training mode configurable: instructor-approved, auto-approved, full-governance (locked decision)
- Training decisions tagged as TRAINING (locked decision)
- Dual-read approach: no data loss from MDMP migration
</success_criteria>

<output>
After completion, create `.planning/phases/28-embedded-dao-governance-at-decision-gates-inserted/28-09-SUMMARY.md`
</output>
