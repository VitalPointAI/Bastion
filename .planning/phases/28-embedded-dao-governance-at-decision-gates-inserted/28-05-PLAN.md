---
phase: 28-embedded-dao-governance-at-decision-gates-inserted
plan: 05
type: execute
wave: 3
depends_on: ["28-03", "28-04"]
files_modified:
  - frontend/src/components/tabs/DirectTab.tsx
  - frontend/src/components/tabs/AssessTab.tsx
  - frontend/src/components/tabs/AssessTab.css
  - frontend/src/components/dao/DAODashboard.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Direct tab retains all existing governance views and adds cross-tab gate overview"
    - "Direct tab shows order release gate with submit button"
    - "AssessTab replaces placeholder with minimal assessment structure and reframing gate"
    - "DAODashboard shows all gates across all tabs as governance hub"
  artifacts:
    - path: "frontend/src/components/tabs/DirectTab.tsx"
      provides: "Order release gate + gate overview in governance hub"
      contains: "DecisionGateBanner"
    - path: "frontend/src/components/tabs/AssessTab.tsx"
      provides: "Minimal assessment tab with reframing decision gate"
      min_lines: 40
    - path: "frontend/src/components/dao/DAODashboard.tsx"
      provides: "Cross-tab gate overview section"
      contains: "useDecisionGates"
  key_links:
    - from: "frontend/src/components/dao/DAODashboard.tsx"
      to: "frontend/src/context/DecisionGateContext.tsx"
      via: "reads all gates for cross-tab overview"
      pattern: "useDecisionGates"
    - from: "frontend/src/components/tabs/AssessTab.tsx"
      to: "frontend/src/components/governance/GateSubmitButton.tsx"
      via: "reframing gate submit"
      pattern: "GateSubmitButton.*reframing"
---

<objective>
Upgrade DirectTab with gate overview and order release gate, implement minimal AssessTab replacing placeholder with reframing gate, and add cross-tab gate summary to DAODashboard.

Purpose: Completes the 5 doctrinal gates across all tabs. Direct tab becomes the governance hub. AssessTab goes from placeholder to functional.
Output: All 5 tabs have their designated gates; DAODashboard shows cross-tab governance status.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-embedded-dao-governance-at-decision-gates-inserted/28-CONTEXT.md
@.planning/phases/28-embedded-dao-governance-at-decision-gates-inserted/28-RESEARCH.md
@.planning/phases/28-embedded-dao-governance-at-decision-gates-inserted/28-03-SUMMARY.md
@.planning/phases/28-embedded-dao-governance-at-decision-gates-inserted/28-04-SUMMARY.md

<interfaces>
From frontend/src/components/tabs/DirectTab.tsx:
```typescript
const DIRECT_ITEMS: SidebarItem[] = [
  { id: 'governance', label: 'Governance Overview' },
  { id: 'proposals', label: 'Proposals & Voting' },
  { id: 'escalation', label: 'Escalation' },
];
interface DirectTabProps { problemSetId: string; daoId?: string; }
export function DirectTab({ problemSetId, daoId }: DirectTabProps) { ... }
```

From frontend/src/components/tabs/AssessTab.tsx:
```typescript
// Currently just DoctrinalPlaceholder — will be fully replaced
export function AssessTab({ problemSetId }: { problemSetId: string }) {
  return <DoctrinalPlaceholder tabId="assess" ... />;
}
```

From frontend/src/components/dao/DAODashboard.tsx:
```typescript
// Existing governance dashboard in Direct tab
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DirectTab order release gate and DAODashboard cross-tab overview</name>
  <files>frontend/src/components/tabs/DirectTab.tsx, frontend/src/components/dao/DAODashboard.tsx</files>
  <action>
Read DirectTab.tsx and DAODashboard.tsx fully.

**DirectTab updates:**
- Add DecisionGateBanner at top: `<DecisionGateBanner tabId="direct" />`
- Add DecisionGateTimeline in sidebar via TabLayout decisionHistory prop
- Add a new sidebar item: `{ id: 'all-gates', label: 'All Decision Gates' }` — this is the cross-tab gate overview
- When 'all-gates' is selected, render a summary view of all gates across all tabs (use useDecisionGates() with no tabId to get all gates)
- For the order release gate: in the relevant content section (likely the 'governance' or a new 'orders' view), add GateSubmitButton with gateType="order_release" — since orders may not be fully itemized, provide a section-level submit button with a descriptive label
- Keep ALL existing Direct tab content intact — governance dashboard, proposals, escalation views. Only ADD gate components.

**DAODashboard updates:**
- Read the component fully
- Add a new section (or card) titled "Decision Gates" showing a summary table/list of all gates across tabs
- Use useDecisionGates() with no tabId to get all gates
- Display: gate_type label, tab, target_item_title, status badge (use GateStatusBadge), submitted_at, decided_at
- Group by tab or sort by status (pending/submitted first)
- This gives commanders the "big picture" view per locked decision
- Keep ALL existing DAODashboard content — this is additive
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>DirectTab has order release gate banner and timeline; DAODashboard shows cross-tab gate overview with all gates across all tabs; all existing Direct tab functionality preserved</done>
</task>

<task type="auto">
  <name>Task 2: AssessTab minimal implementation with reframing gate</name>
  <files>frontend/src/components/tabs/AssessTab.tsx, frontend/src/components/tabs/AssessTab.css</files>
  <action>
Replace the DoctrinalPlaceholder in AssessTab with a minimal but functional tab structure.

Create `AssessTab.tsx` (full rewrite):
- Props: `{ problemSetId: string; daoId?: string }`
- Uses TabLayout with sidebar items:
  - `{ id: 'overview', label: 'Assessment Overview' }` — default selected
  - `{ id: 'reframing', label: 'Reframing' }`
- DecisionGateBanner at top: `<DecisionGateBanner tabId="assess" />`
- DecisionGateTimeline passed as decisionHistory prop to TabLayout

**Overview section:**
- Heading: "Assessment"
- Brief description of assessment purpose (from JP 5-0: "Assessment measures progress toward accomplishing objectives and determines the effectiveness of ongoing operations")
- Placeholder sections for MOEs and MOPs (just section headers with "Coming soon" notes — full implementation deferred)
- List of any approved gates from this problem set as a "Decision Record" (shows that governance has been active)

**Reframing section:**
- Heading: "Reframing Decision"
- Description: "Reframing occurs when the current operational approach is assessed as inadequate to achieve objectives. Submit a reframing recommendation for commander approval."
- GateSubmitButton with gateType="reframing", itemId can be the problemSetId itself, itemTitle="Reframing Recommendation", itemDescription="Assessment-driven recommendation to reframe the operational approach"
- GateBlockOverlay wrapping a placeholder for "post-reframing actions" (when reframing is approved, what happens next — future implementation)

Create `AssessTab.css` with basic styling consistent with other tabs.

Import: TabLayout, SidebarItem, DecisionGateBanner, DecisionGateTimeline, GateSubmitButton, GateBlockOverlay, GateStatusBadge.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>AssessTab replaces placeholder with functional tab having overview + reframing sections; reframing gate has submit button and block overlay; tab has banner and timeline; MOE/MOP sections are marked for future implementation</done>
</task>

</tasks>

<verification>
- All 5 doctrinal gates now have a home: Understand=objective, Design=operational_approach, Plan=COA, Direct=order_release, Assess=reframing
- DirectTab keeps all existing views, adds gate overview
- DAODashboard shows cross-tab gate summary
- AssessTab is no longer a placeholder
- TypeScript compiles cleanly
</verification>

<success_criteria>
- Direct tab governance hub shows all gates across all tabs (locked decision)
- Direct tab has order release gate with banner and timeline
- AssessTab functional with TabLayout, overview, and reframing sections
- Reframing gate with submit button in Assess tab
- DAODashboard augmented with decision gates summary section
- No existing functionality broken in DirectTab or DAODashboard
</success_criteria>

<output>
After completion, create `.planning/phases/28-embedded-dao-governance-at-decision-gates-inserted/28-05-SUMMARY.md`
</output>
