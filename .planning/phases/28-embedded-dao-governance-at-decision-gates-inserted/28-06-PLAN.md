---
phase: 28-embedded-dao-governance-at-decision-gates-inserted
plan: 06
type: execute
wave: 4
depends_on: ["28-03", "28-04"]
files_modified:
  - frontend/src/components/governance/GovernanceGateDashboard.tsx
  - frontend/src/components/governance/GovernanceGateDashboard.css
  - frontend/src/components/governance/DecisionBriefView.tsx
  - frontend/src/components/governance/DecisionBriefView.css
  - frontend/src/components/governance/PhaseProgressionBar.tsx
  - frontend/src/components/governance/PhaseProgressionBar.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "GovernanceGateDashboard works with any gate type, not just MDMP"
    - "DecisionBriefView works for all gate types with context, recommendation, and outcome"
    - "PhaseProgressionBar shows gate workflow progression for any gate type"
    - "Training mode gates display TRAINING badge in DecisionBriefView"
    - "All components maintain backward compatibility with existing MDMP usage"
  artifacts:
    - path: "frontend/src/components/governance/GovernanceGateDashboard.tsx"
      provides: "Generalized gate dashboard for any gate type"
      contains: "gate_type"
    - path: "frontend/src/components/governance/DecisionBriefView.tsx"
      provides: "Generalized decision brief for all gate types"
      contains: "gate_type"
    - path: "frontend/src/components/governance/PhaseProgressionBar.tsx"
      provides: "Generalized gate workflow progression bar"
      contains: "DecisionGate"
  key_links:
    - from: "frontend/src/components/governance/GovernanceGateDashboard.tsx"
      to: "frontend/src/context/DecisionGateContext.tsx"
      via: "reads gates from context"
      pattern: "useDecisionGates"
    - from: "frontend/src/components/governance/DecisionBriefView.tsx"
      to: "frontend/src/lib/gate-service.ts"
      via: "uses DecisionGate type for rendering"
      pattern: "DecisionGate"
---

<objective>
Generalize MDMP governance frontend components (GovernanceGateDashboard, DecisionBriefView, PhaseProgressionBar) to work with any gate type.

Purpose: These components currently only render MDMP-specific gates. Generalizing them means the unified gate registry can use them for all 5 doctrinal gate types.
Output: Three generalized governance components that work for any DecisionGate, backward compatible with existing MDMP data.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-embedded-dao-governance-at-decision-gates-inserted/28-CONTEXT.md
@.planning/phases/28-embedded-dao-governance-at-decision-gates-inserted/28-RESEARCH.md
@.planning/phases/28-embedded-dao-governance-at-decision-gates-inserted/28-01-SUMMARY.md
@.planning/phases/28-embedded-dao-governance-at-decision-gates-inserted/28-02-SUMMARY.md

<interfaces>
From frontend/src/components/governance/GovernanceGateDashboard.tsx:
```typescript
// Currently MDMP-specific — needs generalization to work with any gate_type
```

From frontend/src/components/governance/DecisionBriefView.tsx:
```typescript
// Currently renders MDMP-specific brief — needs to work for all gate types
```

From frontend/src/components/governance/PhaseProgressionBar.tsx:
```typescript
// Currently shows MDMP phase progression — needs generalization
```

From frontend/src/context/ModeContext.tsx:
```typescript
const { mode } = useMode();  // 'training' | 'operational'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize GovernanceGateDashboard and DecisionBriefView</name>
  <files>frontend/src/components/governance/GovernanceGateDashboard.tsx, frontend/src/components/governance/GovernanceGateDashboard.css, frontend/src/components/governance/DecisionBriefView.tsx, frontend/src/components/governance/DecisionBriefView.css</files>
  <action>
Read each file fully before modifying.

**GovernanceGateDashboard.tsx:**
- Currently hardcoded to MDMP gate types and phases
- Generalize: accept gates from useDecisionGates() context instead of MDMP-specific data
- Props should accept an optional `gateFilter?: string` (tab or gate_type to filter by), defaulting to showing all gates
- Render gates as a dashboard grid/table with: gate type label, tab, target item, status, enforcement mode, deadline
- Use GateStatusBadge for status display
- Remove MDMP-specific hardcoded gate names/phases
- If the component currently takes MDMP-specific props, make them optional and add new DecisionGate-based rendering path
- Keep backward compatibility: if old MDMP data is passed, render it; if DecisionGate data is available from context, render that too (dual-read)

**DecisionBriefView.tsx:**
- Currently renders MDMP-specific decision brief (context, recommendation, outcome)
- Generalize: accept a `gate: DecisionGate` prop instead of (or in addition to) MDMP-specific data
- Render for any gate type:
  - Context section: gate.decision_context (metadata, description from proposal)
  - Recommendation section: extracted from decision_context or generated label
  - Outcome section: gate.status, decided_by, decided_at
  - Gate type label, enforcement mode, tab
- Training mode: if gate.mode === 'training', show a "TRAINING" badge/tag prominently
- Keep backward compatibility with existing MDMP brief props
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>GovernanceGateDashboard renders any gate type from context; DecisionBriefView works for all gate types with training mode tag; both backward compatible with existing MDMP usage</done>
</task>

<task type="auto">
  <name>Task 2: Generalize PhaseProgressionBar</name>
  <files>frontend/src/components/governance/PhaseProgressionBar.tsx, frontend/src/components/governance/PhaseProgressionBar.css</files>
  <action>
Read PhaseProgressionBar.tsx fully before modifying.

- Currently shows MDMP phase progression
- Generalize: accept an array of gates and show progression through gate workflow
- Each gate as a step in the progression bar (pending -> submitted -> approved/rejected)
- Color-code by status: green=approved, amber=submitted/pending, red=rejected, purple=escalated, gray=overridden
- Keep backward compatibility with MDMP phase data (if old props are passed, render old way)
- Add new rendering path for DecisionGate[] data
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>PhaseProgressionBar shows gate workflow progression for any gate type; color-coded by status; backward compatible with MDMP phase data</done>
</task>

</tasks>

<verification>
- Generalized components render any gate type (not just MDMP)
- Training mode gates display TRAINING badge in DecisionBriefView
- TypeScript compiles cleanly for frontend
- Existing MDMP usage patterns still function (backward compatible)
</verification>

<success_criteria>
- GovernanceGateDashboard works with any gate type (locked decision: generalize MDMP components)
- DecisionBriefView works for all gate types (locked decision)
- PhaseProgressionBar generalized for any gate workflow
- Training badge visible for training mode gates
- No existing MDMP functionality broken
</success_criteria>

<output>
After completion, create `.planning/phases/28-embedded-dao-governance-at-decision-gates-inserted/28-06-SUMMARY.md`
</output>
