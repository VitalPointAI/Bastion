---
phase: 28-embedded-dao-governance-at-decision-gates-inserted
plan: 06
type: execute
wave: 3
depends_on: ["28-03", "28-04"]
files_modified:
  - frontend/src/components/governance/GovernanceGateDashboard.tsx
  - frontend/src/components/governance/DecisionBriefView.tsx
  - frontend/src/components/governance/PhaseProgressionBar.tsx
  - frontend/src/components/governance/MDMPGovernancePanel.tsx
  - frontend/src/lib/mdmp-service.ts
  - backend/src/gates/gate-service.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "MDMP governance gates and new decision gates use the same registry system"
    - "GovernanceGateDashboard works with any gate type, not just MDMP"
    - "DecisionBriefView works for all gate types with context, recommendation, and outcome"
    - "Training mode gates are tagged as TRAINING in the governance timeline"
    - "Gate service handles training mode configuration per exercise"
  artifacts:
    - path: "frontend/src/components/governance/GovernanceGateDashboard.tsx"
      provides: "Generalized gate dashboard for any gate type"
      contains: "gate_type"
    - path: "frontend/src/components/governance/DecisionBriefView.tsx"
      provides: "Generalized decision brief for all gate types"
      contains: "gate_type"
    - path: "frontend/src/components/governance/MDMPGovernancePanel.tsx"
      provides: "MDMP as one gate workflow type within the system"
      contains: "DecisionGate"
  key_links:
    - from: "frontend/src/components/governance/GovernanceGateDashboard.tsx"
      to: "frontend/src/context/DecisionGateContext.tsx"
      via: "reads gates from context"
      pattern: "useDecisionGates"
    - from: "frontend/src/lib/mdmp-service.ts"
      to: "frontend/src/lib/gate-service.ts"
      via: "dual-read from both old and new gate sources"
      pattern: "gateService"
---

<objective>
Generalize MDMP governance components to work with any gate type, unify MDMP gates into the decision gate registry, and implement training mode tagging.

Purpose: Unifies the two governance systems (MDMP gates and new decision gates) into one registry. MDMP becomes a gate workflow type, not a separate system.
Output: Generalized governance components, MDMP dual-read integration, training mode support.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-embedded-dao-governance-at-decision-gates-inserted/28-CONTEXT.md
@.planning/phases/28-embedded-dao-governance-at-decision-gates-inserted/28-RESEARCH.md
@.planning/phases/28-embedded-dao-governance-at-decision-gates-inserted/28-01-SUMMARY.md
@.planning/phases/28-embedded-dao-governance-at-decision-gates-inserted/28-02-SUMMARY.md

<interfaces>
From frontend/src/components/governance/GovernanceGateDashboard.tsx:
```typescript
// Currently MDMP-specific — needs generalization to work with any gate_type
```

From frontend/src/components/governance/DecisionBriefView.tsx:
```typescript
// Currently renders MDMP-specific brief — needs to work for all gate types
```

From frontend/src/components/governance/MDMPGovernancePanel.tsx:
```typescript
// Container with tab-based governance UI — refactor to be one workflow type
```

From frontend/src/lib/mdmp-service.ts:
```typescript
export async function createWorkflow(params: CreateWorkflowParams): Promise<WorkflowState>;
export async function getWorkflow(missionId: string): Promise<{...}>;
export async function registerPhaseGates(missionId: string, phase: string): Promise<void>;
export async function satisfyGate(missionId: string, gateId: string, ...): Promise<void>;
// Uses separate data model from decision_gates table
```

From frontend/src/context/ModeContext.tsx:
```typescript
const { mode } = useMode();  // 'training' | 'operational'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize GovernanceGateDashboard, DecisionBriefView, PhaseProgressionBar</name>
  <files>frontend/src/components/governance/GovernanceGateDashboard.tsx, frontend/src/components/governance/GovernanceGateDashboard.css, frontend/src/components/governance/DecisionBriefView.tsx, frontend/src/components/governance/DecisionBriefView.css, frontend/src/components/governance/PhaseProgressionBar.tsx, frontend/src/components/governance/PhaseProgressionBar.css</files>
  <action>
Read each file fully before modifying.

**GovernanceGateDashboard.tsx:**
- Currently hardcoded to MDMP gate types and phases
- Generalize: accept gates from useDecisionGates() context instead of MDMP-specific data
- Props should accept an optional `gateFilter?: string` (tab or gate_type to filter by), defaulting to showing all gates
- Render gates as a dashboard grid/table with: gate type label, tab, target item, status, enforcement mode, deadline
- Use GateStatusBadge for status display
- Remove MDMP-specific hardcoded gate names/phases
- If the component currently takes MDMP-specific props, make them optional and add new DecisionGate-based rendering path
- Keep backward compatibility: if old MDMP data is passed, render it; if DecisionGate data is available from context, render that too (dual-read)

**DecisionBriefView.tsx:**
- Currently renders MDMP-specific decision brief (context, recommendation, outcome)
- Generalize: accept a `gate: DecisionGate` prop instead of (or in addition to) MDMP-specific data
- Render for any gate type:
  - Context section: gate.decision_context (metadata, description from proposal)
  - Recommendation section: extracted from decision_context or generated label
  - Outcome section: gate.status, decided_by, decided_at
  - Gate type label, enforcement mode, tab
- Training mode: if gate.mode === 'training', show a "TRAINING" badge/tag prominently
- Keep backward compatibility with existing MDMP brief props

**PhaseProgressionBar.tsx:**
- Currently shows MDMP phase progression
- Generalize: accept an array of gates and show progression through gate workflow
- Each gate as a step in the progression bar (pending -> submitted -> approved/rejected)
- Color-code by status
- Keep backward compatibility with MDMP phase data
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>GovernanceGateDashboard renders any gate type from context; DecisionBriefView works for all gate types with training mode tag; PhaseProgressionBar shows gate workflow progression; all backward compatible with existing MDMP usage</done>
</task>

<task type="auto">
  <name>Task 2: MDMP dual-read integration and training mode gate tagging</name>
  <files>frontend/src/components/governance/MDMPGovernancePanel.tsx, frontend/src/lib/mdmp-service.ts, backend/src/gates/gate-service.ts</files>
  <action>
Read each file fully before modifying.

**mdmp-service.ts (frontend):**
- Add import for gateService from gate-service.ts
- In functions that read MDMP gates (getWorkflow, or wherever phaseGates are returned):
  - After fetching MDMP data, also fetch gates from gateService for the same problem set
  - Merge: return MDMP gates as-is (for backward compatibility) AND append decision gates that have gate_type matching MDMP gate patterns
  - This is the "dual-read" approach from the research: new system is authoritative for new gates, old MDMP data still visible
- In registerPhaseGates: keep existing behavior (registers MDMP gates in old system). New gates are created via gate-service directly.
- Add a helper function `convertMdmpGateToDecisionGate(mdmpGate)` that maps old MDMP gate format to DecisionGate interface for unified rendering

**MDMPGovernancePanel.tsx:**
- Read the component to understand its current structure
- Import useDecisionGates hook
- In addition to existing MDMP data, also render DecisionGate data from context
- Show both old MDMP gates (from mdmp-service) and new gates (from gate context) in a unified view
- Use the generalized GovernanceGateDashboard for rendering when gate context data is available
- This makes MDMPGovernancePanel one view within the unified system rather than a standalone

**gate-service.ts (backend):**
- Add training mode awareness:
  - In createGate: if mode is 'training', set training_config with the exercise's training gate behavior (instructor-approved, auto-approved, full-governance)
  - In submitForApproval: if gate.mode === 'training' and training_config.behavior === 'auto-approved', automatically approve the gate (transition to 'approved' with decision_context noting "Auto-approved in training mode")
  - In submitForApproval: if gate.mode === 'training' and training_config.behavior === 'instructor-approved', proceed normally (instructor has commander-level authority via DAO role mapping)
  - In processTimeouts: training mode gates follow the same timeout logic
- Tag all training mode gate status transitions with mode: 'training' metadata in decision_context
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>mdmp-service reads from both old MDMP and new gate sources; MDMPGovernancePanel renders unified gate view; backend gate-service handles training mode auto-approval and instructor-approval; training gates tagged as TRAINING</done>
</task>

</tasks>

<verification>
- Generalized components render any gate type (not just MDMP)
- MDMP panel shows both old and new gates
- Training mode auto-approval works on backend
- Training gates display TRAINING badge in DecisionBriefView
- TypeScript compiles cleanly for both frontend and backend
- Existing MDMP workflows still function (backward compatible)
</verification>

<success_criteria>
- One gate system, not two (locked decision: unify MDMP gates into same registry)
- MDMP becomes one gate workflow type (locked decision)
- DecisionBriefView works for all gate types (locked decision)
- Training mode configurable: instructor-approved, auto-approved, full-governance (locked decision)
- Training decisions tagged as TRAINING (locked decision)
- Dual-read approach: no data loss from MDMP migration
</success_criteria>

<output>
After completion, create `.planning/phases/28-embedded-dao-governance-at-decision-gates-inserted/28-06-SUMMARY.md`
</output>
