---
phase: 28-embedded-dao-governance-at-decision-gates-inserted
plan: 03
type: execute
wave: 2
depends_on: ["28-01", "28-02"]
files_modified:
  - frontend/src/components/governance/GateProposalModal.tsx
  - frontend/src/components/governance/GateProposalModal.css
  - frontend/src/components/governance/GateSubmitButton.tsx
  - frontend/src/components/governance/GateSubmitButton.css
  - frontend/src/components/governance/DecisionGateTimeline.tsx
  - frontend/src/components/governance/DecisionGateTimeline.css
  - frontend/src/components/tabs/TabLayout.tsx
  - frontend/src/components/tabs/TabLayout.css
  - frontend/src/components/governance/index.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Proposal form pre-populates with context from the item being submitted"
    - "Proposal form opens as a modal overlay consistent with MissionWizard pattern"
    - "Compact decision history log appears in tab sidebar"
    - "Last 5 decisions shown by default with Show All link"
    - "Each log entry is clickable and opens ProposalDetail modal"
  artifacts:
    - path: "frontend/src/components/governance/GateProposalModal.tsx"
      provides: "Contextual proposal creation modal"
      exports: ["GateProposalModal"]
    - path: "frontend/src/components/governance/GateSubmitButton.tsx"
      provides: "Submit for Approval button placed next to decidable items"
      exports: ["GateSubmitButton"]
    - path: "frontend/src/components/governance/DecisionGateTimeline.tsx"
      provides: "Compact decision history for sidebar"
      exports: ["DecisionGateTimeline"]
  key_links:
    - from: "frontend/src/components/governance/GateSubmitButton.tsx"
      to: "frontend/src/components/governance/GateProposalModal.tsx"
      via: "button click opens modal"
      pattern: "GateProposalModal"
    - from: "frontend/src/components/governance/DecisionGateTimeline.tsx"
      to: "frontend/src/components/dao/ProposalDetail.tsx"
      via: "entry click opens detail modal"
      pattern: "ProposalDetail"
    - from: "frontend/src/components/tabs/TabLayout.tsx"
      to: "frontend/src/components/governance/DecisionGateTimeline.tsx"
      via: "optional sidebar section"
      pattern: "DecisionGateTimeline|decisionHistory"
---

<objective>
Build the contextual proposal creation modal, inline submit button, and decision history timeline, then extend TabLayout to host the timeline in its sidebar.

Purpose: These are the interaction components that make governance contextual — submit at the point of decision, see history in context.
Output: GateProposalModal, GateSubmitButton, DecisionGateTimeline, and extended TabLayout.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-embedded-dao-governance-at-decision-gates-inserted/28-CONTEXT.md
@.planning/phases/28-embedded-dao-governance-at-decision-gates-inserted/28-RESEARCH.md
@.planning/phases/28-embedded-dao-governance-at-decision-gates-inserted/28-01-SUMMARY.md
@.planning/phases/28-embedded-dao-governance-at-decision-gates-inserted/28-02-SUMMARY.md

<interfaces>
From frontend/src/components/tabs/TabLayout.tsx:
```typescript
export interface SidebarItem {
  id: string;
  label: string;
  icon?: string;
  badge?: number;
}
export interface TabLayoutProps {
  items: SidebarItem[];
  selectedItem: string;
  onSelectItem: (id: string) => void;
  children: React.ReactNode;
}
export function TabLayout({ items, selectedItem, onSelectItem, children }: TabLayoutProps) { ... }
```

From frontend/src/components/dao/ProposalDetail.tsx:
```typescript
// Existing full proposal detail modal — reuse for "Details" link
```

From frontend/src/context/DecisionGateContext.tsx (Plan 02):
```typescript
export function useDecisionGates(tabId?: string): {
  gates: DecisionGate[];
  pendingCount: number;
  pendingApprovals: DecisionGate[];
  loading: boolean;
  error: string | null;
  submitForApproval: (gateId: string, context: GateProposalContext) => Promise<void>;
  approveGate: (gateId: string) => Promise<void>;
  rejectGate: (gateId: string, reason: string) => Promise<void>;
  isCommander: boolean;
  refreshGates: () => Promise<void>;
  createGate: (params: CreateGateParams) => Promise<void>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GateProposalModal and GateSubmitButton</name>
  <files>frontend/src/components/governance/GateProposalModal.tsx, frontend/src/components/governance/GateProposalModal.css, frontend/src/components/governance/GateSubmitButton.tsx, frontend/src/components/governance/GateSubmitButton.css</files>
  <action>
Create `GateProposalModal.tsx`:
- Follow the MissionWizard/CreateProblemSetWizard modal overlay pattern (inspect one of these for exact structure — fixed overlay, centered card, header/body/footer sections)
- Props: `gateType: string, tabId: string, prefillContext: { title: string, description: string, metadata: Record<string,unknown> }, daoId: string, onSubmit: (proposal: GateProposalContext) => Promise<void>, onClose: () => void`
- Pre-populates form with prefillContext:
  - Title field (editable, pre-filled from prefillContext.title)
  - Description textarea (editable, pre-filled from prefillContext.description)
  - Gate type label (read-only display, derived from gateType)
  - Justification textarea (empty, user provides reasoning)
  - Metadata display (read-only key-value pairs from prefillContext.metadata)
- Submit button calls onSubmit with edited context, then onClose
- Cancel button calls onClose
- Loading state during submission
- Gate type labels: objective_approval -> "Objective Approval", operational_approach -> "Operational Approach Approval", coa_selection -> "COA Selection", order_release -> "Order Release", reframing -> "Reframing Decision"

Create `GateSubmitButton.tsx`:
- Props: `gateType: string, itemId: string, itemTitle: string, itemDescription: string, tabId: string, metadata?: Record<string,unknown>, className?: string`
- Renders a contextual "Submit for Approval" button (compact, secondary style)
- Uses useDecisionGates(tabId) to get createGate and submitForApproval
- On click: first creates a gate (if none exists for this item), then opens GateProposalModal pre-populated with item context
- If a gate already exists for this itemId in 'submitted' status, shows GateStatusBadge instead of submit button
- If gate is 'approved', shows green checkmark badge
- If gate is 'rejected', shows "Resubmit" button variant
- Uses useState for modal open/close
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>GateProposalModal renders as modal overlay with pre-populated form; GateSubmitButton renders contextually based on gate state (submit/status/resubmit); both integrate with DecisionGateContext</done>
</task>

<task type="auto">
  <name>Task 2: DecisionGateTimeline and TabLayout sidebar extension</name>
  <files>frontend/src/components/governance/DecisionGateTimeline.tsx, frontend/src/components/governance/DecisionGateTimeline.css, frontend/src/components/tabs/TabLayout.tsx, frontend/src/components/tabs/TabLayout.css, frontend/src/components/governance/index.ts</files>
  <action>
Create `DecisionGateTimeline.tsx`:
- Props: `tabId: string, onEntryClick?: (gate: DecisionGate) => void`
- Uses useDecisionGates(tabId) to get gates for this tab
- Filters to gates with status != 'pending' (only show gates that have been acted on)
- Sorts by updated_at descending (most recent first)
- Shows last 5 entries by default
- "Show all" link at bottom expands to full list (useState toggle)
- Each entry is a compact card/row:
  - Status icon (colored dot: green=approved, red=rejected, amber=submitted, purple=escalated)
  - target_item_title (or gate_type label if no title)
  - Relative timestamp (e.g., "2 hours ago")
  - Status text
  - Clickable — calls onEntryClick(gate) which parent uses to open ProposalDetail modal
- Collapsible section wrapper with "Decision History" header and chevron toggle
- Empty state: "No decisions yet" message

Extend `TabLayout.tsx`:
- Add optional props to TabLayoutProps:
  - `decisionHistory?: ReactNode` — optional slot for decision history section below sidebar items
- In the sidebar rendering, after the sidebar items list, render the decisionHistory slot if provided
- This is a simple slot — the actual DecisionGateTimeline component is passed in by each tab
- Do NOT break existing TabLayout usage — decisionHistory is optional and additive
- Update TabLayout.css if needed for the new section (separator line, padding)

Update `frontend/src/components/governance/index.ts`:
- Add exports for GateProposalModal, GateSubmitButton, DecisionGateTimeline
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>DecisionGateTimeline shows last 5 decisions with Show All toggle; entries are clickable; TabLayout accepts optional decisionHistory slot in sidebar; governance index exports all new components</done>
</task>

</tasks>

<verification>
- GateProposalModal follows MissionWizard overlay pattern
- GateSubmitButton shows correct state (submit/badge/resubmit) based on gate status
- DecisionGateTimeline shows last 5 with expand capability
- TabLayout backward-compatible (existing tabs unaffected)
- All components exported from governance/index.ts
</verification>

<success_criteria>
- Proposal modal pre-populates title/description from item context per locked decision
- Modal follows established project modal pattern
- Submit button renders contextually next to decidable items
- Timeline compact with 5-entry default, expandable
- Timeline entries clickable for full detail
- TabLayout extended without breaking existing tabs
</success_criteria>

<output>
After completion, create `.planning/phases/28-embedded-dao-governance-at-decision-gates-inserted/28-03-SUMMARY.md`
</output>
