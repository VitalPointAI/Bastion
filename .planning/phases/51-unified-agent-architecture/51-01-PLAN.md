---
phase: 51-unified-agent-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/agents/standard-agent.ts
  - backend/src/agents/agent-store.ts
  - backend/src/agents/agent-memory-store.ts
  - backend/src/agents/team-store.ts
  - backend/src/db/migrations/034-agent-tables.sql
  - backend/src/db/migrations/035-agent-memory.sql
autonomous: true
requirements: [REQ-51-01, REQ-51-02]

must_haves:
  truths:
    - "StandardAgent interface extends AgentManifest with systemPrompt, clearance, skills, status, health metrics"
    - "AgentStore persists agents to PostgreSQL and retrieves them by ID"
    - "AgentMemoryStore persists memory entries with embedding support and semantic recall"
    - "TeamStore persists teams to PostgreSQL"
    - "SQL migrations create agents_v2, agent_teams, and agent_memory tables"
  artifacts:
    - path: "backend/src/agents/standard-agent.ts"
      provides: "StandardAgent interface, AgentSkill interface, toStandardAgent helper"
      exports: ["StandardAgent", "AgentSkill", "toStandardAgent", "MemoryEntry", "EpisodeSummary"]
    - path: "backend/src/agents/agent-store.ts"
      provides: "PostgreSQL-backed agent CRUD"
      exports: ["AgentStore", "getAgentStore"]
    - path: "backend/src/agents/agent-memory-store.ts"
      provides: "Per-agent memory with semantic recall"
      exports: ["AgentMemoryStore", "getAgentMemoryStore"]
    - path: "backend/src/agents/team-store.ts"
      provides: "PostgreSQL-backed team CRUD"
      exports: ["TeamStore", "getTeamStore"]
    - path: "backend/src/db/migrations/034-agent-tables.sql"
      provides: "agents_v2 and agent_teams tables"
      contains: "CREATE TABLE IF NOT EXISTS agents_v2"
    - path: "backend/src/db/migrations/035-agent-memory.sql"
      provides: "agent_memory table with indexes"
      contains: "CREATE TABLE IF NOT EXISTS agent_memory"
  key_links:
    - from: "backend/src/agents/standard-agent.ts"
      to: "backend/src/agents/types.ts"
      via: "extends AgentManifest"
      pattern: "extends AgentManifest"
    - from: "backend/src/agents/agent-store.ts"
      to: "backend/src/db/migrations/034-agent-tables.sql"
      via: "queries agents_v2 table"
      pattern: "agents_v2"
    - from: "backend/src/agents/agent-memory-store.ts"
      to: "backend/src/validation/scoring/cosine-similarity.ts"
      via: "semantic recall uses existing cosine similarity"
      pattern: "cosineSimilarity"
---

<objective>
Create the foundational StandardAgent type system, DB migrations, and PostgreSQL-backed stores for agents, teams, and agent memory.

Purpose: Replace in-memory Maps with persistent DB storage. Define the single agent template all agents will be built from.
Output: StandardAgent types, AgentStore, TeamStore, AgentMemoryStore, SQL migrations 034-035.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-unified-agent-architecture/51-CONTEXT.md
@.planning/phases/51-unified-agent-architecture/51-RESEARCH.md
@.planning/phases/51-unified-agent-architecture/ARCHITECTURE.md

@backend/src/agents/types.ts
@backend/src/agents/registry.ts
@backend/src/agents/team-registry.ts
@backend/src/validation/scoring/cosine-similarity.ts
@backend/src/lib/database.ts

<interfaces>
<!-- From backend/src/agents/types.ts — the interface StandardAgent extends -->
AgentManifest interface with fields: agentId, name, description, phase, capabilities[], maxAutonomy, allowedProposalKinds[], requiresHumanApproval[], createdAt, createdBy, active, agentDID?, agentBlindedKey?, agentPublicKey?, modelConfig?, character?

<!-- From backend/src/lib/database.ts — DB access pattern -->
getPool() returns pg.Pool instance for all queries

<!-- From backend/src/validation/scoring/cosine-similarity.ts — existing utility -->
cosineSimilarity(a: number[], b: number[]) => number  (0 to 1)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StandardAgent types + SQL migrations</name>
  <files>
    backend/src/agents/standard-agent.ts,
    backend/src/db/migrations/034-agent-tables.sql,
    backend/src/db/migrations/035-agent-memory.sql
  </files>
  <behavior>
    - toStandardAgent(manifest) returns valid StandardAgent with defaults for missing fields
    - toStandardAgent(manifest, {systemPrompt: 'X'}) merges extras
    - StandardAgent extends AgentManifest (all existing fields preserved)
    - MemoryEntry has: entryId, agentId, memoryType, category, content, embedding, importance, createdAt, lastAccessed, taskId
  </behavior>
  <action>
    Create `backend/src/agents/standard-agent.ts`:
    - Export `AgentSkill` interface with skillId, name, description, inputSchema (z.ZodType), outputSchema (z.ZodType)
    - Export `StandardAgent` interface extending `AgentManifest` from `./types.js`. Add fields per ARCHITECTURE.md: systemPrompt (string), clearance (ClassificationLevel — import from existing types or define as string union 'Unclassified'|'CUI'|'Secret'|'TopSecret'), skills (AgentSkill[]), status ('active'|'inactive'|'degraded'|'error'), activatedAt?, deactivatedAt?, lastInvocation?, successRate? (number 0-1), avgResponseTimeMs?, validationScore?
    - Export `MemoryEntry` interface: entryId, agentId, memoryType ('knowledge'|'working'|'episode'), category?, content, embedding? (number[]), importance (number, default 0.5), createdAt, lastAccessed?, taskId?
    - Export `EpisodeSummary` interface: entryId, agentId, taskId, summary, outcome, timestamp
    - Export `toStandardAgent(manifest: AgentManifest, extras?: Partial<StandardAgent>): StandardAgent` helper that wraps an existing manifest with defaults: systemPrompt='', clearance='Unclassified', skills=[], status = manifest.active ? 'active' : 'inactive'

    Create `backend/src/db/migrations/034-agent-tables.sql`:
    - agents_v2 table: agent_id TEXT PRIMARY KEY, agent_data JSONB NOT NULL, created_at TIMESTAMPTZ DEFAULT NOW(), updated_at TIMESTAMPTZ DEFAULT NOW(), status TEXT DEFAULT 'active', last_invocation TIMESTAMPTZ, success_rate NUMERIC(4,3), avg_response_time_ms INTEGER, validation_score NUMERIC(4,3)
    - agent_teams table: team_id TEXT PRIMARY KEY, team_data JSONB NOT NULL, created_at TIMESTAMPTZ DEFAULT NOW(), updated_at TIMESTAMPTZ DEFAULT NOW()
    - agent_action_log table: log_id TEXT PRIMARY KEY DEFAULT gen_random_uuid()::TEXT, agent_id TEXT NOT NULL, action TEXT NOT NULL, details JSONB, created_at TIMESTAMPTZ DEFAULT NOW()
    - Index on agent_action_log(agent_id)

    Create `backend/src/db/migrations/035-agent-memory.sql`:
    - agent_memory table: entry_id TEXT PRIMARY KEY DEFAULT gen_random_uuid()::TEXT, agent_id TEXT NOT NULL, memory_type TEXT NOT NULL, category TEXT, content TEXT NOT NULL, embedding JSONB, importance NUMERIC(4,3) DEFAULT 0.5, created_at TIMESTAMPTZ DEFAULT NOW(), last_accessed TIMESTAMPTZ, task_id TEXT, CONSTRAINT fk_agent FOREIGN KEY (agent_id) REFERENCES agents_v2(agent_id) ON DELETE CASCADE
    - Indexes: idx_agent_memory_agent_id ON agent_memory(agent_id), idx_agent_memory_type ON agent_memory(agent_id, memory_type)
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && bash -lc 'npx --prefix backend tsc --noEmit --pretty 2>&1 | tail -20'</automated>
  </verify>
  <done>StandardAgent interface extends AgentManifest, toStandardAgent helper works, SQL migrations define all 4 tables (agents_v2, agent_teams, agent_action_log, agent_memory) with proper constraints and indexes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AgentStore + TeamStore + AgentMemoryStore</name>
  <files>
    backend/src/agents/agent-store.ts,
    backend/src/agents/team-store.ts,
    backend/src/agents/agent-memory-store.ts
  </files>
  <behavior>
    - AgentStore.registerAgent(agent) upserts to agents_v2
    - AgentStore.getAgent(id) returns StandardAgent or undefined
    - AgentStore.listAgents() returns all agents
    - AgentStore.updateHealth(id, metrics) updates health columns
    - AgentStore.deleteAgent(id) removes agent
    - TeamStore.createTeam(team) inserts to agent_teams
    - TeamStore.getTeam(id) returns team or undefined
    - TeamStore.listTeams() returns all teams
    - TeamStore.deleteTeam(id) removes team
    - AgentMemoryStore.remember(agentId, content, type, opts) persists entry
    - AgentMemoryStore.recall(agentId, query, limit) returns semantically similar entries
    - AgentMemoryStore.forget(entryId) deletes entry
    - AgentMemoryStore.listEntries(agentId, type) returns entries by type
  </behavior>
  <action>
    Create `backend/src/agents/agent-store.ts`:
    - Export `AgentStore` class. Use `getPool()` from `../lib/database.js` for all queries.
    - Methods: registerAgent(agent: StandardAgent), getAgent(agentId: string), listAgents(filters?: {status?: string}), updateAgent(agentId: string, updates: Partial<StandardAgent>), deleteAgent(agentId: string), updateHealth(agentId: string, health: {lastInvocation: Date, successRate: number, avgResponseTimeMs: number}), logAction(agentId: string, action: string, details?: any)
    - registerAgent uses INSERT ... ON CONFLICT DO UPDATE (upsert pattern)
    - agent_data column stores full StandardAgent as JSONB; health columns stored separately for efficient querying
    - Export `getAgentStore()` singleton factory (same pattern as other stores in the project)

    Create `backend/src/agents/team-store.ts`:
    - Export `TeamStore` class with: createTeam(team), getTeam(teamId), listTeams(), updateTeam(teamId, updates), deleteTeam(teamId)
    - team_data column stores full team object as JSONB
    - Export `getTeamStore()` singleton factory

    Create `backend/src/agents/agent-memory-store.ts`:
    - Export `AgentMemoryStore` class
    - remember(agentId, content, memoryType, opts?: {category?, importance?, taskId?, embedding?}): Insert to agent_memory. Do NOT generate embeddings inline — caller passes embedding if available, or entry stored without embedding. Only 'knowledge' type entries should have embeddings (per research: embedding generation is expensive, do it async/lazily).
    - recall(agentId, query, limit=5): Load all entries for agent with non-null embeddings. If a queryEmbedding is provided (or generate one via helper), compute cosine similarity using `cosineSimilarity` from `../../validation/scoring/cosine-similarity.js`. Sort by score descending, return top N. If no embeddings exist, fall back to simple text matching (ILIKE on content).
    - forget(entryId): DELETE FROM agent_memory WHERE entry_id = $1
    - listEntries(agentId, memoryType?): SELECT with optional type filter
    - clearWorkingMemory(agentId): DELETE WHERE agent_id=$1 AND memory_type='working'
    - Export `getAgentMemoryStore()` singleton factory
    - Import cosineSimilarity from `../validation/scoring/cosine-similarity.js`
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && bash -lc 'npx --prefix backend tsc --noEmit --pretty 2>&1 | tail -20'</automated>
  </verify>
  <done>AgentStore, TeamStore, and AgentMemoryStore all compile, use getPool() for DB access, AgentMemoryStore uses existing cosineSimilarity utility for semantic recall, all exports follow singleton factory pattern</done>
</task>

</tasks>

<verification>
- `bash -lc 'cd /home/vitalpointai/projects/ssr && npx --prefix backend tsc --noEmit'` passes (no type errors)
- SQL migrations are numbered 034 and 035 (sequential after existing 033)
- StandardAgent extends AgentManifest without breaking existing type consumers
</verification>

<success_criteria>
- StandardAgent type is defined and extends AgentManifest
- toStandardAgent helper converts existing manifests to new type
- AgentStore, TeamStore, AgentMemoryStore all have full CRUD methods
- SQL migrations create agents_v2, agent_teams, agent_action_log, agent_memory tables
- AgentMemoryStore.recall uses cosine-similarity.ts for semantic matching
- All code compiles with tsc --noEmit
</success_criteria>

<output>
After completion, create `.planning/phases/51-unified-agent-architecture/51-01-SUMMARY.md`
</output>
