---
phase: 51-unified-agent-architecture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/agents/narrative-synthesis.ts
  - backend/src/agents/loe-gap-analysis.ts
  - backend/src/agents/message-handlers.ts
  - backend/src/agents/adversary-modeler.ts
  - backend/src/agents/assumption-auditor.ts
  - backend/src/agents/data-bias-detector.ts
  - backend/src/agents/deception-detector.ts
  - backend/src/agents/deception-planner.ts
  - backend/src/agents/deescalation-manager.ts
  - backend/src/agents/effect-cascader.ts
  - backend/src/agents/escalation-modeler.ts
  - backend/src/agents/exploitation-analyst.ts
  - backend/src/agents/orders-validator.ts
  - backend/src/agents/problem-framing.ts
  - backend/src/agents/roe-compliance.ts
  - backend/src/agents/uncertainty-quantifier.ts
  - backend/src/agents/cog-analysis.ts
  - frontend/src/components/ai-staff/AIShowContributions.tsx
  - frontend/src/components/ai-staff/InlineAnnotation.tsx
  - frontend/src/components/ai-staff/AgentRoutingConfig.ts
  - frontend/src/hooks/useAgentRouting.ts
  - frontend/src/hooks/useInlineAnnotations.ts
  - frontend/src/components/brain/IngestionSidebar.tsx
autonomous: true
requirements: [REQ-51-06]

must_haves:
  truths:
    - "Dead backend agent files (narrative-synthesis, loe-gap-analysis, message-handlers) are deleted"
    - "Stub agent files (14 files: adversary-modeler through uncertainty-quantifier) are deleted"
    - "Dead frontend files (AIShowContributions, InlineAnnotation, AgentRoutingConfig, useAgentRouting, useInlineAnnotations) are deleted"
    - "IngestionSidebar.tsx (replaced by IngestionDrawer in Phase 50) is deleted"
    - "Backend and frontend both compile cleanly after deletions"
  artifacts:
    - path: "backend/src/agents/"
      provides: "Cleaned directory with only live agent files"
  key_links:
    - from: "backend/src/agents/langgraph/agent-seeder.ts"
      to: "backend/src/agents/"
      via: "seeder must not import deleted stub files"
      pattern: "import.*from.*agents/(adversary|assumption|data-bias|deception|deescalation|effect|escalation|exploitation|orders|problem|roe|uncertainty|cog)"
---

<objective>
Remove confirmed dead code from both backend and frontend: stub agent files, orphaned AI staff components, dead hooks, and replaced components.

Purpose: Clean slate before rewriting the agent system. Removes ~2000+ lines of dead code that would cause confusion during the rewrite.
Output: Cleaner codebase with only live files. Backend and frontend compile cleanly.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/AUDIT-BACKEND-DEAD-CODE.md
@.planning/codebase/AUDIT-FRONTEND-DEAD-CODE.md
@.planning/phases/51-unified-agent-architecture/51-CONTEXT.md

@backend/src/agents/langgraph/agent-seeder.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead backend agent files and fix imports</name>
  <files>
    backend/src/agents/narrative-synthesis.ts,
    backend/src/agents/loe-gap-analysis.ts,
    backend/src/agents/message-handlers.ts,
    backend/src/agents/adversary-modeler.ts,
    backend/src/agents/assumption-auditor.ts,
    backend/src/agents/data-bias-detector.ts,
    backend/src/agents/deception-detector.ts,
    backend/src/agents/deception-planner.ts,
    backend/src/agents/deescalation-manager.ts,
    backend/src/agents/effect-cascader.ts,
    backend/src/agents/escalation-modeler.ts,
    backend/src/agents/exploitation-analyst.ts,
    backend/src/agents/orders-validator.ts,
    backend/src/agents/problem-framing.ts,
    backend/src/agents/roe-compliance.ts,
    backend/src/agents/uncertainty-quantifier.ts,
    backend/src/agents/cog-analysis.ts
  </files>
  <action>
    1. Delete 3 confirmed-dead standalone files: `narrative-synthesis.ts`, `loe-gap-analysis.ts`, `message-handlers.ts`

    2. Delete 14 stub agent files that are registered by agent-seeder via agentId strings (NOT via TypeScript imports): adversary-modeler.ts, assumption-auditor.ts, data-bias-detector.ts, deception-detector.ts, deception-planner.ts, deescalation-manager.ts, effect-cascader.ts, escalation-modeler.ts, exploitation-analyst.ts, orders-validator.ts, problem-framing.ts, roe-compliance.ts, uncertainty-quantifier.ts, cog-analysis.ts

    3. BEFORE deleting each stub file, verify no TypeScript imports exist for it: `grep -r "from.*agents/FILENAME" backend/src/ --include="*.ts"`. The agent-seeder registers agents by manifest data (agentId string), NOT by importing from these files. If any file DOES have a live import, do NOT delete it — comment it as "stub, needs rewrite" instead.

    4. After all deletions, run `tsc --noEmit` to confirm no broken imports.

    5. Check `agent-seeder.ts` — it should still work because it creates manifests inline (objects with agentId, name, description, capabilities) rather than importing from the deleted files. Verify this assumption and fix if wrong.

    IMPORTANT: Do NOT delete files that are NOT in the confirmed dead list. Keep: copilot.ts, registry.ts, executor.ts, team-registry.ts, tool-registry.ts, agent-did.ts, agent-messaging.ts, character-builder.ts, character-schema.ts, definition-schema.ts, tool-did.ts, types.ts, and everything in agents/langgraph/.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && bash -lc 'npx --prefix backend tsc --noEmit --pretty 2>&1 | tail -20'</automated>
  </verify>
  <done>17 dead backend agent files deleted, tsc compiles cleanly, agent-seeder still functions (registers agents by manifest data, not by import)</done>
</task>

<task type="auto">
  <name>Task 2: Delete dead frontend AI staff files and hooks</name>
  <files>
    frontend/src/components/ai-staff/AIShowContributions.tsx,
    frontend/src/components/ai-staff/InlineAnnotation.tsx,
    frontend/src/components/ai-staff/AgentRoutingConfig.ts,
    frontend/src/hooks/useAgentRouting.ts,
    frontend/src/hooks/useInlineAnnotations.ts,
    frontend/src/components/brain/IngestionSidebar.tsx
  </files>
  <action>
    1. Delete confirmed-dead AI staff exports: `AIShowContributions.tsx`, `InlineAnnotation.tsx`, `AgentRoutingConfig.ts`

    2. Delete confirmed-dead hooks: `useAgentRouting.ts`, `useInlineAnnotations.ts`

    3. Delete `IngestionSidebar.tsx` (replaced by IngestionDrawer in Phase 50)

    4. Check if `ai-staff/index.ts` or any barrel file re-exports the deleted files. If so, remove those re-exports.

    5. Check if `components/brain/index.ts` re-exports IngestionSidebar. If so, remove that re-export.

    6. Run the frontend build to verify no broken imports.

    NOTE: Do NOT delete AIStaffContext.tsx, AIStaffPanel.tsx, AIStaffDocked.tsx, AIStaffFloating.tsx, AIStaffChatInput.tsx, or DesignAIPanel.tsx yet — those are removed in Plan 07 (after Ironclaw is ready to replace them). This task only removes files that are ALREADY dead (zero live consumers).
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && bash -lc 'npm --prefix frontend run build 2>&1 | tail -20'</automated>
  </verify>
  <done>6 dead frontend files deleted, barrel exports cleaned, frontend builds cleanly</done>
</task>

</tasks>

<verification>
- `bash -lc 'cd /home/vitalpointai/projects/ssr && npx --prefix backend tsc --noEmit'` passes
- `bash -lc 'cd /home/vitalpointai/projects/ssr && npm --prefix frontend run build'` passes
- None of the deleted files are imported anywhere
</verification>

<success_criteria>
- 17 dead backend files deleted (3 standalone dead + 14 stub agents)
- 6 dead frontend files deleted (3 AI staff + 2 hooks + 1 replaced component)
- Both backend and frontend compile cleanly
- No runtime regressions (agent-seeder still registers agents by manifest data)
</success_criteria>

<output>
After completion, create `.planning/phases/51-unified-agent-architecture/51-02-SUMMARY.md`
</output>
