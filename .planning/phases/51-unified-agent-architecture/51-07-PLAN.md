---
phase: 51-unified-agent-architecture
plan: 07
type: execute
wave: 4
depends_on: [51-04, 51-05, 51-06]
files_modified:
  - frontend/src/context/AIStaffContext.tsx
  - frontend/src/components/ai-staff/AIStaffChatInput.tsx
  - frontend/src/components/ai-staff/AIStaffDocked.tsx
  - frontend/src/components/ai-staff/AIStaffPanel.tsx
  - frontend/src/components/ai-staff/AIStaffFloating.tsx
  - frontend/src/components/design/DesignAIPanel.tsx
  - frontend/src/components/design/CoGAnalysisSection.tsx
  - frontend/src/components/design/LOETimelineSection.tsx
  - frontend/src/components/design/ProblemFramingSection.tsx
  - frontend/src/components/design/OperationalApproachSection.tsx
  - frontend/src/components/problem-set/ProblemSetTabContainer.tsx
  - frontend/src/App.tsx
autonomous: false
requirements: [REQ-51-05, REQ-51-06]

must_haves:
  truths:
    - "AIStaffContext.tsx is deleted and no component imports it"
    - "AIStaffProvider wrapper removed from ProblemSetTabContainer"
    - "All per-tab AI panels (DesignAIPanel, AIStaffPanel, AIStaffDocked, AIStaffFloating, AIStaffChatInput) are deleted"
    - "Design sections (CoG, LOE, ProblemFraming, OperationalApproach) no longer dispatch to AIStaff"
    - "Ironclaw is the sole AI interface — available everywhere via global drawer"
    - "Frontend compiles cleanly with zero AIStaff references"
    - "Feature flag IRONCLAW_SOLE_INTERFACE controls the cutover (optional gradual rollout)"
  artifacts:
    - path: "frontend/src/context/"
      provides: "Only IronclawContext remains (AIStaffContext deleted)"
    - path: "frontend/src/components/ai-staff/"
      provides: "Directory is empty or deleted"
  key_links:
    - from: "frontend/src/components/problem-set/ProblemSetTabContainer.tsx"
      to: "frontend/src/context/IronclawContext.tsx"
      via: "Ironclaw context available (no AIStaffProvider wrapper)"
      pattern: "IronclawContext|useIronclaw"
    - from: "frontend/src/components/design/CoGAnalysisSection.tsx"
      to: "frontend/src/context/IronclawContext.tsx"
      via: "AI analysis dispatches to Ironclaw (not AIStaff)"
      pattern: "useIronclaw|sendMessage"
---

<objective>
Remove AIStaffContext and all per-tab AI panels, making Ironclaw the sole AI interface. Update design sections to use Ironclaw for AI dispatch. Clean up the ai-staff directory.

Purpose: Complete the Ironclaw consolidation — single AI entry point everywhere, removing ~1500 lines of redundant AI panel code.
Output: AIStaffContext deleted, all AI staff components deleted, design sections updated, Ironclaw sole interface.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-unified-agent-architecture/51-CONTEXT.md
@.planning/phases/51-unified-agent-architecture/51-RESEARCH.md
@.planning/phases/51-unified-agent-architecture/51-06-SUMMARY.md

@frontend/src/context/AIStaffContext.tsx
@frontend/src/components/ai-staff/AIStaffPanel.tsx
@frontend/src/components/ai-staff/AIStaffDocked.tsx
@frontend/src/components/ai-staff/AIStaffFloating.tsx
@frontend/src/components/ai-staff/AIStaffChatInput.tsx
@frontend/src/components/design/DesignAIPanel.tsx
@frontend/src/components/design/CoGAnalysisSection.tsx
@frontend/src/components/design/LOETimelineSection.tsx
@frontend/src/components/design/ProblemFramingSection.tsx
@frontend/src/components/design/OperationalApproachSection.tsx
@frontend/src/components/problem-set/ProblemSetTabContainer.tsx
@frontend/src/App.tsx

<interfaces>
<!-- From IronclawContext.tsx (extended in Plan 06) -->
useIronclaw() hook provides: sendMessage(content), isOpen, toggle, messages
Context-aware: automatically includes tab, problemSet, role
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update design sections to use Ironclaw instead of AIStaff dispatch</name>
  <files>
    frontend/src/components/design/CoGAnalysisSection.tsx,
    frontend/src/components/design/LOETimelineSection.tsx,
    frontend/src/components/design/ProblemFramingSection.tsx,
    frontend/src/components/design/OperationalApproachSection.tsx,
    frontend/src/components/design/DesignAIPanel.tsx
  </files>
  <action>
    IMPORTANT: This must happen BEFORE deleting AIStaffContext, not after.

    For each of the 4 design sections (CoGAnalysisSection, LOETimelineSection, ProblemFramingSection, OperationalApproachSection):
    1. Find all `useAIStaff()` or `useAIStaffDispatch()` calls
    2. Replace with `useIronclaw()` from `../../context/IronclawContext`
    3. Where the section dispatches an AI analysis request (e.g., "analyze this CoG"), replace with `sendMessage()` via Ironclaw context. Format the message to include the section data: e.g., `sendMessage("Analyze center of gravity: " + JSON.stringify(cogData))`
    4. Where the section renders an inline AI response panel, remove it. AI responses now appear in the Ironclaw drawer (which is globally available). If the section had an inline "AI Analysis" section, replace it with a button: "Ask Ironclaw to Analyze" that opens the drawer and sends the query.
    5. Remove all imports of AIStaffContext or AIStaff types

    For `DesignAIPanel.tsx`:
    - This component renders AI staff in the design tab. It will be deleted in Task 2.
    - Find where DesignAIPanel is rendered (likely in a design tab layout component) and remove the render.
    - If the design tab has a layout that reserves space for DesignAIPanel, remove that space allocation.

    After these changes, grep for any remaining `AIStaff` references in design components — there should be zero.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && bash -lc '! grep -r "AIStaff\|useAIStaff\|AIStaffContext" frontend/src/components/design/ --include="*.tsx" --include="*.ts"'</automated>
  </verify>
  <done>All 4 design sections use Ironclaw for AI dispatch. Zero AIStaff references remain in design components. DesignAIPanel render removed from layout.</done>
</task>

<task type="auto">
  <name>Task 2: Delete AIStaffContext, AI staff components, and clean up wiring</name>
  <files>
    frontend/src/context/AIStaffContext.tsx,
    frontend/src/components/ai-staff/AIStaffChatInput.tsx,
    frontend/src/components/ai-staff/AIStaffDocked.tsx,
    frontend/src/components/ai-staff/AIStaffPanel.tsx,
    frontend/src/components/ai-staff/AIStaffFloating.tsx,
    frontend/src/components/design/DesignAIPanel.tsx,
    frontend/src/components/problem-set/ProblemSetTabContainer.tsx,
    frontend/src/App.tsx
  </files>
  <action>
    1. Remove `AIStaffProvider` wrapper from `ProblemSetTabContainer.tsx`:
    - Find where `<AIStaffProvider>` wraps tab content
    - Remove the provider wrapper (keep the children)
    - Remove the AIStaffContext import

    2. Remove `AIStaffProvider` from `App.tsx` if it's provided there:
    - Check if App.tsx wraps with AIStaffProvider
    - Remove if present

    3. Delete the following files:
    - `frontend/src/context/AIStaffContext.tsx`
    - `frontend/src/components/ai-staff/AIStaffChatInput.tsx`
    - `frontend/src/components/ai-staff/AIStaffDocked.tsx`
    - `frontend/src/components/ai-staff/AIStaffPanel.tsx`
    - `frontend/src/components/ai-staff/AIStaffFloating.tsx`
    - `frontend/src/components/design/DesignAIPanel.tsx`

    4. Check for and clean up any barrel files (index.ts) in ai-staff/ that re-export deleted files

    5. If the ai-staff/ directory is now empty, delete the directory

    6. Global grep for any remaining references to deleted files/components:
    - `grep -r "AIStaffContext\|AIStaffProvider\|AIStaffPanel\|AIStaffDocked\|AIStaffFloating\|AIStaffChatInput\|DesignAIPanel\|useAIStaff" frontend/src/ --include="*.tsx" --include="*.ts"`
    - Fix any remaining references (likely import statements that need removal)

    7. Run frontend build to verify clean compilation

    NOTE: Per CONTEXT.md, consider adding a feature flag `IRONCLAW_SOLE_INTERFACE=true` in the environment. However, since AIStaffContext is being deleted entirely (not conditionally), the feature flag is only useful if we want to gate this in production. If implementing: check `import.meta.env.VITE_IRONCLAW_SOLE_INTERFACE` and show a deprecation notice on the old panels before deletion. For simplicity, delete directly — the old panels are non-functional without AIStaffContext anyway.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && bash -lc 'npm --prefix frontend run build 2>&1 | tail -20'</automated>
  </verify>
  <done>AIStaffContext deleted, all AI staff components deleted, AIStaffProvider removed from wrappers, zero AIStaff references remain in codebase, frontend builds cleanly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Ironclaw consolidation: AIStaffContext and all per-tab AI panels removed. Ironclaw is now the sole AI interface. Design sections use Ironclaw for AI analysis dispatch. Agent admin dashboard at /admin/agents with CRUD, health monitoring, memory viewer, test harness. Team designer at /admin/teams with drag-and-drop composition. Ironclaw context-aware (knows current tab, problem set, user role).
  </what-built>
  <how-to-verify>
    1. Start the application and navigate to any tab
    2. Verify the Ironclaw drawer button is available (floating button or sidebar toggle)
    3. Open the Ironclaw drawer — verify it shows the context banner (current tab, problem set)
    4. Navigate to the Design tab — verify NO separate AI panel appears (only Ironclaw drawer)
    5. In Design tab, find an "Ask Ironclaw" button in a section (e.g., CoG Analysis) — click it and verify it opens the Ironclaw drawer with the analysis request
    6. Navigate to /admin/agents — verify the agent dashboard loads with health cards
    7. Try creating a new agent, viewing memory, running a test prompt
    8. Navigate to /admin/teams — verify the team designer loads with drag-and-drop
    9. Verify no "AI Staff" panels appear anywhere in the application
    10. Check browser console for any import/reference errors related to AIStaff
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- `bash -lc 'cd /home/vitalpointai/projects/ssr && npm --prefix frontend run build'` passes
- `! grep -r "AIStaff" frontend/src/ --include="*.tsx" --include="*.ts"` exits 0 (no matches)
- Ironclaw drawer is available on all tabs
- Design sections dispatch AI analysis to Ironclaw
</verification>

<success_criteria>
- AIStaffContext.tsx deleted
- All 5 AI staff component files deleted
- DesignAIPanel.tsx deleted
- AIStaffProvider removed from ProblemSetTabContainer and App
- 4 design sections use useIronclaw() instead of useAIStaff()
- Zero AIStaff references in entire frontend
- Frontend builds cleanly
- Ironclaw is the sole AI interface in the application
</success_criteria>

<output>
After completion, create `.planning/phases/51-unified-agent-architecture/51-07-SUMMARY.md`
</output>
