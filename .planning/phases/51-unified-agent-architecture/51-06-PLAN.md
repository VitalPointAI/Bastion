---
phase: 51-unified-agent-architecture
plan: 06
type: execute
wave: 3
depends_on: [51-03]
files_modified:
  - ironclaw/Dockerfile
  - backend/src/ironclaw/ironclaw-service.ts
  - backend/src/ironclaw/action-registry.ts
  - frontend/src/context/IronclawContext.tsx
  - frontend/src/components/ironclaw/IronclawDrawer.tsx
autonomous: true
requirements: [REQ-51-05]

must_haves:
  truths:
    - "Ironclaw sidecar is upgraded to latest version (Dockerfile IRONCLAW_COMMIT updated)"
    - "IronclawService accepts context (currentTab, problemSetId, userRole) and injects it as message prefix"
    - "Ironclaw can list active agents, get agent status, assign/unassign agents to problem sets, form teams"
    - "IronclawContext on frontend passes current tab, problem set, and user role with every message"
    - "New action types registered in action-registry for agent/team delegation"
  artifacts:
    - path: "ironclaw/Dockerfile"
      provides: "Updated IRONCLAW_COMMIT to latest"
      contains: "IRONCLAW_COMMIT"
    - path: "backend/src/ironclaw/ironclaw-service.ts"
      provides: "Extended with context injection and delegation commands"
    - path: "backend/src/ironclaw/action-registry.ts"
      provides: "New agent/team action types registered"
    - path: "frontend/src/context/IronclawContext.tsx"
      provides: "Context-aware message sending (tab, problemSet, role)"
    - path: "frontend/src/components/ironclaw/IronclawDrawer.tsx"
      provides: "Extended with agent/team assignment UI elements"
  key_links:
    - from: "frontend/src/context/IronclawContext.tsx"
      to: "backend/src/ironclaw/ironclaw-service.ts"
      via: "sends context with each message"
      pattern: "currentTab|userRole|problemSetId"
    - from: "backend/src/ironclaw/ironclaw-service.ts"
      to: "backend/src/agents/agent-store.ts"
      via: "delegation commands query agent store"
      pattern: "getAgentStore|getTeamStore"
---

<objective>
Upgrade Ironclaw to latest version, add context-awareness (tab, problem set, user role), and add agent/team delegation commands.

Purpose: Transform Ironclaw from a simple chat interface into a context-aware AI command center that can assign and manage agents/teams.
Output: Updated Dockerfile, extended IronclawService with delegation, context-aware frontend.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-unified-agent-architecture/51-CONTEXT.md
@.planning/phases/51-unified-agent-architecture/51-RESEARCH.md
@.planning/phases/51-unified-agent-architecture/51-03-SUMMARY.md

@backend/src/ironclaw/ironclaw-service.ts
@backend/src/ironclaw/action-registry.ts
@frontend/src/context/IronclawContext.tsx
@frontend/src/components/ironclaw/IronclawDrawer.tsx
@ironclaw/Dockerfile

<interfaces>
<!-- From ironclaw-service.ts — current signature -->
handleMessage(problemSetId, userDid, content): Promise<void>
buildSystemPrompt(): string

<!-- From action-registry.ts — pattern for registering actions -->
Action types with risk levels: LOW, MEDIUM, HIGH, CRITICAL
Locked registry pattern (register once, cannot re-register)

<!-- From IronclawContext.tsx — current context -->
IronclawProvider wraps AuthenticatedShell
sendMessage(content: string) — current simple signature
activeProblemSetId from ProblemSetContext

<!-- From 51-03: Agent/Team stores are now DB-backed -->
AgentStore: getAgent, listAgents (with status filter)
TeamStore: getTeam, listTeams
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Ironclaw + extend service with context and delegation</name>
  <files>
    ironclaw/Dockerfile,
    backend/src/ironclaw/ironclaw-service.ts,
    backend/src/ironclaw/action-registry.ts
  </files>
  <action>
    1. Upgrade Ironclaw sidecar:
    - Check the latest commit from https://github.com/nearai/ironclaw (use `gh api repos/nearai/ironclaw/commits?per_page=1` or `git ls-remote https://github.com/nearai/ironclaw HEAD`)
    - Update `IRONCLAW_COMMIT` ARG in `ironclaw/Dockerfile` from `56b72188974e042c379f9b74384a82bd0f5e8449` to the latest commit SHA
    - Verify the Dockerfile still builds correctly by running: `docker build ironclaw/ -t ironclaw-test --no-cache 2>&1 | tail -20`. If Docker is not available in the execution environment, note this as a manual verify step and proceed.
    - If the latest commit breaks the webhook API (different endpoint or body format), fall back to a known working commit between current and HEAD

    2. Extend `ironclaw-service.ts`:
    - Add `MessageContext` interface: { currentTab?: string, problemSetId?: string, userRole?: string }
    - Extend `handleMessage` signature: add optional `context?: MessageContext` parameter (backward compatible — existing callers pass nothing)
    - When context is provided, prefix the message content with `[Context: tab=${ctx.currentTab ?? 'unknown'}, problemSet=${ctx.problemSetId ?? 'none'}, role=${ctx.userRole ?? 'user'}]\n` before sending to Ironclaw webhook
    - Add delegation command handlers that Ironclaw can invoke via action cards:
      - `listActiveAgents()`: Query AgentStore.listAgents({status: 'active'}), return formatted list
      - `getAgentStatus(agentId)`: Query AgentStore.getAgent, return health summary
      - `assignAgentToProblemSet(agentId, problemSetId)`: Update agent assignment (store in agent_data JSONB or a mapping table)
      - `unassignAgentFromProblemSet(agentId, problemSetId)`: Remove assignment
      - `formTeamForTask(teamId, taskDescription)`: Activate a team for a task via supervisor
    - These handlers are invoked when Ironclaw returns an action card with the corresponding action_type

    3. Register new action types in `action-registry.ts`:
    - `list_active_agents` — LOW risk (read-only)
    - `get_agent_status` — LOW risk (read-only)
    - `assign_agent_to_problem_set` — MEDIUM risk
    - `unassign_agent_from_problem_set` — LOW risk
    - `form_team_for_task` — MEDIUM risk
    - Follow existing locked registry pattern
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && bash -lc 'npx --prefix backend tsc --noEmit --pretty 2>&1 | tail -20' && bash -lc 'docker build ironclaw/ -t ironclaw-test --no-cache 2>&1 | tail -5' || echo "Docker build skipped (docker not available) — verify manually"</automated>
  </verify>
  <done>Ironclaw Dockerfile updated with latest commit SHA and Docker image builds successfully. IronclawService accepts context and injects it as message prefix. Five new delegation action types registered. All backward compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend context-awareness + delegation UI in IronclawDrawer</name>
  <files>
    frontend/src/context/IronclawContext.tsx,
    frontend/src/components/ironclaw/IronclawDrawer.tsx
  </files>
  <action>
    Extend `IronclawContext.tsx`:
    - Add context derivation: use `useLocation()` from react-router to determine current tab from pathname. Create `deriveTabFromPath(pathname: string): string` helper that maps route segments to tab names (understand, design, plan, direct, cop, assess, admin, brain, etc.)
    - Get `userRole` from existing auth context (check what the auth context provides — likely a role or permissions field)
    - Get `activeProblemSetId` from existing `ProblemSetContext` (already available)
    - Extend `sendMessage(content: string)` to include context: call the backend API with `{content, context: {currentTab, problemSetId: activeProblemSetId, userRole}}`
    - Update the API call to pass the context object as part of the request body (check the current endpoint format — likely POST /api/ironclaw/message or similar)

    Extend `IronclawDrawer.tsx`:
    - Add a small context indicator at the top of the drawer showing current context: "[Tab: Design | Problem Set: Pacific Strategy | Role: Commander]" in a subtle banner
    - When Ironclaw returns action cards for agent/team delegation (list_active_agents, assign_agent, etc.), render them as interactive cards in the chat flow (this likely already works via the existing IronclawActionCard component — verify and extend if needed)
    - Add quick-action buttons at the bottom of the drawer:
      - "Show Active Agents" — sends a pre-formatted message asking Ironclaw to list agents
      - "Agent Status" — dropdown of agents, sends status query
    - These are convenience shortcuts, not required for delegation to work (Ironclaw handles natural language delegation via the action registry)
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && bash -lc 'npm --prefix frontend run build 2>&1 | tail -20'</automated>
  </verify>
  <done>IronclawContext sends tab/problemSet/role context with every message. IronclawDrawer shows context banner and quick-action buttons for agent delegation. Frontend builds cleanly.</done>
</task>

</tasks>

<verification>
- Backend: `bash -lc 'npx --prefix backend tsc --noEmit'` passes
- Frontend: `bash -lc 'npm --prefix frontend run build'` passes
- Ironclaw Dockerfile has updated IRONCLAW_COMMIT and Docker image builds
- IronclawService.handleMessage accepts optional context parameter
</verification>

<success_criteria>
- Ironclaw sidecar pinned to latest commit (or known-working newer commit)
- Docker build of ironclaw/ succeeds
- Context injection works: each message prefixed with tab/role/problemSet info
- Five delegation action types registered in action registry
- Frontend derives context from route, auth, and problem set
- Drawer shows context banner and agent quick-actions
- Backward compatible: existing callers without context still work
</success_criteria>

<output>
After completion, create `.planning/phases/51-unified-agent-architecture/51-06-SUMMARY.md`
</output>
