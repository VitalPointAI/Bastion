---
phase: 51-unified-agent-architecture
plan: 05
type: execute
wave: 2
depends_on: [51-01]
files_modified:
  - backend/src/api/teams.ts
  - frontend/src/components/admin/TeamDesignerPanel.tsx
  - frontend/src/components/admin/AdminDashboard.tsx
autonomous: true
requirements: [REQ-51-04]

must_haves:
  truths:
    - "Admin can create a team by dragging agents into a team composition area"
    - "Admin can designate a leader/orchestrator for each team"
    - "Admin can define team workflow (execution order, parallelism, checkpoints)"
    - "Admin can assign a team to a problem set"
    - "Admin can test a team against a test scenario"
    - "Team data persists to PostgreSQL via TeamStore"
  artifacts:
    - path: "backend/src/api/teams.ts"
      provides: "Team admin API endpoints"
    - path: "frontend/src/components/admin/TeamDesignerPanel.tsx"
      provides: "Drag-and-drop team designer panel"
  key_links:
    - from: "frontend/src/components/admin/TeamDesignerPanel.tsx"
      to: "backend/src/api/teams.ts"
      via: "fetch /api/admin/teams endpoints"
      pattern: "fetch.*api/admin/teams"
    - from: "frontend/src/components/admin/TeamDesignerPanel.tsx"
      to: "@dnd-kit/core"
      via: "DnD for agent composition"
      pattern: "DndContext|useDraggable|useDroppable"
---

<objective>
Build the team designer with drag-and-drop agent composition, leader designation, workflow definition, team testing, and problem set assignment.

Purpose: Enable admins to compose agent teams visually, define execution workflows, and assign teams to problem sets.
Output: Backend team API endpoints + frontend TeamDesignerPanel extending existing TeamComposerPanel patterns.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-unified-agent-architecture/51-CONTEXT.md
@.planning/phases/51-unified-agent-architecture/51-RESEARCH.md
@.planning/phases/51-unified-agent-architecture/51-01-SUMMARY.md

@frontend/src/components/admin/TeamComposerPanel.tsx
@frontend/src/components/admin/AdminDashboard.tsx

<interfaces>
<!-- From TeamComposerPanel.tsx — existing patterns -->
Existing panel has: Zod schema validation, react-hook-form, member roles, workflow types
AgentTeam type with: teamId, name, agents[], leader?, workflow?

<!-- From 51-01: TeamStore -->
TeamStore: createTeam, getTeam, listTeams, updateTeam, deleteTeam
getTeamStore() singleton

<!-- @dnd-kit already in package.json -->
@dnd-kit/core ^6.3.1
@dnd-kit/sortable ^10.0.0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Team admin API endpoints</name>
  <files>backend/src/api/teams.ts</files>
  <action>
    Create or extend `backend/src/api/teams.ts` with admin endpoints. All require auth + admin role.

    Endpoints:
    - GET /api/admin/teams — list all teams with member counts and status
    - GET /api/admin/teams/:teamId — get full team detail (members, workflow, leader)
    - POST /api/admin/teams — create team. Body: {name, description, agents: [{agentId, role}], leaderId?, workflow: {type, stages}}. Uses TeamStore.createTeam()
    - PUT /api/admin/teams/:teamId — update team config
    - DELETE /api/admin/teams/:teamId — delete team
    - POST /api/admin/teams/:teamId/assign — assign team to problem set. Body: {problemSetId}. Store the assignment (add to team_data JSONB or a separate assignment field)
    - POST /api/admin/teams/:teamId/unassign — unassign from problem set
    - POST /api/admin/teams/:teamId/test — test team execution. Body: {scenario, prompt}. Execute team workflow via supervisor, return per-agent trace + timing. This is a simple pass-through to the LangGraph supervisor with the team's agents.

    Team workflow types to support: 'sequential' (agents execute in order), 'parallel' (all at once), 'pipeline' (output of one feeds next), 'supervised' (leader routes to specialists).

    Mount router alongside agent admin routes.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && bash -lc 'npx --prefix backend tsc --noEmit --pretty 2>&1 | tail -20'</automated>
  </verify>
  <done>Team admin API compiles with all CRUD, assign, and test endpoints</done>
</task>

<task type="auto">
  <name>Task 2: Team designer panel with DnD and workflow editor</name>
  <files>
    frontend/src/components/admin/TeamDesignerPanel.tsx,
    frontend/src/components/admin/AdminDashboard.tsx
  </files>
  <action>
    Create `TeamDesignerPanel.tsx` — extends or replaces the existing `TeamComposerPanel`:
    - Read the existing TeamComposerPanel.tsx FIRST. It has Zod validation, react-hook-form, member roles, and workflow types. Build on this foundation — don't rebuild from scratch.

    Layout:
    - Left pane: Available agents list (fetched from GET /api/admin/agents, filterable by status/name). Each agent shown as a draggable card with name, status, capabilities summary.
    - Center pane: Team composition area (droppable). Agents dragged here become team members. Use `@dnd-kit/core` DndContext + useDraggable + useDroppable (already in package.json).
    - Right pane: Team config form + workflow editor

    Team config form (zod + react-hook-form):
    - Team name (required, 3-50 chars)
    - Description (optional textarea)
    - Leader/orchestrator: dropdown of team members, required
    - Workflow type: select (sequential | parallel | pipeline | supervised)
    - Workflow stages: for sequential/pipeline, sortable list of agents defining execution order. Use @dnd-kit/sortable for reordering.
    - Problem set assignment: dropdown of problem sets (fetch from existing problem set API)

    Team member card (in composition area):
    - Agent name, status badge
    - Role dropdown (leader, specialist, reviewer, observer)
    - Remove button (X)
    - Crown icon if designated as leader

    Team testing section:
    - Test prompt textarea
    - "Run Team Test" button
    - Results: per-agent output trace (collapsible sections per agent), total execution time, success/failure per agent

    Team list view:
    - Table of existing teams: name, member count, workflow type, assigned problem sets, actions (edit/delete)
    - "Create Team" button switches to the designer view

    Update `AdminDashboard.tsx`:
    - Ensure 'teams' view renders `TeamDesignerPanel` instead of old `TeamComposerPanel` (or swap the import)
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && bash -lc 'npm --prefix frontend run build 2>&1 | tail -20'</automated>
  </verify>
  <done>TeamDesignerPanel renders at /admin/teams with DnD composition, leader designation, workflow editor, problem set assignment, team testing. Frontend builds cleanly.</done>
</task>

</tasks>

<verification>
- Backend: `bash -lc 'npx --prefix backend tsc --noEmit'` passes
- Frontend: `bash -lc 'npm --prefix frontend run build'` passes
- Team CRUD API endpoints all work
- DnD imports from @dnd-kit resolve correctly
</verification>

<success_criteria>
- Drag-and-drop agent composition works with @dnd-kit
- Leader/orchestrator can be designated per team
- Workflow types (sequential, parallel, pipeline, supervised) configurable
- Teams can be assigned to problem sets
- Team testing sends prompt through team workflow and shows per-agent trace
- Existing TeamComposerPanel patterns preserved (Zod, react-hook-form)
- Both backend and frontend compile cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/51-unified-agent-architecture/51-05-SUMMARY.md`
</output>
