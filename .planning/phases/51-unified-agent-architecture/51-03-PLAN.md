---
phase: 51-unified-agent-architecture
plan: 03
type: execute
wave: 2
depends_on: [51-01, 51-02]
files_modified:
  - backend/src/agents/registry.ts
  - backend/src/agents/team-registry.ts
  - backend/src/agents/executor.ts
  - backend/src/agents/langgraph/agent-seeder.ts
autonomous: true
requirements: [REQ-51-01]

must_haves:
  truths:
    - "AgentRegistry delegates all storage to AgentStore (no more in-memory Map)"
    - "TeamRegistry delegates all storage to TeamStore (no more in-memory Map)"
    - "AgentRegistry preserves all existing method signatures (callers unchanged)"
    - "Agent seeder upserts agents to DB on startup via AgentStore"
    - "AgentExecutor uses LangGraph wrapper path for all agents (not just governance)"
    - "Stub agents marked status='inactive' instead of silently returning empty output"
  artifacts:
    - path: "backend/src/agents/registry.ts"
      provides: "DB-backed agent registry with preserved API surface"
      exports: ["AgentRegistry", "getAgentRegistry"]
    - path: "backend/src/agents/team-registry.ts"
      provides: "DB-backed team registry with preserved API surface"
      exports: ["TeamRegistry", "getTeamRegistry"]
    - path: "backend/src/agents/executor.ts"
      provides: "Generalized agent executor via LangGraph"
      exports: ["AgentExecutor"]
    - path: "backend/src/agents/langgraph/agent-seeder.ts"
      provides: "Seeds agents to DB using AgentStore"
  key_links:
    - from: "backend/src/agents/registry.ts"
      to: "backend/src/agents/agent-store.ts"
      via: "delegates to AgentStore for all reads/writes"
      pattern: "getAgentStore"
    - from: "backend/src/agents/team-registry.ts"
      to: "backend/src/agents/team-store.ts"
      via: "delegates to TeamStore for all reads/writes"
      pattern: "getTeamStore"
    - from: "backend/src/agents/executor.ts"
      to: "backend/src/orchestration/agent-wrapper.ts"
      via: "LangGraphAgentWrapper for agent execution"
      pattern: "LangGraphAgentWrapper"
---

<objective>
Rewrite AgentRegistry, TeamRegistry, and AgentExecutor to use DB-backed stores from Plan 01. Migrate agent-seeder to upsert StandardAgent records.

Purpose: Complete the backend transition from in-memory Maps to persistent DB storage. Make agents actually execute via LangGraph instead of stub returns.
Output: Registry, team-registry, executor, and seeder all use DB-backed stores with preserved API surfaces.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-unified-agent-architecture/51-CONTEXT.md
@.planning/phases/51-unified-agent-architecture/51-RESEARCH.md
@.planning/phases/51-unified-agent-architecture/51-01-SUMMARY.md

@backend/src/agents/registry.ts
@backend/src/agents/team-registry.ts
@backend/src/agents/executor.ts
@backend/src/agents/langgraph/agent-seeder.ts
@backend/src/orchestration/agent-wrapper.ts
@backend/src/agents/agent-did.ts

<interfaces>
<!-- From 51-01: new stores created -->
AgentStore: registerAgent(agent), getAgent(agentId), listAgents(filters?), updateAgent(agentId, updates), deleteAgent(agentId), updateHealth(agentId, health), logAction(agentId, action, details?)
getAgentStore() singleton

TeamStore: createTeam(team), getTeam(teamId), listTeams(), updateTeam(teamId, updates), deleteTeam(teamId)
getTeamStore() singleton

AgentMemoryStore: remember(agentId, content, type, opts?), recall(agentId, query, limit?), forget(entryId), listEntries(agentId, type?), clearWorkingMemory(agentId)
getAgentMemoryStore() singleton

StandardAgent extends AgentManifest + {systemPrompt, clearance, skills, status, activatedAt?, deactivatedAt?, lastInvocation?, successRate?, avgResponseTimeMs?, validationScore?}
toStandardAgent(manifest, extras?) helper
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite AgentRegistry and TeamRegistry to use DB stores</name>
  <files>backend/src/agents/registry.ts, backend/src/agents/team-registry.ts</files>
  <action>
    Rewrite `registry.ts` — KEEP all existing method signatures on AgentRegistry:
    - Replace the internal `Map<string, AgentManifest>` with calls to `getAgentStore()`
    - `registerAgent(manifest)` → convert to StandardAgent via `toStandardAgent(manifest)`, then `agentStore.registerAgent(sa)`
    - `getAgent(agentId)` → `agentStore.getAgent(agentId)`
    - `listAgents()` → `agentStore.listAgents()`
    - `activateAgent(agentId)` / `deactivateAgent(agentId)` → update status via `agentStore.updateAgent(agentId, {status: 'active'|'inactive'})`
    - KEEP `createAgentDID()` logic (import from agent-did.ts, unchanged)
    - KEEP `canActivateAgent()` logic (import from validation/activation-gate.ts, unchanged)
    - KEEP `getCharacter()` / `setCharacter()` methods — these read/write the character field on the agent in DB
    - Replace `actionLog` array with `agentStore.logAction()`
    - The `ensureInitialized()` method should now call `agentSeeder()` which upserts to DB (see Task 2)
    - IMPORTANT: All existing method signatures MUST be preserved. Callers include: executor.ts, agent-seeder.ts, api/agents.ts, api/admin.ts, orchestration/supervisor.ts, doc-intelligence/specialist-base.ts, validation/activation-gate.ts

    Rewrite `team-registry.ts`:
    - Replace internal `Map<string, AgentTeam>` with calls to `getTeamStore()`
    - `registerTeam(team)` → `teamStore.createTeam(team)`
    - `getTeam(teamId)` → `teamStore.getTeam(teamId)`
    - `listTeams()` → `teamStore.listTeams()`
    - Keep all existing method signatures
    - `initialize()` → no-op or call team seeder if one exists
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && bash -lc 'npx --prefix backend tsc --noEmit --pretty 2>&1 | tail -20'</automated>
  </verify>
  <done>AgentRegistry and TeamRegistry use DB stores internally, all existing method signatures preserved, no callers broken</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite agent-seeder and executor for DB + LangGraph path</name>
  <files>backend/src/agents/langgraph/agent-seeder.ts, backend/src/agents/executor.ts</files>
  <action>
    Rewrite `agent-seeder.ts`:
    - Instead of calling `registry.registerAgent(manifest)` with a basic AgentManifest, build `StandardAgent` objects via `toStandardAgent(manifest, extras)` where extras include: systemPrompt (agent-specific prompt), clearance, skills[] (define 1-2 skills per agent based on their capabilities), status
    - The seeder should call `agentStore.registerAgent()` directly (or via registry.registerAgent which now delegates to store)
    - For the 14 deleted stub agents: still seed them as manifests but with `status: 'inactive'`. This preserves the agent definitions in the DB for future reactivation without the dead code files.
    - For working agents (doc-intel specialists, graph agents): seed with `status: 'active'` and actual system prompts
    - The seeder is idempotent — uses upsert via AgentStore.registerAgent

    Rewrite `executor.ts`:
    - Replace the governance-only switch/case with a generalized execution path
    - For ANY agent skill execution: look up agent from AgentStore, wrap with `LangGraphAgentWrapper` from `orchestration/agent-wrapper.ts`, execute via the wrapper's `createNode()` function
    - Update health metrics after execution: call `agentStore.updateHealth(agentId, {lastInvocation: new Date(), successRate: computed, avgResponseTimeMs: elapsed})`
    - If agent.status !== 'active', throw a clear error: `Agent ${agentId} is ${agent.status} — cannot execute`
    - Keep the existing `executeHandler` method signature but generalize the internal routing
    - Log each execution to `agentStore.logAction(agentId, 'execute', {skill, input, duration, success})`
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && bash -lc 'npx --prefix backend tsc --noEmit --pretty 2>&1 | tail -20'</automated>
  </verify>
  <done>Agent seeder creates StandardAgent records in DB with proper status and system prompts. Executor uses LangGraph wrapper for all agents, updates health metrics, rejects inactive agents clearly.</done>
</task>

</tasks>

<verification>
- `bash -lc 'cd /home/vitalpointai/projects/ssr && npx --prefix backend tsc --noEmit'` passes
- No callers of AgentRegistry or TeamRegistry are broken (same method signatures)
- Agent seeder produces StandardAgent records (not just AgentManifest)
- Executor routes through LangGraph wrapper instead of governance-only switch
</verification>

<success_criteria>
- AgentRegistry backed by AgentStore (no in-memory Map)
- TeamRegistry backed by TeamStore (no in-memory Map)
- Agent seeder produces StandardAgent records with status, systemPrompt, skills
- Inactive stub agents are seeded with status='inactive'
- Executor uses LangGraph wrapper for all skill execution
- Health metrics updated after each execution
- All code compiles with tsc --noEmit
</success_criteria>

<output>
After completion, create `.planning/phases/51-unified-agent-architecture/51-03-SUMMARY.md`
</output>
