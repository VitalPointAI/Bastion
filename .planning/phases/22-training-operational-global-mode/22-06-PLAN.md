---
phase: 22-training-operational-global-mode
plan: 06
type: execute
wave: 3
depends_on: ["22-01", "22-02", "22-03", "22-04"]
files_modified:
  - backend/src/planning/documents/generators/pdf-generator.ts
  - backend/src/planning/documents/generators/docx-generator.ts
  - backend/src/middleware/exercise-watermark.ts
  - frontend/src/components/planning/DocumentExport.tsx
  - frontend/src/context/WorkspaceContext.tsx
  - frontend/src/context/ModeContext.tsx
  - backend/src/api/workspaces.ts
  - frontend/src/lib/workspace-service.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "All documents/exports generated in training mode are stamped with EXERCISE watermark"
    - "Each mode remembers its own last-active workspace independently"
    - "Loading an exercise scenario creates a training-mode workspace pre-populated with scenario data"
  artifacts:
    - path: "backend/src/middleware/exercise-watermark.ts"
      provides: "Utility to detect training mode and stamp EXERCISE on document outputs"
      exports: ["isTrainingMode", "addExerciseWatermark"]
    - path: "frontend/src/context/WorkspaceContext.tsx"
      provides: "Per-mode last-active workspace persistence"
      contains: "ACTIVE_WORKSPACE_KEY"
    - path: "backend/src/api/workspaces.ts"
      provides: "Scenario-to-workspace creation endpoint"
      contains: "scenario"
  key_links:
    - from: "backend/src/middleware/exercise-watermark.ts"
      to: "backend/src/planning/documents/generators/pdf-generator.ts"
      via: "watermark injection before PDF finalize"
      pattern: "EXERCISE"
    - from: "frontend/src/context/WorkspaceContext.tsx"
      to: "frontend/src/context/ModeContext.tsx"
      via: "mode-keyed localStorage for last-active workspace"
      pattern: "workspace-active-.*mode"
    - from: "backend/src/api/workspaces.ts"
      to: "backend/src/exercise/scenario-store.ts"
      via: "scenario load + workspace creation"
      pattern: "scenarioId"
---

<objective>
EXERCISE watermark on training-mode documents, per-mode workspace memory, and scenario-to-workspace wiring.

Purpose: Close three gaps identified by checker: (1) CONTEXT.md locked decision that all training-mode documents/exports get EXERCISE watermark, (2) per-mode last-active workspace persistence so each mode remembers where the user was, (3) exercise scenario selection creating a training workspace with pre-populated data.
Output: Watermark utility + document generator updates, mode-keyed workspace memory, scenario workspace creation endpoint.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-training-operational-global-mode/22-CONTEXT.md
@.planning/phases/22-training-operational-global-mode/22-01-SUMMARY.md
@.planning/phases/22-training-operational-global-mode/22-02-SUMMARY.md
@.planning/phases/22-training-operational-global-mode/22-03-SUMMARY.md
@.planning/phases/22-training-operational-global-mode/22-04-SUMMARY.md

<interfaces>
<!-- From Plan 01: mode middleware -->
From backend/src/middleware/mode-context.ts:
```typescript
// req.userMode = 'training' | 'operational' (set by modeMiddleware)
```

From backend/src/workspace/types.ts:
```typescript
export type AppMode = 'training' | 'operational';
```

<!-- From Plan 02: ModeContext -->
From frontend/src/context/ModeContext.tsx:
```typescript
export function useMode(): {
  mode: AppMode;
  isTraining: boolean;
  // ...
}
```

<!-- Document generators -->
From backend/src/planning/documents/generators/pdf-generator.ts:
```typescript
// Uses PDFKit to generate OPORD PDFs
// doc = new PDFDocument({ size: 'LETTER', margins: {...} })
// Returns { buffer, filename, mimeType }
export async function generateOPORDPdf(planId, metadata, options): Promise<GeneratedDocument>
```

From frontend/src/components/planning/DocumentExport.tsx:
```typescript
// Downloads documents via URL: getOPORDDocxUrl(planId), getOPORDPdfUrl(planId), getBriefingUrl(planId, type)
// Uses anchor element download pattern
```

<!-- WorkspaceContext active workspace persistence -->
From frontend/src/context/WorkspaceContext.tsx:
```typescript
const ACTIVE_WORKSPACE_KEY = 'workspace-active-id';
// setActiveWorkspace saves to localStorage.setItem(ACTIVE_WORKSPACE_KEY, id)
// On load: localStorage.getItem(ACTIVE_WORKSPACE_KEY) to restore
```

<!-- Scenario store -->
From backend/src/exercise/scenario-store.ts:
```typescript
export class ScenarioStore {
  async findById(id: string): Promise<ExerciseScenario | null>
  async findAll(): Promise<ExerciseScenario[]>
}
export const scenarioStore: ScenarioStore;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EXERCISE watermark on training-mode document exports</name>
  <files>backend/src/middleware/exercise-watermark.ts, backend/src/planning/documents/generators/pdf-generator.ts, backend/src/api/planning-documents.ts</files>
  <action>
1. Create `backend/src/middleware/exercise-watermark.ts`:
   - Export `function isTrainingMode(req: Request): boolean` — returns `(req as any).userMode === 'training'`
   - Export `function addPdfExerciseWatermark(doc: PDFKit.PDFDocument): void` — adds diagonal "EXERCISE" watermark text across each page:
     - Save graphics state, set opacity 0.15, font size 60, color red (#CC0000)
     - Rotate 45 degrees, draw "EXERCISE" text centered on each page
     - Restore graphics state
     - Use PDFKit's `doc.save()`, `doc.opacity()`, `doc.rotate()`, `doc.text()`, `doc.restore()` pattern
   - Export `function addExerciseHeader(doc: PDFKit.PDFDocument): void` — adds a bold red "EXERCISE - EXERCISE - EXERCISE" line at the very top of the first page, above the normal content. Font size 10, centered, red text. This mimics the military exercise message header convention.
   - Export `function getExerciseFilenamePrefix(): string` — returns `'EXERCISE_'` for prepending to filenames

2. In `backend/src/planning/documents/generators/pdf-generator.ts`:
   - Import `addPdfExerciseWatermark`, `addExerciseHeader`, `getExerciseFilenamePrefix` from the watermark utility
   - The function signature already receives `metadata` — add an optional `exerciseMode?: boolean` field to `OPORDGeneratorOptions` or `DocumentMetadata` (whichever is more appropriate per the types file)
   - Before `doc.end()`, if `exerciseMode` is true:
     - Call `addExerciseHeader(doc)` to add the EXERCISE header
     - Call `addPdfExerciseWatermark(doc)` to add the watermark
     - Prepend `EXERCISE_` to the output filename
   - If there is a DOCX generator (`docx-generator.ts`), apply similar logic: add "EXERCISE - EXERCISE - EXERCISE" as a header paragraph and prepend filename. For DOCX, add a Header element with red bold "EXERCISE - EXERCISE - EXERCISE" text.

3. In the document serving route (find where `generateOPORDPdf` is called — likely in `backend/src/api/planning-documents.ts` or the planning route file):
   - Import `isTrainingMode` from the watermark middleware
   - When handling a document generation request, pass `exerciseMode: isTrainingMode(req)` to the generator options
   - This ensures documents generated while user is in training mode automatically get watermarked
   - The modeMiddleware (from Plan 01) must be applied to these routes so `req.userMode` is available. If not already applied globally, ensure modeMiddleware runs before these handlers.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit --project backend/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>Training-mode document exports include EXERCISE watermark (PDF diagonal text + header), EXERCISE filename prefix, and DOCX exercise header. Watermark is automatically applied based on user's current mode.</done>
</task>

<task type="auto">
  <name>Task 2: Per-mode workspace memory and scenario-to-workspace creation</name>
  <files>frontend/src/context/WorkspaceContext.tsx, frontend/src/context/ModeContext.tsx, backend/src/api/workspaces.ts, frontend/src/lib/workspace-service.ts</files>
  <action>
1. In `frontend/src/context/WorkspaceContext.tsx`:
   - The existing code uses `ACTIVE_WORKSPACE_KEY = 'workspace-active-id'` for localStorage persistence
   - Change the key to be mode-aware: instead of a single key, use `workspace-active-id-${mode}` where mode comes from `useMode()`
   - Import `useMode` from ModeContext (may already be imported from Plan 03)
   - Update every reference to `ACTIVE_WORKSPACE_KEY`:
     - `localStorage.getItem(ACTIVE_WORKSPACE_KEY)` -> `localStorage.getItem(`workspace-active-id-${mode}`)`
     - `localStorage.setItem(ACTIVE_WORKSPACE_KEY, id)` -> `localStorage.setItem(`workspace-active-id-${mode}`, id)`
   - This means when mode switches (which triggers loadMemberships per Plan 03), the workspace restoration logic reads from the mode-specific key, restoring the user to their last workspace in that mode
   - The `setActiveWorkspace` callback must also write to the mode-specific key
   - You can keep the `ACTIVE_WORKSPACE_KEY` constant as a base: `const getActiveWorkspaceKey = (mode: string) => `workspace-active-id-${mode}`;`
   - IMPORTANT: `mode` must be available when the workspace restoration runs. Since ModeContext loads mode from API on mount, there may be a timing issue. Handle this by:
     - If mode is still loading (from ModeContext), skip workspace restoration until mode is resolved
     - Add `mode` to the dependency array of the effect that restores active workspace
     - Default to 'operational' if mode is not yet loaded (safe default per user decision)

2. In `backend/src/api/workspaces.ts`:
   - Add a new POST endpoint: `POST /api/workspaces/from-scenario` (or add a `scenarioId` field to the existing creation endpoint)
   - This endpoint:
     - Requires auth and mode middleware
     - Accepts `{ scenarioId: string, name?: string }` in body
     - Loads scenario from `scenarioStore.findById(scenarioId)`
     - Returns 404 if scenario not found
     - Creates a new workspace with `mode: 'training'` (always training, regardless of user mode — scenarios are inherently training)
     - Sets workspace name to `name ?? scenario.name`
     - After workspace creation, populate the workspace with scenario data:
       - If the scenario has environment/forces/phase data (from exercise_scenarios table fields), store these as workspace metadata or initial planning data
       - For now, store the full scenario snapshot in a workspace metadata field or as the first AAR event (`eventType: 'scenario_loaded'`)
       - Add the requesting user as workspace member with 'commander' role
     - Return the created workspace object

3. In `frontend/src/lib/workspace-service.ts`:
   - Add a `createFromScenario(scenarioId: string, name?: string): Promise<Workspace>` method
   - POST to `/api/workspaces/from-scenario` with `{ scenarioId, name }` and credentials: 'include'

4. NOTE for downstream integration: The ExerciseDashboard component will need to be updated to call `workspaceService.createFromScenario()` when a user selects/loads a scenario, then navigate to the created workspace. That wiring happens in Plan 05 integration or a future plan — this task just provides the API and service method.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit --project backend/tsconfig.json 2>&1 | head -20 && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Each mode persists its own last-active workspace independently via mode-keyed localStorage. Scenario-to-workspace creation endpoint exists and creates training-mode workspaces pre-populated with scenario data. Frontend service method available for ExerciseDashboard integration.</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes for both frontend and backend
- WorkspaceContext uses mode-specific localStorage keys for active workspace
- PDF generator applies EXERCISE watermark when user is in training mode
- POST /api/workspaces/from-scenario creates training workspace from scenario
- workspace-service.ts exports createFromScenario method
</verification>

<success_criteria>
Documents exported in training mode are watermarked with EXERCISE header and diagonal text. Switching between training and operational modes restores the last-active workspace for each mode independently. Exercise scenarios can create pre-populated training workspaces via API.
</success_criteria>

<output>
After completion, create `.planning/phases/22-training-operational-global-mode/22-06-SUMMARY.md`
</output>
