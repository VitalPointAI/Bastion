---
phase: 22-training-operational-global-mode
plan: 05
type: execute
wave: 3
depends_on: ["22-02", "22-03"]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/UserStatusBar.tsx
  - frontend/src/context/ModeContext.tsx
  - frontend/src/context/WorkspaceContext.tsx
autonomous: false
requirements: []

must_haves:
  truths:
    - "Full mode toggle flow works end-to-end: click toggle -> confirm -> mode switches -> banner appears/disappears -> workspaces refresh"
    - "Training mode shows amber EXERCISE banner, operational mode shows clean UI"
    - "Workspace list changes when mode switches"
    - "Train tab is gone from workspace tabs"
    - "Mode persists across page refresh"
  artifacts: []
  key_links:
    - from: "UserStatusBar toggle"
      to: "ModeContext.requestModeSwitch"
      via: "onClick handler"
      pattern: "requestModeSwitch"
    - from: "ModeContext.confirmModeSwitch"
      to: "WorkspaceContext.loadMemberships"
      via: "mode dependency change triggers re-fetch"
      pattern: "mode"
---

<objective>
End-to-end integration verification: test the complete mode toggle flow and fix any wiring issues between plans 01-04.

Purpose: Verify all four prior plans work together as a coherent system. Catch integration gaps (stale imports, missing API call params, layout issues with banner).
Output: Working end-to-end mode toggle with visual and data isolation verified.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-training-operational-global-mode/22-CONTEXT.md
@.planning/phases/22-training-operational-global-mode/22-01-SUMMARY.md
@.planning/phases/22-training-operational-global-mode/22-02-SUMMARY.md
@.planning/phases/22-training-operational-global-mode/22-03-SUMMARY.md
@.planning/phases/22-training-operational-global-mode/22-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration fixes and TypeScript compilation verification</name>
  <files>frontend/src/App.tsx, frontend/src/context/ModeContext.tsx, frontend/src/context/WorkspaceContext.tsx, frontend/src/components/UserStatusBar.tsx</files>
  <action>
1. Run full TypeScript compilation for both frontend and backend. Fix any errors.

2. Verify integration points:
   - ModeProvider is in correct position in App.tsx component tree (after auth, before WorkspaceProvider)
   - WorkspaceContext imports and calls useMode() correctly
   - WorkspaceContext passes mode to workspace service API calls
   - UserStatusBar imports and uses useMode() correctly
   - ExerciseBanner renders above header without layout overlap — check that the banner does not push content down unexpectedly. If the app layout uses fixed positioning for the header, adjust top offset when banner is present.
   - ModeConfirmationModal renders via portal or z-index above all other content

3. Verify the exercise banner placement in the layout:
   - The banner should appear at the absolute top of the viewport, above the header
   - If the app uses a flex column layout, banner should be the first child
   - If the app uses fixed header positioning, adjust header top when banner is visible
   - Test: In ModeContext, temporarily hardcode mode='training' to verify banner appears correctly, then revert

4. Verify mode middleware is applied in backend:
   - Check if modeMiddleware needs to be registered globally in index.ts (app.use(modeMiddleware) after auth middleware) or if it's applied per-route
   - Ensure the workspace endpoints have access to req.userMode

5. Fix any import path issues, missing type exports, or parameter mismatches between the four plans.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | head -20 && npx tsc --noEmit --project backend/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Both frontend and backend compile with zero TypeScript errors, all integration points verified, banner layout correct, mode middleware registered</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete training/operational mode toggle system: header toggle button, confirmation modal, amber EXERCISE banner, mode-aware workspace filtering, Train tab removal, AAR store, checkpoint store</what-built>
  <how-to-verify>
    1. Start the application (frontend + backend)
    2. Log in — verify mode defaults to OPERATIONAL (button in header shows "OPERATIONAL")
    3. Click the mode toggle button in the header bar
    4. Verify confirmation modal appears with appropriate warning text
    5. Click Cancel — verify nothing changes
    6. Click toggle again, then Confirm — verify:
       a. Amber "EXERCISE - EXERCISE - EXERCISE" banner appears at top of viewport
       b. Header button now shows "TRAINING"
       c. App navigates to workspace selector/home
       d. Workspace list shows only training-mode workspaces (likely empty initially)
    7. Refresh the page — verify mode persists (still shows TRAINING with banner)
    8. Switch back to OPERATIONAL — verify banner disappears, workspaces refresh
    9. Navigate to any workspace — verify Train tab is NOT in the tab bar
    10. Try navigating to /workspace/:id/train URL directly — verify it redirects to default tab
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- Full TypeScript compilation passes for both projects
- Visual verification of complete mode flow by user
- Mode persists across page refresh
- Workspace isolation works (different workspaces per mode)
- Train tab fully removed
</verification>

<success_criteria>
User confirms the complete training/operational mode system works end-to-end: toggle, confirmation, visual distinction (banner), workspace filtering, persistence, and Train tab removal.
</success_criteria>

<output>
After completion, create `.planning/phases/22-training-operational-global-mode/22-05-SUMMARY.md`
</output>
