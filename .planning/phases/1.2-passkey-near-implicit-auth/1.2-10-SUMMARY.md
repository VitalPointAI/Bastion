---
phase: 1.2
plan: 10
subsystem: authentication
tags:
  - passkey
  - webauthn
  - registration
  - auth-routes
  - user-context
dependency-graph:
  requires:
    - 1.2-05 (Frontend Passkey Authentication UI)
    - 1.2-08 (Auth Routing with React Router)
  provides:
    - Fixed @simplewebauthn/browser v13 compatibility
    - Working RegisterPage component
    - Protected route redirect to /login
    - User context with NEAR account ID and email
  affects:
    - All authenticated users (fixed passkey flow)
    - New user registration (new component)
    - Protected routes (redirect behavior)
tech-stack:
  added:
    - none (upgraded @simplewebauthn/browser 10.0.0 -> 13.2.2)
  patterns:
    - PRF extension type assertion for TypeScript compatibility
    - Session join with auth_users for email retrieval
key-files:
  created:
    - frontend/src/components/RegisterPage.tsx
    - frontend/src/components/RegisterPage.css
  modified:
    - frontend/package.json (@simplewebauthn/browser upgrade)
    - frontend/src/lib/passkey.ts (PRF type fixes)
    - frontend/src/App.tsx (/register route)
    - frontend/src/components/AuthWrapper.tsx (redirect logic)
    - frontend/src/lib/auth-service.ts (email field, dual accountId handling)
    - frontend/src/hooks/useAuth.tsx (email in state)
    - backend/src/api/auth.ts (accountId + email in session response)
    - backend/src/auth/types.ts (email in UserSession/CreateSessionInput)
    - backend/src/auth/session-store.ts (email from auth_users join)
    - backend/src/auth/passkey-service.ts (email in createSession)
    - backend/src/auth/magic-link-service.ts (email in createSession)
decisions:
  - type: fix
    title: "@simplewebauthn/browser v13 upgrade for API compatibility"
    rationale: "v10 API differs from v13; code was written for v13 syntax"
  - type: pattern
    title: "PRF extension type assertions"
    rationale: "WebAuthn PRF extension not in TypeScript definitions yet"
  - type: pattern
    title: "Session join for email retrieval"
    rationale: "Avoid schema migration by joining auth_users on session fetch"
metrics:
  duration: 7 min
  completed: 2026-02-01
---

# Phase 1.2 Plan 10: UAT Gap Closure Summary

Fixes all 4 UAT gaps from Phase 1.2 testing to make passkey authentication fully functional.

## One-liner

Fixed passkey TypeError via v13 upgrade, added RegisterPage, enabled protected route redirect, and propagated email in user context.

## Tasks Completed

### Task 1: Upgrade @simplewebauthn/browser to v13 (BLOCKER FIX)

- **Commit:** `bad6f9a`
- **Changes:**
  - Updated package.json from `^10.0.0` to `^13.2.2`
  - Fixed PRF extension type assertions in passkey.ts
  - Converted PRF ArrayBuffer to base64url string
- **Impact:** Passkey registration/authentication no longer throws TypeError

### Task 2: Create RegisterPage component

- **Commit:** `2bd12d6`
- **Changes:**
  - Created RegisterPage.tsx with passkey registration flow
  - Created RegisterPage.css with military-themed styling
  - Added `/register` route to App.tsx
- **Impact:** "Create an account" link from LoginPage now works

### Task 3: Add protected route redirect in AuthWrapper

- **Commit:** `28316ed`
- **Changes:**
  - Added Navigate and useLocation imports from react-router-dom
  - Added redirect to /login when !isLoading && !isAuthenticated
  - Preserves original location in state for potential redirect back
- **Impact:** Unauthenticated users redirected to /login instead of infinite loading

### Task 4: Fix user context field mapping

- **Commit:** `539d74b`
- **Changes:**
  - Backend returns both `accountId` and `nearAccountId` from /session
  - Backend includes `email` in session response
  - Session store joins with auth_users to get email
  - Frontend handles both field names for backward compatibility
  - useAuth hook exposes email in state
  - AuthWrapper uses email from useAuth
- **Impact:** User context shows NEAR account ID and email after authentication

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] PRF extension type errors**

- **Found during:** Task 1
- **Issue:** TypeScript errors on `credential.clientExtensionResults?.prf`
- **Fix:** Added type assertions for PRF extension which is not in TS definitions
- **Files modified:** frontend/src/lib/passkey.ts
- **Commit:** bad6f9a

**2. [Rule 1 - Bug] PRF output type mismatch**

- **Found during:** Task 1
- **Issue:** prfOutput was ArrayBuffer, expected string
- **Fix:** Convert ArrayBuffer to base64url string
- **Files modified:** frontend/src/lib/passkey.ts
- **Commit:** bad6f9a

**3. [Rule 2 - Missing Critical] Session missing email**

- **Found during:** Task 4
- **Issue:** Backend session had no email for UI display
- **Fix:** Join session store with auth_users table to retrieve email
- **Files modified:** backend/src/auth/types.ts, backend/src/auth/session-store.ts, backend/src/auth/passkey-service.ts, backend/src/auth/magic-link-service.ts
- **Commit:** 539d74b

## Verification Results

| Test | Result |
|------|--------|
| @simplewebauthn/browser ^13.2.2 in package.json | PASS |
| RegisterPage.tsx > 80 lines | PASS (102 lines) |
| /register route in App.tsx | PASS |
| Navigate redirect in AuthWrapper | PASS |
| nearAccountId field handling in auth-service | PASS |

## Notes

- Pre-existing TypeScript errors in mission components (MissionDetail, MissionMap, BulkImporter) are outside this plan's scope
- These errors do not affect the passkey authentication flow
- The LoginPage already has "Create an account" link pointing to /register (line 98)

## Next Phase Readiness

Phase 1.2 UAT gaps are resolved. All passkey authentication flows should now work:
- New user registration via /register
- Passkey login without TypeError
- Protected routes redirect properly
- User context displays NEAR account ID and email

Remaining TypeScript errors are in Phase 4.4 mission components and should be addressed in a separate maintenance plan.
