---
status: complete
phase: 1.2-passkey-near-implicit-auth
source: Post-1.2-12 fix verification (PRF ArrayBuffer, React hooks)
started: 2026-02-01T20:00:00Z
completed: 2026-02-03T11:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Passkey Authentication (PRF Fix Verification)
expected: Sign in with existing passkey - completes without PRF ArrayBuffer TypeError
result: pass
notes: Authentication successful. Also fixed credential ID lookup (legacy base64url storage), JSONB double-parsing, and updateLastUsed SQL typo discovered during testing.

### 2. Protected Route Redirect (React Hooks Fix Verification)
expected: Clear localStorage, navigate to /strategic - immediately redirects to /login (no React hooks error or infinite loading)
result: pass

### 3. NEAR Implicit Account (Account ID Verification)
expected: After authentication, user dropdown shows 64-character hex account ID (e.g., "a1b2c3d4...") NOT named account (e.g., "bastion-xxx.testnet")
result: pass
notes: User confirmed seeing "ad8a86bd8b696ca73841b9d64575a2c4d09601f6037c045ed0412f012bcd3df6" - correct 64-char hex implicit account format.

## Summary

total: 3
passed: 3
issues: 0
pending: 0
skipped: 0

## Gaps (New Requirements Identified)

- truth: "Implicit NEAR accounts are funded and active on-chain"
  status: new-requirement
  reason: "Implicit accounts exist as valid IDs but need funding (minimum NEAR transfer) to become active on testnet/mainnet"
  severity: enhancement
  recommendation: "Add as Phase 1.3 or separate task - implement faucet/funding mechanism during registration"

## Fixes Applied During UAT

1. **JSONB double-parsing** (passkey-store.ts): Removed JSON.parse() on JSONB transports column already parsed by pg driver
2. **Credential ID lookup** (passkey-store.ts): Added fallback to handle legacy credentials stored as base64url ASCII bytes
3. **Credential ID storage** (passkey-service.ts): Added detection for string vs Uint8Array credential.id
4. **SQL typo** (passkey-store.ts): Fixed `$2` → `$1` in updateLastUsed query

## Previous Issues (Fixed in 1.2-11/1.2-12)

- PRF extension ArrayBuffer serialization error → Fixed with base64url encoding (1.2-11)
- Protected route redirect race condition → Fixed by skipping overlay (1.2-11)
- Named account instead of implicit account → Fixed with SHA256-based derivation (1.2-11)
- PRF base64url to ArrayBuffer decoding → Fixed in passkey.ts (1.2-12, commit 642aa93)
- React hooks order violation → Fixed in AuthWrapper.tsx (1.2-12, commit 728b24b)
