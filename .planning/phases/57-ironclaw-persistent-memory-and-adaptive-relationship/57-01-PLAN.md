---
phase: 57-ironclaw-persistent-memory-and-adaptive-relationship
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/src/ironclaw/ironclaw-memory-types.ts
  - backend/src/ironclaw/ironclaw-memory-store.ts
  - backend/src/ironclaw/ironclaw-memory-store.test.ts
  - backend/src/db/migrations/043-ironclaw-user-memory.sql
  - backend/src/db/migrations/044-ironclaw-context-memory.sql
autonomous: true
requirements:
  - MEM-01
  - MEM-02
  - MEM-03
  - MEM-04
  - MEM-07
  - MEM-10

must_haves:
  truths:
    - "User memory can be created, read, updated with upsert semantics keyed by (user_did, memory_key)"
    - "Context memory can be created, read, updated with upsert semantics keyed by (problem_set_id, memory_key)"
    - "Expired memories are never returned by getActiveMemories()"
    - "User A's memory is never returned when querying for User B"
    - "Interaction outcomes are recorded with user_did, problem_set_id, outcome_type, and context"
  artifacts:
    - path: "backend/src/ironclaw/ironclaw-memory-types.ts"
      provides: "Type definitions for memory system — UserMemoryEntry, ContextMemoryEntry, InteractionOutcome, AdaptivePreferences, memory key constants"
      exports: ["UserMemoryEntry", "ContextMemoryEntry", "InteractionOutcome", "AdaptivePreferences", "MEMORY_KEYS", "OUTCOME_TYPES"]
    - path: "backend/src/ironclaw/ironclaw-memory-store.ts"
      provides: "IronclawUserMemoryStore and IronclawContextMemoryStore with CRUD + outcome recording"
      exports: ["ironclawUserMemoryStore", "ironclawContextMemoryStore", "ironclawOutcomeStore"]
    - path: "backend/src/db/migrations/043-ironclaw-user-memory.sql"
      provides: "ironclaw_user_memory table with (user_did, memory_key) UNIQUE constraint"
      contains: "CREATE TABLE IF NOT EXISTS ironclaw_user_memory"
    - path: "backend/src/db/migrations/044-ironclaw-context-memory.sql"
      provides: "ironclaw_context_memory + ironclaw_interaction_outcomes tables"
      contains: "CREATE TABLE IF NOT EXISTS ironclaw_context_memory"
    - path: "backend/src/ironclaw/ironclaw-memory-store.test.ts"
      provides: "Unit tests for all store operations"
  key_links:
    - from: "backend/src/ironclaw/ironclaw-memory-store.ts"
      to: "backend/src/lib/database.ts"
      via: "getPool() import"
      pattern: "import.*getPool.*from.*database"
    - from: "backend/src/ironclaw/ironclaw-memory-store.ts"
      to: "backend/src/ironclaw/ironclaw-memory-types.ts"
      via: "type imports"
      pattern: "import.*from.*ironclaw-memory-types"
---

<objective>
Create the foundational type definitions, database migrations, and persistence stores for Ironclaw's dual-scope memory system (user memory + context memory) plus interaction outcome tracking.

Purpose: All subsequent plans depend on these stores and types. This is the data layer that enables Ironclaw to persist memories and track interaction outcomes.
Output: Two migration SQL files, a types file, a store file with three singleton exports, and passing unit tests.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-ironclaw-persistent-memory-and-adaptive-relationship/57-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor MUST follow -->

From backend/src/ironclaw/ironclaw-store.ts (singleton pattern):
```typescript
import { getPool } from '../lib/database.js';

export class IronclawStore {
  async ensureTable(): Promise<void> {
    const pool = getPool();
    await pool.query(`CREATE TABLE IF NOT EXISTS ...`);
  }
  // ... methods using getPool() for each query
}

export const ironclawStore = new IronclawStore();
```

From backend/src/ironclaw/ironclaw-types.ts (TTL pattern):
```typescript
export const TRUST_TTL_DAYS = 30;
```

From backend/src/db/migrations/035-agent-memory.sql (migration pattern):
```sql
CREATE TABLE IF NOT EXISTS agent_memory (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  ...
);
CREATE INDEX IF NOT EXISTS idx_... ON agent_memory (...);
```

From backend/src/ironclaw/ironclaw-store.ts (upsert pattern -- trust_preferences):
```sql
INSERT INTO ironclaw_trust_preferences (...)
VALUES ($1, $2, $3, ...)
ON CONFLICT (user_did, problem_set_id, action_type) DO UPDATE SET
  decision = EXCLUDED.decision,
  updated_at = NOW(),
  expires_at = NOW() + INTERVAL '...'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Types, migrations, and store with tests (TDD)</name>
  <files>
    backend/src/ironclaw/ironclaw-memory-types.ts,
    backend/src/db/migrations/043-ironclaw-user-memory.sql,
    backend/src/db/migrations/044-ironclaw-context-memory.sql,
    backend/src/ironclaw/ironclaw-memory-store.ts,
    backend/src/ironclaw/ironclaw-memory-store.test.ts
  </files>
  <behavior>
    - MEM-01: setUserMemory('did:user:a', 'working_style', {draft_preference:'draft-first'}) creates a new row; getActiveMemories('did:user:a') returns it
    - MEM-02: calling setUserMemory with same (user_did, memory_key) updates memory_value, refreshes expires_at, does NOT create duplicate row
    - MEM-03: a memory with expires_at in the past is NOT returned by getActiveMemories()
    - MEM-04: setContextMemory('ps-1', 'decisions_made', {...}) creates row; getActiveMemories('ps-1') returns it; upsert on same key updates
    - MEM-07: recordOutcome('did:user:a', 'ps-1', 'suggestion_accepted', {suggestion_type:'coa'}) writes a row; getOutcomes returns it
    - MEM-10: setUserMemory for 'did:user:a' then getActiveMemories('did:user:b') returns empty array (no cross-user leakage)
    - deleteUserMemory('did:user:a', 'working_style') removes that specific memory
    - deleteAllUserMemories('did:user:a') removes all memories for that user
  </behavior>
  <action>
    **RED phase -- write tests first:**

    1. Create `ironclaw-memory-types.ts` with type definitions:
       - `UserMemoryEntry`: { id, user_did, memory_key, memory_value (Record<string,unknown>), confidence, source ('inferred'|'explicit'), created_at, updated_at, expires_at }
       - `ContextMemoryEntry`: { id, problem_set_id, memory_key, memory_value, session_count, created_at, updated_at, expires_at }
       - `InteractionOutcome`: { id, user_did, problem_set_id (nullable), outcome_type, context (Record<string,unknown>|null), created_at }
       - `AdaptivePreferences`: { proactivityLevel: 'low'|'medium'|'high', critiqueFrequency: 'low'|'medium'|'high', prefersDraftFirst: boolean }
       - Constants: `MEMORY_KEYS` = { WORKING_STYLE: 'working_style', CRITIQUE_TOLERANCE: 'critique_tolerance', DOMAIN_EXPERTISE: 'domain_expertise', COMMUNICATION_STYLE: 'communication_style', ADAPTIVE_BEHAVIOR: 'adaptive_behavior' }
       - Constants: `OUTCOME_TYPES` = { SUGGESTION_ACCEPTED, SUGGESTION_REJECTED, CORRECTION_MADE, QUESTION_ASKED, EDIT_POST_CRITIQUE, DRAFT_ACCEPTED, BLANK_PAGE_PREFERRED }
       - Constants: `USER_MEMORY_TTL_DAYS = 90`, `CONTEXT_MEMORY_TTL_DAYS = 180`

    2. Create `ironclaw-memory-store.test.ts` with tests for all behaviors listed above. Mock `getPool()` to return a mock pool that captures queries and returns canned rows. Use vitest `vi.mock()` for `../lib/database.js`. Test that:
       - setUserMemory calls INSERT...ON CONFLICT with correct params
       - getActiveMemories filters by `expires_at > NOW()`
       - deleteUserMemory issues DELETE with correct WHERE
       - Cross-user isolation: mock returns rows only when user_did matches
       - recordOutcome inserts into ironclaw_interaction_outcomes
       - Context memory methods use problem_set_id not user_did

    3. Run tests -- they should FAIL (store not implemented yet).

    **GREEN phase -- implement store:**

    4. Create migrations:
       - `043-ironclaw-user-memory.sql`: CREATE TABLE ironclaw_user_memory with columns (id UUID PK default gen_random_uuid(), user_did TEXT NOT NULL, memory_key TEXT NOT NULL, memory_value JSONB NOT NULL, confidence NUMERIC(4,3) DEFAULT 0.5, source TEXT NOT NULL DEFAULT 'inferred', created_at TIMESTAMPTZ DEFAULT NOW(), updated_at TIMESTAMPTZ DEFAULT NOW(), expires_at TIMESTAMPTZ DEFAULT NOW() + INTERVAL '90 days', UNIQUE(user_did, memory_key)). Indexes on user_did and expires_at.
       - `044-ironclaw-context-memory.sql`: CREATE TABLE ironclaw_context_memory (id UUID PK, problem_set_id TEXT NOT NULL, memory_key TEXT NOT NULL, memory_value JSONB NOT NULL, session_count INT DEFAULT 1, created_at, updated_at, expires_at DEFAULT NOW() + '180 days', UNIQUE(problem_set_id, memory_key)). Index on problem_set_id. Also CREATE TABLE ironclaw_interaction_outcomes (id UUID PK, user_did TEXT NOT NULL, problem_set_id TEXT nullable, outcome_type TEXT NOT NULL, context JSONB, created_at). Indexes on (user_did, created_at) and (outcome_type, created_at).

    5. Create `ironclaw-memory-store.ts` following the IronclawStore singleton pattern exactly:
       - `IronclawUserMemoryStore` class with: ensureTable(), setUserMemory(userDid, memoryKey, value, source, confidence), getActiveMemories(userDid), getUserMemory(userDid, memoryKey), deleteUserMemory(userDid, memoryKey), deleteAllUserMemories(userDid). Every query WHERE clause MUST include user_did. No problemSetId parameter on user memory methods (privacy guard).
       - `IronclawContextMemoryStore` class with: ensureTable(), setContextMemory(problemSetId, memoryKey, value), getActiveMemories(problemSetId), deleteContextMemory(problemSetId, memoryKey). Every query WHERE clause MUST include problem_set_id.
       - `IronclawOutcomeStore` class with: ensureTable(), recordOutcome(userDid, problemSetId, outcomeType, context), getOutcomes(userDid, options?: {days?: number, limit?: number}), getOutcomeCounts(userDid, days: number).
       - Export singletons: ironclawUserMemoryStore, ironclawContextMemoryStore, ironclawOutcomeStore.
       - Use row mapper functions (rowToUserMemory, rowToContextMemory, rowToOutcome) like ironclaw-store.ts pattern.

    6. Run tests -- they should PASS.

    **REFACTOR:** Clean up any redundancy, ensure consistent error handling with try/catch logging pattern from IronclawStore.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr/backend && bash -lc 'npx vitest run src/ironclaw/ironclaw-memory-store.test.ts --reporter=verbose'</automated>
  </verify>
  <done>
    - ironclaw-memory-types.ts exports all type definitions and constants
    - Two migration SQL files exist with correct schemas
    - ironclaw-memory-store.ts exports three singleton stores
    - All unit tests pass covering upsert, TTL filtering, cross-user isolation, outcome recording, and deletion
  </done>
</task>

</tasks>

<verification>
- `cd /home/vitalpointai/projects/ssr/backend && bash -lc 'npx vitest run src/ironclaw/ironclaw-memory-store.test.ts --reporter=verbose'` -- all tests green
- `bash -lc 'cd /home/vitalpointai/projects/ssr/backend && npx tsc --noEmit'` -- no TypeScript errors
</verification>

<success_criteria>
- Three store singletons exported and importable
- Types cover all memory and outcome data structures
- Migration SQL is idempotent (IF NOT EXISTS)
- Unit tests cover MEM-01 through MEM-04, MEM-07, MEM-10
- No cross-user memory leakage possible by API design (user memory methods take userDid only)
</success_criteria>

<output>
After completion, create `.planning/phases/57-ironclaw-persistent-memory-and-adaptive-relationship/57-01-SUMMARY.md`
</output>
