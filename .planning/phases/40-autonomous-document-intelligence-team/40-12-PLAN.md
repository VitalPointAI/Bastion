---
phase: 40-autonomous-document-intelligence-team
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/tabs/UnderstandTab.tsx
  - frontend/src/components/doc-intelligence/DocIntelligencePanel.tsx
autonomous: true
gap_closure: true
requirements:
  - DOCTEAM-01
  - DOCTEAM-10
  - DOCTEAM-12

must_haves:
  truths:
    - "Understand tab renders doc-intelligence components accessible to users"
    - "Knowledge graph (StrategicDashboard) remains the centered focal element"
    - "Mission control, scoping interview, intelligence reports, and NATO ratings are accessible from the Understand tab"
  artifacts:
    - path: "frontend/src/components/doc-intelligence/DocIntelligencePanel.tsx"
      provides: "Composite panel wiring all doc-intelligence components"
      exports: ["DocIntelligencePanel"]
    - path: "frontend/src/components/tabs/UnderstandTab.tsx"
      provides: "Updated tab with doc-intelligence sidebar entry"
      contains: "DocIntelligencePanel"
  key_links:
    - from: "frontend/src/components/tabs/UnderstandTab.tsx"
      to: "frontend/src/components/doc-intelligence/DocIntelligencePanel.tsx"
      via: "import and conditional render"
      pattern: "DocIntelligencePanel"
    - from: "frontend/src/components/doc-intelligence/DocIntelligencePanel.tsx"
      to: "frontend/src/components/doc-intelligence/MissionControl.tsx"
      via: "import and render"
      pattern: "MissionControl"
    - from: "frontend/src/components/doc-intelligence/DocIntelligencePanel.tsx"
      to: "frontend/src/components/doc-intelligence/ScopingInterview.tsx"
      via: "import and render"
      pattern: "ScopingInterview"
    - from: "frontend/src/components/doc-intelligence/DocIntelligencePanel.tsx"
      to: "frontend/src/components/doc-intelligence/IntelligenceReport.tsx"
      via: "import and render"
      pattern: "IntelligenceReportList"
---

<objective>
Wire all 5 orphaned doc-intelligence frontend components into the Understand tab so users can access the document intelligence UI with the knowledge graph as the centered focal element.

Purpose: Without this wiring, 2,040 lines of doc-intelligence UI are invisible to users. The Understand tab should be the primary home for document upload, processing, reports, and knowledge graph.
Output: DocIntelligencePanel composite component; updated UnderstandTab with "Document Intelligence" sidebar entry.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-autonomous-document-intelligence-team/40-CONTEXT.md
@.planning/phases/40-autonomous-document-intelligence-team/40-VERIFICATION.md

<interfaces>
<!-- From frontend/src/components/tabs/UnderstandTab.tsx -->
type UnderstandView = 'strategic-docs' | 'subscriptions' | 'ai-context-preview' | 'training-packages';
interface UnderstandTabProps { problemSetId: string; }
// Uses TabLayout with SidebarItem[] for navigation

<!-- From doc-intelligence components -->
// MissionControl: { problemSetId: string; processingState: DocProcessingState; onApproveFlag?: () => void; onRejectFlag?: () => void; }
// ScopingInterview: { problemSetId: string; onComplete: (context: ProblemSetContext) => void; onClose: () => void; }
// IntelligenceReportList: { reports: DocumentIntelligenceReport[]; onRatingOverride: (documentId: string, newRating: Partial<NATORating>, reason: string) => void; }
// ProcessingFeed: { events: ProcessingEvent[]; maxHeight?: string; }
// NATORatingPanel: { rating: NATORating; documentId: string; onOverride?: (documentId: string, newRating: Partial<NATORating>, reason: string) => void; }

<!-- From frontend/src/hooks/useDocProcessing.ts -->
// export function useDocProcessing(processingId: string | null): { state: DocProcessingState; events: ProcessingEvent[]; report: DocumentIntelligenceReport | null; }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DocIntelligencePanel composite component</name>
  <files>frontend/src/components/doc-intelligence/DocIntelligencePanel.tsx</files>
  <action>
Create a new composite component that orchestrates all doc-intelligence UI in one panel. This component will be rendered as a sidebar view in the Understand tab.

**DocIntelligencePanel props:** `{ problemSetId: string }`

**Layout structure** (vertical stack):
1. **Scoping Interview section** -- Button to open scoping interview modal/overlay. If context already exists (fetch from `/api/doc-intelligence/context/{problemSetId}` on mount), show a small summary badge "Context set" with a "Re-run" button. When opened, render `<ScopingInterview>` in a modal overlay.

2. **Document Upload trigger** -- A styled file upload area (drag-and-drop or click) that POSTs to `/api/doc-intelligence/upload` with the file and problemSetId. On upload, capture the returned processingId and pass it to useDocProcessing hook.

3. **Mission Control section** -- Render `<MissionControl>` when there is an active processingId. Pass the processingState from useDocProcessing. This shows the NASA mission control dashboard with specialist agent cards.

4. **Processing Feed** -- Render `<ProcessingFeed>` below mission control with events from useDocProcessing.

5. **Intelligence Reports section** -- Fetch reports from `/api/doc-intelligence/reports/{problemSetId}` on mount and when processing completes. Render `<IntelligenceReportList>` with the fetched reports. The onRatingOverride callback should PATCH `/api/doc-intelligence/reports/{documentId}/rating`.

**State management:**
- `const [processingId, setProcessingId] = useState<string | null>(null);`
- `const { state, events, report } = useDocProcessing(processingId);`
- `const [reports, setReports] = useState<DocumentIntelligenceReport[]>([]);`
- `const [hasContext, setHasContext] = useState(false);`
- `const [showInterview, setShowInterview] = useState(false);`

**Styling:** Use inline styles consistent with the existing UnderstandTab patterns. Dark-themed section headers for the mission control area to match the "mission control dashboard aesthetic" from CONTEXT.md decisions.

**API calls:** Use the API_BASE pattern from useDocProcessing (`import.meta.env.VITE_API_URL || ''`). All API paths under `/api/doc-intelligence/`.

**Important:** Do NOT duplicate any logic from the existing components -- just compose them. The upload handler is the main new code; everything else is wiring.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && grep -n "MissionControl\|ScopingInterview\|IntelligenceReportList\|ProcessingFeed\|useDocProcessing" frontend/src/components/doc-intelligence/DocIntelligencePanel.tsx && echo "All components wired"</automated>
  </verify>
  <done>DocIntelligencePanel.tsx exists, imports and renders all 5 doc-intelligence components (MissionControl, ScopingInterview, IntelligenceReportList, ProcessingFeed, NATORatingPanel via reports), uses useDocProcessing hook, and includes document upload trigger.</done>
</task>

<task type="auto">
  <name>Task 2: Wire DocIntelligencePanel into UnderstandTab sidebar</name>
  <files>frontend/src/components/tabs/UnderstandTab.tsx</files>
  <action>
Modify UnderstandTab.tsx to add a "Document Intelligence" sidebar entry and render DocIntelligencePanel.

1. **Add import:** `import { DocIntelligencePanel } from '../doc-intelligence/DocIntelligencePanel.js';`

2. **Extend the view type:** Change `type UnderstandView` to include `'doc-intelligence'`:
   `type UnderstandView = 'strategic-docs' | 'doc-intelligence' | 'subscriptions' | 'ai-context-preview' | 'training-packages';`

3. **Add sidebar item:** Insert a new entry in the `items` array after 'strategic-docs':
   ```
   {
     id: 'doc-intelligence',
     label: 'Document Intelligence',
     tooltip: 'Autonomous document processing, analysis, and intelligence reports',
   },
   ```

4. **Add render block:** After the strategic-docs render block (after line 86), add:
   ```
   {selectedView === 'doc-intelligence' && (
     <DocIntelligencePanel problemSetId={problemSetId} />
   )}
   ```

5. **Keep strategic-docs as default** -- the knowledge graph (StrategicDashboard) remains the default centered view when opening the Understand tab. Document Intelligence is a sidebar navigation option alongside it, per the CONTEXT.md decision that "knowledge graph is the centerpiece" and doc-intelligence "surrounds it peripherally."

Do NOT change the default selectedView. Do NOT move or remove any existing views.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && grep -n "doc-intelligence\|DocIntelligencePanel" frontend/src/components/tabs/UnderstandTab.tsx && echo "Wiring confirmed"</automated>
  </verify>
  <done>UnderstandTab.tsx imports DocIntelligencePanel, adds "Document Intelligence" sidebar entry, and renders the panel when selected. Knowledge graph (strategic-docs) remains the default centered view. All 5 previously-orphaned components are now accessible to users through the Understand tab.</done>
</task>

</tasks>

<verification>
- DocIntelligencePanel.tsx exists and imports all 5 doc-intelligence components
- UnderstandTab.tsx imports and renders DocIntelligencePanel
- grep confirms no doc-intelligence components remain orphaned
- TypeScript compilation: cd frontend && npx tsc --noEmit 2>&1 | head -20
</verification>

<success_criteria>
- All 5 doc-intelligence components reachable from the Understand tab (gap 1 closed)
- Knowledge graph remains centered default view, doc-intelligence accessible via sidebar (gap 3 closed)
- Users can access scoping interview, upload documents, see mission control, view reports from Understand tab
</success_criteria>

<output>
After completion, create `.planning/phases/40-autonomous-document-intelligence-team/40-12-SUMMARY.md`
</output>
