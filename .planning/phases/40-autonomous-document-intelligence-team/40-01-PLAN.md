---
phase: 40-autonomous-document-intelligence-team
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/doc-intelligence/types.ts
  - backend/src/doc-intelligence/schemas.ts
  - backend/src/doc-intelligence/specialist-base.ts
  - backend/src/doc-intelligence/source-registry/nato-ratings.ts
  - backend/migrations/040_doc_intelligence_tables.sql
autonomous: true
requirements:
  - DOCTEAM-01
  - DOCTEAM-02
  - DOCTEAM-10

must_haves:
  truths:
    - "All specialist agent types have defined input/output Zod schemas"
    - "Database tables exist for problem_set_context, entity_provenance, source_registry, briefing_access_log"
    - "NATO Admiralty System ratings (A-F / 1-6) have type-safe definitions with display labels"
    - "Specialist base class provides common LangGraph node creation pattern"
  artifacts:
    - path: "backend/src/doc-intelligence/types.ts"
      provides: "TypeScript types for all specialist I/O, document types taxonomy, processing state"
      exports: ["DocumentType", "SpecialistId", "TriageDecision", "DocumentIntelligenceReport", "ProcessingState"]
    - path: "backend/src/doc-intelligence/schemas.ts"
      provides: "Zod schemas for ProblemSetContext, specialist outputs, NATO ratings, report assembly"
      exports: ["ProblemSetContextSchema", "TriageDecisionSchema", "FactSchema", "PerspectiveAnalysisSchema", "DocumentIntelligenceReportSchema"]
    - path: "backend/src/doc-intelligence/specialist-base.ts"
      provides: "Base class wrapping LangGraphAgentWrapper for specialist agents"
      exports: ["SpecialistBase"]
    - path: "backend/src/doc-intelligence/source-registry/nato-ratings.ts"
      provides: "NATO STANAG 2511 rating types, labels, and helper functions"
      exports: ["NATORatingSchema", "RELIABILITY_LABELS", "CREDIBILITY_LABELS", "NATORating"]
    - path: "backend/migrations/040_doc_intelligence_tables.sql"
      provides: "Database schema for problem_set_context, entity_provenance, source_registry, briefing_access_log"
  key_links:
    - from: "backend/src/doc-intelligence/schemas.ts"
      to: "backend/src/doc-intelligence/types.ts"
      via: "z.infer type exports"
      pattern: "z\\.infer<typeof"
    - from: "backend/src/doc-intelligence/specialist-base.ts"
      to: "backend/src/orchestration/agent-wrapper.ts"
      via: "extends LangGraphAgentWrapper pattern"
      pattern: "LangGraphAgentWrapper"
---

<objective>
Establish the foundation layer for the document intelligence team: all shared types, Zod schemas, database tables, NATO rating definitions, and the specialist base class that every subsequent plan builds on.

Purpose: Every specialist agent, the orchestrator, and the UI depend on shared type contracts and database tables. Defining these first eliminates ambiguity and prevents conflicts across parallel plans.
Output: Type definitions, Zod schemas, SQL migration, specialist base class, NATO rating module.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-autonomous-document-intelligence-team/40-CONTEXT.md
@.planning/phases/40-autonomous-document-intelligence-team/40-RESEARCH.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From backend/src/orchestration/agent-wrapper.ts:
```typescript
export interface AgentWrapperConfig {
  agentId: string;
  name: string;
  description: string;
  clearance: string;
  tools: any[];
  systemPrompt: string;
}
export class LangGraphAgentWrapper {
  createNode(): (state: any) => Promise<any>;
}
```

From backend/src/graph/construction/graph-builder.ts:
```typescript
export interface GraphBuildOptions {
  sourceDocumentId?: string;
  problemSetId: string;
  onEntityCreated?: (event: GraphEntityEvent) => void;
  onProgress?: (stage: string, detail: string) => void;
}
export interface GraphEntityEvent {
  entityId: string;
  entityType: string;
  label: string;
  properties: Record<string, any>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types, Zod schemas, and NATO rating module</name>
  <files>
    backend/src/doc-intelligence/types.ts,
    backend/src/doc-intelligence/schemas.ts,
    backend/src/doc-intelligence/source-registry/nato-ratings.ts
  </files>
  <action>
Create the `backend/src/doc-intelligence/` directory structure as specified in RESEARCH.md.

**types.ts** - Define all TypeScript types:
- `DocumentType` enum/const: INTEL_ESTIMATE, CONOP, POLICY_PAPER, NEWS_ARTICLE, ACADEMIC_RESEARCH, MILITARY_ORDER, DIPLOMATIC_CABLE, OSINT_REPORT, OTHER (use const object pattern per project convention)
- `SpecialistId` const: format-converter, document-classifier, fact-extractor, objective-extractor, perspective-analyst, cross-doc-linker, bias-identifier, quality-assessor, trust-agent, researcher
- `TriageDecision` interface: documentType, relevanceScore (0-1), specialists (SpecialistId[]), reasoning
- `SpecialistResult` interface: specialistId, status (success/error/skipped), output (generic), duration, error?
- `ProcessingState` interface: documentId, problemSetId, triageDecision, specialistResults Map, currentPhase, startedAt, completedAt?
- `DocumentIntelligenceReport` interface: documentId, problemSetId, triage, facts[], perspectives[], biasFindings[], qualityRating (NATORating), crossDocLinks[], summary
- `PerspectiveAnalysis` interface: perspective (friendly/adversary/neutral/partner), implications[], opportunities[], threats[], unknowns[]
- `ExtractedFact` interface: claim, type (entity/date/location/quantity/assertion/capability), confidence, sourceReference {page, paragraph, quote}, entities[], temporalContext?, geospatialContext?
- `BiasAssessment` interface: biasType, severity, evidence, recommendation
- `CrossDocLink` interface: sourceDocId, targetDocId, linkType (corroborates/contradicts/extends/references), strength, evidence

**schemas.ts** - Zod schemas for all above types (mirroring types.ts but as runtime validation):
- `ProblemSetContextSchema` exactly as in RESEARCH.md code example (geographic scope, temporal range, actor focus, core problem, classification ceiling, echelon, standing requirements)
- `TriageDecisionSchema`, `ExtractedFactSchema`, `PerspectiveAnalysisSchema`, `BiasAssessmentSchema`, `CrossDocLinkSchema`, `DocumentIntelligenceReportSchema`
- Export `z.infer` types alongside manual types for runtime-validated paths

**nato-ratings.ts** - NATO Admiralty System (STANAG 2511):
- Use the exact Zod schema from RESEARCH.md code example: SourceReliabilitySchema (A-F enum), InformationCredibilitySchema (1-6 int), NATORatingSchema (composite with override tracking)
- RELIABILITY_LABELS and CREDIBILITY_LABELS record objects
- `formatNATORating(rating: NATORating): string` helper that returns "Source: B - Usually Reliable | Info: 3 - Possibly True" format
- `isHumanReviewRequired(rating: NATORating): boolean` - returns true if reliability D/E/F or credibility 4/5/6
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit backend/src/doc-intelligence/types.ts backend/src/doc-intelligence/schemas.ts backend/src/doc-intelligence/source-registry/nato-ratings.ts 2>&1 | head -30</automated>
  </verify>
  <done>All types compile without errors. Zod schemas export inferred types. NATO ratings have A-F/1-6 definitions with labels and helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Database migration and specialist base class</name>
  <files>
    backend/migrations/040_doc_intelligence_tables.sql,
    backend/src/doc-intelligence/specialist-base.ts
  </files>
  <action>
**040_doc_intelligence_tables.sql** - Create all tables needed by the document intelligence team. Use the SQL from RESEARCH.md as the foundation, plus additional tables from PHASE-DESIGN.md:

1. `problem_set_context` - scoping interview output (id, problem_set_id UNIQUE, context_data JSONB, version, created_at, updated_at)
2. `entity_provenance` - per-source entity tracking (id, entity_id, entity_type, source_document_id, extracted_by, extracted_at, is_revoked, revoked_at, revoked_by, UNIQUE(entity_id, source_document_id))
3. `source_registry` - known source trust scores (id, source_name, source_type, default_reliability A-F, trust_notes, is_blocked, created_at, updated_at)
4. `briefing_access_log` - per-user/agent last-access tracking (id, problem_set_id, accessed_by, accessed_at, graph_snapshot_hash)
5. `document_intelligence_reports` - orchestrator output per document (id, document_id, problem_set_id, triage_decision JSONB, report_data JSONB, nato_rating JSONB, processing_duration_ms, created_at)
6. ALTER TABLE `strategic_documents` ADD COLUMNS: nato_reliability, nato_credibility, trust_status (trusted/pending/flagged/revoked), trust_assessed_by, trust_assessed_at

Add indexes per RESEARCH.md: idx_provenance_entity, idx_provenance_source, idx_provenance_revoked, idx_briefing_access. Add idx_reports_document, idx_reports_problem_set.

**specialist-base.ts** - Abstract base for all specialist agents:
- Import `LangGraphAgentWrapper` and `AgentWrapperConfig` from `../../orchestration/agent-wrapper.js`
- Import relevant types from `./types.js` and schemas from `./schemas.js`
- `SpecialistBase` class that:
  - Accepts config: { specialistId: SpecialistId, name: string, description: string, systemPrompt: string, tools: any[], clearance: string }
  - Creates internal `LangGraphAgentWrapper` instance
  - Exposes `createNode()` method for LangGraph StateGraph registration
  - Provides `validateOutput(output: unknown, schema: ZodSchema)` helper using Zod safeParse
  - Provides `reportProgress(stage: string, detail: string, callback?: Function)` helper for SSE streaming
  - Stores problemSetContext reference for agents to access during processing
  - Has abstract `getSystemPrompt(context: ProblemSetContext): string` that subclasses override to inject problem set context into prompts
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit backend/src/doc-intelligence/specialist-base.ts 2>&1 | head -20</automated>
  </verify>
  <done>Migration SQL is syntactically valid with all 5 new tables + ALTER. SpecialistBase class compiles, wraps LangGraphAgentWrapper, and provides shared validation/progress helpers.</done>
</task>

</tasks>

<verification>
- All TypeScript files in `backend/src/doc-intelligence/` compile without errors
- Migration SQL parses correctly (no syntax errors)
- Types cover all 12 requirement areas (scoping, triage, extraction, perspectives, bias, quality, linking, research, briefing)
- NATO ratings match STANAG 2511 specification exactly
</verification>

<success_criteria>
- `backend/src/doc-intelligence/` directory exists with types.ts, schemas.ts, specialist-base.ts
- `backend/src/doc-intelligence/source-registry/nato-ratings.ts` exists with A-F/1-6 definitions
- `backend/migrations/040_doc_intelligence_tables.sql` exists with 5 new tables + 1 ALTER
- All files compile via `npx tsc --noEmit`
- Zod schemas export runtime validators for all specialist I/O contracts
</success_criteria>

<output>
After completion, create `.planning/phases/40-autonomous-document-intelligence-team/40-01-SUMMARY.md`
</output>
