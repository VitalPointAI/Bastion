---
phase: 40-autonomous-document-intelligence-team
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/index.ts
  - backend/src/doc-intelligence/specialists/researcher.ts
  - backend/src/doc-intelligence/web-search.ts
autonomous: true
gap_closure: true
requirements:
  - DOCTEAM-02
  - DOCTEAM-11

must_haves:
  truths:
    - "registerDocIntelligenceTeam() is called during application startup"
    - "Researcher specialist calls a real web search API when configured"
    - "Web search gracefully falls back to placeholder when no API key is set"
  artifacts:
    - path: "backend/src/index.ts"
      provides: "Team registration at startup"
      contains: "registerDocIntelligenceTeam"
    - path: "backend/src/doc-intelligence/web-search.ts"
      provides: "Pluggable web search provider"
      exports: ["performWebSearch"]
    - path: "backend/src/doc-intelligence/specialists/researcher.ts"
      provides: "Researcher using real web search"
      contains: "performWebSearch"
  key_links:
    - from: "backend/src/index.ts"
      to: "backend/src/doc-intelligence/team-setup.ts"
      via: "import and call registerDocIntelligenceTeam()"
      pattern: "registerDocIntelligenceTeam"
    - from: "backend/src/doc-intelligence/specialists/researcher.ts"
      to: "backend/src/doc-intelligence/web-search.ts"
      via: "import performWebSearch"
      pattern: "import.*web-search"
---

<objective>
Close two verification gaps: (1) registerDocIntelligenceTeam() is never called at startup, and (2) the researcher specialist returns placeholder web search results.

Purpose: Without team registration, the specialist team is invisible to TeamRegistry at runtime. Without real web search, autonomous OSINT research cannot function.
Output: Backend startup calls team registration; researcher uses Tavily API when TAVILY_API_KEY is set, falls back gracefully otherwise.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-autonomous-document-intelligence-team/40-VERIFICATION.md

<interfaces>
<!-- From backend/src/doc-intelligence/team-setup.ts -->
export async function registerDocIntelligenceTeam(): Promise<void>;

<!-- From backend/src/index.ts startup pattern (lines 335-405) -->
// Pattern: try { await someInit(); console.log('...'); } catch (error) { console.error('...', error); }

<!-- From backend/src/doc-intelligence/specialists/researcher.ts lines 507-516 -->
// performWebSearch is a private method returning { url, title, retrievalDate, snippet }[]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register doc-intelligence team at startup and create pluggable web search</name>
  <files>backend/src/index.ts, backend/src/doc-intelligence/web-search.ts, backend/src/doc-intelligence/specialists/researcher.ts</files>
  <action>
1. **backend/src/index.ts** -- Add import for registerDocIntelligenceTeam from './doc-intelligence/team-setup.js' at the top with other imports. Add a startup block after the validation scheduler registration (around line 397) following the established try/catch pattern:
```
try {
  const { registerDocIntelligenceTeam } = await import('./doc-intelligence/team-setup.js');
  await registerDocIntelligenceTeam();
  console.log('Document intelligence team registered');
} catch (error) {
  console.error('Failed to register document intelligence team:', error);
}
```
Use dynamic import (consistent with other late-init blocks like initAIWorkspace).

2. **backend/src/doc-intelligence/web-search.ts** -- Create a new pluggable web search module:
   - Export interface `WebSearchResult { url: string; title: string; snippet: string; retrievalDate: string; }`
   - Export async function `performWebSearch(query: string, maxResults?: number): Promise<WebSearchResult[]>`
   - Check for `process.env.TAVILY_API_KEY`; if set, call Tavily Search API via fetch (POST to `https://api.tavily.com/search` with `{ api_key, query, max_results, search_depth: 'basic' }`)
   - Parse Tavily response (results array with url, title, content fields) into WebSearchResult[]
   - If no API key, log a warning once and return placeholder results (preserving current fallback behavior)
   - Wrap the fetch in try/catch; on error, log and return empty array (graceful degradation)
   - Do NOT add tavily as an npm dependency -- use raw fetch (Node 18+ built-in)

3. **backend/src/doc-intelligence/specialists/researcher.ts** -- Replace the private `performWebSearch` method body (lines 507-516):
   - Import `performWebSearch` from '../web-search.js' at the top of the file
   - Replace the placeholder return with a call to the imported `performWebSearch(query)`
   - Remove the hardcoded example.com placeholder
   - Keep the audit trail INSERT above it (lines 498-505) as-is
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && grep -n "registerDocIntelligenceTeam" backend/src/index.ts && grep -n "performWebSearch" backend/src/doc-intelligence/web-search.ts && grep -n "from.*web-search" backend/src/doc-intelligence/specialists/researcher.ts && echo "All wiring confirmed"</automated>
  </verify>
  <done>registerDocIntelligenceTeam() is called during server startup in index.ts. Researcher specialist imports and uses the pluggable web search module. Web search works with Tavily when TAVILY_API_KEY is set, falls back gracefully otherwise.</done>
</task>

</tasks>

<verification>
- grep confirms registerDocIntelligenceTeam import and call in index.ts
- web-search.ts exists with performWebSearch export
- researcher.ts imports from web-search.ts instead of returning placeholders
- TypeScript compilation succeeds: cd backend && npx tsc --noEmit 2>&1 | head -20
</verification>

<success_criteria>
- registerDocIntelligenceTeam() called at startup (gap 2 closed)
- performWebSearch() calls Tavily API when configured (gap 4 closed)
- Graceful fallback when no API key is set
</success_criteria>

<output>
After completion, create `.planning/phases/40-autonomous-document-intelligence-team/40-11-SUMMARY.md`
</output>
