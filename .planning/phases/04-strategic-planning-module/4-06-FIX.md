---
phase: 04-strategic-planning-module
plan: 06-FIX
type: fix
---

<objective>
Fix 2 UAT blocker issues from plan 4-06.

Source: 4-06-ISSUES.md
Priority: 2 blockers, 0 major, 0 minor

The strategic API's `ensureTableExists()` function only initializes the `strategic_documents` table but fails to initialize tables for:
1. `risk_assessments` — used by risk assessment endpoints
2. `workflow_states` / `workflow_events` — used by workflow endpoints

Both modules have initialization functions (`initRiskAssessmentTable` and `WorkflowEngine.initialize()`) but they aren't called from the shared lazy init.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

**Issues being fixed:**
@.planning/phases/04-strategic-planning-module/4-06-ISSUES.md

**Original plan for reference:**
@.planning/phases/04-strategic-planning-module/4-06-PLAN.md

**Key files:**
@backend/src/api/strategic.ts (lines 44-51 — current ensureTableExists)
@backend/src/strategic/assessment/store.ts (initRiskAssessmentTable function)
@backend/src/strategic/workflows/engine.ts (WorkflowEngine.initialize method)
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add table initialization for risk_assessments and workflow tables</name>
  <files>backend/src/api/strategic.ts</files>
  <action>
Update the `ensureTableExists()` function (lines 44-51) to also initialize:
1. The risk_assessments table via `initRiskAssessmentTable()` from assessment module
2. The workflow tables via `workflowEngine.initialize()` from workflows module

Steps:
1. Import `initRiskAssessmentTable` from '../strategic/assessment/index.js' (already partially imported — add to existing import)
2. Update `ensureTableExists()` to call all three initialization functions:
   - `await initStrategicDocumentsTable();` (existing)
   - `await initRiskAssessmentTable();` (new)
   - `await workflowEngine.initialize();` (new — workflowEngine is already imported)

Note: The workflowEngine singleton already exists and has an `initialize()` method that creates both `workflow_states` and `workflow_events` tables. The `initRiskAssessmentTable` function is exported from the assessment index but not currently imported in strategic.ts.
  </action>
  <verify>
Run curl commands to verify both endpoints work:
```bash
curl -X GET "http://localhost:3001/api/strategic/risk/high-risk" -H "X-DID: did:near:testuser.testnet"
# Should return: {"count":0,"assessments":[]}

curl -X GET "http://localhost:3001/api/strategic/objectives/test-id/workflow" -H "X-DID: did:near:testuser.testnet"
# Should return: {"error":"No workflow found for this objective"} (404, not 500 table error)
```
  </verify>
  <done>
- GET /api/strategic/risk/high-risk returns 200 with empty assessments array (not 500 table error)
- GET /api/strategic/objectives/:id/workflow returns 404 "No workflow found" (not 500 table error)
- All existing strategic API tests continue to pass
  </done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `curl -X GET "http://localhost:3001/api/strategic/risk/high-risk" -H "X-DID: did:near:testuser.testnet"` returns 200 with assessments array
- [ ] `curl -X GET "http://localhost:3001/api/strategic/objectives/test-id/workflow" -H "X-DID: did:near:testuser.testnet"` returns 404 with "No workflow found" message
- [ ] Backend builds without TypeScript errors
- [ ] Existing strategic endpoints still work (GET /objectives, GET /documents)
</verification>

<success_criteria>
- All UAT issues from 4-06-ISSUES.md addressed
- Both blocker issues resolved (tables auto-created on first request)
- Ready for re-verification with /gsd:verify-work 4-06
</success_criteria>

<output>
After completion, create `.planning/phases/04-strategic-planning-module/4-06-FIX-SUMMARY.md`
</output>
