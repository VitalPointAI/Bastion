---
phase: 04-strategic-planning-module
plan: 4-12-FIX
type: fix
---

<objective>
Fix 2 UAT issues from plan 4-12.

Source: 4-12-ISSUES.md
Priority: 0 critical, 0 major, 2 minor
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

**Issues being fixed:**
@.planning/phases/04-strategic-planning-module/4-12-ISSUES.md

**Original plan for reference:**
@.planning/phases/04-strategic-planning-module/4-12-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Fix UAT-001: Integrate AgentBuilderWizard into Admin UI</name>
  <files>
    frontend/src/components/admin/AgentManagementPanel.tsx
  </files>
  <action>
Add a button to the AgentManagementPanel that opens the AgentBuilderWizard modal.

1. Import AgentBuilderWizard component at the top of the file
2. Add state for wizard visibility: `const [showWizard, setShowWizard] = useState(false);`
3. Add an "Agent Builder" button in the panel header alongside the tabs, styled with orange accent
4. When clicked, render the AgentBuilderWizard overlay component
5. Pass required props: userDID (from context or props), onClose, onAgentCreated
6. On agent created, refresh the agent list and close the wizard

Button styling should match the command-center aesthetic (orange accent for admin, gradient background).
  </action>
  <verify>
Build succeeds: `cd frontend && npm run build`
Navigate to Admin Dashboard -> Agents tab and see "Agent Builder" button that opens the wizard.
  </verify>
  <done>
AgentBuilderWizard accessible via button in Agents tab, 7-step wizard opens in modal overlay.
  </done>
</task>

<task type="auto">
  <name>Fix UAT-002: Apply dark theme styling to ReviewPanel</name>
  <files>
    frontend/src/components/strategic/ReviewPanel.css
  </files>
  <action>
Update ReviewPanel.css to use the dark theme CSS variables consistent with the rest of the app.

Key changes:
1. `.review-panel` background: Change from `#ffffff` to `var(--bg-elevated, #222230)` with gradient
2. Border: Change from `#e5e7eb` to `var(--border-color, rgba(255, 165, 0, 0.2))`
3. Text colors: Use `var(--text-primary, #e5e7e9)` and `var(--text-secondary, #94a3b8)`
4. `.panel-header h3` color: Use `var(--accent-color, #ff8c00)` or similar orange accent
5. `.trigger-review-btn`: Change blue `#3b82f6` to orange accent `#ff8c00` for consistency
6. Error state: Darken background from `#fef2f2` to `rgba(255, 68, 68, 0.15)` with dark text
7. Empty state: Update colors to match dark theme

Reference DAODashboard.css and ProposalDetail.css for consistent patterns:
- Use `linear-gradient(135deg, var(--bg-elevated) 0%, var(--bg-secondary) 100%)` for card backgrounds
- Add subtle border glow effects for command-center aesthetic
  </action>
  <verify>
Build succeeds: `cd frontend && npm run build`
ReviewPanel visually matches the rest of the dark-themed application.
  </verify>
  <done>
ReviewPanel has dark theme styling consistent with command-center aesthetic, no more white box.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Both TypeScript builds succeed without errors
- [ ] AgentBuilderWizard button visible and opens wizard
- [ ] ReviewPanel displays with dark theme styling
- [ ] No visual inconsistencies in either component
</verification>

<success_criteria>
- All UAT issues from 4-12-ISSUES.md addressed
- Builds pass
- Ready for re-verification
</success_criteria>

<output>
After completion, create `.planning/phases/04-strategic-planning-module/4-12-FIX-SUMMARY.md`
</output>
