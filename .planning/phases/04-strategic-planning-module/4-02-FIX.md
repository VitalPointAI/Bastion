---
phase: 04-strategic-planning-module
plan: 4-02-FIX
type: fix
---

<objective>
Fix 1 UAT issue from plan 4-02.

Source: 4-02-ISSUES.md
Priority: 0 critical, 0 major, 1 minor
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

**Issues being fixed:**
@.planning/phases/04-strategic-planning-module/4-02-ISSUES.md

**Original plan for reference:**
@.planning/phases/04-strategic-planning-module/4-02-PLAN.md
</context>

<tasks>
<task type="auto">
  <name>Fix UAT-001: Update deprecated NEAR RPC endpoint fallbacks</name>
  <files>
    backend/src/api/accounts.ts
    backend/src/lib/mpc-accounts.ts
    backend/src/identity/did-service.ts
    backend/src/lib/near-events.ts
    backend/src/dao/dao-service.ts
  </files>
  <action>
Update all fallback RPC URLs from the deprecated `https://rpc.testnet.near.org` to `https://rpc.testnet.fastnear.com`.

There are 5 files with hardcoded fallback URLs:
1. `backend/src/api/accounts.ts:192` - `process.env.NEAR_RPC || 'https://rpc.testnet.near.org'`
2. `backend/src/lib/mpc-accounts.ts:154` - `process.env.NEAR_RPC || 'https://rpc.testnet.near.org'`
3. `backend/src/identity/did-service.ts:5` - `process.env.NEAR_RPC_URL || 'https://rpc.testnet.near.org'`
4. `backend/src/lib/near-events.ts:165` - `process.env.NEAR_RPC_URL || 'https://rpc.testnet.near.org'`
5. `backend/src/dao/dao-service.ts:563` - `process.env.NEAR_RPC_URL || 'https://rpc.testnet.near.org'`

Replace all instances with `https://rpc.testnet.fastnear.com` as the fallback.

Note: Some files use NEAR_RPC, others use NEAR_RPC_URL. Keep the env var names consistent with existing usage, just update the fallback URL.
  </action>
  <verify>
1. Run `grep -r "rpc.testnet.near.org" backend/src/` - should return no results
2. Run `grep -r "rpc.testnet.fastnear.com" backend/src/` - should show 5 occurrences
3. Run `cd backend && pnpm tsc --noEmit` - should compile without errors
  </verify>
  <done>
All deprecated RPC fallback URLs replaced with fastnear.com. No TypeScript errors. Backend will no longer log deprecation warnings.
  </done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] All 5 files updated with new RPC URL
- [ ] `grep` confirms no deprecated URLs remain
- [ ] TypeScript compilation passes
- [ ] No other breaking changes introduced
</verification>

<success_criteria>
- UAT-001 resolved: deprecated RPC endpoint replaced
- Backend compiles without errors
- Ready for re-verification
</success_criteria>

<output>
After completion, create `.planning/phases/04-strategic-planning-module/4-02-FIX-SUMMARY.md`
</output>
