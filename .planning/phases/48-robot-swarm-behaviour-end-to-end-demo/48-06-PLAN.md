---
phase: 48-robot-swarm-behaviour-end-to-end-demo
plan: 06
type: execute
wave: 3
depends_on: ["48-03", "48-04", "48-05"]
files_modified:
  - frontend/src/components/direct/CoalitionCaveatDashboard.tsx
  - frontend/src/components/direct/RobotMissionTrigger.tsx
  - frontend/src/components/cop/SwarmCOPLayer.tsx
autonomous: true
requirements:
  - CAVEAT-DASHBOARD
  - DETECTION-ATTRIBUTION
  - BOUNDING-ANIMATION
  - CAVEAT-BLOCK-UI

must_haves:
  truths:
    - "Pre-flight dashboard shows all robots with green/amber/red caveat status per mission type"
    - "If override attempted, system blocks with specific DID caveat details and suggests alternative asset"
    - "Detection attribution lines are toggleable — default off, showing clean COP"
    - "Bounding overwatch is visualized with animated roles (bounding element with moving arrow, overwatch with arc)"
  artifacts:
    - path: "frontend/src/components/direct/CoalitionCaveatDashboard.tsx"
      provides: "Pre-flight caveat status grid with robot/mission-type matrix"
      min_lines: 100
    - path: "frontend/src/components/direct/RobotMissionTrigger.tsx"
      provides: "Caveat enforcement integrated into mission dispatch flow"
    - path: "frontend/src/components/cop/SwarmCOPLayer.tsx"
      provides: "Detection attribution toggle and bounding overwatch animation"
  key_links:
    - from: "frontend/src/components/direct/CoalitionCaveatDashboard.tsx"
      to: "backend/src/robot/coalition-caveat-service.ts"
      via: "API call to check caveats before mission dispatch"
      pattern: "checkSwarmCaveat"
    - from: "frontend/src/components/direct/RobotMissionTrigger.tsx"
      to: "frontend/src/components/direct/CoalitionCaveatDashboard.tsx"
      via: "Renders dashboard before mission dispatch, blocks on caveat failure"
      pattern: "CoalitionCaveatDashboard"
---

<objective>
Build the coalition caveat pre-flight dashboard, integrate caveat enforcement into mission dispatch, add detection attribution toggle and bounding overwatch animation to the swarm COP layer.

Purpose: The caveat dashboard visually demonstrates automated coalition policy enforcement. Detection attribution and bounding animation add tactical richness to the COP visualization for the demo narrative.
Output: CoalitionCaveatDashboard component, updated RobotMissionTrigger, enhanced SwarmCOPLayer
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-robot-swarm-behaviour-end-to-end-demo/48-CONTEXT.md
@.planning/phases/48-robot-swarm-behaviour-end-to-end-demo/48-RESEARCH.md
@.planning/phases/48-robot-swarm-behaviour-end-to-end-demo/48-03-SUMMARY.md
@.planning/phases/48-robot-swarm-behaviour-end-to-end-demo/48-05-SUMMARY.md

@frontend/src/components/direct/RobotMissionTrigger.tsx
@frontend/src/components/cop/SwarmCOPLayer.tsx

<interfaces>
<!-- From 48-03 coalition-caveat-service.ts -->
CaveatCheckResult { allowed: boolean; blockedRobots: Array<{ robotId, nationalDid, nation, reason }> }
CoalitionProfile { nation, did, authority, allowed_missions: string[], restrictions: Array<{ mission_type, area_type?, reason, except? }> }

<!-- From 48-05 SwarmCOPLayer.tsx -->
SwarmFormationSpec includes detectionAttributions?: DetectionAttribution[]
DetectionAttribution { robotId, entityId, confidence, detectedAt }

<!-- From 48-02 layer-types.ts -->
SwarmFormationSpec.technique: 'traveling' | 'traveling_overwatch' | 'bounding_overwatch' | 'successive_bounds'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CoalitionCaveatDashboard and integrate into RobotMissionTrigger</name>
  <files>frontend/src/components/direct/CoalitionCaveatDashboard.tsx, frontend/src/components/direct/RobotMissionTrigger.tsx</files>
  <action>
**Create `frontend/src/components/direct/CoalitionCaveatDashboard.tsx`:**

A pre-flight dashboard component that shows a matrix of robots x mission types with caveat status.

Props: `swarmMembers: Array<{ robotId: string; nationalDid: string }>`, `missionType: string`, `areaType: string`, `onCaveatResult: (result: CaveatCheckResult) => void`

Layout:
- Header: "Coalition Pre-Flight Caveat Check"
- Grid/table: rows = swarm members (robot ID + nation flag/label), columns = selected mission type
- Each cell shows a status badge:
  - Green circle: allowed
  - Red circle with X: blocked (tooltip shows restriction reason from DID caveat)
  - Amber circle: partial restriction (area-dependent)
- Below the grid: summary line — "All clear" (green) or "BLOCKED: {robot} - {reason}" (red)
- If blocked, show "Suggest Alternative" button that calls the backend `suggestAlternativeAsset` and displays the suggestion
- Nation labels: Taiwan flag emoji or "TW", US flag or "US", AU flag or "AU"

The component fetches coalition profiles from a backend API endpoint. Add a simple GET endpoint `/api/robot/coalition-profiles` in the robot routes (or read from the swarm-cop-bridge if profiles are already loaded). If no API exists yet, load the profiles JSON directly via fetch from a static route or embed them as a constant loaded at init.

**Update `frontend/src/components/direct/RobotMissionTrigger.tsx`:**

Read the existing file carefully to understand its structure. Integrate caveat enforcement:

1. Import `CoalitionCaveatDashboard`
2. Before the mission dispatch button/action, render `CoalitionCaveatDashboard` when a swarm mission type is selected (swarm_recon, swarm_advance, swarm_patrol, find_engage)
3. If `caveatResult.allowed === false`, disable the dispatch button and show the blocked robots with reasons
4. If allowed, show green "Pre-Flight Clear" badge and enable dispatch
5. Do NOT change the existing non-swarm mission dispatch flow — caveat check only applies to swarm missions with multiple members
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>CoalitionCaveatDashboard renders a robot/mission caveat matrix. RobotMissionTrigger blocks swarm mission dispatch when any member's caveat forbids it. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add detection attribution toggle and bounding overwatch animation to SwarmCOPLayer</name>
  <files>frontend/src/components/cop/SwarmCOPLayer.tsx</files>
  <action>
Extend the SwarmCOPLayer created in Plan 05 with two new visual features.

**Detection Attribution Toggle:**
1. Add a toggle button in the SwarmTelemetryPanel (or as a floating map control): "Show Attribution" checkbox/toggle
2. When toggled ON, for each `DetectionAttribution` in the swarm's `detectionAttributions` array:
   - Draw a `L.polyline` from the detecting robot's position to the detected entity's COP symbol position
   - Style: thin dashed line (weight: 1, dashArray: '4 4'), colored by robot's nation (TW=green, US=blue, AU=gold)
   - Add a small label or tooltip on the line showing robot ID and confidence
3. When toggled OFF (default), remove all attribution lines
4. Use refs to manage the polyline layers, clean up on toggle-off and component unmount

**Bounding Overwatch Animation:**
1. When `swarm.technique === 'bounding_overwatch'` or `'successive_bounds'`:
   - Determine which members are in "bounding" role (moving) vs "overwatch" role (stationary). Use slot index: even slots bound first, odd slots overwatch (alternating). Or check if member positions are changing vs static between telemetry updates.
   - Bounding element: highlight with a pulsing border or animated arrow pointing in movement direction. Use CSS animation on a `L.divIcon` overlay, or a `L.polyline` arrow from current to next position.
   - Overwatch element: show a translucent arc/sector emanating from the overwatch position in the swarm's heading direction. Use `L.semiCircle` or a custom SVG fan shape with ~60-degree spread.
2. When technique changes away from bounding, remove the animation elements
3. For `'traveling'` and `'traveling_overwatch'`: no special animation beyond smooth movement (already handled by Plan 05)

**Implementation notes:**
- Keep attribution lines on a separate Leaflet featureGroup for easy show/hide
- Bounding animation elements should be cleaned up in the same useEffect as the formation polygon
- Z-ordering: base map < formation polygon < member markers < attribution lines < bounding animation
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>SwarmCOPLayer has a working detection attribution toggle (default off) and bounding overwatch animation (pulsing bounding element, overwatch arc). TypeScript compiles.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- CoalitionCaveatDashboard.tsx exists with caveat matrix rendering
- RobotMissionTrigger.tsx integrates caveat check before swarm mission dispatch
- SwarmCOPLayer.tsx has attribution toggle and bounding animation
</verification>

<success_criteria>
Pre-flight caveat dashboard shows green/amber/red status per robot per mission. Mission dispatch blocked when any swarm member's DID caveat forbids the mission type with specific reason shown. Detection attribution lines toggle on/off. Bounding overwatch shows animated roles.
</success_criteria>

<output>
After completion, create `.planning/phases/48-robot-swarm-behaviour-end-to-end-demo/48-06-SUMMARY.md`
</output>
