---
phase: 48-robot-swarm-behaviour-end-to-end-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - robot/tests/test_swarm_cop.py
  - robot/tests/test_coalition_caveat.py
  - robot/tests/test_corroboration.py
  - robot/tests/test_swarm_graph.py
  - backend/data/demo-taiwan-seed/taiwan-defense-directive.txt
  - backend/data/demo-taiwan-seed/adversary-orbat.json
  - backend/data/demo-taiwan-seed/coalition-forces.json
  - backend/data/calibration-profiles.json
autonomous: true
requirements:
  - DEMO-SEED
  - TAIPEI-COORDS
  - WAVE0-TESTS

must_haves:
  truths:
    - "Test scaffolds exist for swarm COP polygon ordering, coalition caveats, corroboration, and graph event dedup"
    - "Taiwan defense directive document exists as ingestible seed data"
    - "Coalition force composition with 3 national profiles is defined"
    - "Adversary ORBAT with PLA + Russian vehicles is defined"
    - "Calibration profile maps 5x5m room to Taipei Zhongzheng District coordinates"
  artifacts:
    - path: "robot/tests/test_swarm_cop.py"
      provides: "Swarm COP polygon hull ordering and state color tests"
    - path: "robot/tests/test_coalition_caveat.py"
      provides: "Coalition caveat enforcement tests for TW/US/AU profiles"
    - path: "robot/tests/test_corroboration.py"
      provides: "Multi-robot confidence fusion formula tests"
    - path: "robot/tests/test_swarm_graph.py"
      provides: "Brain graph event dedup and national provenance tests"
    - path: "backend/data/demo-taiwan-seed/taiwan-defense-directive.txt"
      provides: "Strategic directive for doc-intelligence ingestion"
    - path: "backend/data/demo-taiwan-seed/adversary-orbat.json"
      provides: "PLA + Russian adversary order of battle"
    - path: "backend/data/demo-taiwan-seed/coalition-forces.json"
      provides: "Taiwan/US/Australia force composition with robot assignments"
    - path: "backend/data/calibration-profiles.json"
      provides: "Taipei demo coordinate mapping"
  key_links: []
---

<objective>
Create Wave 0 test scaffolds for all Phase 48 testable behaviors, demo scenario seed data, and Taipei coordinate calibration profile.

Purpose: Establishes the testing foundation (Nyquist compliance) and demo data that all subsequent plans depend on. Test scaffolds define expected behaviors before implementation. Seed data provides the Taiwan defense scenario narrative.
Output: 4 test files (initially failing), 3 seed data files, updated calibration profile
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-robot-swarm-behaviour-end-to-end-demo/48-CONTEXT.md
@.planning/phases/48-robot-swarm-behaviour-end-to-end-demo/48-RESEARCH.md
@.planning/phases/48-robot-swarm-behaviour-end-to-end-demo/48-VALIDATION.md

@backend/src/cop/layers/layer-types.ts
@robot/swarm/models.py
@robot/swarm/formations.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 test scaffolds for all Phase 48 behaviors</name>
  <files>robot/tests/test_swarm_cop.py, robot/tests/test_coalition_caveat.py, robot/tests/test_corroboration.py, robot/tests/test_swarm_graph.py</files>
  <action>
Create four pytest test files with tests that define expected behaviors. Tests should import from modules that do not yet exist (they will fail with ImportError until implementation plans run). Each test uses `pytest.mark.skip(reason="Wave 0 scaffold — implementation pending")` so the test suite remains green while clearly documenting expected behavior.

**robot/tests/test_swarm_cop.py:**
- `test_polygon_hull_ordering`: Given 3 member positions in arbitrary order, `order_polygon_hull(positions)` returns them sorted by angle from centroid (no self-intersection). Test with a known triangle: positions at (0,0), (1,2), (2,0) should return in counterclockwise order.
- `test_formation_state_colors`: `FORMATION_STATE_COLORS` dict maps forming→blue (#3b82f6), ready→green (#22c55e), moving→amber (#f59e0b), holding→green (#22c55e), dispersing→gray (#6b7280), contact→red (#ef4444).

**robot/tests/test_coalition_caveat.py:**
- `test_us_urban_advance_blocked`: Given US profile with restriction on swarm_advance in urban area, `check_swarm_caveat("swarm_advance", "urban", [us_member], profiles)` returns `allowed=False` with US robot in `blocked_robots`.
- `test_au_recon_only`: AU observer profile allows "recon_area" and "swarm_recon", blocks "swarm_advance". Test both allow and block paths.
- `test_tw_full_authority`: Taiwan profile allows all mission types including "find_engage".
- `test_mixed_swarm_blocks_on_any_member`: Swarm with TW leader + US follower, "swarm_advance" in urban area blocked because US member cannot participate.

**robot/tests/test_corroboration.py:**
- `test_confidence_fusion_two_sources`: Two detections with confidence 0.7 each, VISION_PIPELINE_WEIGHT=0.70. Expected fused confidence = 1 - (1 - 0.7*0.7) * (1 - 0.7*0.7) = 1 - 0.51 * 0.51 ≈ 0.7399.
- `test_confidence_fusion_single_source`: Single detection returns its own weighted confidence.
- `test_confidence_fusion_three_sources`: Three detections converge toward 1.0.

**robot/tests/test_swarm_graph.py:**
- `test_event_dedup`: Calling `build_swarm_event_id(swarm_id, event_type, timestamp)` with timestamps 2 seconds apart (within 5s window) returns the same deterministic event ID.
- `test_national_provenance`: `build_swarm_event_assertion(event)` includes `prov:wasAttributedTo` set to the national DID.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && python -m pytest robot/tests/test_swarm_cop.py robot/tests/test_coalition_caveat.py robot/tests/test_corroboration.py robot/tests/test_swarm_graph.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 test files exist. All tests are marked as skipped (suite green). Test names and docstrings document expected behaviors for implementation plans.</done>
</task>

<task type="auto">
  <name>Task 2: Create Taiwan demo seed data and Taipei calibration profile</name>
  <files>backend/data/demo-taiwan-seed/taiwan-defense-directive.txt, backend/data/demo-taiwan-seed/adversary-orbat.json, backend/data/demo-taiwan-seed/coalition-forces.json, backend/data/calibration-profiles.json</files>
  <action>
Create the `backend/data/demo-taiwan-seed/` directory and three seed files, plus update or create `backend/data/calibration-profiles.json`.

**taiwan-defense-directive.txt:** A 2-3 page strategic defense directive document suitable for ingestion by the doc-intelligence pipeline (Phase 40). Written as a formal military directive (OPORD-style) for the defense of Taipei against a Chinese/Russian incursion. Include:
- Situation: PLA + Russian forces approaching Taipei from the north. Mixed armor column (T-99, ZBD-04, T-90, BTR-82).
- Mission: Coalition forces (Taiwan, US, Australia) defend Taipei strategic district. Deny enemy access to Presidential Office Building area.
- Execution: Phase 1 — Recon sweep of Zhongzheng District. Phase 2 — Establish defensive positions. Phase 3 — Engage advancing armor if authorized.
- Coordinating instructions: Coalition ROE, national caveats, lethal force requires commander authorization via DAO gate.

**adversary-orbat.json:** JSON array of adversary units with fields: `id`, `designation`, `type` (t-99, zbd-04, t-90, btr-82), `nation` (PLA or Russia), `estimated_position` (lat/lng in north Taipei approach), `echelon`, `strength`. Include 6-8 units representing a mixed mechanized company.

**coalition-forces.json:** JSON object with three national force entries (Taiwan, US, Australia). Each has: `nation`, `did` (did:near:resource-{nation}-coalition), `robots` array with `robot_id`, `role` (e.g., "point", "overwatch", "recon"), `national_did`, `authority_level` (full/restricted/observer). Map the 3 physical robots to 3 different nations.

**calibration-profiles.json:** If file exists, update it; if not, create it. Add/update a "default" profile:
```json
{
  "default": {
    "room_width": 5,
    "room_height": 5,
    "map_bounds": {
      "north": 25.0480,
      "south": 25.0420,
      "east": 121.5180,
      "west": 121.5120
    },
    "label": "Taipei Zhongzheng District — Strategic Demo Area"
  }
}
```
This maps the 5x5m physical room to a ~700m x 700m area around Taipei Main Station / Presidential Office Building area (Zhongzheng District, 25.04N 121.51E).
  </action>
  <verify>
    <automated>ls -la /home/vitalpointai/projects/ssr/backend/data/demo-taiwan-seed/ && cat /home/vitalpointai/projects/ssr/backend/data/calibration-profiles.json | python3 -m json.tool > /dev/null && echo "Valid JSON"</automated>
  </verify>
  <done>Three seed data files exist in backend/data/demo-taiwan-seed/. calibration-profiles.json has a "default" profile mapping to Taipei Zhongzheng District coordinates. All JSON files are valid.</done>
</task>

</tasks>

<verification>
- All 4 test scaffold files exist and are importable by pytest
- Test suite runs green (all tests skipped, none erroring)
- Seed data directory exists with 3 files
- calibration-profiles.json is valid JSON with correct Taipei coordinates
- TypeScript compilation unaffected: `npx tsc --noEmit`
</verification>

<success_criteria>
Wave 0 test scaffolds define all Phase 48 testable behaviors. Taiwan demo seed data provides the complete scenario narrative. Calibration profile maps physical room to real Taipei geography.
</success_criteria>

<output>
After completion, create `.planning/phases/48-robot-swarm-behaviour-end-to-end-demo/48-01-SUMMARY.md`
</output>
