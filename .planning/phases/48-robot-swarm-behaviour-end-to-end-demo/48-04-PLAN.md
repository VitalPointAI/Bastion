---
phase: 48-robot-swarm-behaviour-end-to-end-demo
plan: 04
type: execute
wave: 2
depends_on: ["48-02"]
files_modified:
  - backend/src/robot/swarm-graph-writer.ts
  - backend/src/robot/vision-cop-pipeline.ts
  - robot/swarm_graph.py
  - robot/corroboration.py
autonomous: true
requirements:
  - BRAIN-GRAPH-EVENTS
  - NATIONAL-PROVENANCE
  - MULTI-ROBOT-CORROBORATION
  - DETECTION-CONFIDENCE-FUSION

must_haves:
  truths:
    - "Every swarm lifecycle event is persisted to Neo4j with PROV-O provenance and national DID tag"
    - "Event MERGE with deterministic IDs prevents duplicate nodes on robot reconnection"
    - "Multiple robots detecting the same threat fuse confidence using the Phase 47 weighted product formula"
    - "COP symbols transition from ghosted (single detection) to solid (corroborated)"
  artifacts:
    - path: "backend/src/robot/swarm-graph-writer.ts"
      provides: "writeSwarmEventToGraph for all 8 event types with PROV-O provenance"
      exports: ["writeSwarmEventToGraph", "SwarmEventContext", "buildSwarmEventId"]
    - path: "backend/src/robot/vision-cop-pipeline.ts"
      provides: "Multi-robot detection corroboration with confidence fusion"
    - path: "robot/swarm_graph.py"
      provides: "Python helpers for swarm event ID generation and assertion building"
    - path: "robot/corroboration.py"
      provides: "Python confidence fusion function for robot-side pre-computation"
  key_links:
    - from: "backend/src/robot/swarm-graph-writer.ts"
      to: "backend/src/graph/neo4j-client"
      via: "executeWriteQuery for MERGE-based event creation"
      pattern: "MERGE.*Event"
    - from: "backend/src/robot/vision-cop-pipeline.ts"
      to: "backend/src/cop/layers/layer-types.ts"
      via: "confidence field on COPSymbolSpec for corroboration visual encoding"
      pattern: "confidence"
---

<objective>
Create the brain graph write path for swarm lifecycle events with national provenance, and extend the vision-cop-pipeline for multi-robot detection corroboration with confidence fusion.

Purpose: Persists the complete mission lifecycle to the knowledge graph for timeline playback, and enables multi-source intelligence confidence scoring — ghosted symbols for single detections, solid for corroborated.
Output: swarm-graph-writer.ts, extended vision-cop-pipeline.ts, Python helpers for graph events and corroboration
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-robot-swarm-behaviour-end-to-end-demo/48-CONTEXT.md
@.planning/phases/48-robot-swarm-behaviour-end-to-end-demo/48-RESEARCH.md
@.planning/phases/48-robot-swarm-behaviour-end-to-end-demo/48-02-SUMMARY.md

@backend/src/robot/vision-cop-pipeline.ts
@backend/src/cop/layers/layer-types.ts

<interfaces>
<!-- From 48-02: SwarmFormationSpec already in layer-types.ts -->
<!-- From vision-cop-pipeline.ts: updateKnowledgeGraph() pattern with executeWriteQuery -->
<!-- From Phase 47: PROV-O model — prov:wasGeneratedBy, prov:wasAttributedTo, prov:wasDerivedFrom -->
<!-- From Phase 47: Confidence fusion formula — conf = 1 - product(1 - w_i * conf_i) -->
<!-- VISION_PIPELINE_WEIGHT = 0.70 from Phase 47 provenance-types.ts -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create swarm-graph-writer with PROV-O provenance and MERGE dedup</name>
  <files>backend/src/robot/swarm-graph-writer.ts, robot/swarm_graph.py, robot/tests/test_swarm_graph.py</files>
  <behavior>
    - buildSwarmEventId("swarm-1", "swarm_formed", "2026-03-16T10:00:01Z") === buildSwarmEventId("swarm-1", "swarm_formed", "2026-03-16T10:00:03Z") (within 5s window)
    - buildSwarmEventId("swarm-1", "swarm_formed", "2026-03-16T10:00:01Z") !== buildSwarmEventId("swarm-1", "swarm_formed", "2026-03-16T10:00:07Z") (different 5s window)
    - writeSwarmEventToGraph produces MERGE query with prov:wasAttributedTo set to national DID
  </behavior>
  <action>
**Create `backend/src/robot/swarm-graph-writer.ts`:**

1. Export `SwarmEventContext` interface:
```typescript
interface SwarmEventContext {
  swarmId: string;
  eventType: 'mission_authorized' | 'mission_dispatched' | 'swarm_formed' | 'formation_changed'
    | 'detection_shared' | 'escalation_requested' | 'authorization_granted'
    | 'authorization_denied' | 'mission_complete';
  timestamp: string;       // ISO string
  nationalDid: string;     // Asserting nation's DID
  daoTxHash?: string;      // NEAR blockchain tx hash if DAO-authorized
  members?: string[];      // Robot IDs involved
  payload: Record<string, unknown>;
  workspaceId: string;
}
```

2. Export `buildSwarmEventId(swarmId: string, eventType: string, timestamp: string): string` — rounds timestamp to nearest 5-second window (`Math.floor(epoch / 5000) * 5000`), returns `EVT-${swarmId}-${eventType}-${rounded}`. This deduplicates reconnection bursts.

3. Export `writeSwarmEventToGraph(event: SwarmEventContext): Promise<void>`:
   - Import `executeWriteQuery` from `../graph/neo4j-client.js` (look at how vision-cop-pipeline.ts imports it — may be a different path or named export)
   - Build deterministic event ID via `buildSwarmEventId()`
   - Execute a Cypher `MERGE (e:Event {id: $eventId}) ON CREATE SET ...` query
   - Set PROV-O properties: `e["@type"] = "cco:Process"`, `e["@context"] = "bastion:v1"`, `e["cco:process_type"] = $eventType`, `e["prov:wasGeneratedBy"] = "swarm_coordinator"`, `e["prov:wasAttributedTo"] = $nationalDid`, `e["bastion:swarmId"] = $swarmId`
   - If `daoTxHash` is present, set `e["bastion:daoTxHash"]`
   - Set `e["xsd:validFrom"] = $timestamp`, `e.workspaceId = $workspaceId`
   - Add `ON MATCH SET e["xsd:validThrough"] = $timestamp` to update the end time on dedup matches

**Create `robot/swarm_graph.py`:**

Port `build_swarm_event_id()` and `build_swarm_event_assertion()` to Python:
- `build_swarm_event_id(swarm_id, event_type, timestamp_iso)` — same 5-second rounding logic
- `build_swarm_event_assertion(event: dict) -> dict` — returns a JSON-LD dict with `@type`, `prov:wasAttributedTo`, etc. matching the TypeScript version. Used by robot agent to pre-build assertions before sending to backend.

**Update `robot/tests/test_swarm_graph.py`:**

Remove `pytest.mark.skip`. Import from `robot.swarm_graph`. Test:
- `test_event_dedup`: timestamps 2s apart produce same ID; timestamps 6s apart produce different IDs
- `test_national_provenance`: `build_swarm_event_assertion()` output includes `prov:wasAttributedTo` matching the input national DID
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && python -m pytest robot/tests/test_swarm_graph.py -v --tb=short && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>swarm-graph-writer.ts exports writeSwarmEventToGraph with MERGE dedup. Python tests pass for event ID deduplication and national provenance tagging. TypeScript compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend vision-cop-pipeline for multi-robot detection corroboration</name>
  <files>backend/src/robot/vision-cop-pipeline.ts, robot/corroboration.py, robot/tests/test_corroboration.py, robot/tests/test_swarm_cop.py</files>
  <behavior>
    - fuse_detection_confidence([0.7, 0.7], weight=0.70) approx 0.7399
    - fuse_detection_confidence([0.8], weight=0.70) approx 0.56
    - fuse_detection_confidence([0.7, 0.7, 0.7], weight=0.70) > 0.87
    - Corroborated detections (confidence >= 0.5) get full opacity; single-source (< 0.5) get ghosted
  </behavior>
  <action>
**Extend `backend/src/robot/vision-cop-pipeline.ts`:**

1. Add new adversary classes to `THREAT_CLASS_MAP` (or equivalent constant): `t-99` (Type 99 MBT, symbolSet '10', entity '120100'), `zbd-04` (ZBD-04 IFV, symbolSet '10', entity '120200'), `btr-82` (BTR-82 APC, symbolSet '10', entity '120200'). Also add aliases: `type99`, `ztz99`, `zbd04`, `btr82`.

2. Add a `fuseDetectionConfidence(detections: Array<{confidence: number}>): number` function using the Phase 47 weighted product formula:
```typescript
const VISION_PIPELINE_WEIGHT = 0.70;
function fuseDetectionConfidence(detections: Array<{confidence: number}>): number {
  const product = detections.reduce(
    (prod, d) => prod * (1 - d.confidence * VISION_PIPELINE_WEIGHT),
    1,
  );
  return 1 - product;
}
```

3. Add corroboration logic: When a new detection arrives, check existing COP symbols for position proximity (< 50m) AND class match. If found, fuse confidence and update the existing symbol rather than creating a new one. Use the `DetectionAttribution` type from layer-types.ts to track which robot detected what.

4. Add a `corroborationStatus(confidence: number): 'ghosted' | 'low' | 'solid'` function: `< 0.5` → ghosted, `0.5-0.85` → low, `> 0.85` → solid. This is used by frontend for visual encoding.

**Create `robot/corroboration.py`:**

Port `fuse_detection_confidence()` to Python for robot-side pre-computation:
- `fuse_detection_confidence(confidences: list[float], weight: float = 0.70) -> float`
- Same formula: `1 - product(1 - c * weight for c in confidences)`

**Update `robot/tests/test_corroboration.py`:**

Remove `pytest.mark.skip`. Import from `robot.corroboration`. Test the three scenarios: 2-source, single-source, 3-source fusion values.

**Update `robot/tests/test_swarm_cop.py`:**

Remove `pytest.mark.skip` from `test_polygon_hull_ordering` and `test_formation_state_colors`. For polygon hull ordering, create a simple Python helper `order_polygon_hull(positions)` in `robot/swarm_cop_utils.py` that sorts positions by angle from centroid. Test that 3 positions in arbitrary order come out in counterclockwise order. For state colors, define `FORMATION_STATE_COLORS` dict in same module and test the color mapping.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && python -m pytest robot/tests/test_corroboration.py robot/tests/test_swarm_cop.py -v --tb=short && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>Corroboration tests pass with correct confidence fusion values. Swarm COP utility tests pass for polygon hull ordering and state colors. vision-cop-pipeline.ts includes new adversary classes and fuseDetectionConfidence. TypeScript compiles.</done>
</task>

</tasks>

<verification>
- `python -m pytest robot/tests/test_swarm_graph.py robot/tests/test_corroboration.py robot/tests/test_swarm_cop.py -v` — all tests pass
- `npx tsc --noEmit` passes
- vision-cop-pipeline.ts includes T-99, ZBD-04, BTR-82 in threat class map
- swarm-graph-writer.ts uses MERGE with deterministic event IDs
</verification>

<success_criteria>
Swarm lifecycle events persist to Neo4j brain graph with national provenance tags and dedup protection. Multi-robot detection corroboration fuses confidence using the Phase 47 formula. COP symbols have corroboration status for visual encoding (ghosted/low/solid). All Wave 0 Python tests are unskipped and passing.
</success_criteria>

<output>
After completion, create `.planning/phases/48-robot-swarm-behaviour-end-to-end-demo/48-04-SUMMARY.md`
</output>
