---
phase: 48-robot-swarm-behaviour-end-to-end-demo
plan: 08
type: execute
wave: 4
depends_on: ["48-05", "48-06", "48-07"]
files_modified:
  - backend/src/robot/robot-mission-service.ts
  - frontend/src/components/cop/SwarmCOPLayer.tsx
  - frontend/src/components/cop/COPMapView.tsx
autonomous: false
requirements:
  - END-TO-END-WIRING
  - GHOSTED-TO-SOLID
  - CORROBORATION-COP
  - TIMELINE-PLAYBACK

must_haves:
  truths:
    - "Full demo flow works: directive ingestion to timeline playback in one continuous sequence"
    - "COP symbols transition from ghosted (single detection, <0.5 confidence) to solid (corroborated, >0.85)"
    - "Corroborated detections show confidence badges on COP"
    - "Brain timeline slider replays the full mission evolution after completion"
    - "Swarm telemetry flows from robots through bridge to COP to brain graph without manual intervention"
  artifacts:
    - path: "backend/src/robot/robot-mission-service.ts"
      provides: "Complete swarm mission lifecycle with COP bridge, graph writer, and corroboration wired"
    - path: "frontend/src/components/cop/SwarmCOPLayer.tsx"
      provides: "Ghosted-to-solid confidence visual encoding for corroborated detections"
    - path: "frontend/src/components/cop/COPMapView.tsx"
      provides: "Hostile symbol confidence badges and corroboration visual encoding"
  key_links:
    - from: "backend/src/robot/robot-mission-service.ts"
      to: "backend/src/robot/swarm-cop-bridge.ts"
      via: "handleSwarmTelemetry calls bridge"
      pattern: "bridgeSwarmTelemetryToCOP"
    - from: "backend/src/robot/robot-mission-service.ts"
      to: "backend/src/robot/swarm-graph-writer.ts"
      via: "mission lifecycle events written to graph"
      pattern: "writeSwarmEventToGraph"
    - from: "frontend/src/components/cop/SwarmCOPLayer.tsx"
      to: "frontend/src/components/cop/COPMapView.tsx"
      via: "Confidence visual encoding shared between swarm and hostile layers"
      pattern: "corroborationStatus"
---

<objective>
Wire all Phase 48 components into a complete end-to-end demo flow, add corroboration visual encoding to the COP, and verify with a human checkpoint.

Purpose: This is the final assembly plan. All individual services (bridge, graph writer, caveats, escalation, COP layer) get connected into a single continuous flow. The human checkpoint verifies the complete demo narrative works.
Output: Fully wired end-to-end demo flow, corroboration visual encoding, human-verified demo
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-robot-swarm-behaviour-end-to-end-demo/48-CONTEXT.md
@.planning/phases/48-robot-swarm-behaviour-end-to-end-demo/48-RESEARCH.md
@.planning/phases/48-robot-swarm-behaviour-end-to-end-demo/48-02-SUMMARY.md
@.planning/phases/48-robot-swarm-behaviour-end-to-end-demo/48-04-SUMMARY.md
@.planning/phases/48-robot-swarm-behaviour-end-to-end-demo/48-05-SUMMARY.md
@.planning/phases/48-robot-swarm-behaviour-end-to-end-demo/48-06-SUMMARY.md
@.planning/phases/48-robot-swarm-behaviour-end-to-end-demo/48-07-SUMMARY.md

@backend/src/robot/robot-mission-service.ts
@frontend/src/components/cop/SwarmCOPLayer.tsx
@frontend/src/components/cop/COPMapView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add corroboration visual encoding and verify end-to-end wiring</name>
  <files>frontend/src/components/cop/SwarmCOPLayer.tsx, frontend/src/components/cop/COPMapView.tsx, backend/src/robot/robot-mission-service.ts</files>
  <action>
**Corroboration visual encoding on COP:**

1. In COPMapView.tsx (or wherever hostile COP symbols are rendered), add confidence-based visual encoding:
   - Read the confidence field from each COPSymbolSpec
   - If `confidence < 0.5`: render symbol at 40% opacity with dotted border (ghosted — single detection)
   - If `confidence >= 0.5 && < 0.85`: render at 70% opacity with amber confidence badge showing percentage
   - If `confidence >= 0.85`: render at full opacity (solid — corroborated) with green confidence badge
   - Badge: small rounded pill overlay showing confidence % (e.g., "87%") positioned above the symbol

2. In SwarmCOPLayer.tsx, update the detection attribution lines to reflect corroboration:
   - If multiple attribution lines point to the same hostile symbol, draw them in distinct colors per robot
   - Add a small "2x" or "3x" badge on the hostile symbol showing source count

**End-to-end wiring verification:**

3. In `robot-mission-service.ts`, verify the complete telemetry processing chain is wired:
   - `handleSwarmTelemetry()` calls `bridgeSwarmTelemetryToCOP()` (from Plan 02)
   - Mission dispatch calls `writeMissionDispatchedEvent()` (from Plan 07)
   - Vision pipeline calls `fuseDetectionConfidence()` for multi-robot detections (from Plan 04)
   - Escalation creates gates and writes events (from Plan 07)

   If any of these connections are missing (because plans were executed independently), add the import and call. This task is the "final assembly" that ensures nothing was missed.

4. Verify that swarm `formation_changed` events are written to the brain graph when the swarm coordinator changes formation. In `handleSwarmTelemetry()`, compare the incoming formation with the stored formation in `swarmStates`. If different, call `writeSwarmEventToGraph({ eventType: 'formation_changed', ... })`.

5. Verify that `swarm_formed` events are written when a new swarm ID appears in `swarmStates` for the first time.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit 2>&1 | head -20 && python -m pytest robot/tests/ -x -q --tb=short</automated>
  </verify>
  <done>Corroboration visual encoding shows ghosted/amber/solid symbols with confidence badges. All wiring between plans is verified. Formation change and swarm formed events are written to brain graph. All tests pass. TypeScript compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Phase 48 end-to-end demo</name>
  <files>n/a</files>
  <action>
Human verifies the complete Phase 48 integration. All automated work is complete — this checkpoint confirms visual and functional correctness of the full demo pipeline.

What was built:
- Swarm formation polygons on COP map (translucent, state-colored, smooth movement)
- Coalition caveat pre-flight dashboard (TW/US/AU profiles)
- Detection corroboration (ghosted to solid symbols with confidence badges)
- Detection attribution toggle
- Bounding overwatch animation
- DAO expedited authorization with NEAR blockchain recording
- Lethal escalation gates (both approve and deny paths)
- Brain graph swarm event persistence with national provenance
- Taiwan demo seed data with Taipei coordinate mapping

How to verify:
1. Start the backend: `cd backend && npm run dev`
2. Start the frontend: `cd frontend && npm run dev`
3. Open the COP view in the browser
4. Verify the Taipei Zhongzheng District map area loads (coordinates around 25.04N, 121.51E)
5. If robots are available: dispatch a swarm recon mission and verify:
   a. Formation polygon appears on COP with correct state color
   b. Leader marker is visually distinct from followers
   c. Positions interpolate smoothly (no jumps)
   d. Click polygon to open telemetry panel
   e. Toggle detection attribution lines on/off
6. If robots are NOT available: verify TypeScript compiles, all Python tests pass, and components render without runtime errors by checking browser console
7. Verify brain graph: check Neo4j for swarm event nodes with PROV-O properties
8. Check coalition caveat dashboard renders in RobotMissionTrigger for swarm missions

Resume signal: Type "approved" or describe issues to fix
  </action>
  <verify>Human visual and functional verification</verify>
  <done>Human confirms: formation polygons render correctly, caveat dashboard blocks restricted missions, detection corroboration shows ghosted-to-solid transitions, all pipeline components are connected end-to-end.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `python -m pytest robot/tests/ -v` — all tests pass
- All Phase 48 services are wired into a continuous pipeline
- Human verifies visual output on COP map
</verification>

<success_criteria>
The complete demo pipeline is wired: strategic directive ingestion to AI objective extraction to operational plan to DAO-authorized tactical missions to swarm execution to COP visualization to brain graph persistence to timeline playback. Human confirms visual and functional correctness.
</success_criteria>

<output>
After completion, create `.planning/phases/48-robot-swarm-behaviour-end-to-end-demo/48-08-SUMMARY.md`
</output>
