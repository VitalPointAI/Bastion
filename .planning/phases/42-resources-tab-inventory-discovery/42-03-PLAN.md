---
phase: 42-resources-tab-inventory-discovery
plan: 03
type: execute
wave: 1
depends_on: [42-01]
files_modified:
  - frontend/src/components/resources/discovery/ClientDiscoveryPanel.tsx
  - frontend/src/components/resources/discovery/DiscoverySubView.tsx
  - frontend/src/components/resources/discovery/DevicePipelineKanban.tsx
  - frontend/src/components/resources/ResourcesTab.tsx
autonomous: true
requirements: [RES-04, RES-05]

must_haves:
  truths:
    - "Discovery sub-view shows ClientDiscoveryPanel for BLE/Serial scanning"
    - "Device pipeline kanban shows 6 columns: Discovered, Authenticating, Onboarding, Connected, Offline, Rejected"
    - "Devices move between kanban columns in real-time as their state changes via WebSocket"
    - "Each device card shows name, type, and current state with color-coded badge"
    - "Clicking a device card sets selectedResourceId in ResourcesContext"
  artifacts:
    - path: "frontend/src/components/resources/discovery/DiscoverySubView.tsx"
      provides: "Discovery sub-view combining scanner and pipeline"
      exports: ["DiscoverySubView"]
    - path: "frontend/src/components/resources/discovery/DevicePipelineKanban.tsx"
      provides: "Kanban-style pipeline view of device state machine"
      exports: ["DevicePipelineKanban"]
    - path: "frontend/src/components/resources/discovery/ClientDiscoveryPanel.tsx"
      provides: "Moved from cop/ — BLE/Serial scanning UI"
      exports: ["ClientDiscoveryPanel"]
  key_links:
    - from: "frontend/src/components/resources/discovery/DiscoverySubView.tsx"
      to: "frontend/src/hooks/useDiscovery.ts"
      via: "props from ResourcesTab (hoisted useDiscovery)"
      pattern: "devices|scannerStatus"
    - from: "frontend/src/components/resources/discovery/DevicePipelineKanban.tsx"
      to: "frontend/src/lib/discovery-service.ts"
      via: "DeviceState enum for column grouping"
      pattern: "DeviceState"
---

<objective>
Wire the Discovery sub-view with ClientDiscoveryPanel (moved from cop/), a new DevicePipelineKanban showing device state machine progression in kanban columns, and real-time WebSocket updates via the hoisted useDiscovery hook.

Purpose: Provides at-a-glance onboarding status for all discovered devices with visual pipeline progression.
Output: Working Discovery sub-view with BLE/Serial scanner and kanban-style device pipeline.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-resources-tab-inventory-discovery/RESEARCH.md
@.planning/phases/42-resources-tab-inventory-discovery/42-01-SUMMARY.md

<interfaces>
<!-- Key types the executor needs -->

From frontend/src/hooks/useDiscovery.ts:
```typescript
export interface UseDiscoveryResult {
  devices: DiscoveredDevice[];
  scannerStatus: DiscoveryStatus | null;
  connected: number;
  refetch: () => Promise<void>;
}
export function useDiscovery(): UseDiscoveryResult;
```

From frontend/src/lib/discovery-service.ts:
```typescript
export const DeviceState = {
  discovered: 'discovered',
  fingerprinting: 'fingerprinting',
  authenticating: 'authenticating',
  gate_check: 'gate_check',
  ironclaw_analysis: 'ironclaw_analysis',
  pending_dao: 'pending_dao',
  onboarding: 'onboarding',
  connected: 'connected',
  disconnected: 'disconnected',
  quarantined: 'quarantined',
  rejected: 'rejected',
  revoked: 'revoked',
} as const;

// DiscoveredDevice has: id, name, type, state (DeviceState value), rssi, lastSeen, etc.
```

From frontend/src/components/cop/ClientDiscoveryPanel.tsx:
```typescript
// Zero-prop component — mounts and works via useClientDiscovery hook internally
export function ClientDiscoveryPanel(): JSX.Element;
```

From frontend/src/components/resources/ResourcesContext.tsx (42-01):
```typescript
export function useResourcesContext(): {
  selectedResourceId: string | null;
  setSelectedResourceId: (id: string | null) => void;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move ClientDiscoveryPanel and create DevicePipelineKanban</name>
  <files>frontend/src/components/resources/discovery/ClientDiscoveryPanel.tsx, frontend/src/components/resources/discovery/DevicePipelineKanban.tsx</files>
  <action>
**Move `frontend/src/components/cop/ClientDiscoveryPanel.tsx` to `frontend/src/components/resources/discovery/ClientDiscoveryPanel.tsx`:**
- Copy the file content to the new location. Keep the original file in cop/ for now (COP tab currently does not import it — it's orphaned — but the move decision says COP retains read-only view by importing from new location, so keep original only if COP imports it; since COP does NOT import it, just move it).
- Update any relative import paths inside the file if they break (e.g., imports from `../../hooks/` may need to become `../../../hooks/`).
- The component uses `useClientDiscovery` hook internally — verify that import path resolves from the new location.

**Create `frontend/src/components/resources/discovery/DevicePipelineKanban.tsx`:**
- Accept props: `{ devices: DiscoveredDevice[], onSelectDevice: (deviceId: string) => void }`.
- Map the 12 DeviceState values to 6 display columns per RESEARCH.md Pitfall 1:
  - **Discovered**: states `discovered`, `fingerprinting`
  - **Authenticating**: states `authenticating`, `gate_check`, `ironclaw_analysis`, `pending_dao`
  - **Onboarding**: state `onboarding`
  - **Connected**: state `connected`
  - **Offline**: state `disconnected`
  - **Rejected**: states `rejected`, `revoked`, `quarantined`
- Define a `PIPELINE_COLUMNS` constant array: `[{ id, label, states[], color }]` where color is a Tailwind border-color class:
  - Discovered: `border-blue-500`
  - Authenticating: `border-yellow-500`
  - Onboarding: `border-purple-500`
  - Connected: `border-green-500`
  - Offline: `border-gray-500`
  - Rejected: `border-red-500`
- Render as a horizontal flex row of columns. Each column:
  - Header with column label, count badge, and colored top border (4px).
  - Scrollable card list (`max-h-96 overflow-y-auto`).
- Each device card:
  - Device name (bold), device type (smaller, gray).
  - State badge with color from `STATE_STYLES` in discovery-service (or custom if not available).
  - RSSI signal indicator if available.
  - `cursor-pointer` — onClick calls `onSelectDevice(device.id)`.
- Group devices using: `const grouped = PIPELINE_COLUMNS.map(col => ({ ...col, devices: devices.filter(d => col.states.includes(d.state)) }))`.
- Empty columns show subtle dashed border with "No devices" text.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && test -f frontend/src/components/resources/discovery/ClientDiscoveryPanel.tsx && test -f frontend/src/components/resources/discovery/DevicePipelineKanban.tsx && grep -q "PIPELINE_COLUMNS" frontend/src/components/resources/discovery/DevicePipelineKanban.tsx</automated>
  </verify>
  <done>ClientDiscoveryPanel moved to resources/discovery/. DevicePipelineKanban renders 6 columns mapping 12 DeviceState values. Device cards are clickable with state badges.</done>
</task>

<task type="auto">
  <name>Task 2: Create DiscoverySubView and wire into ResourcesTab</name>
  <files>frontend/src/components/resources/discovery/DiscoverySubView.tsx, frontend/src/components/resources/ResourcesTab.tsx</files>
  <action>
**Create `frontend/src/components/resources/discovery/DiscoverySubView.tsx`:**
- Accept props: `{ devices: DiscoveredDevice[], scannerStatus: DiscoveryStatus | null }` — these come from the hoisted useDiscovery in ResourcesTab.
- Import `useResourcesContext` for cross-view selection.
- Layout (vertical stack):
  1. **Scanner section** (top, collapsible): `<ClientDiscoveryPanel />` wrapped in a collapsible section with "Device Scanner" header and expand/collapse toggle. Default: collapsed.
  2. **Pipeline section** (main): `<DevicePipelineKanban devices={devices} onSelectDevice={setSelectedResourceId} />`.
  3. **Status bar** (bottom): Show scanner status text from `scannerStatus` (e.g., "Scanning", "Idle", "Error") with a colored dot indicator. Show total device count and connected count.
- When a device is selected via the kanban, set it in ResourcesContext so the detail panel (from Plan 42-02) can show device info.

**Update `frontend/src/components/resources/ResourcesTab.tsx`:**
- Add `useDiscovery()` call at the ResourcesTab level (hoisted — per RESEARCH.md anti-pattern: do NOT call useDiscovery in both DiscoverySubView and NetworkSubView).
- Store `{ devices, scannerStatus, connected }` from the hook.
- Replace the discovery placeholder with: `<DiscoverySubView devices={devices} scannerStatus={scannerStatus} />`.
- Pass devices/scannerStatus/connected as props to the network placeholder too (will be used by Plan 42-04).
- Import DiscoverySubView from `./discovery/DiscoverySubView`.
- Import `useDiscovery` from `../../hooks/useDiscovery`.

CRITICAL: Only one useDiscovery() call in the entire Resources tab tree. It lives in ResourcesTab, NOT in sub-views.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && test -f frontend/src/components/resources/discovery/DiscoverySubView.tsx && grep -q "useDiscovery" frontend/src/components/resources/ResourcesTab.tsx && grep -c "useDiscovery" frontend/src/components/resources/ResourcesTab.tsx | xargs test 1 -eq</automated>
  </verify>
  <done>DiscoverySubView renders scanner + kanban pipeline. ResourcesTab hoists useDiscovery (exactly 1 call). Devices flow as props to sub-views. Device selection updates ResourcesContext.</done>
</task>

</tasks>

<verification>
1. ClientDiscoveryPanel exists at new path `resources/discovery/`
2. DevicePipelineKanban maps 12 states to 6 columns
3. DiscoverySubView renders both scanner (collapsible) and pipeline
4. ResourcesTab has exactly ONE useDiscovery() call
5. No TypeScript errors from moved files
</verification>

<success_criteria>
- Discovery sub-view shows device scanner (collapsible) and kanban pipeline
- Kanban has 6 columns: Discovered, Authenticating, Onboarding, Connected, Offline, Rejected
- Device cards show name, type, state badge, and are clickable
- Devices update in real-time via WebSocket (useDiscovery provides this)
- Selecting a device sets selectedResourceId in ResourcesContext
</success_criteria>

<output>
After completion, create `.planning/phases/42-resources-tab-inventory-discovery/42-03-SUMMARY.md`
</output>
