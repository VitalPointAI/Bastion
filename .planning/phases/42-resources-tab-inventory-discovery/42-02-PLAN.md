---
phase: 42-resources-tab-inventory-discovery
plan: 02
type: execute
wave: 1
depends_on: [42-01]
files_modified:
  - frontend/src/components/mission/resources/ResourceCatalog.tsx
  - frontend/src/components/mission/resources/ResourceForm.tsx
  - frontend/src/components/mission/resources/BulkImporter.tsx
  - frontend/src/components/mission/resources/ConsumableTracker.tsx
  - frontend/src/components/resources/inventory/InventorySubView.tsx
  - frontend/src/components/resources/inventory/ResourceActionMenu.tsx
  - frontend/src/components/resources/ResourceDetailPanel.tsx
autonomous: true
requirements: [RES-03]

must_haves:
  truths:
    - "Inventory sub-view shows ResourceCatalog with equipment, personnel, and consumables tabs"
    - "ResourceCatalog is scoped to the current problem set via problemSetId prop"
    - "Each resource row has a three-dot action menu with context-sensitive options"
    - "Clicking a resource opens a slide-over detail panel on the right"
    - "Detail panel shows DID master record section (read-only) and local editable section"
  artifacts:
    - path: "frontend/src/components/resources/inventory/InventorySubView.tsx"
      provides: "Inventory sub-view wrapper mounting ResourceCatalog"
      exports: ["InventorySubView"]
    - path: "frontend/src/components/resources/inventory/ResourceActionMenu.tsx"
      provides: "Three-dot action menu for resource rows"
      exports: ["ResourceActionMenu"]
    - path: "frontend/src/components/resources/ResourceDetailPanel.tsx"
      provides: "Slide-over detail panel shared across all sub-views"
      exports: ["ResourceDetailPanel"]
    - path: "frontend/src/components/mission/resources/ResourceCatalog.tsx"
      provides: "Refactored to accept problemSetId instead of missionId"
      contains: "problemSetId"
  key_links:
    - from: "frontend/src/components/resources/inventory/InventorySubView.tsx"
      to: "frontend/src/components/mission/resources/ResourceCatalog.tsx"
      via: "ResourceCatalog import with problemSetId prop"
      pattern: "problemSetId"
    - from: "frontend/src/components/resources/ResourceDetailPanel.tsx"
      to: "frontend/src/components/resources/ResourcesContext.tsx"
      via: "useResourcesContext for selectedResourceId"
      pattern: "useResourcesContext"
---

<objective>
Wire the Inventory sub-view by refactoring ResourceCatalog from missionId to problemSetId, creating the InventorySubView wrapper, adding a resource action menu, and building the shared ResourceDetailPanel slide-over.

Purpose: The Inventory sub-view is the default view and the primary resource management interface. The detail panel and action menu are reused by all other sub-views.
Output: Working Inventory tab showing equipment/personnel/consumables scoped to problem set, with action menus and detail panel.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-resources-tab-inventory-discovery/RESEARCH.md
@.planning/phases/42-resources-tab-inventory-discovery/42-01-SUMMARY.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From frontend/src/components/mission/resources/ResourceCatalog.tsx:
```typescript
// BEFORE (to be refactored):
interface ResourceCatalogProps { missionId: string; }
export function ResourceCatalog({ missionId }: ResourceCatalogProps) {
  useEffect(() => { loadData(); }, [missionId]);
  // calls: resourceService.getResources(missionId), getPersonnel(missionId), getConsumables(missionId)
}
// The service layer passes the value as missionId in query strings — no backend change needed.
```

From frontend/src/components/resources/ResourcesContext.tsx (created in 42-01):
```typescript
export const ResourcesContext: React.Context<{
  selectedResourceId: string | null;
  setSelectedResourceId: (id: string | null) => void;
}>;
export function ResourcesProvider({ children }: { children: React.ReactNode }): JSX.Element;
export function useResourcesContext(): { selectedResourceId: string | null; setSelectedResourceId: (id: string | null) => void };
```

From frontend/src/lib/resource-registry-service.ts:
```typescript
export interface RegisteredResource {
  id: string; did: string; name: string; category: ResourceCategory;
  status: ResourceStatus; capabilities: string[]; isAutonomous: boolean;
  // ... additional fields
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor ResourceCatalog from missionId to problemSetId</name>
  <files>frontend/src/components/mission/resources/ResourceCatalog.tsx, frontend/src/components/mission/resources/ResourceForm.tsx, frontend/src/components/mission/resources/BulkImporter.tsx, frontend/src/components/mission/resources/ConsumableTracker.tsx</files>
  <action>
Rename `missionId` prop to `problemSetId` in all 4 files. This is a prop/variable rename ONLY — the service layer passes the value as `missionId` in query strings to the backend, and that does NOT change.

**ResourceCatalog.tsx:**
1. Change interface: `interface ResourceCatalogProps { problemSetId: string; }`
2. Change destructure: `export function ResourceCatalog({ problemSetId }: ResourceCatalogProps)`
3. Update useEffect dependency: `[problemSetId]`
4. Update all service calls: `resourceService.getResources(problemSetId)`, `getPersonnel(problemSetId)`, `getConsumables(problemSetId)`
5. Update child component props: `<ResourceForm problemSetId={problemSetId} .../>`, `<BulkImporter problemSetId={problemSetId} .../>`, `<ConsumableTracker problemSetId={problemSetId} .../>`

**ResourceForm.tsx:**
1. Change prop interface from `missionId: string` to `problemSetId: string`
2. Update all internal references from `missionId` to `problemSetId`
3. Service calls pass `problemSetId` as the value (service internally maps to missionId query param)

**BulkImporter.tsx:**
1. Same prop rename pattern as ResourceForm

**ConsumableTracker.tsx:**
1. Same prop rename pattern as ResourceForm

IMPORTANT: Do NOT rename the query parameter in resource-service.ts — the backend expects `missionId`. Only rename the component-level prop/variable names.

Also check if MissionDetail.tsx passes missionId to ResourceCatalog. If so, update the call site: `<ResourceCatalog problemSetId={missionId} />` (passing the mission's ID value under the new prop name).
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && grep -c "problemSetId" frontend/src/components/mission/resources/ResourceCatalog.tsx | xargs test 3 -le && ! grep -q "missionId" frontend/src/components/mission/resources/ResourceCatalog.tsx</automated>
  </verify>
  <done>All 4 resource components accept problemSetId prop. No remaining missionId prop references in component interfaces. Service calls pass problemSetId value correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Create InventorySubView, ResourceActionMenu, and ResourceDetailPanel</name>
  <files>frontend/src/components/resources/inventory/InventorySubView.tsx, frontend/src/components/resources/inventory/ResourceActionMenu.tsx, frontend/src/components/resources/ResourceDetailPanel.tsx</files>
  <action>
**Create `frontend/src/components/resources/inventory/InventorySubView.tsx`:**
- Accept `{ problemSetId: string }` prop.
- Import and render `<ResourceCatalog problemSetId={problemSetId} />`.
- Import and render `<ResourceDetailPanel />` (conditionally visible based on `useResourcesContext().selectedResourceId`).
- Wrap in a div with `className="flex flex-1 min-h-0"` — ResourceCatalog takes flex-1, detail panel is fixed width on right.
- **"Show disposed" toggle (CONTEXT.md locked decision):** Add `const [showDisposed, setShowDisposed] = useState(false)` (default false — disposed resources hidden). Add a toolbar row above ResourceCatalog with a toggle button: "Show Disposed" (gray when off, amber when on). Pass `showDisposed` to ResourceCatalog as a new optional prop `showDisposed?: boolean`. In ResourceCatalog, when `showDisposed` is false, filter out resources with status 'disposed' from the rendered table. Disposed resources render grayed out when visible.

**Create `frontend/src/components/resources/inventory/ResourceActionMenu.tsx`:**
- Accept `{ resourceId: string, status: string, distributionType?: string, onAction: (action: string, resourceId: string) => void }` props.
- Render a three-dot button (vertical ellipsis icon using unicode or SVG) that opens a dropdown menu on click.
- Menu items per CONTEXT.md decisions: "View Detail", "Reassign", "Loan Out", "Redistribute", "Report Damaged", "Dispose/Decommission".
- Distribution constraint enforcement:
  - ASSGN: "Redistribute" disabled with tooltip "Assigned resources cannot be redistributed"
  - ALLOC: "Dispose/Decommission" disabled with tooltip "Allocated resources must be returned to parent echelon"
  - APPRTN: "Redistribute" and "Dispose" disabled with tooltip "Apportioned resources are managed at parent echelon"
- Status-based disabling: already "Disposed" resources only show "View Detail".
- Use Tailwind for dropdown styling: `absolute right-0 mt-1 bg-gray-800 border border-gray-600 rounded shadow-lg z-50`.
- Close on outside click and Escape key.
- Call `onAction(actionName, resourceId)` when an item is clicked.

**Create `frontend/src/components/resources/ResourceDetailPanel.tsx`:**
- Accept `{ resourceId: string | null, onClose: () => void }` props.
- Render as a slide-over panel from the right side: `fixed top-0 right-0 h-full w-96 bg-gray-800 border-l border-gray-600 z-40 transform transition-transform` — translate-x-full when resourceId is null, translate-x-0 when present.
- Two sections with clear visual separation:
  1. **DID Master Record** (read-only): Header with lock icon and `bg-gray-750` background. Shows resource name/designation, category, capabilities, DID identifier. All fields are plain text (no edit controls).
  2. **Local Editable** (problem-set level): Header with edit icon. Shows status dropdown, group assignment, local notes textarea, operational nickname input, current location input, readiness assessment dropdown.
- For now, use placeholder data structure — actual data fetching will be wired when registry service is integrated in Plan 42-06.
- Close button (X) in top-right corner calls onClose.
- Backdrop overlay (semi-transparent) that also triggers onClose on click.

**Update ResourcesTab.tsx (from 42-01):**
- Replace the inventory placeholder div with `<InventorySubView problemSetId={problemSetId} />`.
- Import InventorySubView from `./inventory/InventorySubView`.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && test -f frontend/src/components/resources/inventory/InventorySubView.tsx && test -f frontend/src/components/resources/inventory/ResourceActionMenu.tsx && test -f frontend/src/components/resources/ResourceDetailPanel.tsx && grep -q "InventorySubView" frontend/src/components/resources/ResourcesTab.tsx && grep -q "showDisposed" frontend/src/components/resources/inventory/InventorySubView.tsx</automated>
  </verify>
  <done>InventorySubView renders ResourceCatalog scoped to problemSetId. ResourceActionMenu shows context-sensitive actions with distribution constraint enforcement. ResourceDetailPanel slides in from right with DID master (read-only) and local editable sections. ResourcesTab imports InventorySubView for the inventory sub-view.</done>
</task>

</tasks>

<verification>
1. `grep "problemSetId" frontend/src/components/mission/resources/ResourceCatalog.tsx` — confirms prop rename
2. `grep -rn "missionId" frontend/src/components/mission/resources/` — should only appear in service call context (passing value), not in prop interfaces
3. All 3 new files exist in resources/inventory/ and resources/
4. ResourcesTab now imports InventorySubView instead of placeholder
</verification>

<success_criteria>
- ResourceCatalog accepts problemSetId (not missionId) and renders equipment/personnel/consumables tabs
- InventorySubView mounts ResourceCatalog with correct problemSetId
- Action menu shows distribution-constraint-aware options
- Detail panel slides in/out with DID read-only and local editable sections
- No TypeScript errors from the refactor
</success_criteria>

<output>
After completion, create `.planning/phases/42-resources-tab-inventory-discovery/42-02-SUMMARY.md`
</output>
