---
phase: 44-robot-vision-capabilities-and-mission-intent-translation
plan: 02
type: execute
wave: 2
depends_on: [44-01]
files_modified:
  - robot/vision/camera.py
  - robot/vision/vision_engine.py
  - robot/tests/test_vision_engine_simulate.py
autonomous: true
requirements: [VIS-05]

must_haves:
  truths:
    - "VisionEngine can run detection in simulate mode and return mock detections"
    - "Camera module wraps CSI camera with simulate fallback"
    - "Detection inference runs in asyncio.to_thread to avoid blocking the event loop"
    - "MockVisionEngine returns periodic mock detections for testing"
  artifacts:
    - path: "robot/vision/camera.py"
      provides: "CSI camera wrapper with MockCamera for simulate mode"
      exports: ["Camera", "MockCamera"]
    - path: "robot/vision/vision_engine.py"
      provides: "VisionEngine wrapping jetson-inference detectNet + simulate mode"
      exports: ["VisionEngine", "MockVisionEngine"]
    - path: "robot/tests/test_vision_engine_simulate.py"
      provides: "Tests for MockVisionEngine simulate behavior"
  key_links:
    - from: "robot/vision/vision_engine.py"
      to: "robot/vision/models.py"
      via: "imports DetectionResult"
      pattern: "from vision.models import DetectionResult"
    - from: "robot/vision/vision_engine.py"
      to: "robot/vision/camera.py"
      via: "uses Camera for frame capture"
      pattern: "camera.Capture"
---

<objective>
Implement the vision engine (detectNet wrapper) and camera module with full simulate mode support.

Purpose: The vision engine is the core sensing capability for all four new mission types. It must work in simulate mode for development and real mode on Jetson hardware.
Output: VisionEngine + Camera classes with tests proving simulate mode works correctly.
</objective>

<execution_context>
@/home/vitalpoint/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpoint/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-robot-vision-capabilities-and-mission-intent-translation/44-CONTEXT.md
@.planning/phases/44-robot-vision-capabilities-and-mission-intent-translation/RESEARCH.md
@robot/config.py
@robot/vision/models.py

<interfaces>
From robot/vision/models.py (created in Plan 01):
```python
class DetectionResult(BaseModel):
    class_desc: str
    confidence: float
    bbox: Dict[str, float]  # {"left", "top", "right", "bottom"}
    center_x: Optional[float] = None
    center_y: Optional[float] = None

class VisionConfig(BaseModel):
    enabled: bool = True
    model: str = "ssd-mobilenet-v2"
    threshold: float = 0.5
    keyframe_enabled: bool = False
    keyframe_quality: int = 50
    vlm_enabled: bool = False
    vision_cadence_ms: int = 500
```

From robot/config.py:
```python
SIMULATE: bool
VISION_ENABLED: bool
VISION_MODEL: str
VISION_THRESHOLD: float
CAMERA_SENSOR_ID: int
KEYFRAME_ENABLED: bool
KEYFRAME_JPEG_QUALITY: int
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create camera.py with CSI wrapper and MockCamera</name>
  <files>robot/vision/camera.py</files>
  <behavior>
    - MockCamera.Capture() returns None (no CUDA image in simulate)
    - MockCamera is used when SIMULATE=True or jetson-utils not available
    - Camera class wraps jetson.utils.videoSource with try/except ImportError guard
    - Camera.capture_jpeg() returns bytes (JPEG-encoded frame)
  </behavior>
  <action>
    Create `robot/vision/camera.py`:

    1. `MockCamera` class:
       - `Capture()` returns None
       - `capture_jpeg()` returns a minimal valid JPEG bytes (a tiny 1x1 pixel JPEG constant for testing)
       - Property `is_mock` returns True

    2. `Camera` class:
       - `__init__(self, sensor_id: int = 0, simulate: bool = False)`:
         - If simulate=True, use MockCamera internally
         - If simulate=False, try `import jetson.utils` and create `videoSource(f"csi://{sensor_id}")`. On ImportError, log warning and fall back to MockCamera
       - `Capture()` -> CUDA image or None
       - `capture_jpeg(img_cuda=None, quality: int = 50, max_width: int = 640) -> Optional[bytes]`:
         - If mock or img_cuda is None, return mock JPEG
         - Otherwise, use `jetson.utils.cudaToNumpy` + `cv2.imencode('.jpg', ...)` to compress
         - Downsample to max_width if needed
       - Property `is_mock` -> bool

    Guard all jetson imports with try/except ImportError so tests run on any machine.
    Use structlog for logging (consistent with rest of codebase).
  </action>
  <verify>
    <automated>cd /home/vitalpoint/projects/bastion/robot && python -c "from vision.camera import Camera, MockCamera; c = MockCamera(); assert c.Capture() is None; assert c.is_mock; print('OK')"</automated>
  </verify>
  <done>Camera and MockCamera classes exist; MockCamera.Capture() returns None; Camera falls back to MockCamera when jetson-utils unavailable</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create vision_engine.py with VisionEngine and MockVisionEngine</name>
  <files>robot/vision/vision_engine.py, robot/tests/test_vision_engine_simulate.py</files>
  <behavior>
    - MockVisionEngine.detect_once() returns empty list most frames, mock detection every 5th call
    - MockVisionEngine mock detections have class_desc, confidence, bbox keys
    - VisionEngine.detect_once() is async and runs inference in asyncio.to_thread
    - VisionEngine falls back to MockVisionEngine when jetson-inference is unavailable
    - VisionEngine.get_keyframe_jpeg() returns JPEG bytes when enabled, None when disabled
  </behavior>
  <action>
    1. Create `robot/vision/vision_engine.py`:

    `MockVisionEngine`:
    - `__init__(self)`: frame_count=0
    - `async detect_once(self, camera=None) -> List[DetectionResult]`: increments frame_count; returns a mock DetectionResult every 5th frame (class_desc="person", confidence=0.85, bbox with plausible coords); empty list otherwise
    - `async get_keyframe_jpeg(self, camera=None, quality: int = 50) -> Optional[bytes]`: returns mock JPEG bytes
    - Property `is_mock` -> True

    `VisionEngine`:
    - `__init__(self, model: str = "ssd-mobilenet-v2", threshold: float = 0.5, simulate: bool = False)`:
      - If simulate=True, delegate to MockVisionEngine
      - If simulate=False, try `import jetson.inference` and create `detectNet(model, threshold=threshold)`. On ImportError, log warning, delegate to MockVisionEngine
    - `_capture_and_detect(self, camera) -> List[DetectionResult]`: synchronous; calls camera.Capture(), runs self._net.Detect(img), converts to DetectionResult list
    - `async detect_once(self, camera) -> List[DetectionResult]`: calls `await asyncio.to_thread(self._capture_and_detect, camera)` (CRITICAL: must not block asyncio loop)
    - `async get_keyframe_jpeg(self, camera, quality: int = 50) -> Optional[bytes]`: capture + compress via Camera.capture_jpeg()
    - Property `is_mock` -> bool

    2. Write `robot/tests/test_vision_engine_simulate.py` (tests first):
    - `test_mock_engine_returns_empty_initially`: first call returns empty list
    - `test_mock_engine_returns_detection_every_5th`: 5th call returns non-empty list with valid DetectionResult
    - `test_mock_engine_detection_has_required_fields`: detection has class_desc, confidence, bbox
    - `test_vision_engine_simulate_mode`: VisionEngine(simulate=True).is_mock is True
    - `test_vision_engine_detect_once_async`: await detect_once returns list (async test with pytest-asyncio)
    - `test_mock_engine_keyframe_jpeg`: get_keyframe_jpeg returns bytes
  </action>
  <verify>
    <automated>cd /home/vitalpoint/projects/bastion/robot && python -m pytest tests/test_vision_engine_simulate.py -x -v</automated>
  </verify>
  <done>VisionEngine and MockVisionEngine exist; detect_once is async via to_thread; MockVisionEngine returns periodic mock detections; all tests pass</done>
</task>

</tasks>

<verification>
- `cd robot && python -m pytest tests/test_vision_engine_simulate.py -x -v` all green
- `cd robot && python -c "from vision.vision_engine import VisionEngine; e = VisionEngine(simulate=True); print('mock:', e.is_mock)"` prints True
- `cd robot && python -c "from vision.camera import Camera; c = Camera(simulate=True); print('mock:', c.is_mock)"` prints True
- All existing tests still pass: `cd robot && python -m pytest tests/ -x`
</verification>

<success_criteria>
- VisionEngine wraps jetson-inference with asyncio.to_thread for non-blocking detection
- MockVisionEngine provides predictable mock detections for simulate mode
- Camera falls back gracefully to MockCamera when jetson-utils unavailable
- All simulate-mode tests pass on any machine (no hardware required)
</success_criteria>

<output>
After completion, create `.planning/phases/44-robot-vision-capabilities-and-mission-intent-translation/44-02-SUMMARY.md`
</output>
