---
phase: 62-knowledge-graph-entity-deduplication-and-auto-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/graph/resolution/canonical-aliases.ts
  - backend/src/graph/resolution/name-normalizer.ts
  - backend/src/graph/resolution/name-normalizer.test.ts
  - backend/src/graph/resolution/index.ts
autonomous: true
requirements:
  - DEDUP-01

must_haves:
  truths:
    - "normalizeActorName('PRC') returns 'China'"
    - "normalizeActorName('US') returns 'United States'"
    - "normalizeActorName('Republic of China') returns 'Republic of China' (NOT 'China')"
    - "normalizeActorName('unknown actor') returns 'unknown actor' unchanged"
    - "normalizeActorName('  PRC  ') trims whitespace before lookup"
  artifacts:
    - path: "backend/src/graph/resolution/canonical-aliases.ts"
      provides: "Static alias registry mapping variant names to canonical forms"
      exports: ["CANONICAL_ALIASES", "normalizeActorName"]
    - path: "backend/src/graph/resolution/name-normalizer.ts"
      provides: "Re-export wrapper for normalizer function"
      exports: ["normalizeActorName"]
    - path: "backend/src/graph/resolution/name-normalizer.test.ts"
      provides: "Unit tests for name normalization"
      min_lines: 40
  key_links:
    - from: "backend/src/graph/resolution/name-normalizer.ts"
      to: "backend/src/graph/resolution/canonical-aliases.ts"
      via: "import and re-export"
      pattern: "import.*canonical-aliases"
    - from: "backend/src/graph/resolution/index.ts"
      to: "backend/src/graph/resolution/name-normalizer.ts"
      via: "barrel export"
      pattern: "export.*name-normalizer"
---

<objective>
Create the canonical alias registry and name normalizer — the foundation for all deduplication wiring in subsequent plans.

Purpose: Every ingestion pathway needs to normalize actor names before node creation. This plan builds the pure-data alias registry and the normalizeActorName() function, fully tested.
Output: Two new source files, one test file, updated barrel export.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-knowledge-graph-entity-deduplication-and-auto-resolution/62-RESEARCH.md

<interfaces>
<!-- Existing resolution module exports that the new files must integrate with -->

From backend/src/graph/resolution/index.ts:
```typescript
export { findCandidateMatches, generateBlockingKeys, buildBlockingIndex } from './blocking.js';
export { EntityResolutionService, entityResolutionService, computeHybridScore, classifyHybridScore } from './resolution-service.js';
export { normalizeName, calculateSimilarity, matchWithAliases } from './string-matcher.js';
```

From backend/src/graph/resolution/string-matcher.ts:
```typescript
export function normalizeName(name: string): string;  // existing — lowercases + trims
```
Note: The new normalizeActorName() is DIFFERENT from the existing normalizeName(). normalizeName() lowercases for comparison. normalizeActorName() maps aliases to canonical display names.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create canonical alias registry and name normalizer with tests</name>
  <files>backend/src/graph/resolution/canonical-aliases.ts, backend/src/graph/resolution/name-normalizer.ts, backend/src/graph/resolution/name-normalizer.test.ts</files>
  <behavior>
    - normalizeActorName('PRC') returns 'China'
    - normalizeActorName('prc') returns 'China' (case insensitive)
    - normalizeActorName('US') returns 'United States'
    - normalizeActorName('usa') returns 'United States'
    - normalizeActorName('U.S.A.') returns 'United States'
    - normalizeActorName('Republic of China') returns 'Republic of China' (NOT 'China' — this is Taiwan/ROC)
    - normalizeActorName('DPRK') returns 'North Korea'
    - normalizeActorName('ROK') returns 'South Korea'
    - normalizeActorName('russian federation') returns 'Russia'
    - normalizeActorName('NATO') returns 'NATO'
    - normalizeActorName('  PRC  ') returns 'China' (trims whitespace)
    - normalizeActorName('Unknown Actor XYZ') returns 'Unknown Actor XYZ' (pass-through)
    - normalizeActorName('') returns '' (empty string pass-through)
  </behavior>
  <action>
    1. Create `backend/src/graph/resolution/canonical-aliases.ts` with the CANONICAL_ALIASES Record and normalizeActorName() function exactly as specified in 62-RESEARCH.md Pattern 1. Include all aliases from the research (US, China, Korea, Russia, NATO, military shorthand). CRITICAL: Do NOT map "Republic of China" to "China" — it is Taiwan (ROC), a distinct entity per entity-resolution-agent.ts knowledge.

    2. Create `backend/src/graph/resolution/name-normalizer.ts` as a thin re-export: `export { normalizeActorName, CANONICAL_ALIASES } from './canonical-aliases.js';` This provides a clean import path for consumers.

    3. Create `backend/src/graph/resolution/name-normalizer.test.ts` with vitest tests covering all behaviors above. Use `describe('normalizeActorName')` with individual `it()` blocks for each alias group (US variants, China variants, Korea variants, pass-through, edge cases).

    Note: The existing `normalizeName()` in string-matcher.ts is for comparison (lowercases). The new `normalizeActorName()` is for canonicalization (maps to display name). These are complementary, not conflicting.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr/backend && bash -lc 'npx vitest run --reporter=verbose src/graph/resolution/name-normalizer.test.ts'</automated>
  </verify>
  <done>All 13+ test cases pass. normalizeActorName correctly maps known aliases to canonical forms, passes through unknown names unchanged, handles case insensitivity and whitespace trimming, and does NOT conflate ROC/Taiwan with China/PRC.</done>
</task>

<task type="auto">
  <name>Task 2: Update resolution barrel export</name>
  <files>backend/src/graph/resolution/index.ts</files>
  <action>
    Add re-exports for the new modules to `backend/src/graph/resolution/index.ts`:

    ```typescript
    export { normalizeActorName, CANONICAL_ALIASES } from './name-normalizer.js';
    ```

    Place this after the existing exports. This enables downstream consumers (OSINT sync, graph builder) to import from the barrel: `import { normalizeActorName } from '../graph/resolution/index.js';`
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr/backend && bash -lc 'npx tsc --noEmit --pretty 2>&1 | head -30'</automated>
  </verify>
  <done>TypeScript compiles without errors. normalizeActorName and CANONICAL_ALIASES are exported from the resolution barrel.</done>
</task>

</tasks>

<verification>
- All name-normalizer tests pass
- TypeScript compiles cleanly
- No changes to existing resolution service behavior
</verification>

<success_criteria>
normalizeActorName() exists, is tested, and is exported from the resolution barrel. Ready for Plans 02 and 03 to wire into ingestion pipelines and API endpoints.
</success_criteria>

<output>
After completion, create `.planning/phases/62-knowledge-graph-entity-deduplication-and-auto-resolution/62-01-SUMMARY.md`
</output>
