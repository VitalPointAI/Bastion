---
phase: 25-operational-design-workspace-inserted
plan: 04
type: execute
wave: 3
depends_on: ["25-02", "25-03"]
files_modified:
  - frontend/src/components/design/LOETimelineSection.tsx
  - frontend/src/components/design/LOELane.tsx
  - frontend/src/components/design/DecisivePointNode.tsx
  - frontend/src/components/tabs/DesignTab.tsx
autonomous: true
requirements:
  - OD-LINES-OF-EFFORT
  - OD-LOE-VISUALIZATION

must_haves:
  truths:
    - "User sees horizontal timeline with LOE lanes and phase columns"
    - "User can add/edit/delete Lines of Effort"
    - "User can add/edit/delete decisive points within LOE lanes"
    - "Decisive points can be linked to CoG vulnerability nodes"
    - "Visual indicators show which vulnerabilities each LOE addresses"
    - "Phase columns divide the timeline into operational phases"
    - "Data auto-saves on changes"
  artifacts:
    - path: "frontend/src/components/design/LOETimelineSection.tsx"
      provides: "Timeline container with phase columns and LOE management"
      min_lines: 150
    - path: "frontend/src/components/design/LOELane.tsx"
      provides: "Single horizontal LOE lane with decisive points"
      min_lines: 80
    - path: "frontend/src/components/design/DecisivePointNode.tsx"
      provides: "Interactive decisive point marker on timeline"
      min_lines: 50
  key_links:
    - from: "frontend/src/components/design/LOETimelineSection.tsx"
      to: "frontend/src/lib/design-service.ts"
      via: "auto-save LOE data on changes"
      pattern: "designService\\.updateSection.*lines-of-effort"
    - from: "frontend/src/components/design/DecisivePointNode.tsx"
      to: "CoGAnalysis data"
      via: "cogLinks array references CoG vulnerability node IDs"
      pattern: "cogLinks|cogNodeId"
---

<objective>
Build the Lines of Effort/Operation timeline visualization with horizontal lanes, decisive points, phase columns, and CoG vulnerability linkages. This is the primary operational approach visualization.

Purpose: LOE timelines translate CoG analysis into actionable operational lines with phased decisive points. The visualization must match standard military operational approach diagrams with lanes, decisive points, and phase boundaries.
Output: Interactive LOE timeline with lanes per LOE, decisive points positioned by phase, and visual links to CoG vulnerabilities.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-operational-design-workspace-inserted/25-CONTEXT.md
@.planning/phases/25-operational-design-workspace-inserted/25-RESEARCH.md
@.planning/phases/25-operational-design-workspace-inserted/25-01-SUMMARY.md

@frontend/src/components/escalation/EffectChainDiagram.tsx

<interfaces>
<!-- From Plan 01 outputs -->

From backend/src/design/types.ts:
```typescript
interface DecisivePoint {
  id: string;
  label: string;
  description: string;
  phase: string;
  position: number;
  cogLinks: LOECoGLink[];
}

interface LOECoGLink {
  loeId: string;
  decisivePointId: string;
  cogNodeId: string;
  cogNodeType: CoGNode['type'];
}

interface LineOfEffort {
  id: string;
  name: string;
  description: string;
  objectiveId?: string;
  decisivePoints: DecisivePoint[];
  order: number;
}

interface CoGNode {
  id: string;
  type: 'cog' | 'critical-capability' | 'critical-requirement' | 'critical-vulnerability';
  label: string;
  description: string;
  children: CoGNode[];
}
```

From frontend/src/lib/design-service.ts:
```typescript
export const designService = {
  updateSection(problemSetId: string, section: string, data: unknown): Promise<OperationalDesign>,
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LOE lane and decisive point components</name>
  <files>frontend/src/components/design/LOELane.tsx, frontend/src/components/design/DecisivePointNode.tsx</files>
  <action>
  **1. Create `frontend/src/components/design/LOELane.tsx`**:
  - A single horizontal lane representing one Line of Effort in the timeline.
  - Props: `{ loe: LineOfEffort; phases: Array<{ id: string; name: string }>; phaseWidth: number; laneHeight: number; onLoeUpdate: (loe: LineOfEffort) => void; onLoeDelete: (loeId: string) => void; cogVulnerabilities: CoGNode[]; yOffset: number }`.
  - Renders as an SVG group `<g>` translated to proper y position.
  - Lane background: subtle horizontal stripe (bg-gray-800/50 rect) spanning full timeline width.
  - LOE label area on the left (fixed width ~180px): LOE name, edit icon on hover, delete icon on hover. Click name to edit inline.
  - For each phase column: render a region where decisive points for that phase are plotted.
  - Decisive points rendered as DecisivePointNode components positioned within their phase column.
  - "Add Decisive Point" button appears on hover within each phase column (small + icon at bottom of lane within that column).

  **2. Create `frontend/src/components/design/DecisivePointNode.tsx`**:
  - Interactive node on the timeline representing a decisive point.
  - Props: `{ dp: DecisivePoint; x: number; y: number; onUpdate: (dp: DecisivePoint) => void; onDelete: (dpId: string) => void; onLinkCoG: (dpId: string) => void; cogVulnerabilities: CoGNode[] }`.
  - Render as SVG group at (x, y):
    - Diamond shape (rotated rect) or circle, size ~24px.
    - Label text below the shape (truncated).
    - If dp.cogLinks.length > 0: small green indicator dot showing linked CVs.
  - Click: open editor popover with fields for label, description.
  - CoG link indicator: on hover, show tooltip listing linked CV names. Include "Link to CV" button that opens a dropdown of available CoG vulnerability nodes (from cogVulnerabilities prop). User selects a CV to create a LOECoGLink. Can also unlink existing links.
  - Delete: small X icon on hover.
  - Styling: fill based on whether linked to CVs (green-tinted if linked, gray if unlinked), dark theme compatible.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>LOELane renders a horizontal lane with LOE label, decisive points positioned by phase. DecisivePointNode renders interactive markers with CoG link indicators and edit capability.</done>
</task>

<task type="auto">
  <name>Task 2: LOETimelineSection container with phase columns and DesignTab wiring</name>
  <files>frontend/src/components/design/LOETimelineSection.tsx, frontend/src/components/tabs/DesignTab.tsx</files>
  <action>
  **1. Create `frontend/src/components/design/LOETimelineSection.tsx`**:
  - Full timeline visualization container.
  - Props: `{ problemSetId: string; initialLOEs: LineOfEffort[]; cogAnalysis: CoGAnalysis; onUpdate: (loes: LineOfEffort[]) => void }`.
  - State: local LOEs array, phases array (user-defined phase list), AI panel open/closed.

  Layout (horizontal timeline with lanes):
  - Constants: LANE_HEIGHT = 100, PHASE_WIDTH = 200, LABEL_WIDTH = 180, HEADER_HEIGHT = 50.
  - Phase management bar at top: editable list of phases (e.g., "Phase I: Shape", "Phase II: Deter", "Phase III: Dominate"). Add/edit/delete phases. Default: 3 phases.
  - Below phase bar: SVG timeline area.
    - Phase column headers at top showing phase names with vertical divider lines.
    - LOE lanes stacked vertically, each rendered by LOELane component.
  - "Add Line of Effort" button at bottom — adds new LOE with empty decisive points.

  CoG vulnerability extraction:
  - Flatten adversary CoGTree to collect all critical-vulnerability nodes.
  - Pass as cogVulnerabilities to each LOELane for linking.

  SVG viewport:
  - Width = LABEL_WIDTH + (phases.length * PHASE_WIDTH) + padding.
  - Height = HEADER_HEIGHT + (loes.length * LANE_HEIGHT) + padding.
  - Horizontal scroll wrapper (overflow-x: auto) for wide timelines.

  LOE management:
  - Add LOE: create new LineOfEffort with randomUUID, push to array.
  - Delete LOE: remove from array, reorder remaining.
  - Reorder LOEs: drag handle on label area (optional — can just use up/down buttons).
  - All mutations trigger auto-save with 2-second debounce.

  AI Panel (right side, collapsible):
  - Reuse DesignAIPanel with `activeSection="lines-of-effort"` and `sectionData={{ loes, cogAnalysis }}`.
  - LOE gap analysis stub: AI panel shows "LOE gap analysis coming soon" placeholder.

  Section header: "Lines of Effort / Operation" with subtitle "Decisive Points and Phasing" in text-gray-400.

  **2. Wire LOETimelineSection into DesignTab.tsx**:
  - Replace the lines-of-effort placeholder div with `<LOETimelineSection>`.
  - Pass `initialLOEs={designData.linesOfEffort}`, `cogAnalysis={designData.cogAnalysis}`, `problemSetId`, and `onUpdate` callback that calls `designService.updateSection(problemSetId, 'lines-of-effort', loes)` and refreshes design state.
  - Note: cogAnalysis is passed from the DesignTab's loaded designData, ensuring LOE section can reference CoG vulnerability nodes for linking.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>LOE timeline renders with phase columns, horizontal lanes per LOE, and decisive point markers. Users can add/edit/delete LOEs and decisive points. Decisive points can be linked to CoG vulnerabilities. Timeline scrolls horizontally for wide content.</done>
</task>

</tasks>

<verification>
- TypeScript compiles
- LOE timeline section renders with default 3 phase columns
- "Add Line of Effort" creates a new lane
- Decisive points can be added within phase columns
- CoG vulnerability linking shows available CVs from adversary tree
- Linked decisive points show green indicator
- Phase columns can be added/edited/deleted
- Auto-save triggers on LOE changes
</verification>

<success_criteria>
- Horizontal timeline with lanes matches standard military operational approach diagram layout
- LOE lanes with decisive points positioned by phase
- Explicit links between decisive points and CoG vulnerabilities
- Visual indicators show which vulnerabilities each LOE addresses
- Phase management (add/edit/delete columns)
- LOE management (add/edit/delete/reorder lanes)
- Auto-save with debounce
</success_criteria>

<output>
After completion, create `.planning/phases/25-operational-design-workspace-inserted/25-04-SUMMARY.md`
</output>
