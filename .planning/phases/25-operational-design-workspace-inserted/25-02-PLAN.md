---
phase: 25-operational-design-workspace-inserted
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - frontend/src/components/design/ProblemFramingSection.tsx
  - frontend/src/components/design/DesignAIPanel.tsx
  - frontend/src/components/design/AIFramingCard.tsx
  - frontend/src/components/tabs/DesignTab.tsx
  - backend/src/api/design.ts
autonomous: true
requirements:
  - OD-PROBLEM-FRAMING
  - OD-AI-PANEL

must_haves:
  truths:
    - "User can fill out all JP 5-0 problem framing fields"
    - "Problem statement auto-generates from current state and desired end state gap"
    - "User can click Analyze to get AI alternative framings"
    - "AI framings appear as cards with Adopt, Merge, and Dismiss actions"
    - "AI panel is collapsible and section-aware"
    - "Data auto-saves on field changes with debounce"
    - "Confidence scores display as High/Medium/Low badges on AI cards"
  artifacts:
    - path: "frontend/src/components/design/ProblemFramingSection.tsx"
      provides: "Structured form with all JP 5-0 fields"
      min_lines: 150
    - path: "frontend/src/components/design/DesignAIPanel.tsx"
      provides: "Collapsible right-side AI panel, section-aware"
      min_lines: 80
    - path: "frontend/src/components/design/AIFramingCard.tsx"
      provides: "Interactive AI suggestion card with Adopt/Merge/Dismiss"
      min_lines: 60
  key_links:
    - from: "frontend/src/components/design/ProblemFramingSection.tsx"
      to: "frontend/src/lib/design-service.ts"
      via: "debounced auto-save on field change"
      pattern: "designService\\.updateSection.*problem-framing"
    - from: "frontend/src/components/design/DesignAIPanel.tsx"
      to: "/api/design/:id/analyze"
      via: "fetch on Analyze button click"
      pattern: "fetch.*analyze"
    - from: "frontend/src/components/design/AIFramingCard.tsx"
      to: "ProblemFramingSection"
      via: "Adopt/Merge callbacks update form state"
      pattern: "onAdopt|onMerge"
---

<objective>
Build the Problem Framing section with structured form fields (JP 5-0) and the collapsible AI panel that provides alternative framings from the existing problem-framing agent. This is the first functional section of the Design workspace.

Purpose: Problem framing is the doctrinal starting point — define the problem before analyzing centers of gravity. The AI panel pattern built here is reused by CoG and LOE sections.
Output: Fully interactive problem framing form with AI-assisted alternative framings, auto-save, and section status updates.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-operational-design-workspace-inserted/25-CONTEXT.md
@.planning/phases/25-operational-design-workspace-inserted/25-RESEARCH.md
@.planning/phases/25-operational-design-workspace-inserted/25-01-SUMMARY.md

@backend/src/agents/problem-framing.ts
@frontend/src/components/tabs/DesignTab.tsx

<interfaces>
<!-- From Plan 01 outputs -->

From backend/src/design/types.ts (created in Plan 01):
```typescript
interface ProblemFramingData {
  currentState: string;
  desiredEndState: string;
  problemStatement: string;
  keyTensions: string[];
  obstacles: string[];
  opportunities: string[];
  assumptions: string[];
  constraints: string[];
}

type SectionStatus = 'not-started' | 'in-progress' | 'complete';
```

From backend/src/agents/problem-framing.ts (existing):
```typescript
export type PerspectiveType = 'military' | 'diplomatic' | 'economic' | 'informational' | 'social' | 'legal' | 'adversary' | 'coalition_partner' | 'local_population' | 'historical_analogy';

export interface AlternativeFraming {
  perspectiveType: PerspectiveType;
  framingStatement: string;
  rootCauses: string[];
  keyStakeholders: Array<{ name: string; interest: string; influence: 'high' | 'medium' | 'low' }>;
  interventionPoints: string[];
  assumptions: string[];
  blindSpots: string[];
  framingConfidence: number;
  confidenceBounds: { lower: number; upper: number };
}
```

From frontend/src/lib/design-service.ts (created in Plan 01):
```typescript
export const designService = {
  getDesign(problemSetId: string): Promise<OperationalDesign>,
  updateSection(problemSetId: string, section: string, data: unknown): Promise<OperationalDesign>,
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend AI analyze endpoint and frontend AI panel components</name>
  <files>backend/src/api/design.ts, frontend/src/components/design/DesignAIPanel.tsx, frontend/src/components/design/AIFramingCard.tsx, frontend/src/lib/design-service.ts</files>
  <action>
  **1. Add AI analyze endpoint to `backend/src/api/design.ts`**:
  - `POST /api/design/:problemSetId/analyze` — Accepts `{ section: string, context: object }` in body.
  - For section='problem-framing': import `generateFramings` from `../../agents/problem-framing.js` and call it with the problem framing context. Return `{ framings: AlternativeFraming[] }`.
  - For other sections (cog-analysis, lines-of-effort): return `{ suggestions: [] }` stub for now — Plan 03/04 will add real logic.
  - Error handling: try/catch, return 500 with error message.

  **2. Add `analyzeSection` method to `frontend/src/lib/design-service.ts`**:
  - `analyzeSection(problemSetId: string, section: string, context: object): Promise<any>` — POST /api/design/:id/analyze

  **3. Create `frontend/src/components/design/DesignAIPanel.tsx`**:
  - Collapsible split-pane panel that sits on the right side of the content area.
  - Props: `{ problemSetId: string; activeSection: string; sectionData: any; isOpen: boolean; onToggle: () => void }`.
  - State: `results` (section-keyed Map to prevent stale results on section switch), `loading`, `error`.
  - "Analyze" button at top — explicit trigger, no auto-run. When clicked, calls designService.analyzeSection with current section and sectionData as context.
  - Results area below button — renders section-specific content:
    - For 'problem-framing': renders AIFramingCard for each AlternativeFraming
    - For other sections: renders placeholder "AI analysis coming soon"
  - Section-aware: when `activeSection` changes, show cached results for that section or empty state.
  - Styling: bg-gray-900 right panel, border-l border-gray-700, w-80 when open, collapse to w-0 with a toggle button visible.
  - Header shows "AI Assistant" with section name. Toggle button with chevron icon.

  **4. Create `frontend/src/components/design/AIFramingCard.tsx`**:
  - Renders one AlternativeFraming as an interactive card.
  - Props: `{ framing: AlternativeFraming; onAdopt: (framing: AlternativeFraming) => void; onMerge: (framing: AlternativeFraming) => void; onDismiss: (framing: AlternativeFraming) => void }`.
  - Card layout:
    - Header: Perspective type label (capitalize, replace underscores) + confidence badge
    - Confidence badge: map framingConfidence to High (>= 0.7) / Medium (0.4-0.7) / Low (< 0.4) with green/amber/red text
    - Body: framingStatement in larger text, then collapsible sections for rootCauses, keyStakeholders, interventionPoints, assumptions, blindSpots
    - Footer: three action buttons — "Adopt" (primary blue), "Merge" (secondary gray), "Dismiss" (subtle text-red-400)
  - Dark UI styling: bg-gray-800, border-gray-700, rounded-lg, text-gray-200.
  - Dismiss removes the card from visible list (client-side state managed by parent DesignAIPanel).
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit --project backend/tsconfig.json 2>&1 | head -20 && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>AI analyze endpoint accepts POST and returns framings from problem-framing agent. DesignAIPanel renders as collapsible right panel with Analyze button. AIFramingCard displays framings with confidence badges and Adopt/Merge/Dismiss actions.</done>
</task>

<task type="auto">
  <name>Task 2: Problem Framing section form with auto-save and AI panel integration</name>
  <files>frontend/src/components/design/ProblemFramingSection.tsx, frontend/src/components/tabs/DesignTab.tsx</files>
  <action>
  **1. Create `frontend/src/components/design/ProblemFramingSection.tsx`**:
  - Layout: flex row. Left side (flex-1): structured form. Right side: DesignAIPanel (collapsible).
  - Props: `{ problemSetId: string; initialData: ProblemFramingData; onUpdate: (data: ProblemFramingData) => void }`.
  - State: local form state initialized from initialData. AI panel open/closed state.

  Form fields (left side, vertical stack):
  - **Current State**: textarea, label "Current State", placeholder "Describe the current operational environment..."
  - **Desired End State**: textarea, label "Desired End State", placeholder "Describe the desired conditions..."
  - **Problem Statement**: textarea, label "Problem Statement (Auto-generated)", read-only with light bg-gray-700. Auto-populates: "How to transition from [currentState summary] to [desiredEndState summary] given [obstacles count] obstacles and [constraints count] constraints." Updates whenever currentState or desiredEndState change.
  - **Key Tensions/Contradictions**: dynamic list — each item is a text input with delete button. "Add Tension" button appends empty item.
  - **Obstacles**: same dynamic list pattern as tensions.
  - **Opportunities**: same dynamic list pattern.
  - **Assumptions**: same dynamic list pattern.
  - **Constraints/Restraints**: same dynamic list pattern.

  Auto-save behavior: use a 2-second debounced effect that calls `onUpdate(formData)` whenever form state changes. Show a small "Saving..." / "Saved" indicator near the top of the form.

  AI panel integration:
  - Toggle button in top-right corner of section header: "AI Assistant" with expand/collapse icon.
  - DesignAIPanel rendered on right side with `activeSection="problem-framing"` and `sectionData={formData}`.
  - When user clicks "Adopt" on an AIFramingCard: replace entire form state with the framing's data mapped to ProblemFramingData fields (framingStatement -> problemStatement, rootCauses -> keyTensions, assumptions -> assumptions, etc.)
  - When user clicks "Merge": show a modal/popover listing the framing's fields with checkboxes — user selects which fields to merge into current form.
  - When user clicks "Dismiss": handled by DesignAIPanel internally.

  Styling: dark UI with bg-gray-800 form area, each field group with label (text-gray-300 text-sm font-medium mb-1), textarea (bg-gray-700 border-gray-600 text-gray-200 rounded-md p-2), dynamic lists with hover effects.

  **2. Wire ProblemFramingSection into DesignTab.tsx**:
  - Replace the problem-framing placeholder div with `<ProblemFramingSection>`.
  - Pass `initialData={designData.problemFraming}`, `problemSetId`, and `onUpdate` callback that calls `designService.updateSection(problemSetId, 'problem-framing', data)` and refreshes design state.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Problem Framing section renders all JP 5-0 fields with auto-save. Problem statement auto-generates from current/desired state gap. AI panel opens on right side, Analyze button triggers agent, framing cards display with Adopt/Merge/Dismiss. Status updates to in-progress/complete based on field completion.</done>
</task>

</tasks>

<verification>
- TypeScript compiles for both backend and frontend
- Problem Framing form renders all 8 field groups (Current State through Constraints)
- Problem Statement auto-updates when Current State or Desired End State changes
- Dynamic lists (tensions, obstacles, etc.) support add/delete
- Auto-save triggers 2 seconds after last edit
- AI panel toggles open/closed
- Clicking "Analyze" calls the backend and returns framings
- AI framing cards show perspective, confidence badge, and action buttons
- Adopt replaces form data, Dismiss removes card
</verification>

<success_criteria>
- Complete problem framing form with all JP 5-0 fields functional
- Auto-generated problem statement from current/desired state gap
- AI panel with explicit trigger model (Analyze button, no auto-run)
- Framing cards with Adopt/Merge/Dismiss actions working
- Confidence scores displayed as High/Medium/Low badges
- Section status updates from not-started to in-progress/complete based on data
</success_criteria>

<output>
After completion, create `.planning/phases/25-operational-design-workspace-inserted/25-02-SUMMARY.md`
</output>
