---
phase: 25-operational-design-workspace-inserted
plan: 05
type: execute
wave: 4
depends_on: ["25-02", "25-03", "25-04"]
files_modified:
  - frontend/src/components/design/OperationalApproachSection.tsx
  - frontend/src/components/design/DesignOverview.tsx
  - frontend/src/components/tabs/DesignTab.tsx
autonomous: false
requirements:
  - OD-OPERATIONAL-APPROACH
  - OD-HANDOFF
  - OD-INTEGRATION

must_haves:
  truths:
    - "Operational Approach section synthesizes problem framing, CoG analysis, and LOEs into narrative"
    - "Push to Plan Tab button packages outputs as DesignHandoffPayload"
    - "Overview dashboard shows live status and key outputs from each section"
    - "Full Design tab workflow is navigable from overview through all sections"
    - "All sections save and load data correctly across page refresh"
  artifacts:
    - path: "frontend/src/components/design/OperationalApproachSection.tsx"
      provides: "Synthesis view with narrative editor and handoff button"
      min_lines: 100
    - path: "frontend/src/components/design/DesignOverview.tsx"
      provides: "Updated overview with live section summaries"
  key_links:
    - from: "frontend/src/components/design/OperationalApproachSection.tsx"
      to: "frontend/src/lib/design-service.ts"
      via: "getHandoff for packaging design outputs"
      pattern: "designService\\.getHandoff"
    - from: "frontend/src/components/design/DesignOverview.tsx"
      to: "DesignTab state"
      via: "reads designData to show live summaries"
      pattern: "designData\\."
---

<objective>
Build the Operational Approach synthesis section with narrative editor and Design-to-Plan handoff, update Overview dashboard with live section summaries, and verify the complete Design tab workflow end-to-end.

Purpose: This completes the operational design workflow — from problem framing through CoG analysis and LOEs to synthesized operational approach. The handoff mechanism bridges Design and Plan tabs.
Output: Complete Design tab workspace with all 5 sections functional, overview dashboard with live data, and Design-to-Plan handoff.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-operational-design-workspace-inserted/25-CONTEXT.md
@.planning/phases/25-operational-design-workspace-inserted/25-RESEARCH.md
@.planning/phases/25-operational-design-workspace-inserted/25-01-SUMMARY.md
@.planning/phases/25-operational-design-workspace-inserted/25-02-SUMMARY.md
@.planning/phases/25-operational-design-workspace-inserted/25-03-SUMMARY.md
@.planning/phases/25-operational-design-workspace-inserted/25-04-SUMMARY.md

<interfaces>
<!-- From Plan 01 types -->

From backend/src/design/types.ts:
```typescript
interface OperationalApproach {
  phases: Array<{ id: string; name: string; description: string; order: number }>;
  transitions: Array<{ fromPhaseId: string; toPhaseId: string; conditions: string[] }>;
  decisionPoints: Array<{ id: string; label: string; phaseId: string; criteria: string[] }>;
  narrative: string;
}

interface DesignHandoffPayload {
  problemStatement: string;
  cogAnalysis: CoGAnalysis;
  linesOfEffort: LineOfEffort[];
  phases: OperationalApproach['phases'];
  objectives: string[];
  assumptions: string[];
  constraints: string[];
}

interface OperationalDesign {
  id: string;
  problemSetId: string;
  problemFraming: ProblemFramingData;
  cogAnalysis: CoGAnalysis;
  linesOfEffort: LineOfEffort[];
  operationalApproach: OperationalApproach;
  status: DesignStatus;
  createdBy: string;
  createdAt: string;
  updatedAt: string;
}
```

From frontend/src/lib/design-service.ts:
```typescript
export const designService = {
  getDesign(problemSetId: string): Promise<OperationalDesign>,
  updateSection(problemSetId: string, section: string, data: unknown): Promise<OperationalDesign>,
  getHandoff(problemSetId: string): Promise<DesignHandoffPayload>,
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Operational Approach section with narrative and handoff</name>
  <files>frontend/src/components/design/OperationalApproachSection.tsx, frontend/src/components/tabs/DesignTab.tsx</files>
  <action>
  **1. Create `frontend/src/components/design/OperationalApproachSection.tsx`**:
  - Synthesis section that brings together all prior design work.
  - Props: `{ problemSetId: string; initialData: OperationalApproach; designData: OperationalDesign; onUpdate: (data: OperationalApproach) => void }`.
  - State: local operationalApproach state, AI panel open/closed, handoff state (idle/packaging/ready/pushed).

  Layout (vertical stack):

  **Summary Cards Row** (read-only synthesis from prior sections):
  - Problem Statement card: shows designData.problemFraming.problemStatement (read-only, link to "Edit in Problem Framing" that navigates to that section).
  - CoG Summary card: "Friendly CoG: [label]" and "Adversary CoG: [label]" extracted from designData.cogAnalysis (read-only).
  - LOE Summary card: count of LOEs, count of decisive points, count of CV links (read-only).
  - These cards show the inputs to operational approach synthesis.

  **Phase Transitions** (editable):
  - List of transitions between phases. Each transition has: fromPhase dropdown, toPhase dropdown, conditions (dynamic text list).
  - Phases come from designData.linesOfEffort phase structure OR from the operationalApproach.phases if already defined.
  - "Add Transition" button.

  **Decision Points** (editable):
  - List of decision points. Each has: label text input, phase dropdown, criteria (dynamic text list).
  - "Add Decision Point" button.

  **Operational Narrative** (editable):
  - Large textarea for the synthesized operational approach narrative.
  - Placeholder: "Describe the overall operational approach — how the lines of effort combine to achieve the desired end state through the identified phases..."
  - Auto-save with 2-second debounce.

  **Design-to-Plan Handoff** (bottom of section):
  - Prominent "Push to Plan Tab" button (bg-blue-600 hover:bg-blue-700, large, full-width).
  - Enabled only when all sections have status 'complete' or 'in-progress' (not all 'not-started').
  - When clicked:
    1. Call `designService.getHandoff(problemSetId)` to get packaged payload.
    2. Store payload in a well-known location — for Phase 25, store as a JSONB field on the operational_designs table by calling a new endpoint `POST /api/design/:problemSetId/push-handoff`. This endpoint stores the handoff payload and sets a `handoff_pushed_at` timestamp.
    3. Show success message: "Design outputs pushed to Plan tab. Switch to the Plan tab to begin structured planning."
    4. Update button to show "Pushed" state with timestamp.
  - The Plan tab will read this handoff in a future phase. For now, the data is persisted and available.

  Auto-save: all editable fields (transitions, decision points, narrative) auto-save with 2-second debounce via onUpdate callback.

  **2. Add handoff endpoint to backend**:
  - Add `POST /api/design/:problemSetId/push-handoff` to `backend/src/api/design.ts`.
  - Calls `designStore.getHandoffPayload(problemSetId)` and stores result in a new `handoff_payload JSONB` column and `handoff_pushed_at TIMESTAMPTZ` column on operational_designs table.
  - Update design-store.ts initDesignTable to include these columns (use `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` pattern or add to CREATE TABLE IF NOT EXISTS).

  **3. Wire OperationalApproachSection into DesignTab.tsx**:
  - Replace the operational-approach placeholder div with `<OperationalApproachSection>`.
  - Pass full designData for synthesis cards, initialData for editable fields, problemSetId, and onUpdate callback.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit --project backend/tsconfig.json 2>&1 | head -20 && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Operational Approach section renders with synthesis summary cards, editable transitions/decision points/narrative, and Push to Plan Tab button. Handoff payload persists in database. Button shows pushed state after successful push.</done>
</task>

<task type="auto">
  <name>Task 2: Update Overview dashboard with live section summaries</name>
  <files>frontend/src/components/design/DesignOverview.tsx</files>
  <action>
  Update DesignOverview.tsx (created in Plan 01) to show live data from each section:

  **Problem Framing card**: If data exists, show truncated problem statement (first 100 chars), count of assumptions and constraints. Show "No problem framing yet" if empty.

  **CoG Analysis card**: If data exists, show friendly CoG label and adversary CoG label. Show count of total nodes across both trees. Show "No CoG analysis yet" if both trees are empty.

  **Lines of Effort card**: If data exists, show count of LOEs, total decisive points, count of CV-linked decisive points vs total. Show "No lines of effort yet" if empty.

  **Operational Approach card**: If data exists, show count of phases, transitions, decision points, and whether narrative is written. Show "No operational approach yet" if empty.

  Each card's status badge now reflects real data from designData.status.

  Add a "Design Progress" summary bar at the top showing: X/4 sections complete, with a simple progress indicator (colored segments).

  The "Go to Section" buttons on each card call onNavigate to switch the DesignTab to that section.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Overview dashboard shows live summaries from all 4 sections with accurate status badges. Progress bar shows completion state. Navigation buttons work.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Operational Design workspace in the Design tab with 5 sections: Overview dashboard, Problem Framing (with AI panel), CoG Analysis (with interactive SVG trees), Lines of Effort (with timeline visualization), and Operational Approach (with Design-to-Plan handoff).</what-built>
  <how-to-verify>
    1. Navigate to any problem set and click the "Design" tab
    2. Verify the sidebar shows 5 items with status badges (all "Not Started" initially)
    3. Click "Overview" — verify dashboard shows 4 section cards with descriptions
    4. Click "Problem Framing" — fill out Current State and Desired End State, verify Problem Statement auto-generates
    5. Add tensions, obstacles, assumptions — verify auto-save indicator shows "Saving..."/"Saved"
    6. Open AI panel, click "Analyze" — verify framing cards appear with confidence badges
    7. Test "Adopt" on a framing card — verify form updates
    8. Click "CoG Analysis" — verify two tree areas (Friendly/Adversary) side by side
    9. Click "add" to create a root CG node, add children (CC, CR, CV)
    10. Click a node to edit its label — verify editor opens
    11. Verify node colors match type (red CG, amber CC, blue CR, green CV)
    12. Click "Lines of Effort" — verify timeline with phase columns
    13. Add an LOE, add decisive points in different phases
    14. Link a decisive point to a CoG vulnerability — verify green indicator appears
    15. Click "Operational Approach" — verify summary cards show data from prior sections
    16. Write a narrative, click "Push to Plan Tab" — verify success message
    17. Return to "Overview" — verify status badges updated and section summaries populated
    18. Refresh the page — verify all data persists
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Complete Design tab workflow from overview through all 4 sections
- All data persists across page refresh
- Status badges update correctly per section completion state
- Push to Plan Tab packages and stores handoff payload
- AI panel works in Problem Framing section
- CoG trees render and edit correctly
- LOE timeline renders with phases and decisive points
- No console errors during normal usage
</verification>

<success_criteria>
- All 5 Design tab sections functional (Overview, Problem Framing, CoG Analysis, LOEs, Operational Approach)
- Complete doctrinal workflow: frame problem, analyze CoG, define LOEs, synthesize approach, handoff to Plan
- AI assistant provides alternative framings with Adopt/Merge/Dismiss
- Interactive SVG visualizations for CoG trees and LOE timeline
- Design-to-Plan handoff persists packaged outputs
- All data saved to and loaded from PostgreSQL
- Human verification confirms visual and functional quality
</success_criteria>

<output>
After completion, create `.planning/phases/25-operational-design-workspace-inserted/25-05-SUMMARY.md`
</output>
