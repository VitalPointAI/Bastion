---
phase: 34-plan-tab-echelon-routing-mdmp-tactical-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/tabs/TabLayout.tsx
  - frontend/src/components/tabs/TabLayout.css
  - frontend/src/components/tabs/PlanTab.tsx
  - frontend/src/components/plan/PlanEchelonRouter.tsx
  - frontend/src/components/plan/EchelonBadge.tsx
  - frontend/src/components/plan/PlanEmptyState.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Plan tab auto-routes to the correct planning workflow based on problem set echelon"
    - "Echelon badge displays above sidebar nav showing TACTICAL-MDMP, OPERATIONAL-JPP, or STRATEGIC-Strategic Guidance"
    - "Strategic problem sets see DoctrinalPlaceholder with Phase 36 description"
    - "Problem sets without a workflow instance see empty state card with Start Planning button"
    - "Operational problem sets continue to see existing JPP sidebar workflow unchanged"
  artifacts:
    - path: "frontend/src/components/plan/PlanEchelonRouter.tsx"
      provides: "Echelon-based conditional rendering of JPP, MDMP, or strategic views"
      exports: ["PlanEchelonRouter"]
    - path: "frontend/src/components/plan/EchelonBadge.tsx"
      provides: "Echelon header badge component"
      exports: ["EchelonBadge"]
    - path: "frontend/src/components/plan/PlanEmptyState.tsx"
      provides: "Empty state card with Start Planning CTA"
      exports: ["PlanEmptyState"]
    - path: "frontend/src/components/tabs/TabLayout.tsx"
      provides: "Extended TabLayout with header slot for echelon badge"
      exports: ["TabLayout", "SidebarItem", "TabLayoutProps"]
  key_links:
    - from: "frontend/src/components/tabs/PlanTab.tsx"
      to: "frontend/src/components/plan/PlanEchelonRouter.tsx"
      via: "PlanTab delegates to PlanEchelonRouter"
      pattern: "PlanEchelonRouter"
    - from: "frontend/src/components/plan/PlanEchelonRouter.tsx"
      to: "frontend/src/context/ProblemSetContext.tsx"
      via: "useProblemSet().activeProblemSet.echelon"
      pattern: "useProblemSet.*echelon"
    - from: "frontend/src/components/plan/EchelonBadge.tsx"
      to: "frontend/src/components/tabs/TabLayout.tsx"
      via: "header prop slot in TabLayout"
      pattern: "header.*EchelonBadge"
---

<objective>
Build the echelon routing infrastructure for the Plan tab. PlanTab delegates to PlanEchelonRouter which reads echelon from ProblemSetContext and renders JPP (operational), MDMP placeholder (tactical — full implementation in Plan 02), or DoctrinalPlaceholder (strategic). Extend TabLayout with a header slot for the echelon badge. Create shared PlanEmptyState component for workflow-not-started states.

Purpose: Transforms PlanTab from a hardcoded JPP view into an echelon-aware routing shell that selects the appropriate planning workflow. This is the structural foundation for Plan 02 (MDMP sidebar) and future Phase 36 (strategic workflow).

Output: PlanEchelonRouter, EchelonBadge, PlanEmptyState components; TabLayout header slot; PlanTab refactored to delegate.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-plan-tab-echelon-routing-mdmp-tactical-wiring/34-CONTEXT.md
@.planning/phases/34-plan-tab-echelon-routing-mdmp-tactical-wiring/34-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/components/tabs/TabLayout.tsx:
```typescript
export interface SidebarItem {
  id: string;
  label: string;
  tooltip?: string;
  status?: 'not-started' | 'in-progress' | 'complete';
}

export interface TabLayoutProps {
  items: SidebarItem[];
  selectedItem: string;
  onSelectItem: (id: string) => void;
  children: React.ReactNode;
  decisionHistory?: ReactNode;
}
```

From frontend/src/lib/problem-set-service.ts:
```typescript
export interface ProblemSetDetail {
  id: string;
  daoId: string;
  name: string;
  description: string | null;
  echelon: 'strategic' | 'operational' | 'tactical';
  // ... other fields
}
```

From frontend/src/context/ProblemSetContext.tsx:
```typescript
// Access via: const { activeProblemSet } = useProblemSet();
// activeProblemSet?.echelon gives 'strategic' | 'operational' | 'tactical'
```

From frontend/src/components/tabs/PlanTab.tsx:
```typescript
interface PlanTabProps {
  problemSetId: string;
  daoId?: string;
}
// Currently renders JPP sidebar directly with TabLayout
```

From frontend/src/components/tabs/DoctrinalPlaceholder.tsx:
```typescript
interface DoctrinalPlaceholderProps {
  tabId: string;
  tabName: string;
  description: string;
  futureContent: string;
  deliveredBy: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend TabLayout with header slot and create EchelonBadge + PlanEmptyState components</name>
  <files>
    frontend/src/components/tabs/TabLayout.tsx
    frontend/src/components/tabs/TabLayout.css
    frontend/src/components/plan/EchelonBadge.tsx
    frontend/src/components/plan/PlanEmptyState.tsx
  </files>
  <action>
1. **TabLayout.tsx** — Add optional `header?: ReactNode` prop to `TabLayoutProps`. Render `{header}` inside the sidebar `<aside>`, after the toggle button and before the `<nav>`. Only render when `sidebarOpen` is true. This is backward-compatible — existing TabLayout consumers that don't pass `header` see no change.

2. **TabLayout.css** — Add `.sidebar-header` class with `flex-shrink: 0; padding: 0.5rem;` for the header slot. No other CSS changes needed.

3. **EchelonBadge.tsx** — Create at `frontend/src/components/plan/EchelonBadge.tsx`:
   - Props: `echelon: 'strategic' | 'operational' | 'tactical'`
   - Const config object mapping each echelon to label, workflow name, and Tailwind color class:
     - tactical: label "TACTICAL", workflow "MDMP", color `bg-amber-600`
     - operational: label "OPERATIONAL", workflow "JPP", color `bg-blue-600`
     - strategic: label "STRATEGIC", workflow "Strategic Guidance", color `bg-purple-600`
   - Renders a compact badge: `<div className={config.color + ' px-3 py-1.5 rounded-md'}>` with `<span>` for label (bold, tracking-wider, white) and `<span>` for `- {workflow}` (white/70 opacity).
   - Export `EchelonBadge` and `ECHELON_CONFIG` (the config object, useful for empty state messaging).

4. **PlanEmptyState.tsx** — Create at `frontend/src/components/plan/PlanEmptyState.tsx`:
   - Props: `workflowName: string`, `onStartPlanning: () => void`, `loading?: boolean`
   - Renders a centered card (min-h-[40vh], bg-gray-800, border-gray-700, rounded-lg, p-8, max-w-md):
     - Heading: "No {workflowName} Started"
     - Description: "Begin the {workflowName} planning workflow for this problem set."
     - Blue "Start Planning" button calling `onStartPlanning`, disabled when `loading` (shows "Starting...")
   - Export `PlanEmptyState`.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>TabLayout accepts optional header prop (backward-compatible). EchelonBadge renders echelon label + workflow name with color coding. PlanEmptyState renders centered card with Start Planning CTA. All compile without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create PlanEchelonRouter and refactor PlanTab to delegate</name>
  <files>
    frontend/src/components/plan/PlanEchelonRouter.tsx
    frontend/src/components/tabs/PlanTab.tsx
  </files>
  <action>
1. **PlanEchelonRouter.tsx** — Create at `frontend/src/components/plan/PlanEchelonRouter.tsx`:
   - Props: `problemSetId: string`, `daoId?: string`
   - Import `useProblemSet` from context, `EchelonBadge` from `./EchelonBadge`, `DoctrinalPlaceholder` from `../tabs/DoctrinalPlaceholder`
   - Read echelon: `const { activeProblemSet } = useProblemSet(); const echelon = activeProblemSet?.echelon ?? 'operational';`
   - Conditional rendering based on echelon:
     - `'operational'`: Render the existing JPP plan view. Move ALL the current PlanTab logic (JPPView type, buildJPPItems, JPP instance loading, TabLayout with JPP sidebar items) into a local `JPPPlanView` component within this file. Pass `<EchelonBadge echelon="operational" />` as the `header` prop to TabLayout.
     - `'tactical'`: For now (Plan 02 will replace), render `<TabLayout items={[]} selectedItem="" onSelectItem={() => {}} header={<EchelonBadge echelon="tactical" />}><div className="p-4 text-gray-400">MDMP tactical workflow — Plan 02</div></TabLayout>`. This is a temporary placeholder that Plan 02 will replace with the full MDMPPlanView.
     - `'strategic'`: Render `<EchelonBadge echelon="strategic" />` followed by `<DoctrinalPlaceholder tabId="plan" tabName="Strategic Guidance" description="Strategic-level planning workflow for directive development and force apportionment." futureContent="Objective setting, force apportionment, constraint definition, directive drafting. Strategic directives become initiating directives for child campaign JPP." deliveredBy="Phase 36" />`
   - Export `PlanEchelonRouter`.

2. **JPPPlanView** (inside PlanEchelonRouter.tsx):
   - Move the entire body of the current `PlanTab` component into this new component: the `JPPView` type, `mapStatusToBadge`, `buildJPPItems`, state management (jppInstance, loading, error), `loadJPPInstance` callback, and the `TabLayout` rendering with all JPP step content panels.
   - Add `header={<EchelonBadge echelon="operational" />}` to the TabLayout props.
   - The loading and error states should also include the echelon badge (wrap them in a fragment with the badge).
   - Import `PlanEmptyState` from `./PlanEmptyState`. If `jppInstance` is null after loading completes (and no error), show `<PlanEmptyState workflowName="JPP" onStartPlanning={() => jppService.createInstance(problemSetId).then(loadJPPInstance)} />` instead of the current error fallback. Keep the error state as-is for actual errors.

3. **PlanTab.tsx** — Gut the existing implementation. Replace with a thin wrapper:
   ```typescript
   import { PlanEchelonRouter } from '../plan/PlanEchelonRouter';

   interface PlanTabProps {
     problemSetId: string;
     daoId?: string;
   }

   export function PlanTab({ problemSetId, daoId }: PlanTabProps) {
     return <PlanEchelonRouter problemSetId={problemSetId} daoId={daoId} />;
   }
   ```
   This preserves the import contract from ProblemSetTabContainer.tsx (which imports PlanTab) while delegating all logic to PlanEchelonRouter.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>PlanTab delegates to PlanEchelonRouter. Operational echelon shows existing JPP sidebar with echelon badge. Strategic echelon shows DoctrinalPlaceholder with Phase 36 description. Tactical echelon shows temporary placeholder (replaced by Plan 02). All existing PlanTab functionality preserved. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes with no errors: `npx tsc --noEmit --project frontend/tsconfig.json`
2. ProblemSetTabContainer.tsx import of PlanTab still works (export signature unchanged)
3. TabLayout header prop is optional — existing TabLayout consumers (DesignTab, DirectTab, etc.) unaffected
4. EchelonBadge renders three distinct configurations for tactical/operational/strategic
</verification>

<success_criteria>
- Plan tab routes to JPP view for operational echelon (default behavior preserved)
- Plan tab routes to DoctrinalPlaceholder for strategic echelon
- Plan tab routes to placeholder for tactical echelon (Plan 02 replaces)
- Echelon badge renders above sidebar nav in all three views
- Empty state with Start Planning button shows when no workflow instance exists
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/34-plan-tab-echelon-routing-mdmp-tactical-wiring/34-01-SUMMARY.md`
</output>
