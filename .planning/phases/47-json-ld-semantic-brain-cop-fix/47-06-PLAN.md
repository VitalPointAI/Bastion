---
phase: 47-json-ld-semantic-brain-cop-fix
plan: 06
type: execute
wave: 3
depends_on: ["47-01", "47-03"]
files_modified:
  - backend/src/cop/agents/semantic-entity-query.ts
  - backend/src/cop/agents/cop-coordinator.ts
  - backend/src/cop/agents/layer-sub-agents/sub-agent-types.ts
  - backend/src/cop/index.ts
  - backend/src/cop/api/cop-handlers.ts
autonomous: true
requirements:
  - COP-01
  - COP-02

must_haves:
  truths:
    - "COP coordinator fetches entities via semantic query module (not actorStore.listActors)"
    - "Sub-agents receive SemanticEntity[] with jsonldType, confidence, provenance"
    - "Each sub-agent can filter entities by CCO/BFO type and affiliation"
    - "COP pipeline generates non-empty layer symbols from JSON-LD graph data"
  artifacts:
    - path: "backend/src/cop/agents/semantic-entity-query.ts"
      provides: "Shared semantic entity fetcher for all sub-agents"
      exports: ["fetchSemanticEntities"]
    - path: "backend/src/cop/agents/cop-coordinator.ts"
      provides: "Updated coordinator using semantic queries"
      contains: "fetchSemanticEntities"
    - path: "backend/src/cop/agents/layer-sub-agents/sub-agent-types.ts"
      provides: "Updated SubAgentInput with SemanticEntity type"
      contains: "SemanticEntity"
  key_links:
    - from: "backend/src/cop/agents/cop-coordinator.ts"
      to: "backend/src/cop/agents/semantic-entity-query.ts"
      via: "imports fetchSemanticEntities"
      pattern: "import.*semantic-entity-query"
    - from: "backend/src/cop/agents/semantic-entity-query.ts"
      to: "backend/src/graph/neo4j-client.ts"
      via: "executes ontology-typed Cypher queries"
      pattern: "executeReadQuery.*jsonldType"
    - from: "backend/src/cop/index.ts"
      to: "backend/src/cop/agents/semantic-entity-query.ts"
      via: "replaces actorStore.listActors with fetchSemanticEntities"
      pattern: "fetchSemanticEntities"
---

<objective>
Create the semantic entity query module, update the COP coordinator to use it, and update SubAgentInput types. This is the core COP pipeline fix — replacing flat actorStore queries with ontology-typed semantic queries that pass provenance and confidence to sub-agents.

Purpose: The COP pipeline is currently broken (per CONTEXT.md). The root cause is likely the coordinator's entity fetch path. This plan creates the new semantic query layer and wires it into the coordinator, fixing the data flow from graph to sub-agents.
Output: semantic-entity-query.ts, updated cop-coordinator.ts, updated sub-agent-types.ts, updated cop/index.ts + cop-handlers.ts
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-json-ld-semantic-brain-cop-fix/47-CONTEXT.md
@.planning/phases/47-json-ld-semantic-brain-cop-fix/47-RESEARCH.md
@.planning/phases/47-json-ld-semantic-brain-cop-fix/47-01-SUMMARY.md
@.planning/phases/47-json-ld-semantic-brain-cop-fix/47-03-SUMMARY.md
@backend/src/cop/agents/cop-coordinator.ts
@backend/src/cop/agents/layer-sub-agents/sub-agent-types.ts
@backend/src/cop/index.ts
@backend/src/cop/api/cop-handlers.ts

<interfaces>
From backend/src/graph/provenance-types.ts (created in Plan 01):
```typescript
export interface SemanticEntity {
  id: string;
  name: string;
  jsonldType: string;
  confidence: number;
  provenance: ProvenanceProps;
  temporalValid: boolean;
  properties: Record<string, unknown>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create semantic entity query module + update SubAgentInput</name>
  <files>backend/src/cop/agents/semantic-entity-query.ts, backend/src/cop/agents/layer-sub-agents/sub-agent-types.ts</files>
  <action>
1. Create `backend/src/cop/agents/semantic-entity-query.ts`:
   - Import `executeReadQuery` from `../../graph/neo4j-client.js`
   - Import `SemanticEntity` from `../../graph/provenance-types.js`
   - `fetchSemanticEntities(workspaceId: string, typeFilters: string[], affiliationFilters?: string[]): Promise<SemanticEntity[]>`:
     - Cypher query per RESEARCH Pattern 8:
       ```
       MATCH (a:Actor {workspaceId: $workspaceId})
       WHERE a.jsonldType IN $typeFilters
         AND (a.validTo IS NULL OR a.validTo > datetime())
         ${affiliationFilters ? "AND a.attributes_affiliation IN $affiliations" : ""}
       WITH a,
         duration.between(datetime(a.updatedAt), datetime()).days AS ageDays
       RETURN a,
         a.confidence * (0.5 ^ (toFloat(ageDays) / a.halfLifeDays)) AS currentConf
       ORDER BY currentConf DESC
       ```
     - Map results to SemanticEntity array (parse provenance fields, compute temporalValid)
   - `fetchAllSemanticEntities(workspaceId: string): Promise<SemanticEntity[]>`:
     - Same query without typeFilters/affiliationFilters — returns all temporally valid entities
     - Used by coordinator when no specific filter needed
   - Handle edge cases: null halfLifeDays (default 180), null confidence (default 0.5), null updatedAt (use createdAt)

2. Update `backend/src/cop/agents/layer-sub-agents/sub-agent-types.ts`:
   - Import `SemanticEntity` from `../../../graph/provenance-types.js`
   - Change `graphEntities` type from `Array<{ id, name, type, properties }>` to `SemanticEntity[]`
   - Keep all other fields in SubAgentInput unchanged
   - Add backward-compatible type alias: `export type LegacyGraphEntity = { id: string; name: string; type: string; properties: Record<string, unknown> }`
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit backend/src/cop/agents/semantic-entity-query.ts backend/src/cop/agents/layer-sub-agents/sub-agent-types.ts 2>&1 | head -20</automated>
  </verify>
  <done>semantic-entity-query.ts exports fetchSemanticEntities (filtered) and fetchAllSemanticEntities (all). SubAgentInput.graphEntities typed as SemanticEntity[] with jsonldType, confidence, provenance.</done>
</task>

<task type="auto">
  <name>Task 2: Wire coordinator + COP index + handlers to use semantic queries</name>
  <files>backend/src/cop/agents/cop-coordinator.ts, backend/src/cop/index.ts, backend/src/cop/api/cop-handlers.ts</files>
  <action>
1. Update `backend/src/cop/agents/cop-coordinator.ts`:
   - The coordinator currently receives `graphEntities` in its state annotation (line ~85)
   - Update the state annotation type to use `SemanticEntity[]`
   - In the node that passes entities to sub-agents (line ~143), ensure `state.graphEntities` is passed through as-is (it's already SemanticEntity[] from the caller)
   - Add structured logging at the entity fetch boundary: `console.log('[COP] Coordinator received ${state.graphEntities.length} semantic entities')`
   - Add logging before each sub-agent invocation: `console.log('[COP] Invoking ${agentId} with ${entities.length} entities')`
   - These logs are the diagnostic trace recommended in RESEARCH open question 2

2. Update `backend/src/cop/index.ts`:
   - Find the `actorStore.listActors()` call (line ~128) that builds graphEntities
   - Replace with: `import { fetchAllSemanticEntities } from './agents/semantic-entity-query.js'`
   - Replace the `.map()` transformation with: `graphEntities = await fetchAllSemanticEntities(data.workspaceId)`
   - Remove the old `actorStore` import if no longer used elsewhere in this file
   - Keep all other event bus handlers, trigger handlers, and initialization code unchanged

3. Update `backend/src/cop/api/cop-handlers.ts`:
   - Find the `actorStore.listActors()` call (line ~542) that builds graphEntities for the API handler
   - Replace with same pattern: `import { fetchAllSemanticEntities }` and use it
   - Remove the old dynamic `import('../../graph/raft/actor-store.js')` if no longer needed
   - Keep all other handler logic unchanged
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit backend/src/cop/agents/cop-coordinator.ts backend/src/cop/index.ts backend/src/cop/api/cop-handlers.ts 2>&1 | head -20</automated>
  </verify>
  <done>COP coordinator receives SemanticEntity[] with provenance and confidence. cop/index.ts and cop-handlers.ts use fetchAllSemanticEntities instead of actorStore.listActors. Diagnostic logging added at coordinator entity boundaries.</done>
</task>

</tasks>

<verification>
- All files compile without TypeScript errors
- No remaining references to `actorStore.listActors` in COP module (except possibly tests)
- SubAgentInput.graphEntities is typed as SemanticEntity[]
- Coordinator logs entity count at fetch boundary
</verification>

<success_criteria>
- Semantic entity query module returns typed entities with confidence decay from Neo4j
- COP coordinator receives SemanticEntity[] instead of flat entity objects
- Both COP entry points (event bus trigger in index.ts, API handler in cop-handlers.ts) use fetchAllSemanticEntities
- Diagnostic logging traces entity flow through coordinator
</success_criteria>

<output>
After completion, create `.planning/phases/47-json-ld-semantic-brain-cop-fix/47-06-SUMMARY.md`
</output>
