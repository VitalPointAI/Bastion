---
phase: 47-json-ld-semantic-brain-cop-fix
plan: 10
type: execute
wave: 5
depends_on: ["47-03", "47-07"]
files_modified:
  - backend/src/api/graph.ts
  - backend/src/graph/tools/raft-tools.ts
  - backend/src/graph/tools/entity-tools.ts
  - backend/src/graph/problem-set/aggregation-service.ts
  - frontend/src/components/cop/COPMapView.tsx
  - frontend/src/components/cop/COPLayerControls.tsx
  - frontend/src/components/cop/COPEntityTooltip.tsx
autonomous: true
requirements:
  - WIRE-01
  - COP-03

must_haves:
  truths:
    - "Graph API endpoints return JSON-LD formatted entities with provenance and temporal fields"
    - "RAFT tools (used by LangGraph agents) pass JSON-LD fields through"
    - "Aggregation service handles JSON-LD entities for problem set summaries"
    - "COP map view applies confidence visual encoding (solid/dashed/ghost)"
    - "COP layer controls include confidence threshold slider"
  artifacts:
    - path: "backend/src/api/graph.ts"
      provides: "Graph API returning JSON-LD entities"
      contains: "jsonldType"
    - path: "backend/src/graph/tools/raft-tools.ts"
      provides: "LangGraph tools handling JSON-LD entities"
      contains: "jsonldType"
    - path: "frontend/src/components/cop/COPMapView.tsx"
      provides: "COP map with confidence visual encoding on symbols"
      contains: "confidenceTier"
    - path: "frontend/src/components/cop/COPLayerControls.tsx"
      provides: "COP controls with confidence threshold slider"
      contains: "confidenceThreshold"
  key_links:
    - from: "backend/src/api/graph.ts"
      to: "backend/src/graph/raft/actor-store.ts"
      via: "returns JSON-LD formatted actor data"
      pattern: "jsonldType|provenance"
    - from: "frontend/src/components/cop/COPMapView.tsx"
      to: "frontend/src/types/cop.ts"
      via: "reads confidenceTier from symbol spec"
      pattern: "confidenceTier|opacity"
---

<objective>
Wire remaining downstream consumers to the JSON-LD graph: graph API endpoints, LangGraph RAFT tools, aggregation service, and COP confidence visual rendering. This completes the "all consumers wired" requirement.

Purpose: Every consumer of graph data must understand the new JSON-LD format. API responses carry provenance, tools pass semantic entities to AI agents, and COP renders confidence tiers visually.
Output: Updated graph API, RAFT tools, entity tools, aggregation service, COP map view + controls
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-json-ld-semantic-brain-cop-fix/47-CONTEXT.md
@.planning/phases/47-json-ld-semantic-brain-cop-fix/47-RESEARCH.md
@.planning/phases/47-json-ld-semantic-brain-cop-fix/47-01-SUMMARY.md
@.planning/phases/47-json-ld-semantic-brain-cop-fix/47-07-SUMMARY.md
@backend/src/api/graph.ts
@backend/src/graph/tools/raft-tools.ts
@backend/src/graph/tools/entity-tools.ts
@backend/src/graph/problem-set/aggregation-service.ts
@frontend/src/components/cop/COPMapView.tsx
@frontend/src/components/cop/COPLayerControls.tsx
@frontend/src/components/cop/COPEntityTooltip.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update graph API + RAFT tools + aggregation service</name>
  <files>backend/src/api/graph.ts, backend/src/graph/tools/raft-tools.ts, backend/src/graph/tools/entity-tools.ts, backend/src/graph/problem-set/aggregation-service.ts</files>
  <action>
1. Update `backend/src/api/graph.ts`:
   - Graph API endpoints that return actor/relationship/tension/decision data should include new JSON-LD fields in response
   - Add `atTime` query parameter support to list endpoints: when provided, pass to store's list method for temporal filtering
   - Add `includeProvenance=true` query parameter: when true, include assertedBy, assertedVia, derivedFrom, confidence in response (default: true)
   - Response shape for entities includes: id, name, type (legacy), jsonldType (new), confidence, confidenceTier, validFrom, validTo, provenance (if requested)
   - No breaking changes to existing response format — new fields are additive

2. Update `backend/src/graph/tools/raft-tools.ts`:
   - Tools call actorStore, relationshipStore, tensionStore — these now return JSON-LD entities
   - Update tool response formatting to include jsonldType, confidence, provenance in the tool output strings that LangGraph agents read
   - When tools create entities (if applicable), pass JSON-LD fields through
   - Import types from provenance-types if needed for type safety

3. Update `backend/src/graph/tools/entity-tools.ts`:
   - Same pattern: entity tools that read/write actors should handle new fields
   - Pass jsonldType through on entity creation
   - Return provenance fields in entity query results

4. Update `backend/src/graph/problem-set/aggregation-service.ts`:
   - Aggregation queries that summarize actors/relationships should handle jsonldType grouping
   - Add capability to aggregate by ontology type (e.g., count of MilitaryOrganization vs Person vs Facility)
   - Include average confidence in aggregation results
   - Handle entities that may not yet have JSON-LD fields (migration may not have run) — use coalesce/defaults
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit backend/src/api/graph.ts backend/src/graph/tools/raft-tools.ts backend/src/graph/tools/entity-tools.ts backend/src/graph/problem-set/aggregation-service.ts 2>&1 | head -20</automated>
  </verify>
  <done>Graph API returns JSON-LD entities with optional provenance and temporal filtering. RAFT tools and entity tools pass JSON-LD fields to LangGraph agents. Aggregation service groups by ontology type and includes average confidence.</done>
</task>

<task type="auto">
  <name>Task 2: COP confidence visual encoding in map view + controls</name>
  <files>frontend/src/components/cop/COPMapView.tsx, frontend/src/components/cop/COPLayerControls.tsx, frontend/src/components/cop/COPEntityTooltip.tsx</files>
  <action>
1. Update `frontend/src/components/cop/COPMapView.tsx`:
   - This is the main COP map rendering component that displays military symbols
   - Add confidence visual encoding per locked decision:
     - **High confidence (> 0.85)**: solid symbol outlines, full opacity (1.0), no badge
     - **Medium confidence (0.5-0.85)**: dashed outlines (CSS `border-style: dashed` or SVG `strokeDasharray: '5,3'`), amber/yellow badge showing confidence %, 70% opacity
     - **Low confidence (< 0.5)**: dotted/ghost outlines (`strokeDasharray: '2,2'`), red badge showing confidence %, 40% opacity
   - Read `confidenceTier` from the symbol spec (added to COPSymbolSpec in Plan 07)
   - Apply inline styles or CSS classes based on tier
   - Filter symbols below confidence threshold (from COPLayerControls state)

2. Update `frontend/src/components/cop/COPLayerControls.tsx`:
   - Add a confidence threshold slider to the layer controls panel:
     - Range: 0 to 1.0, step 0.05
     - Label: "Confidence Filter"
     - Default: 0 (show all symbols)
     - When adjusted, symbols below threshold are hidden from the map
   - Pass threshold value up via callback or state management

3. Update `frontend/src/components/cop/COPEntityTooltip.tsx`:
   - Add provenance information to the entity tooltip/popover:
     - "Source: {assertedVia}" (formatted: "Document Intelligence", "OSINT Feed", "Manual Entry", etc.)
     - "Confidence: {confidence}%"
     - "Last assessed: {updatedAt}"
     - "Assessed by: {assertedBy}"
   - Use existing tooltip styling patterns
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit frontend/src/components/cop/COPMapView.tsx frontend/src/components/cop/COPLayerControls.tsx frontend/src/components/cop/COPEntityTooltip.tsx 2>&1 | head -20</automated>
  </verify>
  <done>COP symbols render with confidence-based visual encoding: solid (high), dashed+amber (medium), ghost+red (low). COPLayerControls has confidence threshold slider. COPEntityTooltip shows provenance, confidence %, source method.</done>
</task>

</tasks>

<verification>
- All files compile without TypeScript errors
- Graph API returns jsonldType and provenance in entity responses
- COP map view applies different visual styles based on confidenceTier
- Confidence threshold slider filters symbols
- No breaking changes to existing API consumers or COP rendering
</verification>

<success_criteria>
- Graph API endpoints return JSON-LD fields with optional provenance and temporal filtering
- RAFT tools pass semantic entity data to LangGraph agents
- Aggregation service handles ontology-typed entities
- COP symbols visually encode confidence: solid = confirmed, dashed = medium, ghost = low
- Confidence threshold slider available in COP layer controls
- Entity tooltip shows provenance information
- All 7+ downstream consumers now wired to JSON-LD graph
</success_criteria>

<output>
After completion, create `.planning/phases/47-json-ld-semantic-brain-cop-fix/47-10-SUMMARY.md`
</output>
