---
phase: 47-json-ld-semantic-brain-cop-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/cop/cco/bastion-context.jsonld
  - backend/src/cop/cco/cco-schema-loader.ts
  - backend/src/cop/cco/cco-types.ts
  - backend/src/graph/raft/types.ts
  - backend/src/graph/confidence-calculator.ts
  - backend/src/graph/provenance-types.ts
autonomous: true
requirements:
  - JSONLD-01
  - JSONLD-02
  - JSONLD-03
  - JSONLD-04
  - JSONLD-05
  - PROV-01
  - TEMP-01
  - CONF-01

must_haves:
  truths:
    - "JSON-LD context file loads at startup with all ontology namespaces (BFO, CCO, PROV-O, JC3IEDM, DODAF, APP-6)"
    - "Provenance types (assertedBy, assertedVia, derivedFrom, confidence, sourceWeight) are defined and exported"
    - "Confidence decay formula computes correctly: conf(t) = conf_0 * 2^(-t/half_life)"
    - "Multi-source fusion formula computes correctly: conf = 1 - prod(1 - w_i)"
    - "RAFT entity types include JSON-LD, provenance, and temporal fields"
  artifacts:
    - path: "backend/src/cop/cco/bastion-context.jsonld"
      provides: "Bundled JSON-LD context file with all ontology namespaces"
      contains: "@context"
    - path: "backend/src/graph/confidence-calculator.ts"
      provides: "Decay and fusion confidence utilities"
      exports: ["computeDecayedConfidence", "fuseConfidence", "SOURCE_WEIGHTS", "HALF_LIFE_DEFAULTS"]
    - path: "backend/src/graph/provenance-types.ts"
      provides: "Shared provenance + temporal + semantic entity types"
      exports: ["ProvenanceProps", "SemanticEntity", "ContradictionRecord", "SourceMethod"]
    - path: "backend/src/graph/raft/types.ts"
      provides: "Rewritten RAFT types with JSON-LD native fields"
      contains: "jsonldType"
  key_links:
    - from: "backend/src/cop/cco/cco-schema-loader.ts"
      to: "backend/src/cop/cco/bastion-context.jsonld"
      via: "JSON file import at startup"
      pattern: "bastion-context\\.jsonld"
    - from: "backend/src/graph/raft/types.ts"
      to: "backend/src/graph/provenance-types.ts"
      via: "imports ProvenanceProps, SemanticEntity"
      pattern: "import.*provenance-types"
---

<objective>
Create the foundational JSON-LD ontology context file, shared type definitions (provenance, temporal, confidence), and rewrite RAFT entity types to be JSON-LD native. This plan produces the contracts that all subsequent plans build against.

Purpose: Every downstream plan (migration, COP fix, consumer wiring) depends on having stable type definitions and the ontology context file. Creating these first prevents the scavenger-hunt anti-pattern.
Output: bastion-context.jsonld, provenance-types.ts, confidence-calculator.ts, rewritten raft/types.ts, extended cco-schema-loader.ts
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-json-ld-semantic-brain-cop-fix/47-CONTEXT.md
@.planning/phases/47-json-ld-semantic-brain-cop-fix/47-RESEARCH.md
@backend/src/cop/cco/cco-schema-loader.ts
@backend/src/cop/cco/cco-types.ts
@backend/src/graph/raft/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create JSON-LD context file + provenance/confidence utilities</name>
  <files>backend/src/cop/cco/bastion-context.jsonld, backend/src/graph/provenance-types.ts, backend/src/graph/confidence-calculator.ts</files>
  <action>
1. Create `backend/src/cop/cco/bastion-context.jsonld` — the bundled JSON-LD context file with all ontology namespaces:
   - `bfo`: `http://purl.obolibrary.org/obo/BFO_`
   - `cco`: `http://www.ontologyrepository.com/CommonCoreOntologies/`
   - `prov`: `http://www.w3.org/ns/prov#`
   - `jc3`: `http://www.mip.saic.com/jc3iedm/`
   - `dodaf`: `https://dodcio.defense.gov/ontology/dodaf/`
   - `bastion`: `https://bastion.vitalpoint.ai/ontology/`
   - `rdfs`, `xsd` standard namespaces
   - Property aliases: `jsonldType` → `@type`, `jsonldId` → `@id`, `validFrom`, `validTo`, `confidence`, `assertedBy`, `assertedVia`, `derivedFrom`, `sourceWeight`, `halfLifeDays`
   - Follow exact structure from RESEARCH.md Pattern 1

2. Create `backend/src/graph/provenance-types.ts`:
   - `SourceMethod` type: `'manual_entry' | 'doc_intelligence' | 'osint' | 'vision_pipeline' | 'ai_inference' | 'sigint'`
   - `ProvenanceProps` interface: `assertedBy: string`, `assertedVia: SourceMethod`, `derivedFrom: string` (JSON.stringify'd array), `confidence: number`, `sourceWeight: number`
   - `TemporalProps` interface: `validFrom: string`, `validTo: string | null`, `halfLifeDays: number`
   - `SemanticEntity` interface: `id, name, jsonldType, confidence, provenance: ProvenanceProps, temporalValid: boolean, properties: Record<string, unknown>`
   - `ContradictionRecord` interface: `id, entityId, propertyKey, assertionAId, assertionBId, detectedAt, resolvedAt?, resolution?: 'accept_a' | 'accept_b' | 'both_valid' | 'flagged_for_intel'`
   - `ConfidenceTier` type: `'high' | 'medium' | 'low'`
   - `getConfidenceTier(confidence: number): ConfidenceTier` — high > 0.85, medium 0.5-0.85, low < 0.5

3. Create `backend/src/graph/confidence-calculator.ts`:
   - `SOURCE_WEIGHTS: Record<SourceMethod, number>` — manual_entry: 0.95, sigint: 0.90, doc_intelligence: 0.75, vision_pipeline: 0.70, osint: 0.65, ai_inference: 0.60
   - `HALF_LIFE_DEFAULTS: Record<string, number>` — personnel: 180, capability: 365, political: 90, geographic: 1825, economic: 365
   - `computeDecayedConfidence(baseConfidence: number, lastAssertedAt: Date, halfLifeDays: number, atTime?: Date): number` — formula: `conf_0 * 2^(-ageDays/halfLife)`
   - `fuseConfidence(weights: number[]): number` — formula: `1 - prod(1 - w_i)`
   - All functions are pure (no side effects, no storage). Export everything.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit backend/src/graph/confidence-calculator.ts backend/src/graph/provenance-types.ts 2>&1 | head -20</automated>
  </verify>
  <done>bastion-context.jsonld exists with all 6 ontology namespaces + property aliases. provenance-types.ts exports ProvenanceProps, SemanticEntity, ContradictionRecord, SourceMethod, ConfidenceTier, getConfidenceTier. confidence-calculator.ts exports computeDecayedConfidence, fuseConfidence, SOURCE_WEIGHTS, HALF_LIFE_DEFAULTS.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite RAFT types + extend CCO schema loader</name>
  <files>backend/src/graph/raft/types.ts, backend/src/cop/cco/cco-schema-loader.ts, backend/src/cop/cco/cco-types.ts</files>
  <action>
1. Rewrite `backend/src/graph/raft/types.ts`:
   - Import `ProvenanceProps`, `TemporalProps` from `../provenance-types.js`
   - Add `JsonLdEntityBase` interface with: `jsonldType: string`, `jsonldContext: string`, plus spread `ProvenanceProps` and `TemporalProps` fields
   - Update `Actor` interface: extend `JsonLdEntityBase`. Keep all existing fields (id, name, workspaceId, containerIds, etc.). Add promoted attribute fields: `attributes_affiliation?: string`, `attributes_echelon?: string`, `attributes_unitType?: string`, `attributes_lat?: number`, `attributes_lng?: number`. Keep existing `attributes` JSON blob as `attributesJson?: string` for backward compat during migration.
   - Update `Relationship` interface: extend `JsonLdEntityBase`. Keep existing fields.
   - Update `Tension` interface: extend `JsonLdEntityBase`. Keep existing fields.
   - Update `Decision`/`DecisionBasis` interfaces: extend `JsonLdEntityBase`. Keep existing fields.
   - Add `ActorFunction` interface if not present: extend `JsonLdEntityBase`.
   - Keep ALL existing type exports. Do NOT remove any existing fields. Add JSON-LD fields alongside.
   - Add `ACTOR_TYPE_TO_CCO_MAP: Record<string, string>` mapping existing Actor.type values to CCO URIs: nation → cco:GovernmentOrganization, organization → cco:Organization, individual → cco:Person, non_state_actor → cco:Organization, military_unit → cco:MilitaryOrganization, facility → jc3:Facility, equipment → cco:Artifact, default → cco:Agent

2. Extend `backend/src/cop/cco/cco-schema-loader.ts`:
   - Add a second loader function `loadBastionContext()` that reads and parses `bastion-context.jsonld` from the same directory
   - Cache the parsed context in a module-level variable (same pattern as existing CCO class caching)
   - Export `getBastionContext()` accessor
   - Keep existing `loadCCOClasses()` and `getCCOClasses()` unchanged

3. Extend `backend/src/cop/cco/cco-types.ts`:
   - Add BFO base classes to the type map: `BFO_CLASSES` with key URIs (Entity, Continuant, Occurrent, Process, Quality, Role, Function, Disposition)
   - Add DODAF view types: `DODAF_VIEWS` (OV-1 through OV-6, SV-1 through SV-6)
   - Add JC3IEDM entity types: `JC3_ENTITY_TYPES` (Unit, Facility, Equipment, Feature)
   - Keep all existing exports and types unchanged
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit backend/src/graph/raft/types.ts backend/src/cop/cco/cco-schema-loader.ts backend/src/cop/cco/cco-types.ts 2>&1 | head -20</automated>
  </verify>
  <done>RAFT types include jsonldType, jsonldContext, provenance fields, temporal fields on all entity interfaces. CCO schema loader can load bastion-context.jsonld. cco-types.ts has BFO, DODAF, JC3IEDM type maps. All existing exports preserved.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all modified files
- bastion-context.jsonld is valid JSON with @context containing all 6 ontology namespaces
- All existing imports of raft/types.ts still resolve (no removed exports)
</verification>

<success_criteria>
- JSON-LD context file exists with BFO, CCO, PROV-O, JC3IEDM, DODAF, bastion namespaces
- ProvenanceProps, SemanticEntity, ContradictionRecord types exported
- Confidence calculator exports decay and fusion functions
- RAFT types (Actor, Relationship, Tension, Decision) all include JSON-LD + provenance + temporal fields
- CCO schema loader extended to load bastion-context.jsonld
</success_criteria>

<output>
After completion, create `.planning/phases/47-json-ld-semantic-brain-cop-fix/47-01-SUMMARY.md`
</output>
