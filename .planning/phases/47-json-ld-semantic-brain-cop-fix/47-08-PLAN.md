---
phase: 47-json-ld-semantic-brain-cop-fix
plan: 08
type: execute
wave: 4
depends_on: ["47-01", "47-03"]
files_modified:
  - frontend/src/components/brain/types.ts
  - frontend/src/components/brain/hooks/useBrainTimeline.ts
  - frontend/src/components/brain/hooks/useBrainData.ts
  - frontend/src/components/brain/BrainTimeline.tsx
autonomous: true
requirements:
  - TEMP-01
  - TEMP-02
  - CONTRA-01

must_haves:
  truths:
    - "Brain nodes display validFrom/validTo, assertedVia, and confidence fields"
    - "Timeline slider filters graph to assertions valid at selected point in time"
    - "Stale/expired facts fade out based on staleness decay"
    - "Animated playback auto-advances through time showing entities appearing/disappearing"
    - "Contradicted nodes show visual indicator (isContradicted)"
  artifacts:
    - path: "frontend/src/components/brain/types.ts"
      provides: "BrainNode with provenance and temporal fields"
      contains: "validFrom"
    - path: "frontend/src/components/brain/hooks/useBrainTimeline.ts"
      provides: "Timeline hook with per-assertion temporal filtering"
      contains: "validFrom"
    - path: "frontend/src/components/brain/BrainTimeline.tsx"
      provides: "Timeline slider with animated playback"
      contains: "playback"
  key_links:
    - from: "frontend/src/components/brain/hooks/useBrainTimeline.ts"
      to: "frontend/src/components/brain/types.ts"
      via: "uses BrainNode.validFrom/validTo for temporal filtering"
      pattern: "validFrom|validTo"
    - from: "frontend/src/components/brain/hooks/useBrainData.ts"
      to: "/api/brain/graph-snapshot"
      via: "fetch with atTime parameter for temporal queries"
      pattern: "graph-snapshot|atTime"
---

<objective>
Upgrade brain visualization to display JSON-LD provenance data and implement the temporal timeline slider with animated playback. Nodes show confidence, provenance, temporal validity, and contradiction status.

Purpose: The brain visualization is the primary interface for analysts to explore the knowledge graph. Temporal slider enables "watching the situation evolve" for briefings. Confidence overlay gives at-a-glance reliability.
Output: Updated BrainNode types, temporal timeline hook, timeline slider component with playback
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-json-ld-semantic-brain-cop-fix/47-CONTEXT.md
@.planning/phases/47-json-ld-semantic-brain-cop-fix/47-RESEARCH.md
@.planning/phases/47-json-ld-semantic-brain-cop-fix/47-01-SUMMARY.md
@frontend/src/components/brain/types.ts
@frontend/src/components/brain/hooks/useBrainTimeline.ts
@frontend/src/components/brain/hooks/useBrainData.ts
@frontend/src/components/brain/BrainTimeline.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update BrainNode types + useBrainData for JSON-LD graph</name>
  <files>frontend/src/components/brain/types.ts, frontend/src/components/brain/hooks/useBrainData.ts</files>
  <action>
1. Update `frontend/src/components/brain/types.ts`:
   - Add to `BrainNode` interface (per RESEARCH open question 4):
     - `validFrom?: string` — ISO datetime when this assertion became valid
     - `validTo?: string | null` — ISO datetime when assertion expired (null = current)
     - `assertedVia?: string` — source method (manual_entry, doc_intelligence, osint, etc.)
     - `assertedBy?: string` — DID of asserter
     - `isContradicted?: boolean` — true if node has unresolved :CONTRADICTS edges
     - `confidenceTier?: 'high' | 'medium' | 'low'` — computed tier for visual styling
     - `jsonldType?: string` — CCO/BFO class URI
     - `halfLifeDays?: number` — for client-side decay display
   - Keep ALL existing BrainNode fields (id, name, type, group, sourceDocumentIds, validityScore, etc.)
   - Add to `BrainEdge` interface:
     - `isContradiction?: boolean` (may already exist as `isConflict` — check and align naming)
     - `confidence?: number`

2. Update `frontend/src/components/brain/hooks/useBrainData.ts`:
   - Update the graph snapshot fetch to pass `atTime` query parameter when timeline slider is active
   - Add `atTime` to the fetch URL: `/api/brain/graph-snapshot?problemSetId=${id}&atTime=${atTime}`
   - Map response data to include new BrainNode fields (validFrom, validTo, assertedVia, isContradicted, confidenceTier, jsonldType)
   - Compute `confidenceTier` on the client: > 0.85 = 'high', 0.5-0.85 = 'medium', < 0.5 = 'low'
   - Add `atTime` as a dependency to the fetch effect (re-fetch when timeline position changes)
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit frontend/src/components/brain/types.ts frontend/src/components/brain/hooks/useBrainData.ts 2>&1 | head -20</automated>
  </verify>
  <done>BrainNode type includes validFrom, validTo, assertedVia, assertedBy, isContradicted, confidenceTier, jsonldType, halfLifeDays. useBrainData passes atTime to graph-snapshot API and maps response to new fields.</done>
</task>

<task type="auto">
  <name>Task 2: Implement temporal timeline slider with animated playback</name>
  <files>frontend/src/components/brain/hooks/useBrainTimeline.ts, frontend/src/components/brain/BrainTimeline.tsx</files>
  <action>
1. Upgrade `frontend/src/components/brain/hooks/useBrainTimeline.ts`:
   - Current hook already manages timeline state. Extend with:
   - `filterByTemporalValidity(nodes: BrainNode[], atTime: Date): BrainNode[]`:
     - Filter: node.validFrom <= atTime AND (node.validTo == null OR node.validTo > atTime)
     - For remaining nodes, compute visual opacity based on staleness decay:
       - `decayedConf = node.validityScore * Math.pow(0.5, ageDays / (node.halfLifeDays || 180))`
       - High conf (> 0.85): full opacity
       - Medium (0.5-0.85): 70% opacity
       - Low (< 0.5): 40% opacity, ghost style
   - `playbackState`: { isPlaying: boolean, speed: number, currentTime: Date }
   - `startPlayback(speed?: number)`: starts interval that advances `currentTime` (default 1x = 1 month/second)
   - `stopPlayback()`: clears interval
   - `setPlaybackSpeed(speed: number)`: 0.5x, 1x, 2x, 5x options
   - Expose `currentTime` and setter for slider position
   - Reuse the existing COP phase slider playback pattern (per CONTEXT.md)

2. Upgrade `frontend/src/components/brain/BrainTimeline.tsx`:
   - Add a temporal slider control to the brain toolbar area:
     - Date range slider: min = earliest validFrom in dataset, max = now
     - Current position indicator showing selected date
     - Play/pause button
     - Speed selector (0.5x, 1x, 2x, 5x)
   - When slider moves:
     - Call `useBrainTimeline.setCurrentTime()` which triggers `useBrainData` re-fetch with `atTime`
     - Nodes outside temporal validity fade out
     - Nodes with decayed confidence show reduced opacity
     - Contradicted nodes show red pulsing border (use CSS animation)
   - During animated playback:
     - Entities fade in at their `validFrom`
     - Entities fade out at their `validTo`
     - Confidence badges update with decay
     - Contradictions flash when detected time is reached
   - Style with Tailwind classes. Keep existing BrainTimeline functionality.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit frontend/src/components/brain/hooks/useBrainTimeline.ts frontend/src/components/brain/BrainTimeline.tsx 2>&1 | head -20</automated>
  </verify>
  <done>useBrainTimeline filters nodes by temporal validity, computes staleness-based opacity, supports playback state (play/pause/speed). BrainTimeline.tsx has date range slider, play/pause, speed selector. Nodes fade in/out by temporal validity. Contradicted nodes pulse red.</done>
</task>

</tasks>

<verification>
- All frontend files compile without TypeScript errors
- BrainNode type includes all 8 new fields
- Timeline slider renders a date range control
- Playback auto-advances time and triggers graph re-fetch
- Confidence tiers map to correct opacity levels
</verification>

<success_criteria>
- Brain visualization shows provenance (assertedVia), confidence tier, temporal validity on each node
- Timeline slider scrubs through time, filtering visible nodes by validFrom/validTo
- Animated playback shows entities appearing and fading over time
- Stale facts visually decay (reduced opacity based on half-life)
- Contradicted nodes have red pulsing indicator
</success_criteria>

<output>
After completion, create `.planning/phases/47-json-ld-semantic-brain-cop-fix/47-08-SUMMARY.md`
</output>
