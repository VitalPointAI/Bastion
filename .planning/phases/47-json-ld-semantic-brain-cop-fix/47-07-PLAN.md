---
phase: 47-json-ld-semantic-brain-cop-fix
plan: 07
type: execute
wave: 4
depends_on: ["47-06"]
files_modified:
  - backend/src/cop/agents/layer-sub-agents/intel-overlay.ts
  - backend/src/cop/agents/layer-sub-agents/force-disposition.ts
  - backend/src/cop/agents/layer-sub-agents/objectives-overlay.ts
  - backend/src/cop/agents/layer-sub-agents/c2-overlay.ts
  - backend/src/cop/agents/layer-sub-agents/control-measures.ts
  - backend/src/cop/agents/layer-sub-agents/logistics-overlay.ts
  - backend/src/cop/layers/layer-types.ts
autonomous: true
requirements:
  - COP-02
  - COP-03
  - CONF-01

must_haves:
  truths:
    - "All 6 sub-agents filter entities by jsonldType instead of flat type strings"
    - "Sub-agents pass confidence and provenance data through to COP symbols"
    - "COPSymbolSpec includes confidenceTier for visual encoding"
    - "J2 intel-overlay queries hostile/suspect/unknown entities by jsonldType"
    - "J3 force-disposition queries friendly entities by jsonldType"
  artifacts:
    - path: "backend/src/cop/agents/layer-sub-agents/intel-overlay.ts"
      provides: "J2 sub-agent with semantic entity filtering"
      contains: "jsonldType"
    - path: "backend/src/cop/agents/layer-sub-agents/force-disposition.ts"
      provides: "J3 sub-agent with semantic entity filtering"
      contains: "jsonldType"
    - path: "backend/src/cop/layers/layer-types.ts"
      provides: "COPSymbolSpec with confidence visual fields"
      contains: "confidenceTier"
  key_links:
    - from: "backend/src/cop/agents/layer-sub-agents/intel-overlay.ts"
      to: "backend/src/graph/provenance-types.ts"
      via: "uses SemanticEntity type for entity filtering"
      pattern: "SemanticEntity|jsonldType"
    - from: "backend/src/cop/layers/layer-types.ts"
      to: "backend/src/graph/provenance-types.ts"
      via: "imports ConfidenceTier"
      pattern: "confidenceTier"
---

<objective>
Upgrade all 6 COP layer sub-agents to consume SemanticEntity data (filter by jsonldType, pass confidence/provenance to symbols) and add confidence visual encoding fields to COPSymbolSpec.

Purpose: Sub-agents are the "last mile" of the COP pipeline. They receive semantic entities from the coordinator and produce COP symbols. Each must filter by ontology type and encode confidence into symbol visual properties.
Output: 6 updated sub-agent files + updated layer-types.ts
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-json-ld-semantic-brain-cop-fix/47-CONTEXT.md
@.planning/phases/47-json-ld-semantic-brain-cop-fix/47-RESEARCH.md
@.planning/phases/47-json-ld-semantic-brain-cop-fix/47-06-SUMMARY.md
@backend/src/cop/agents/layer-sub-agents/sub-agent-types.ts
@backend/src/cop/agents/layer-sub-agents/intel-overlay.ts
@backend/src/cop/agents/layer-sub-agents/force-disposition.ts
@backend/src/cop/layers/layer-types.ts

<interfaces>
From backend/src/graph/provenance-types.ts:
```typescript
export type ConfidenceTier = 'high' | 'medium' | 'low';
export function getConfidenceTier(confidence: number): ConfidenceTier;
export interface SemanticEntity {
  id: string; name: string; jsonldType: string; confidence: number;
  provenance: ProvenanceProps; temporalValid: boolean; properties: Record<string, unknown>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update layer-types + intel-overlay + force-disposition + objectives-overlay</name>
  <files>backend/src/cop/layers/layer-types.ts, backend/src/cop/agents/layer-sub-agents/intel-overlay.ts, backend/src/cop/agents/layer-sub-agents/force-disposition.ts, backend/src/cop/agents/layer-sub-agents/objectives-overlay.ts</files>
  <action>
1. Update `backend/src/cop/layers/layer-types.ts`:
   - Import `ConfidenceTier` from `../../graph/provenance-types.js`
   - Add to `COPSymbolSpec` interface:
     - `confidenceTier: ConfidenceTier` — computed from confidence score
     - `assertedVia?: string` — provenance source method
     - `provenanceSummary?: string` — human-readable provenance string for tooltips
   - Keep all existing COPSymbolSpec fields unchanged

2. Update `intel-overlay.ts` (J2 — hostile/unknown entities):
   - graphEntities is now `SemanticEntity[]` (from updated SubAgentInput)
   - Replace flat `.filter(e => e.properties.affiliation ...)` with:
     `input.graphEntities.filter(e => ['hostile', 'suspect', 'unknown'].includes(e.properties.attributes_affiliation as string))`
   - Filter by jsonldType: `e.jsonldType` in `['cco:MilitaryOrganization', 'jc3:Unit', 'jc3:Facility', 'cco:Organization']`
   - When building COP symbols from graph entities, include:
     - `confidence: entity.confidence`
     - `confidenceTier: getConfidenceTier(entity.confidence)`
     - `assertedVia: entity.provenance.assertedVia`
   - Import `getConfidenceTier` from `../../../graph/provenance-types.js`

3. Update `force-disposition.ts` (J3 — friendly forces):
   - Same pattern as intel-overlay but filter for affiliation = 'friendly'
   - Filter by jsonldType: `['cco:MilitaryOrganization', 'jc3:Unit']`
   - Include confidence fields in symbol output

4. Update `objectives-overlay.ts` (J5 — objectives):
   - Filter by jsonldType relevant to objectives (if applicable, or keep existing logic)
   - Include confidence fields in symbol output
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit backend/src/cop/layers/layer-types.ts backend/src/cop/agents/layer-sub-agents/intel-overlay.ts backend/src/cop/agents/layer-sub-agents/force-disposition.ts backend/src/cop/agents/layer-sub-agents/objectives-overlay.ts 2>&1 | head -20</automated>
  </verify>
  <done>COPSymbolSpec has confidenceTier, assertedVia, provenanceSummary fields. Intel-overlay filters by jsonldType for hostile/unknown entities. Force-disposition filters for friendly. Objectives-overlay updated. All include confidence in symbol output.</done>
</task>

<task type="auto">
  <name>Task 2: Update c2-overlay + control-measures + logistics-overlay</name>
  <files>backend/src/cop/agents/layer-sub-agents/c2-overlay.ts, backend/src/cop/agents/layer-sub-agents/control-measures.ts, backend/src/cop/agents/layer-sub-agents/logistics-overlay.ts</files>
  <action>
1. Update `c2-overlay.ts` (C2 — command structure):
   - graphEntities is now `SemanticEntity[]`
   - Update entity filtering to use `e.properties.attributes_*` (promoted attributes) instead of parsed blob
   - Filter by relevant jsonldType for C2 entities
   - Include confidence and provenance fields in symbol output
   - Import `getConfidenceTier` from `../../../graph/provenance-types.js`

2. Update `control-measures.ts`:
   - Same pattern: update filtering to use SemanticEntity properties
   - Include confidence fields in symbol output
   - Filter by relevant jsonldType if applicable

3. Update `logistics-overlay.ts`:
   - Same pattern: update filtering to use SemanticEntity properties
   - Include confidence fields in symbol output
   - Filter by relevant jsonldType if applicable (cco:Artifact for equipment, jc3:Facility for logistics nodes)

For all 3 sub-agents:
- Replace `e.properties.TYPE` access with `e.properties.attributes_TYPE` (promoted attributes)
- Add `confidenceTier: getConfidenceTier(entity.confidence)` to all symbol outputs
- Add `assertedVia: entity.provenance.assertedVia` to symbol outputs
- Keep existing document-based extraction logic unchanged (sub-agents process both documents AND graph entities)
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit backend/src/cop/agents/layer-sub-agents/c2-overlay.ts backend/src/cop/agents/layer-sub-agents/control-measures.ts backend/src/cop/agents/layer-sub-agents/logistics-overlay.ts 2>&1 | head -20</automated>
  </verify>
  <done>All 6 sub-agents updated to consume SemanticEntity[], filter by jsonldType and promoted attributes, and output confidence/provenance in COP symbols. COPSymbolSpec confidence encoding complete.</done>
</task>

</tasks>

<verification>
- All 7 files compile without TypeScript errors
- Every sub-agent includes `confidenceTier` in symbol output
- No sub-agent accesses `e.type` (old flat property) — all use `e.jsonldType` or `e.properties.attributes_*`
- Existing sub-agent tests still pass: `npm test -- --testPathPattern="sub-agents" --no-coverage`
</verification>

<success_criteria>
- All 6 sub-agents filter by jsonldType and promoted attributes
- COP symbols carry confidenceTier (high/medium/low), assertedVia, provenanceSummary
- COPSymbolSpec type definition includes new fields
- Sub-agents handle both document-based and graph-based entity sources
</success_criteria>

<output>
After completion, create `.planning/phases/47-json-ld-semantic-brain-cop-fix/47-07-SUMMARY.md`
</output>
