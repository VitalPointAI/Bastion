---
phase: 47-json-ld-semantic-brain-cop-fix
plan: 09
type: execute
wave: 5
depends_on: ["47-03", "47-05"]
files_modified:
  - backend/src/graph/construction/graph-builder.ts
  - backend/src/doc-intelligence/specialists/fact-extractor.ts
  - backend/src/osint/osint-graph-sync.ts
  - backend/src/robot/vision-cop-pipeline.ts
autonomous: true
requirements:
  - WIRE-01
  - PROV-01
  - ENTRES-01

must_haves:
  truths:
    - "Graph builder writes JSON-LD native properties on all entity extraction"
    - "Doc-intelligence fact extractor creates entities with provenance (assertedVia: doc_intelligence)"
    - "OSINT sync creates entities with provenance (assertedVia: osint)"
    - "Vision pipeline creates entities with provenance (assertedVia: vision_pipeline)"
    - "All ingestion paths trigger entity resolution after write"
    - "All ingestion paths trigger contradiction detection on property conflicts"
  artifacts:
    - path: "backend/src/graph/construction/graph-builder.ts"
      provides: "LLM entity extraction writing JSON-LD native properties"
      contains: "jsonldType"
    - path: "backend/src/doc-intelligence/specialists/fact-extractor.ts"
      provides: "Doc intelligence writing JSON-LD with doc_intelligence provenance"
      contains: "assertedVia"
    - path: "backend/src/osint/osint-graph-sync.ts"
      provides: "OSINT sync writing JSON-LD with osint provenance"
      contains: "assertedVia"
    - path: "backend/src/robot/vision-cop-pipeline.ts"
      provides: "Vision pipeline writing JSON-LD with vision_pipeline provenance"
      contains: "assertedVia"
  key_links:
    - from: "backend/src/graph/construction/graph-builder.ts"
      to: "backend/src/graph/raft/actor-store.ts"
      via: "creates actors with JSON-LD fields"
      pattern: "actorStore.*create|jsonldType"
    - from: "backend/src/graph/construction/graph-builder.ts"
      to: "backend/src/graph/resolution/resolution-service.ts"
      via: "triggers entity resolution after write"
      pattern: "resolveEntities|resolution"
    - from: "backend/src/graph/construction/graph-builder.ts"
      to: "backend/src/graph/contradiction-detector.ts"
      via: "checks for contradictions on property updates"
      pattern: "detectContradiction"
---

<objective>
Wire all ingestion pipelines (graph-builder, doc-intelligence, OSINT, vision) to write JSON-LD native properties with proper provenance. Each pipeline writes entities with its specific source method and triggers entity resolution + contradiction detection.

Purpose: Data enters the graph from 4 pathways. All must produce the same JSON-LD format with appropriate provenance so that confidence fusion and contradiction detection work across sources.
Output: 4 updated ingestion pipeline files, all writing JSON-LD with provenance
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-json-ld-semantic-brain-cop-fix/47-CONTEXT.md
@.planning/phases/47-json-ld-semantic-brain-cop-fix/47-RESEARCH.md
@.planning/phases/47-json-ld-semantic-brain-cop-fix/47-01-SUMMARY.md
@.planning/phases/47-json-ld-semantic-brain-cop-fix/47-03-SUMMARY.md
@.planning/phases/47-json-ld-semantic-brain-cop-fix/47-05-SUMMARY.md
@backend/src/graph/construction/graph-builder.ts
@backend/src/doc-intelligence/specialists/fact-extractor.ts
@backend/src/osint/osint-graph-sync.ts
@backend/src/robot/vision-cop-pipeline.ts

<interfaces>
From backend/src/graph/provenance-types.ts:
```typescript
export type SourceMethod = 'manual_entry' | 'doc_intelligence' | 'osint' | 'vision_pipeline' | 'ai_inference' | 'sigint';
export interface ProvenanceProps {
  assertedBy: string; assertedVia: SourceMethod; derivedFrom: string;
  confidence: number; sourceWeight: number;
}
```
From backend/src/graph/confidence-calculator.ts:
```typescript
export const SOURCE_WEIGHTS: Record<SourceMethod, number>;
export function fuseConfidence(weights: number[]): number;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update graph-builder + fact-extractor for JSON-LD writes</name>
  <files>backend/src/graph/construction/graph-builder.ts, backend/src/doc-intelligence/specialists/fact-extractor.ts</files>
  <action>
1. Update `backend/src/graph/construction/graph-builder.ts` (505 lines — the main LLM entity extraction pipeline):
   - Import `SOURCE_WEIGHTS` from `../confidence-calculator.js`
   - Import `ACTOR_TYPE_TO_CCO_MAP` from `../raft/types.js`
   - Import `detectContradiction` from `../contradiction-detector.js`
   - When calling `actorStore.createActor()` or `actorStore.updateActor()`, pass JSON-LD fields:
     - `jsonldType`: map from extracted entity type using ACTOR_TYPE_TO_CCO_MAP
     - `assertedVia`: pass through from caller (default 'ai_inference' for LLM extraction)
     - `assertedBy`: pass through from caller (user DID or 'system:llm-extraction')
     - `derivedFrom`: JSON.stringify([documentId]) — the source document
     - `confidence`: SOURCE_WEIGHTS[assertedVia]
     - `sourceWeight`: SOURCE_WEIGHTS[assertedVia]
     - `validFrom`: new Date().toISOString()
     - `halfLifeDays`: derive from entity type (personnel: 180, etc.) using HALF_LIFE_DEFAULTS
   - Same pattern for `relationshipStore.createRelationship()`, `tensionStore.createTension()`
   - After entity write, call entity resolution service to check for duplicates
   - On property updates (entity already exists), call `detectContradiction()` before overwriting
   - Add `assertedVia` and `assertedBy` as parameters to graph builder's main extraction function

2. Update `backend/src/doc-intelligence/specialists/fact-extractor.ts` (476 lines):
   - When calling graph builder, pass provenance:
     - `assertedVia: 'doc_intelligence'`
     - `assertedBy`: the user who uploaded the document (from request context)
     - `derivedFrom`: the document ID being processed
   - If fact-extractor creates entities directly (not through graph-builder), add JSON-LD fields inline
   - Ensure `sourceWeight: SOURCE_WEIGHTS['doc_intelligence']` (0.75) is passed
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit backend/src/graph/construction/graph-builder.ts backend/src/doc-intelligence/specialists/fact-extractor.ts 2>&1 | head -20</automated>
  </verify>
  <done>graph-builder.ts writes JSON-LD native properties with provenance on all entity creates/updates. Triggers entity resolution and contradiction detection. fact-extractor.ts passes assertedVia='doc_intelligence' with document provenance chain.</done>
</task>

<task type="auto">
  <name>Task 2: Update OSINT sync + vision pipeline for JSON-LD writes</name>
  <files>backend/src/osint/osint-graph-sync.ts, backend/src/robot/vision-cop-pipeline.ts</files>
  <action>
1. Update `backend/src/osint/osint-graph-sync.ts` (221 lines):
   - Import `SOURCE_WEIGHTS` from `../graph/confidence-calculator.js`
   - Import `ACTOR_TYPE_TO_CCO_MAP` from `../graph/raft/types.js`
   - When creating Actor nodes from OSINT data, add JSON-LD fields:
     - `jsonldType`: map from OSINT entity classification using ACTOR_TYPE_TO_CCO_MAP
     - `assertedVia: 'osint'`
     - `assertedBy: 'system:osint-feed-poller'`
     - `derivedFrom`: JSON.stringify([feedId, articleUrl]) — the OSINT source
     - `confidence: SOURCE_WEIGHTS['osint']` (0.65)
     - `sourceWeight: 0.65`
     - `validFrom`: article publication date or now
     - `halfLifeDays`: 90 (political) or 365 (capability) based on content type
   - After entity creation, trigger entity resolution to check for existing duplicates
   - On duplicate found, call `fuseConfidence()` to combine source weights

2. Update `backend/src/robot/vision-cop-pipeline.ts` (381 lines):
   - Import `SOURCE_WEIGHTS` from `../graph/confidence-calculator.js`
   - Import `ACTOR_TYPE_TO_CCO_MAP` from `../graph/raft/types.js`
   - When creating hostile Actor nodes from vision detections:
     - `jsonldType`: map from detection class (tank → 'jc3:Equipment', soldier → 'cco:Person', vehicle → 'jc3:Equipment')
     - `assertedVia: 'vision_pipeline'`
     - `assertedBy: 'system:yolov8-detector'`
     - `derivedFrom`: JSON.stringify([imageId, detectionId])
     - `confidence: SOURCE_WEIGHTS['vision_pipeline']` (0.70)
     - `sourceWeight: 0.70`
     - `validFrom`: detection timestamp
     - `halfLifeDays`: 1 (tactical detections are very perishable)
     - `attributes_lat`, `attributes_lng`: from geolocation data if available
   - Trigger entity resolution after write (may match existing entity from other sources)
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit backend/src/osint/osint-graph-sync.ts backend/src/robot/vision-cop-pipeline.ts 2>&1 | head -20</automated>
  </verify>
  <done>OSINT sync writes JSON-LD with assertedVia='osint', confidence=0.65, OSINT source provenance. Vision pipeline writes JSON-LD with assertedVia='vision_pipeline', confidence=0.70, detection provenance. Both trigger entity resolution after write.</done>
</task>

</tasks>

<verification>
- All 4 files compile without TypeScript errors
- Every entity creation path includes jsonldType, assertedVia, assertedBy, derivedFrom, confidence, sourceWeight, validFrom, halfLifeDays
- Each pipeline uses its correct SOURCE_WEIGHTS value
- Entity resolution triggered after writes
- Contradiction detection triggered on property updates
</verification>

<success_criteria>
- All 4 ingestion pipelines write JSON-LD native properties with appropriate provenance
- Source weights match locked decisions: doc_intelligence=0.75, osint=0.65, vision_pipeline=0.70, ai_inference=0.60
- Entity resolution runs after each write to check for duplicates
- Contradiction detection runs on property conflicts
- derivedFrom chains trace back to source documents/feeds/images
</success_criteria>

<output>
After completion, create `.planning/phases/47-json-ld-semantic-brain-cop-fix/47-09-SUMMARY.md`
</output>
