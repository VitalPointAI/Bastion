---
phase: 47-json-ld-semantic-brain-cop-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/graph/confidence-calculator.test.ts
  - backend/src/graph/contradiction-detector.test.ts
  - backend/src/graph/raft/actor-store.test.ts
  - backend/src/graph/resolution/resolution-service.test.ts
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONTRA-01
  - ENTRES-01

must_haves:
  truths:
    - "Test files exist for all core behaviors before implementation begins"
    - "Tests define expected behavior for decay, fusion, contradiction, and entity resolution"
  artifacts:
    - path: "backend/src/graph/confidence-calculator.test.ts"
      provides: "Tests for decay formula and multi-source fusion"
      contains: "computeDecayedConfidence"
    - path: "backend/src/graph/contradiction-detector.test.ts"
      provides: "Tests for contradiction detection with temporal overlap check"
      contains: "CONTRADICTS"
    - path: "backend/src/graph/raft/actor-store.test.ts"
      provides: "Tests for JSON-LD property migration and temporal queries"
      contains: "jsonldType"
    - path: "backend/src/graph/resolution/resolution-service.test.ts"
      provides: "Tests for hybrid three-signal entity resolution scoring"
      contains: "fuseScore"
  key_links: []
---

<objective>
Create test scaffolds for the core domain behaviors: confidence decay/fusion, contradiction detection, actor store JSON-LD migration, and hybrid entity resolution. These tests define expected behavior before implementation and serve as verification for subsequent plans.

Purpose: Wave 0 test scaffolding ensures every core behavior has an automated verification command. Tests will initially fail (RED) and pass as implementation plans execute.
Output: 4 test files with behavior expectations defined
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-json-ld-semantic-brain-cop-fix/47-RESEARCH.md
@backend/src/cop/cco/cco-schema-loader.test.ts
@backend/src/cop/agents/layer-sub-agents/sub-agents.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create confidence calculator + contradiction detector tests</name>
  <files>backend/src/graph/confidence-calculator.test.ts, backend/src/graph/contradiction-detector.test.ts</files>
  <behavior>
    - computeDecayedConfidence(0.95, 180daysAgo, 180) returns ~0.475 (half-life)
    - computeDecayedConfidence(0.95, 0daysAgo, 180) returns 0.95 (no decay)
    - computeDecayedConfidence(0.75, 365daysAgo, 365) returns ~0.375 (half-life)
    - fuseConfidence([0.95, 0.65]) returns ~0.9825
    - fuseConfidence([0.95]) returns 0.95
    - fuseConfidence([]) returns 0
    - Contradiction detected: same entity, same property, different values, overlapping temporal range
    - Contradiction NOT detected: same entity, same property, different values, non-overlapping temporal range (historical succession)
    - Contradiction lowers confidence of both assertions by 20%
  </behavior>
  <action>
1. Create `backend/src/graph/confidence-calculator.test.ts`:
   - Import `computeDecayedConfidence`, `fuseConfidence`, `SOURCE_WEIGHTS`, `HALF_LIFE_DEFAULTS` from `./confidence-calculator.js`
   - Test suite "computeDecayedConfidence": half-life decay, zero-age no decay, multi-half-life decay
   - Test suite "fuseConfidence": single source, two sources (0.95+0.65=0.9825), empty array, three sources
   - Test suite "SOURCE_WEIGHTS": all 6 source types defined, values in 0-1 range
   - Test suite "HALF_LIFE_DEFAULTS": all 5 fact types defined, personnel=180, geographic=1825
   - Use `toBeCloseTo(expected, 2)` for floating point comparisons

2. Create `backend/src/graph/contradiction-detector.test.ts`:
   - Import will be from `./contradiction-detector.js` (created in Plan 05)
   - Test suite "detectContradiction":
     - Returns contradiction when same entity+property have different values with overlapping validFrom/validTo
     - Returns null when temporal ranges do not overlap (historical succession — per Pitfall 7)
     - Returns null when values are the same (no conflict)
     - Lowers confidence on both assertions by 20% when contradiction found
   - Mock Neo4j executeWriteQuery for :CONTRADICTS edge creation
   - Tests can reference the ContradictionRecord interface from provenance-types
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit backend/src/graph/confidence-calculator.test.ts backend/src/graph/contradiction-detector.test.ts 2>&1 | head -20</automated>
  </verify>
  <done>Two test files exist. confidence-calculator.test.ts has tests for decay, fusion, source weights, half-lives. contradiction-detector.test.ts has tests for temporal overlap contradiction detection and false positive avoidance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create actor-store + entity resolution test scaffolds</name>
  <files>backend/src/graph/raft/actor-store.test.ts, backend/src/graph/resolution/resolution-service.test.ts</files>
  <behavior>
    - Actor created with jsonldType, jsonldContext, provenance fields stored in Neo4j
    - Actor query with temporal filter returns only valid-at-time entities
    - Actor query with staleness decay returns decayed confidence
    - Hybrid entity resolution: string=0.8, embedding=0.9, type=1.0 → fused=0.86 (auto-merge)
    - Hybrid entity resolution: string=0.4, embedding=0.3, type=0.0 → fused=0.28 (distinct)
    - Hybrid entity resolution: string=0.6, embedding=0.7, type=1.0 → fused=0.72 (human review)
    - Resolution thresholds: >= 0.85 auto-merge, 0.5-0.85 human review, < 0.5 distinct
  </behavior>
  <action>
1. Create `backend/src/graph/raft/actor-store.test.ts`:
   - Mock `executeWriteQuery` and `executeReadQuery` from `../neo4j-client.js`
   - Test suite "createActor (JSON-LD)": actor creation includes jsonldType, jsonldContext, assertedBy, assertedVia, derivedFrom, confidence, sourceWeight, validFrom, validTo, halfLifeDays
   - Test suite "temporal queries": mock query returning actors with validFrom/validTo, verify filter logic
   - Test suite "staleness decay": verify decayed confidence calculation in query results
   - Tests will initially fail (no implementation yet) — they define the target behavior

2. Extend or create `backend/src/graph/resolution/resolution-service.test.ts`:
   - Check if file exists; if so, extend with new test suites. If not, create fresh.
   - Test suite "hybrid three-signal scoring":
     - `computeHybridScore(stringSim, embeddingSim, typeSim)` = 0.4*string + 0.4*embedding + 0.2*type
     - Score >= 0.85 → action: 'auto_merge'
     - Score 0.5-0.85 → action: 'human_review'
     - Score < 0.5 → action: 'distinct'
   - Test suite "embedding similarity": mock embedding API, verify cosine similarity computation
   - Test suite "ontology type matching": same jsonldType = 1.0, different = 0.0
   - Mock createLLMForAgent for embedding generation
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit backend/src/graph/raft/actor-store.test.ts backend/src/graph/resolution/resolution-service.test.ts 2>&1 | head -20</automated>
  </verify>
  <done>actor-store.test.ts defines tests for JSON-LD property storage and temporal queries. resolution-service.test.ts defines tests for hybrid three-signal scoring with auto-merge/review/distinct thresholds.</done>
</task>

</tasks>

<verification>
- All 4 test files compile without TypeScript errors
- Test files import from the correct module paths
- Expected behaviors match locked decisions (fusion formula, decay formula, thresholds)
</verification>

<success_criteria>
- 4 test files created with behavior expectations for: confidence decay, confidence fusion, contradiction detection (with temporal overlap), actor JSON-LD storage, temporal queries, hybrid entity resolution scoring
- Tests reference correct interfaces and module paths
- Tests will fail until implementation plans execute (RED phase of TDD)
</success_criteria>

<output>
After completion, create `.planning/phases/47-json-ld-semantic-brain-cop-fix/47-02-SUMMARY.md`
</output>
