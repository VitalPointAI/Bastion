---
phase: 47-json-ld-semantic-brain-cop-fix
plan: 11
type: execute
wave: 6
depends_on: ["47-03", "47-10"]
files_modified:
  - backend/src/api/jpp.ts
  - frontend/src/lib/entity-service.ts
  - frontend/src/components/plan/EntityResolutionPanel.tsx
  - frontend/src/components/design/DesignAIPanel.tsx
  - frontend/src/components/assess/OperationalAssess.tsx
autonomous: true
requirements:
  - WIRE-01

must_haves:
  truths:
    - "Entity search API returns JSON-LD fields (jsonldType, confidence, assertedVia, validFrom) in results"
    - "Plan tab EntityResolutionPanel displays confidence tier and provenance for each entity"
    - "Design tab receives ontology-typed entity data when showing related actors"
    - "Assess tab can view entity confidence for assessment context"
  artifacts:
    - path: "backend/src/api/jpp.ts"
      provides: "JPP entity API returning JSON-LD fields"
      contains: "jsonldType"
    - path: "frontend/src/lib/entity-service.ts"
      provides: "Entity client types with JSON-LD fields"
      contains: "jsonldType"
    - path: "frontend/src/components/plan/EntityResolutionPanel.tsx"
      provides: "Entity resolution UI showing confidence and provenance"
      contains: "confidenceTier"
  key_links:
    - from: "frontend/src/lib/entity-service.ts"
      to: "backend/src/api/jpp.ts"
      via: "fetch /api/jpp/entities/search returns JSON-LD enriched entities"
      pattern: "jsonldType|confidence"
    - from: "frontend/src/components/plan/EntityResolutionPanel.tsx"
      to: "frontend/src/lib/entity-service.ts"
      via: "imports Entity type with JSON-LD fields"
      pattern: "import.*entity-service"
---

<objective>
Wire the design, plan, and assess tab components to consume JSON-LD graph data with provenance and confidence fields. The JPP entity API and entity-service client are the primary data path for these tabs.

Purpose: The CONTEXT.md locked decision requires "all 7+ downstream consumers" wired to the JSON-LD graph, explicitly listing "design tab (read), plan tab (read), assess tab (read)". Plans 09 and 10 cover ingestion pipelines, graph API, COP, and brain viz. This plan completes the remaining three tab consumers.
Output: Updated JPP entity API, entity-service client, and design/plan/assess tab components consuming JSON-LD fields
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-json-ld-semantic-brain-cop-fix/47-CONTEXT.md
@.planning/phases/47-json-ld-semantic-brain-cop-fix/47-01-SUMMARY.md
@.planning/phases/47-json-ld-semantic-brain-cop-fix/47-10-SUMMARY.md
@backend/src/api/jpp.ts
@frontend/src/lib/entity-service.ts
@frontend/src/components/plan/EntityResolutionPanel.tsx
@frontend/src/components/design/DesignAIPanel.tsx
@frontend/src/components/assess/OperationalAssess.tsx

<interfaces>
From backend/src/graph/provenance-types.ts (created in Plan 01):
```typescript
export type SourceMethod = 'manual_entry' | 'doc_intelligence' | 'osint' | 'vision_pipeline' | 'ai_inference' | 'sigint';
export type ConfidenceTier = 'high' | 'medium' | 'low';
export function getConfidenceTier(confidence: number): ConfidenceTier;
```
From frontend/src/lib/entity-service.ts (current):
```typescript
export type EntityType = 'nation' | 'organization' | 'person' | 'location' | 'equipment' | 'unit';
export interface Entity {
  id: string; canonicalName: string; entityType: EntityType;
  aliases: string[]; metadata: Record<string, unknown>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update JPP entity API + entity-service client with JSON-LD fields</name>
  <files>backend/src/api/jpp.ts, frontend/src/lib/entity-service.ts</files>
  <action>
1. Update `backend/src/api/jpp.ts`:
   - The `GET /api/jpp/entities/search` endpoint returns entity results from entityToolHandlers
   - Ensure the response includes JSON-LD fields for each entity result:
     - `jsonldType`: the CCO/BFO class URI from the entity's Neo4j node
     - `confidence`: numeric confidence score
     - `confidenceTier`: computed via `getConfidenceTier()` from `../graph/provenance-types.js`
     - `assertedVia`: source method string
     - `validFrom`: ISO datetime string
     - `validTo`: ISO datetime string or null
   - Import `getConfidenceTier` from `../graph/provenance-types.js`
   - If underlying entityToolHandlers already returns these fields from the rewritten RAFT stores (Plan 03), just ensure they're passed through. If not, map them from the raw Neo4j response.
   - The `GET /api/jpp/entities/:entityId/references` endpoint: include provenance on each reference (which source asserted the link)
   - No breaking changes: new fields are additive to existing response

2. Update `frontend/src/lib/entity-service.ts`:
   - Add JSON-LD fields to the `Entity` interface:
     - `jsonldType?: string` -- CCO/BFO class URI
     - `confidence?: number` -- 0-1 confidence score
     - `confidenceTier?: 'high' | 'medium' | 'low'`
     - `assertedVia?: string` -- source method
     - `validFrom?: string` -- ISO datetime
     - `validTo?: string | null`
   - Keep all existing Entity fields unchanged
   - Add helper function: `formatSourceMethod(method: string): string` -- maps 'doc_intelligence' to 'Document Intelligence', 'osint' to 'OSINT Feed', 'manual_entry' to 'Manual Entry', 'vision_pipeline' to 'Vision Detection', 'ai_inference' to 'AI Inference', 'sigint' to 'SIGINT'
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit backend/src/api/jpp.ts frontend/src/lib/entity-service.ts 2>&1 | head -20</automated>
  </verify>
  <done>JPP entity search API returns JSON-LD fields (jsonldType, confidence, confidenceTier, assertedVia, validFrom, validTo) for each entity. entity-service.ts Entity interface includes JSON-LD fields and formatSourceMethod helper.</done>
</task>

<task type="auto">
  <name>Task 2: Wire design, plan, assess tabs to display JSON-LD provenance data</name>
  <files>frontend/src/components/plan/EntityResolutionPanel.tsx, frontend/src/components/design/DesignAIPanel.tsx, frontend/src/components/assess/OperationalAssess.tsx</files>
  <action>
1. Update `frontend/src/components/plan/EntityResolutionPanel.tsx`:
   - This component displays entity search results and merge actions
   - For each entity in the search results, add a provenance badge row:
     - Show confidence as a colored badge: green (high), amber (medium), red (low) using confidenceTier
     - Show source method: "Source: {formatSourceMethod(entity.assertedVia)}"
     - Show ontology type: "{entity.jsonldType}" formatted as readable label (strip namespace prefix)
   - When displaying merge candidates, show confidence comparison to help user decide which entity is more reliable
   - Use existing component styling patterns (Tailwind classes)

2. Update `frontend/src/components/design/DesignAIPanel.tsx`:
   - This panel shows AI-generated operational design framings
   - When the panel references actors/entities from the graph (via design-service responses), display confidence context:
     - If framing references entities, show their confidence tier as inline badges
     - Add a small "Graph confidence" indicator if the framing is based on entities with low confidence (< 0.5)
   - This is a lightweight integration: read JSON-LD fields from entity data when available, gracefully degrade when not present (entities created before migration may lack these fields)

3. Update `frontend/src/components/assess/OperationalAssess.tsx`:
   - The assess tab evaluates operational effectiveness
   - Add a "Graph Confidence Summary" section or widget that shows:
     - Average confidence of entities in the current problem set
     - Count of entities by confidence tier (high/medium/low)
     - Count of unresolved contradictions (if available from graph API)
   - Fetch this data from the graph API's entity listing endpoint with aggregation
   - This gives assessors context on the reliability of the underlying intelligence
   - Gracefully handle cases where graph data is not yet JSON-LD enriched (show "Confidence data pending migration" placeholder)
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit frontend/src/components/plan/EntityResolutionPanel.tsx frontend/src/components/design/DesignAIPanel.tsx frontend/src/components/assess/OperationalAssess.tsx 2>&1 | head -20</automated>
  </verify>
  <done>EntityResolutionPanel shows confidence badges and source method for each entity. DesignAIPanel shows confidence context for referenced entities. OperationalAssess shows graph confidence summary (avg confidence, tier distribution, contradiction count). All gracefully degrade for pre-migration entities.</done>
</task>

</tasks>

<verification>
- All files compile without TypeScript errors
- Entity search results include jsonldType, confidence, assertedVia fields
- EntityResolutionPanel renders confidence badges for entities
- Design and assess tabs display graph confidence context
- No breaking changes to existing tab functionality
</verification>

<success_criteria>
- JPP entity API returns JSON-LD enriched entity data
- Plan tab EntityResolutionPanel displays provenance and confidence for each entity
- Design tab shows confidence context when referencing graph entities
- Assess tab shows graph confidence summary for operational assessment
- All 7+ downstream consumers from CONTEXT.md are now wired to JSON-LD graph
</success_criteria>

<output>
After completion, create `.planning/phases/47-json-ld-semantic-brain-cop-fix/47-11-SUMMARY.md`
</output>
