---
phase: 43-robot-agent-local-discovery-bridge
plan: 06
type: execute
wave: 3
depends_on: [43-02, 43-03, 43-04]
files_modified:
  - backend/src/robot/index.ts
  - backend/src/index.ts
autonomous: false
requirements: [BRIDGE-01, BRIDGE-02, BRIDGE-03, BRIDGE-04, BRIDGE-05, BRIDGE-06, BRIDGE-07]

must_haves:
  truths:
    - "Bridge WebSocket endpoint is wired into the backend HTTP server"
    - "Bridge REST routes are mounted in Express app"
    - "Backend starts with bridge support enabled"
    - "Full end-to-end flow verified: bridge connects, registers, receives DID"
  artifacts:
    - path: "backend/src/robot/index.ts"
      provides: "Re-exports bridge setup functions"
      contains: "setupBridgeWebSocket"
    - path: "backend/src/index.ts"
      provides: "Backend entry point with bridge WS and routes mounted"
      contains: "bridgeRouter"
  key_links:
    - from: "backend/src/index.ts"
      to: "backend/src/robot/bridge-ws.ts"
      via: "setupBridgeWebSocket(server)"
      pattern: "setupBridgeWebSocket"
    - from: "backend/src/index.ts"
      to: "backend/src/robot/bridge-router.ts"
      via: "app.use('/api', bridgeRouter)"
      pattern: "bridgeRouter"
---

<objective>
Wire the bridge WebSocket handler and REST routes into the backend server, then verify the complete integration works end-to-end.

Purpose: Plans 02-04 created the components but they need to be mounted in the server. This plan wires everything together and verifies the full system.
Output: Backend server starts with bridge support; human-verified end-to-end connectivity.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/43-robot-agent-local-discovery-bridge/43-CONTEXT.md
@.planning/phases/43-robot-agent-local-discovery-bridge/43-02-SUMMARY.md
@.planning/phases/43-robot-agent-local-discovery-bridge/43-03-SUMMARY.md
@.planning/phases/43-robot-agent-local-discovery-bridge/43-04-SUMMARY.md

@backend/src/robot/index.ts
@backend/src/index.ts
@backend/src/robot/bridge-ws.ts
@backend/src/robot/bridge-router.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire bridge WebSocket and REST routes into backend server</name>
  <files>backend/src/robot/index.ts, backend/src/index.ts</files>
  <action>
    1. Update `backend/src/robot/index.ts`:
       - Add re-exports: `export { setupBridgeWebSocket } from './bridge-ws.js'` and `export { bridgeRouter } from './bridge-router.js'`.

    2. Update `backend/src/index.ts` (or wherever the HTTP server and Express app are configured):
       - Import `setupBridgeWebSocket` and `bridgeRouter` from `./robot/index.js`.
       - Mount bridge WebSocket: `setupBridgeWebSocket(server)` alongside existing `setupRobotWebSocket(server)`.
       - Mount bridge routes: `app.use('/api', bridgeRouter)` or `app.use('/api/admin', bridgeRouter)` depending on existing route mounting pattern.
       - Follow the existing pattern for how setupRobotWebSocket is mounted — mirror it exactly for the bridge.

    3. Verify the backend compiles and starts without errors:
       - `npx tsc --noEmit` passes
       - Server starts and logs "[BridgeWS] WebSocket server mounted at /ws/bridge"
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Backend compiles cleanly; bridge WebSocket and REST routes are mounted; server starts with bridge support</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 43 integration end-to-end</name>
  <files>none</files>
  <action>
    Human verifies the complete Phase 43 implementation:
    - robot/common/ shared package with models, ws_protocol, mDNS utilities
    - Enhanced robot agent with mDNS discovery, DID auth, message stamping, bridge fallback
    - Bridge service (bridge/) with cloud uplink, LAN scanner, robot relay, command queue, mDNS advertisement
    - Backend bridge WebSocket handler, token store, message dedup, bridge REST API
    - Bridge FastAPI dashboard and Docker packaging
  </action>
  <verify>
    1. Backend compiles and starts:
       - Run `npx tsc --noEmit` — should have zero errors
       - Start backend server — should see both "[RobotWS] WebSocket server mounted at /ws/robot" and "[BridgeWS] WebSocket server mounted at /ws/bridge"
    2. Python tests pass:
       - `cd robot && python -m pytest tests/ -x -q` — all robot tests pass
       - `cd bridge && python -m pytest tests/ -x -q` — all bridge tests pass
    3. Bridge Docker build (optional):
       - `cd bridge && docker build -t bastion-bridge -f Dockerfile ..` — builds without error
    4. Code review spot-checks:
       - robot/common/models.py has message_id on all outbound message types
       - robot/mission_client.py calls browse_service() before connecting
       - bridge/bridge_ws.py sends bridge:discovery_report on scan interval
       - backend/src/robot/robot-mission-service.ts has isDuplicate() check
       - backend/src/robot/bridge-token-store.ts has DB-backed token store
  </verify>
  <done>Human approves the complete Phase 43 system</done>
</task>

</tasks>

<verification>
- TypeScript compilation: `npx tsc --noEmit` zero errors
- Python robot tests: `cd robot && python -m pytest tests/ -x -q` all pass
- Python bridge tests: `cd bridge && python -m pytest tests/ -x -q` all pass
- Bridge Docker build: `docker build -t bastion-bridge -f bridge/Dockerfile .` succeeds
</verification>

<success_criteria>
- Backend starts with bridge WebSocket and REST routes mounted
- All Python tests pass (robot and bridge)
- TypeScript compiles cleanly
- Human verifies the complete system
</success_criteria>

<output>
After completion, create `.planning/phases/43-robot-agent-local-discovery-bridge/43-06-SUMMARY.md`
</output>
