---
phase: 43-robot-agent-local-discovery-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - robot/common/__init__.py
  - robot/common/models.py
  - robot/common/ws_protocol.py
  - robot/common/mdns.py
  - robot/requirements.txt
  - robot/pytest.ini
  - robot/tests/__init__.py
  - robot/tests/test_models.py
  - robot/tests/test_ws_protocol.py
  - robot/tests/test_mdns.py
autonomous: true
requirements: [BRIDGE-02, BRIDGE-03, BRIDGE-05]

must_haves:
  truths:
    - "Shared message models include message_id field on all outbound types"
    - "ws_protocol.stamp() injects UUID v4 message_id into any message dict"
    - "mDNS browse helper discovers _bastion._tcp.local services within timeout"
    - "mDNS advertise helper registers _bastion-robot._tcp.local with TXT records"
    - "All shared code is importable from robot.common package"
  artifacts:
    - path: "robot/common/__init__.py"
      provides: "Package init with exports"
    - path: "robot/common/models.py"
      provides: "Pydantic models with message_id field"
      contains: "message_id"
    - path: "robot/common/ws_protocol.py"
      provides: "stamp() and send_stamped() helpers"
      exports: ["stamp", "send_stamped"]
    - path: "robot/common/mdns.py"
      provides: "advertise_service and browse_service async helpers"
      exports: ["advertise_service", "browse_service"]
    - path: "robot/pytest.ini"
      provides: "pytest + pytest-asyncio config"
    - path: "robot/tests/test_ws_protocol.py"
      provides: "Tests for message stamping"
    - path: "robot/tests/test_mdns.py"
      provides: "Tests for mDNS browse/advertise with mocked zeroconf"
  key_links:
    - from: "robot/common/models.py"
      to: "robot/models.py"
      via: "Extends existing models with message_id"
      pattern: "message_id.*Optional\\[str\\]"
    - from: "robot/common/ws_protocol.py"
      to: "robot/common/models.py"
      via: "Uses stamp() to inject message_id"
      pattern: "uuid\\.uuid4"
---

<objective>
Create the shared Python package (`robot/common/`) that both the robot agent and bridge will import. This establishes the message models (with message_id for dedup), WebSocket protocol helpers, and mDNS browse/advertise utilities. Also sets up the pytest test infrastructure for the robot/ directory.

Purpose: Both the robot agent (plan 03) and bridge (plan 04) depend on these shared modules. Building them first avoids duplication and ensures protocol consistency.
Output: `robot/common/` package with models, ws_protocol, mdns modules; pytest config; unit tests.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-robot-agent-local-discovery-bridge/43-CONTEXT.md
@.planning/phases/43-robot-agent-local-discovery-bridge/43-RESEARCH.md

@robot/models.py
@robot/config.py
@robot/requirements.txt

<interfaces>
<!-- Existing robot models that common/models.py must extend -->
From robot/models.py:
```python
class StateUpdateMsg(BaseModel):
    type: str = "robot:state_update"
    mission_id: str
    state: MissionState
    timestamp: datetime = Field(default_factory=datetime.utcnow)
    metadata: Dict[str, Any] = Field(default_factory=dict)

class TelemetryMsg(BaseModel):
    type: str = "robot:telemetry"
    robot_id: str
    position: Dict[str, float]
    heading: float
    battery_pct: int
    timestamp: datetime = Field(default_factory=datetime.utcnow)

class RegisterMsg(BaseModel):
    type: str = "robot:register"
    robot_id: str
    auth_token: str
    capabilities: List[str] = Field(default_factory=list)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create robot/common/ shared package with models, ws_protocol, and mDNS utilities</name>
  <files>robot/common/__init__.py, robot/common/models.py, robot/common/ws_protocol.py, robot/common/mdns.py, robot/requirements.txt, robot/pytest.ini, robot/tests/__init__.py, robot/tests/test_models.py, robot/tests/test_ws_protocol.py, robot/tests/test_mdns.py</files>
  <behavior>
    - test_stamp_injects_message_id: stamp({}) returns dict with 'message_id' key that is a valid UUID v4 string
    - test_stamp_preserves_existing_id: stamp({"message_id": "abc"}) keeps "abc", does not overwrite
    - test_stamp_preserves_other_fields: stamp({"type": "telemetry", "robot_id": "alpha"}) retains all original keys
    - test_models_have_message_id: StateUpdateMsg, TelemetryMsg, RegisterMsg all accept optional message_id field
    - test_mdns_browse_returns_info: browse_service("_bastion._tcp.local.", timeout=1.0) with mocked AsyncZeroconf returns discovered service info list
    - test_mdns_browse_timeout_returns_empty: browse_service with no services found returns empty list after timeout
    - test_mdns_advertise_registers_service: advertise_service(...) calls async_register_service on the AsyncZeroconf instance with correct service type and TXT records
  </behavior>
  <action>
    1. Create `robot/common/__init__.py` that exports key classes/functions from submodules.

    2. Create `robot/common/models.py`:
       - Import existing models from `robot/models.py` and re-export them with `message_id: Optional[str] = None` added.
       - Add new `BridgeRegisterMsg(BaseModel)` with fields: type="bridge:register", bridge_id: str, token: Optional[str], did: Optional[str], capabilities: List[str].
       - Add `BridgeDiscoveryReportMsg(BaseModel)` with fields: type="bridge:discovery_report", bridge_id: str, devices: List[Dict], scanned_at: str, message_id: Optional[str].
       - All outbound message types MUST have `message_id: Optional[str] = None`.

    3. Create `robot/common/ws_protocol.py`:
       - `stamp(msg: dict) -> dict`: If 'message_id' not in msg, add message_id=str(uuid.uuid4()). Return new dict (do not mutate original).
       - `async send_stamped(ws, msg: dict) -> None`: Call stamp(), then json.dumps and ws.send().
       - Import uuid and json.

    4. Create `robot/common/mdns.py`:
       - `async browse_service(service_type: str, timeout: float = 10.0) -> List[ServiceResult]` where ServiceResult is a dataclass with name, addresses, port, properties fields. Uses AsyncZeroconf + AsyncServiceBrowser. Returns list of discovered services. If none found within timeout, returns empty list.
       - `async advertise_service(service_type: str, name: str, port: int, properties: dict, shutdown_event: asyncio.Event) -> None`: Registers service via AsyncZeroconf, awaits shutdown_event, then unregisters. Uses `async_register_service`.
       - Helper `get_local_ip() -> str`: Returns first non-loopback IPv4 address.

    5. Update `robot/requirements.txt`: Add `zeroconf>=0.148.0`, `pytest>=8.0`, `pytest-asyncio>=0.23`.

    6. Create `robot/pytest.ini` with asyncio_mode=auto.

    7. Create test files in `robot/tests/`:
       - `__init__.py` (empty)
       - `test_models.py`: Verify message_id field on all models
       - `test_ws_protocol.py`: Test stamp() behavior (inject, preserve, preserve fields)
       - `test_mdns.py`: Test browse_service and advertise_service with mocked zeroconf (use unittest.mock.patch on AsyncZeroconf)
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr/robot && pip install -e ".[dev]" 2>/dev/null; pip install zeroconf pytest pytest-asyncio 2>/dev/null; python -m pytest tests/ -x -q</automated>
  </verify>
  <done>robot/common/ package exists with models.py, ws_protocol.py, mdns.py; all tests pass; stamp() injects UUID; browse_service and advertise_service work with mocked zeroconf</done>
</task>

</tasks>

<verification>
- `python -c "from robot.common.models import StateUpdateMsg, TelemetryMsg, RegisterMsg, BridgeRegisterMsg"` succeeds
- `python -c "from robot.common.ws_protocol import stamp, send_stamped"` succeeds
- `python -c "from robot.common.mdns import browse_service, advertise_service"` succeeds
- `cd robot && python -m pytest tests/ -x -q` — all tests pass
</verification>

<success_criteria>
- robot/common/ is a valid Python package with __init__.py
- All message models have optional message_id field
- stamp() injects UUID v4 when message_id absent, preserves existing
- mDNS browse/advertise helpers use AsyncZeroconf correctly
- pytest infrastructure configured with asyncio_mode=auto
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/43-robot-agent-local-discovery-bridge/43-01-SUMMARY.md`
</output>
