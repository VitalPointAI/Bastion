---
phase: 26-strategic-environment-inheritance-inserted
plan: 04
type: execute
wave: 2
depends_on: ["26-01", "26-02"]
files_modified:
  - frontend/src/components/inheritance/AnnotationPanel.tsx
  - frontend/src/components/inheritance/RFIThread.tsx
autonomous: true
requirements: [SEI-06, SEI-07]

must_haves:
  truths:
    - "Users can create inline annotations and interpretation documents on inherited items"
    - "Annotations are local to the child problem set and do not modify the source"
    - "Users can send RFI (Request for Information) to parent problem set on inherited items"
    - "RFI creates a threaded conversation between echelons"
    - "Stale annotations show 'based on previous version' warning"
  artifacts:
    - path: "frontend/src/components/inheritance/AnnotationPanel.tsx"
      provides: "Annotation creation, display, and stale warning UI"
    - path: "frontend/src/components/inheritance/RFIThread.tsx"
      provides: "RFI creation and threaded message UI"
  key_links:
    - from: "frontend/src/components/inheritance/AnnotationPanel.tsx"
      to: "frontend/src/lib/inheritance-service.ts"
      via: "inheritanceApi annotation methods"
      pattern: "inheritanceApi\\.(createAnnotation|getAnnotations|updateAnnotation)"
    - from: "frontend/src/components/inheritance/RFIThread.tsx"
      to: "frontend/src/lib/inheritance-service.ts"
      via: "inheritanceApi RFI methods"
      pattern: "inheritanceApi\\.(createRFI|getRFIs|addRFIMessage)"
---

<objective>
Build the annotation and RFI (Request for Information) components for inherited context.

Purpose: Enable subordinate problem sets to annotate inherited items with local interpretations and to request clarification from parent echelons, mirroring real military RFI workflows.

Output: AnnotationPanel for inline comments and interpretation documents, RFIThread for threaded RFI conversations between echelons.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-strategic-environment-inheritance-inserted/26-CONTEXT.md
@.planning/phases/26-strategic-environment-inheritance-inserted/26-RESEARCH.md
@.planning/phases/26-strategic-environment-inheritance-inserted/26-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 API client -->
inheritanceApi.createAnnotation(problemSetId, { sourceProblemSetId, targetItemId, targetItemType, annotationType, content, visibility })
inheritanceApi.getAnnotations(problemSetId, targetItemId?)
inheritanceApi.updateAnnotation(problemSetId, annotationId, content)
inheritanceApi.createRFI(problemSetId, { targetProblemSetId, targetItemId, targetItemType, subject, initialMessage })
inheritanceApi.getRFIs(problemSetId, direction: 'sent'|'received')
inheritanceApi.addRFIMessage(problemSetId, rfiId, content)
inheritanceApi.getRFIMessages(problemSetId, rfiId)
inheritanceApi.updateRFIStatus(problemSetId, rfiId, status)

<!-- InheritanceAnnotation type -->
interface InheritanceAnnotation {
  id: string; problemSetId: string; sourceProblemSetId: string;
  targetItemId: string; targetItemType: string;
  annotationType: 'inline'|'interpretation'; content: string;
  basedOnVersion: string; isStale: boolean;
  visibility: 'upward'|'local_only'; createdBy: string;
  createdAt: string; updatedAt: string;
}

<!-- InheritanceRFI type -->
interface InheritanceRFI {
  id: string; requestingProblemSetId: string; targetProblemSetId: string;
  targetItemId: string; targetItemType: string; subject: string;
  status: 'open'|'responded'|'closed'; createdBy: string;
  createdAt: string; closedAt: string|null;
}

<!-- Existing pattern: EscalationPanel shows parent-child communication -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AnnotationPanel component</name>
  <files>frontend/src/components/inheritance/AnnotationPanel.tsx</files>
  <action>
Create AnnotationPanel for managing annotations on inherited items. Props:
- `problemSetId: string`
- `sourceProblemSetId: string`
- `targetItemId: string`
- `targetItemType: 'strategic_document' | 'graph_summary'`
- `onClose?: () => void`

Internal state: fetch annotations for this targetItemId on mount, loading/error states.

Renders a slide-out or modal panel with two sections:

**1. Inline Annotations section:**
- List of existing inline annotations for this item, each showing: author, timestamp, content, visibility toggle (upward/local_only)
- Stale annotations (isStale=true) show amber warning badge: "Based on previous version — review and update or dismiss"
- Quick-add form at bottom: textarea + "Add Note" button, creates annotation with `annotationType: 'inline'`
- Each annotation has an edit icon to inline-edit content (calls updateAnnotation)
- Visibility toggle: small eye icon, clicking toggles between 'upward' and 'local_only' (per user decision: child can mark as local only)

**2. Commander's Interpretation section:**
- A single rich-text area for the `annotationType: 'interpretation'` annotation on this item
- If one exists, show it with edit capability
- If none, show "Add Commander's Interpretation" button that creates one
- Per user decision: "full interpretation documents ('Commander's Interpretation' per inherited item)"
- Stale warning same as inline annotations

Both sections clearly labeled. Panel has a header with the item title and echelon source info.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit frontend/src/components/inheritance/AnnotationPanel.tsx 2>&1 | head -20</automated>
  </verify>
  <done>AnnotationPanel renders inline annotations with add/edit, Commander's Interpretation section, stale warnings on version mismatch, visibility toggle for upward/local_only</done>
</task>

<task type="auto">
  <name>Task 2: RFIThread component</name>
  <files>frontend/src/components/inheritance/RFIThread.tsx</files>
  <action>
Create RFIThread component for Request for Information workflows. This mirrors real military RFI workflows per user decision.

Props:
- `problemSetId: string`
- `targetProblemSetId?: string` — if provided, creating a new RFI to this target
- `targetItemId?: string` — inherited item being questioned
- `targetItemType?: string`
- `rfiId?: string` — if provided, viewing an existing RFI thread
- `onClose?: () => void`

Two modes:

**Create mode (no rfiId):**
- Form with: Subject text input, initial message textarea
- "Send RFI" button — calls inheritanceApi.createRFI, then switches to thread mode
- Shows target item info: source PS name, echelon badge, item title

**Thread mode (rfiId provided or after creation):**
- Header: Subject, status badge (open=blue, responded=green, closed=gray), source and target PS info with echelon badges
- Message list: chronological messages, each showing author DID (abbreviated), author PS name with echelon badge, timestamp, content
- Messages from the requesting PS aligned left, messages from the target PS aligned right (chat-style)
- Reply form at bottom: textarea + "Send" button (calls addRFIMessage)
- Status controls: if user is from target PS, "Mark Responded" button; if user is from requesting PS, "Close RFI" button
- Auto-refresh messages on interval (every 30 seconds) or via manual refresh button

Also create a small **RFIList** sub-component (exported from same file) for listing RFIs:
- Props: `problemSetId: string`, `direction: 'sent'|'received'`, `onSelectRFI: (rfiId: string) => void`
- Lists RFIs with: subject, status badge, target/source PS name, created date
- Unresponded RFIs show red dot indicator
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit frontend/src/components/inheritance/RFIThread.tsx 2>&1 | head -20</automated>
  </verify>
  <done>RFIThread supports create and thread modes, messages displayed in chat-style layout with echelon badges, RFIList shows sent/received RFIs with status indicators</done>
</task>

</tasks>

<verification>
- AnnotationPanel creates inline annotations and interpretations per user decision
- Annotations are local to child PS (API enforces, UI confirms)
- Stale annotations show "based on previous version" warning per user decision
- Visibility toggle works (upward vs local_only) per user decision
- RFIThread creates RFIs and supports threaded conversation
- RFI mirrors military Request for Information workflow per user specifics
</verification>

<success_criteria>
- Inline annotations can be added, edited, and visibility toggled
- Commander's Interpretation document can be created per inherited item
- Stale annotations flagged when source content updates
- RFI can be created targeting a specific inherited item
- RFI thread shows messages in chat-style with echelon context
- RFI status transitions work (open > responded > closed)
</success_criteria>

<output>
After completion, create `.planning/phases/26-strategic-environment-inheritance-inserted/26-04-SUMMARY.md`
</output>
