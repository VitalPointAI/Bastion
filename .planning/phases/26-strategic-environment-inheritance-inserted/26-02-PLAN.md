---
phase: 26-strategic-environment-inheritance-inserted
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/inheritance-service.ts
  - frontend/src/components/inheritance/InheritedItemCard.tsx
  - frontend/src/components/inheritance/InheritedItemCard.css
  - frontend/src/components/inheritance/ContextDashboardWidget.tsx
  - frontend/src/components/inheritance/AcknowledgmentBanner.tsx
autonomous: true
requirements: [SEI-03, SEI-04]

must_haves:
  truths:
    - "Frontend API client can fetch inherited context, acknowledge updates, manage annotations, and manage RFIs"
    - "InheritedItemCard renders each inherited document/graph item with echelon color coding and source labels"
    - "ContextDashboardWidget shows connected ancestors, sync status, and pending acknowledgments"
    - "AcknowledgmentBanner appears when unacknowledged updates exist"
  artifacts:
    - path: "frontend/src/lib/inheritance-service.ts"
      provides: "Frontend API client for all inheritance endpoints"
      exports: ["inheritanceApi"]
    - path: "frontend/src/components/inheritance/InheritedItemCard.tsx"
      provides: "Summary card for inherited documents with echelon colors"
    - path: "frontend/src/components/inheritance/ContextDashboardWidget.tsx"
      provides: "Dashboard widget showing ancestor hierarchy and sync status"
    - path: "frontend/src/components/inheritance/AcknowledgmentBanner.tsx"
      provides: "Banner prompting commander acknowledgment"
  key_links:
    - from: "frontend/src/lib/inheritance-service.ts"
      to: "/api/problem-sets/:id/inherited-context"
      via: "fetch calls to backend API"
      pattern: "fetch.*inherited-context"
---

<objective>
Build the frontend API client and reusable inheritance display components.

Purpose: Create the building blocks needed for the inherited context UI — the API client, item cards with echelon color coding, dashboard widget, and acknowledgment banner. These components are composed in Plan 03.

Output: Frontend inheritance API service, InheritedItemCard with echelon colors, ContextDashboardWidget, AcknowledgmentBanner.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-strategic-environment-inheritance-inserted/26-CONTEXT.md
@.planning/phases/26-strategic-environment-inheritance-inserted/26-RESEARCH.md

<interfaces>
<!-- InheritedContextResponse from RESEARCH.md (to be created in Plan 01 backend, replicated in frontend types) -->
interface InheritedContextResponse {
  ancestors: Array<{ problemSetId: string; name: string; echelon: 'strategic'|'operational'|'tactical'; depth: number }>;
  inheritedDocuments: Array<{
    id: string; title: string; docType: string; summary: string;
    sourceProblemSetId: string; sourceProblemSetName: string; sourceEchelon: string;
    lastUpdated: string; isNew: boolean; isUpdated: boolean;
  }>;
  inheritedGraphSummaries: Array<{
    containerName: string; summary: any; sourceProblemSetId: string;
    sourceProblemSetName: string; sourceEchelon: string; lastUpdated: string;
  }>;
  syncStatus: { lastSyncAt: string|null; hasStaleCaches: boolean; pendingAcknowledgments: number };
  changelog: Array<{
    id: string; changeType: string; changeSeverity: string;
    itemTitle: string; summary: string; createdAt: string; sourceProblemSetName: string;
  }>;
}

<!-- Echelon color palette from RESEARCH.md -->
ECHELON_COLORS = {
  strategic: { border: '#D4A843', bg: 'rgba(212,168,67,0.1)', badge: 'bg-yellow-700 text-yellow-100', label: 'Strategic' },
  operational: { border: '#3B82F6', bg: 'rgba(59,130,246,0.1)', badge: 'bg-blue-700 text-blue-100', label: 'Operational' },
  tactical: { border: '#22C55E', bg: 'rgba(34,197,94,0.1)', badge: 'bg-green-700 text-green-100', label: 'Tactical' },
}

<!-- Existing frontend patterns from StrategicContextPreview component -->
<!-- Uses fetch() with /api/ prefix, JSON responses, standard error handling -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frontend API client and shared types</name>
  <files>frontend/src/lib/inheritance-service.ts</files>
  <action>
Create the frontend API client for all inheritance endpoints. Follow the pattern of existing frontend services (e.g., `frontend/src/lib/strategic-context-service.ts`).

Include TypeScript types mirroring the backend types:
- `InheritedContextResponse`, `AncestorInfo`, `InheritedDocument`, `InheritedGraphSummary`, `SyncStatus`, `ChangelogEntry`, `PendingAck`
- `InheritanceAnnotation`, `InheritanceRFI`, `RFIMessage`
- `ECHELON_COLORS` constant per RESEARCH.md

Export `inheritanceApi` object with methods:
- `getInheritedContext(problemSetId)` — GET `/api/problem-sets/${id}/inherited-context`
- `acknowledgeContext(problemSetId, sourceProblemSetId)` — POST `.../inherited-context/acknowledge`
- `getChangelog(problemSetId)` — GET `.../inherited-context/changelog`
- `createAnnotation(problemSetId, data)` — POST `.../annotations`
- `getAnnotations(problemSetId, targetItemId?)` — GET `.../annotations`
- `updateAnnotation(problemSetId, annotationId, content)` — PUT `.../annotations/${annotationId}`
- `getParentViewAnnotations(problemSetId)` — GET `.../annotations/parent-view`
- `createRFI(problemSetId, data)` — POST `.../rfis`
- `getRFIs(problemSetId, direction)` — GET `.../rfis?direction=sent|received`
- `addRFIMessage(problemSetId, rfiId, content)` — POST `.../rfis/${rfiId}/messages`
- `getRFIMessages(problemSetId, rfiId)` — GET `.../rfis/${rfiId}/messages`
- `updateRFIStatus(problemSetId, rfiId, status)` — PUT `.../rfis/${rfiId}/status`

All methods should use fetch with credentials: 'include', handle errors consistently, return typed responses.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit frontend/src/lib/inheritance-service.ts 2>&1 | head -20</automated>
  </verify>
  <done>Frontend API client exports all methods for inheritance CRUD, types mirror backend InheritedContextResponse shape, ECHELON_COLORS constant defined</done>
</task>

<task type="auto">
  <name>Task 2: InheritedItemCard, ContextDashboardWidget, and AcknowledgmentBanner components</name>
  <files>frontend/src/components/inheritance/InheritedItemCard.tsx, frontend/src/components/inheritance/InheritedItemCard.css, frontend/src/components/inheritance/ContextDashboardWidget.tsx, frontend/src/components/inheritance/AcknowledgmentBanner.tsx</files>
  <action>
**InheritedItemCard.tsx + InheritedItemCard.css:**
Summary card for a single inherited document or graph item. Props:
- `item: InheritedDocument | InheritedGraphSummary`
- `echelon: 'strategic' | 'operational' | 'tactical'`
- `onAnnotate?: (itemId: string) => void`
- `onRequestInfo?: (itemId: string) => void`
- `annotations?: InheritanceAnnotation[]`
- `isExpanded?: boolean`
- `onToggleExpand?: () => void`

Renders:
- Left border colored by echelon (ECHELON_COLORS[echelon].border)
- Background tinted by echelon (ECHELON_COLORS[echelon].bg)
- Echelon badge (e.g., "Strategic" in gold) + source label ("From: INDOPACOM (Strategic)")
- Title, doc type badge, last updated timestamp
- Brief summary (truncated to 2 lines when collapsed)
- "New" badge if isNew, "Updated" badge if isUpdated (per user decision: visual indicators on updated items)
- Click to expand full content
- Action buttons: "Annotate" (sticky note icon), "Request Info" (question icon)
- If annotations exist, show count badge and list inline annotations below summary
- Stale annotation warning: if any annotation has isStale=true, show "Based on previous version" warning

CSS: Use existing project dark-theme conventions. Echelon border is 3px left border. Cards have subtle hover effect.

**ContextDashboardWidget.tsx:**
Dashboard widget showing inheritance overview. Props:
- `ancestors: AncestorInfo[]`
- `syncStatus: SyncStatus`
- `pendingAcknowledgments: number`
- `onRefresh?: () => void`

Renders:
- Compact card at top of inherited context section
- "Connected Ancestors" — vertical chain visualization showing echelon icons (strategic gold dot > operational blue dot > tactical green dot) with PS names
- "Sync Status" — green check if no stale caches, yellow warning if stale, with last sync timestamp
- "Pending Acknowledgments" — red badge count if > 0
- "Last Update" — relative timestamp of most recent change
- Refresh button calling onRefresh

**AcknowledgmentBanner.tsx:**
Banner for pending acknowledgments. Props:
- `pendingAcks: PendingAck[]`
- `onAcknowledge: (sourceProblemSetId: string) => void`

Renders:
- Dismissible banner at top of inherited context (amber/warning color scheme)
- Text: "Strategic context updated — {N} source(s) require acknowledgment"
- For each pending ack: source PS name + echelon badge + "Acknowledge" button
- Clicking "Acknowledge" calls onAcknowledge and removes from list
- Per user decision: creates an audit trail (the API handles the record)
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit frontend/src/components/inheritance/InheritedItemCard.tsx frontend/src/components/inheritance/ContextDashboardWidget.tsx frontend/src/components/inheritance/AcknowledgmentBanner.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Three components render correctly with echelon color coding, InheritedItemCard shows source labels and new/updated badges, ContextDashboardWidget shows ancestor chain and sync status, AcknowledgmentBanner shows pending acks with acknowledge buttons</done>
</task>

</tasks>

<verification>
- All frontend files compile without TypeScript errors
- ECHELON_COLORS matches user decision (strategic=gold, operational=blue, tactical=green)
- InheritedItemCard uses echelon-colored left border and background tint
- Source labels follow "From: {name} ({Echelon})" format per user decision
- AcknowledgmentBanner appears only when pendingAcknowledgments > 0
</verification>

<success_criteria>
- Frontend API client covers all 12 inheritance API endpoints
- InheritedItemCard renders with echelon colors and source labels per user decisions
- ContextDashboardWidget shows ancestor hierarchy, sync status, pending acks
- AcknowledgmentBanner prompts commander acknowledgment with audit trail
- All components accept typed props and compile
</success_criteria>

<output>
After completion, create `.planning/phases/26-strategic-environment-inheritance-inserted/26-02-SUMMARY.md`
</output>
