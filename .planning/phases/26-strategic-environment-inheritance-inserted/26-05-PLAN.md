---
phase: 26-strategic-environment-inheritance-inserted
plan: 05
type: execute
wave: 3
depends_on: ["26-03", "26-04"]
files_modified:
  - frontend/src/components/inheritance/InheritedContextSection.tsx
  - frontend/src/components/tabs/UnderstandTab.tsx
  - backend/src/api/problem-sets.ts
  - backend/src/inheritance/inheritance-service.ts
autonomous: false
requirements: [SEI-01, SEI-02, SEI-03, SEI-04, SEI-05, SEI-06, SEI-07]

must_haves:
  truths:
    - "Full inheritance flow works end-to-end: create child PS, inherited context appears in Understand tab"
    - "Annotations and RFI panels are accessible from InheritedItemCard action buttons"
    - "Update propagation works: changing parent context marks child caches stale and shows notification"
    - "Commander acknowledgment creates audit trail"
    - "Backfill migration creates inheritance subscriptions for existing hierarchies"
  artifacts:
    - path: "frontend/src/components/inheritance/InheritedContextSection.tsx"
      provides: "Fully wired section with annotation and RFI panel integration"
    - path: "backend/src/inheritance/inheritance-service.ts"
      provides: "Service with propagation hooks wired to strategic document changes"
  key_links:
    - from: "frontend/src/components/inheritance/InheritedContextSection.tsx"
      to: "frontend/src/components/inheritance/AnnotationPanel.tsx"
      via: "onAnnotate callback from InheritedItemCard"
      pattern: "AnnotationPanel"
    - from: "frontend/src/components/inheritance/InheritedContextSection.tsx"
      to: "frontend/src/components/inheritance/RFIThread.tsx"
      via: "onRequestInfo callback from InheritedItemCard"
      pattern: "RFIThread"
    - from: "backend/src/api/problem-sets.ts"
      to: "backend/src/inheritance/inheritance-service.ts"
      via: "onParentContextChanged on strategic document write"
      pattern: "onParentContextChanged"
---

<objective>
Wire annotation/RFI panels into the inherited context section, add update propagation hooks, run backfill, and verify the complete end-to-end flow.

Purpose: Connect all the pieces built in Plans 01-04 into a working end-to-end system, wire propagation triggers to strategic document changes, and verify the full inheritance lifecycle.

Output: Fully integrated inheritance system with all features functional, backfill migration for existing data, visual verification of end-to-end flow.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-strategic-environment-inheritance-inserted/26-CONTEXT.md
@.planning/phases/26-strategic-environment-inheritance-inserted/26-RESEARCH.md
@.planning/phases/26-strategic-environment-inheritance-inserted/26-01-SUMMARY.md
@.planning/phases/26-strategic-environment-inheritance-inserted/26-02-SUMMARY.md
@.planning/phases/26-strategic-environment-inheritance-inserted/26-03-SUMMARY.md
@.planning/phases/26-strategic-environment-inheritance-inserted/26-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire annotation/RFI panels and propagation hooks</name>
  <files>frontend/src/components/inheritance/InheritedContextSection.tsx, backend/src/api/problem-sets.ts, backend/src/inheritance/inheritance-service.ts</files>
  <action>
**InheritedContextSection.tsx updates:**
- Add state for active annotation panel: `annotatingItemId: string | null`
- Add state for active RFI: `rfiMode: { itemId: string, targetPsId: string } | null` and `viewingRfiId: string | null`
- Wire InheritedItemCard `onAnnotate` callback to open AnnotationPanel for that item
- Wire InheritedItemCard `onRequestInfo` callback to open RFIThread in create mode for that item
- Render AnnotationPanel as a slide-out panel on the right when annotatingItemId is set
- Render RFIThread as a slide-out panel on the right when rfiMode or viewingRfiId is set
- Add an "RFIs" tab alongside "Documents" and "Changelog" — shows RFIList for sent and received RFIs
- Fetch and pass annotations to each InheritedItemCard (batch fetch all annotations for the PS, then distribute by targetItemId)
- When annotations or RFI actions complete, re-fetch inherited context to update counts/status

**Backend propagation hooks (problem-sets.ts and related routes):**
Find where strategic documents are created/updated/deleted in the API routes. These are the routes that write to `strategic_documents` table. After each write operation, call:
```typescript
await inheritanceService.onParentContextChanged(
  problemSetId,
  changeType, // 'document_added', 'document_updated', 'document_removed'
  inheritanceService.classifyChangeSeverity(changeType),
  documentId,
  documentTitle
);
```

Look for the strategic document routes — likely in problem-sets.ts or a separate strategic documents API file. Also check for graph data update routes from Phase 25.3 and add propagation there too (changeType: 'graph_updated').

**Backfill endpoint:**
Add a POST `/api/inheritance/backfill` endpoint (admin-only) that calls `inheritanceService.backfillExistingRelationships()`. This is a one-time operation to create inheritance subscriptions for all existing parent-child problem set relationships. Log progress to console.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit frontend/src/components/inheritance/InheritedContextSection.tsx backend/src/api/problem-sets.ts 2>&1 | head -20</automated>
  </verify>
  <done>Annotation and RFI panels accessible from item card actions, propagation hooks fire on strategic document changes, backfill endpoint available for existing data migration</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete strategic environment inheritance system:
1. Auto-inheritance subscriptions on child PS creation
2. Inherited Strategic Context section at top of Understand tab
3. Echelon color-coded document/graph cards with source labels
4. Context dashboard widget showing ancestor chain and sync status
5. Commander acknowledgment workflow with audit trail
6. Inline annotations and Commander's Interpretation on inherited items
7. RFI (Request for Information) threads between echelons
8. Changelog with significant/minor change distinction
9. Push invalidation on parent changes with lazy refresh
10. Backfill for existing parent-child relationships
  </what-built>
  <how-to-verify>
1. Start the app: `cd /home/vitalpointai/projects/ssr && npm run dev`
2. Log in and navigate to a strategic-level problem set that has documents
3. Create a new operational-level child problem set under it
4. Navigate to the child problem set's Understand tab
5. Verify: "Inherited Strategic Context" section appears at top, collapsible
6. Verify: Documents show with gold borders (from strategic parent) and source labels
7. Verify: Context dashboard widget shows the ancestor chain
8. Click "Annotate" on an inherited item — verify annotation panel opens
9. Add an inline annotation — verify it appears
10. Click "Request Info" on an item — verify RFI creation form appears
11. Go back to parent PS, add or update a strategic document
12. Return to child PS Understand tab — verify acknowledgment banner appears
13. Click "Acknowledge" — verify banner clears
14. Check Changelog tab — verify the update is listed
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- Full end-to-end inheritance flow works from PS creation to context display
- All user decisions implemented: auto-inherit, full cascade, echelon colors, collapsible section, acknowledgment, annotations, RFI
- No deferred ideas implemented (none were deferred)
- Update propagation triggers on strategic document changes
</verification>

<success_criteria>
- Creating a child PS automatically shows inherited context in Understand tab
- Documents display with correct echelon color coding and source labels
- Commander can acknowledge updated context
- Users can annotate inherited items (inline + interpretation)
- Users can create RFIs that create threaded conversations
- Strategic document changes propagate to descendants
- Backfill works for existing problem set hierarchies
</success_criteria>

<output>
After completion, create `.planning/phases/26-strategic-environment-inheritance-inserted/26-05-SUMMARY.md`
</output>
