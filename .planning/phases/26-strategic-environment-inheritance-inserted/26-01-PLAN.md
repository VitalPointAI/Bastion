---
phase: 26-strategic-environment-inheritance-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/inheritance/inheritance-types.ts
  - backend/src/inheritance/inheritance-store.ts
  - backend/src/inheritance/inheritance-service.ts
  - backend/src/api/inheritance.ts
  - backend/src/api/problem-sets.ts
autonomous: true
requirements: [SEI-01, SEI-02]

must_haves:
  truths:
    - "Creating a child problem set with a parent automatically creates inheritance subscriptions to all ancestors"
    - "Existing parent-child problem sets receive backfilled inheritance subscriptions"
    - "Subscription table distinguishes inheritance from manual subscriptions via subscription_type column"
  artifacts:
    - path: "backend/src/inheritance/inheritance-types.ts"
      provides: "TypeScript interfaces for inheritance system"
      exports: ["InheritanceAcknowledgment", "InheritanceAnnotation", "InheritanceRFI", "RFIMessage", "InheritanceChangelog", "InheritedContextResponse", "PendingAck"]
    - path: "backend/src/inheritance/inheritance-store.ts"
      provides: "Database CRUD for inheritance tables"
      exports: ["inheritanceStore"]
    - path: "backend/src/inheritance/inheritance-service.ts"
      provides: "Business logic for auto-inheritance chain creation and ancestor traversal"
      exports: ["inheritanceService"]
    - path: "backend/src/api/inheritance.ts"
      provides: "Express routes for inheritance endpoints"
    - path: "backend/src/api/problem-sets.ts"
      provides: "Modified PS creation to trigger auto-inheritance"
  key_links:
    - from: "backend/src/api/problem-sets.ts"
      to: "backend/src/inheritance/inheritance-service.ts"
      via: "createInheritanceChain call after PS creation"
      pattern: "inheritanceService\\.createInheritanceChain"
    - from: "backend/src/inheritance/inheritance-service.ts"
      to: "backend/src/problem-set/problem-set-subscription-store.ts"
      via: "createSubscription with subscription_type='inheritance'"
      pattern: "problemSetSubscriptionStore\\.createSubscription"
---

<objective>
Build the inheritance data model, backend store, service layer, and auto-inheritance mechanism.

Purpose: Establish the foundational database schema and backend logic that enables strategic context to flow automatically from parent to child problem sets via the existing subscription system.

Output: New inheritance tables (acknowledgments, annotations, RFIs, changelog), subscription_type column on existing subscriptions, InheritanceStore for CRUD, InheritanceService for chain creation/traversal, auto-inheritance hook in PS creation route, backfill migration for existing parent-child relationships.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-strategic-environment-inheritance-inserted/26-CONTEXT.md
@.planning/phases/26-strategic-environment-inheritance-inserted/26-RESEARCH.md

<interfaces>
<!-- From backend/src/problem-set/types.ts -->
export type Echelon = 'strategic' | 'operational' | 'tactical';
export interface ProblemSet {
  id: string;
  name: string;
  echelon: Echelon;
  parentProblemSetId: string | null;
  // ... other fields
}
export interface CreateProblemSetInput {
  echelon: Echelon;
  parentProblemSetId?: string;
  // ... other fields
}
export function validateEchelonHierarchy(parentEchelon: Echelon | null, childEchelon: Echelon): boolean;

<!-- From backend/src/problem-set/problem-set-subscription-store.ts -->
export interface ProblemSetSubscription {
  id: string;
  subscriberProblemSetId: string;
  publisherProblemSetId: string;
  dataTypes: string[];
  approvalStatus: string;
  approvalMechanism: string;
  requestedBy: string;
  createdAt: string;
}
export class ProblemSetSubscriptionStore {
  async createSubscription(sub: { subscriberProblemSetId, publisherProblemSetId, dataTypes, approvalMechanism, requestedBy }): Promise<ProblemSetSubscription>;
  async updateApprovalStatus(id, status, approvedBy): Promise<void>;
  async listBySubscriber(subscriberId): Promise<ProblemSetSubscription[]>;
  async materializeCache(subscriberId, sourceId): Promise<void>;
  async refreshCacheForPublisher(publisherId): Promise<void>;
  async getCachedDataForSubscriber(subscriberId): Promise<CachedDoc[]>;
}
export const problemSetSubscriptionStore: ProblemSetSubscriptionStore;

<!-- From backend/src/api/problem-sets.ts (PS creation at line ~291) -->
// After createProblemSet(), this is where auto-inheritance hook goes:
// if (body.parentProblemSetId) { await inheritanceService.createInheritanceChain(...) }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database schema, types, and InheritanceStore</name>
  <files>backend/src/inheritance/inheritance-types.ts, backend/src/inheritance/inheritance-store.ts</files>
  <action>
Create the inheritance module directory and two files:

**inheritance-types.ts:** Define all TypeScript interfaces per the RESEARCH.md schema:
- `InheritanceAcknowledgment` (id "IACK-{uuid}", problemSetId, sourceProblemSetId, sourceVersion, acknowledgedBy, acknowledgedAt)
- `InheritanceAnnotation` (id "IANN-{uuid}", problemSetId, sourceProblemSetId, targetItemId, targetItemType: 'strategic_document'|'graph_summary', annotationType: 'inline'|'interpretation', content, basedOnVersion, isStale, visibility: 'upward'|'local_only', createdBy, createdAt, updatedAt)
- `InheritanceRFI` (id "IRFI-{uuid}", requestingProblemSetId, targetProblemSetId, targetItemId, targetItemType, subject, status: 'open'|'responded'|'closed', createdBy, createdAt, closedAt)
- `RFIMessage` (id "IRFIM-{uuid}", rfiId, authorDid, authorProblemSetId, content, createdAt)
- `InheritanceChangelog` (id "ICLOG-{uuid}", sourceProblemSetId, changeType: 'document_added'|'document_updated'|'graph_updated'|'document_removed', changeSeverity: 'significant'|'minor', itemId, itemTitle, summary, createdAt)
- `InheritedContextResponse` — the full API response shape per RESEARCH.md (ancestors, inheritedDocuments, inheritedGraphSummaries, syncStatus, changelog)
- `PendingAck` (sourceProblemSetId, sourceProblemSetName, sourceEchelon, currentVersion, lastAcknowledgedVersion)
- `AncestorInfo` (problemSetId, name, echelon, depth)

**inheritance-store.ts:** Create `InheritanceStore` class with `getPool()` from existing `backend/src/config/db.ts` pattern. Include:
- `initInheritanceTables()` — CREATE TABLE IF NOT EXISTS for all 5 tables from RESEARCH.md schema PLUS ALTER TABLE to add `subscription_type` column to `problem_set_subscriptions` (default 'subscription')
- `getAncestorChain(problemSetId)` — recursive CTE query per RESEARCH.md Pattern 3, with depth limit 3
- `getDescendantProblemSetIds(problemSetId)` — recursive CTE walking down from parent
- `markDescendantCachesStale(publisherProblemSetId)` — UPDATE problem_set_data_cache SET stale_at = NOW() for all descendants
- `logChangelog(sourceProblemSetId, changeType, changeSeverity, itemId, itemTitle, summary)` — INSERT into inheritance_changelog
- `getChangelog(sourceProblemSetId, limit?)` — SELECT from changelog ordered by createdAt DESC
- `createAcknowledgment(problemSetId, sourceProblemSetId, sourceVersion, acknowledgedBy)` — INSERT
- `getPendingAcknowledgments(problemSetId)` — compare cache versions with latest acknowledgment versions
- `createAnnotation(...)` — INSERT into inheritance_annotations
- `getAnnotations(problemSetId, targetItemId?)` — SELECT with optional filter
- `updateAnnotation(id, content)` — UPDATE
- `markAnnotationsStale(sourceProblemSetId, targetItemId)` — SET is_stale=true for annotations on that item
- `getAnnotationsForParentView(sourceProblemSetId, visibility?)` — annotations visible to parent (upward visibility)
- `createRFI(...)` — INSERT into inheritance_rfis
- `getRFIs(problemSetId, direction: 'sent'|'received')` — list RFIs
- `addRFIMessage(rfiId, authorDid, authorProblemSetId, content)` — INSERT message
- `getRFIMessages(rfiId)` — list messages for an RFI
- `updateRFIStatus(rfiId, status)` — UPDATE status
- Export singleton `inheritanceStore`

Call `initInheritanceTables()` at store construction time (same pattern as other stores). Use `crypto.randomUUID()` for ID generation with the appropriate prefix.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit backend/src/inheritance/inheritance-types.ts backend/src/inheritance/inheritance-store.ts 2>&1 | head -30</automated>
  </verify>
  <done>All inheritance types defined, InheritanceStore class has all CRUD methods, tables created on init, subscription_type column added to existing subscriptions table</done>
</task>

<task type="auto">
  <name>Task 2: InheritanceService, auto-inheritance hook, and API routes</name>
  <files>backend/src/inheritance/inheritance-service.ts, backend/src/api/inheritance.ts, backend/src/api/problem-sets.ts</files>
  <action>
**inheritance-service.ts:** Create `InheritanceService` class with:
- `createInheritanceChain(childProblemSetId, parentProblemSetId, requestedBy)` — walk ancestor chain via `inheritanceStore.getAncestorChain(parentProblemSetId)`, then for each ancestor (including direct parent), call `problemSetSubscriptionStore.createSubscription()` with `dataTypes: ['*']`, `approvalMechanism: 'auto'`. After creation, immediately approve and set subscription_type to 'inheritance' via direct SQL update. Then materialize cache for each subscription.
- `onParentContextChanged(publisherProblemSetId, changeType, changeSeverity, itemId, itemTitle)` — per RESEARCH.md Pattern 2: mark descendant caches stale, log changelog entry, log activity via `problemSetActivityStore.log()` with appropriate activity type for tiered notifications
- `getInheritedContext(problemSetId)` — build the full `InheritedContextResponse`: get ancestor chain, get cached data for each ancestor subscription (using existing `getCachedDataForSubscriber`), get sync status (check stale_at on caches), get pending acknowledgments, get recent changelog
- `acknowledgeContext(problemSetId, sourceProblemSetId, acknowledgedBy)` — get current cache version, create acknowledgment record
- `backfillExistingRelationships()` — query all problem_sets where parent_problem_set_id IS NOT NULL, create inheritance subscriptions for each (idempotent — skip if subscription already exists)
- `classifyChangeSeverity(changeType)` — per RESEARCH.md: 'document_added', 'document_removed', 'graph_major_update' = 'significant'; others = 'minor'
- Export singleton `inheritanceService`

**inheritance.ts (API routes):** Create Express router with:
- `GET /api/problem-sets/:id/inherited-context` — returns full InheritedContextResponse
- `POST /api/problem-sets/:id/inherited-context/acknowledge` — body: { sourceProblemSetId }, calls acknowledgeContext
- `GET /api/problem-sets/:id/inherited-context/changelog` — returns changelog entries
- `POST /api/problem-sets/:id/annotations` — create annotation (body: sourceProblemSetId, targetItemId, targetItemType, annotationType, content, visibility)
- `GET /api/problem-sets/:id/annotations` — list annotations, optional ?targetItemId query param
- `PUT /api/problem-sets/:id/annotations/:annotationId` — update annotation content
- `GET /api/problem-sets/:id/annotations/parent-view` — annotations visible to parent (upward visibility), for when parent PS wants to see child interpretations
- `POST /api/problem-sets/:id/rfis` — create RFI (body: targetProblemSetId, targetItemId, targetItemType, subject, initialMessage)
- `GET /api/problem-sets/:id/rfis` — list RFIs with ?direction=sent|received
- `POST /api/problem-sets/:id/rfis/:rfiId/messages` — add message to RFI thread
- `GET /api/problem-sets/:id/rfis/:rfiId/messages` — get messages for RFI
- `PUT /api/problem-sets/:id/rfis/:rfiId/status` — update RFI status
- All routes use zod validation. Auth middleware via existing pattern (req.userDid).

**problem-sets.ts modification:** In the POST `/api/problem-sets` route, after the `createProblemSet()` call (around line 303), add:
```typescript
if (body.parentProblemSetId) {
  await inheritanceService.createInheritanceChain(problemSet.id, body.parentProblemSetId, userDid);
}
```
Also import the inheritanceService. Register the inheritance router in the main app setup.

Register the inheritance router in the Express app. Check how other routers are registered (likely in backend/src/index.ts or a routes file) and follow the same pattern.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit backend/src/inheritance/inheritance-service.ts backend/src/api/inheritance.ts 2>&1 | head -30</automated>
  </verify>
  <done>InheritanceService orchestrates auto-inheritance on PS creation, the backfill method exists for existing relationships, all 12 API routes are defined with zod validation, PS creation route triggers auto-inheritance</done>
</task>

</tasks>

<verification>
- TypeScript compiles with no errors for all new files
- InheritanceStore initializes tables including ALTER TABLE on subscriptions
- InheritanceService.createInheritanceChain creates subscription for parent and all ancestors
- API routes are registered and respond to requests
- PS creation with parentProblemSetId triggers auto-inheritance
</verification>

<success_criteria>
- All 5 inheritance tables created via initInheritanceTables
- subscription_type column exists on problem_set_subscriptions
- Creating a child PS with a parent auto-creates inheritance subscriptions to all ancestors
- Backfill method can retroactively create subscriptions for existing hierarchies
- Full CRUD API for acknowledgments, annotations, and RFIs
- InheritedContextResponse shape matches RESEARCH.md spec
</success_criteria>

<output>
After completion, create `.planning/phases/26-strategic-environment-inheritance-inserted/26-01-SUMMARY.md`
</output>
