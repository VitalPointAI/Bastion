---
phase: 54-update-research-whitepaper-and-docs-for-demo-briefing
plan: 07
type: execute
wave: 2
depends_on:
  - 54-01
  - 54-02
  - 54-03
  - 54-04
files_modified:
  - docs/whitepaper/scripts/export.sh
  - docs/site/docs/api/rest-endpoints.md
autonomous: true
requirements:
  - DOC-01
  - DOC-03
must_haves:
  truths:
    - "Export script includes new background files in correct order"
    - "DOCX file generated successfully via pandoc"
    - "API reference generated from actual backend routes"
    - "No 'Direct tab' or 'direct-tab' references remain anywhere in docs/"
  artifacts:
    - path: "docs/whitepaper/scripts/export.sh"
      provides: "Updated export script with v0.2 file list"
      contains: "02-background-robotics.md"
    - path: "docs/whitepaper/exports/"
      provides: "Generated DOCX file"
    - path: "docs/site/docs/api/rest-endpoints.md"
      provides: "Auto-generated API reference from backend routes"
      min_lines: 50
  key_links:
    - from: "docs/whitepaper/scripts/export.sh"
      to: "docs/whitepaper/*.md"
      via: "INPUT_FILES array"
      pattern: "02-background-robotics"
---

<objective>
Update export script for v0.2 file list, generate DOCX output, auto-generate API reference from backend routes, and run final stale-reference sweep.

Purpose: Produce the editable DOCX whitepaper the user requested, ensure API docs are current, and verify no stale references remain.
Output: DOCX file in exports/, updated export.sh, API reference page, clean reference sweep
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/54-update-research-whitepaper-and-docs-for-demo-briefing/54-CONTEXT.md
@.planning/phases/54-update-research-whitepaper-and-docs-for-demo-briefing/54-RESEARCH.md
@docs/whitepaper/scripts/export.sh
@docs/whitepaper/ASSEMBLY.md

<interfaces>
From docs/whitepaper/scripts/export.sh:
- INPUT_FILES array defines document assembly order
- VERSION variable controls output filename
- export_docx() function runs pandoc with --defaults config
- Output goes to docs/whitepaper/exports/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update export script and generate DOCX whitepaper</name>
  <files>docs/whitepaper/scripts/export.sh</files>
  <action>
1. **Update export.sh INPUT_FILES array** to include the two new background files in correct document order:
   ```
   INPUT_FILES=(
     "00-title-page.md"
     "00-abstract.md"
     "01-introduction.md"
     "02-background-daos.md"
     "02-background-military.md"
     "02-background-ai.md"
     "02-background-robotics.md"          # NEW
     "02-background-knowledge-graphs.md"  # NEW
     "03-methodology.md"
     "04-results.md"
     "05-discussion.md"
     "06-conclusion.md"
     "07-references.md"
     "appendix-a-sitrep.md"
     "appendix-b-demo-script.md"
   )
   ```

2. **Ensure VERSION is "v0.2"** (it already is based on file read, but verify).

3. **Run the export script to generate DOCX**:
   ```bash
   cd docs/whitepaper && bash scripts/export.sh docx
   ```

4. **Verify the DOCX was created** and report its file size. The user explicitly requested an editable DOCX version.

If pandoc fails, troubleshoot the error. Common issues: malformed markdown, missing files. Fix the source file causing the issue and retry.
  </action>
  <verify>
    <automated>ls -la docs/whitepaper/exports/BASTION-Whitepaper-v0.2-*.docx 2>/dev/null | head -1 | awk '{if (NF > 0) print "PASS: DOCX exists, size " $5 " bytes"; else print "FAIL: no DOCX found"}'</automated>
  </verify>
  <done>Export script updated with v0.2 file list including new background sections. DOCX file generated in docs/whitepaper/exports/ and ready for user editing.</done>
</task>

<task type="auto">
  <name>Task 2: Auto-generate API reference and run final stale-reference sweep</name>
  <files>docs/site/docs/api/rest-endpoints.md</files>
  <action>
1. **Auto-generate API reference** from actual backend routes:
   - Scan `backend/src/` for Express route definitions using grep: `router.get`, `router.post`, `router.put`, `router.delete`, `app.get`, `app.post`, etc.
   - Count total endpoints
   - Organize by functional domain (auth, dao, planning, agents, resources, documents, exercises, governance, brain/knowledge-graph, missions, etc.) based on file path or route prefix
   - Write structured markdown to `docs/site/docs/api/rest-endpoints.md` with:
     - Total endpoint count at top
     - Table of contents by domain
     - For each domain: table with Method | Path | Description (infer description from route handler name or file context)
   - This is a static markdown generation task, not building Swagger middleware

2. **Final stale-reference sweep** across ALL docs:
   - `grep -rn "direct-tab\|Direct tab" docs/ --include="*.md"` — fix any remaining references
   - `grep -rn "131 agents\|131 AI" docs/ --include="*.md"` — update to verified count (from SITREP task)
   - `grep -rn "417 endpoint\|~417" docs/ --include="*.md"` — update to current count
   - `grep -rn "31 completed\|27 completed\|12 completed" docs/ --include="*.md"` — update phase counts
   - Fix all found stale references in place

3. **Verify all DOC requirements are met**:
   - DOC-01: v0.2 whitepaper with updated abstract (check)
   - DOC-02: SITREP covers Phase 53 (check)
   - DOC-03: No "direct-tab" references in docs/site/ (verify)
   - DOC-04: Slide deck has 20+ slides (check)
   - DOC-05: Demo script has 30-min timing (check)
   - DOC-06: All figure specs exist (check)
   - DOC-07: New docs site pages exist (check)
  </action>
  <verify>
    <automated>test -f docs/site/docs/api/rest-endpoints.md && grep -rn "direct-tab\|Direct tab" docs/ --include="*.md" | wc -l | awk '{if ($1 == 0) print "PASS: no stale refs"; else print "FAIL: " $1 " stale refs"}' && echo "API_REF: $(wc -l < docs/site/docs/api/rest-endpoints.md) lines"</automated>
  </verify>
  <done>API reference generated from actual backend routes with current endpoint count. Zero stale "direct-tab" or "Direct tab" references remain. All DOC requirements verified green.</done>
</task>

</tasks>

<verification>
- DOCX file exists in docs/whitepaper/exports/
- Export script INPUT_FILES includes both new background files
- API reference exists with organized endpoint listing
- `grep -rn "direct-tab\|Direct tab" docs/ --include="*.md"` returns empty
- All 7 DOC requirements pass
</verification>

<success_criteria>
Editable DOCX whitepaper generated for user. API reference current. All stale references eliminated. Phase 54 deliverables complete.
</success_criteria>

<output>
After completion, create `.planning/phases/54-update-research-whitepaper-and-docs-for-demo-briefing/54-07-SUMMARY.md`
</output>
