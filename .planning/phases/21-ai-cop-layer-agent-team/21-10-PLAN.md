---
phase: 21-ai-cop-layer-agent-team
plan: 10
type: execute
wave: 4
depends_on: ["21-07", "21-08"]
files_modified:
  - frontend/src/components/cop/COPPhaseSlider.tsx
  - frontend/src/components/cop/COPVersionBrowser.tsx
  - frontend/src/components/cop/COPAgentActivity.tsx
  - frontend/src/components/cop/COPPhaseSlider.css
  - frontend/src/components/cop/COPVersionBrowser.css
  - frontend/src/components/cop/COPAgentActivity.css
autonomous: false
requirements:
  - TEMPORAL-PLAYBACK
  - VERSION-HISTORY-UI
  - AGENT-OBSERVABILITY

must_haves:
  truths:
    - "Phase slider scrubs through temporal phases showing symbol positions at each phase"
    - "Animated playback advances through phases with smooth transitions"
    - "Version browser lets staff view COP at any historical snapshot"
    - "Agent activity feed shows what COP agents are doing in real time"
  artifacts:
    - path: "frontend/src/components/cop/COPPhaseSlider.tsx"
      provides: "Temporal phase scrub control with animated playback"
      exports: ["COPPhaseSlider"]
    - path: "frontend/src/components/cop/COPVersionBrowser.tsx"
      provides: "Historical snapshot browser for post-operation analysis"
      exports: ["COPVersionBrowser"]
    - path: "frontend/src/components/cop/COPAgentActivity.tsx"
      provides: "Agent activity feed integrated with workspace activity"
      exports: ["COPAgentActivity"]
  key_links:
    - from: "frontend/src/components/cop/COPPhaseSlider.tsx"
      to: "frontend/src/components/cop/COPMapView.tsx"
      via: "controls which phase symbols display at via currentPhase state"
      pattern: "onPhaseChange|currentPhase"
    - from: "frontend/src/components/cop/COPVersionBrowser.tsx"
      to: "frontend/src/lib/cop-service.ts"
      via: "fetches historical snapshots"
      pattern: "copService\\.listVersions|copService\\.getVersionSpec"
---

<objective>
Build temporal playback controls, version history browser, and agent activity feed for the COP system.

Purpose: Phase slider enables staff to visualize operation phasing over time. Version browser supports post-operation analysis by browsing COP snapshots. Agent activity feed provides observability into what COP agents are doing.

Output: Temporal playback, historical browsing, and agent observability components.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-ai-cop-layer-agent-team/21-CONTEXT.md
@.planning/phases/21-ai-cop-layer-agent-team/21-RESEARCH.md
@.planning/phases/21-ai-cop-layer-agent-team/21-08-SUMMARY.md

<interfaces>
From frontend/src/types/cop.ts (Plan 01):
```typescript
interface COPPhaseSpec { phaseNumber, label, startTime, endTime, description }
interface COPSymbolSpec { ... movementPath?: Array<{ phase: number; position: { lat: number; lng: number } }> }
interface LayerSnapshot { id, layerId, version, state, spec, transitionedBy, transitionedAt }
```

From frontend/src/lib/cop-service.ts (Plan 08):
- copService.listVersions(layerId)
- copService.getVersionSpec(layerId, version)
- copService.getAgentActivity(workspaceId, limit?)

From frontend/src/components/cop/COPMapView.tsx (Plan 08):
- Accepts currentPhase prop to filter symbol positions by phase
- Renders symbols at phase-specific positions from movementPath
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase slider with animated playback</name>
  <files>frontend/src/components/cop/COPPhaseSlider.tsx, frontend/src/components/cop/COPPhaseSlider.css</files>
  <action>
Create `frontend/src/components/cop/COPPhaseSlider.tsx`:
- Props: { phases: COPPhaseSpec[], currentPhase: number, onPhaseChange: (phase: number) => void }
- State: isPlaying (boolean), playbackSpeed (number, default 2000ms per phase step per RESEARCH discretion)

**Manual Scrub Control:**
- Range input slider from phase 0 to max phase number
- Phase label display showing current phase name and description
- Phase markers along the slider showing each phase position
- Drag to scrub through phases

**Animated Playback:**
- Play/Pause button (toggle)
- When playing: use requestAnimationFrame or setInterval at playbackSpeed interval to advance currentPhase
- Smooth transitions: when phase changes, symbols with movementPath should animate position from previous phase position to current phase position using CSS transition (800ms ease-in-out per RESEARCH discretion)
- Speed control: 0.5x, 1x, 2x buttons (multiplier on base 2000ms interval)
- Loop toggle: when reaching last phase, optionally restart from phase 0
- Stop at end if loop is off

**Phase indicator bar:**
- Horizontal bar below the map showing all phases as segments
- Current phase highlighted
- Click on a phase segment to jump to it
- Phase labels shown on hover

**Animation mechanics for COPMapView integration:**
- onPhaseChange callback provides the new phase number
- COPMapView filters symbols: show all symbols where movementPath includes current phase, position = movementPath entry for current phase (or default position if no movementPath entry for this phase)
- Transition: map markers should smoothly slide to new positions. Use Leaflet's marker.setLatLng with CSS transition on the marker div.

CSS: Styled as transport controls (play/pause icons, speed labels). Positioned at bottom of map. Dark semi-transparent background. Phase markers as tick marks.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Phase slider scrubs through phases. Play/pause animates through phases at configurable speed. Phase markers show all phases. Smooth transitions on phase change.</done>
</task>

<task type="auto">
  <name>Task 2: Version browser and agent activity feed</name>
  <files>frontend/src/components/cop/COPVersionBrowser.tsx, frontend/src/components/cop/COPAgentActivity.tsx, frontend/src/components/cop/COPVersionBrowser.css, frontend/src/components/cop/COPAgentActivity.css</files>
  <action>
Create `frontend/src/components/cop/COPVersionBrowser.tsx`:
- Props: { layerId: string, currentVersion: number, onVersionSelect: (spec: COPLayerSpec) => void }
- On mount: fetch version list via copService.listVersions(layerId)
- Display as a timeline/list panel:
  - Each version entry: version number, state at time of snapshot, transitioned by (user/agent name), timestamp, previous state -> new state arrow
  - COP snapshots highlighted with star icon (these are full snapshots)
  - Click on a version: fetches spec via copService.getVersionSpec and calls onVersionSelect to display that historical view on the map
  - "Return to Current" button restores live view
- Date range filter: start date / end date to narrow version list
- State filter: checkboxes for Draft/Review/Published/COP to filter which transitions shown
- Visual diff indicator: show brief summary of changes between versions (symbol count delta, new entities, removed entities) -- compute from spec comparison

Create `frontend/src/components/cop/COPAgentActivity.tsx`:
- Props: { workspaceId: string }
- Polls copService.getAgentActivity(workspaceId, 50) every 5 seconds for real-time feel
- Displays as a scrollable activity log (similar to existing ObserverPanel pattern):
  - Each entry: agent name/rank, action performed, detail text, timestamp
  - Color-coded by action type: generation=blue, validation=green, error=red, transition=yellow
  - Agent avatar/icon based on branch/specialty
- Auto-scroll to bottom on new entries (with "scroll lock" toggle to pause auto-scroll)
- Filter by agent: dropdown to show only specific agent's activity
- Compact mode toggle for dense information display
- Integration point: this component should be embeddable in the existing workspace activity feed (ObserverPanel or WorkspaceDashboard)

CSS: Version browser as slide-in panel from left. Agent activity as bottom panel or sidebar section. Both follow existing panel styling patterns. Agent activity uses monospace for timestamps.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Version browser displays historical snapshots with filtering. Agent activity feed polls and displays real-time agent actions. Both compile without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete COP system: agent team with LangGraph coordinator, 6 layer sub-agents, 4-state layer lifecycle, MIL-STD-2525D symbology via milsymbol, interactive map with stacked layers, entity tooltips/detail views, review workflow with spatial annotations, phase slider with animated playback, version history browser, agent activity feed, CCO ontology validation, SVG security (DOMPurify + shadow DOM), cross-section conflict detection, entity linkage with hybrid discovery.</what-built>
  <how-to-verify>
    1. Start the application: `npm run dev`
    2. Navigate to a workspace with existing OPORD/strategic documents
    3. Verify COP Map View:
       - Map loads with Leaflet tiles
       - Layer controls panel visible on right side
       - Perspective toggle (Friendly/Adversary/Combined) present
    4. Trigger COP generation:
       - Use the manual trigger button or agent activity panel
       - Watch agent activity feed for sub-agent progress
       - Verify a draft layer appears in layer controls
    5. Test layer lifecycle:
       - Draft layer visible with gray badge
       - "Submit for Review" transitions to review state
       - Add spatial annotation (click on map) and general comment
       - "Approve for Publish" transitions to published
       - "Promote to COP" promotes to top-level
    6. Test entity interaction:
       - Hover over a military symbol: tooltip shows name + affiliation + linked entities
       - Click symbol: detail panel slides in with full entity data
    7. Test perspective toggle:
       - Friendly: only blue symbols visible
       - Adversary: only red symbols visible
       - Combined: all symbols visible
    8. Test phase slider:
       - Scrub slider: symbols move to phase-appropriate positions
       - Play button: animated playback through phases
       - Speed controls work
    9. Test conflict detection:
       - If multiple sections have promoted layers, conflict banner appears for contradictions
    10. Test version browser:
       - Browse historical snapshots
       - Click a past version: map shows that snapshot's state
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Frontend TypeScript compiles clean
- Phase slider animates through all phases smoothly
- Version browser loads and displays historical snapshots
- Agent activity feed shows real-time agent actions
- Full end-to-end COP workflow functional
</verification>

<success_criteria>
- Phase slider enables temporal browsing with animated playback
- Version browser allows historical snapshot viewing for post-operation analysis
- Agent activity feed provides real-time observability into agent actions
- Complete COP system verified by human review
</success_criteria>

<output>
After completion, create `.planning/phases/21-ai-cop-layer-agent-team/21-10-SUMMARY.md`
</output>
