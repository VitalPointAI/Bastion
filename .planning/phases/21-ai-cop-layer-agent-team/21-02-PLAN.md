---
phase: 21-ai-cop-layer-agent-team
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/cop/messaging/event-bus.ts
  - backend/src/cop/messaging/trigger-handler.ts
  - backend/src/cop/messaging/activity-bridge.ts
  - backend/src/cop/agents/agent-definitions.ts
  - backend/src/cop/agents/agent-pool.ts
autonomous: true
requirements:
  - AGENT-INFRA
  - EVENT-BUS
  - TRIPLE-TRIGGER

must_haves:
  truths:
    - "Typed event bus emits and receives COP events with full type safety"
    - "Triple trigger handler fires on document commit, manual trigger, and periodic polling"
    - "Activity bridge forwards agent events to workspace activity feed"
    - "COP agent definitions follow StaffAgentDef pattern with 6 layer sub-agent types"
    - "Agent pool manages pool-with-affinity scoping for section context"
  artifacts:
    - path: "backend/src/cop/messaging/event-bus.ts"
      provides: "Typed EventEmitter wrapper for COP coordination"
      exports: ["COPEventBus", "copEventBus", "COPEvents"]
    - path: "backend/src/cop/messaging/trigger-handler.ts"
      provides: "Triple trigger model: commit, manual, polling"
      exports: ["TriggerHandler", "TriggerType"]
    - path: "backend/src/cop/messaging/activity-bridge.ts"
      provides: "Forwards COP agent events to workspace ActivityFeed"
      exports: ["ActivityBridge"]
    - path: "backend/src/cop/agents/agent-definitions.ts"
      provides: "StaffAgentDef[] for COP coordinator + 6 layer sub-agents"
      exports: ["COP_AGENT_DEFINITIONS", "COP_COORDINATOR_DEF"]
    - path: "backend/src/cop/agents/agent-pool.ts"
      provides: "Pool-with-affinity agent manager"
      exports: ["AgentPool", "AgentAssignment"]
  key_links:
    - from: "backend/src/cop/messaging/trigger-handler.ts"
      to: "backend/src/cop/messaging/event-bus.ts"
      via: "emits layer:generation:start on trigger"
      pattern: "copEventBus\\.emit.*layer:generation:start"
    - from: "backend/src/cop/messaging/activity-bridge.ts"
      to: "backend/src/cop/messaging/event-bus.ts"
      via: "subscribes to agent:activity events"
      pattern: "copEventBus\\.on.*agent:activity"
---

<objective>
Build the agent infrastructure layer: typed event bus for coordination, triple trigger model, activity feed bridge, COP agent definitions, and pool-with-affinity agent manager.

Purpose: The event bus is the nervous system of the COP agent team. The trigger handler initiates layer generation. Agent definitions seed the database. The pool manager assigns agents to sections with affinity.

Output: Complete messaging infrastructure, agent definition seed data, pool manager.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-ai-cop-layer-agent-team/21-CONTEXT.md
@.planning/phases/21-ai-cop-layer-agent-team/21-RESEARCH.md

<interfaces>
From backend/src/exercise/types.ts:
```typescript
export interface StaffAgentDef {
  id: string;
  roleKey: string;
  name: string;
  rank: string;
  branch: string;
  specialty: string;
  focus: string;
  tools: string[];
  personality: string[];
  systemPromptHint: string;
  isDefault: boolean;
}
```

From backend/src/exercise/agent-library.ts:
- 31 existing JPP staff agent definitions using StaffAgentDef pattern
- Seeded into database on init

From frontend/src/components/workspace/ObserverPanel.tsx:
- Read-only workspace panel pattern for activity feed integration
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Typed event bus and trigger handler</name>
  <files>backend/src/cop/messaging/event-bus.ts, backend/src/cop/messaging/trigger-handler.ts, backend/src/cop/messaging/activity-bridge.ts, backend/src/cop/messaging/event-bus.test.ts</files>
  <behavior>
    - COPEventBus.emit('agent:activity', data) delivers to registered on('agent:activity') handler
    - COPEventBus supports all event types: document:committed, layer:generation:start, layer:generation:complete, layer:state:transition, agent:activity, conflict:detected, linkage:discovered
    - TriggerHandler.handleCommitTrigger emits layer:generation:start with triggeredBy='commit'
    - TriggerHandler.handleManualTrigger emits layer:generation:start with triggeredBy='manual'
    - TriggerHandler.startPolling sets up interval that checks for changes and emits when found
    - TriggerHandler.stopPolling clears the interval
    - ActivityBridge forwards agent:activity events by calling workspace activity feed endpoint
  </behavior>
  <action>
Create `backend/src/cop/messaging/event-bus.ts`:
- Define COPEvents interface mapping event names to payload types (per RESEARCH.md Pattern: Typed Event Bus):
  - 'document:committed': { workspaceId, sectionId, documentId }
  - 'layer:generation:start': { workspaceId, sectionId, triggeredBy: 'commit' | 'manual' | 'polling' }
  - 'layer:generation:complete': { layerId, status: 'success' | 'error', error?: string }
  - 'layer:state:transition': { layerId, from: LayerState, to: LayerState, by: string }
  - 'agent:activity': { agentId, action, detail, workspaceId, sectionId, timestamp }
  - 'conflict:detected': { layerId, conflictingLayerId, entities: string[] }
  - 'linkage:discovered': { entityId, linkedEntityId, confidence, autoCommitted: boolean }
- COPEventBus class wrapping EventEmitter with typed on/emit/off methods
- Export singleton `copEventBus`

Create `backend/src/cop/messaging/trigger-handler.ts`:
- TriggerHandler class with constructor taking copEventBus instance
- `handleCommitTrigger(workspaceId, sectionId, documentId)` -- emits document:committed then layer:generation:start
- `handleManualTrigger(workspaceId, sectionId)` -- emits layer:generation:start with triggeredBy='manual'
- `startPolling(workspaceId, sectionId, intervalMs)` -- stores interval handle, checks for changes (stub: always triggers on first poll, then checks a lastChecked timestamp). Default intervals per RESEARCH discretion: 60s normal, 15s active, 300s deep idle.
- `stopPolling(workspaceId, sectionId)` -- clears interval
- Private `pollIntervals: Map<string, NodeJS.Timeout>` keyed by `${workspaceId}:${sectionId}`

Create `backend/src/cop/messaging/activity-bridge.ts`:
- ActivityBridge class subscribes to copEventBus 'agent:activity' events
- Formats activity into workspace activity feed format (match existing ObserverPanel data shape)
- For now, stores activities in an in-memory ring buffer (last 100 per workspace) with a method `getActivities(workspaceId, limit?)` -- actual persistence integration deferred to API plan

Tests in `backend/src/cop/messaging/event-bus.test.ts`: event delivery, trigger handler emit sequences, activity bridge buffering.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr/backend && npx jest --testPathPattern='cop/messaging' --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>Event bus delivers typed events. Trigger handler fires correct events for all three trigger types. Activity bridge captures and buffers agent activity. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: COP agent definitions and pool manager</name>
  <files>backend/src/cop/agents/agent-definitions.ts, backend/src/cop/agents/agent-pool.ts</files>
  <action>
Create `backend/src/cop/agents/agent-definitions.ts`:
- Import StaffAgentDef from backend/src/exercise/types.ts
- Define COP_COORDINATOR_DEF: StaffAgentDef with id='cop-coordinator-001', roleKey='cop_coordinator', name='COL R. Martinez', rank='COL', branch='AR', specialty='COP Assembly & Coordination', focus='Orchestrates layer generation requests across warfighting function leads, assembles final COP layers', tools=['route_to_leads', 'assemble_layers', 'validate_cco', 'manage_conflicts'], personality=['decisive', 'systematic', 'coordinating'], systemPromptHint='You are the COP coordinator. Route layer generation requests to the appropriate warfighting function lead agents. Assemble their outputs into coherent COP layers. Never generate layer content directly.', isDefault=true.

- Define COP_LAYER_AGENTS: StaffAgentDef[] with 6 sub-agents:
  1. cop_j3_force (Force Disposition): MAJ D. Torres, IN branch, extracts unit positions and SIDC codes from OPORD/FRAGO data
  2. cop_j35_objectives (Objectives Overlay): MAJ K. Chen, FA branch, marks objectives, named areas of interest, target areas of interest
  3. cop_j3_control (Control Measures): CPT L. Rivera, EN branch, boundaries, phase lines, axes of advance, routes, fire support coordination measures
  4. cop_j2_intel (Intel Overlay): MAJ A. Kowalski, MI branch, threat assessment, enemy positions, named areas of interest from enemy perspective
  5. cop_j4_logistics (Logistics Overlay): MAJ S. Patel, QM branch, supply routes, lines of communication, logistics nodes
  6. cop_c2_overlay (C2 Overlay): LTC J. Washington, SC branch, command relationships, command posts, communication networks

- Each agent has tools: ['read_orders', 'query_raft_graph', 'generate_layer_spec', 'validate_cco'] plus domain-specific tools
- Export COP_AGENT_DEFINITIONS combining coordinator + all 6 sub-agents

Create `backend/src/cop/agents/agent-pool.ts`:
- AgentAssignment interface: { agentId, workspaceId, sectionId, assignedAt, lastActiveAt, contextSummary? }
- AgentPool class:
  - Private `assignments: Map<string, AgentAssignment>` keyed by `${agentId}:${workspaceId}:${sectionId}`
  - `assign(agentId, workspaceId, sectionId)` -- creates assignment with timestamp
  - `getAssignment(agentId, workspaceId, sectionId)` -- returns current assignment or null
  - `getAgentsForSection(workspaceId, sectionId)` -- returns all agents assigned to a section
  - `getPreferredAgent(roleKey, workspaceId, sectionId)` -- returns agent with affinity (longest assignment) for this section, or any available agent with that roleKey
  - `recordActivity(agentId, workspaceId, sectionId)` -- updates lastActiveAt for affinity tracking
  - `releaseAgent(agentId, workspaceId, sectionId)` -- removes assignment
- Pool-with-affinity: when multiple agents have same roleKey, prefer the one with existing section assignment (affinity). If none assigned, pick least-recently-active one.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit --project backend/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>7 COP agent definitions follow StaffAgentDef pattern. Agent pool manages assignments with affinity tracking. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for backend
- Event bus tests pass
- Agent definitions array has exactly 7 entries (1 coordinator + 6 sub-agents)
- Pool manager correctly handles affinity-based agent selection
</verification>

<success_criteria>
- Typed event bus delivers all COP event types
- Triple trigger handler covers commit, manual, and polling triggers
- Activity bridge buffers agent activity for workspace feeds
- 7 COP agent definitions ready for database seeding
- Agent pool supports pool-with-affinity section assignment
</success_criteria>

<output>
After completion, create `.planning/phases/21-ai-cop-layer-agent-team/21-02-SUMMARY.md`
</output>
