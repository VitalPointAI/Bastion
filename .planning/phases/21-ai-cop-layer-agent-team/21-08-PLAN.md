---
phase: 21-ai-cop-layer-agent-team
plan: 08
type: execute
wave: 4
depends_on: ["21-07"]
files_modified:
  - frontend/src/lib/cop-service.ts
  - frontend/src/components/cop/COPMapView.tsx
  - frontend/src/components/cop/COPLayerControls.tsx
  - frontend/src/components/cop/COPPerspectiveToggle.tsx
  - frontend/src/components/cop/COPMapView.css
  - frontend/src/components/cop/COPLayerControls.css
autonomous: true
requirements:
  - COP-MAP-VIEW
  - LAYER-CONTROLS
  - PERSPECTIVE-TOGGLE

must_haves:
  truths:
    - "COP map renders stacked layers with milsymbol symbols on Leaflet"
    - "Layer controls toggle visibility and opacity per layer (extends IPBLayerControls pattern)"
    - "Perspective toggle switches between Friendly/Adversary/Combined views"
    - "Symbols filter by affiliation based on current perspective"
    - "API service provides typed fetch wrappers for all COP endpoints"
  artifacts:
    - path: "frontend/src/lib/cop-service.ts"
      provides: "Typed API client for all COP backend endpoints"
      exports: ["copService"]
    - path: "frontend/src/components/cop/COPMapView.tsx"
      provides: "Main COP map with stacked layer rendering"
      exports: ["COPMapView"]
    - path: "frontend/src/components/cop/COPLayerControls.tsx"
      provides: "Layer toggle panel with visibility and opacity"
      exports: ["COPLayerControls"]
    - path: "frontend/src/components/cop/COPPerspectiveToggle.tsx"
      provides: "Friendly/Adversary/Combined perspective switch"
      exports: ["COPPerspectiveToggle"]
  key_links:
    - from: "frontend/src/components/cop/COPMapView.tsx"
      to: "frontend/src/lib/cop-service.ts"
      via: "fetches layers from API"
      pattern: "copService\\.queryLayers"
    - from: "frontend/src/components/cop/COPMapView.tsx"
      to: "milsymbol"
      via: "renders symbols via MilSymbolMarker pattern"
      pattern: "milsymbol|MilSymbolMarker|createMilSymbolIcon"
    - from: "frontend/src/components/cop/COPLayerControls.tsx"
      to: "frontend/src/components/cop/COPMapView.tsx"
      via: "controls layer visibility and opacity state"
      pattern: "onVisibilityChange|onOpacityChange"
---

<objective>
Build the COP API client, main map view with stacked layer rendering, layer controls panel, and perspective toggle.

Purpose: This is the primary visualization surface for the COP. Military staff see all promoted layers stacked on a Leaflet map with standard MIL-STD-2525D symbology, control layer visibility/opacity, and switch between friendly/adversary/combined perspectives.

Output: Interactive COP map with layer management controls.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-ai-cop-layer-agent-team/21-CONTEXT.md
@.planning/phases/21-ai-cop-layer-agent-team/21-RESEARCH.md
@.planning/phases/21-ai-cop-layer-agent-team/21-07-SUMMARY.md

<interfaces>
From frontend/src/components/mission/map/MilSymbolMarker.tsx:
```typescript
// Uses milsymbol.Symbol(sidc, options) to create SVG icons for Leaflet markers
export function createMilSymbolIcon(sidc: string, options?: object): L.DivIcon;
```

From frontend/src/components/planning/COASketchMap.tsx:
- SVG overlay on Leaflet with phase filtering, movement paths, control measures
- Pattern for rendering polylines, polygons, markers on map

From frontend/src/components/exercise/IPBLayerControls.tsx:
- GIS-style layer toggle panel with grouped visibility toggles
- Color swatches, indeterminate checkboxes, visible/total counts

From frontend/src/types/cop.ts (Plan 01):
```typescript
interface COPLayer { id, workspaceId, sectionId, layerType, state, spec, ... }
interface COPLayerControlsProps { layers, layerVisibility, layerOpacity, onVisibilityChange, onOpacityChange, currentPerspective, onPerspectiveChange }
type Perspective = 'friendly' | 'adversary' | 'combined'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: COP API service client</name>
  <files>frontend/src/lib/cop-service.ts</files>
  <action>
Create `frontend/src/lib/cop-service.ts` as a typed API client wrapping fetch calls to the COP REST API. Follow existing service patterns in frontend/src/lib/ (check what exists there for the fetch/error handling pattern).

Methods:
- **Layer operations:**
  - `queryLayers(workspaceId: string, filters?: { sectionId?, state?, layerType? }): Promise<COPLayer[]>`
  - `getLayer(layerId: string): Promise<COPLayer>`
  - `createLayer(input: { workspaceId, sectionId, layerType, spec? }): Promise<COPLayer>`
  - `updateLayerSpec(layerId: string, spec: COPLayerSpec): Promise<COPLayer>`
  - `transitionLayer(layerId: string, targetState: LayerState, reason?: string): Promise<COPLayer>`
  - `addFeedback(layerId: string, feedback: Omit<ReviewFeedback, 'id' | 'createdAt'>): Promise<COPLayer>`
  - `recallLayer(layerId: string, reason: string): Promise<COPLayer>`

- **Version operations:**
  - `listVersions(layerId: string): Promise<LayerSnapshot[]>`
  - `getVersion(layerId: string, version: number): Promise<LayerSnapshot>`
  - `getVersionSpec(layerId: string, version: number): Promise<COPLayerSpec>`

- **Agent operations:**
  - `triggerGeneration(workspaceId: string, sectionId: string): Promise<COPLayer>`
  - `startPolling(workspaceId: string, sectionId: string, intervalMs?: number): Promise<void>`
  - `stopPolling(workspaceId: string, sectionId: string): Promise<void>`
  - `getAgentActivity(workspaceId: string, limit?: number): Promise<AgentActivity[]>`

- **Linkage operations:**
  - `getPendingLinkages(workspaceId?: string): Promise<EntityLinkage[]>`
  - `reviewLinkage(linkageId: string, approved: boolean): Promise<EntityLinkage>`
  - `getEntityLinkages(entityId: string): Promise<EntityLinkage[]>`

- **Conflict operations:**
  - `getConflicts(workspaceId: string): Promise<COPConflict[]>`

All methods use the project's standard fetch pattern with error handling. Export as singleton `copService`.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Typed API client covers all COP endpoints. Methods return typed responses. Error handling consistent with project patterns.</done>
</task>

<task type="auto">
  <name>Task 2: COP map view with layer controls and perspective toggle</name>
  <files>frontend/src/components/cop/COPMapView.tsx, frontend/src/components/cop/COPLayerControls.tsx, frontend/src/components/cop/COPPerspectiveToggle.tsx, frontend/src/components/cop/COPMapView.css, frontend/src/components/cop/COPLayerControls.css</files>
  <action>
Create `frontend/src/components/cop/COPMapView.tsx`:
- Main component receiving workspaceId prop
- State: layers (COPLayer[]), layerVisibility (Record&lt;string, boolean&gt;), layerOpacity (Record&lt;string, number&gt;), currentPerspective (Perspective), loading
- On mount: fetch layers via copService.queryLayers(workspaceId, { state: 'cop' }) for top-level COP, also fetch 'published' for section-level view
- Render a Leaflet MapContainer (follow COASketchMap.tsx pattern for map setup)
- For each visible layer, render its symbols:
  - Filter symbols by perspective: 'friendly' shows affiliation=friendly only, 'adversary' shows affiliation=enemy only, 'combined' shows all
  - For each COPSymbolSpec, render a MilSymbolMarker using createMilSymbolIcon(symbol.sidc) at symbol.position
  - Set marker opacity based on layerOpacity[layer.id]
  - For control measures: render Polyline/Polygon components with label
  - For custom annotations: render in a shadow DOM container (sandboxed). Use a ref to create shadow root, inject sanitized SVG. Position via Leaflet's L.svgOverlay or absolute positioning.
- Pass layerVisibility/layerOpacity/perspective state to COPLayerControls and COPPerspectiveToggle

Create `frontend/src/components/cop/COPLayerControls.tsx`:
- Extends IPBLayerControls pattern: grouped by layerType (Force Disposition, Objectives, etc.)
- Each layer row: checkbox for visibility, opacity slider (0-100%), layer type icon/color swatch, state badge (Draft/Review/Published/COP), layer name
- Group headers with expand/collapse
- "Show All" / "Hide All" buttons
- Layer count indicator: "3/6 layers visible"

Create `frontend/src/components/cop/COPPerspectiveToggle.tsx`:
- Three-way toggle: Friendly | Adversary | Combined
- Styled as segmented control with military colors (blue for friendly, red for adversary, gray for combined)
- Emits onPerspectiveChange callback

CSS files: follow existing component CSS patterns (COASketchMap.css, IPBLayerControls pattern). Use Tailwind utilities where consistent with project patterns. Layer controls panel: positioned as sidebar overlay on right side of map.

Shadow DOM for LLM-generated SVG:
- Create a SandboxedSVG wrapper component that uses useRef + useEffect to attach shadow DOM and inject sanitized SVG
- Keep interactive elements (click handlers for entity selection) OUTSIDE the shadow DOM
- Use data-entity-id attributes to map clicks back to entities
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>COP map renders stacked layers with milsymbol symbols. Layer controls toggle visibility and opacity. Perspective toggle filters by affiliation. Shadow DOM sandboxes LLM-generated SVG. TypeScript compiles.</done>
</task>

</tasks>

<verification>
- Frontend TypeScript compiles clean
- COPMapView renders Leaflet map with symbol markers
- Layer controls group by type with visibility/opacity
- Perspective toggle filters symbols by affiliation
</verification>

<success_criteria>
- COP map shows all promoted layers stacked with standard military symbology
- Staff can toggle individual layer visibility and adjust opacity
- Perspective switch instantly filters friendly/adversary/combined views
- LLM-generated SVG safely sandboxed in shadow DOM
- API service provides typed access to all COP operations
</success_criteria>

<output>
After completion, create `.planning/phases/21-ai-cop-layer-agent-team/21-08-SUMMARY.md`
</output>
