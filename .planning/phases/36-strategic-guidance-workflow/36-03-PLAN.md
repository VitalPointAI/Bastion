---
phase: 36-strategic-guidance-workflow
plan: 03
type: execute
wave: 2
depends_on: [36-01, 36-02]
files_modified:
  - frontend/src/components/plan/steps/StrategicAssessment.tsx
  - frontend/src/components/plan/steps/OperationalApproach.tsx
  - frontend/src/components/plan/ForceApportionmentPanel.tsx
  - frontend/src/components/plan/ConstraintManager.tsx
  - frontend/src/components/plan/StrategicGuidancePlanView.tsx
autonomous: true
requirements: [SG-05, SG-06, SG-07, SG-08]

must_haves:
  truths:
    - "Strategic Assessment step shows strategic environment summary, center of gravity analysis (friendly + adversary), key assumptions, and strategic factors"
    - "Operational Approach step shows lines of effort, objectives hierarchy, force apportionment, and constraints/restraints/assumptions/limitations"
    - "Force apportionment panel displays forces organized by line of effort with priority tier badges (Main Effort, Supporting Effort, Reserve, Economy of Force)"
    - "Constraint manager supports all 4 doctrinal types with source authority and applicability fields"
    - "Step content auto-saves and updates step status from not_started to in_progress"
  artifacts:
    - path: "frontend/src/components/plan/steps/StrategicAssessment.tsx"
      provides: "Step 1 content: environment summary, COG analysis, assumptions, strategic factors"
      exports: ["StrategicAssessment"]
    - path: "frontend/src/components/plan/steps/OperationalApproach.tsx"
      provides: "Step 2 content: LOE manager, objectives, force apportionment, constraints"
      exports: ["OperationalApproach"]
    - path: "frontend/src/components/plan/ForceApportionmentPanel.tsx"
      provides: "LOE-based force allocation visualization with priority tiers and summary dashboard"
      exports: ["ForceApportionmentPanel"]
    - path: "frontend/src/components/plan/ConstraintManager.tsx"
      provides: "Doctrinal constraint/restraint/assumption/limitation editor"
      exports: ["ConstraintManager"]
  key_links:
    - from: "frontend/src/components/plan/steps/StrategicAssessment.tsx"
      to: "/api/strategic-guidance"
      via: "sgService.getStepContent and sgService.saveStepContent"
      pattern: "sgService\\."
    - from: "frontend/src/components/plan/steps/OperationalApproach.tsx"
      to: "frontend/src/components/plan/ForceApportionmentPanel.tsx"
      via: "ForceApportionmentPanel component render"
      pattern: "ForceApportionmentPanel"
    - from: "frontend/src/components/plan/StrategicGuidancePlanView.tsx"
      to: "frontend/src/components/plan/steps/"
      via: "import and render step components replacing placeholders"
      pattern: "StrategicAssessment|OperationalApproach"
---

<objective>
Build the Strategic Assessment and Operational Approach step content components, plus the shared ForceApportionmentPanel and ConstraintManager. Wire into StrategicGuidancePlanView replacing placeholder content.

Purpose: Deliver the first two steps of the strategic guidance workflow with full interactive content — the strategic analyst reviews the environment and COG analysis, then the planner defines LOEs, force allocation, and constraints.

Output: Two functional step components with data persistence, plus reusable force apportionment and constraint management panels.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-strategic-guidance-workflow/36-CONTEXT.md
@.planning/phases/36-strategic-guidance-workflow/36-RESEARCH.md
@.planning/phases/36-strategic-guidance-workflow/36-01-SUMMARY.md
@.planning/phases/36-strategic-guidance-workflow/36-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 types.ts -->
StrategicAssessmentContent: { strategicEnvironmentSummary, centerOfGravityAnalysis: { friendly: { cog, criticalCapabilities[], criticalRequirements[], criticalVulnerabilities[] }, adversary: same }, keyAssumptions: Assumption[], strategicFactors: string[], sourceContainerIds: string[] }

OperationalApproachContent: { linesOfEffort: LineOfEffort[], objectivesHierarchy: { id, parentId?, title, description }[], forceApportionment: ForceAllocation[], constraints: ConstraintEntry[], restraints: ConstraintEntry[], assumptions: Assumption[], limitations: ConstraintEntry[] }

ForceAllocation: { id, forceId, forceName, forceType, isRegistered, lineOfEffortId, priority: ForceAllocationPriority, allocationPct, notes }
LineOfEffort: { id, name, description, linkedObjectiveIds: string[], allocatedForces: ForceAllocation[] }
ConstraintEntry: { id, type: ConstraintType, description, sourceAuthority, applicability, applicablePhases?, inheritedFrom?, canDelete }
Assumption: { id, description, validityConditions: string[], isValid, invalidatedAt?, invalidatedReason? }

<!-- From Plan 02 -->
sgService: { getStepContent, saveStepContent, getForceAllocations, saveForceAllocation, deleteForceAllocation, getForceAllocationSummary }
StrategicGuidanceStepLayout wraps step content
SGStepId, SGStepConfig from StrategicGuidanceStepConfig.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StrategicAssessment step, ConstraintManager, and ForceApportionmentPanel</name>
  <files>frontend/src/components/plan/steps/StrategicAssessment.tsx, frontend/src/components/plan/ConstraintManager.tsx, frontend/src/components/plan/ForceApportionmentPanel.tsx</files>
  <action>
**StrategicAssessment.tsx** — Step 1 content component:
- Props: { problemSetId: string; instanceId: string }
- On mount: load step content via sgService.getStepContent(instanceId, 'strategic_assessment')
- Render sections with inline editing (textarea/input fields):
  1. **Strategic Environment Summary** — large textarea for narrative summary. Note above: "Pull from strategic document containers in the Understand tab for source material."
  2. **Center of Gravity Analysis** — two-column layout (Friendly | Adversary), each with: COG (text input), Critical Capabilities (editable list), Critical Requirements (editable list), Critical Vulnerabilities (editable list). Use simple add/remove pattern for list items.
  3. **Key Assumptions** — list of assumptions with description textarea, validity conditions (editable string list), isValid toggle. Add/remove assumption buttons.
  4. **Strategic Factors** — editable list of strategic factor strings
- Auto-save on blur/change with debounce (500ms). Call sgService.saveStepContent(instanceId, 'strategic_assessment', content).
- Styling: dark theme (gray-800 backgrounds, gray-300 text), section headers with bottom borders, consistent with existing Plan tab styling.

**ConstraintManager.tsx** — Reusable constraint/restraint/assumption/limitation editor:
- Props: { constraints: ConstraintEntry[]; onChange: (updated: ConstraintEntry[]) => void; readOnlyInherited?: boolean }
- Render grouped by type with headers: "Constraints (Must Do)", "Restraints (Must Not Do)", "Assumptions", "Limitations"
- Each entry shows: description (textarea), sourceAuthority (text input, e.g. "SECDEF"), applicability toggle (standing | phase_bounded), applicablePhases (multi-select if phase_bounded)
- Inherited entries (inheritedFrom !== undefined): show with a badge "Inherited from [parent]", canDelete=false disables remove button
- Add button per group to create new entry with generated crypto.randomUUID() id
- Remove button per entry (disabled if canDelete=false)
- Color-code by type: constraint=blue border-left, restraint=red border-left, assumption=yellow border-left, limitation=gray border-left

**ForceApportionmentPanel.tsx** — LOE-based force allocation visualization:
- Props: { instanceId: string; linesOfEffort: LineOfEffort[]; onAllocationsChange?: () => void }
- On mount: load allocations via sgService.getForceAllocations(instanceId)
- Layout: vertical stack of LOE sections. Each LOE section:
  - LOE header with name and description
  - Allocated forces as cards within the LOE section. Each card shows: forceName, forceType, priority tier badge (color-coded: main_effort=red, supporting_effort=blue, reserve=green, economy_of_force=gray), allocationPct bar, isRegistered indicator (checkmark if registered, warning icon if not), notes
  - "Add Force" button per LOE — opens inline form with: forceName (text), forceType (text), priority (select from 4 tiers), allocationPct (number 0-100), notes (textarea). isRegistered defaults to false (future Phase 27 integration will add registry lookup).
  - Remove force button per card
- **Summary dashboard** at top: Total forces across all LOEs, Main Effort count, Forces committed (sum allocationPct), over-allocation warning if any LOE total > 100%
- Save/delete via sgService.saveForceAllocation / sgService.deleteForceAllocation, then refresh
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit frontend/src/components/plan/steps/StrategicAssessment.tsx frontend/src/components/plan/ConstraintManager.tsx frontend/src/components/plan/ForceApportionmentPanel.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Strategic Assessment shows environment summary, COG analysis, assumptions, strategic factors with auto-save. ConstraintManager handles all 4 doctrinal types with inherited entry support. ForceApportionmentPanel shows LOE-based allocation with priority tiers and summary dashboard.</done>
</task>

<task type="auto">
  <name>Task 2: Create OperationalApproach step and wire both steps into StrategicGuidancePlanView</name>
  <files>frontend/src/components/plan/steps/OperationalApproach.tsx, frontend/src/components/plan/StrategicGuidancePlanView.tsx</files>
  <action>
**OperationalApproach.tsx** — Step 2 content component:
- Props: { problemSetId: string; instanceId: string }
- On mount: load step content via sgService.getStepContent(instanceId, 'operational_approach')
- Render sections:
  1. **Lines of Effort** — List of LOEs with add/edit/remove. Each LOE: name (text input), description (textarea), linkedObjectiveIds (multi-select or text list of objective IDs). Add LOE button generates new LOE with crypto.randomUUID() id.
  2. **Objectives Hierarchy** — Flat editable list of objectives: id, parentId (select from other objectives or null for top-level), title, description. Add/remove objective. Display as indented tree based on parentId relationships.
  3. **Force Apportionment** — Render ForceApportionmentPanel component, passing linesOfEffort from step content and instanceId. This is the main force allocation area.
  4. **Constraints & Restraints** — Render ConstraintManager component, passing combined constraints/restraints/assumptions/limitations from step content, with onChange handler that updates the step content.
- Auto-save on changes with debounce (500ms). Save both step content (LOEs, objectives, constraints) and force allocations (via ForceApportionmentPanel's own save).
- Styling: consistent with StrategicAssessment — dark theme sections with headers.

**StrategicGuidancePlanView.tsx** — Replace placeholder content with real step components:
- Add imports at top:
  ```
  import { StrategicAssessment } from './steps/StrategicAssessment.tsx';
  import { OperationalApproach } from './steps/OperationalApproach.tsx';
  ```
- In the step rendering loop, replace the placeholder divs for 'strategic_assessment' and 'operational_approach':
  - strategic_assessment: `<StrategicAssessment problemSetId={problemSetId} instanceId={instance.id} />`
  - operational_approach: `<OperationalApproach problemSetId={problemSetId} instanceId={instance.id} />`
- Keep the commander_directive step as placeholder (Plan 04 will replace it)
- Create the `frontend/src/components/plan/steps/` directory if it doesn't exist
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit frontend/src/components/plan/steps/OperationalApproach.tsx frontend/src/components/plan/StrategicGuidancePlanView.tsx 2>&1 | head -20</automated>
  </verify>
  <done>OperationalApproach renders LOE manager, objectives hierarchy, force apportionment panel, and constraint manager. StrategicGuidancePlanView renders real step components for steps 1 and 2, with placeholder remaining only for step 3.</done>
</task>

</tasks>

<verification>
- StrategicAssessment renders all 4 sections with auto-save
- OperationalApproach renders LOE manager, objectives, force apportionment, constraints
- ForceApportionmentPanel shows LOE-based layout with priority tier badges and summary
- ConstraintManager groups entries by doctrinal type with color coding
- StrategicGuidancePlanView renders real content for steps 1 and 2
- All files pass TypeScript type checking
</verification>

<success_criteria>
- Strategic Assessment step displays and saves environment summary, COG analysis, assumptions, strategic factors
- Operational Approach step displays and saves LOEs, objectives, force allocations, constraints
- Force apportionment shows forces organized by LOE with priority tier color coding and summary dashboard
- Constraint manager handles all 4 types (constraint, restraint, assumption, limitation) with source authority
- Auto-save works with debounce on content changes
</success_criteria>

<output>
After completion, create `.planning/phases/36-strategic-guidance-workflow/36-03-SUMMARY.md`
</output>
