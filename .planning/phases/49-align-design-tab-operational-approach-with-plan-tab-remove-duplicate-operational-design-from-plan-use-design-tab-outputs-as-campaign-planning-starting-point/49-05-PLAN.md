---
phase: 49-align-design-tab-operational-approach-with-plan-tab-remove-duplicate-operational-design-from-plan-use-design-tab-outputs-as-campaign-planning-starting-point
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 04]
files_modified:
  - frontend/src/components/governance/DecisionGateBanner.tsx
  - frontend/src/lib/design-service.ts
  - backend/src/api/design.ts
autonomous: false

requirements: [DAP-03, DAP-05]

must_haves:
  truths:
    - "Governance dashboard shows design_revision gates with proper labels"
    - "Approving a design_revision gate triggers merge of proposed data into Design tab"
    - "Push-handoff endpoint is deprecated with no UI trigger"
    - "Full flow works: Design data appears in Plan tab read-only, revision proposal creates gate, approval merges back"
  artifacts:
    - path: "frontend/src/components/governance/DecisionGateBanner.tsx"
      provides: "design_revision gate type label in GATE_TYPE_LABELS"
      contains: "design_revision"
  key_links:
    - from: "governance gate approval"
      to: "PATCH /revisions/:id/status + /revisions/:id/merge"
      via: "gate approval callback"
      pattern: "design_revision"
---

<objective>
Wire the governance approval callback to trigger revision merges, add design_revision to governance labels, deprecate push-handoff, and verify the complete Design-to-Plan flow with a human checkpoint.

Purpose: Close the loop on the fork-and-merge system — approved revisions automatically merge back to Design, and the full bidirectional flow (Design -> Plan read-only, Plan -> revision proposal -> governance -> merge -> Design) is verified end-to-end.

Output: Complete bidirectional Design/Plan integration with governance-backed revision merging.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-align-design-tab-operational-approach-with-plan-tab-remove-duplicate-operational-design-from-plan-use-design-tab-outputs-as-campaign-planning-starting-point/49-CONTEXT.md
@.planning/phases/49-align-design-tab-operational-approach-with-plan-tab-remove-duplicate-operational-design-from-plan-use-design-tab-outputs-as-campaign-planning-starting-point/49-01-SUMMARY.md
@.planning/phases/49-align-design-tab-operational-approach-with-plan-tab-remove-duplicate-operational-design-from-plan-use-design-tab-outputs-as-campaign-planning-starting-point/49-02-SUMMARY.md
@.planning/phases/49-align-design-tab-operational-approach-with-plan-tab-remove-duplicate-operational-design-from-plan-use-design-tab-outputs-as-campaign-planning-starting-point/49-03-SUMMARY.md
@.planning/phases/49-align-design-tab-operational-approach-with-plan-tab-remove-duplicate-operational-design-from-plan-use-design-tab-outputs-as-campaign-planning-starting-point/49-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire governance approval to revision merge and add gate type labels</name>
  <files>
    frontend/src/components/governance/DecisionGateBanner.tsx,
    frontend/src/lib/design-service.ts,
    backend/src/api/design.ts
  </files>
  <action>
1. In `DecisionGateBanner.tsx`:
   - Find the `GATE_TYPE_LABELS` object (or the `formatGateType` function) and add: `'design_revision': 'Design Revision Proposal'`
   - Also check `DecisionBriefView.tsx` and `PhaseProgressionBar.tsx` for `GATE_TYPE_LABELS` or `GATE_TYPE_SHORT` mappings and add the design_revision entry there too

2. Wire the governance approval callback to trigger revision merge:
   - Search the codebase for where gate approval status changes are handled (look for where `status === 'approved'` triggers downstream actions — likely in a gate approval handler or callback)
   - Add a handler: when a gate with `gate_type === 'design_revision'` is approved, call `PATCH /api/design/:problemSetId/revisions/:targetItemId/status` with `{ status: 'approved' }` then `PATCH /api/design/:problemSetId/revisions/:targetItemId/merge`
   - If no callback mechanism exists, add the merge trigger on the backend side: in the design-revisions API, check if there's a way to listen for gate status changes, or add it to the approval flow. The simplest approach: when the frontend displays an approved gate for `design_revision`, show a "Merge to Design" button that calls the merge endpoint. This is acceptable as a manual step in the approval flow.

3. In `design-service.ts`:
   - Add a deprecation comment to the `pushHandoff` method: `/** @deprecated Phase 49 — Plan tab fetches directly via getDesign(). Kept for backward compatibility. */`
   - Do NOT delete the method — backward compatibility

4. In `backend/src/api/design.ts`:
   - Add a deprecation comment to the `POST /:problemSetId/push-handoff` route handler
   - Do NOT delete the route — backward compatibility
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit 2>&1 | head -50</automated>
  </verify>
  <done>design_revision appears in GATE_TYPE_LABELS. Gate approval triggers revision merge (either via callback or manual "Merge to Design" button). pushHandoff deprecated with JSDoc comment. TypeScript compiles clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Design-to-Plan bidirectional flow</name>
  <action>
Human verifies the complete end-to-end integration of all 5 plans:
1. Strategic Guidance restructured: Assessment / Alignment / Directive (no more Operational Approach)
2. StrategicAssessment no longer has CoG section
3. DesignContextPanels in JPP Steps 2 (problem statement + CoG), 3 (LOEs), 7 (phases) — all read-only
4. DesignSyncIndicator in Design tab overview showing which sections flow to Plan
5. Fork-and-merge revision system: "Propose Revision" button -> modal with diff view -> DAO governance gate
6. Push-to-Plan button removed, replaced by automatic fetch-on-render
  </action>
  <verify>
1. Navigate to a problem set's Plan tab at strategic echelon — verify Strategic Guidance shows 3 steps: Assessment, Alignment, Directive (NOT Operational Approach)
2. Open Strategic Assessment step — verify NO CoG analysis section is present
3. Open Strategic Alignment step — verify it shows objective mapping UI with AI agent panel
4. Navigate to JPP Step 2 (Mission Analysis) — verify blue-tinted DesignContextPanels showing "Problem Statement (from Design)" and "Center of Gravity Analysis (from Design)" at the top
5. Navigate to JPP Step 3 (COA Development) — verify DesignContextPanel showing "Lines of Effort (from Design)"
6. Navigate to JPP Step 7 (Plan/Order Development) — verify DesignContextPanel showing "Operational Phases and Transitions (from Design)"
7. On any DesignContextPanel with complete Design data, click "Propose Revision" — verify modal opens with diff view and rationale field
8. Navigate to Design tab overview — verify sync indicator showing "-> Plan Step N" for each section
9. Verify Design tab no longer has "Push to Plan Tab" button
  </verify>
  <done>User approves all 9 verification items. Full bidirectional Design/Plan integration confirmed working.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes clean
- Full browser walkthrough of Design -> Plan flow
- Revision proposal creates both design_revision record and decision_gate record
- Governance dashboard shows design_revision gates
</verification>

<success_criteria>
Complete bidirectional Design/Plan integration verified: auto-sync (Design->Plan read-only), revision proposals (Plan->Design via governance), Strategic Guidance restructured, CoG removed from StrategicAssessment, push-handoff deprecated. User approves visual verification.
</success_criteria>

<output>
After completion, create `.planning/phases/49-align-design-tab-operational-approach-with-plan-tab-remove-duplicate-operational-design-from-plan-use-design-tab-outputs-as-campaign-planning-starting-point/49-05-SUMMARY.md`
</output>
