---
phase: 24-doctrinal-tab-restructure-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/tabs/DoctrinalPlaceholder.tsx
  - frontend/src/components/tabs/UnderstandTab.tsx
  - frontend/src/components/tabs/PlanTab.tsx
  - frontend/src/components/tabs/DirectTab.tsx
  - frontend/src/components/tabs/AssessTab.tsx
  - frontend/src/components/tabs/DesignTab.tsx
autonomous: true
requirements: [TAB-01, TAB-02, TAB-03]

must_haves:
  truths:
    - "UnderstandTab renders StrategicDashboard and SubscriptionManager with sidebar navigation"
    - "PlanTab renders Missions (MissionList/Detail/Wizard) and MDMP Workflow with sidebar navigation"
    - "DirectTab renders Governance, Proposals, and Escalation with sidebar navigation"
    - "AssessTab and DesignTab render DoctrinalPlaceholder with correct doctrinal descriptions and workflow position indicator"
    - "DoctrinalPlaceholder shows all 6 tabs in a horizontal progress bar with the current tab highlighted"
  artifacts:
    - path: "frontend/src/components/tabs/DoctrinalPlaceholder.tsx"
      provides: "Reusable placeholder component with workflow position indicator"
      exports: ["DoctrinalPlaceholder"]
    - path: "frontend/src/components/tabs/UnderstandTab.tsx"
      provides: "Understand tab with strategic docs + data sharing"
      exports: ["UnderstandTab"]
    - path: "frontend/src/components/tabs/PlanTab.tsx"
      provides: "Plan tab with missions + MDMP"
      exports: ["PlanTab"]
    - path: "frontend/src/components/tabs/DirectTab.tsx"
      provides: "Direct tab with governance + proposals + escalation"
      exports: ["DirectTab"]
    - path: "frontend/src/components/tabs/AssessTab.tsx"
      provides: "Assess tab placeholder"
      exports: ["AssessTab"]
  key_links:
    - from: "frontend/src/components/tabs/UnderstandTab.tsx"
      to: "../strategic/index.js"
      via: "import StrategicDashboard"
      pattern: "import.*StrategicDashboard"
    - from: "frontend/src/components/tabs/DirectTab.tsx"
      to: "../dao/index.js"
      via: "import DAODashboard"
      pattern: "import.*DAODashboard"
    - from: "frontend/src/components/tabs/PlanTab.tsx"
      to: "../mission/index.js"
      via: "import MissionList, MissionDetail, MissionWizard"
      pattern: "import.*MissionList"
---

<objective>
Create the new doctrinal tab components and a reusable placeholder, redistributing existing content from DecideTab, DesignTab, and CampaignTab into UnderstandTab, PlanTab, DirectTab, and AssessTab per JP 5-0 alignment.

Purpose: Produce the tab component files that Plan 02 will wire into ProblemSetTabContainer.
Output: 6 new/rewritten component files in frontend/src/components/tabs/
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-doctrinal-tab-restructure-inserted/24-CONTEXT.md
@.planning/phases/24-doctrinal-tab-restructure-inserted/24-RESEARCH.md

<interfaces>
<!-- Existing imports the new tabs must use -->

From frontend/src/components/tabs/TabLayout.tsx:
```typescript
export interface SidebarItem {
  id: string;
  label: string;
  tooltip?: string;
}

export function TabLayout(props: {
  items: SidebarItem[];
  selectedItem: string;
  onSelectItem: (id: string) => void;
  children: React.ReactNode;
}): JSX.Element;
```

From frontend/src/components/strategic/index.js:
```typescript
export { StrategicDashboard } from './StrategicDashboard';
```

From frontend/src/components/dao/index.js:
```typescript
export { DAODashboard } from './DAODashboard';
```

From frontend/src/components/mission/index.js:
```typescript
export { MissionList, MissionDetail, MissionWizard } from '...';
```

From frontend/src/components/problem-set/EscalationPanel.js:
```typescript
export function EscalationPanel({ problemSetId }: { problemSetId: string }): JSX.Element;
```

From frontend/src/components/problem-set/SubscriptionManager.js:
```typescript
export function SubscriptionManager({ problemSetId }: { problemSetId: string }): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DoctrinalPlaceholder component</name>
  <files>frontend/src/components/tabs/DoctrinalPlaceholder.tsx</files>
  <action>
Create a reusable placeholder component for tabs that lack functional content yet.

Props interface:
```typescript
interface DoctrinalPlaceholderProps {
  tabId: string;
  tabName: string;
  description: string;      // Doctrinal purpose of this tab
  futureContent: string;    // What it will contain
  deliveredBy: string;      // Which future phase delivers it
}
```

Component renders:
1. A horizontal workflow position indicator bar showing all 6 tabs: Understand > Design > Plan > Direct > COP > Assess. Use a flex row of rounded segments with chevron separators. The current tab (matching `tabId`) gets `bg-blue-600 text-white`; others get `bg-gray-700 text-gray-400`. Use `Fragment` from React for the map.

2. Below the bar: `tabName` as an h2 heading (text-xl, font-semibold, text-gray-200), then `description` as a paragraph (text-sm, text-gray-400).

3. A card (bg-gray-800, border border-gray-700, rounded-lg, p-4) showing "Coming in {deliveredBy}" as a heading and `futureContent` as body text.

Center everything vertically and horizontally with `min-h-[60vh]` and `max-w-2xl mx-auto`.

The ALL_TABS constant and TAB_DISPLAY record are local to this file (the canonical tab definitions live in ProblemSetTabContainer, but this component needs its own copy for the visual indicator).

```typescript
const ALL_TABS = ['understand', 'design', 'plan', 'direct', 'cop', 'assess'] as const;
const TAB_DISPLAY: Record<string, string> = {
  understand: 'Understand', design: 'Design', plan: 'Plan',
  direct: 'Direct', cop: 'COP', assess: 'Assess',
};
```

Export as named export: `export function DoctrinalPlaceholder(...)`.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit frontend/src/components/tabs/DoctrinalPlaceholder.tsx 2>&1 | head -20</automated>
  </verify>
  <done>DoctrinalPlaceholder.tsx exists, exports the component, TypeScript compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create UnderstandTab, PlanTab, DirectTab, AssessTab and rewrite DesignTab</name>
  <files>
    frontend/src/components/tabs/UnderstandTab.tsx
    frontend/src/components/tabs/PlanTab.tsx
    frontend/src/components/tabs/DirectTab.tsx
    frontend/src/components/tabs/AssessTab.tsx
    frontend/src/components/tabs/DesignTab.tsx
  </files>
  <action>
Create 4 new tab files and rewrite DesignTab to be a placeholder. Each tab uses `TabLayout` for sidebar navigation following the exact pattern in the existing DecideTab/CampaignTab.

**UnderstandTab.tsx** — Contains StrategicDashboard + Data Sharing (SubscriptionManager)
- Props: `{ problemSetId: string }`
- Sidebar items: `[{ id: 'strategic-docs', label: 'Strategic Documents' }, { id: 'subscriptions', label: 'Data Sharing', tooltip: 'Manage cross-problem-set data subscriptions' }]`
- Default view: 'strategic-docs'
- Renders StrategicDashboard for 'strategic-docs', SubscriptionManager for 'subscriptions'
- Import StrategicDashboard from `../strategic/index.js`, SubscriptionManager from `../problem-set/SubscriptionManager.js`

**PlanTab.tsx** — Contains Missions + MDMP Workflow
- Props: `{ problemSetId: string; daoId?: string }`
- Sidebar items: `[{ id: 'missions', label: 'Missions' }, { id: 'mdmp-workflow', label: 'MDMP Workflow', tooltip: 'Military Decision Making Process phase progression and governance gates' }]`
- Default view: 'missions'
- For 'missions': replicate the exact CampaignTab rendering logic (MissionList/MissionDetail/MissionWizard with selectedMissionId/showWizard state). Import from `../mission/index.js` and `../../context/UserContext.js`.
- For 'mdmp-workflow': render `<DAODashboard daoId={daoId} initialView="mdmp" />` from `../dao/index.js`

**DirectTab.tsx** — Contains Governance + Proposals + Escalation
- Props: `{ problemSetId: string; daoId?: string }`
- Sidebar items: `[{ id: 'governance', label: 'Governance Overview' }, { id: 'proposals', label: 'Proposals & Voting', tooltip: 'Active proposals requiring action' }, { id: 'escalation', label: 'Escalation', tooltip: 'Escalate decisions to parent problem set' }]`
- Default view: 'governance'
- For 'governance': render `<DAODashboard daoId={daoId} initialView="governance" />`
- For 'proposals': render `<DAODashboard daoId={daoId} initialView="proposals" />`
- For 'escalation': render `<EscalationPanel problemSetId={problemSetId} />`
- Import DAODashboard from `../dao/index.js`, EscalationPanel from `../problem-set/EscalationPanel.js`

**AssessTab.tsx** — Clean placeholder
- Props: `{ problemSetId: string }` (accept but unused for now — needed for interface consistency)
- Renders `<DoctrinalPlaceholder>` with:
  - tabId: "assess"
  - tabName: "Assess"
  - description: "Assessment measures progress toward accomplishing objectives and determines the effectiveness of ongoing operations. It enables adaptation of plans and operations."
  - futureContent: "Measures of effectiveness (MOEs), measures of performance (MOPs), assessment dashboards, and reframing triggers."
  - deliveredBy: "Phase 28+"

**DesignTab.tsx** — Rewrite to placeholder (StrategicDashboard moved to Understand)
- Props: `{ problemSetId: string }` (accept but unused)
- Renders `<DoctrinalPlaceholder>` with:
  - tabId: "design"
  - tabName: "Design"
  - description: "Operational Design translates strategic guidance into an operational approach. This is where commanders and planners develop the broad concept for achieving objectives."
  - futureContent: "Operational design workspace with center of gravity analysis, lines of effort/operation, decisive points, and operational approach visualization."
  - deliveredBy: "Phase 25"
- Remove old StrategicDashboard import. Import DoctrinalPlaceholder instead.

All files use named exports matching their component name.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && for f in UnderstandTab PlanTab DirectTab AssessTab DesignTab; do echo "--- $f ---"; npx tsc --noEmit "frontend/src/components/tabs/${f}.tsx" 2>&1 | head -5; done</automated>
  </verify>
  <done>All 5 tab files exist with correct content, correct imports, correct sidebar items, TypeScript compiles without errors</done>
</task>

</tasks>

<verification>
All 6 component files (DoctrinalPlaceholder + 5 tabs) exist in frontend/src/components/tabs/ with named exports and correct TypeScript types.
</verification>

<success_criteria>
- DoctrinalPlaceholder renders workflow position indicator with 6 tabs
- UnderstandTab has StrategicDashboard + SubscriptionManager via TabLayout sidebar
- PlanTab has Missions + MDMP Workflow via TabLayout sidebar
- DirectTab has Governance + Proposals + Escalation via TabLayout sidebar
- AssessTab and DesignTab render DoctrinalPlaceholder with correct doctrinal content
- All files compile with TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/24-doctrinal-tab-restructure-inserted/24-01-SUMMARY.md`
</output>
