---
phase: 23-problem-set-model-workspace-rename-inserted
plan: 05
type: execute
wave: 3
depends_on: ["23-03", "23-04"]
files_modified:
  - backend/src/api/problem-sets.ts
  - backend/src/api/workspaces.ts
  - backend/src/server.ts
  - backend/src/cop/coordinator.ts
  - backend/src/cop/sub-agents/*.ts
  - backend/src/exercise/*.ts
  - backend/src/mission/*.ts
  - backend/src/planning/*.ts
  - backend/src/middleware/mode-context.ts
autonomous: true
requirements:
  - PS-API-RENAME
  - PS-CROSS-CUTTING

must_haves:
  truths:
    - "API route /api/workspaces renamed to /api/problem-sets"
    - "API route file renamed from workspaces.ts to problem-sets.ts"
    - "Server entry point registers /api/problem-sets route"
    - "All COP agent files reference problemSetId instead of workspaceId"
    - "All exercise, mission, planning modules import from problem-set"
    - "Mode context middleware references problem set terminology"
    - "Old workspace/ directory deleted and old workspace API route removed"
  artifacts:
    - path: "backend/src/api/problem-sets.ts"
      provides: "Problem set API routes"
      exports: ["problemSetsRouter"]
  key_links:
    - from: "backend/src/api/problem-sets.ts"
      to: "backend/src/problem-set/problem-set-store.ts"
      via: "store imports"
      pattern: "import.*problemSetStore.*from.*problem-set"
    - from: "backend/src/server.ts"
      to: "backend/src/api/problem-sets.ts"
      via: "route registration"
      pattern: "problem-sets"
---

<objective>
Rename the backend API routes and update all cross-cutting backend modules (COP, exercise, mission, planning, middleware) to use problem-set imports and terminology.

Purpose: Complete the backend rename by updating the API layer and every module that imports from the workspace system. After this plan, the entire backend uses problem-set terminology with zero workspace references.

Output: Renamed API route file, updated server registration, and all cross-cutting modules updated.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-problem-set-model-workspace-rename-inserted/23-CONTEXT.md
@.planning/phases/23-problem-set-model-workspace-rename-inserted/23-RESEARCH.md
@.planning/phases/23-problem-set-model-workspace-rename-inserted/23-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename API routes and update server registration</name>
  <files>
    backend/src/api/problem-sets.ts
    backend/src/server.ts
  </files>
  <action>
1. Read `backend/src/api/workspaces.ts` entirely.
2. Create `backend/src/api/problem-sets.ts` with all renames:
   - Router/handler name: `workspacesRouter` -> `problemSetsRouter`
   - Route paths: `/workspaces` -> `/problem-sets` (or wherever the prefix is applied)
   - Import paths: `../workspace/workspace-store.js` -> `../problem-set/problem-set-store.js` (and all other store imports)
   - Import names: `workspaceStore` -> `problemSetStore`, `WorkspaceStore` -> `ProblemSetStore`, etc.
   - Parameter names: `workspaceId` -> `problemSetId` in request params
   - Response field names: workspace -> problemSet where appropriate
   - Variable names: workspace -> problemSet throughout
   - Type references: Workspace -> ProblemSet, WorkspaceType -> Echelon, etc.
   - Add echelon validation on create: call `validateEchelonHierarchy` when parent is specified

3. Read `backend/src/server.ts` (or main entry point — could be `index.ts` or `app.ts`).
4. Update the server to:
   - Import `problemSetsRouter` from `./api/problem-sets.js`
   - Register under `/api/problem-sets` path
   - Remove old `/api/workspaces` import/registration

5. Delete `backend/src/api/workspaces.ts` after the new file is created.

Also check for any other API files that reference workspace routes (e.g., graph workspace API endpoints) and update those.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && grep -r "api/workspaces\|workspacesRouter\|from.*workspace/workspace" backend/src/api/ backend/src/server.ts backend/src/index.ts 2>/dev/null --include="*.ts" -l | head -10</automated>
  </verify>
  <done>API route file renamed, server registers /api/problem-sets, no remaining references to old workspace API paths in API layer or server entry.</done>
</task>

<task type="auto">
  <name>Task 2: Update all cross-cutting backend modules</name>
  <files>
    backend/src/cop/**/*.ts
    backend/src/exercise/*.ts
    backend/src/mission/*.ts
    backend/src/planning/*.ts
    backend/src/middleware/*.ts
  </files>
  <action>
This is the comprehensive cross-cutting update. Run:
```
grep -r "workspace" backend/src/ --include="*.ts" -l | grep -v "node_modules\|problem-set\|workspace/"
```
to find ALL files outside the workspace/ and problem-set/ directories that reference "workspace".

For EACH file found:
1. Read the file
2. Update import paths: `../workspace/` -> `../problem-set/`, `./workspace/` -> `./problem-set/`
3. Update import names: workspaceStore -> problemSetStore, Workspace -> ProblemSet, etc.
4. Update variable names: workspaceId -> problemSetId, workspace -> problemSet
5. Update string literals in agent prompts/tools: "workspace" -> "problem set" where it appears in user-facing text
6. Update function parameter names

Key modules to check (per research):
- `backend/src/cop/` (~15 files): Agent tools, coordinators, sub-agents all use workspaceId
- `backend/src/exercise/` (~6 files): Exercise stores have workspace_id FK
- `backend/src/mission/` (~4 files): Mission store/types reference workspaceId
- `backend/src/planning/` (~1 file): Planning types reference workspaceId
- `backend/src/middleware/mode-context.ts`: Mode context references workspace

After updating all files, delete the OLD workspace directory:
```bash
rm -rf backend/src/workspace/
rm -rf backend/src/graph/workspace/
```

Then verify the entire backend compiles:
```bash
cd backend && npx tsc --noEmit
```

Fix any compilation errors.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && grep -r "from.*['\"].*workspace" backend/src/ --include="*.ts" -l 2>/dev/null | grep -v node_modules | head -20</automated>
  </verify>
  <done>Zero files in backend/src/ import from old workspace paths. Old workspace/ and graph/workspace/ directories deleted. Backend compiles with `tsc --noEmit` (or minimal errors from frontend-only issues).</done>
</task>

</tasks>

<verification>
- `backend/src/api/problem-sets.ts` exists, `workspaces.ts` deleted
- `backend/src/workspace/` directory no longer exists
- `backend/src/graph/workspace/` directory no longer exists
- `grep -r "from.*workspace" backend/src/ --include="*.ts"` returns zero matches
- Backend compiles cleanly
</verification>

<success_criteria>
Entire backend uses problem-set terminology exclusively. No workspace references remain in any backend TypeScript file. All cross-cutting modules updated. Backend compiles.
</success_criteria>

<output>
After completion, create `.planning/phases/23-problem-set-model-workspace-rename-inserted/23-05-SUMMARY.md`
</output>
