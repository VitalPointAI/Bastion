---
phase: 23-problem-set-model-workspace-rename-inserted
plan: 06
type: execute
wave: 3
depends_on: ["23-03"]
files_modified:
  - frontend/src/lib/problem-set-service.ts
  - frontend/src/context/ProblemSetContext.tsx
autonomous: true
requirements:
  - PS-FRONTEND-SERVICE
  - PS-FRONTEND-CONTEXT
  - PS-LOCALSTORAGE

must_haves:
  truths:
    - "Frontend service layer calls /api/problem-sets endpoints"
    - "All service function names use problemSet terminology"
    - "ProblemSetContext replaces WorkspaceContext with renamed hook useProblemSet"
    - "localStorage keys migrated from workspace-* to problem-set-*"
    - "One-time localStorage migration runs on app init"
  artifacts:
    - path: "frontend/src/lib/problem-set-service.ts"
      provides: "API service layer for problem sets"
      exports: ["problemSetService", "ProblemSetMembership", "ProblemSetDetail"]
    - path: "frontend/src/context/ProblemSetContext.tsx"
      provides: "React context for active problem set state"
      exports: ["ProblemSetProvider", "useProblemSet"]
  key_links:
    - from: "frontend/src/lib/problem-set-service.ts"
      to: "/api/problem-sets"
      via: "fetch calls"
      pattern: "api/problem-sets"
    - from: "frontend/src/context/ProblemSetContext.tsx"
      to: "frontend/src/lib/problem-set-service.ts"
      via: "service import"
      pattern: "import.*problemSetService"
---

<objective>
Create the renamed frontend service layer and context provider, including localStorage key migration.

Purpose: These are the two foundational frontend files that every component depends on. The service layer connects to the renamed API endpoints, and the context provider manages active problem set state app-wide.

Output: `problem-set-service.ts` and `ProblemSetContext.tsx` with complete renames and localStorage migration.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-problem-set-model-workspace-rename-inserted/23-CONTEXT.md
@.planning/phases/23-problem-set-model-workspace-rename-inserted/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create problem-set-service.ts with renamed API calls</name>
  <files>frontend/src/lib/problem-set-service.ts</files>
  <action>
1. Read `frontend/src/lib/workspace-service.ts` entirely.
2. Create `frontend/src/lib/problem-set-service.ts` with all renames:
   - Service object name: `workspaceService` -> `problemSetService`
   - API URLs: `/api/workspaces` -> `/api/problem-sets` throughout
   - Type names: `WorkspaceMembership` -> `ProblemSetMembership`, `WorkspaceDetail` -> `ProblemSetDetail`, etc.
   - Function names: `getWorkspaces` -> `getProblemSets`, `createWorkspace` -> `createProblemSet`, `joinWorkspace` -> `joinProblemSet`, etc.
   - Parameter names: `workspaceId` -> `problemSetId`
   - Interface fields: `workspaceType` -> `echelon`, `parentWorkspaceId` -> `parentProblemSetId`
   - Add `problemStatement` field to the detail type
   - Add `echelon` field (replacing `workspaceType`) with type `'strategic' | 'operational' | 'tactical'`

3. Do NOT delete the old file yet (components still import from it).
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && grep -c "problem-sets\|problemSet\|ProblemSet" frontend/src/lib/problem-set-service.ts</automated>
  </verify>
  <done>problem-set-service.ts exists with all API URLs pointing to /api/problem-sets, all types renamed, all functions renamed. Echelon and problemStatement fields present.</done>
</task>

<task type="auto">
  <name>Task 2: Create ProblemSetContext with localStorage migration</name>
  <files>frontend/src/context/ProblemSetContext.tsx</files>
  <action>
1. Read `frontend/src/context/WorkspaceContext.tsx` entirely.
2. Create `frontend/src/context/ProblemSetContext.tsx` with all renames:
   - Context name: `WorkspaceContext` -> `ProblemSetContext`
   - Provider name: `WorkspaceProvider` -> `ProblemSetProvider`
   - Hook name: `useWorkspace` -> `useProblemSet`
   - Import: `workspaceService` -> `problemSetService` from `../lib/problem-set-service`
   - Type imports: `WorkspaceMembership` -> `ProblemSetMembership`, `WorkspaceDetail` -> `ProblemSetDetail`
   - State variables: `activeWorkspace` -> `activeProblemSet`, `workspaces` -> `problemSets`, etc.
   - localStorage keys: `workspace-active-id` -> `problem-set-active-id`, `workspace-last-seen` -> `problem-set-last-seen`
   - All function names: `setActiveWorkspace` -> `setActiveProblemSet`, etc.
   - Interface: `CrossWorkspaceUpdate` -> `CrossProblemSetUpdate`, with field renames

3. Add localStorage migration function at the top of the file:
```typescript
function migrateLocalStorageKeys(): void {
  const migrated = localStorage.getItem('ps-migration-done');
  if (migrated) return;

  const migrations: [string, string][] = [
    ['workspace-active-id-training', 'problem-set-active-id-training'],
    ['workspace-active-id-operational', 'problem-set-active-id-operational'],
    ['workspace-last-seen', 'problem-set-last-seen'],
  ];

  for (const [oldKey, newKey] of migrations) {
    const value = localStorage.getItem(oldKey);
    if (value !== null) {
      localStorage.setItem(newKey, value);
      localStorage.removeItem(oldKey);
    }
  }

  localStorage.setItem('ps-migration-done', '1');
}
```

4. Call `migrateLocalStorageKeys()` at the start of the Provider component (before any state initialization that reads localStorage).

5. Do NOT delete old WorkspaceContext.tsx yet.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && grep -c "useProblemSet\|ProblemSetProvider\|migrateLocalStorageKeys\|problem-set-active-id" frontend/src/context/ProblemSetContext.tsx</automated>
  </verify>
  <done>ProblemSetContext.tsx exists with renamed provider, hook, and localStorage keys. Migration function runs on first load. Old WorkspaceContext.tsx preserved for now.</done>
</task>

</tasks>

<verification>
- `frontend/src/lib/problem-set-service.ts` calls `/api/problem-sets`
- `frontend/src/context/ProblemSetContext.tsx` exports `useProblemSet` and `ProblemSetProvider`
- localStorage migration code present and runs on init
</verification>

<success_criteria>
Frontend service and context layers fully renamed. These are ready for components to import from in the next plan.
</success_criteria>

<output>
After completion, create `.planning/phases/23-problem-set-model-workspace-rename-inserted/23-06-SUMMARY.md`
</output>
