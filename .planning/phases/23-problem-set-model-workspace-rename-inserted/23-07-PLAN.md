---
phase: 23-problem-set-model-workspace-rename-inserted
plan: 07
type: execute
wave: 4
depends_on: ["23-05", "23-06"]
files_modified:
  - frontend/src/components/problem-set/*.tsx
  - frontend/src/components/problem-set/*.css
  - frontend/src/components/workspace/*.tsx
autonomous: true
requirements:
  - PS-COMPONENT-RENAME
  - PS-UI-LABELS

must_haves:
  truths:
    - "All workspace component files moved to problem-set directory with renamed exports"
    - "All component imports updated from WorkspaceContext to ProblemSetContext"
    - "All component imports updated from workspace-service to problem-set-service"
    - "All UI labels changed from 'Workspace' to 'Problem Set'"
    - "All internal variable/prop names use problemSet terminology"
  artifacts:
    - path: "frontend/src/components/problem-set/ProblemSetSelector.tsx"
      provides: "Main problem set selector component"
    - path: "frontend/src/components/problem-set/OrgTree.tsx"
      provides: "Hierarchical tree display (echelon icons added in Plan 08)"
    - path: "frontend/src/components/problem-set/CreateProblemSetWizard.tsx"
      provides: "Problem set creation wizard with echelon field"
  key_links:
    - from: "frontend/src/components/problem-set/*.tsx"
      to: "frontend/src/context/ProblemSetContext.tsx"
      via: "useProblemSet hook"
      pattern: "useProblemSet"
    - from: "frontend/src/components/problem-set/*.tsx"
      to: "frontend/src/lib/problem-set-service.ts"
      via: "service imports"
      pattern: "problemSetService"
---

<objective>
Rename all frontend workspace components to problem-set equivalents with updated imports, exports, and UI text.

Purpose: Move the 24 workspace component files to the problem-set directory with comprehensive renames. This is the largest single rename operation in the phase.

Output: `frontend/src/components/problem-set/` directory with all renamed components.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-problem-set-model-workspace-rename-inserted/23-CONTEXT.md
@.planning/phases/23-problem-set-model-workspace-rename-inserted/23-RESEARCH.md
@.planning/phases/23-problem-set-model-workspace-rename-inserted/23-06-SUMMARY.md

<interfaces>
<!-- Frontend workspace components to rename -->
Files in frontend/src/components/workspace/:
- WorkspaceSelector.tsx -> ProblemSetSelector.tsx
- WorkspaceSwitcher.tsx -> ProblemSetSwitcher.tsx
- WorkspaceSwitcher.css -> ProblemSetSwitcher.css
- WorkspaceBreadcrumb.tsx -> ProblemSetBreadcrumb.tsx
- WorkspaceDashboard.tsx -> ProblemSetDashboard.tsx
- WorkspaceInviteModal.tsx -> ProblemSetInviteModal.tsx
- WorkspaceMemberManager.tsx -> ProblemSetMemberManager.tsx
- WorkspaceTabContainer.tsx -> ProblemSetTabContainer.tsx
- CreateWorkspaceWizard.tsx -> CreateProblemSetWizard.tsx
- CreateWorkspaceWizard.css -> CreateProblemSetWizard.css
- CrossWorkspaceLayerToggle.tsx -> CrossProblemSetLayerToggle.tsx
- OrgTree.tsx (keep name, update internals)
- OrgTreeSidebar.tsx (keep name, update internals)
- ActivityFeed.tsx (keep name, update imports)
- CommanderPanel.tsx (keep name, update imports)
- CompartmentManager.tsx (keep name, update imports)
- EscalationPanel.tsx (keep name, update imports)
- InviteAcceptPage.tsx (keep name, update imports)
- MemberDirectory.tsx (keep name, update imports)
- NotificationBadge.tsx (keep name, update imports)
- ObserverPanel.tsx (keep name, update imports)
- StaffPanel.tsx (keep name, update imports)
- SubscriptionManager.tsx (keep name, update imports)
- TabNotificationDropdown.tsx (keep name, update imports)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create renamed component files (Workspace-prefixed files)</name>
  <files>
    frontend/src/components/problem-set/ProblemSetSelector.tsx
    frontend/src/components/problem-set/ProblemSetSwitcher.tsx
    frontend/src/components/problem-set/ProblemSetSwitcher.css
    frontend/src/components/problem-set/ProblemSetBreadcrumb.tsx
    frontend/src/components/problem-set/ProblemSetDashboard.tsx
    frontend/src/components/problem-set/ProblemSetInviteModal.tsx
    frontend/src/components/problem-set/ProblemSetMemberManager.tsx
    frontend/src/components/problem-set/ProblemSetTabContainer.tsx
    frontend/src/components/problem-set/CreateProblemSetWizard.tsx
    frontend/src/components/problem-set/CreateProblemSetWizard.css
    frontend/src/components/problem-set/CrossProblemSetLayerToggle.tsx
  </files>
  <action>
Create `frontend/src/components/problem-set/` directory.

For each Workspace-prefixed component file, read the original and create a renamed copy:

1. Rename the file itself (WorkspaceSelector.tsx -> ProblemSetSelector.tsx)
2. Rename the component function/export (WorkspaceSelector -> ProblemSetSelector)
3. Update imports:
   - `useWorkspace` -> `useProblemSet` from `../../context/ProblemSetContext`
   - `workspaceService` -> `problemSetService` from `../../lib/problem-set-service`
   - `WorkspaceMembership` -> `ProblemSetMembership`
   - `WorkspaceDetail` -> `ProblemSetDetail`
   - Cross-component imports: update paths to `./` (same directory)
4. Update all internal variable names: workspace -> problemSet, workspaceId -> problemSetId, activeWorkspace -> activeProblemSet
5. Update all UI text strings: "Workspace" -> "Problem Set", "workspace" -> "problem set"
6. Update CSS class names if they reference workspace (in .css files too)
7. For CreateProblemSetWizard: Replace WorkspaceType selector (Organization/Unit/Team) with Echelon selector (strategic/operational/tactical). Make echelon REQUIRED with no default. Add optional problem_statement text field.

Do NOT delete old component files yet.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && ls frontend/src/components/problem-set/ProblemSetSelector.tsx frontend/src/components/problem-set/CreateProblemSetWizard.tsx frontend/src/components/problem-set/ProblemSetBreadcrumb.tsx 2>&1</automated>
  </verify>
  <done>11 Workspace-prefixed files renamed and created in problem-set/ directory. All imports updated. All UI text uses "Problem Set". CreateProblemSetWizard has echelon selector.</done>
</task>

<task type="auto">
  <name>Task 2: Move and update remaining component files</name>
  <files>
    frontend/src/components/problem-set/OrgTree.tsx
    frontend/src/components/problem-set/OrgTreeSidebar.tsx
    frontend/src/components/problem-set/ActivityFeed.tsx
    frontend/src/components/problem-set/CommanderPanel.tsx
    frontend/src/components/problem-set/CompartmentManager.tsx
    frontend/src/components/problem-set/EscalationPanel.tsx
    frontend/src/components/problem-set/InviteAcceptPage.tsx
    frontend/src/components/problem-set/MemberDirectory.tsx
    frontend/src/components/problem-set/NotificationBadge.tsx
    frontend/src/components/problem-set/ObserverPanel.tsx
    frontend/src/components/problem-set/StaffPanel.tsx
    frontend/src/components/problem-set/SubscriptionManager.tsx
    frontend/src/components/problem-set/TabNotificationDropdown.tsx
  </files>
  <action>
For each of the 13 non-Workspace-prefixed component files, read the original from `frontend/src/components/workspace/` and create in `frontend/src/components/problem-set/`:

1. Keep the filename as-is (OrgTree.tsx stays OrgTree.tsx)
2. Update imports:
   - `useWorkspace` -> `useProblemSet` from `../../context/ProblemSetContext`
   - `workspaceService` -> `problemSetService` from `../../lib/problem-set-service`
   - Cross-component imports within same directory: update to reference new file names (e.g., `./WorkspaceSelector` -> `./ProblemSetSelector`)
3. Update internal variable names: workspace -> problemSet, workspaceId -> problemSetId
4. Update UI text strings: "Workspace" -> "Problem Set", "workspace" -> "problem set"
5. For OrgTree.tsx: Update `workspace_type` or `workspaceType` references to use `echelon`. The visual echelon symbol rendering will be added in Plan 08, but the data references should use the new field name now.

After ALL files are created in the new directory, delete the old directory:
```bash
rm -rf frontend/src/components/workspace/
```

Verify no broken imports by checking for references to the old path:
```bash
grep -r "components/workspace" frontend/src/ --include="*.tsx" --include="*.ts" -l
```
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && ls frontend/src/components/problem-set/ | wc -l</automated>
  </verify>
  <done>24 files in frontend/src/components/problem-set/. Old workspace/ directory deleted. All internal imports within the directory use new names.</done>
</task>

</tasks>

<verification>
- `frontend/src/components/problem-set/` has 24 files (same count as old workspace/)
- `frontend/src/components/workspace/` no longer exists
- All component imports reference ProblemSetContext and problem-set-service
- No "Workspace" text in UI strings (except backward-compat redirect comments)
</verification>

<success_criteria>
All frontend workspace components renamed and relocated. UI text updated. Old directory removed.
</success_criteria>

<output>
After completion, create `.planning/phases/23-problem-set-model-workspace-rename-inserted/23-07-SUMMARY.md`
</output>
