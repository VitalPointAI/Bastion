---
phase: 23-problem-set-model-workspace-rename-inserted
plan: 04
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - backend/src/graph/problem-set/types.ts
  - backend/src/graph/problem-set/store.ts
  - backend/src/graph/problem-set/aggregation-service.ts
  - backend/src/graph/problem-set/index.ts
autonomous: true
requirements:
  - PS-GRAPH-RENAME

must_haves:
  truths:
    - "Graph workspace module fully renamed to graph problem-set"
    - "Graph WorkspaceType renamed to GraphProblemSetCategory (keeps country/adversary/region/topic/coalition/custom values)"
    - "Graph workspace store class and singleton renamed"
    - "Old graph/workspace/ directory contents moved to graph/problem-set/"
  artifacts:
    - path: "backend/src/graph/problem-set/types.ts"
      provides: "Graph problem set types (separate from main echelon types)"
      exports: ["GraphProblemSetCategory", "GraphProblemSet"]
    - path: "backend/src/graph/problem-set/store.ts"
      provides: "Graph problem set store"
      exports: ["graphProblemSetStore"]
  key_links:
    - from: "backend/src/graph/problem-set/store.ts"
      to: "graph_problem_sets table"
      via: "SQL queries"
      pattern: "graph_problem_sets"
---

<objective>
Rename the graph workspace module to graph problem-set, keeping its distinct taxonomy (country/adversary/region/topic/coalition/custom) separate from the main echelon model.

Purpose: Per the "no dual terminology" locked decision, the graph workspace system must also be renamed. However, graph WorkspaceType (country/adversary/etc.) is NOT an echelon - it is renamed to GraphProblemSetCategory to avoid type collisions.

Output: `backend/src/graph/problem-set/` directory with all 4 files renamed.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-problem-set-model-workspace-rename-inserted/23-CONTEXT.md
@.planning/phases/23-problem-set-model-workspace-rename-inserted/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename graph workspace module to graph problem-set</name>
  <files>
    backend/src/graph/problem-set/types.ts
    backend/src/graph/problem-set/store.ts
    backend/src/graph/problem-set/aggregation-service.ts
    backend/src/graph/problem-set/index.ts
  </files>
  <action>
Read ALL files in `backend/src/graph/workspace/` (types.ts, store.ts, aggregation-service.ts, index.ts).

Create `backend/src/graph/problem-set/` directory and create renamed copies of each file:

1. **types.ts**: Rename `WorkspaceType` to `GraphProblemSetCategory` (keep values: 'country'|'adversary'|'region'|'topic'|'coalition'|'custom'). Rename `Workspace` interface to `GraphProblemSet`. Rename `workspaceId` fields to `problemSetId`. Rename any other workspace references.

2. **store.ts**: Rename class `WorkspaceStore` to `GraphProblemSetStore`. Rename singleton export. Update SQL to reference `graph_problem_sets` table (already renamed by Plan 02 migration). Update column references (`workspace_id` -> `problem_set_id`). Update type imports to reference local `./types.js`.

3. **aggregation-service.ts**: Rename any workspace references in class/function/variable names. Update imports.

4. **index.ts**: Update re-exports to reference new file/type names.

Do NOT rename `backend/src/graph/workspace/` directory in-place (git mv). Instead create new files in the new directory. The old directory will be cleaned up in a later plan.

IMPORTANT: The graph module's `WorkspaceType` (country/adversary/region/topic/coalition/custom) is a DIFFERENT concept from the main echelon. Rename it to `GraphProblemSetCategory` to avoid collision with the main `Echelon` type.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && ls backend/src/graph/problem-set/ && grep -c "GraphProblemSet\|graphProblemSet" backend/src/graph/problem-set/store.ts</automated>
  </verify>
  <done>4 files exist in backend/src/graph/problem-set/ with all workspace references renamed. GraphProblemSetCategory type replaces graph WorkspaceType. Store references graph_problem_sets table. No "workspace" references remain except in comments explaining the migration.</done>
</task>

<task type="auto">
  <name>Task 2: Update graph module consumers to use new imports</name>
  <files>
    backend/src/graph/tools/entity-tools.ts
    backend/src/graph/tools/graph-tools.ts
    backend/src/graph/tools/raft-tools.ts
  </files>
  <action>
Search for all files in `backend/src/graph/` (excluding the workspace/ and problem-set/ directories) that import from `../workspace/` or `./workspace/`.

For each file found:
1. Read the file
2. Update import paths from `../workspace/` or `./workspace/` to `../problem-set/` or `./problem-set/`
3. Update imported type names (Workspace -> GraphProblemSet, WorkspaceType -> GraphProblemSetCategory, workspaceStore -> graphProblemSetStore)
4. Update variable names and function parameters that used workspace terminology

Also check `backend/src/api/` for any graph-workspace API routes and update those imports.

Run `grep -r "graph/workspace" backend/src/ --include="*.ts"` to find all references and update them.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && grep -r "graph/workspace" backend/src/ --include="*.ts" -l 2>/dev/null | grep -v "node_modules" | head -10</automated>
  </verify>
  <done>No files outside backend/src/graph/workspace/ import from the old graph workspace path. All graph workspace consumers use new graph/problem-set/ imports.</done>
</task>

</tasks>

<verification>
- `backend/src/graph/problem-set/` has 4 files (types, store, aggregation-service, index)
- No remaining imports of `graph/workspace` anywhere in backend/src/
- Graph problem set types use `GraphProblemSetCategory` (not `Echelon`)
</verification>

<success_criteria>
Graph workspace module fully renamed with no import breakage. Graph taxonomy preserved as separate concept from echelon.
</success_criteria>

<output>
After completion, create `.planning/phases/23-problem-set-model-workspace-rename-inserted/23-04-SUMMARY.md`
</output>
