---
phase: 23-problem-set-model-workspace-rename-inserted
plan: 03
type: execute
wave: 2
depends_on: ["23-01", "23-02"]
files_modified:
  - backend/src/problem-set/problem-set-store.ts
  - backend/src/problem-set/problem-set-member-store.ts
  - backend/src/problem-set/problem-set-invite-store.ts
  - backend/src/problem-set/problem-set-activity-store.ts
  - backend/src/problem-set/problem-set-role-store.ts
  - backend/src/problem-set/problem-set-compartment-store.ts
  - backend/src/problem-set/problem-set-panel-config-store.ts
  - backend/src/problem-set/problem-set-subscription-store.ts
  - backend/src/problem-set/problem-set-escalation-store.ts
autonomous: true
requirements:
  - PS-STORE-RENAME

must_haves:
  truths:
    - "All 9 workspace store files copied to problem-set directory with renamed files"
    - "All class names renamed (WorkspaceStore->ProblemSetStore, etc.)"
    - "All imports reference problem-set/types.ts"
    - "All exported singletons renamed (workspaceStore->problemSetStore, etc.)"
    - "All SQL references use new table/column names"
  artifacts:
    - path: "backend/src/problem-set/problem-set-store.ts"
      provides: "Main ProblemSet CRUD store"
      exports: ["ProblemSetStore", "problemSetStore"]
    - path: "backend/src/problem-set/problem-set-member-store.ts"
      provides: "ProblemSet member management"
      exports: ["ProblemSetMemberStore", "problemSetMemberStore"]
  key_links:
    - from: "backend/src/problem-set/problem-set-store.ts"
      to: "backend/src/problem-set/types.ts"
      via: "type imports"
      pattern: "import.*from.*problem-set/types"
    - from: "backend/src/problem-set/problem-set-store.ts"
      to: "backend/src/api/problem-sets.ts"
      via: "store singleton export"
      pattern: "problemSetStore"
---

<objective>
Create renamed problem-set store files in the new directory, updating all class names, imports, and exports.

Purpose: Move from workspace/ to problem-set/ directory with properly renamed stores. Each store file gets its class, singleton export, type imports, and internal references renamed.

Output: 9 store files in `backend/src/problem-set/` with complete renames.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-problem-set-model-workspace-rename-inserted/23-CONTEXT.md
@.planning/phases/23-problem-set-model-workspace-rename-inserted/23-RESEARCH.md
@.planning/phases/23-problem-set-model-workspace-rename-inserted/23-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create renamed store files in problem-set directory</name>
  <files>
    backend/src/problem-set/problem-set-store.ts
    backend/src/problem-set/problem-set-member-store.ts
    backend/src/problem-set/problem-set-invite-store.ts
    backend/src/problem-set/problem-set-activity-store.ts
    backend/src/problem-set/problem-set-role-store.ts
  </files>
  <action>
For each of the 5 store files (workspace-store, workspace-member-store, workspace-invite-store, workspace-activity-store, workspace-role-store):

1. Read the ENTIRE original file from `backend/src/workspace/`
2. Create a new file in `backend/src/problem-set/` with the renamed filename
3. Apply these renames throughout the file content:
   - Class names: `WorkspaceStore` -> `ProblemSetStore`, `WorkspaceMemberStore` -> `ProblemSetMemberStore`, etc.
   - Singleton exports: `workspaceStore` -> `problemSetStore`, etc.
   - Type imports: Change `from './types.js'` or `from '../workspace/types.js'` to `from './types.js'` (same directory now)
   - Type names in imports: `Workspace` -> `ProblemSet`, `WorkspaceType` -> `Echelon`, `WorkspaceMember` -> `ProblemSetMember`, `CreateWorkspaceInput` -> `CreateProblemSetInput`, etc.
   - Internal variable names: `workspaceId` -> `problemSetId`, `parentWorkspaceId` -> `parentProblemSetId`, etc.
   - Function names: `createWorkspace` -> `createProblemSet`, `getWorkspace` -> `getProblemSet`, `listWorkspaces` -> `listProblemSets`, etc.
   - Row interface fields: `workspace_type` -> `echelon`, `parent_workspace_id` -> `parent_problem_set_id`
   - SQL strings: Should already be updated from Plan 02 Task 2 — verify they use new table/column names
   - ID prefixes in code: `WS-${uuid}` -> `PS-${uuid}`, `WM-` -> `PM-`, `WI-` -> `PI-`, `WA-` -> `PA-`
   - Comments and JSDoc: Update workspace references to problem set

4. Ensure imports from OTHER modules (e.g., `../lib/database.js`) remain unchanged

Do NOT delete old files yet. Do NOT update files that import from these stores yet.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && ls backend/src/problem-set/problem-set-store.ts backend/src/problem-set/problem-set-member-store.ts backend/src/problem-set/problem-set-invite-store.ts backend/src/problem-set/problem-set-activity-store.ts backend/src/problem-set/problem-set-role-store.ts 2>&1</automated>
  </verify>
  <done>5 renamed store files exist in backend/src/problem-set/ with all class names, exports, type imports, variable names, and function names properly renamed.</done>
</task>

<task type="auto">
  <name>Task 2: Create remaining 4 renamed store files</name>
  <files>
    backend/src/problem-set/problem-set-compartment-store.ts
    backend/src/problem-set/problem-set-panel-config-store.ts
    backend/src/problem-set/problem-set-subscription-store.ts
    backend/src/problem-set/problem-set-escalation-store.ts
  </files>
  <action>
Same process as Task 1 for the remaining 4 store files:
- workspace-compartment-store.ts -> problem-set-compartment-store.ts
- workspace-panel-config-store.ts -> problem-set-panel-config-store.ts
- workspace-subscription-store.ts -> problem-set-subscription-store.ts
- workspace-escalation-store.ts -> problem-set-escalation-store.ts

Apply identical rename transformations. Pay special attention to:
- `workspace-subscription-store.ts` has `subscriber_workspace_id`, `publisher_workspace_id` columns -> rename to `subscriber_problem_set_id`, `publisher_problem_set_id`
- `workspace-escalation-store.ts` has `source_workspace_id` -> `source_problem_set_id`
- `workspace-compartment-store.ts` manages two tables (compartments + member_compartments)

After creating all files, verify the complete problem-set directory has: types.ts + 9 store files.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && ls backend/src/problem-set/*.ts | wc -l</automated>
  </verify>
  <done>10 files exist in backend/src/problem-set/ (types.ts + 9 stores). All store files have consistent renames. No remaining "workspace" references in any problem-set/*.ts file except in comments explaining the migration.</done>
</task>

</tasks>

<verification>
- `ls backend/src/problem-set/` shows 10 .ts files
- `grep -r "WorkspaceStore\|workspaceStore\|WorkspaceMember\b" backend/src/problem-set/` returns zero matches
- Each store exports a properly named singleton (problemSetStore, problemSetMemberStore, etc.)
</verification>

<success_criteria>
Complete problem-set backend module exists with types + 9 stores, all properly renamed. Ready for API routes and cross-cutting modules to import from.
</success_criteria>

<output>
After completion, create `.planning/phases/23-problem-set-model-workspace-rename-inserted/23-03-SUMMARY.md`
</output>
