---
phase: 23-problem-set-model-workspace-rename-inserted
plan: 09
type: execute
wave: 5
depends_on: ["23-07", "23-08"]
files_modified:
  - frontend/src/components/problem-set/OrgTree.tsx
  - frontend/src/components/problem-set/ProblemSetSelector.tsx
  - frontend/src/components/problem-set/CreateProblemSetWizard.tsx
autonomous: true
requirements:
  - PS-ECHELON-ICONS
  - PS-DETAIL-CARD
  - PS-CREATE-WIZARD

must_haves:
  truths:
    - "OrgTree shows doctrinal military echelon symbols (XX, III, II) instead of type badges"
    - "Detail card shows problem statement, echelon indicator, child count, active staff"
    - "Detail card button reads 'Enter Problem Set' instead of 'Enter Workspace'"
    - "Create wizard requires echelon selection (strategic/operational/tactical) with no default"
    - "Create wizard has optional problem statement text field"
    - "Echelon hierarchy enforced in create wizard (cannot nest tactical under strategic)"
  artifacts:
    - path: "frontend/src/components/problem-set/OrgTree.tsx"
      provides: "Tree with echelon symbol rendering"
      contains: "ECHELON_SYMBOLS"
    - path: "frontend/src/components/problem-set/ProblemSetSelector.tsx"
      provides: "Redesigned detail card"
      contains: "problemStatement"
    - path: "frontend/src/components/problem-set/CreateProblemSetWizard.tsx"
      provides: "Wizard with echelon selector"
      contains: "echelon"
  key_links:
    - from: "frontend/src/components/problem-set/OrgTree.tsx"
      to: "echelon field"
      via: "node data rendering"
      pattern: "ECHELON_SYMBOLS.*echelon"
---

<objective>
Implement the echelon-specific UI enhancements: military unit symbols on OrgTree nodes, redesigned detail card, and updated create wizard with echelon selection.

Purpose: These are the visible UI improvements that go beyond rename — they make the echelon model tangible in the interface with doctrinal military symbols and the problem-statement concept.

Output: OrgTree with echelon symbols, redesigned detail card, create wizard with echelon + problem statement fields.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-problem-set-model-workspace-rename-inserted/23-CONTEXT.md
@.planning/phases/23-problem-set-model-workspace-rename-inserted/23-RESEARCH.md
@.planning/phases/23-problem-set-model-workspace-rename-inserted/23-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add echelon symbols to OrgTree and redesign detail card</name>
  <files>
    frontend/src/components/problem-set/OrgTree.tsx
    frontend/src/components/problem-set/ProblemSetSelector.tsx
  </files>
  <action>
**OrgTree.tsx echelon symbols:**

1. Read the current OrgTree.tsx (now in problem-set/ directory).
2. Add echelon symbol constant:
```typescript
const ECHELON_SYMBOLS: Record<string, string> = {
  strategic: 'XX',    // Division-level indicator
  operational: 'III', // Regiment-level indicator
  tactical: 'II',     // Battalion-level indicator
};
```

3. In the custom SVG node renderer, replace the WorkspaceType badge (which showed "Organization", "Unit", or "Team" in a colored rect) with the echelon symbol text rendered ABOVE the node rectangle:
```tsx
<text
  x={0}
  y={-nodeHeight / 2 - 6}
  textAnchor="middle"
  style={{ fontSize: '11px', fontWeight: 700, fill: '#94a3b8', fontFamily: 'monospace' }}
>
  {ECHELON_SYMBOLS[nodeData.echelon] ?? ''}
</text>
```

4. Remove the old type badge rect+text elements. Keep classification badge if it exists separately.

**ProblemSetSelector.tsx detail card redesign:**

1. Read the current ProblemSetSelector.tsx.
2. Redesign the detail card section to emphasize:
   - **Problem statement** (new): Display the `problemStatement` field in a prominent position. If null/empty, show a subtle placeholder like "No problem statement defined".
   - **Echelon indicator**: Show the military symbol (XX/III/II) with label (Strategic/Operational/Tactical) instead of the old Organization/Unit/Team badge.
   - **Child problem set count**: Display count of children (sub-problem sets).
   - **Active staff count**: Display count of active members.
   - **Classification badge**: Retain existing classification display.
   - **"Enter Problem Set" button**: Replace "Enter Workspace" button text.

3. Use a layout that puts the problem statement front and center — this is the JP 5-0 concept the redesign emphasizes.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && grep -c "ECHELON_SYMBOLS\|problemStatement\|Enter Problem Set" frontend/src/components/problem-set/OrgTree.tsx frontend/src/components/problem-set/ProblemSetSelector.tsx</automated>
  </verify>
  <done>OrgTree renders echelon symbols (XX/III/II) above each tree node. Detail card shows problem statement, echelon with military symbol, child count, active staff, and "Enter Problem Set" button.</done>
</task>

<task type="auto">
  <name>Task 2: Update create wizard with echelon selection and problem statement</name>
  <files>
    frontend/src/components/problem-set/CreateProblemSetWizard.tsx
  </files>
  <action>
1. Read CreateProblemSetWizard.tsx (already renamed in Plan 07).
2. Replace the WorkspaceType selector step:
   - Remove Organization/Unit/Team radio buttons or dropdown
   - Add Echelon selector with three options:
     - **Strategic** (XX) — Theater/combatant command level
     - **Operational** (III) — Corps/division level
     - **Tactical** (II) — Brigade/battalion level
   - Each option should show the military symbol and a brief description
   - Make echelon REQUIRED — no default value, user must select
   - Show validation error if no echelon selected when trying to proceed

3. Add echelon hierarchy enforcement:
   - If creating under a parent, only show valid child echelons:
     - Under strategic parent -> only operational is valid
     - Under operational parent -> only tactical is valid
     - Under no parent -> only strategic is valid
   - If only one option is valid, auto-select it and show it as informational (not a choice)
   - Show an explanation: "Strategic problem sets contain operational, which contain tactical"

4. Add problem_statement field:
   - Add a textarea input for "Problem Statement"
   - Label: "Problem Statement" with hint text: "What operational problem does this set address? (Optional)"
   - This appears after the name/description fields
   - Optional — can be left empty

5. Update the form submission to include `echelon` and `problemStatement` in the API call to `createProblemSet`.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && grep -c "echelon\|problemStatement\|strategic\|operational\|tactical" frontend/src/components/problem-set/CreateProblemSetWizard.tsx</automated>
  </verify>
  <done>Create wizard has echelon selector (required, with hierarchy enforcement), problem statement textarea (optional), and submits both fields to the API.</done>
</task>

</tasks>

<verification>
- OrgTree shows XX/III/II symbols instead of Organization/Unit/Team badges
- Detail card displays problem statement and echelon indicator
- Create wizard enforces echelon hierarchy and includes problem statement field
- Frontend compiles
</verification>

<success_criteria>
Echelon model is visually represented throughout the UI. Users see doctrinal military symbols, problem statements, and must choose echelon level when creating problem sets.
</success_criteria>

<output>
After completion, create `.planning/phases/23-problem-set-model-workspace-rename-inserted/23-09-SUMMARY.md`
</output>
