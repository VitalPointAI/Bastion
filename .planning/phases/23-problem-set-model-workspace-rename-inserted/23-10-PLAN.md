---
phase: 23-problem-set-model-workspace-rename-inserted
plan: 10
type: execute
wave: 5
depends_on: ["23-08", "23-09"]
files_modified: []
autonomous: false
requirements:
  - PS-VERIFICATION

must_haves:
  truths:
    - "Application builds and starts without errors"
    - "No remaining 'workspace' references in codebase (except backward-compat redirect)"
    - "Problem set creation with echelon works end-to-end"
    - "Existing data migrated with correct echelon values"
    - "/workspace/* routes redirect to /problem-set/*"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete rename and echelon model work end-to-end: build, lint, navigate, create, and verify data migration.

Purpose: Comprehensive verification that the rename is complete across all layers and the application functions correctly.

Output: Verified working application with problem-set terminology throughout.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-problem-set-model-workspace-rename-inserted/23-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and workspace reference audit</name>
  <files></files>
  <action>
1. Run full backend TypeScript compilation:
```bash
cd backend && npx tsc --noEmit
```
Fix any errors.

2. Run full frontend TypeScript compilation:
```bash
cd frontend && npx tsc --noEmit
```
Fix any errors.

3. Run frontend build:
```bash
cd frontend && npm run build
```
Fix any errors.

4. Run lint if available:
```bash
cd frontend && npm run lint 2>/dev/null
cd backend && npm run lint 2>/dev/null
```

5. Audit for remaining workspace references:
```bash
# Backend - should return ZERO results
grep -ri "workspace" backend/src/ --include="*.ts" -l | grep -v node_modules

# Frontend - should return ZERO results (or only the redirect component)
grep -ri "workspace" frontend/src/ --include="*.ts" --include="*.tsx" -l | grep -v node_modules
```

6. If any workspace references found, fix them.

7. Verify the migration SQL file is syntactically valid:
```bash
cat backend/src/db/migrations/023-workspace-to-problem-set.sql | head -5
```

8. Verify seed scripts if they reference workspace terminology:
```bash
grep -r "workspace" scripts/ --include="*.sh" --include="*.ts" --include="*.sql" -l 2>/dev/null
```
Update any seed scripts that reference old workspace tables/terminology.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && cd frontend && npx tsc --noEmit 2>&1 | tail -5 && cd ../backend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Both frontend and backend compile cleanly. Zero workspace references remain in source code (excluding backward-compat redirect). Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete workspace-to-problem-set rename across the entire application: database schema, backend types/stores/API, frontend service/context/components/routing. Echelon model (strategic/operational/tactical) replaces WorkspaceType (Organization/Unit/Team). OrgTree shows military unit symbols (XX/III/II). Detail card shows problem statement. Create wizard requires echelon with hierarchy enforcement. /workspace/* routes redirect to /problem-set/*.</what-built>
  <how-to-verify>
    1. Start the application (`npm run dev` or equivalent)
    2. Run the database migration: `psql -f backend/src/db/migrations/023-workspace-to-problem-set.sql` (or let the app run init)
    3. Navigate to the problem set selector page — verify it says "Problem Set" not "Workspace"
    4. Check the OrgTree — verify echelon symbols (XX/III/II) appear instead of Organization/Unit/Team badges
    5. Click a problem set — verify the detail card shows echelon indicator, problem statement field, child count, and "Enter Problem Set" button
    6. Try creating a new problem set — verify echelon selector (strategic/operational/tactical) is required
    7. If creating under a parent, verify only valid child echelons are offered
    8. Navigate to /workspace/anything — verify it redirects to /problem-set/anything
    9. Check browser console for errors
    10. Verify existing data loaded correctly with echelon values (Organization->strategic, Unit->operational, Team->tactical)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Frontend and backend compile
- Application starts without errors
- Problem set selector displays correctly with echelon symbols
- Create wizard enforces echelon hierarchy
- /workspace/* redirect works
- Existing data migrated correctly
</verification>

<success_criteria>
Complete Phase 23 verified: terminology rename is comprehensive, echelon model functions correctly, UI displays doctrinal military symbols, and backward compatibility is maintained.
</success_criteria>

<output>
After completion, create `.planning/phases/23-problem-set-model-workspace-rename-inserted/23-10-SUMMARY.md`
</output>
