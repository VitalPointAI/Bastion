---
phase: 50-universal-intelligence-input
plan: 05
type: execute
wave: 4
depends_on: [50-04]
files_modified: []
autonomous: false
requirements: [UNIV-01, UNIV-04, UNIV-05, UNIV-09, UNIV-13, UNIV-14, UNIV-18, UNIV-19, UNIV-20]

must_haves:
  truths:
    - "Full ingestion flow works end-to-end: input -> classify -> route -> process -> status update"
    - "All input methods work: drag-drop, paste, type URL"
    - "RSS feed auto-subscription works without modal"
    - "Advanced link reveals old panels"
    - "Accessibility labels are present on all interactive elements"
---

<objective>
Human verification of the complete universal ingestion flow.

Purpose: Verify that the full end-to-end flow works in the browser — drag-drop, paste, URL entry, classification, routing, status updates, smart suggestions, and Advanced panel access.

Output: Verified working universal intelligence input system.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-universal-intelligence-input/50-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify complete universal ingestion flow</name>
  <files>none</files>
  <action>
    Human verifies the full end-to-end universal ingestion system in the browser:

    1. Navigate to any problem set's Brain tab
    2. Verify UniversalInputZone appears at top of ingestion sidebar with placeholder text
    3. Drag-drop test: Drop a PDF or text file into the input zone — verify status chip shows queued to classifying to processing to complete
    4. URL test: Type an RSS feed URL and press Enter — verify classified as RSS, auto-creates feed subscription
    5. Paste test: Ctrl+V a block of text — verify classified and processed as text content
    6. Smart suggestions: Paste an ambiguous URL — verify suggestion chips appear with classification options
    7. Advanced link: Click "Advanced options..." — verify old DocIntelligencePanel and AddOSINTSourceModal accessible
    8. Batch test: Drop multiple files — verify each gets own status chip with independent progress
    9. Error handling: Test with no scoping interview — verify "Complete scoping interview first" banner
    10. Accessibility: Tab through input zone — verify focus visible, check accessibility tree for labels
  </action>
  <verify>Human confirms all 10 verification steps pass. Type "approved" or describe issues.</verify>
  <done>All input methods (drag-drop, paste, URL entry) work end-to-end. Status chips show real-time progression. RSS auto-subscription works. Smart suggestions appear. Advanced link works. Batch processing tracks independently. Accessible with keyboard.</done>
</task>

</tasks>

<verification>
Human confirms all 10 verification steps pass.
</verification>

<success_criteria>
- All input methods (drag-drop, paste, URL entry) trigger the classify -> route -> process flow
- Status chips show real-time progression
- RSS auto-subscription works without modal
- Smart suggestions appear for ambiguous content
- Advanced link reveals old panels
- Batch processing tracks items independently
- Interview-required state surfaces clearly
- Accessible with keyboard navigation
</success_criteria>

<output>
After completion, create `.planning/phases/50-universal-intelligence-input/50-05-SUMMARY.md`
</output>
