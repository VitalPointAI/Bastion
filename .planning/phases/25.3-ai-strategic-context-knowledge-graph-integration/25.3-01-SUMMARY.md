---
phase: 25.3-ai-strategic-context-knowledge-graph-integration
plan: 01
subsystem: api, database
tags: [pg-boss, express, cache, subscriptions, strategic-context]

# Dependency graph
requires:
  - phase: 23-problem-set-model
    provides: problem_set_subscriptions and problem_set_data_cache tables, ProblemSetSubscriptionStore
provides:
  - materializeCache() method on ProblemSetSubscriptionStore for cache population
  - refreshCacheForPublisher() for async cache invalidation
  - getCachedDocs() for querying cached strategic documents
  - POST /api/strategic-context/cache/refresh endpoint
  - POST /api/strategic-context/cache/invalidate endpoint with pg-boss dedup
  - GET /api/strategic-context/cache/:problemSetId endpoint
  - stale_at column on problem_set_data_cache table
  - strategic-cache-refresh pg-boss worker
affects: [25.3-02, 25.3-03, 25.3-04, 25.3-05]

# Tech tracking
tech-stack:
  added: []
  patterns: [subscription-cache-materialization, version-based-skip, pg-boss-singletonKey-dedup, stale-while-revalidate]

key-files:
  created:
    - backend/src/api/strategic-context.ts
  modified:
    - backend/src/problem-set/problem-set-subscription-store.ts
    - backend/src/index.ts

key-decisions:
  - "Adapted to actual strategic_documents schema: uses 'level' instead of 'doc_type', 'created_at' instead of 'updated_at' for version hashing"
  - "Documents grouped by data_type in payload array to respect UNIQUE(consumer, source, data_type) constraint"
  - "pg-boss worker registered in index.ts server startup alongside other init code rather than in database.ts"

patterns-established:
  - "Version-based cache skip: SHA256 hash of document timestamps compared before re-materialization"
  - "pg-boss singletonKey dedup: prevents cache stampede when multiple invalidations arrive for same publisher"

requirements-completed: [SC-CACHE]

# Metrics
duration: 3min
completed: 2026-03-06
---

# Phase 25.3 Plan 01: Subscription Cache Materializer Summary

**Subscription cache materializer with version-based skip logic, pg-boss async invalidation, and strategic-context API endpoints**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-06T11:44:37Z
- **Completed:** 2026-03-06T11:48:07Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- materializeCache() fetches approved publisher docs, computes version hash, skips if unchanged
- pg-boss worker with singletonKey prevents cache stampede on publisher doc changes
- Three API endpoints for on-demand refresh, async invalidation, and cache querying

## Task Commits

Each task was committed atomically:

1. **Task 1: Add materializeCache and cache query methods** - `b3de29e` (feat)
2. **Task 2: Create strategic-context API router and register pg-boss worker** - `f55c96e` (feat)

## Files Created/Modified
- `backend/src/problem-set/problem-set-subscription-store.ts` - Added materializeCache(), refreshCacheForPublisher(), getCachedDocs(), stale_at migration, CachedDoc interface
- `backend/src/api/strategic-context.ts` - New router with cache/refresh, cache/invalidate, cache/:problemSetId endpoints
- `backend/src/index.ts` - Mounted strategic-context router, registered pg-boss strategic-cache-refresh worker

## Decisions Made
- Used `level` column from strategic_documents as the data_type filter (plan referenced non-existent `doc_type`)
- Used `created_at` for version hashing since strategic_documents has no `updated_at` column
- Stored documents grouped by data_type in payload array to work within UNIQUE(consumer, source, data_type) constraint
- Registered pg-boss worker in index.ts startup block (consistent with other worker registrations like blockchain-sync)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Adapted to actual strategic_documents schema**
- **Found during:** Task 1 (materializeCache implementation)
- **Issue:** Plan referenced `doc_type`, `extracted_data`, and `updated_at` columns that do not exist on strategic_documents table. Actual columns are `level`, `text_content`, and `created_at`.
- **Fix:** Query uses `level` for type filtering, `created_at` for version hashing, `text_content` for payload (no extracted_data available)
- **Files modified:** backend/src/problem-set/problem-set-subscription-store.ts
- **Verification:** TypeScript compiles cleanly
- **Committed in:** b3de29e (Task 1 commit)

**2. [Rule 1 - Bug] Documents grouped by data_type instead of one-per-document**
- **Found during:** Task 1 (materializeCache implementation)
- **Issue:** Plan specified "one cache entry per document" but UNIQUE constraint on (consumer, source, data_type) prevents multiple entries of same type per publisher pair
- **Fix:** Group documents by level/data_type and store array in payload. Each cache row contains all documents of that type.
- **Files modified:** backend/src/problem-set/problem-set-subscription-store.ts
- **Verification:** TypeScript compiles, respects existing schema constraint
- **Committed in:** b3de29e (Task 1 commit)

---

**Total deviations:** 2 auto-fixed (2 bugs - schema mismatch)
**Impact on plan:** Both fixes necessary for correctness against actual database schema. No scope creep.

## Issues Encountered
- Node.js v12 is system default but TypeScript compiler requires v20+; resolved by using NVM node path directly for tsc verification

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Cache materializer ready for Plan 02 (container-scoped knowledge graph) and Plan 04 (AI context assembly)
- API endpoints available for testing with existing subscriptions
- stale_at column ready for stale-while-revalidate pattern

## Self-Check: PASSED

- All 4 files exist (3 source + 1 summary)
- Both task commits found (b3de29e, f55c96e)
- materializeCache, refreshCacheForPublisher, getCachedDocs methods present
- stale_at column migration present
- singletonKey dedup present in API router

---
*Phase: 25.3-ai-strategic-context-knowledge-graph-integration*
*Completed: 2026-03-06*
