---
phase: 1.4-navigation-architecture-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/tabs/TabLayout.tsx
  - frontend/src/components/tabs/TabLayout.css
  - frontend/src/components/tabs/DecideTab.tsx
  - frontend/src/components/tabs/DesignTab.tsx
  - frontend/src/components/tabs/CampaignTab.tsx
  - frontend/src/components/tabs/MonitorTab.tsx
autonomous: true
requirements:
  - NAV-RESTRUCTURE

must_haves:
  truths:
    - "Each activity tab renders a collapsible left sidebar with doctrinal labels"
    - "Sidebar is open by default when entering a tab"
    - "Sidebar can be collapsed to maximize content space"
    - "Consistent sidebar pattern used across all four main tabs (Decide, Design, Campaign, Monitor)"
    - "Sidebar items render the correct existing component when selected"
    - "Monitor tab defaults to StrategicValidityDashboard (contains ValidityMap)"
    - "Campaign tab handles mission selection flow (list -> detail -> wizard) via local state"
  artifacts:
    - path: "frontend/src/components/tabs/TabLayout.tsx"
      provides: "Shared sidebar shell component"
      exports: ["TabLayout", "SidebarItem"]
    - path: "frontend/src/components/tabs/TabLayout.css"
      provides: "Sidebar layout and transition CSS"
      contains: ".tab-layout"
    - path: "frontend/src/components/tabs/DecideTab.tsx"
      provides: "Decide activity tab container"
      exports: ["DecideTab"]
    - path: "frontend/src/components/tabs/DesignTab.tsx"
      provides: "Design activity tab container"
      exports: ["DesignTab"]
    - path: "frontend/src/components/tabs/CampaignTab.tsx"
      provides: "Campaign activity tab container"
      exports: ["CampaignTab"]
    - path: "frontend/src/components/tabs/MonitorTab.tsx"
      provides: "Monitor activity tab container with ValidityMap default"
      exports: ["MonitorTab"]
  key_links:
    - from: "frontend/src/components/tabs/DecideTab.tsx"
      to: "frontend/src/components/tabs/TabLayout.tsx"
      via: "import TabLayout"
      pattern: "import.*TabLayout"
    - from: "frontend/src/components/tabs/MonitorTab.tsx"
      to: "frontend/src/components/validity/StrategicValidityDashboard.tsx"
      via: "import and render as default view"
      pattern: "import.*StrategicValidityDashboard"
    - from: "frontend/src/components/tabs/CampaignTab.tsx"
      to: "frontend/src/components/mission/MissionList.tsx"
      via: "import and render for mission selection"
      pattern: "import.*MissionList"
---

<objective>
Create the shared TabLayout sidebar component and all four activity tab containers (Decide, Design, Campaign, Monitor) that organize existing components under doctrine-aligned navigation.

Purpose: Establish the tab container layer that wraps existing components in a consistent sidebar + content layout pattern, ready for App.tsx to render them.

Output: 6 new files in `frontend/src/components/tabs/` — TabLayout shell, CSS, and 4 tab containers.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1.4-navigation-architecture-restructure/1.4-CONTEXT.md
@.planning/phases/1.4-navigation-architecture-restructure/1.4-RESEARCH.md

# Source files for component interfaces
@frontend/src/App.tsx
@frontend/src/App.css
@frontend/src/components/dao/DAODashboard.tsx
@frontend/src/components/strategic/StrategicDashboard.tsx
@frontend/src/components/validity/StrategicValidityDashboard.tsx
@frontend/src/components/graph/GraphExplorer.tsx
@frontend/src/components/graph/NodeDetailPanel.tsx
@frontend/src/components/mission/MissionList.tsx
@frontend/src/components/mission/MissionDetail.tsx
@frontend/src/components/mission/wizard/MissionWizard.tsx
@frontend/src/components/governance/MDMPGovernancePanel.tsx
@frontend/src/components/planning/PlanningDashboard.tsx
@frontend/src/components/escalation/EscalationLadder.tsx
@frontend/src/components/escalation/EffectChainDiagram.tsx
@frontend/src/components/planning/ForceRatioDisplay.tsx
@frontend/src/components/planning/BranchSequelTimeline.tsx
@frontend/src/components/planning/SustainmentDisplay.tsx
@frontend/src/context/UserContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TabLayout shared sidebar shell component and CSS</name>
  <files>
    frontend/src/components/tabs/TabLayout.tsx
    frontend/src/components/tabs/TabLayout.css
  </files>
  <action>
Create the `frontend/src/components/tabs/` directory and two files:

**TabLayout.tsx:**
- Export a `SidebarItem` interface: `{ id: string; label: string; tooltip?: string }`
- Export a `TabLayoutProps` interface: `{ items: SidebarItem[]; selectedItem: string; onSelectItem: (id: string) => void; children: React.ReactNode }`
- Export a `TabLayout` function component that renders:
  - A flex container div `.tab-layout` (horizontal: sidebar + content)
  - An `<aside>` with class `.tab-sidebar` that toggles between `.open` and `.collapsed` via local `useState(true)` (open by default per user decision)
  - A toggle button (chevron `‹`/`›`) with `aria-label` for accessibility
  - When open: a `<nav>` with `.sidebar-nav` containing buttons for each item
  - Each sidebar button: `.sidebar-item` class, `.active` when selected, `title={item.tooltip}` for tooltip, `onClick` calls `onSelectItem(item.id)`
  - A `<main>` with class `.tab-content` that renders `{children}`
- Use React 19 import style (`import { useState } from 'react'`)
- Import `./TabLayout.css`

**TabLayout.css:**
- Follow the CSS pattern from the RESEARCH.md code example exactly (`.tab-layout`, `.tab-sidebar`, `.tab-sidebar.collapsed`, `.sidebar-toggle`, `.sidebar-nav`, `.sidebar-item`, `.sidebar-item.active`, `.tab-content`)
- Use existing CSS custom properties from App.css: `--bg-secondary`, `--border-subtle`, `--text-secondary`, `--text-primary`, `--bg-hover`, `--bg-elevated`, `--accent-blue`, `--transition-medium`, `--transition-fast`
- Sidebar open width: 220px. Collapsed width: 40px (enough for toggle button)
- CSS `transition` on sidebar width for smooth collapse animation
- Active sidebar item: left blue border + blue text (per research pattern)
- Add responsive breakpoint at 768px: sidebar collapsed by default on mobile
  </action>
  <verify>
- Files exist: `frontend/src/components/tabs/TabLayout.tsx` and `frontend/src/components/tabs/TabLayout.css`
- TypeScript compiles without errors (run `cd frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | head -20`)
- `TabLayout` and `SidebarItem` are exported from TabLayout.tsx
  </verify>
  <done>
TabLayout component renders a sidebar + content flex layout. Sidebar is open by default, has a toggle button, renders items with active state highlighting, and collapses to 40px. CSS uses project CSS variables for consistent theming.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create four activity tab container components</name>
  <files>
    frontend/src/components/tabs/DecideTab.tsx
    frontend/src/components/tabs/DesignTab.tsx
    frontend/src/components/tabs/CampaignTab.tsx
    frontend/src/components/tabs/MonitorTab.tsx
  </files>
  <action>
Create four tab container components that use `TabLayout` and render existing components based on sidebar selection.

**MonitorTab.tsx (default landing tab):**
- Import `TabLayout` and `SidebarItem` from `./TabLayout`
- Import `StrategicValidityDashboard` from `../validity`
- Import `GraphExplorer` from `../graph/GraphExplorer`
- Import `NodeDetailPanel` from `../graph/NodeDetailPanel`
- Define `type MonitorView = 'validity-dashboard' | 'actor-graph' | 'actor-detail'`
- Sidebar items (per RESEARCH recommended order):
  1. `{ id: 'validity-dashboard', label: 'IPB Map' }` (default per user decision — StrategicValidityDashboard already contains ValidityMap with its own view toggle)
  2. `{ id: 'actor-graph', label: 'Actor Graph', tooltip: 'Relationship network — Intelligence Preparation of the Battlefield' }`
  3. `{ id: 'actor-detail', label: 'Actor Detail', tooltip: 'Detailed actor profile with relationships and tensions' }`
- Default `useState<MonitorView>('validity-dashboard')` — always starts on ValidityMap per user decision
- Render: `StrategicValidityDashboard` for validity-dashboard, `GraphExplorer` for actor-graph (pass `workspaceId="default"`, `onSelectActor` to switch to detail view and store actorId), `NodeDetailPanel` for actor-detail (pass stored `actorId`, `onClose` returns to graph, `onNavigateToActor` updates actorId)
- Track `selectedActorId` in state for graph->detail navigation

**DecideTab.tsx:**
- Import `TabLayout` from `./TabLayout`
- Import `DAODashboard` from `../dao`
- Sidebar items (per RESEARCH):
  1. `{ id: 'governance', label: 'Governance Overview' }` (default)
  2. `{ id: 'proposals', label: 'Proposals', tooltip: 'Active proposals requiring action' }`
  3. `{ id: 'voting', label: 'Voting', tooltip: 'Vote on active proposals' }`
  4. `{ id: 'mdmp-workflow', label: 'MDMP Workflow', tooltip: 'Military Decision Making Process phase progression and governance gates' }`
  5. `{ id: 'commander-guidance', label: "Commander's Guidance", tooltip: 'Intent and authority delegation' }`
  6. `{ id: 'assumptions', label: 'Assumptions', tooltip: 'Planning assumption registry' }`
- Default view: `'governance'`
- For v1: All sidebar items render `DAODashboard` (which already contains proposals, voting, MDMP toggle, etc. internally). The sidebar acts as a preview of future decomposition. Render `<DAODashboard />` for all views. Per RESEARCH open question #1: keep current DAODashboard toggle pattern for v1.

**DesignTab.tsx:**
- Import `TabLayout` from `./TabLayout`
- Import `StrategicDashboard` from `../strategic`
- Import `EscalationLadder` from `../escalation`
- Import `EffectChainDiagram` from `../escalation`
- Import `ForceRatioDisplay` from `../planning/ForceRatioDisplay`
- Import `BranchSequelTimeline` from `../planning/BranchSequelTimeline`
- Import `SustainmentDisplay` from `../planning/SustainmentDisplay`
- Sidebar items:
  1. `{ id: 'strategic-docs', label: 'Strategic Documents' }` (default)
  2. `{ id: 'coa-development', label: 'COA Development', tooltip: 'Course of Action development — JP 5-0 Steps 4-5 (requires mission context)' }`
  3. `{ id: 'escalation', label: 'Escalation Modeling' }`
  4. `{ id: 'force-ratio', label: 'Force Ratio', tooltip: 'Comparative force strength analysis' }`
  5. `{ id: 'branch-sequel', label: 'Branch / Sequel', tooltip: 'Contingency plan timelines with decision points' }`
  6. `{ id: 'sustainment', label: 'Sustainment', tooltip: 'Logistics feasibility analysis' }`
- Default view: `'strategic-docs'`
- Render `StrategicDashboard` for strategic-docs
- For coa-development: render a placeholder message: "COA Development requires a mission context. Navigate to Campaign > Missions to select a mission, then access the Planning Dashboard." (Per RESEARCH open question #2 — PlanningDashboard stays inside MissionDetail)
- For escalation: render `EscalationLadder` with empty/default props (these components are designed to be used standalone with demo data — check their props and pass reasonable defaults or empty arrays)
- For force-ratio, branch-sequel, sustainment: render the respective components with empty/default props. These components gracefully handle empty data (show "no data" states). Pass required props with sensible defaults.
- NOTE: Read each component file to understand its required props before wiring. If a component requires a `planId` or `missionId`, render a similar placeholder message about needing mission context.

**CampaignTab.tsx:**
- Import `TabLayout` from `./TabLayout`
- Import `MissionList`, `MissionDetail`, `MissionWizard` from `../mission`
- Import `useUser` from `../../context/UserContext`
- Sidebar items:
  1. `{ id: 'missions', label: 'Missions' }` (default)
  2. `{ id: 'orders', label: 'OPORD / WARNORD', tooltip: 'Operational Order / Warning Order — orders production' }`
  3. `{ id: 'command', label: 'Command Structure', tooltip: 'Chain of command visualization' }`
  4. `{ id: 'resources', label: 'Force Allocation', tooltip: 'Resource assignment and equipment tracking' }`
- Default view: `'missions'`
- Track `selectedMissionId` and `showWizard` in local state
- For missions view:
  - If `showWizard && userDID`: render `MissionWizard` with `userDID`, `onClose={() => setShowWizard(false)}`, `onMissionCreated={(id) => { setSelectedMissionId(id); setShowWizard(false); }}`
  - Else if `selectedMissionId`: render `MissionDetail` with `missionId={selectedMissionId}`, `onBack={() => setSelectedMissionId(null)}`
  - Else: render `MissionList` with `onSelectMission={(id) => setSelectedMissionId(id)}`, `onCreateMission={() => setShowWizard(true)}`
- For orders, command, resources: render placeholder messages about selecting a mission first (these are sub-views of MissionDetail in current architecture — keep them here as navigation targets for future decomposition)
  </action>
  <verify>
- All four files exist in `frontend/src/components/tabs/`
- TypeScript compiles: `cd frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | head -30`
- Each component exports its named function (DecideTab, DesignTab, CampaignTab, MonitorTab)
- Each component imports and uses TabLayout
  </verify>
  <done>
Four tab container components created. Each renders TabLayout with doctrinal sidebar items and the correct existing components for the selected sub-view. MonitorTab defaults to StrategicValidityDashboard (containing ValidityMap). CampaignTab handles full mission flow (list->detail->wizard). Components that need mission context show informative placeholder messages. All sidebar items use doctrinal labels with tooltips for non-obvious terms.
  </done>
</task>

</tasks>

<verification>
- `ls frontend/src/components/tabs/` shows: TabLayout.tsx, TabLayout.css, DecideTab.tsx, DesignTab.tsx, CampaignTab.tsx, MonitorTab.tsx
- TypeScript compilation succeeds: `cd frontend && npx tsc --noEmit`
- Each tab component correctly imports TabLayout and passes items/selectedItem/onSelectItem
- MonitorTab default view is 'validity-dashboard' (maps to StrategicValidityDashboard)
- CampaignTab handles mission selection state locally
</verification>

<success_criteria>
All 6 new files created in `frontend/src/components/tabs/`. TypeScript compiles without errors. Each tab container wraps existing components in the shared sidebar layout. No existing component files modified.
</success_criteria>

<output>
After completion, create `.planning/phases/1.4-navigation-architecture-restructure/1.4-01-SUMMARY.md`
</output>
