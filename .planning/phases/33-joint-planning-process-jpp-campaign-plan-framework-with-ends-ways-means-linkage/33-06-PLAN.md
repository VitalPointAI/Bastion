---
phase: 33-joint-planning-process-jpp-campaign-plan-framework-with-ends-ways-means-linkage
plan: 06
type: execute
wave: 4
depends_on: ["33-04", "33-05"]
files_modified:
  - frontend/src/components/plan/PlanningInitiation.tsx
  - frontend/src/components/plan/MissionAnalysis.tsx
  - frontend/src/components/plan/COADevelopment.tsx
autonomous: true
requirements:
  - JPP-01
  - JPP-06
  - JPP-10

must_haves:
  truths:
    - "Planning Initiation shows commander guidance, staff estimates, and planning timeline with role-gated editing"
    - "Mission Analysis shows IPB, tasks, constraints, assumptions, mission statement with J2/J3 role sections"
    - "COA Development pulls LOEs from Design tab and enables COA drafting with E-W-M linkage creation"
    - "Parent JPP products appear as read-only higher HQ guidance in Step 1"
  artifacts:
    - path: "frontend/src/components/plan/PlanningInitiation.tsx"
      provides: "JPP Step 1 component with commander guidance and staff estimates"
      exports: ["PlanningInitiation"]
    - path: "frontend/src/components/plan/MissionAnalysis.tsx"
      provides: "JPP Step 2 component with IPB, tasks, constraints, mission statement"
      exports: ["MissionAnalysis"]
    - path: "frontend/src/components/plan/COADevelopment.tsx"
      provides: "JPP Step 3 component with LOE input, COA drafting, E-W-M linking"
      exports: ["COADevelopment"]
  key_links:
    - from: "frontend/src/components/plan/COADevelopment.tsx"
      to: "frontend/src/lib/ewm-service.ts"
      via: "createLinkage for E-W-M connections"
      pattern: "ewmService.*createLinkage"
    - from: "frontend/src/components/plan/PlanningInitiation.tsx"
      to: "frontend/src/lib/jpp-service.ts"
      via: "getParentProducts for inheritance"
      pattern: "jppService.*getParentProducts"
---

<objective>
Build JPP Steps 1-3: Planning Initiation, Mission Analysis, and COA Development. These are the early-phase steps that take inputs (commander guidance, LOEs from Design) and produce COAs. Each step is a composition of role-gated sections using JPPStepLayout.

Purpose: Steps 1-3 are the input and creative phases of JPP -- they transform strategic guidance and operational design into actionable COAs.
Output: PlanningInitiation.tsx, MissionAnalysis.tsx, COADevelopment.tsx
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-joint-planning-process-jpp-campaign-plan-framework-with-ends-ways-means-linkage/33-01-SUMMARY.md
@.planning/phases/33-joint-planning-process-jpp-campaign-plan-framework-with-ends-ways-means-linkage/33-04-SUMMARY.md
@.planning/phases/33-joint-planning-process-jpp-campaign-plan-framework-with-ends-ways-means-linkage/33-05-SUMMARY.md

@frontend/src/components/plan/JPPStepLayout.tsx (shared layout from Plan 05)
@frontend/src/components/plan/RoleGatedSection.tsx (role gating from Plan 05)
@frontend/src/lib/jpp-service.ts (API client from Plan 04)
@frontend/src/lib/ewm-service.ts (E-W-M client from Plan 04)
@frontend/src/lib/design-service.ts (LOE data from Design tab)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Planning Initiation (Step 1) with higher HQ guidance inheritance</name>
  <files>frontend/src/components/plan/PlanningInitiation.tsx</files>
  <action>
Create PlanningInitiation.tsx as a composition of role-gated sections inside JPPStepLayout.

Props: { problemSetId: string; jppInstanceId: string; currentRole: string; }

Sections (each wrapped in RoleGatedSection):
1. **Higher HQ Guidance** (read-only for all, auto-populated from parent JPP):
   - Fetch via jppService.getParentProducts(jppInstanceId)
   - If parent exists: display parent commander's guidance, strategic objectives, constraints as read-only cards
   - If no parent (top-level): show "No higher headquarters guidance available" message
   - allowedRoles: [] (nobody edits -- it's inherited)

2. **Commander's Guidance** (allowedRoles: ['commander', 'xo', 'chief_of_staff']):
   - Textarea for commander's initial guidance text
   - Problem statement input
   - Planning timeline with key milestones (simple date + milestone name pairs)
   - Priority list (ordered list of priorities)
   - Save via jppService.saveStepProduct with step='planning_initiation', content structured as { commanderGuidance, problemStatement, timeline, priorities }

3. **Initial Staff Estimates** (allowedRoles: all J-codes):
   - Collapsible sections per staff function (J1-Personnel, J2-Intel, J3-Ops, J4-Logistics, J5-Plans, J6-Comms)
   - Each section: textarea for initial assessment
   - Each section gated to its J-code role
   - Save individually via saveStepProduct with roleId

4. **Planning Timeline** (allowedRoles: ['j5', 'j3', 'chief_of_staff']):
   - Key dates: mission analysis brief, COA brief, decision brief, order publication
   - Simple table with milestone name + date inputs

Wrap all in JPPStepLayout with stepId='planning_initiation', aiAgentId='jpp-planning-init-agent'.
Load existing products on mount. Auto-save on blur/change.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr/.claude/worktrees/joint-planning-process && npx tsc --noEmit frontend/src/components/plan/PlanningInitiation.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Planning Initiation renders 4 role-gated sections; parent JPP products displayed as read-only HQ guidance; commander guidance editable by commander/xo; staff estimates per J-code; compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Mission Analysis (Step 2) and COA Development (Step 3)</name>
  <files>frontend/src/components/plan/MissionAnalysis.tsx, frontend/src/components/plan/COADevelopment.tsx</files>
  <action>
**MissionAnalysis.tsx (Step 2):**
Props: { problemSetId: string; jppInstanceId: string; currentRole: string; }

Sections:
1. **Intelligence Preparation** (allowedRoles: ['j2', 'j2x']):
   - Threat assessment textarea
   - Area of operations description
   - Area of interest description
   - Weather/terrain summary
   - Enemy COA summary (text)

2. **Task Analysis** (allowedRoles: ['j3', 'j5']):
   - Specified tasks list (add/remove items)
   - Implied tasks list (add/remove items)
   - Essential tasks list (derived from specified + implied, checkboxes to mark)
   - Constraints (list of limitation items)
   - Assumptions (list with validation status: valid/invalid/unconfirmed)

3. **Mission Statement** (allowedRoles: ['j3', 'j5', 'commander']):
   - 5 fields matching MissionStatement type: who, what, when, where, why
   - Preview rendering: "{who} will {what} NLT {when} in {where} in order to {why}"
   - Restated mission (editable text)

4. **Commander's Intent** (allowedRoles: ['commander', 'xo']):
   - 7 Klein facets: purpose, keyTasks (list), endState, context, constraints (list), criticalFactors (list), antigoals (list)

Wrap in JPPStepLayout with stepId='mission_analysis', aiAgentId='jpp-mission-analysis-agent'.

**COADevelopment.tsx (Step 3):**
Props: { problemSetId: string; jppInstanceId: string; currentRole: string; }

Critical: LOEs from Operational Design (Design tab) are the INPUT to COA development. COAs are the OUTPUT.

Sections:
1. **LOE Input** (read-only display):
   - Fetch LOEs from designService.getDesign(problemSetId) -> extract LOE data
   - Display LOEs as cards with name, description, decisive points
   - These are reference material from the Design tab -- not editable here

2. **COA Workspace** (allowedRoles: ['j3', 'j5', 'commander']):
   - List of COAs for this JPP instance (fetch from step products)
   - "New COA" button creates a COA card with:
     - COA number (auto-increment)
     - Name input
     - Description textarea
     - Scheme of maneuver textarea
     - Decisive operation input
     - Shaping operations input
     - Sustaining operations input
     - Subordinate task list (unitId + task + purpose per row)
   - Each COA expandable/collapsible
   - Save COA as step product

3. **E-W-M Linkage** (allowedRoles: ['j3', 'j5']):
   - For each COA, show which Ends (objectives) it supports
   - Dropdown to link COA to objectives (from strategic objectives)
   - Button to link COA to specific LOEs
   - Creates EWM linkages via ewmService.createLinkage
   - Show gap warnings: objectives without COA support highlighted amber

4. **Governance Gate** (visible to all):
   - DecisionGateBanner for 'coa_development' gate type
   - GateSubmitButton for COA development info brief

Wrap in JPPStepLayout with stepId='coa_development', aiAgentId='jpp-coa-dev-agent'.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr/.claude/worktrees/joint-planning-process && npx tsc --noEmit frontend/src/components/plan/MissionAnalysis.tsx frontend/src/components/plan/COADevelopment.tsx 2>&1 | head -20</automated>
  </verify>
  <done>MissionAnalysis renders IPB, task analysis, mission statement (5W), commander intent (Klein 7); COADevelopment shows LOEs as input, COA workspace for drafting, E-W-M linkage creation, governance gate; both compile</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all 3 step components
- PlanningInitiation fetches parent JPP products for HQ guidance
- MissionAnalysis has 4 role-gated sections with correct role assignments
- COADevelopment pulls LOEs from Design tab as read-only input
- COADevelopment creates E-W-M linkages via ewmService
- No step navigation is blocked -- these are content components, not gates
</verification>

<success_criteria>
JPP Steps 1-3 fully functional with role-gated editing. Parent JPP inheritance works in Step 1. LOEs from Design tab flow into Step 3 as input. COAs can be drafted and linked to objectives via E-W-M. Governance gates present at Step 3.
</success_criteria>

<output>
After completion, create `.planning/phases/33-joint-planning-process-jpp-campaign-plan-framework-with-ends-ways-means-linkage/33-06-SUMMARY.md`
</output>
