---
phase: 33-joint-planning-process-jpp-campaign-plan-framework-with-ends-ways-means-linkage
plan: 09
type: execute
wave: 4
depends_on: ["33-02", "33-03", "33-06", "33-07", "33-08"]
files_modified:
  - frontend/src/components/plan/EntityResolutionPanel.tsx
  - frontend/src/components/tabs/PlanTab.tsx
  - backend/src/api/index.ts
autonomous: false
requirements:
  - JPP-07
  - JPP-08
  - JPP-09
  - JPP-13

must_haves:
  truths:
    - "Entity resolution panel shows pending low-confidence matches for staff review"
    - "All 7 JPP step components are wired into PlanTab and render correctly"
    - "OSINT alerts appear contextually within steps"
    - "Parent JPP products inherit into child problem sets"
    - "Full JPP workflow navigable end-to-end"
  artifacts:
    - path: "frontend/src/components/plan/EntityResolutionPanel.tsx"
      provides: "Entity review queue with approve/reject/merge actions"
      exports: ["EntityResolutionPanel"]
    - path: "frontend/src/components/tabs/PlanTab.tsx"
      provides: "Fully wired PlanTab with all 8 views"
      exports: ["PlanTab"]
  key_links:
    - from: "frontend/src/components/tabs/PlanTab.tsx"
      to: "frontend/src/components/plan/PlanningInitiation.tsx"
      via: "conditional render in selectedView"
      pattern: "PlanningInitiation"
    - from: "frontend/src/components/tabs/PlanTab.tsx"
      to: "frontend/src/components/plan/EWMOverview.tsx"
      via: "conditional render in selectedView"
      pattern: "EWMOverview"
    - from: "backend/src/api/index.ts"
      to: "backend/src/api/jpp.ts"
      via: "router mount"
      pattern: "jppRouter"
---

<objective>
Final wiring: integrate all JPP step components into PlanTab, build the entity resolution review panel, ensure all backend routers are registered, and verify the complete end-to-end JPP workflow. Human verification checkpoint.

Purpose: Connect all previously built pieces into a working whole. Entity resolution panel enables staff review of AI-matched entities. Human verification confirms the full workflow.
Output: EntityResolutionPanel.tsx, fully wired PlanTab.tsx, verified backend API registration
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-joint-planning-process-jpp-campaign-plan-framework-with-ends-ways-means-linkage/33-05-SUMMARY.md
@.planning/phases/33-joint-planning-process-jpp-campaign-plan-framework-with-ends-ways-means-linkage/33-06-SUMMARY.md
@.planning/phases/33-joint-planning-process-jpp-campaign-plan-framework-with-ends-ways-means-linkage/33-07-SUMMARY.md
@.planning/phases/33-joint-planning-process-jpp-campaign-plan-framework-with-ends-ways-means-linkage/33-08-SUMMARY.md

@frontend/src/components/tabs/PlanTab.tsx (from Plan 05 with placeholders)
@frontend/src/components/plan/ (all step components from Plans 06-08)
@frontend/src/lib/entity-service.ts (entity API client from Plan 04)
@backend/src/api/index.ts (API router registration)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Entity resolution panel and full PlanTab wiring</name>
  <files>frontend/src/components/plan/EntityResolutionPanel.tsx, frontend/src/components/tabs/PlanTab.tsx, backend/src/api/index.ts</files>
  <action>
**EntityResolutionPanel.tsx:**
Floating panel (slide-out or modal) for reviewing entity resolution matches.

Props: { problemSetId: string; }

Implementation:
1. Fetch pending low-confidence entity matches from entityService (entities below 0.9 confidence that need human review)
2. Display as a list of match cards, each showing:
   - Mention text (from document)
   - Suggested canonical entity (name, type)
   - Confidence score (visual bar)
   - Source document reference
   - Actions: Approve (accept match), Reject (mark as different entity), Merge (combine with existing), Create New (create new canonical entity)
3. Approve: calls entityService.createAlias(canonicalId, mention, source)
4. Create New: opens inline form for name + type, then creates entity
5. Filter tabs: All | Nations | Military | Geographic | Organizations
6. Badge showing count of pending reviews

**PlanTab.tsx final wiring:**
Replace all placeholder divs from Plan 05 with actual step components:
```
{selectedView === 'planning-initiation' && <PlanningInitiation problemSetId={problemSetId} jppInstanceId={instance.id} currentRole={currentRole} />}
{selectedView === 'mission-analysis' && <MissionAnalysis problemSetId={problemSetId} jppInstanceId={instance.id} currentRole={currentRole} />}
{selectedView === 'coa-development' && <COADevelopment problemSetId={problemSetId} jppInstanceId={instance.id} currentRole={currentRole} />}
{selectedView === 'coa-analysis' && <COAAnalysis problemSetId={problemSetId} jppInstanceId={instance.id} currentRole={currentRole} />}
{selectedView === 'coa-comparison' && <COAComparison problemSetId={problemSetId} jppInstanceId={instance.id} currentRole={currentRole} />}
{selectedView === 'coa-approval' && <COAApproval problemSetId={problemSetId} jppInstanceId={instance.id} currentRole={currentRole} />}
{selectedView === 'plan-development' && <PlanOrderDevelopment problemSetId={problemSetId} jppInstanceId={instance.id} currentRole={currentRole} />}
{selectedView === 'ewm-overview' && <EWMOverview problemSetId={problemSetId} jppInstanceId={instance.id} currentRole={currentRole} />}
```

Add imports for all step components. Get currentRole from useProblemSet() or similar context hook (check how staff role is determined -- likely from exercise role assignment).

Add EntityResolutionPanel as a floating button + slide-out panel accessible from any step (bottom-right corner badge showing pending count).

**Backend API registration:**
Ensure backend/src/api/index.ts (or equivalent main router file) registers:
- jppRouter from './jpp.js'
- osintWebhookRouter from './osint-webhook.js'
If already registered in Plan 04/02, verify. If not, add the mount lines.

Also ensure jppStore.ensureTable(), ewmStore.ensureTable(), and osintFeedStore.ensureTable() are called during server startup (check server.ts or similar init file).
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr/.claude/worktrees/joint-planning-process && npx tsc --noEmit frontend/src/components/tabs/PlanTab.tsx frontend/src/components/plan/EntityResolutionPanel.tsx 2>&1 | head -20</automated>
  </verify>
  <done>PlanTab renders all 8 views with real step components; EntityResolutionPanel shows pending matches with approve/reject/merge; backend routers registered; stores initialized at startup</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete JPP workflow</name>
  <action>Human verifies the complete JPP workflow renders and navigates correctly end-to-end.</action>
  <what-built>Complete JPP workflow with 7 steps, E-W-M Overview, entity resolution, and OSINT integration</what-built>
  <how-to-verify>
    1. Navigate to any problem set's Plan tab
    2. Verify 8 sidebar items visible: 7 JPP steps + E-W-M Overview
    3. Click through all 7 steps -- all should be navigable (no blocking)
    4. In Step 1, verify higher HQ guidance section appears (empty if no parent problem set)
    5. In Step 3, verify LOEs from Design tab appear as read-only input
    6. In Step 5, verify decision matrix displays with 5 criteria columns
    7. In Step 7, verify 5-paragraph order structure with annex sections
    8. Click E-W-M Overview -- verify tree and Sankey view toggle works
    9. Verify entity resolution panel accessible via floating button
    10. Check that role-gated sections show "(read-only)" for non-owning roles
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all modified files
- Frontend build succeeds
- Backend build succeeds
- All 8 Plan tab views render
- Entity resolution panel shows/hides correctly
- Backend stores initialize tables on startup
</verification>

<success_criteria>
Full JPP workflow navigable end-to-end. All 7 steps render with role-gated sections. E-W-M Overview shows tree and Sankey views with gap analysis. Entity resolution panel enables human review of AI matches. OSINT alerts surface within steps. Parent JPP products inherit to child problem sets. Human verification confirms visual correctness.
</success_criteria>

<output>
After completion, create `.planning/phases/33-joint-planning-process-jpp-campaign-plan-framework-with-ends-ways-means-linkage/33-09-SUMMARY.md`
</output>
