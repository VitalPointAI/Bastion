---
phase: 15-jpp-staff-organization-workspaces
plan: 05
type: execute
wave: 4
depends_on: [15-03, 15-04]
files_modified:
  - frontend/src/components/exercise/ProductDiffView.tsx
  - frontend/src/components/exercise/ProductDiffView.css
  - frontend/src/components/exercise/AgentSuggestionPanel.tsx
  - frontend/src/components/exercise/AgentSuggestionPanel.css
  - frontend/src/components/exercise/RoleDashboard.tsx
  - frontend/src/components/exercise/NotificationPanel.tsx
  - frontend/src/components/exercise/StaffWorkspace.tsx
autonomous: true
requirements: [JPP-04, JPP-05, JPP-06]

must_haves:
  truths:
    - "Clicking 'Integrate' on a notification opens a diff view showing what changed in the source product"
    - "Diff view splits structured field changes (summary table) from narrative text changes (text diff below)"
    - "User can accept or reject the integration after reviewing the diff"
    - "'Import Strategic Direction' button in Commander workspace pulls objectives and intent from Design tab"
    - "AI agent suggestion panel slides open alongside the editor and generates suggestions on demand"
    - "Agent suggestions are broken into accept/reject blocks"
  artifacts:
    - path: "frontend/src/components/exercise/ProductDiffView.tsx"
      provides: "Side-by-side diff rendering with accept/reject integration"
      min_lines: 120
    - path: "frontend/src/components/exercise/AgentSuggestionPanel.tsx"
      provides: "Collapsible right-side AI suggestion panel with on-demand generation"
      min_lines: 80
  key_links:
    - from: "frontend/src/components/exercise/NotificationPanel.tsx"
      to: "frontend/src/components/exercise/ProductDiffView.tsx"
      via: "'Integrate' button opens ProductDiffView"
      pattern: "<ProductDiffView"
    - from: "frontend/src/components/exercise/RoleDashboard.tsx"
      to: "frontend/src/components/exercise/AgentSuggestionPanel.tsx"
      via: "Agent panel slides open alongside StaffProduct editor"
      pattern: "<AgentSuggestionPanel"
    - from: "frontend/src/components/exercise/RoleDashboard.tsx"
      to: "frontend/src/services/exercise-service.ts"
      via: "Import Strategic Direction calls backend endpoint"
      pattern: "exerciseService\\.importStrategicDirection"
---

<objective>
Complete the cross-staff integration workflow (diff view + integrate), wire the strategic direction import button, and build the AI agent suggestion panel. This plan connects the remaining loose ends: notification "Integrate" action opens a diff view, Commander workspace gets the strategic import button, and all role workspaces get an AI suggestion panel.

Purpose: These three capabilities complete the full JPP staff workflow: integration of cross-staff products, top-down strategic direction flow, and AI-assisted product creation.
Output: Working diff integration flow, strategic direction import, and AI agent suggestion panel.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-jpp-staff-organization-workspaces/15-RESEARCH.md
@.planning/phases/15-jpp-staff-organization-workspaces/15-CONTEXT.md
@.planning/phases/15-jpp-staff-organization-workspaces/15-01-SUMMARY.md
@.planning/phases/15-jpp-staff-organization-workspaces/15-02-SUMMARY.md
@.planning/phases/15-jpp-staff-organization-workspaces/15-03-SUMMARY.md
@.planning/phases/15-jpp-staff-organization-workspaces/15-04-SUMMARY.md
@frontend/src/components/exercise/NotificationPanel.tsx
@frontend/src/components/exercise/StaffProduct.tsx
@frontend/src/components/exercise/RoleDashboard.tsx
@frontend/src/services/exercise-service.ts
@backend/src/api/exercise.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Product diff view and integration flow</name>
  <files>
    frontend/src/components/exercise/ProductDiffView.tsx
    frontend/src/components/exercise/ProductDiffView.css
    frontend/src/components/exercise/NotificationPanel.tsx
    frontend/src/components/exercise/StaffWorkspace.tsx
  </files>
  <action>
**ProductDiffView** (`frontend/src/components/exercise/ProductDiffView.tsx`):

Per CONTEXT.md: "Auto-merge with preview: system proposes merged content — user reviews a before/after diff and confirms. Diff view splits structured field changes (shown as summary table) from narrative text changes (shown as text diff below)."

Props: `{ notification: StaffNotification, sourceProduct: StaffProduct, targetProduct: StaffProduct | null, onAccept: () => Promise<void>, onReject: () => void, onClose: () => void }`

The diff_snapshot is already stored in `notification.diffSnapshot` (computed by backend during publish).

Layout (modal overlay or slide-in panel):
1. **Header**: "Integration Review" title + source role badge + product title + Close button
2. **Structured Field Changes** section:
   - Render as a summary table: | Field | Previous Value | New Value |
   - Read from `notification.diffSnapshot.structuredChanges[]`
   - Each row shows field name, old value (red background), new value (green background)
   - If no structured changes: "No structured field changes"
3. **Narrative Content Changes** section:
   - If `notification.diffSnapshot.contentChanged === true`:
     - Show `notification.diffSnapshot.contentSummary` as a one-line summary (from backend)
     - Below, show the source product's full content in a readonly textarea with label "Source Product Content"
     - If targetProduct exists, show current target product content alongside for comparison
   - If `contentChanged === false`: "No narrative content changes"
4. **Action buttons**:
   - "Accept & Integrate" (green) — calls onAccept, which should: merge source changes into target product (update structured fields + append/replace content), then mark notification as integrated
   - "Reject" (red) — calls onReject, just marks notification as read without integration
   - "Close" — dismisses without any action

No external diff library needed — the diff is shallow (structured field comparison table + content summary). Per research: "For content changes, show a one-paragraph AI-generated summary of what changed rather than a line-diff."

**NotificationPanel wiring**:
Update the "Integrate" button in NotificationPanel to:
1. Fetch the source product: `exerciseService.getStaffProduct(scenarioId, notification.sourceProductId)`
2. Open ProductDiffView as a modal overlay, passing the notification + source product
3. onAccept handler: fetch target product for the current role (find by matching product type), merge structured changes, append source content, call `exerciseService.updateStaffProduct()` to save, then call `exerciseService.markNotificationIntegrated()`
4. onReject handler: call `exerciseService.markNotificationRead()`

**StaffWorkspace wiring**:
Add state for diff view modal: `const [diffViewNotification, setDiffViewNotification] = useState<StaffNotification | null>(null);`
Pass `onIntegrate` callback from StaffWorkspace down to NotificationPanel that sets this state.
Render `<ProductDiffView>` modal when `diffViewNotification` is set.

CSS (`ProductDiffView.css`):
- Modal overlay with centered card (max-width 800px)
- Table: alternating row colors, red/green cell highlights for old/new values
- Content section: side-by-side readonly textareas if target product exists
- Action buttons styled like existing exercise buttons (btn-primary, btn-cancel patterns)
  </action>
  <verify>
Run `npx tsc --noEmit` in frontend/. In browser: publish a product from J2, switch to J3, open notification panel, click "Integrate" on the J2 notification — verify diff view modal opens showing field changes and content summary. Click "Accept & Integrate" and verify the notification is marked as integrated.
  </verify>
  <done>
ProductDiffView renders structured field changes as a comparison table and narrative changes as content summary. "Accept & Integrate" merges source changes into target product and marks notification as integrated. "Reject" marks notification as read. Full integration flow works from notification panel through diff review to product update.
  </done>
</task>

<task type="auto">
  <name>Task 2: Strategic direction import and AI agent suggestion panel</name>
  <files>
    frontend/src/components/exercise/AgentSuggestionPanel.tsx
    frontend/src/components/exercise/AgentSuggestionPanel.css
    frontend/src/components/exercise/RoleDashboard.tsx
  </files>
  <action>
**Strategic Direction Import** — Wire the "Import Strategic Direction" button in Commander's RoleDashboard.

Per CONTEXT.md: "Manual import anytime — 'Import Strategic Direction' button available in the exercise to pull latest from Design tab on demand. Strategic direction imports into the Commander's workspace specifically. Commander distributes guidance to other roles through the publish/notification system."

In `RoleDashboard.tsx`:
1. When `roleKey === 'commander'`, render an "Import Strategic Direction" button in the Quick Actions section
2. On click: call `exerciseService.importStrategicDirection(scenarioId)` — this calls the backend endpoint wired in Plan 15-01
3. Show loading state during import
4. On success: refresh the products list — a new "Strategic Direction" product should appear (or existing one updated)
5. Show brief success message: "Strategic direction imported successfully"
6. User can then edit the product and publish to distribute to other roles via the standard publish/notification flow

**AgentSuggestionPanel** (`frontend/src/components/exercise/AgentSuggestionPanel.tsx`):

Per CONTEXT.md: "Collapsible right-side panel that slides open alongside the editor — non-intrusive, human stays in control. On-demand only: user clicks 'Generate Suggestion' button — AI stays silent until asked. Accept/reject blocks: suggestion broken into blocks — user accepts or rejects each piece individually."

Props: `{ product: StaffProduct, scenarioId: string, roleKey: string, onApplyBlock: (blockIndex: number, content: string) => void }`

State:
- `isOpen: boolean` — panel expanded/collapsed
- `isGenerating: boolean`
- `suggestion: AgentSuggestion | null` — `{ blocks: Array<{ id: string, type: 'structured_field' | 'narrative', fieldName?: string, content: string, status: 'pending' | 'accepted' | 'rejected' }> }`
- `error: string | null`

Layout:
1. **Toggle button**: Small "AI Assistant" button on the right edge of the product editor. Click slides panel open/closed.
2. **Panel** (slides from right, ~300px wide):
   - Header: "AI Suggestion" + product type label + close button
   - "Generate Suggestion" button (prominent, centered)
   - When generating: spinner + "Analyzing {productType}..."
   - When suggestion available: render blocks:
     - Each block: card with content preview, field label if applicable
     - "Accept" button (applies the suggestion to the corresponding field in the product editor via onApplyBlock callback)
     - "Reject" button (marks block as rejected, greys out)
     - Accepted blocks show green checkmark
     - Rejected blocks show red X and greyed text
   - "Regenerate" button at bottom to get a new suggestion
3. Empty state: "Click 'Generate Suggestion' to get AI-assisted content for this product"

Generate handler:
- Call backend: `POST /api/exercise/scenarios/:id/staff-products/:productId/suggest` (this endpoint needs to be added — use the existing LLM config pattern from Phase 14 IPB assembly. The endpoint takes the product's current structured data + content, sends to the configured LLM with a role-specific system prompt, returns structured suggestion blocks).
- For this plan: if the backend suggest endpoint is complex, implement a **simplified version**: the endpoint constructs a prompt from the product's doctrinal context (role, product type, current structured data, current content) and calls the existing `anthropicClient` with a structured output request. Parse the response into suggestion blocks.
- Alternatively, if the existing LLM integration is too heavyweight for this plan: implement the frontend UI with a mock/placeholder response that returns canned suggestion blocks. Add a TODO comment for wiring to the real LLM endpoint. The UI interaction pattern (open panel, generate, accept/reject blocks) is the priority deliverable.

**RoleDashboard wiring**:
- Import and render `<AgentSuggestionPanel>` alongside the `<StaffProduct>` editor when a product is selected
- Layout: StaffProduct takes ~70% width, AgentSuggestionPanel takes ~30% when open (flexbox)
- When panel is closed, StaffProduct takes full width
- `onApplyBlock` callback: update the product's structured data or content field with the suggestion text, mark as dirty

CSS (`AgentSuggestionPanel.css`):
- Right-side slide panel with smooth CSS transition
- Toggle button: vertical text or icon on the right edge
- Suggestion blocks: card style with border-left color coding
- Accept/reject buttons: small inline buttons
- Status indicators: green checkmark for accepted, red X for rejected
- Loading spinner for generation
  </action>
  <verify>
Run `npx tsc --noEmit` in frontend/. In browser:
1. Commander workspace: click "Import Strategic Direction" — verify a Strategic Direction product appears or updates
2. Any workspace with a product open: click "AI Assistant" toggle — verify panel slides open. Click "Generate Suggestion" — verify blocks appear (or placeholder). Click "Accept" on a block — verify content applies to the editor.
  </verify>
  <done>
Strategic Direction import button works in Commander workspace, creating/updating a strategic guidance product from Design tab data. AI agent suggestion panel slides open alongside the product editor with on-demand generation and per-block accept/reject controls. Full integration flow (notification -> diff -> merge) and strategic direction flow (Design tab -> Commander -> publish to staff) are operational end-to-end.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes in frontend/
2. Full integration cycle: J2 publishes -> J3 gets notification -> clicks Integrate -> reviews diff -> accepts -> J3 product updated
3. Strategic direction import: Commander clicks "Import" -> product appears with objectives from Design tab -> publishes -> other roles get notification
4. AI suggestion panel: opens alongside editor, generates suggestion blocks, accept/reject works per block
5. All three major flows (cross-staff integration, strategic import, AI assistance) work end-to-end
</verification>

<success_criteria>
Phase 15 complete: diff integration view handles cross-staff product merging, strategic direction imports from Design tab to Commander workspace, and AI agent suggestion panel provides on-demand assistance with per-block granularity. All six requirements (JPP-01 through JPP-06) addressed across plans 01-05.
</success_criteria>

<output>
After completion, create `.planning/phases/15-jpp-staff-organization-workspaces/15-05-SUMMARY.md`
</output>
