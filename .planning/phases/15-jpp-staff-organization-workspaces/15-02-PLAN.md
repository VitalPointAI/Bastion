---
phase: 15-jpp-staff-organization-workspaces
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - frontend/src/types/exercise.ts
  - frontend/src/services/exercise-service.ts
  - frontend/src/components/exercise/StaffWorkspace.tsx
  - frontend/src/components/exercise/StaffWorkspace.css
  - frontend/src/components/exercise/RoleDashboard.tsx
  - frontend/src/components/exercise/ExerciseDashboard.tsx
  - frontend/src/components/exercise/ExerciseDashboard.css
autonomous: true
requirements:
  - JPP-01

must_haves:
  truths:
    - "Exercise area shows a vertical sidebar listing enabled staff roles for the active scenario"
    - "Clicking a role in the sidebar loads that role's workspace in the main content area without page reload"
    - "Role switching is free — no assignment restriction, any user can view any enabled role"
    - "Each role workspace shows a dashboard overview with product summary, recent activity, and quick actions"
    - "Create Scenario modal includes role selection checkboxes for configuring enabled roles"
    - "ExerciseDashboard header row (scenario selector, perspective toggle, phase nav) is preserved"
  artifacts:
    - path: "frontend/src/components/exercise/StaffWorkspace.tsx"
      provides: "Role sidebar + workspace shell using TabLayout"
      contains: "TabLayout"
    - path: "frontend/src/components/exercise/RoleDashboard.tsx"
      provides: "Per-role overview dashboard"
      contains: "RoleDashboard"
    - path: "frontend/src/components/exercise/ExerciseDashboard.tsx"
      provides: "Modified to host StaffWorkspace instead of horizontal tabs"
    - path: "frontend/src/services/exercise-service.ts"
      provides: "Staff workspace API methods added"
      contains: "staffProducts"
  key_links:
    - from: "frontend/src/components/exercise/StaffWorkspace.tsx"
      to: "frontend/src/components/tabs/TabLayout.tsx"
      via: "Import and render TabLayout with STAFF_ROLES as SidebarItems"
      pattern: "import.*TabLayout"
    - from: "frontend/src/components/exercise/ExerciseDashboard.tsx"
      to: "frontend/src/components/exercise/StaffWorkspace.tsx"
      via: "Renders StaffWorkspace in place of tab-nav + content area"
      pattern: "StaffWorkspace"
    - from: "frontend/src/components/exercise/RoleDashboard.tsx"
      to: "frontend/src/services/exercise-service.ts"
      via: "Fetches staff products list for the active role"
      pattern: "exerciseService\\.getStaffProducts"
---

<objective>
Restructure the Exercise area UI to use a vertical staff role sidebar with per-role dashboard workspaces, replacing the existing horizontal tab navigation while preserving the header row (scenario selector, perspective toggle, phase nav).

Purpose: This is the core UX transformation — users navigate by staff role (Commander, J2, J3, etc.) instead of by tab (IPB, COAs, Orders). The sidebar mirrors the doctrinal JPP staff organization.
Output: StaffWorkspace component wrapping TabLayout, RoleDashboard for per-role overview, modified ExerciseDashboard, updated frontend types and service layer.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-jpp-staff-organization-workspaces/15-RESEARCH.md
@.planning/phases/15-jpp-staff-organization-workspaces/15-01-SUMMARY.md

# Existing components to build on:
@frontend/src/components/tabs/TabLayout.tsx
@frontend/src/components/tabs/TabLayout.css
@frontend/src/components/exercise/ExerciseDashboard.tsx
@frontend/src/components/exercise/ExerciseDashboard.css
@frontend/src/types/exercise.ts
@frontend/src/services/exercise-service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frontend types, service methods, and StaffWorkspace shell</name>
  <files>
    frontend/src/types/exercise.ts
    frontend/src/services/exercise-service.ts
    frontend/src/components/exercise/StaffWorkspace.tsx
    frontend/src/components/exercise/StaffWorkspace.css
  </files>
  <action>
**Update frontend types** (`frontend/src/types/exercise.ts`):
Add mirror types for the backend domain model created in Plan 01:
- `StaffRoleKey` type union: 'commander' | 'j1' | 'j2' | 'j3' | 'j35' | 'j4' | 'j5' | 'j6'
- `StaffProduct` interface: id, scenarioId, roleKey, productType, title, status ('draft'|'published'), structured (Record<string, unknown>), content (string), version, publishedAt?, publishedBy?, createdBy, createdAt, updatedAt
- `CreateStaffProductInput` interface: roleKey, productType, title, structured?, content?
- `StaffNotification` interface: id, scenarioId, sourceProductId, sourceRole, targetRole, notificationType, diffSnapshot (DiffSnapshot), isRead, isIntegrated, createdAt
- `DiffSnapshot` interface: structuredChanges array, contentChanged boolean, contentSummary? string
- Add `enabledRoles?: string[]` to existing `ExerciseScenario` interface
- Add `enabledRoles?: string[]` to existing `CreateScenarioInput` interface

**Update exercise-service** (`frontend/src/services/exercise-service.ts`):
Add API methods matching the Plan 01 routes:
- `getStaffProducts(scenarioId: string, role?: string): Promise<StaffProduct[]>` — GET /scenarios/:id/staff-products?role=...
- `getStaffProduct(scenarioId: string, productId: string): Promise<StaffProduct>` — GET /scenarios/:id/staff-products/:productId
- `createStaffProduct(scenarioId: string, input: CreateStaffProductInput): Promise<StaffProduct>` — POST
- `updateStaffProduct(scenarioId: string, productId: string, fields: Partial<CreateStaffProductInput>): Promise<StaffProduct>` — PUT
- `publishStaffProduct(scenarioId: string, productId: string): Promise<void>` — POST .../publish
- `deleteStaffProduct(scenarioId: string, productId: string): Promise<void>` — DELETE
- `getStaffNotifications(scenarioId: string, role: string): Promise<StaffNotification[]>` — GET
- `getUnreadNotificationCount(scenarioId: string, role: string): Promise<number>` — GET
- `markNotificationRead(scenarioId: string, notificationId: string): Promise<void>` — PUT
- `markNotificationIntegrated(scenarioId: string, notificationId: string): Promise<void>` — PUT
- `importStrategicDirection(scenarioId: string): Promise<StaffProduct>` — POST
- `updateEnabledRoles(scenarioId: string, enabledRoles: string[]): Promise<void>` — PUT
Follow the existing `fetchJson` helper pattern in exercise-service.ts.

**Create StaffWorkspace component** (`StaffWorkspace.tsx`):
- Import `TabLayout` and `SidebarItem` from `../../components/tabs/TabLayout`
- Define `STAFF_ROLES` constant: array of SidebarItem objects for all 8 roles (commander, j1-j6, j35) with labels matching JP 5-0 naming (e.g., 'J2 Intelligence', 'J3 Operations', 'J35 Plans', 'J4 Logistics', 'J5 Strategic Plans', 'J6 Communications', 'J1 Personnel')
- Props: `{ scenario: ExerciseScenario, perspective: 'blue' | 'red', exercisePhase: string }`
- State: `activeRole` (string, default 'commander')
- Filter `STAFF_ROLES` by `scenario.enabledRoles` — only show enabled roles in sidebar
- Render `<TabLayout items={enabledItems} selectedItem={activeRole} onSelectItem={setActiveRole}>` wrapping a `<RoleDashboard>` component
- Pass `roleKey={activeRole}`, `scenarioId={scenario.id}`, `exercisePhase`, `perspective` to RoleDashboard

**Create StaffWorkspace.css**: Minimal styling — TabLayout handles sidebar chrome. Add any workspace-specific spacing or role-indicator styling. Use existing CSS custom properties from the project (check ExerciseDashboard.css for color variables).

CRITICAL per research pitfall 1: Do NOT build a custom sidebar. REUSE TabLayout.
CRITICAL per research pitfall 5: Filter sidebar items by scenario.enabledRoles.
  </action>
  <verify>
Run `grep -c 'StaffRoleKey\|StaffProduct\|StaffNotification\|DiffSnapshot' frontend/src/types/exercise.ts` to confirm frontend types added. Run `grep -c 'getStaffProducts\|publishStaffProduct\|importStrategicDirection\|getStaffNotifications' frontend/src/services/exercise-service.ts` to confirm service methods. Run `grep 'TabLayout' frontend/src/components/exercise/StaffWorkspace.tsx` to confirm TabLayout reuse.
  </verify>
  <done>Frontend types mirror backend. Exercise-service has 12 new API methods. StaffWorkspace renders TabLayout with role sidebar filtered by enabledRoles.</done>
</task>

<task type="auto">
  <name>Task 2: RoleDashboard and ExerciseDashboard restructure</name>
  <files>
    frontend/src/components/exercise/RoleDashboard.tsx
    frontend/src/components/exercise/ExerciseDashboard.tsx
    frontend/src/components/exercise/ExerciseDashboard.css
  </files>
  <action>
**Create RoleDashboard component** (`RoleDashboard.tsx`):
Props: `{ roleKey: string, scenarioId: string, exercisePhase: string, perspective: 'blue' | 'red' }`

This is the overview dashboard shown when a role is selected in the sidebar. It provides:

1. **Role header**: Display role name and brief doctrinal description (e.g., "J2 Intelligence — IPB, Threat Assessment, OOB")
2. **Product summary cards**: Fetch `exerciseService.getStaffProducts(scenarioId, roleKey)` and display:
   - Count of draft products
   - Count of published products
   - List of product titles with status badges (draft=grey, published=green)
   - "Create Product" button (for Plan 03 — render as disabled placeholder for now with text "Coming in next update")
3. **Notification badge**: Fetch `exerciseService.getUnreadNotificationCount(scenarioId, roleKey)` and display unread count
4. **Quick actions section**: Static list of doctrinally-relevant actions for each role (e.g., J2: "Create Threat Assessment", "View IPB Products"; Commander: "Import Strategic Direction", "Issue Guidance"). Render as disabled buttons for now — Plan 03-05 will wire them.
5. **Recent activity placeholder**: "Recent activity will appear here" text block — will be populated when products exist

Use a ROLE_CONFIG constant that maps each roleKey to:
- `label`: Display name (e.g., 'Commander', 'J2 Intelligence')
- `description`: One-line doctrinal responsibility
- `defaultProducts`: Array of product types this role typically creates (from research "Doctrinal Products by Role" table)
- `quickActions`: Array of action labels for the quick actions section

Layout: CSS grid — 2-column on desktop (products left, notifications/actions right), 1-column on mobile. Use existing project CSS patterns (card styling from ExerciseDashboard.css or AdminDashboard.css).

**Modify ExerciseDashboard** (`ExerciseDashboard.tsx`):
This is the CRITICAL structural change. Per research pitfall 3, do NOT rewrite ExerciseDashboard — restructure it:

1. **PRESERVE** the header row: scenario selector dropdown, perspective toggle (Blue/Red), phase timeline navigation, controller toggle, and create scenario modal. These all remain exactly as they are.
2. **REMOVE** the horizontal tab navigation bar (the `TABS` constant and the tab button row)
3. **REMOVE** the tab content switch (`renderTabContent` or equivalent)
4. **REPLACE** the tab-nav + content area with `<StaffWorkspace scenario={selectedScenario} perspective={perspective} exercisePhase={currentPhase} />` when a scenario is selected
5. The existing tab components (IPBPanel, COAScoringPanel, OrderEditor, PlanningBoard, ExerciseTimeline, GateControl, CommanderDecisionPanel, ScenarioPackageUpload) should still be importable — they will be rendered from within role workspaces in Plan 03. For now, keep the imports but comment out the tab switch. Do NOT delete these components.
6. Add `enabledRoles` to the CreateScenarioModal: add a "Staff Roles" section with checkboxes for each of the 8 roles, all checked by default. The selected roles get passed as `enabledRoles` in the create scenario API call.

**Update ExerciseDashboard.css**:
- Remove tab-bar styling (`.exercise-tabs`, `.tab-button`, etc.)
- Add any spacing needed for the StaffWorkspace integration
- Keep all header, perspective toggle, and phase timeline styles

CRITICAL: Do NOT remove the `scenario-selector`, `perspective-toggle`, or `phase-indicator` elements.
CRITICAL: Do NOT remove imports of existing Phase 14 components — they will be used in Plan 03.
  </action>
  <verify>
Run `grep 'StaffWorkspace' frontend/src/components/exercise/ExerciseDashboard.tsx` to confirm StaffWorkspace is rendered. Run `grep 'enabledRoles' frontend/src/components/exercise/ExerciseDashboard.tsx` to confirm role selection in create modal. Run `grep 'scenario-selector\|perspective-toggle' frontend/src/components/exercise/ExerciseDashboard.tsx` to confirm header elements preserved. Visually verify in browser: navigate to /exercise, select a scenario, confirm sidebar appears with role list.
  </verify>
  <done>ExerciseDashboard header preserved (scenario selector, perspective toggle, phase nav). Tab navigation replaced by StaffWorkspace sidebar. RoleDashboard shows per-role overview with product summary and notification count. CreateScenarioModal includes role selection checkboxes.</done>
</task>

</tasks>

<verification>
1. StaffWorkspace.tsx imports and renders TabLayout with role-filtered sidebar items
2. ExerciseDashboard.tsx renders StaffWorkspace instead of tab navigation
3. ExerciseDashboard header row (scenario selector, perspective toggle, phase nav) is preserved
4. RoleDashboard fetches and displays product summary and notification count per role
5. CreateScenarioModal includes enabledRoles checkboxes
6. Frontend types and service methods match Plan 01 backend API
</verification>

<success_criteria>
- Role sidebar visible in exercise area with collapsible toggle
- Clicking a role loads RoleDashboard without page reload
- All 8 roles appear by default; filtering by enabledRoles works
- Header row fully functional (scenario switching, perspective toggle, phase navigation)
- No Phase 14 components deleted (just not rendered in tab bar anymore)
</success_criteria>

<output>
After completion, create `.planning/phases/15-jpp-staff-organization-workspaces/15-02-SUMMARY.md`
</output>
