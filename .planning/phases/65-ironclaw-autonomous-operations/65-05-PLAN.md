---
phase: 65-ironclaw-autonomous-operations
plan: 05
type: execute
wave: 3
depends_on: ["65-01", "65-03"]
files_modified:
  - frontend/src/components/ironclaw/IronclawActivityFeed.tsx
  - frontend/src/components/ironclaw/IronclawDrawer.tsx
  - frontend/src/types/ironclaw.ts
  - backend/src/ironclaw/ironclaw-router.ts
autonomous: false
requirements:
  - SC-08-activity-observable
  - SC-05-decisions-surfaced

must_haves:
  truths:
    - "Commander can see what Ironclaw has been doing autonomously"
    - "Activity feed updates in real-time via WebSocket"
    - "Each activity entry shows timestamp, type icon, severity badge, and summary"
    - "Critical/urgent activities link to their decision gates"
    - "Activity feed is separate from chat history"
  artifacts:
    - path: "frontend/src/components/ironclaw/IronclawActivityFeed.tsx"
      provides: "Autonomous activity feed panel component"
      min_lines: 80
    - path: "frontend/src/types/ironclaw.ts"
      provides: "AutonomousActivityEntry type definition"
      contains: "AutonomousActivityEntry"
    - path: "backend/src/ironclaw/ironclaw-router.ts"
      provides: "GET /api/ironclaw/activity/:problemSetId endpoint"
      contains: "activity"
  key_links:
    - from: "frontend/src/components/ironclaw/IronclawActivityFeed.tsx"
      to: "backend/src/ironclaw/ironclaw-router.ts"
      via: "fetch GET /api/ironclaw/activity/{problemSetId}"
      pattern: "api/ironclaw/activity"
    - from: "frontend/src/components/ironclaw/IronclawActivityFeed.tsx"
      to: "WebSocket"
      via: "Subscribe to ironclaw.autonomous-activity messages"
      pattern: "ironclaw\\.autonomous-activity"
    - from: "frontend/src/components/ironclaw/IronclawDrawer.tsx"
      to: "frontend/src/components/ironclaw/IronclawActivityFeed.tsx"
      via: "Tab or section rendering IronclawActivityFeed"
      pattern: "IronclawActivityFeed"
---

<objective>
Build the frontend autonomous activity feed so commanders can observe what Ironclaw has been doing between interactions. Add a REST endpoint for fetching historical activity and a WebSocket subscription for real-time updates. Integrate the feed into the IronclawDrawer as a separate tab from chat.

Purpose: Make autonomous operations visible to the commander
Output: Activity feed component, REST endpoint, real-time WebSocket updates
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-ironclaw-autonomous-operations/GOAL.md
@.planning/phases/65-ironclaw-autonomous-operations/65-RESEARCH.md
@.planning/phases/65-ironclaw-autonomous-operations/65-01-SUMMARY.md
@.planning/phases/65-ironclaw-autonomous-operations/65-03-SUMMARY.md

@frontend/src/components/ironclaw/IronclawDrawer.tsx
@frontend/src/types/ironclaw.ts
@backend/src/ironclaw/ironclaw-router.ts

<interfaces>
<!-- From 65-01: autonomous-activity-store.ts -->
autonomousActivityStore.getRecent(problemSetId: string, limit?: number): Promise<ActivityEntry[]>
interface ActivityEntry {
  id: string;
  problemSetId: string;
  activityType: string;
  severity: 'critical' | 'urgent' | 'routine' | 'informational';
  summary: string;
  detail: Record<string, unknown> | null;
  decisionId: string | null;
  createdAt: Date;
}

<!-- WebSocket message format from Plan 01 callback router -->
channel: ironclaw.{problemSetId}
messageType: ironclaw.autonomous-activity
payload: { type, payload, severity }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add activity REST endpoint and frontend types</name>
  <files>backend/src/ironclaw/ironclaw-router.ts, frontend/src/types/ironclaw.ts</files>
  <action>
1. **Add REST endpoint** to `backend/src/ironclaw/ironclaw-router.ts`:
   - Read the full file to understand existing route patterns and auth middleware
   - Add `GET /activity/:problemSetId` route:
     - Requires authentication (use same auth middleware as other ironclaw routes)
     - Query params: `limit` (default 50, max 200), `since` (ISO timestamp, optional)
     - Calls `autonomousActivityStore.getRecent(problemSetId, limit)`
     - If `since` provided, filter to entries after that timestamp
     - Returns `{ activities: ActivityEntry[], total: number }`
   - Import `autonomousActivityStore` from `./autonomous-activity-store.js`

2. **Add frontend types** to `frontend/src/types/ironclaw.ts`:
   - Read the existing file to understand the type structure
   - Add:
   ```typescript
   export interface AutonomousActivityEntry {
     id: string;
     problemSetId: string;
     activityType: string;
     severity: 'critical' | 'urgent' | 'routine' | 'informational';
     summary: string;
     detail: Record<string, unknown> | null;
     decisionId: string | null;
     createdAt: string; // ISO string from API
   }
   ```
  </action>
  <verify>
    <automated>bash -lc 'cd /home/vitalpointai/projects/ssr && npx tsc --noEmit backend/src/ironclaw/ironclaw-router.ts 2>&1 | head -10 && npx tsc --noEmit frontend/src/types/ironclaw.ts 2>&1 | head -10'</automated>
  </verify>
  <done>GET /api/ironclaw/activity/:problemSetId returns paginated activity entries, AutonomousActivityEntry type exported from frontend types</done>
</task>

<task type="auto">
  <name>Task 2: Build activity feed component and integrate into IronclawDrawer</name>
  <files>frontend/src/components/ironclaw/IronclawActivityFeed.tsx, frontend/src/components/ironclaw/IronclawDrawer.tsx, frontend/src/components/ironclaw/index.ts</files>
  <action>
1. **Create `IronclawActivityFeed.tsx`:**
   - Props: `{ problemSetId: string }`
   - On mount, fetch `GET /api/ironclaw/activity/${problemSetId}?limit=50`
   - Subscribe to WebSocket messages on channel `ironclaw.${problemSetId}` with messageType `ironclaw.autonomous-activity` — prepend new entries to the list in real-time
   - Check how other components subscribe to WebSocket (look at IronclawDrawer.tsx or similar for the pattern — likely via a useEffect + WebSocket hook)

   **Render each entry as a card/row with:**
   - Timestamp (relative: "5 min ago", "2 hours ago") — use same date formatting as elsewhere in the app
   - Activity type icon:
     - `gap_detected` → magnifying glass or search icon
     - `conflict_found` / `conflict_detected` → warning triangle
     - `assessment_drafted` / `situation_assessment` → document/clipboard icon
     - `skill_created` / `skill_creation_request` → wrench/gear icon
     - `alert` → bell icon
     - Default → info circle
   - Severity badge: critical (red), urgent (amber/orange), routine (blue), informational (gray)
   - Summary text
   - If `decisionId` is present, render a "View Decision" link/button that navigates to the decision gate

   **Empty state:** "No autonomous activity yet. Ironclaw will begin monitoring on the next heartbeat cycle."

   **Styling:** Use Tailwind classes consistent with the rest of the app. Compact card layout, scrollable container. Most recent entries on top.

2. **Integrate into `IronclawDrawer.tsx`:**
   - Read the full file to understand its tab/section structure
   - Add a new tab or section labeled "Activity" (or "Autonomous Activity") alongside the existing Chat tab
   - Use a simple tab toggle: Chat | Activity
   - When Activity tab is selected, render `<IronclawActivityFeed problemSetId={currentProblemSetId} />`
   - The activity feed should be visually distinct from chat — it is NOT a conversation, it is a log/feed
   - Add a subtle badge/indicator on the Activity tab showing count of unread critical/urgent entries (optional, only if straightforward)

3. **Update `frontend/src/components/ironclaw/index.ts`:**
   - Export `IronclawActivityFeed` from the barrel file
  </action>
  <verify>
    <automated>bash -lc 'cd /home/vitalpointai/projects/ssr && npx tsc --noEmit frontend/src/components/ironclaw/IronclawActivityFeed.tsx frontend/src/components/ironclaw/IronclawDrawer.tsx 2>&1 | head -20'</automated>
  </verify>
  <done>IronclawActivityFeed renders activity entries with icons/badges/timestamps, real-time WebSocket updates prepend new entries, IronclawDrawer has Chat/Activity tab toggle, decision gate links work</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete autonomous operations integration</name>
  <what-built>Complete Ironclaw autonomous operations: callback webhook, 7 MCP tools, event forwarding, enriched HEARTBEAT.md, autonomous monitoring routines, SOUL.md proactive identity, activity feed UI</what-built>
  <how-to-verify>
    1. Check the IronclawDrawer in the UI — verify there is a Chat/Activity tab toggle
    2. Click the Activity tab — should show empty state "No autonomous activity yet..."
    3. Verify backend builds without errors: `bash -lc 'cd /home/vitalpointai/projects/ssr && npm run build 2>&1 | tail -20'`
    4. Check that bastion-mcp health endpoint shows increased tool count: the 7 new tools should be included
    5. Verify gap filler service is disabled: grep for deprecation notice in gap-filler-service.ts
    6. Check HEARTBEAT.md rendering includes "Autonomous Monitoring Tasks" section
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `tsc --noEmit` passes for all frontend and backend files
- `npm run build` succeeds for both frontend and backend
- Activity feed component renders with proper styling
- WebSocket subscription wired for real-time updates
- REST endpoint returns activity entries
- IronclawDrawer has Chat/Activity tab toggle
</verification>

<success_criteria>
- Commander opens Ironclaw drawer and sees Chat/Activity tabs
- Activity tab shows historical autonomous actions
- New autonomous findings appear in real-time via WebSocket
- Each entry has timestamp, type icon, severity badge, summary
- Critical/urgent entries link to decision gates
- Activity feed is visually distinct from chat history
</success_criteria>

<output>
After completion, create `.planning/phases/65-ironclaw-autonomous-operations/65-05-SUMMARY.md`
</output>
