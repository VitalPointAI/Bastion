---
phase: 65-ironclaw-autonomous-operations
plan: 04
type: execute
wave: 2
depends_on: ["65-02"]
files_modified:
  - backend/src/ironclaw/routine-service.ts
  - backend/src/ironclaw/ironclaw-service.ts
  - backend/src/ironclaw/identity-renderer.ts
autonomous: true
requirements:
  - SC-01-autonomous-operation
  - SC-06-self-extends
  - SC-07-governance-respected

must_haves:
  truths:
    - "Ironclaw registers an autonomous monitoring routine on startup for each active problem set"
    - "SOUL.md frames Ironclaw as proactive Chief of Staff, not reactive chatbot"
    - "Self-extending skill creation goes through medium-risk governance gate"
    - "Heartbeat interval is configurable with 15-minute minimum"
  artifacts:
    - path: "backend/src/ironclaw/routine-service.ts"
      provides: "Autonomous monitoring routine registration"
      contains: "autonomous_monitoring"
    - path: "backend/src/ironclaw/identity-renderer.ts"
      provides: "Updated SOUL.md with proactive Chief of Staff identity"
      contains: "proactive Chief of Staff"
  key_links:
    - from: "backend/src/ironclaw/routine-service.ts"
      to: "backend/src/ironclaw/ironclaw-client.ts"
      via: "/routine register command via sendMessage"
      pattern: "routine register"
    - from: "backend/src/ironclaw/ironclaw-service.ts"
      to: "backend/src/ironclaw/routine-service.ts"
      via: "registerAutonomousMonitoring call during identity sync"
      pattern: "registerAutonomousMonitoring"
---

<objective>
Register autonomous monitoring routines with Ironclaw's OpenClaw scheduler and update SOUL.md to frame Ironclaw as a proactive Chief of Staff that operates continuously. Wire routine registration into the identity sync flow so each active problem set gets an autonomous monitoring heartbeat.

Purpose: Activate Ironclaw's native heartbeat-driven autonomous operation
Output: Registered autonomous routines, proactive SOUL.md identity, self-extending governance
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-ironclaw-autonomous-operations/GOAL.md
@.planning/phases/65-ironclaw-autonomous-operations/65-RESEARCH.md
@.planning/phases/65-ironclaw-autonomous-operations/65-02-SUMMARY.md

@backend/src/ironclaw/routine-service.ts
@backend/src/ironclaw/ironclaw-service.ts
@backend/src/ironclaw/identity-renderer.ts
@backend/src/ironclaw/ironclaw-client.ts

<interfaces>
<!-- From backend/src/ironclaw/ironclaw-client.ts -->
ironclawClient.sendMessage(threadId, content, didSlug?): Promise<WebhookResponse>
// /routine register command: content = '/routine register autonomous_monitoring__{psId} "*/30 * * * *"'

<!-- From backend/src/ironclaw/routine-service.ts -->
export const BUILT_IN_ROUTINES: BuiltInRoutine[];
// Already has bastion_knowledge_sync, bastion_user_knowledge_sync, weekly_capability_update, daily_situation_brief

<!-- From 65-02: New MCP tools -->
bastion.intel.detect_conflicts, bastion.intel.web_search, bastion.autonomous.log_activity, etc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add autonomous monitoring routine and register on identity sync</name>
  <files>backend/src/ironclaw/routine-service.ts, backend/src/ironclaw/ironclaw-service.ts</files>
  <action>
1. **Update `backend/src/ironclaw/routine-service.ts`:**
   - Read the full file to understand existing routine patterns
   - Add a new built-in routine to `BUILT_IN_ROUTINES`:
     ```typescript
     {
       id: 'autonomous_monitoring',
       name: 'Autonomous Operational Monitoring',
       description: 'Heartbeat-driven monitoring: conflict detection, gap research, PIR checking, situation assessment drafting. Ironclaw evaluates HEARTBEAT.md directives and takes autonomous action.',
       defaultCron: '*/30 * * * *',  // Every 30 minutes
       editable: true,
       category: 'monitoring',
     }
     ```
   - Add a new method `registerAutonomousMonitoring(problemSetId: string, cronOverride?: string)`:
     - Sends `/routine register autonomous_monitoring__${problemSetId} "${cron}"` via `ironclawClient.sendMessage()` to the shared thread
     - The cron defaults to `*/30 * * * *` (every 30 minutes) but is overridable from agent config
     - Enforces minimum 15-minute interval: if cron resolves to <15 min, warn and use 15 min
     - Wraps in try/catch — routine registration failure should log a warning, not crash
   - Add `unregisterAutonomousMonitoring(problemSetId: string)` that sends `/routine unregister autonomous_monitoring__${problemSetId}`

2. **Wire into identity sync** (`backend/src/ironclaw/ironclaw-service.ts`):
   - Read the full file to find the identity sync flow (where SOUL.md, HEARTBEAT.md, USER.md are synced)
   - After identity sync completes for a problem set, call `routineService.registerAutonomousMonitoring(problemSetId)`
   - This ensures every problem set that gets identity sync also gets an autonomous monitoring heartbeat
   - Add a config check: only register if agent config `autonomousMonitoring` is not explicitly disabled
   - Add to the agent config type if needed: `autonomousMonitoring?: { enabled: boolean; intervalMinutes?: number }`
  </action>
  <verify>
    <automated>bash -lc 'cd /home/vitalpointai/projects/ssr && npx tsc --noEmit backend/src/ironclaw/routine-service.ts backend/src/ironclaw/ironclaw-service.ts 2>&1 | head -20'</automated>
  </verify>
  <done>Autonomous monitoring routine defined in BUILT_IN_ROUTINES, registerAutonomousMonitoring sends /routine register command, identity sync calls registration for each problem set, 15-minute minimum interval enforced</done>
</task>

<task type="auto">
  <name>Task 2: Update SOUL.md for proactive Chief of Staff and self-extending governance</name>
  <files>backend/src/ironclaw/identity-renderer.ts</files>
  <action>
1. **Update SOUL.md rendering** in `identity-renderer.ts`:
   - Read the full file to find the SOUL.md rendering function/template
   - Add a new section to every SOUL.md variant that establishes proactive identity:

   ```markdown
   ## Autonomous Operations Protocol

   You are not a chatbot. You are a proactive Chief of Staff who operates continuously
   between user interactions. Your heartbeat routine fires periodically, and you MUST
   use that time to actively monitor the operational environment.

   ### Between User Interactions
   When your heartbeat fires, follow the directives in HEARTBEAT.md. Prioritize:
   1. Check for new events (OSINT, documents, graph changes) forwarded to your autonomous thread
   2. Curate the problem set brain — evaluate the global knowledge graph for newly relevant actors/relationships, augment the slice with discoveries, prune stale/irrelevant entries
   3. Run conflict detection against the knowledge graph
   4. Assess intelligence gaps and research the highest-priority ones
   5. If enough new intelligence has accumulated, draft a situation assessment
   6. Surface any findings that need commander attention via the callback endpoint

   ### Brain Curation Protocol
   The problem set brain is a living, focused subgraph extracted from the global
   knowledge graph. You are responsible for keeping it current, relevant, and complete.

   On each heartbeat cycle:
   1. Call bastion.brain.get_slice_stats to assess the current state of the slice
   2. Call bastion.brain.evaluate_relevance to find actors in the global brain that
      are newly relevant (connected to existing slice actors, matching scope terms,
      or linked to recent OSINT/documents for this problem set)
   3. If high-scoring candidates are found, call bastion.brain.augment_slice to pull
      them in — include a reason explaining why they are relevant
   4. Check for actors in the slice that are stale (not updated recently, no active
      relationships, contradicted by newer intelligence) and prune them with
      bastion.brain.prune_slice
   5. Log curation actions via bastion.autonomous.log_activity so the commander
      can see what changed and why

   The brain should grow organically as the situation develops — new alliances form,
   supply chains are discovered, threat actors emerge. It should also shrink when
   actors become irrelevant — conflicts resolve, organizations dissolve, intelligence
   is superseded. Your judgment determines relevance.

   ### Self-Extension Protocol
   When you identify a recurring task or pattern that you keep doing manually:
   1. Propose a new skill or routine by calling bastion.autonomous.send_alert with the proposal
   2. This creates a medium-risk governance gate requiring commander approval
   3. Once approved, register the new routine with /routine register
   4. Log the creation via bastion.autonomous.log_activity

   ### Decision Surfacing
   Not every finding needs a decision gate. Use judgment:
   - Critical: Contradictions in intelligence that affect ongoing operations → decision gate + Telegram alert
   - Urgent: Intelligence gap filled with significant findings → decision gate
   - Routine: PIR partially answered, situation update drafted → activity log only
   - Informational: Minor graph changes, routine checks with no findings → skip (do not log noise)
   ```

   - Update the Commander SOUL_TEMPLATE to reference autonomous monitoring:
     Add to the Commander template's cognitive style: "- Review Ironclaw's autonomous activity feed for findings that need your decision."

2. **Self-extending skill governance:**
   - In the SOUL.md section above, the self-extension protocol directs Ironclaw to use `bastion.autonomous.send_alert` for proposals
   - The callback handler (from Plan 01) routes `skill_creation_request` type to `createDecision()` with medium-risk governance gate
   - This is already wired — just ensure SOUL.md instructions match the callback types defined in Plan 01
  </action>
  <verify>
    <automated>bash -lc 'cd /home/vitalpointai/projects/ssr && npx tsc --noEmit backend/src/ironclaw/identity-renderer.ts 2>&1 | head -20'</automated>
  </verify>
  <done>SOUL.md includes Autonomous Operations Protocol, Self-Extension Protocol, and Decision Surfacing guidelines. Commander SOUL template references autonomous activity feed. Self-extending skills route through medium-risk governance.</done>
</task>

</tasks>

<verification>
- `tsc --noEmit` passes for all modified files
- BUILT_IN_ROUTINES includes `autonomous_monitoring` entry
- Identity sync flow calls `registerAutonomousMonitoring`
- SOUL.md rendering includes "Autonomous Operations Protocol" section
- Self-extension protocol directs to medium-risk governance gate
</verification>

<success_criteria>
- Ironclaw registers autonomous monitoring routine (cron) for each active problem set
- Heartbeat fires every 30 minutes by default, configurable with 15-minute minimum
- SOUL.md establishes proactive Chief of Staff identity across all staff roles
- SOUL.md includes Brain Curation Protocol with evaluate → augment → prune cycle
- Self-extending skill creation goes through governance approval before registration
- Decision surfacing guidelines prevent noise while catching critical findings
- Brain curation is priority 2 in the heartbeat cycle (after checking new events)
</success_criteria>

<output>
After completion, create `.planning/phases/65-ironclaw-autonomous-operations/65-04-SUMMARY.md`
</output>
