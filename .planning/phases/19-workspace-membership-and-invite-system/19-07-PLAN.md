---
phase: 19-workspace-membership-and-invite-system
plan: 07
type: execute
wave: 7
depends_on: [19-05, 19-06]
files_modified:
  - frontend/src/components/workspace/WorkspaceDashboard.tsx
  - frontend/src/components/workspace/CommanderPanel.tsx
  - frontend/src/components/workspace/StaffPanel.tsx
  - frontend/src/components/workspace/ObserverPanel.tsx
  - frontend/src/App.tsx
autonomous: true
requirements: [WS-ONBOARDING, WS-ROLES]
must_haves:
  truths:
    - "Dashboard renders a shared ActivityFeed plus role-specific panel"
    - "Commander sees pending decisions and command overview"
    - "Staff sections see role-specific content panels"
    - "Observers see read-only mission/exercise list"
    - "Dashboard adapts to role without separate pages per role"
  artifacts:
    - path: "frontend/src/components/workspace/WorkspaceDashboard.tsx"
      provides: "Role-adaptive dashboard shell with conditional panel rendering"
      exports: ["WorkspaceDashboard"]
    - path: "frontend/src/components/workspace/CommanderPanel.tsx"
      provides: "Commander-specific panel (pending decisions, command overview)"
      exports: ["CommanderPanel"]
    - path: "frontend/src/components/workspace/StaffPanel.tsx"
      provides: "Generic staff panel with role label and content"
      exports: ["StaffPanel"]
    - path: "frontend/src/components/workspace/ObserverPanel.tsx"
      provides: "Observer read-only panel"
      exports: ["ObserverPanel"]
  key_links:
    - from: "frontend/src/components/workspace/WorkspaceDashboard.tsx"
      to: "frontend/src/context/WorkspaceContext.tsx"
      via: "useWorkspace() for active workspace and role"
      pattern: "useWorkspace"
---

<objective>
Create the role-adaptive workspace dashboard with CommanderPanel, StaffPanel, and ObserverPanel. This is the primary view users see when they enter a workspace.

Purpose: The dashboard is the workspace landing page. It adapts to the user's role, showing role-specific panels alongside a shared base. This is the "CMS dashboard that adapts" pattern from the user's decisions.
Output: WorkspaceDashboard with 3 role-specific panel components.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-workspace-membership-and-invite-system/19-CONTEXT.md
@.planning/phases/19-workspace-membership-and-invite-system/19-05-SUMMARY.md

@frontend/src/components/exercise/ExerciseDashboard.tsx (composite panel pattern reference)
@frontend/src/context/WorkspaceContext.tsx (useWorkspace hook)
@frontend/src/lib/workspace-service.ts (API client)

<interfaces>
From frontend/src/context/WorkspaceContext.tsx:
```typescript
useWorkspace(): {
  activeWorkspaceId: string | null;
  activeWorkspace: WorkspaceDetail | null;  // { id, name, workspaceType, classification, memberCount, ... }
  userRoleInActive: string | null;          // 'commander', 's2', 'observer', etc.
  memberships: WorkspaceMembership[];
}
```

From frontend/src/lib/workspace-service.ts:
```typescript
listActivity(workspaceId, options?): Promise<WorkspaceActivityItem[]>
listMembers(workspaceId): Promise<WorkspaceMemberDetail[]>
getHierarchy(workspaceId): Promise<HierarchyNode[]>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create role-specific panel components</name>
  <files>frontend/src/components/workspace/CommanderPanel.tsx, frontend/src/components/workspace/StaffPanel.tsx, frontend/src/components/workspace/ObserverPanel.tsx</files>
  <action>
All three panels receive props: `{ workspaceId: string }`

**CommanderPanel.tsx:**
The commander's view with pending decisions and command overview.

1. Sections:
   - **Command Overview:** Workspace name, type, classification badge, member count, child workspace count (fetch from hierarchy).
   - **Pending Decisions:** List of pending DAO proposals for this workspace (fetch from `workspaceService.listActivity(workspaceId, { types: ['invite_pending_approval'] })` — or placeholder if proposal API not wired yet). Show invite approvals needed.
   - **Quick Actions:** Buttons for "Invite Member", "Create Sub-Workspace", "View Members". These link to respective workspace routes/modals.
   - **Recent Activity:** Last 5 activity items (all types visible to commander).

2. Layout: 2-column grid on desktop (overview + pending left, quick actions + recent right). Single column on mobile.
3. Tailwind CSS. Card-based layout with subtle borders.

**StaffPanel.tsx:**
Generic staff panel that adapts to the specific staff role.

1. Props: `{ workspaceId: string; staffRole?: string }` — staffRole is the military label.
2. Sections:
   - **Role Header:** "S2 - Intelligence" / "S3 - Operations" / etc. with role-specific description.
   - **Staff Section Content:** Placeholder content area with a message like "S2 workspace content will appear here as missions and exercises are created."
   - **Recent Activity:** Last 5 activity items (filtered: joins/departures visible).
   - **Quick Links:** Links to missions/exercises within the workspace.

3. Role descriptions (hardcoded map):
   - s1: Personnel & Admin
   - s2: Intelligence
   - s3: Operations
   - s4: Logistics
   - s5: Plans
   - s6: Communications
   - s7: Training
   - s8: Finance
   - s9: Civil Affairs
   - xo: Executive Officer

4. Tailwind CSS. Single column layout with cards.

**ObserverPanel.tsx:**
Read-only view for observers.

1. Sections:
   - **Workspace Info:** Name, type, classification, member count (read-only card).
   - **Mission List:** List of missions in this workspace (fetch from activity or placeholder). Read-only — no action buttons.
   - **Activity Summary:** Condensed activity feed showing only summary-level items.

2. No action buttons. No edit capabilities.
3. Tailwind CSS. Clean, minimal layout.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit frontend/src/components/workspace/CommanderPanel.tsx frontend/src/components/workspace/StaffPanel.tsx frontend/src/components/workspace/ObserverPanel.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Three role-specific panels created: CommanderPanel (decisions, overview, quick actions), StaffPanel (generic with role label, staff content, activity), ObserverPanel (read-only info, missions, summary). All compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create WorkspaceDashboard with role-adaptive panel selection</name>
  <files>frontend/src/components/workspace/WorkspaceDashboard.tsx</files>
  <action>
Create the workspace dashboard that renders shared base elements + role-specific panel.

1. Uses `useWorkspace()` to get `activeWorkspace`, `userRoleInActive`, `activeWorkspaceId`.
2. Uses `useParams()` to get `workspaceId` from route. If workspaceId differs from activeWorkspaceId, call `setActiveWorkspace(workspaceId)`.

3. Role-to-panel mapping using `useMemo`:
```typescript
const INTELLIGENCE_ROLES = ['s2'];
const COMMANDER_ROLES = ['commander', 'xo'];

const RolePanel = useMemo(() => {
  if (!userRoleInActive) return ObserverPanel;
  if (COMMANDER_ROLES.includes(userRoleInActive)) return CommanderPanel;
  if (INTELLIGENCE_ROLES.includes(userRoleInActive)) return S2IntelPanel; // future, use StaffPanel for now
  return StaffPanel;
}, [userRoleInActive]);
```

4. Layout (three-area grid):
   - **Header:** Workspace name, type badge, classification badge, member count. Breadcrumb if child workspace (Org > Unit > Team).
   - **Main content area:** The selected `<RolePanel workspaceId={activeWorkspaceId} staffRole={userRoleInActive} />`
   - **Sidebar (optional on desktop):** Quick links to Members, Invites, Settings.

5. Loading state: Show skeleton/spinner while activeWorkspace is loading.
6. No workspace selected: Show a prompt to select or create a workspace.
7. Not a member: Show access denied message.

8. Update the App.tsx route for `/workspace/:workspaceId` to render this WorkspaceDashboard (replace the placeholder from 19-05).

9. Tailwind CSS. Responsive layout.

CRITICAL: This is ONE component that renders different panels — NOT separate routes per role. The user explicitly decided against separate pages per role.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit frontend/src/components/workspace/WorkspaceDashboard.tsx 2>&1 | head -20</automated>
  </verify>
  <done>WorkspaceDashboard conditionally renders CommanderPanel/StaffPanel/ObserverPanel based on user's role. Single component, no separate routes per role. Route updated in App.tsx.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all four component files
- Frontend build succeeds
- WorkspaceDashboard renders role-specific panel based on userRoleInActive
- No separate routes per role
</verification>

<success_criteria>
- WorkspaceDashboard renders shared header + role-specific panel
- CommanderPanel: pending decisions, command overview, quick actions
- StaffPanel: role-labeled content with staff section description
- ObserverPanel: read-only workspace info and activity summary
- Single component adapts to role (no separate pages)
</success_criteria>

<output>
After completion, create `.planning/phases/19-workspace-membership-and-invite-system/19-07-SUMMARY.md`
</output>
