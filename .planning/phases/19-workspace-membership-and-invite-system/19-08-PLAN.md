---
phase: 19-workspace-membership-and-invite-system
plan: 08
type: execute
wave: 8
depends_on: [19-07]
files_modified:
  - frontend/src/components/workspace/OrgTree.tsx
  - frontend/src/components/workspace/MemberDirectory.tsx
  - frontend/src/components/workspace/CompartmentManager.tsx
  - frontend/package.json
  - backend/src/api/workspaces.ts
  - backend/src/workspace/workspace-compartment-store.ts
  - frontend/src/lib/workspace-service.ts
autonomous: true
requirements: [WS-ONBOARDING, WS-HIERARCHY, WS-COMPARTMENTS]
must_haves:
  truths:
    - "Interactive org tree shows workspace hierarchy with member counts"
    - "User's position in the hierarchy is highlighted"
    - "Member directory filters by compartment (need-to-know)"
    - "Members only see others in their compartment or who they need to know about"
    - "Compartments can be created and members assigned to them"
  artifacts:
    - path: "frontend/src/components/workspace/OrgTree.tsx"
      provides: "Interactive hierarchy visualization with react-d3-tree"
      exports: ["OrgTree"]
    - path: "frontend/src/components/workspace/MemberDirectory.tsx"
      provides: "Compartment-filtered member list"
      exports: ["MemberDirectory"]
    - path: "frontend/src/components/workspace/CompartmentManager.tsx"
      provides: "Compartment CRUD and member assignment"
      exports: ["CompartmentManager"]
  key_links:
    - from: "frontend/src/components/workspace/OrgTree.tsx"
      to: "frontend/src/lib/workspace-service.ts"
      via: "workspaceService.getHierarchy"
      pattern: "workspaceService.getHierarchy"
    - from: "frontend/src/components/workspace/MemberDirectory.tsx"
      to: "frontend/src/lib/workspace-service.ts"
      via: "workspaceService.listMembers"
      pattern: "workspaceService.listMembers"
---

<objective>
Create the org tree visualization, compartment-filtered member directory, and compartment manager. These provide hierarchy visibility and need-to-know access control.

Purpose: The org tree is a key onboarding element showing users their position in the hierarchy. The member directory implements compartmentalization — members only see others they need to know about. Compartments enforce need-to-know beyond classification.
Output: OrgTree (react-d3-tree), MemberDirectory, CompartmentManager components. react-d3-tree dependency installed.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-workspace-membership-and-invite-system/19-CONTEXT.md
@.planning/phases/19-workspace-membership-and-invite-system/19-07-SUMMARY.md

@frontend/src/lib/workspace-service.ts
@frontend/src/context/WorkspaceContext.tsx
@.planning/phases/19-workspace-membership-and-invite-system/19-RESEARCH.md (react-d3-tree code example)

<interfaces>
From frontend/src/lib/workspace-service.ts:
```typescript
export interface HierarchyNode {
  id: string; name: string; workspaceType: string; memberCount: number;
  children?: HierarchyNode[];
}
getHierarchy(workspaceId): Promise<HierarchyNode[]>
listMembers(workspaceId): Promise<WorkspaceMemberDetail[]>
```

From react-d3-tree v3 API:
```typescript
import Tree from 'react-d3-tree';
// Props: data (tree object), orientation, pathFunc, renderCustomNodeElement, collapsible
// Custom node: ({ nodeDatum }) => <g>...</g>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-d3-tree and create OrgTree component</name>
  <files>frontend/package.json, frontend/src/components/workspace/OrgTree.tsx</files>
  <action>
**Install dependency:**
```bash
cd frontend && pnpm add react-d3-tree
```

**OrgTree.tsx:**

Create an interactive org tree visualization.

1. Props: `{ rootWorkspaceId: string; currentUserWorkspaceId?: string }`

2. Fetch hierarchy: `workspaceService.getHierarchy(rootWorkspaceId)` on mount.

3. Transform API data to react-d3-tree format:
```typescript
interface TreeNode {
  name: string;
  attributes?: Record<string, string>;
  children?: TreeNode[];
}

function transformToTreeData(nodes: HierarchyNode[]): TreeNode {
  // Recursive: map HierarchyNode -> TreeNode
  // attributes: { type: node.workspaceType, members: String(node.memberCount) }
}
```

4. Custom node renderer:
   - Show workspace name in bold.
   - Show type badge (Org/Unit/Team) with color coding.
   - Show member count.
   - Highlight the node matching `currentUserWorkspaceId` with a distinct border/background color.
   - Classification badge on each node.
   - Click handler: navigate to that workspace.

5. Tree configuration:
   - `orientation="vertical"` — top-to-bottom hierarchy.
   - `pathFunc="step"` — angled connectors.
   - `collapsible={true}` — allow collapsing subtrees.
   - Center the tree on mount.
   - Zoom and pan enabled (default in react-d3-tree).

6. Container: `<div style={{ height: 400, width: '100%' }}>` — responsive width, fixed height.

7. Empty state: If no hierarchy data, show "No workspace hierarchy to display."

8. Loading state: Show skeleton while fetching.

9. Tailwind CSS for the container. SVG styling via inline styles or CSS classes on the custom node elements.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr/frontend && pnpm list react-d3-tree 2>&1 && cd /home/vitalpointai/projects/ssr && npx tsc --noEmit frontend/src/components/workspace/OrgTree.tsx 2>&1 | head -20</automated>
  </verify>
  <done>react-d3-tree installed. OrgTree renders interactive hierarchy with custom nodes showing name, type, member count, classification. Current user's workspace highlighted. Zoom/pan/collapse supported.</done>
</task>

<task type="auto">
  <name>Task 2: Create MemberDirectory and CompartmentManager</name>
  <files>frontend/src/components/workspace/MemberDirectory.tsx, frontend/src/components/workspace/CompartmentManager.tsx</files>
  <action>
**MemberDirectory.tsx:**

Compartment-filtered member list for workspace members.

1. Props: `{ workspaceId: string }`
2. Fetch members, roles, and compartments on mount.
3. Get current user's compartments (from API — need to add `listCompartments` and `getMyCompartments` to workspace-service.ts if not present; if not yet available, use member list filtered by basic visibility rules).

4. Filtering logic:
   - Commanders and XOs see ALL members.
   - Staff members see: members in their own compartment(s) + members with no compartment restriction.
   - Observers see: only members explicitly flagged as visible to observers.
   - If compartment system not fully wired yet (API may not exist in 19-03), show all active members with a "Compartment filtering coming soon" note and filter by role-based visibility rules only.

5. Display:
   - Card or list layout for each member.
   - Show: Name/DID, military role label, DAO role, status badge (active/suspended), joined date.
   - Sort: commander/xo first, then staff alphabetically, then observers.
   - Search/filter input at top.
   - Role filter dropdown.
   - Active/suspended toggle filter.

6. Responsive: grid on desktop, stack on mobile.
7. Tailwind CSS.

**CompartmentManager.tsx:**

Compartment CRUD and member assignment (for admins).

1. Props: `{ workspaceId: string }`
2. Show if user has 'manage_workspace' or 'manage_members' permission.

3. Sections:
   - **Compartment List:** Existing compartments with name, description, member count.
   - **Create Compartment:** Form with name (e.g., 'SIGINT', 'HUMINT', 'OP-PLAN-X'), description.
   - **Assign Members:** For each compartment, show a member picker to add/remove members.

4. API calls: These will need backend compartment endpoints. Add them to workspace-service.ts:
   - `listCompartments(workspaceId): Promise<WorkspaceCompartment[]>`
   - `createCompartment(workspaceId, name, description): Promise<WorkspaceCompartment>`
   - `assignMemberToCompartment(workspaceId, memberId, compartmentId): Promise<void>`
   - `removeMemberFromCompartment(workspaceId, memberId, compartmentId): Promise<void>`

5. Also add these endpoints to the backend API (backend/src/api/workspaces.ts):
   - `GET /:id/compartments` — list compartments
   - `POST /:id/compartments` — create compartment
   - `POST /:id/compartments/:cid/members` — assign member
   - `DELETE /:id/compartments/:cid/members/:mid` — remove member

6. Backend store: Create a new `backend/src/workspace/workspace-compartment-store.ts` singleton store class (following the same ensureInitialized pattern) for compartment CRUD. This store manages `workspace_compartments` and `workspace_member_compartments` tables from the research schema. Methods: `createCompartment`, `listCompartments`, `deleteCompartment`, `assignMember`, `removeMember`, `listMembersInCompartment`, `listCompartmentsForMember`. The API route endpoints (added to `backend/src/api/workspaces.ts`) call this store. Add `backend/src/workspace/workspace-compartment-store.ts` to files_modified.

7. Tailwind CSS.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit frontend/src/components/workspace/MemberDirectory.tsx frontend/src/components/workspace/CompartmentManager.tsx 2>&1 | head -20 && npx tsc --noEmit backend/src/api/workspaces.ts 2>&1 | head -20</automated>
  </verify>
  <done>MemberDirectory shows compartment-filtered member list with role-based visibility. CompartmentManager allows creating compartments and assigning members. Backend compartment endpoints added. All compile cleanly.</done>
</task>

</tasks>

<verification>
- react-d3-tree is in frontend/package.json dependencies
- `npx tsc --noEmit` passes for all component files
- Frontend build succeeds with react-d3-tree
- OrgTree renders hierarchy with custom nodes
- MemberDirectory filters by compartment/role
- Compartment CRUD endpoints exist in backend API
</verification>

<success_criteria>
- OrgTree: interactive hierarchy visualization with react-d3-tree, custom nodes, user position highlighted
- MemberDirectory: compartment-filtered member list with role-based visibility rules
- CompartmentManager: compartment CRUD and member assignment for admins
- react-d3-tree installed and working
- Backend compartment endpoints added
</success_criteria>

<output>
After completion, create `.planning/phases/19-workspace-membership-and-invite-system/19-08-SUMMARY.md`
</output>
