---
phase: 19-workspace-membership-and-invite-system
plan: 10
type: execute
wave: 9
depends_on: [19-08, 19-09]
files_modified:
  - backend/src/api/workspaces.ts
  - frontend/src/components/workspace/WorkspaceDashboard.tsx
  - frontend/src/App.tsx
autonomous: false
requirements: [WS-MODEL, WS-MEMBERSHIP, WS-INVITES, WS-HIERARCHY, WS-ROLES, WS-ONBOARDING, WS-ACTIVITY-LOG, WS-NOTIFICATIONS, WS-CLEARANCE, WS-COMPARTMENTS]
must_haves:
  truths:
    - "Complete workspace lifecycle works: create org -> create unit under org -> invite member -> accept -> view dashboard"
    - "Workspace switching updates sidebar and dashboard correctly"
    - "Role-specific dashboard panels render for commander/staff/observer"
    - "Org tree shows correct hierarchy with member counts"
    - "Notification badges appear for non-active workspaces with unread activity"
    - "Backend and frontend both build cleanly"
    - "All existing tests still pass"
  artifacts:
    - path: "backend/src/api/workspaces.ts"
      provides: "Complete workspace API with all endpoints"
    - path: "frontend/src/components/workspace/WorkspaceDashboard.tsx"
      provides: "Fully integrated dashboard with OrgTree, ActivityFeed, role panels"
    - path: "frontend/src/App.tsx"
      provides: "All workspace routes registered and working"
  key_links:
    - from: "frontend/src/components/workspace/WorkspaceDashboard.tsx"
      to: "frontend/src/components/workspace/OrgTree.tsx"
      via: "OrgTree rendered in dashboard"
      pattern: "OrgTree"
    - from: "frontend/src/components/workspace/WorkspaceDashboard.tsx"
      to: "frontend/src/components/workspace/ActivityFeed.tsx"
      via: "ActivityFeed rendered in dashboard"
      pattern: "ActivityFeed"
---

<objective>
Wire all workspace components together into the dashboard, add missing routes, and verify the full system end-to-end. This is the integration and verification plan.

Purpose: All individual pieces (stores, API, context, UI components) exist. This plan wires them together in the dashboard and verifies the full workspace lifecycle works.
Output: Fully integrated workspace system. Both builds clean. Tests pass.
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-workspace-membership-and-invite-system/19-CONTEXT.md
@.planning/phases/19-workspace-membership-and-invite-system/19-08-SUMMARY.md
@.planning/phases/19-workspace-membership-and-invite-system/19-09-SUMMARY.md

@frontend/src/components/workspace/WorkspaceDashboard.tsx
@frontend/src/components/workspace/OrgTree.tsx
@frontend/src/components/workspace/ActivityFeed.tsx
@frontend/src/components/workspace/MemberDirectory.tsx
@frontend/src/components/workspace/WorkspaceMemberManager.tsx
@frontend/src/components/workspace/WorkspaceInviteModal.tsx
@frontend/src/App.tsx
@backend/src/api/workspaces.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire dashboard components and add remaining routes</name>
  <files>frontend/src/components/workspace/WorkspaceDashboard.tsx, frontend/src/App.tsx</files>
  <action>
**Update WorkspaceDashboard.tsx:**

Integrate all workspace components into the dashboard:

1. Import OrgTree, ActivityFeed, MemberDirectory, WorkspaceInviteModal, WorkspaceMemberManager.

2. Dashboard layout (full integration):
   ```
   <div className="workspace-dashboard grid grid-cols-1 lg:grid-cols-3 gap-4 p-4">
     {/* Header - full width */}
     <header className="col-span-full">
       <WorkspaceHeader />  {/* name, type, classification, breadcrumb */}
     </header>

     {/* Left column - 2/3 width on desktop */}
     <main className="lg:col-span-2 space-y-4">
       <RolePanel workspaceId={activeWorkspaceId} staffRole={userRoleInActive} />
       <ActivityFeed workspaceId={activeWorkspaceId} userRole={userRoleInActive} />
     </main>

     {/* Right sidebar - 1/3 width on desktop */}
     <aside className="space-y-4">
       <OrgTree rootWorkspaceId={rootWorkspaceId} currentUserWorkspaceId={activeWorkspaceId} />
       <QuickActions />  {/* Invite, Members, Settings buttons */}
     </aside>
   </div>
   ```

3. Quick actions open modals:
   - "Invite Member" → opens WorkspaceInviteModal
   - "Manage Members" → navigates to `/workspace/:id/members`
   - "Settings" → navigates to `/workspace/:id/settings` (placeholder)

4. Root workspace ID derivation: traverse parent chain from active workspace to find the org-level root (for OrgTree). If activeWorkspace.parentWorkspaceId is null, the active workspace IS the root.

**Update App.tsx:**

Add remaining workspace sub-routes:
- `/workspace/:workspaceId/members` — renders WorkspaceMemberManager
- `/workspace/:workspaceId/directory` — renders MemberDirectory
- `/workspace/:workspaceId/settings` — placeholder settings page

Ensure all workspace routes are nested under an authenticated route guard.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/ssr && npx tsc --noEmit frontend/src/components/workspace/WorkspaceDashboard.tsx frontend/src/App.tsx 2>&1 | head -20</automated>
  </verify>
  <done>WorkspaceDashboard integrates OrgTree, ActivityFeed, and role panels in a responsive grid. All workspace sub-routes registered in App.tsx.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full workspace system</name>
  <what-built>
Complete workspace membership and invite system:
- Backend: 5 store classes (workspace, member, invite, activity, role), REST API with 18+ endpoints, PostgreSQL tables with indexes
- Frontend: WorkspaceContext with polling, WorkspaceSwitcher sidebar, CreateWorkspaceWizard, WorkspaceInviteModal, InviteAcceptPage, WorkspaceDashboard with role-adaptive panels (Commander/Staff/Observer), OrgTree hierarchy visualization, MemberDirectory with compartment filtering, ActivityFeed with role-based visibility, NotificationBadge
  </what-built>
  <how-to-verify>
1. **Build verification:**
   - Run `cd backend && npm run build` — should complete with no errors
   - Run `cd frontend && npm run build` — should complete with no errors
   - Run `cd backend && npm test` — all existing tests should pass

2. **Visual verification (start the app):**
   - Run `docker compose up` or start backend + frontend dev servers
   - Login as an authenticated user
   - Verify WorkspaceSwitcher appears in the left sidebar
   - Click "+" to create a new Organization workspace
   - Walk through the 3-step wizard (name, classification, confirm)
   - Verify the new workspace appears in the sidebar
   - Click the workspace to see the dashboard
   - Verify role-appropriate panel renders (Commander panel for creator)
   - Verify OrgTree shows the workspace
   - Verify ActivityFeed shows "workspace_created" event

3. **Invite flow:**
   - Click "Invite Member" in workspace dashboard
   - Create an invite link with a role
   - Copy the link
   - Open in incognito or different user session
   - Verify invite accept page renders

4. **Hierarchy:**
   - Create a Unit workspace under the Organization
   - Verify OrgTree updates to show parent-child relationship
   - Verify Unit appears as child in hierarchy
  </how-to-verify>
  <resume-signal>Type "approved" if the workspace system works as described, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
- `cd backend && npm run build` — clean build
- `cd frontend && npm run build` — clean build
- `cd backend && npm test` — all tests pass
- Workspace creation lifecycle works visually
- Invite flow works end-to-end
- Hierarchy visualization correct
</verification>

<success_criteria>
- Both backend and frontend build cleanly
- All existing tests pass
- Workspace create -> invite -> accept -> dashboard lifecycle works
- Role-adaptive dashboard renders correctly for different roles
- OrgTree shows correct hierarchy
- Notification badges show cross-workspace activity
- Workspace switching works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/19-workspace-membership-and-invite-system/19-10-SUMMARY.md`
</output>
